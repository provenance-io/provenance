package simulation

import "github.com/cosmos/cosmos-sdk/types/module"

// RandomizedGenState generates a random GenesisState for the asset module.
func RandomizedGenState(simState *module.SimulationState) {
	_ = simState
	// TODO randomize genesis state
}
