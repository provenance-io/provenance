syntax = "proto3";

package provenance.trigger.v1;

// Intentional formatting issue below: this line is too long and should fail the proto-format check
message TestTriggerMessage { string field_one = 1; string field_two = 2; string field_three = 3; string field_four = 4; string field_five = 5; }
