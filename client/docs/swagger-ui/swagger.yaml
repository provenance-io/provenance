swagger: '2.0'
info:
  title: Provenance Blockchain
  description: Provenance Blockchain Protocol API
  version: 1.19.0
paths:
  /provenance/asset/v1/asset:
    get:
      summary: Assets gets all assets for a given address and class.
      operationId: Assets
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              assets:
                type: array
                items:
                  type: object
                  properties:
                    class_id:
                      type: string
                      description: >-
                        class_id associated with the asset, similar to the
                        contract address of ERC721.
                    id:
                      type: string
                      description: id is a unique identifier of the asset.
                    uri:
                      type: string
                      description: uri for the asset metadata stored off chain.
                    uri_hash:
                      type: string
                      description: uri_hash is a hash of the document pointed by uri.
                    data:
                      type: string
                      description: data is an app specific json data of the asset.
                  description: Asset defines the asset. Similar to Nft.
                description: assets are the assets returned from the query.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAssetsResponse is the response type for the Query/Assets RPC
              methods.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: class_id
          description: class_id associated with the asset.
          in: query
          required: false
          type: string
        - name: owner
          description: owner is the owner address of the asset.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Asset
  /provenance/asset/v1/asset/{class_id}/{id}:
    get:
      summary: Asset gets a specified asset by its class ID and asset ID.
      operationId: Asset
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              asset:
                description: asset is the asset returned from the query.
                type: object
                properties:
                  class_id:
                    type: string
                    description: >-
                      class_id associated with the asset, similar to the
                      contract address of ERC721.
                  id:
                    type: string
                    description: id is a unique identifier of the asset.
                  uri:
                    type: string
                    description: uri for the asset metadata stored off chain.
                  uri_hash:
                    type: string
                    description: uri_hash is a hash of the document pointed by uri.
                  data:
                    type: string
                    description: data is an app specific json data of the asset.
            description: >-
              QueryAssetResponse is the response type for the Query/Asset RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: class_id
          description: class_id associated with the asset.
          in: path
          required: true
          type: string
        - name: id
          description: id is a unique identifier of the asset.
          in: path
          required: true
          type: string
      tags:
        - Asset
  /provenance/asset/v1/class:
    get:
      summary: AssetClasses gets all asset classes.
      operationId: AssetClasses
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              asset_classes:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      description: >-
                        id defines the unique identifier of the asset
                        classification, similar to the contract address of
                        ERC721.
                    name:
                      type: string
                      description: >-
                        name defines the human-readable name of the asset
                        classification.
                    symbol:
                      type: string
                      description: symbol is an abbreviated name for asset classification.
                    description:
                      type: string
                      description: >-
                        description is a brief description of asset
                        classification.
                    uri:
                      type: string
                      description: >-
                        uri for the class metadata stored off chain. It can
                        define schema for Class and asset `Data` attributes.
                    uri_hash:
                      type: string
                      description: uri_hash is a hash of the document pointed by uri.
                    data:
                      type: string
                      description: data is the app specific json schema of the asset class.
                  description: >-
                    AssetClass defines the class type data for an Asset. Similar
                    to NftClass.
                description: asset_classes are the asset classes returned from the query.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAssetClassesResponse is the response type for the
              Query/Classes RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Asset
  /provenance/asset/v1/class/{id}:
    get:
      summary: AssetClass gets a specific asset class by its ID.
      operationId: AssetClass
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              asset_class:
                description: asset_class is the asset class returned from the query.
                type: object
                properties:
                  id:
                    type: string
                    description: >-
                      id defines the unique identifier of the asset
                      classification, similar to the contract address of ERC721.
                  name:
                    type: string
                    description: >-
                      name defines the human-readable name of the asset
                      classification.
                  symbol:
                    type: string
                    description: symbol is an abbreviated name for asset classification.
                  description:
                    type: string
                    description: >-
                      description is a brief description of asset
                      classification.
                  uri:
                    type: string
                    description: >-
                      uri for the class metadata stored off chain. It can define
                      schema for Class and asset `Data` attributes.
                  uri_hash:
                    type: string
                    description: uri_hash is a hash of the document pointed by uri.
                  data:
                    type: string
                    description: data is the app specific json schema of the asset class.
            description: >-
              QueryAssetClassResponse is the response type for the Query/Class
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          description: class_id associated with the asset.
          in: path
          required: true
          type: string
      tags:
        - Asset
  /provenance/attribute/v1/accountdata/{account}:
    get:
      summary: AccountData returns the accountdata for a specified account.
      operationId: AccountData
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              value:
                type: string
                description: >-
                  value is the accountdata attribute value for the requested
                  account.
            description: >-
              QueryAccountDataResponse is the response type for the
              Query/AccountData method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: account
          description: account is the bech32 address of the account to get the data for
          in: path
          required: true
          type: string
      tags:
        - Attribute
  /provenance/attribute/v1/accounts/{attribute_name}:
    get:
      summary: AttributeAccounts queries accounts on a given attribute name
      operationId: AttributeAccounts
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              accounts:
                type: array
                items:
                  type: string
                title: list of account addresses that have attributes of request name
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAttributeAccountsResponse is the response type for the
              Query/AttributeAccounts method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: attribute_name
          description: name is the attribute name to query for
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Attribute
  /provenance/attribute/v1/attribute/{account}/scan/{suffix}:
    get:
      summary: >-
        Scan queries attributes on a given account (address) for any that match
        the provided suffix
      operationId: Scan
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              account:
                type: string
                title: >-
                  a string containing the address of the account the attributes
                  are assigned to=
              attributes:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      description: The attribute name.
                    value:
                      type: string
                      format: byte
                      description: The attribute value.
                    attribute_type:
                      description: The attribute value type.
                      type: string
                      enum:
                        - ATTRIBUTE_TYPE_UNSPECIFIED
                        - ATTRIBUTE_TYPE_UUID
                        - ATTRIBUTE_TYPE_JSON
                        - ATTRIBUTE_TYPE_STRING
                        - ATTRIBUTE_TYPE_URI
                        - ATTRIBUTE_TYPE_INT
                        - ATTRIBUTE_TYPE_FLOAT
                        - ATTRIBUTE_TYPE_PROTO
                        - ATTRIBUTE_TYPE_BYTES
                      default: ATTRIBUTE_TYPE_UNSPECIFIED
                      title: >-
                        AttributeType defines the type of the data stored in the
                        attribute value
                    address:
                      type: string
                      title: The address the attribute is bound to
                    expiration_date:
                      type: string
                      format: date-time
                      description: Time that an attribute will expire.
                    concrete_type:
                      type: string
                      description: >-
                        concrete_type defines the specific type of data for
                        PROTO or JSON attribute.

                        Optional. Max 200 characters.
                  title: >-
                    Attribute holds a typed key/value structure for data
                    associated with an account
                title: a list of attribute values
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: QueryScanResponse is the response type for the Query/Scan method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: account
          description: account defines the address to query for.
          in: path
          required: true
          type: string
        - name: suffix
          description: >-
            name defines the partial attribute name to search for base on names
            being in RDNS format.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Attribute
  /provenance/attribute/v1/attribute/{account}/{name}:
    get:
      summary: >-
        Attribute queries attributes on a given account (address) for one (or
        more) with the given name
      operationId: Attribute
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              account:
                type: string
                description: >-
                  a string containing the address of the account the attributes
                  are assigned to.
              attributes:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      description: The attribute name.
                    value:
                      type: string
                      format: byte
                      description: The attribute value.
                    attribute_type:
                      description: The attribute value type.
                      type: string
                      enum:
                        - ATTRIBUTE_TYPE_UNSPECIFIED
                        - ATTRIBUTE_TYPE_UUID
                        - ATTRIBUTE_TYPE_JSON
                        - ATTRIBUTE_TYPE_STRING
                        - ATTRIBUTE_TYPE_URI
                        - ATTRIBUTE_TYPE_INT
                        - ATTRIBUTE_TYPE_FLOAT
                        - ATTRIBUTE_TYPE_PROTO
                        - ATTRIBUTE_TYPE_BYTES
                      default: ATTRIBUTE_TYPE_UNSPECIFIED
                      title: >-
                        AttributeType defines the type of the data stored in the
                        attribute value
                    address:
                      type: string
                      title: The address the attribute is bound to
                    expiration_date:
                      type: string
                      format: date-time
                      description: Time that an attribute will expire.
                    concrete_type:
                      type: string
                      description: >-
                        concrete_type defines the specific type of data for
                        PROTO or JSON attribute.

                        Optional. Max 200 characters.
                  title: >-
                    Attribute holds a typed key/value structure for data
                    associated with an account
                title: a list of attribute values
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAttributeResponse is the response type for the
              Query/Attribute method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: account
          description: account defines the address to query for.
          in: path
          required: true
          type: string
        - name: name
          description: name is the attribute name to query for
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Attribute
  /provenance/attribute/v1/attributes/{account}:
    get:
      summary: >-
        Attributes queries attributes on a given account (address) for any
        defined attributes
      operationId: Attributes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              account:
                type: string
                title: >-
                  a string containing the address of the account the attributes
                  are assigned to=
              attributes:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      description: The attribute name.
                    value:
                      type: string
                      format: byte
                      description: The attribute value.
                    attribute_type:
                      description: The attribute value type.
                      type: string
                      enum:
                        - ATTRIBUTE_TYPE_UNSPECIFIED
                        - ATTRIBUTE_TYPE_UUID
                        - ATTRIBUTE_TYPE_JSON
                        - ATTRIBUTE_TYPE_STRING
                        - ATTRIBUTE_TYPE_URI
                        - ATTRIBUTE_TYPE_INT
                        - ATTRIBUTE_TYPE_FLOAT
                        - ATTRIBUTE_TYPE_PROTO
                        - ATTRIBUTE_TYPE_BYTES
                      default: ATTRIBUTE_TYPE_UNSPECIFIED
                      title: >-
                        AttributeType defines the type of the data stored in the
                        attribute value
                    address:
                      type: string
                      title: The address the attribute is bound to
                    expiration_date:
                      type: string
                      format: date-time
                      description: Time that an attribute will expire.
                    concrete_type:
                      type: string
                      description: >-
                        concrete_type defines the specific type of data for
                        PROTO or JSON attribute.

                        Optional. Max 200 characters.
                  title: >-
                    Attribute holds a typed key/value structure for data
                    associated with an account
                title: a list of attribute values
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAttributesResponse is the response type for the
              Query/Attributes method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: account
          description: account defines the address to query for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Attribute
  /provenance/attribute/v1/params:
    get:
      summary: Params queries params of the attribute module.
      operationId: AttributeParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  max_value_length:
                    type: integer
                    format: int64
                    title: maximum length of data to allow in an attribute value
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Attribute
  /provenance/exchange/v1/commitments:
    get:
      summary: >-
        GetAllCommitments gets all fund committed to any market from any
        account.
      operationId: GetAllCommitments
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              commitments:
                type: array
                items:
                  type: object
                  properties:
                    account:
                      type: string
                      description: >-
                        account is the bech32 address string with the committed
                        funds.
                    market_id:
                      type: integer
                      format: int64
                      description: >-
                        market_id is the numeric identifier of the market the
                        funds are committed to.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: >-
                        amount is the funds that have been committed by the
                        account to the market.
                  description: Commitment contains information on committed funds.
                description: commitments is the requested commitment information.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetAllCommitmentsResponse is a response message for the
              GetAllCommitments query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/commitments/account/{account}:
    get:
      summary: >-
        GetAccountCommitments gets all the funds in an account that are
        committed to any market.

        Optionally, you can filter the results for a specific denomination using
        the `denom` query parameter.
      operationId: GetAccountCommitments
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              commitments:
                type: array
                items:
                  type: object
                  properties:
                    market_id:
                      type: integer
                      format: int64
                      description: >-
                        market_id is the numeric identifier the amount has been
                        committed to.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: amount is the funds associated with the address.
                  description: MarketAmount associates a market with a coins amount.
                description: >-
                  commitments is the amounts committed from the account to the
                  any market.
            description: >-
              QueryGetAccountCommitmentsResponse is a response message for the
              GetAccountCommitments query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: account
          description: >-
            account is the bech32 address string of the account with the
            commitments.
          in: path
          required: true
          type: string
        - name: denom
          description: >-
            denom is an optional denom that, if provided, limits the results to
            just that denom.
          in: query
          required: false
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/commitments/market/{market_id}:
    get:
      summary: >-
        GetMarketCommitments gets all the funds committed to a market from any
        account.
      operationId: GetMarketCommitments
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              commitments:
                type: array
                items:
                  type: object
                  properties:
                    account:
                      type: string
                      description: >-
                        account is the bech32 address string of the account
                        associated with the amount.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: amount is the funds associated with the address.
                  description: AccountAmount associates an account with a coins amount.
                description: >-
                  commitments is the amounts committed to the market from any
                  account.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetMarketCommitmentsResponse is a response message for the
              GetMarketCommitments query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: >-
            market_id is the numeric identifier of the market with the
            commitment.
          in: path
          required: true
          type: integer
          format: int64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/fees/commitment_settlement:
    get:
      summary: >-
        CommitmentSettlementFeeCalc calculates the fees a market will pay for a
        commitment settlement using current NAVs.
      operationId: CommitmentSettlementFeeCalc
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              exchange_fees:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  exchange_fees is the total that the exchange would currently
                  pay for the provided settlement.
              input_total:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  input_total is the sum of all the inputs in the provided
                  settlement.
              converted_total:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  converted_total is the input_total converted to a single
                  intermediary denom or left as the fee denom.
              conversion_navs:
                type: array
                items:
                  type: object
                  properties:
                    assets:
                      description: >-
                        assets is the volume and denom that has been bought or
                        sold.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    price:
                      description: price is what was paid for the assets.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                  description: >-
                    NetAssetPrice is an association of assets and price used to
                    record the value of things.

                    It is related to the NetAssetValue message from the x/marker
                    module, and is therefore often referred to as "a NAV".
                description: >-
                  conversion_navs are the NAVs used to convert the input_total
                  to the converted_total.
              to_fee_nav:
                description: >-
                  to_fee_nav is the NAV used to convert the converted_total into
                  the fee denom.
                type: object
                properties:
                  assets:
                    description: >-
                      assets is the volume and denom that has been bought or
                      sold.
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                  price:
                    description: price is what was paid for the assets.
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
            description: >-
              QueryCommitmentSettlementFeeCalcResponse is a response message for
              the CommitmentSettlementFeeCalc query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: settlement.admin
          description: >-
            admin is the account with "settle" permission requesting this
            settlement.
          in: query
          required: false
          type: string
        - name: settlement.market_id
          description: >-
            market_id is the numerical identifier of the market requesting this
            settlement.
          in: query
          required: false
          type: integer
          format: int64
        - name: settlement.event_tag
          description: >-
            event_tag is a string that is included in the
            funds-committed/released events. Max length is 100 characters.
          in: query
          required: false
          type: string
        - name: include_breakdown_fields
          description: >-
            include_breakdown_fields controls the fields that are populated in
            the response.

            If false, only the exchange_fees field is populated.

            If true, all of the fields are populated as possible.

            If the settlement does not have any inputs, this field defaults to
            true.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/fees/order:
    get:
      summary: >-
        OrderFeeCalc calculates the fees that will be associated with the
        provided order.
      operationId: OrderFeeCalc
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              creation_fee_options:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  creation_fee_options are the order creation flat fee options
                  available for creating the provided order.

                  If it's empty, no order creation fee is required.

                  When creating the order, you should include exactly one of
                  these.
              settlement_flat_fee_options:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  settlement_flat_fee_options are the settlement flat fee
                  options available for the provided order.

                  If it's empty, no settlement flat fee is required.

                  When creating an order, you should include exactly one of
                  these in the settlement fees field.
              settlement_ratio_fee_options:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  settlement_ratio_fee_options are the settlement ratio fee
                  options available for the provided order.

                  If it's empty, no settlement ratio fee is required.


                  If the provided order was a bid order, you should include
                  exactly one of these in the settlement fees field.

                  If the flat and ratio options you've chose have the same
                  denom, a single entry should be included with their sum.


                  If the provided order was an ask order, these are purely
                  informational and represent how much will be removed

                  from your price if it settles at that price. If it settles for
                  more, the actual amount will probably be larger.
            description: >-
              QueryOrderFeeCalcResponse is a response message for the
              OrderFeeCalc query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: ask_order.market_id
          description: market_id identifies the market that this order belongs to.
          in: query
          required: false
          type: integer
          format: int64
        - name: ask_order.seller
          description: >-
            seller is the address of the account that owns this order and has
            the assets to sell.
          in: query
          required: false
          type: string
        - name: ask_order.assets.denom
          in: query
          required: false
          type: string
        - name: ask_order.assets.amount
          in: query
          required: false
          type: string
        - name: ask_order.price.denom
          in: query
          required: false
          type: string
        - name: ask_order.price.amount
          in: query
          required: false
          type: string
        - name: ask_order.seller_settlement_flat_fee.denom
          in: query
          required: false
          type: string
        - name: ask_order.seller_settlement_flat_fee.amount
          in: query
          required: false
          type: string
        - name: ask_order.allow_partial
          description: >-
            allow_partial should be true if partial fulfillment of this order
            should be allowed, and should be false if the

            order must be either filled in full or not filled at all.
          in: query
          required: false
          type: boolean
        - name: ask_order.external_id
          description: >-
            external_id is an optional string used to externally identify this
            order. Max length is 100 characters.

            If an order in this market with this external id already exists,
            this order will be rejected.
          in: query
          required: false
          type: string
        - name: bid_order.market_id
          description: market_id identifies the market that this order belongs to.
          in: query
          required: false
          type: integer
          format: int64
        - name: bid_order.buyer
          description: >-
            buyer is the address of the account that owns this order and has the
            price to spend.
          in: query
          required: false
          type: string
        - name: bid_order.assets.denom
          in: query
          required: false
          type: string
        - name: bid_order.assets.amount
          in: query
          required: false
          type: string
        - name: bid_order.price.denom
          in: query
          required: false
          type: string
        - name: bid_order.price.amount
          in: query
          required: false
          type: string
        - name: bid_order.allow_partial
          description: >-
            allow_partial should be true if partial fulfillment of this order
            should be allowed, and should be false if the

            order must be either filled in full or not filled at all.
          in: query
          required: false
          type: boolean
        - name: bid_order.external_id
          description: >-
            external_id is an optional string used to externally identify this
            order. Max length is 100 characters.

            If an order in this market with this external id already exists,
            this order will be rejected.
          in: query
          required: false
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/fees/payment:
    get:
      summary: >-
        PaymentFeeCalc calculates the fees that must be paid for creating or
        accepting a specific payment.
      operationId: PaymentFeeCalc
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              fee_create:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: fee_create is the fee required to create the provided payment.
              fee_accept:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: fee_accept is the fee required to accept the provided payment.
            description: >-
              QueryPaymentFeeCalcResponse is a response message for the
              PaymentFeeCalc query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: payment.source
          description: >-
            source is the account that created this Payment. It is considered
            the owner of the payment.
          in: query
          required: false
          type: string
        - name: payment.target
          description: |-
            target is the account that can accept this Payment.
            The target is the only thing allowed to change in a payment.
            I.e. it can be empty initially and updated later as needed.
          in: query
          required: false
          type: string
        - name: payment.external_id
          description: >-
            external_id is used along with the source to uniquely identify this
            Payment.


            A source can only have one Payment with any given external id.

            A source can have two payments with two different external ids.

            Two different sources can each have a payment with the same external
            id.

            But a source cannot have two different payments each with the same
            external id.


            An external id can be reused by a source once the payment is
            accepted, rejected, or cancelled.


            The external id is limited to 100 bytes. An empty string is a valid
            external id.
          in: query
          required: false
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/market/{market_id}:
    get:
      summary: GetMarket returns all the information and details about a market.
      operationId: GetMarket
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              address:
                type: string
                description: address is the bech32 address string of this market's account.
              market:
                description: market is all information and details of the market.
                type: object
                properties:
                  market_id:
                    type: integer
                    format: int64
                    description: market_id is the numerical identifier for this market.
                  market_details:
                    description: market_details is some information about this market.
                    type: object
                    properties:
                      name:
                        type: string
                        description: >-
                          name is a moniker that people can use to refer to this
                          market.
                      description:
                        type: string
                        description: >-
                          description extra information about this market. The
                          field is meant to be human-readable.
                      website_url:
                        type: string
                        description: >-
                          website_url is a url people can use to get to this
                          market, or at least get more information about this
                          market.
                      icon_uri:
                        type: string
                        description: >-
                          icon_uri is a uri for an icon to associate with this
                          market.
                  fee_create_ask_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_create_ask_flat is the flat fee charged for creating
                      an ask order.

                      Each coin entry is a separate option. When an ask is
                      created, one of these must be paid.

                      If empty, no fee is required to create an ask order.
                  fee_create_bid_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_create_bid_flat is the flat fee charged for creating a
                      bid order.

                      Each coin entry is a separate option. When a bid is
                      created, one of these must be paid.

                      If empty, no fee is required to create a bid order.
                  fee_seller_settlement_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_seller_settlement_flat is the flat fee charged to the
                      seller during settlement.

                      Each coin entry is a separate option.

                      When an ask is settled, the seller will pay the amount in
                      the denom that matches the price they received.
                  fee_seller_settlement_ratios:
                    type: array
                    items:
                      type: object
                      properties:
                        price:
                          description: >-
                            price is the unit the order price is divided by to
                            get how much of the fee should apply.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        fee:
                          description: fee is the amount to charge per price unit.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                      description: >-
                        FeeRatio defines a ratio of price amount to fee amount.

                        For an order to be valid, its price must be evenly
                        divisible by a FeeRatio's price.
                    description: >-
                      fee_seller_settlement_ratios is the fee to charge a seller
                      during settlement based on the price they are receiving.

                      The price and fee denoms must be equal for each entry, and
                      only one entry for any given denom is allowed.
                  fee_buyer_settlement_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_buyer_settlement_flat is the flat fee charged to the
                      buyer during settlement.

                      Each coin entry is a separate option.

                      When a bid is created, the settlement fees provided must
                      contain one of these.
                  fee_buyer_settlement_ratios:
                    type: array
                    items:
                      type: object
                      properties:
                        price:
                          description: >-
                            price is the unit the order price is divided by to
                            get how much of the fee should apply.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        fee:
                          description: fee is the amount to charge per price unit.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                      description: >-
                        FeeRatio defines a ratio of price amount to fee amount.

                        For an order to be valid, its price must be evenly
                        divisible by a FeeRatio's price.
                    description: >-
                      fee_buyer_settlement_ratios is the fee to charge a buyer
                      during settlement based on the price they are spending.

                      The price and fee denoms do not have to equal. Multiple
                      entries for any given price or fee denom are allowed, but

                      each price denom to fee denom pair can only have one
                      entry.
                  accepting_orders:
                    type: boolean
                    description: >-
                      accepting_orders is whether this market is allowing orders
                      to be created for it.
                  allow_user_settlement:
                    type: boolean
                    description: >-
                      allow_user_settlement is whether this market allows users
                      to initiate their own settlements.

                      For example, the FillBids and FillAsks endpoints are
                      available if and only if this is true.

                      The MarketSettle endpoint is only available to market
                      actors regardless of the value of this field.
                  access_grants:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          description: >-
                            address is the address that these permissions apply
                            to.
                        permissions:
                          type: array
                          items:
                            type: string
                            enum:
                              - PERMISSION_UNSPECIFIED
                              - PERMISSION_SETTLE
                              - PERMISSION_SET_IDS
                              - PERMISSION_CANCEL
                              - PERMISSION_WITHDRAW
                              - PERMISSION_UPDATE
                              - PERMISSION_PERMISSIONS
                              - PERMISSION_ATTRIBUTES
                            default: PERMISSION_UNSPECIFIED
                            description: >-
                              Permission defines the different types of
                              permission that can be given to an account for a
                              market.

                               - PERMISSION_UNSPECIFIED: PERMISSION_UNSPECIFIED is the zero-value Permission; it is an error to use it.
                               - PERMISSION_SETTLE: PERMISSION_SETTLE is the ability to use the Settle Tx endpoint on behalf of a market.
                               - PERMISSION_SET_IDS: PERMISSION_SET_IDS is the ability to use the SetOrderExternalID Tx endpoint on behalf of a market.
                               - PERMISSION_CANCEL: PERMISSION_CANCEL is the ability to use the Cancel Tx endpoint on behalf of a market.
                               - PERMISSION_WITHDRAW: PERMISSION_WITHDRAW is the ability to use the MarketWithdraw Tx endpoint.
                               - PERMISSION_UPDATE: PERMISSION_UPDATE is the ability to use the MarketUpdate* Tx endpoints.
                               - PERMISSION_PERMISSIONS: PERMISSION_PERMISSIONS is the ability to use the MarketManagePermissions Tx endpoint.
                               - PERMISSION_ATTRIBUTES: PERMISSION_ATTRIBUTES is the ability to use the MarketManageReqAttrs Tx endpoint.
                          description: >-
                            allowed is the list of permissions available for the
                            address.
                      description: >-
                        AddrPermissions associates an address with a list of
                        permissions available for that address.
                    description: >-
                      access_grants is the list of addresses and permissions
                      granted for this market.
                  req_attr_create_ask:
                    type: array
                    items:
                      type: string
                    description: >-
                      req_attr_create_ask is a list of attributes required on an
                      account for it to be allowed to create an ask order.

                      An account must have all of these attributes in order to
                      create an ask order in this market.

                      If the list is empty, any account can create ask orders in
                      this market.


                      An entry that starts with "*." will match any attributes
                      that end with the rest of it.

                      E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and
                      "e.d.c.b.a"; but not "b.a", "xb.a", "b.x.a", or "c.b.a.x".
                  req_attr_create_bid:
                    type: array
                    items:
                      type: string
                    description: >-
                      req_attr_create_ask is a list of attributes required on an
                      account for it to be allowed to create a bid order.

                      An account must have all of these attributes in order to
                      create a bid order in this market.

                      If the list is empty, any account can create bid orders in
                      this market.


                      An entry that starts with "*." will match any attributes
                      that end with the rest of it.

                      E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and
                      "e.d.c.b.a"; but not "b.a", "xb.a", "c.b.x.a", or
                      "c.b.a.x".
                  accepting_commitments:
                    type: boolean
                    description: >-
                      accepting_commitments is whether the market is allowing
                      users to commit funds to it.
                  fee_create_commitment_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_create_commitment_flat is the flat fee charged for
                      creating a commitment.

                      Each coin entry is a separate option. When a commitment is
                      created, one of these must be paid.

                      If empty, no fee is required to create a commitment.
                  commitment_settlement_bips:
                    type: integer
                    format: int64
                    description: >-
                      commitment_settlement_bips is the fraction of a commitment
                      settlement that will be paid to the exchange.

                      It is represented in basis points (1/100th of 1%, e.g.
                      0.0001) and is limited to 0 to 10,000 inclusive.

                      During a commitment settlement, the inputs are summed and
                      NAVs are used to convert that total to the

                      intermediary denom, then to the fee denom. That is then
                      multiplied by this value to get the fee amount

                      that will be transferred out of the market's account into
                      the exchange for that settlement.


                      Summing the inputs effectively doubles the value of the
                      settlement from what what is usually thought of

                      as the value of a trade. That should be taken into account
                      when setting this value.

                      E.g. if two accounts are trading 10apples for 100grapes,
                      the inputs total will be 10apples,100grapes

                      (which might then be converted to USD then nhash before
                      applying this ratio); Usually, though, the value

                      of that trade would be viewed as either just 10apples or
                      just 100grapes.
                  intermediary_denom:
                    type: string
                    description: >-
                      intermediary_denom is the denom that funds get converted
                      to (before being converted to the chain's fee denom)

                      when calculating the fees that are paid to the exchange.
                      NAVs are used for this conversion and actions will fail

                      if a NAV is needed but not available.
                  req_attr_create_commitment:
                    type: array
                    items:
                      type: string
                    description: >-
                      req_attr_create_commitment is a list of attributes
                      required on an account for it to be allowed to create a

                      commitment. An account must have all of these attributes
                      in order to create a commitment in this market.

                      If the list is empty, any account can create commitments
                      in this market.


                      An entry that starts with "*." will match any attributes
                      that end with the rest of it.

                      E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and
                      "e.d.c.b.a"; but not "b.a", "xb.a", "c.b.x.a", or
                      "c.b.a.x".
            description: >-
              QueryGetMarketResponse is a response message for the GetMarket
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market to look up.
          in: path
          required: true
          type: integer
          format: int64
      tags:
        - Exchange
  /provenance/exchange/v1/market/{market_id}/commitment/{account}:
    get:
      summary: >-
        GetCommitment gets the funds in an account that are committed to the
        market.
      operationId: GetCommitment
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              amount:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  amount is the total funds committed to the market by the
                  account.
            description: >-
              QueryGetCommitmentResponse is a response message for the
              GetCommitment query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the numeric identifier of the market in the commitment.
          in: path
          required: true
          type: integer
          format: int64
        - name: account
          description: >-
            account is the bech32 address string of the account in the
            commitment.
          in: path
          required: true
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/market/{market_id}/commitments:
    get:
      summary: >-
        GetMarketCommitments gets all the funds committed to a market from any
        account.
      operationId: GetMarketCommitments2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              commitments:
                type: array
                items:
                  type: object
                  properties:
                    account:
                      type: string
                      description: >-
                        account is the bech32 address string of the account
                        associated with the amount.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: amount is the funds associated with the address.
                  description: AccountAmount associates an account with a coins amount.
                description: >-
                  commitments is the amounts committed to the market from any
                  account.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetMarketCommitmentsResponse is a response message for the
              GetMarketCommitments query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: >-
            market_id is the numeric identifier of the market with the
            commitment.
          in: path
          required: true
          type: integer
          format: int64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/market/{market_id}/order/{external_id}:
    get:
      summary: GetOrderByExternalID looks up an order by market id and external id.
      operationId: GetOrderByExternalID2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              order:
                description: order is the requested order.
                type: object
                properties:
                  order_id:
                    type: string
                    format: uint64
                    description: order_id is the numerical identifier for this order.
                  ask_order:
                    description: >-
                      ask_order is the information about this order if it
                      represents an ask order.
                    type: object
                    properties:
                      market_id:
                        type: integer
                        format: int64
                        description: >-
                          market_id identifies the market that this order
                          belongs to.
                      seller:
                        type: string
                        description: >-
                          seller is the address of the account that owns this
                          order and has the assets to sell.
                      assets:
                        description: >-
                          assets are the things that the seller wishes to sell.

                          A hold is placed on this until the order is filled or
                          cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      price:
                        description: >-
                          price is the minimum amount that the seller is willing
                          to accept for the assets. The seller's settlement

                          proportional fee (and possibly the settlement flat
                          fee) is taken out of the amount the seller receives,

                          so it's possible that the seller will still receive
                          less than this price.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      seller_settlement_flat_fee:
                        description: >-
                          seller_settlement_flat_fee is the flat fee for sellers
                          that will be charged during settlement. If this denom
                          is the

                          same denom as the price, it will come out of the
                          actual price received. If this denom is different, the
                          amount must

                          be in the seller's account and a hold is placed on it
                          until the order is filled or cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      allow_partial:
                        type: boolean
                        description: >-
                          allow_partial should be true if partial fulfillment of
                          this order should be allowed, and should be false if
                          the

                          order must be either filled in full or not filled at
                          all.
                      external_id:
                        type: string
                        description: >-
                          external_id is an optional string used to externally
                          identify this order. Max length is 100 characters.

                          If an order in this market with this external id
                          already exists, this order will be rejected.
                  bid_order:
                    description: >-
                      bid_order is the information about this order if it
                      represents a bid order.
                    type: object
                    properties:
                      market_id:
                        type: integer
                        format: int64
                        description: >-
                          market_id identifies the market that this order
                          belongs to.
                      buyer:
                        type: string
                        description: >-
                          buyer is the address of the account that owns this
                          order and has the price to spend.
                      assets:
                        description: assets are the things that the buyer wishes to buy.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      price:
                        description: >-
                          price is the amount that the buyer will pay for the
                          assets.

                          A hold is placed on this until the order is filled or
                          cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      buyer_settlement_fees:
                        type: array
                        items:
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                          description: >-
                            Coin defines a token with a denomination and an
                            amount.


                            NOTE: The amount field is an Int which implements
                            the custom method

                            signatures required by gogoproto.
                        description: >-
                          buyer_settlement_fees are the fees (both flat and
                          proportional) that the buyer will pay (in addition to
                          the price)

                          when the order is settled. A hold is placed on this
                          until the order is filled or cancelled.
                      allow_partial:
                        type: boolean
                        description: >-
                          allow_partial should be true if partial fulfillment of
                          this order should be allowed, and should be false if
                          the

                          order must be either filled in full or not filled at
                          all.
                      external_id:
                        type: string
                        description: >-
                          external_id is an optional string used to externally
                          identify this order. Max length is 100 characters.

                          If an order in this market with this external id
                          already exists, this order will be rejected.
            description: >-
              QueryGetOrderByExternalIDResponse is a response message for the
              GetOrderByExternalID query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market that's expected to have the order.
          in: path
          required: true
          type: integer
          format: int64
        - name: external_id
          description: external_id the external id to look up.
          in: path
          required: true
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/market/{market_id}/orders:
    get:
      summary: GetMarketOrders looks up the orders in a market.
      operationId: GetMarketOrders2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              orders:
                type: array
                items:
                  type: object
                  properties:
                    order_id:
                      type: string
                      format: uint64
                      description: order_id is the numerical identifier for this order.
                    ask_order:
                      description: >-
                        ask_order is the information about this order if it
                        represents an ask order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        seller:
                          type: string
                          description: >-
                            seller is the address of the account that owns this
                            order and has the assets to sell.
                        assets:
                          description: >-
                            assets are the things that the seller wishes to
                            sell.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the minimum amount that the seller is
                            willing to accept for the assets. The seller's
                            settlement

                            proportional fee (and possibly the settlement flat
                            fee) is taken out of the amount the seller receives,

                            so it's possible that the seller will still receive
                            less than this price.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        seller_settlement_flat_fee:
                          description: >-
                            seller_settlement_flat_fee is the flat fee for
                            sellers that will be charged during settlement. If
                            this denom is the

                            same denom as the price, it will come out of the
                            actual price received. If this denom is different,
                            the amount must

                            be in the seller's account and a hold is placed on
                            it until the order is filled or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                    bid_order:
                      description: >-
                        bid_order is the information about this order if it
                        represents a bid order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        buyer:
                          type: string
                          description: >-
                            buyer is the address of the account that owns this
                            order and has the price to spend.
                        assets:
                          description: assets are the things that the buyer wishes to buy.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the amount that the buyer will pay for the
                            assets.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        buyer_settlement_fees:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          description: >-
                            buyer_settlement_fees are the fees (both flat and
                            proportional) that the buyer will pay (in addition
                            to the price)

                            when the order is settled. A hold is placed on this
                            until the order is filled or cancelled.
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                  description: Order associates an order id with one of the order types.
                description: orders are a page of the orders in the provided market.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetMarketOrdersResponse is a response message for the
              GetMarketOrders query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market to get all the orders for.
          in: path
          required: true
          type: integer
          format: int64
        - name: order_type
          description: >-
            order_type is optional and can limit orders to only "ask" or "bid"
            orders.
          in: query
          required: false
          type: string
        - name: after_order_id
          description: >-
            after_order_id is a minimum (exclusive) order id. All results will
            be strictly greater than this.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/market/{market_id}/validate:
    get:
      summary: ValidateMarket checks for any problems with a market's setup.
      operationId: ValidateMarket2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              error:
                type: string
                description: >-
                  error is any problems or inconsistencies in the provided
                  market.
            description: >-
              QueryValidateMarketResponse is a response message for the
              ValidateMarket query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market to check.
          in: path
          required: true
          type: integer
          format: int64
      tags:
        - Exchange
  /provenance/exchange/v1/markets:
    get:
      summary: GetAllMarkets returns brief information about each market.
      operationId: GetAllMarkets
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              markets:
                type: array
                items:
                  type: object
                  properties:
                    market_id:
                      type: integer
                      format: int64
                      description: market_id is the numerical identifier for this market.
                    market_address:
                      type: string
                      description: >-
                        market_address is the bech32 address string of this
                        market's account.
                    market_details:
                      description: market_details is some information about this market.
                      type: object
                      properties:
                        name:
                          type: string
                          description: >-
                            name is a moniker that people can use to refer to
                            this market.
                        description:
                          type: string
                          description: >-
                            description extra information about this market. The
                            field is meant to be human-readable.
                        website_url:
                          type: string
                          description: >-
                            website_url is a url people can use to get to this
                            market, or at least get more information about this
                            market.
                        icon_uri:
                          type: string
                          description: >-
                            icon_uri is a uri for an icon to associate with this
                            market.
                  description: >-
                    MarketBrief is a message containing brief, superficial
                    information about a market.
                description: markets are a page of the briefs for all markets.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetAllMarketsResponse is a response message for the
              GetAllMarkets query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/order/{order_id}:
    get:
      summary: GetOrder looks up an order by id.
      operationId: GetOrder
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              order:
                description: order is the requested order.
                type: object
                properties:
                  order_id:
                    type: string
                    format: uint64
                    description: order_id is the numerical identifier for this order.
                  ask_order:
                    description: >-
                      ask_order is the information about this order if it
                      represents an ask order.
                    type: object
                    properties:
                      market_id:
                        type: integer
                        format: int64
                        description: >-
                          market_id identifies the market that this order
                          belongs to.
                      seller:
                        type: string
                        description: >-
                          seller is the address of the account that owns this
                          order and has the assets to sell.
                      assets:
                        description: >-
                          assets are the things that the seller wishes to sell.

                          A hold is placed on this until the order is filled or
                          cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      price:
                        description: >-
                          price is the minimum amount that the seller is willing
                          to accept for the assets. The seller's settlement

                          proportional fee (and possibly the settlement flat
                          fee) is taken out of the amount the seller receives,

                          so it's possible that the seller will still receive
                          less than this price.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      seller_settlement_flat_fee:
                        description: >-
                          seller_settlement_flat_fee is the flat fee for sellers
                          that will be charged during settlement. If this denom
                          is the

                          same denom as the price, it will come out of the
                          actual price received. If this denom is different, the
                          amount must

                          be in the seller's account and a hold is placed on it
                          until the order is filled or cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      allow_partial:
                        type: boolean
                        description: >-
                          allow_partial should be true if partial fulfillment of
                          this order should be allowed, and should be false if
                          the

                          order must be either filled in full or not filled at
                          all.
                      external_id:
                        type: string
                        description: >-
                          external_id is an optional string used to externally
                          identify this order. Max length is 100 characters.

                          If an order in this market with this external id
                          already exists, this order will be rejected.
                  bid_order:
                    description: >-
                      bid_order is the information about this order if it
                      represents a bid order.
                    type: object
                    properties:
                      market_id:
                        type: integer
                        format: int64
                        description: >-
                          market_id identifies the market that this order
                          belongs to.
                      buyer:
                        type: string
                        description: >-
                          buyer is the address of the account that owns this
                          order and has the price to spend.
                      assets:
                        description: assets are the things that the buyer wishes to buy.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      price:
                        description: >-
                          price is the amount that the buyer will pay for the
                          assets.

                          A hold is placed on this until the order is filled or
                          cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      buyer_settlement_fees:
                        type: array
                        items:
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                          description: >-
                            Coin defines a token with a denomination and an
                            amount.


                            NOTE: The amount field is an Int which implements
                            the custom method

                            signatures required by gogoproto.
                        description: >-
                          buyer_settlement_fees are the fees (both flat and
                          proportional) that the buyer will pay (in addition to
                          the price)

                          when the order is settled. A hold is placed on this
                          until the order is filled or cancelled.
                      allow_partial:
                        type: boolean
                        description: >-
                          allow_partial should be true if partial fulfillment of
                          this order should be allowed, and should be false if
                          the

                          order must be either filled in full or not filled at
                          all.
                      external_id:
                        type: string
                        description: >-
                          external_id is an optional string used to externally
                          identify this order. Max length is 100 characters.

                          If an order in this market with this external id
                          already exists, this order will be rejected.
            description: >-
              QueryGetOrderResponse is a response message for the GetOrder
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: order_id
          description: order_id is the id of the order to look up.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Exchange
  /provenance/exchange/v1/orders:
    get:
      summary: GetAllOrders gets all orders in the exchange module.
      operationId: GetAllOrders
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              orders:
                type: array
                items:
                  type: object
                  properties:
                    order_id:
                      type: string
                      format: uint64
                      description: order_id is the numerical identifier for this order.
                    ask_order:
                      description: >-
                        ask_order is the information about this order if it
                        represents an ask order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        seller:
                          type: string
                          description: >-
                            seller is the address of the account that owns this
                            order and has the assets to sell.
                        assets:
                          description: >-
                            assets are the things that the seller wishes to
                            sell.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the minimum amount that the seller is
                            willing to accept for the assets. The seller's
                            settlement

                            proportional fee (and possibly the settlement flat
                            fee) is taken out of the amount the seller receives,

                            so it's possible that the seller will still receive
                            less than this price.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        seller_settlement_flat_fee:
                          description: >-
                            seller_settlement_flat_fee is the flat fee for
                            sellers that will be charged during settlement. If
                            this denom is the

                            same denom as the price, it will come out of the
                            actual price received. If this denom is different,
                            the amount must

                            be in the seller's account and a hold is placed on
                            it until the order is filled or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                    bid_order:
                      description: >-
                        bid_order is the information about this order if it
                        represents a bid order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        buyer:
                          type: string
                          description: >-
                            buyer is the address of the account that owns this
                            order and has the price to spend.
                        assets:
                          description: assets are the things that the buyer wishes to buy.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the amount that the buyer will pay for the
                            assets.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        buyer_settlement_fees:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          description: >-
                            buyer_settlement_fees are the fees (both flat and
                            proportional) that the buyer will pay (in addition
                            to the price)

                            when the order is settled. A hold is placed on this
                            until the order is filled or cancelled.
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                  description: Order associates an order id with one of the order types.
                description: orders are a page of the all orders.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetAllOrdersResponse is a response message for the
              GetAllOrders query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/orders/asset/{asset}:
    get:
      summary: GetAssetOrders looks up the orders for a specific asset denom.
      operationId: GetAssetOrders
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              orders:
                type: array
                items:
                  type: object
                  properties:
                    order_id:
                      type: string
                      format: uint64
                      description: order_id is the numerical identifier for this order.
                    ask_order:
                      description: >-
                        ask_order is the information about this order if it
                        represents an ask order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        seller:
                          type: string
                          description: >-
                            seller is the address of the account that owns this
                            order and has the assets to sell.
                        assets:
                          description: >-
                            assets are the things that the seller wishes to
                            sell.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the minimum amount that the seller is
                            willing to accept for the assets. The seller's
                            settlement

                            proportional fee (and possibly the settlement flat
                            fee) is taken out of the amount the seller receives,

                            so it's possible that the seller will still receive
                            less than this price.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        seller_settlement_flat_fee:
                          description: >-
                            seller_settlement_flat_fee is the flat fee for
                            sellers that will be charged during settlement. If
                            this denom is the

                            same denom as the price, it will come out of the
                            actual price received. If this denom is different,
                            the amount must

                            be in the seller's account and a hold is placed on
                            it until the order is filled or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                    bid_order:
                      description: >-
                        bid_order is the information about this order if it
                        represents a bid order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        buyer:
                          type: string
                          description: >-
                            buyer is the address of the account that owns this
                            order and has the price to spend.
                        assets:
                          description: assets are the things that the buyer wishes to buy.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the amount that the buyer will pay for the
                            assets.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        buyer_settlement_fees:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          description: >-
                            buyer_settlement_fees are the fees (both flat and
                            proportional) that the buyer will pay (in addition
                            to the price)

                            when the order is settled. A hold is placed on this
                            until the order is filled or cancelled.
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                  description: Order associates an order id with one of the order types.
                description: orders are a page of the orders for the provided asset.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetAssetOrdersResponse is a response message for the
              GetAssetOrders query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: asset
          description: asset is the denom of assets to get orders for.
          in: path
          required: true
          type: string
        - name: order_type
          description: >-
            order_type is optional and can limit orders to only "ask" or "bid"
            orders.
          in: query
          required: false
          type: string
        - name: after_order_id
          description: >-
            after_order_id is a minimum (exclusive) order id. All results will
            be strictly greater than this.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/orders/market/{market_id}:
    get:
      summary: GetMarketOrders looks up the orders in a market.
      operationId: GetMarketOrders
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              orders:
                type: array
                items:
                  type: object
                  properties:
                    order_id:
                      type: string
                      format: uint64
                      description: order_id is the numerical identifier for this order.
                    ask_order:
                      description: >-
                        ask_order is the information about this order if it
                        represents an ask order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        seller:
                          type: string
                          description: >-
                            seller is the address of the account that owns this
                            order and has the assets to sell.
                        assets:
                          description: >-
                            assets are the things that the seller wishes to
                            sell.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the minimum amount that the seller is
                            willing to accept for the assets. The seller's
                            settlement

                            proportional fee (and possibly the settlement flat
                            fee) is taken out of the amount the seller receives,

                            so it's possible that the seller will still receive
                            less than this price.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        seller_settlement_flat_fee:
                          description: >-
                            seller_settlement_flat_fee is the flat fee for
                            sellers that will be charged during settlement. If
                            this denom is the

                            same denom as the price, it will come out of the
                            actual price received. If this denom is different,
                            the amount must

                            be in the seller's account and a hold is placed on
                            it until the order is filled or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                    bid_order:
                      description: >-
                        bid_order is the information about this order if it
                        represents a bid order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        buyer:
                          type: string
                          description: >-
                            buyer is the address of the account that owns this
                            order and has the price to spend.
                        assets:
                          description: assets are the things that the buyer wishes to buy.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the amount that the buyer will pay for the
                            assets.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        buyer_settlement_fees:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          description: >-
                            buyer_settlement_fees are the fees (both flat and
                            proportional) that the buyer will pay (in addition
                            to the price)

                            when the order is settled. A hold is placed on this
                            until the order is filled or cancelled.
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                  description: Order associates an order id with one of the order types.
                description: orders are a page of the orders in the provided market.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetMarketOrdersResponse is a response message for the
              GetMarketOrders query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market to get all the orders for.
          in: path
          required: true
          type: integer
          format: int64
        - name: order_type
          description: >-
            order_type is optional and can limit orders to only "ask" or "bid"
            orders.
          in: query
          required: false
          type: string
        - name: after_order_id
          description: >-
            after_order_id is a minimum (exclusive) order id. All results will
            be strictly greater than this.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/orders/market/{market_id}/{external_id}:
    get:
      summary: GetOrderByExternalID looks up an order by market id and external id.
      operationId: GetOrderByExternalID
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              order:
                description: order is the requested order.
                type: object
                properties:
                  order_id:
                    type: string
                    format: uint64
                    description: order_id is the numerical identifier for this order.
                  ask_order:
                    description: >-
                      ask_order is the information about this order if it
                      represents an ask order.
                    type: object
                    properties:
                      market_id:
                        type: integer
                        format: int64
                        description: >-
                          market_id identifies the market that this order
                          belongs to.
                      seller:
                        type: string
                        description: >-
                          seller is the address of the account that owns this
                          order and has the assets to sell.
                      assets:
                        description: >-
                          assets are the things that the seller wishes to sell.

                          A hold is placed on this until the order is filled or
                          cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      price:
                        description: >-
                          price is the minimum amount that the seller is willing
                          to accept for the assets. The seller's settlement

                          proportional fee (and possibly the settlement flat
                          fee) is taken out of the amount the seller receives,

                          so it's possible that the seller will still receive
                          less than this price.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      seller_settlement_flat_fee:
                        description: >-
                          seller_settlement_flat_fee is the flat fee for sellers
                          that will be charged during settlement. If this denom
                          is the

                          same denom as the price, it will come out of the
                          actual price received. If this denom is different, the
                          amount must

                          be in the seller's account and a hold is placed on it
                          until the order is filled or cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      allow_partial:
                        type: boolean
                        description: >-
                          allow_partial should be true if partial fulfillment of
                          this order should be allowed, and should be false if
                          the

                          order must be either filled in full or not filled at
                          all.
                      external_id:
                        type: string
                        description: >-
                          external_id is an optional string used to externally
                          identify this order. Max length is 100 characters.

                          If an order in this market with this external id
                          already exists, this order will be rejected.
                  bid_order:
                    description: >-
                      bid_order is the information about this order if it
                      represents a bid order.
                    type: object
                    properties:
                      market_id:
                        type: integer
                        format: int64
                        description: >-
                          market_id identifies the market that this order
                          belongs to.
                      buyer:
                        type: string
                        description: >-
                          buyer is the address of the account that owns this
                          order and has the price to spend.
                      assets:
                        description: assets are the things that the buyer wishes to buy.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      price:
                        description: >-
                          price is the amount that the buyer will pay for the
                          assets.

                          A hold is placed on this until the order is filled or
                          cancelled.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      buyer_settlement_fees:
                        type: array
                        items:
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                          description: >-
                            Coin defines a token with a denomination and an
                            amount.


                            NOTE: The amount field is an Int which implements
                            the custom method

                            signatures required by gogoproto.
                        description: >-
                          buyer_settlement_fees are the fees (both flat and
                          proportional) that the buyer will pay (in addition to
                          the price)

                          when the order is settled. A hold is placed on this
                          until the order is filled or cancelled.
                      allow_partial:
                        type: boolean
                        description: >-
                          allow_partial should be true if partial fulfillment of
                          this order should be allowed, and should be false if
                          the

                          order must be either filled in full or not filled at
                          all.
                      external_id:
                        type: string
                        description: >-
                          external_id is an optional string used to externally
                          identify this order. Max length is 100 characters.

                          If an order in this market with this external id
                          already exists, this order will be rejected.
            description: >-
              QueryGetOrderByExternalIDResponse is a response message for the
              GetOrderByExternalID query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market that's expected to have the order.
          in: path
          required: true
          type: integer
          format: int64
        - name: external_id
          description: external_id the external id to look up.
          in: path
          required: true
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/orders/owner/{owner}:
    get:
      summary: GetOwnerOrders looks up the orders from the provided owner address.
      operationId: GetOwnerOrders
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              orders:
                type: array
                items:
                  type: object
                  properties:
                    order_id:
                      type: string
                      format: uint64
                      description: order_id is the numerical identifier for this order.
                    ask_order:
                      description: >-
                        ask_order is the information about this order if it
                        represents an ask order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        seller:
                          type: string
                          description: >-
                            seller is the address of the account that owns this
                            order and has the assets to sell.
                        assets:
                          description: >-
                            assets are the things that the seller wishes to
                            sell.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the minimum amount that the seller is
                            willing to accept for the assets. The seller's
                            settlement

                            proportional fee (and possibly the settlement flat
                            fee) is taken out of the amount the seller receives,

                            so it's possible that the seller will still receive
                            less than this price.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        seller_settlement_flat_fee:
                          description: >-
                            seller_settlement_flat_fee is the flat fee for
                            sellers that will be charged during settlement. If
                            this denom is the

                            same denom as the price, it will come out of the
                            actual price received. If this denom is different,
                            the amount must

                            be in the seller's account and a hold is placed on
                            it until the order is filled or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                    bid_order:
                      description: >-
                        bid_order is the information about this order if it
                        represents a bid order.
                      type: object
                      properties:
                        market_id:
                          type: integer
                          format: int64
                          description: >-
                            market_id identifies the market that this order
                            belongs to.
                        buyer:
                          type: string
                          description: >-
                            buyer is the address of the account that owns this
                            order and has the price to spend.
                        assets:
                          description: assets are the things that the buyer wishes to buy.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        price:
                          description: >-
                            price is the amount that the buyer will pay for the
                            assets.

                            A hold is placed on this until the order is filled
                            or cancelled.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        buyer_settlement_fees:
                          type: array
                          items:
                            type: object
                            properties:
                              denom:
                                type: string
                              amount:
                                type: string
                            description: >-
                              Coin defines a token with a denomination and an
                              amount.


                              NOTE: The amount field is an Int which implements
                              the custom method

                              signatures required by gogoproto.
                          description: >-
                            buyer_settlement_fees are the fees (both flat and
                            proportional) that the buyer will pay (in addition
                            to the price)

                            when the order is settled. A hold is placed on this
                            until the order is filled or cancelled.
                        allow_partial:
                          type: boolean
                          description: >-
                            allow_partial should be true if partial fulfillment
                            of this order should be allowed, and should be false
                            if the

                            order must be either filled in full or not filled at
                            all.
                        external_id:
                          type: string
                          description: >-
                            external_id is an optional string used to externally
                            identify this order. Max length is 100 characters.

                            If an order in this market with this external id
                            already exists, this order will be rejected.
                  description: Order associates an order id with one of the order types.
                description: orders are a page of the orders for the provided address.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetOwnerOrdersResponse is a response message for the
              GetOwnerOrders query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: owner
          description: >-
            owner is the bech32 address string of the owner to get the orders
            for.
          in: path
          required: true
          type: string
        - name: order_type
          description: >-
            order_type is optional and can limit orders to only "ask" or "bid"
            orders.
          in: query
          required: false
          type: string
        - name: after_order_id
          description: >-
            after_order_id is a minimum (exclusive) order id. All results will
            be strictly greater than this.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/params:
    get:
      summary: Params returns the exchange module parameters.
      operationId: ExchangeParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params are the exchange module parameter values.
                type: object
                properties:
                  default_split:
                    type: integer
                    format: int64
                    description: >-
                      default_split is the default proportion of fees the
                      exchange receives in basis points.

                      It is used if there isn't an applicable denom-specific
                      split defined.

                      E.g. 100 = 1%. Min = 0, Max = 10000.
                  denom_splits:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                          description: >-
                            denom is the coin denomination this split applies
                            to.
                        split:
                          type: integer
                          format: int64
                          description: >-
                            split is the proportion of fees the exchange
                            receives for this denom in basis points.

                            E.g. 100 = 1%. Min = 0, Max = 10000.
                      description: >-
                        DenomSplit associates a coin denomination with an amount
                        the exchange receives for that denom.
                    description: >-
                      denom_splits are the denom-specific amounts the exchange
                      receives.
                  fee_create_payment_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_create_payment_flat is the flat fee options for
                      creating a payment.

                      If the source amount is not zero then one of these fee
                      entries is required to create the payment.

                      This field is currently limited to zero or one entries.
                  fee_accept_payment_flat:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      fee_accept_payment_flat is the flat fee options for
                      accepting a payment.

                      If the target amount is not zero then one of these fee
                      entries is required to accept the payment.

                      This field is currently limited to zero or one entries.
            description: QueryParamsResponse is a response message for the Params query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Exchange
  /provenance/exchange/v1/payment:
    get:
      summary: GetPayment gets a single specific payment.
      operationId: GetPayment
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payment:
                description: payment is the info on the requested payment.
                type: object
                properties:
                  source:
                    type: string
                    description: >-
                      source is the account that created this Payment. It is
                      considered the owner of the payment.
                  source_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      source_amount is the funds that the source is will pay the
                      target in exchange for the target_amount.

                      A hold will be placed on this amount in the source account
                      until this Payment is accepted, rejected or cancelled.

                      If the source_amount is zero, this Payment can be
                      considered a "payment request."
                  target:
                    type: string
                    description: >-
                      target is the account that can accept this Payment.

                      The target is the only thing allowed to change in a
                      payment.

                      I.e. it can be empty initially and updated later as
                      needed.
                  target_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      target_amount is the funds that the target will pay the
                      source in exchange for the source_amount.

                      If the target_amount is zero, this Payment can be
                      considered a "peer-to-peer (P2P) payment."
                  external_id:
                    type: string
                    description: >-
                      external_id is used along with the source to uniquely
                      identify this Payment.


                      A source can only have one Payment with any given external
                      id.

                      A source can have two payments with two different external
                      ids.

                      Two different sources can each have a payment with the
                      same external id.

                      But a source cannot have two different payments each with
                      the same external id.


                      An external id can be reused by a source once the payment
                      is accepted, rejected, or cancelled.


                      The external id is limited to 100 bytes. An empty string
                      is a valid external id.
            description: >-
              QueryGetPaymentResponse is a response message for the GetPayment
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: source
          description: source is the source account of the payment to get.
          in: query
          required: false
          type: string
        - name: external_id
          description: external_id is the external id of the payment to get.
          in: query
          required: false
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/payment/{source}:
    get:
      summary: GetPayment gets a single specific payment.
      operationId: GetPayment2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payment:
                description: payment is the info on the requested payment.
                type: object
                properties:
                  source:
                    type: string
                    description: >-
                      source is the account that created this Payment. It is
                      considered the owner of the payment.
                  source_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      source_amount is the funds that the source is will pay the
                      target in exchange for the target_amount.

                      A hold will be placed on this amount in the source account
                      until this Payment is accepted, rejected or cancelled.

                      If the source_amount is zero, this Payment can be
                      considered a "payment request."
                  target:
                    type: string
                    description: >-
                      target is the account that can accept this Payment.

                      The target is the only thing allowed to change in a
                      payment.

                      I.e. it can be empty initially and updated later as
                      needed.
                  target_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      target_amount is the funds that the target will pay the
                      source in exchange for the source_amount.

                      If the target_amount is zero, this Payment can be
                      considered a "peer-to-peer (P2P) payment."
                  external_id:
                    type: string
                    description: >-
                      external_id is used along with the source to uniquely
                      identify this Payment.


                      A source can only have one Payment with any given external
                      id.

                      A source can have two payments with two different external
                      ids.

                      Two different sources can each have a payment with the
                      same external id.

                      But a source cannot have two different payments each with
                      the same external id.


                      An external id can be reused by a source once the payment
                      is accepted, rejected, or cancelled.


                      The external id is limited to 100 bytes. An empty string
                      is a valid external id.
            description: >-
              QueryGetPaymentResponse is a response message for the GetPayment
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: source
          description: source is the source account of the payment to get.
          in: path
          required: true
          type: string
        - name: external_id
          description: external_id is the external id of the payment to get.
          in: query
          required: false
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/payment/{source}/{external_id}:
    get:
      summary: GetPayment gets a single specific payment.
      operationId: GetPayment3
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payment:
                description: payment is the info on the requested payment.
                type: object
                properties:
                  source:
                    type: string
                    description: >-
                      source is the account that created this Payment. It is
                      considered the owner of the payment.
                  source_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      source_amount is the funds that the source is will pay the
                      target in exchange for the target_amount.

                      A hold will be placed on this amount in the source account
                      until this Payment is accepted, rejected or cancelled.

                      If the source_amount is zero, this Payment can be
                      considered a "payment request."
                  target:
                    type: string
                    description: >-
                      target is the account that can accept this Payment.

                      The target is the only thing allowed to change in a
                      payment.

                      I.e. it can be empty initially and updated later as
                      needed.
                  target_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    title: >-
                      target_amount is the funds that the target will pay the
                      source in exchange for the source_amount.

                      If the target_amount is zero, this Payment can be
                      considered a "peer-to-peer (P2P) payment."
                  external_id:
                    type: string
                    description: >-
                      external_id is used along with the source to uniquely
                      identify this Payment.


                      A source can only have one Payment with any given external
                      id.

                      A source can have two payments with two different external
                      ids.

                      Two different sources can each have a payment with the
                      same external id.

                      But a source cannot have two different payments each with
                      the same external id.


                      An external id can be reused by a source once the payment
                      is accepted, rejected, or cancelled.


                      The external id is limited to 100 bytes. An empty string
                      is a valid external id.
            description: >-
              QueryGetPaymentResponse is a response message for the GetPayment
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: source
          description: source is the source account of the payment to get.
          in: path
          required: true
          type: string
        - name: external_id
          description: external_id is the external id of the payment to get.
          in: path
          required: true
          type: string
      tags:
        - Exchange
  /provenance/exchange/v1/payments:
    get:
      summary: GetAllPayments gets all payments.
      operationId: GetAllPayments
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payments:
                type: array
                items:
                  type: object
                  properties:
                    source:
                      type: string
                      description: >-
                        source is the account that created this Payment. It is
                        considered the owner of the payment.
                    source_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      title: >-
                        source_amount is the funds that the source is will pay
                        the target in exchange for the target_amount.

                        A hold will be placed on this amount in the source
                        account until this Payment is accepted, rejected or
                        cancelled.

                        If the source_amount is zero, this Payment can be
                        considered a "payment request."
                    target:
                      type: string
                      description: >-
                        target is the account that can accept this Payment.

                        The target is the only thing allowed to change in a
                        payment.

                        I.e. it can be empty initially and updated later as
                        needed.
                    target_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      title: >-
                        target_amount is the funds that the target will pay the
                        source in exchange for the source_amount.

                        If the target_amount is zero, this Payment can be
                        considered a "peer-to-peer (P2P) payment."
                    external_id:
                      type: string
                      description: >-
                        external_id is used along with the source to uniquely
                        identify this Payment.


                        A source can only have one Payment with any given
                        external id.

                        A source can have two payments with two different
                        external ids.

                        Two different sources can each have a payment with the
                        same external id.

                        But a source cannot have two different payments each
                        with the same external id.


                        An external id can be reused by a source once the
                        payment is accepted, rejected, or cancelled.


                        The external id is limited to 100 bytes. An empty string
                        is a valid external id.
                  description: >-
                    Payment represents one account's desire to trade funds with
                    another account.
                description: payments is all the payments on this page of results.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetAllPaymentsResponse is a response message for the
              GetAllPayments query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/payments/source/{source}:
    get:
      summary: GetPaymentsWithSource gets all payments with a specific source account.
      operationId: GetPaymentsWithSource
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payments:
                type: array
                items:
                  type: object
                  properties:
                    source:
                      type: string
                      description: >-
                        source is the account that created this Payment. It is
                        considered the owner of the payment.
                    source_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      title: >-
                        source_amount is the funds that the source is will pay
                        the target in exchange for the target_amount.

                        A hold will be placed on this amount in the source
                        account until this Payment is accepted, rejected or
                        cancelled.

                        If the source_amount is zero, this Payment can be
                        considered a "payment request."
                    target:
                      type: string
                      description: >-
                        target is the account that can accept this Payment.

                        The target is the only thing allowed to change in a
                        payment.

                        I.e. it can be empty initially and updated later as
                        needed.
                    target_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      title: >-
                        target_amount is the funds that the target will pay the
                        source in exchange for the source_amount.

                        If the target_amount is zero, this Payment can be
                        considered a "peer-to-peer (P2P) payment."
                    external_id:
                      type: string
                      description: >-
                        external_id is used along with the source to uniquely
                        identify this Payment.


                        A source can only have one Payment with any given
                        external id.

                        A source can have two payments with two different
                        external ids.

                        Two different sources can each have a payment with the
                        same external id.

                        But a source cannot have two different payments each
                        with the same external id.


                        An external id can be reused by a source once the
                        payment is accepted, rejected, or cancelled.


                        The external id is limited to 100 bytes. An empty string
                        is a valid external id.
                  description: >-
                    Payment represents one account's desire to trade funds with
                    another account.
                description: payments is all the payments with the requested source.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetPaymentsWithSourceResponse is a response message for the
              GetPaymentsWithSource query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: source
          description: source is the source account of the payments to get.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/payments/target/{target}:
    get:
      summary: GetPaymentsWithTarget gets all payments with a specific target account.
      operationId: GetPaymentsWithTarget
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payments:
                type: array
                items:
                  type: object
                  properties:
                    source:
                      type: string
                      description: >-
                        source is the account that created this Payment. It is
                        considered the owner of the payment.
                    source_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      title: >-
                        source_amount is the funds that the source is will pay
                        the target in exchange for the target_amount.

                        A hold will be placed on this amount in the source
                        account until this Payment is accepted, rejected or
                        cancelled.

                        If the source_amount is zero, this Payment can be
                        considered a "payment request."
                    target:
                      type: string
                      description: >-
                        target is the account that can accept this Payment.

                        The target is the only thing allowed to change in a
                        payment.

                        I.e. it can be empty initially and updated later as
                        needed.
                    target_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      title: >-
                        target_amount is the funds that the target will pay the
                        source in exchange for the source_amount.

                        If the target_amount is zero, this Payment can be
                        considered a "peer-to-peer (P2P) payment."
                    external_id:
                      type: string
                      description: >-
                        external_id is used along with the source to uniquely
                        identify this Payment.


                        A source can only have one Payment with any given
                        external id.

                        A source can have two payments with two different
                        external ids.

                        Two different sources can each have a payment with the
                        same external id.

                        But a source cannot have two different payments each
                        with the same external id.


                        An external id can be reused by a source once the
                        payment is accepted, rejected, or cancelled.


                        The external id is limited to 100 bytes. An empty string
                        is a valid external id.
                  description: >-
                    Payment represents one account's desire to trade funds with
                    another account.
                description: payments is all the payments with the requested target.
              pagination:
                description: pagination is the resulting pagination parameters.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetPaymentsWithTargetResponse is a response message for the
              GetPaymentsWithTarget query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: target
          description: target is the target account of the payments to get.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/validate/create_market:
    get:
      summary: >-
        ValidateCreateMarket checks the provided MsgGovCreateMarketResponse and
        returns any errors it might have.
      operationId: ValidateCreateMarket
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              error:
                type: string
                description: >-
                  error is any problems or inconsistencies in the provided gov
                  prop msg.

                  This goes above and beyond the validation done when actually
                  processing the governance proposal.

                  If an error is returned, and gov_prop_will_pass is true, it
                  means the error is more of an

                  inconsistency that might cause certain aspects of the market
                  to behave unexpectedly.
              gov_prop_will_pass:
                type: boolean
                description: >-
                  gov_prop_will_pass will be true if the the provided msg will
                  be successfully processed at the end of it's voting

                  period (assuming it passes).
            description: >-
              QueryValidateCreateMarketResponse is a response message for the
              ValidateCreateMarket query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: create_market_request.authority
          description: authority should be the governance module account address.
          in: query
          required: false
          type: string
        - name: create_market_request.market.market_id
          description: market_id is the numerical identifier for this market.
          in: query
          required: false
          type: integer
          format: int64
        - name: create_market_request.market.market_details.name
          description: name is a moniker that people can use to refer to this market.
          in: query
          required: false
          type: string
        - name: create_market_request.market.market_details.description
          description: >-
            description extra information about this market. The field is meant
            to be human-readable.
          in: query
          required: false
          type: string
        - name: create_market_request.market.market_details.website_url
          description: >-
            website_url is a url people can use to get to this market, or at
            least get more information about this market.
          in: query
          required: false
          type: string
        - name: create_market_request.market.market_details.icon_uri
          description: icon_uri is a uri for an icon to associate with this market.
          in: query
          required: false
          type: string
        - name: create_market_request.market.accepting_orders
          description: >-
            accepting_orders is whether this market is allowing orders to be
            created for it.
          in: query
          required: false
          type: boolean
        - name: create_market_request.market.allow_user_settlement
          description: >-
            allow_user_settlement is whether this market allows users to
            initiate their own settlements.

            For example, the FillBids and FillAsks endpoints are available if
            and only if this is true.

            The MarketSettle endpoint is only available to market actors
            regardless of the value of this field.
          in: query
          required: false
          type: boolean
        - name: create_market_request.market.req_attr_create_ask
          description: >-
            req_attr_create_ask is a list of attributes required on an account
            for it to be allowed to create an ask order.

            An account must have all of these attributes in order to create an
            ask order in this market.

            If the list is empty, any account can create ask orders in this
            market.


            An entry that starts with "*." will match any attributes that end
            with the rest of it.

            E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
            but not "b.a", "xb.a", "b.x.a", or "c.b.a.x".
          in: query
          required: false
          type: array
          items:
            type: string
          collectionFormat: multi
        - name: create_market_request.market.req_attr_create_bid
          description: >-
            req_attr_create_ask is a list of attributes required on an account
            for it to be allowed to create a bid order.

            An account must have all of these attributes in order to create a
            bid order in this market.

            If the list is empty, any account can create bid orders in this
            market.


            An entry that starts with "*." will match any attributes that end
            with the rest of it.

            E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
            but not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
          in: query
          required: false
          type: array
          items:
            type: string
          collectionFormat: multi
        - name: create_market_request.market.accepting_commitments
          description: >-
            accepting_commitments is whether the market is allowing users to
            commit funds to it.
          in: query
          required: false
          type: boolean
        - name: create_market_request.market.commitment_settlement_bips
          description: >-
            commitment_settlement_bips is the fraction of a commitment
            settlement that will be paid to the exchange.

            It is represented in basis points (1/100th of 1%, e.g. 0.0001) and
            is limited to 0 to 10,000 inclusive.

            During a commitment settlement, the inputs are summed and NAVs are
            used to convert that total to the

            intermediary denom, then to the fee denom. That is then multiplied
            by this value to get the fee amount

            that will be transferred out of the market's account into the
            exchange for that settlement.


            Summing the inputs effectively doubles the value of the settlement
            from what what is usually thought of

            as the value of a trade. That should be taken into account when
            setting this value.

            E.g. if two accounts are trading 10apples for 100grapes, the inputs
            total will be 10apples,100grapes

            (which might then be converted to USD then nhash before applying
            this ratio); Usually, though, the value

            of that trade would be viewed as either just 10apples or just
            100grapes.
          in: query
          required: false
          type: integer
          format: int64
        - name: create_market_request.market.intermediary_denom
          description: >-
            intermediary_denom is the denom that funds get converted to (before
            being converted to the chain's fee denom)

            when calculating the fees that are paid to the exchange. NAVs are
            used for this conversion and actions will fail

            if a NAV is needed but not available.
          in: query
          required: false
          type: string
        - name: create_market_request.market.req_attr_create_commitment
          description: >-
            req_attr_create_commitment is a list of attributes required on an
            account for it to be allowed to create a

            commitment. An account must have all of these attributes in order to
            create a commitment in this market.

            If the list is empty, any account can create commitments in this
            market.


            An entry that starts with "*." will match any attributes that end
            with the rest of it.

            E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
            but not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
          in: query
          required: false
          type: array
          items:
            type: string
          collectionFormat: multi
      tags:
        - Exchange
  /provenance/exchange/v1/validate/manage_fees:
    get:
      summary: >-
        ValidateManageFees checks the provided MsgGovManageFeesRequest and
        returns any errors that it might have.
      operationId: ValidateManageFees
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              error:
                type: string
                description: >-
                  error is any problems or inconsistencies in the provided gov
                  prop msg.

                  This goes above and beyond the validation done when actually
                  processing the governance proposal.

                  If an error is returned, and gov_prop_will_pass is true, it
                  means the error is more of an

                  inconsistency that might cause certain aspects of the market
                  to behave unexpectedly.
              gov_prop_will_pass:
                type: boolean
                description: >-
                  gov_prop_will_pass will be true if the the provided msg will
                  be successfully processed at the end of it's voting

                  period (assuming it passes).
            description: >-
              QueryValidateManageFeesResponse is a response message for the
              ValidateManageFees query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: manage_fees_request.authority
          description: authority should be the governance module account address.
          in: query
          required: false
          type: string
        - name: manage_fees_request.market_id
          description: market_id is the market id that will get these fee updates.
          in: query
          required: false
          type: integer
          format: int64
        - name: manage_fees_request.set_fee_commitment_settlement_bips
          description: >-
            set_fee_commitment_settlement_bips is the new
            fee_commitment_settlement_bips for the market.

            It is ignored if it is zero. To set it to zero set
            unset_fee_commitment_settlement_bips to true.
          in: query
          required: false
          type: integer
          format: int64
        - name: manage_fees_request.unset_fee_commitment_settlement_bips
          description: >-
            unset_fee_commitment_settlement_bips, if true, sets the
            fee_commitment_settlement_bips to zero.

            If false, it is ignored.
          in: query
          required: false
          type: boolean
      tags:
        - Exchange
  /provenance/exchange/v1/validate/market/{market_id}:
    get:
      summary: ValidateMarket checks for any problems with a market's setup.
      operationId: ValidateMarket
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              error:
                type: string
                description: >-
                  error is any problems or inconsistencies in the provided
                  market.
            description: >-
              QueryValidateMarketResponse is a response message for the
              ValidateMarket query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: market_id
          description: market_id is the id of the market to check.
          in: path
          required: true
          type: integer
          format: int64
      tags:
        - Exchange
  /provenance/flatfees/v1/msgfee:
    get:
      summary: >-
        MsgFee will return information about what it will cost to execute a
        given msg type.

        If the provided msg type does not have a specific fee defined, the
        default is returned.
      operationId: MsgFee
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              msg_fee:
                description: msg_fee is the requested entry.
                type: object
                properties:
                  msg_type_url:
                    type: string
                    description: >-
                      msg_type_url is the type-url of the message, e.g.
                      "/cosmos.bank.v1beta1.MsgSend".
                  cost:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      cost is the Tx fee required for this msg_type_url.

                      It should have the same denomination as the default cost
                      and as the conversion factor's

                      definition_amount, e.g. musdc. Any other denomination will
                      be charged as defined.
            description: QueryMsgFeeResponse is the response type for the MsgFee query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: msg_type_url
          description: >-
            msg_type_url is the is the type-url of the message, e.g.
            "/cosmos.bank.v1beta1.MsgSend".
          in: query
          required: false
          type: string
        - name: do_not_convert
          description: >-
            do_not_convert, if true, will return the fees as defined (instead of
            as converted).
          in: query
          required: false
          type: boolean
      tags:
        - Fees
  /provenance/flatfees/v1/msgfee/all:
    get:
      summary: AllMsgFees returns info on all msg types that have a customized msg fee.
      operationId: AllMsgFees
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              msg_fees:
                type: array
                items:
                  type: object
                  properties:
                    msg_type_url:
                      type: string
                      description: >-
                        msg_type_url is the type-url of the message, e.g.
                        "/cosmos.bank.v1beta1.MsgSend".
                    cost:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: >-
                        cost is the Tx fee required for this msg_type_url.

                        It should have the same denomination as the default cost
                        and as the conversion factor's

                        definition_amount, e.g. musdc. Any other denomination
                        will be charged as defined.
                  description: MsgFee defines the cost to use a specific message type.
                description: msg_fees contains the requested information.
              default_cost:
                description: >-
                  default_cost is the amount a msg costs when there is no
                  specific msg-fee defined for it.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              pagination:
                description: pagination defines the pagination parameters of the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAllMsgFeesResponse is the response type for the AllMsgFees
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: do_not_convert
          description: >-
            do_not_convert, if true, will return the fees as defined (instead of
            as converted).
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Fees
  /provenance/flatfees/v1/params:
    get:
      summary: Params returns the parameters for the x/flatfees module.
      operationId: FlatFeeParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the x/flatfees module.
                type: object
                properties:
                  default_cost:
                    description: >-
                      default_cost is the amount a msg costs when there is no
                      specific msg-fee defined for it.

                      The denom used here should be the same as used to define
                      the specific msg costs.

                      The recommended denom is musd.
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                  conversion_factor:
                    description: >-
                      conversion_factor is the ratio used to convert the
                      msg-fees from their defined amounts into the fee
                      denomination.

                      The definition_amount should have the same denom as the
                      default cost.

                      The denom of the converted amount should be the denom that
                      fees are paid in, e.g. nhash.
                    type: object
                    properties:
                      definition_amount:
                        description: >-
                          definition_amount is an amount (in the denomination
                          used to define fees) that is equal to the
                          converted_amount.

                          This cannot have an amount of zero.

                          If this has the same denomination as the
                          converted_amount, then the amounts must also be equal.

                          The denom of this field should be the same as the
                          default cost, e.g. musd.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                      converted_amount:
                        description: >-
                          converted_amount is an amount in the fee denomination
                          equal to the definition_amount.

                          If this is zero, all msgs will be free.

                          If this has the same denomination as the
                          definition_amount, then the amounts must also be
                          equal.

                          The denom of this field should be the fee denom, e.g.
                          nhash.
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                  oracle_addresses:
                    type: array
                    items:
                      type: string
                    description: >-
                      oracle_addresses is the list of addresses allowed to
                      update the conversion factor without governance.
            description: QueryParamsResponse is the response type for the Params query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Fees
  /provenance/tx/v1/calculate_flat_fee:
    post:
      summary: >-
        CalculateTxFees simulates executing a transaction for estimating gas
        usage and fees.
      operationId: CalculateTxFees
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              total_fees:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: total_fees is the amount of fees needed for the Tx.
              estimated_gas:
                type: string
                format: uint64
                description: estimated_gas is the amount of gas needed for the Tx.
            description: >-
              QueryCalculateTxFeesResponse is the response type for the
              CalculateTxFees query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: body
          description: >-
            QueryCalculateTxFeesRequest is the request type for the
            CalculateTxFees query.
          in: body
          required: true
          schema:
            type: object
            properties:
              tx_bytes:
                type: string
                format: byte
                description: tx_bytes is the transaction to simulate.
              gas_adjustment:
                type: number
                format: float
                description: >-
                  gas_adjustment is a multiplier applied to the gas used while
                  simulating the tx.

                  This only affects the estimated gas (since the fee is flat).

                  The default is 1.0. Valid range is 0.0 (exclusive) to 10.0
                  (inclusive).
            description: >-
              QueryCalculateTxFeesRequest is the request type for the
              CalculateTxFees query.
      tags:
        - Fees
  /provenance/hold/v1/funds:
    get:
      summary: >-
        GetAllHolds returns all addresses with funds on hold, and the amount
        held.
      operationId: GetAllHolds
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              holds:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: >-
                        address is the account address that holds the funds on
                        hold.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: amount is the balances that are on hold for the address.
                  description: >-
                    AccountHold associates an address with an amount on hold for
                    that address.
                description: >-
                  holds is a list of addresses with funds on hold and the
                  amounts being held.
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              GetAllHoldsResponse is the response type for the Query/GetAllHolds
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Hold
  /provenance/hold/v1/funds/{address}:
    get:
      summary: GetHolds looks up the funds that are on hold for an address.
      operationId: GetHolds
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              amount:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: amount is the total on hold for the requested address.
            description: >-
              GetHoldsResponse is the response type for the Query/GetHolds
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address is the account address to get on-hold balances for.
          in: path
          required: true
          type: string
      tags:
        - Hold
  /provenance/ibchooks/v1/params:
    get:
      summary: Params queries params of the ihchooks module.
      operationId: IBCHooksParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  allowed_async_ack_contracts:
                    type: array
                    items:
                      type: string
                    description: |-
                      Deprecated: This field has been deprecated and is ignored.
                      Async ack contracts are no longer possible in IBC v10.
                title: Params defines the allowed async ack contracts
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - IBC Hooks
  /provenance/ibcratelimit/v1/params:
    get:
      summary: >-
        Params defines a gRPC query method that returns the ibcratelimit
        module's

        parameters.
      operationId: IBCRateLimitParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  contract_address:
                    type: string
                    description: >-
                      contract_address is the address of the rate limiter
                      contract.
            description: >-
              ParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - IBC
  /provenance/ledger/v1/class:
    get:
      summary: LedgerClasses returns a paginated list of ledger classes.
      operationId: LedgerClasses
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              ledger_classes:
                type: array
                items:
                  type: object
                  properties:
                    ledger_class_id:
                      type: string
                      description: >-
                        Unique ID for the ledger class (eg. 1, 2, 3, etc.).

                        This is necessary since the nft class does not have an
                        owner.
                    asset_class_id:
                      type: string
                      description: Scope Specification ID or NFT Class ID.
                    denom:
                      type: string
                      description: Denom that this class of asset will be ledgered in.
                    maintainer_address:
                      type: string
                      description: Address of the maintainer for the ledger class.
                  description: >-
                    LedgerClass contains the configuration for a ledger related
                    to a particular class of asset. The asset class

                    is defined by the either a scope spec `x/metadata`, or nft
                    class `x/nft`. Ultimately, the configuration will

                    assist in verifying the types that are associated with
                    particular ledger entries.
                description: List of ledger classes.
              pagination:
                description: pagination is the pagination details for this response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: QueryLedgerClassesResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Ledger
  /provenance/ledger/v1/class/{ledger_class_id}:
    get:
      summary: LedgerClass returns the ledger class for a given ledger class id.
      operationId: LedgerClass
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              ledger_class:
                type: object
                properties:
                  ledger_class_id:
                    type: string
                    description: >-
                      Unique ID for the ledger class (eg. 1, 2, 3, etc.).

                      This is necessary since the nft class does not have an
                      owner.
                  asset_class_id:
                    type: string
                    description: Scope Specification ID or NFT Class ID.
                  denom:
                    type: string
                    description: Denom that this class of asset will be ledgered in.
                  maintainer_address:
                    type: string
                    description: Address of the maintainer for the ledger class.
                description: >-
                  LedgerClass contains the configuration for a ledger related to
                  a particular class of asset. The asset class

                  is defined by the either a scope spec `x/metadata`, or nft
                  class `x/nft`. Ultimately, the configuration will

                  assist in verifying the types that are associated with
                  particular ledger entries.
            title: QueryLedgerClassResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: ledger_class_id
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/class/{ledger_class_id}/bucket_types:
    get:
      summary: >-
        LedgerClassBucketTypes returns the bucket types for a given ledger class
        id.
      operationId: LedgerClassBucketTypes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              bucket_types:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      format: int32
                      description: Unique ID for the bucket type (eg. 1, 2, 3, etc.).
                    code:
                      type: string
                      description: >-
                        Code for the bucket type (eg. "PRINCIPAL", "INTEREST",
                        "FEE", "OTHER").
                    description:
                      type: string
                      description: >-
                        Description of the bucket type (eg. "Principal",
                        "Interest", "Fee", "Other").
                  description: >-
                    LedgerClassBucketType represents a bucket type for a ledger
                    class.
            title: QueryLedgerClassBucketTypesResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: ledger_class_id
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/class/{ledger_class_id}/entry_types:
    get:
      summary: >-
        LedgerClassEntryTypes returns the entry types for a given ledger class
        id.
      operationId: LedgerClassEntryTypes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              entry_types:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      format: int32
                      description: Unique ID for the entry type (eg. 1, 2, 3, etc.).
                    code:
                      type: string
                      description: >-
                        Code for the entry type (eg. "DISBURSEMENT", "PAYMENT",
                        "ADJUSTMENT", "INTEREST", "FEE", "OTHER").
                    description:
                      type: string
                      description: >-
                        Description of the entry type (eg. "Disbursement",
                        "Payment", "Adjustment", "Interest", "Fee", "Other").
                  description: >-
                    LedgerClassEntryType defines the types of possible ledger
                    entries for a given asset class. These type codes allow

                    for minimal data storage while providing a human readable
                    description of the entry type.
            title: QueryLedgerClassEntryTypesResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: ledger_class_id
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/class/{ledger_class_id}/status_types:
    get:
      summary: >-
        LedgerClassStatusTypes returns the status types for a given ledger class
        id.
      operationId: LedgerClassStatusTypes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              status_types:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: integer
                      format: int32
                      description: Unique ID for the status type (eg. 1, 2, 3, etc.).
                    code:
                      type: string
                      description: >-
                        Code for the status type (eg. "IN_REPAYMENT",
                        "IN_FORECLOSURE", "FORBEARANCE", "DEFERMENT",
                        "BANKRUPTCY",

                        "CLOSED", "CANCELLED", "SUSPENDED", "OTHER").
                    description:
                      type: string
                      description: >-
                        Description of the status type (eg. "In Repayment", "In
                        Foreclosure", "Forbearance", "Deferment", "Bankruptcy",

                        "Closed", "Cancelled", "Suspended", "Other").
                  description: >-
                    LedgerClassStatusType defines the status types for a ledger
                    class.

                    These status types are used to track the status of
                    underlying loan throughout the loan life cycle.
            title: QueryLedgerClassStatusTypesResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: ledger_class_id
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/config:
    get:
      summary: Ledgers returns a paginated list of ledgers.
      operationId: Ledgers
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              ledgers:
                type: array
                items:
                  type: object
                  properties:
                    key:
                      description: The ledger key identifying the ledger.
                      type: object
                      properties:
                        nft_id:
                          type: string
                          description: >-
                            Identifier for the nft that this ledger is linked
                            to.

                            This could be a `x/metadata` scope id or an `x/nft`
                            nft id.

                            In order to create a ledger for an nft, the nft
                            class must be registered in the ledger module as a
                            LedgerClass.
                        asset_class_id:
                          type: string
                          description: Scope Specification ID or NFT Class ID.
                    ledger_class_id:
                      type: string
                      description: The ledger class ID for the ledger.
                    status_type_id:
                      type: integer
                      format: int32
                      description: The status of the ledger.
                    next_pmt_date:
                      type: integer
                      format: int32
                      description: The next payment date (days since epoch).
                    next_pmt_amt:
                      type: string
                      description: >-
                        The next payment amount.

                        The units of this field are defined by the denom field
                        in this ledger's class.
                    interest_rate:
                      type: integer
                      format: int32
                      description: >-
                        The interest rate. Min = 0, Max = 100,000,000 = 100%,
                        e.g. 4,321,987 = 4.321987%.
                    maturity_date:
                      type: integer
                      format: int32
                      description: The maturity date (days since epoch).
                    interest_day_count_convention:
                      description: The day count convention for interest calculations.
                      type: string
                      enum:
                        - DAY_COUNT_CONVENTION_UNSPECIFIED
                        - DAY_COUNT_CONVENTION_ACTUAL_365
                        - DAY_COUNT_CONVENTION_ACTUAL_360
                        - DAY_COUNT_CONVENTION_THIRTY_360
                        - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
                        - DAY_COUNT_CONVENTION_DAYS_365
                        - DAY_COUNT_CONVENTION_DAYS_360
                        - DAY_COUNT_CONVENTION_NOT_DEFINED
                      default: DAY_COUNT_CONVENTION_UNSPECIFIED
                    interest_accrual_method:
                      description: The interest accrual method for interest calculations.
                      type: string
                      enum:
                        - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
                        - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
                        - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
                        - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
                        - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
                        - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
                        - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
                        - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
                        - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
                      default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
                    payment_frequency:
                      description: The payment frequency.
                      type: string
                      enum:
                        - PAYMENT_FREQUENCY_UNSPECIFIED
                        - PAYMENT_FREQUENCY_DAILY
                        - PAYMENT_FREQUENCY_WEEKLY
                        - PAYMENT_FREQUENCY_MONTHLY
                        - PAYMENT_FREQUENCY_QUARTERLY
                        - PAYMENT_FREQUENCY_ANNUALLY
                        - PAYMENT_FREQUENCY_NOT_DEFINED
                      default: PAYMENT_FREQUENCY_UNSPECIFIED
                  description: Ledger defines an servicing ledger for an asset.
                description: List of ledgers.
              pagination:
                description: pagination is the pagination details for this response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: QueryLedgersResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Ledger
  /provenance/ledger/v1/config/{key.asset_class_id}/{key.nft_id}:
    get:
      summary: Ledger returns the ledger for a given ledger key.
      operationId: Ledger
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              ledger:
                description: The ledger data.
                type: object
                properties:
                  key:
                    description: The ledger key identifying the ledger.
                    type: object
                    properties:
                      nft_id:
                        type: string
                        description: >-
                          Identifier for the nft that this ledger is linked to.

                          This could be a `x/metadata` scope id or an `x/nft`
                          nft id.

                          In order to create a ledger for an nft, the nft class
                          must be registered in the ledger module as a
                          LedgerClass.
                      asset_class_id:
                        type: string
                        description: Scope Specification ID or NFT Class ID.
                  ledger_class_id:
                    type: string
                    description: The ledger class ID for the ledger.
                  status_type_id:
                    type: integer
                    format: int32
                    description: The status of the ledger.
                  next_pmt_date:
                    type: integer
                    format: int32
                    description: The next payment date (days since epoch).
                  next_pmt_amt:
                    type: string
                    description: >-
                      The next payment amount.

                      The units of this field are defined by the denom field in
                      this ledger's class.
                  interest_rate:
                    type: integer
                    format: int32
                    description: >-
                      The interest rate. Min = 0, Max = 100,000,000 = 100%, e.g.
                      4,321,987 = 4.321987%.
                  maturity_date:
                    type: integer
                    format: int32
                    description: The maturity date (days since epoch).
                  interest_day_count_convention:
                    description: The day count convention for interest calculations.
                    type: string
                    enum:
                      - DAY_COUNT_CONVENTION_UNSPECIFIED
                      - DAY_COUNT_CONVENTION_ACTUAL_365
                      - DAY_COUNT_CONVENTION_ACTUAL_360
                      - DAY_COUNT_CONVENTION_THIRTY_360
                      - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
                      - DAY_COUNT_CONVENTION_DAYS_365
                      - DAY_COUNT_CONVENTION_DAYS_360
                      - DAY_COUNT_CONVENTION_NOT_DEFINED
                    default: DAY_COUNT_CONVENTION_UNSPECIFIED
                  interest_accrual_method:
                    description: The interest accrual method for interest calculations.
                    type: string
                    enum:
                      - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
                      - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
                      - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
                      - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
                      - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
                      - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
                      - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
                      - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
                      - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
                    default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
                  payment_frequency:
                    description: The payment frequency.
                    type: string
                    enum:
                      - PAYMENT_FREQUENCY_UNSPECIFIED
                      - PAYMENT_FREQUENCY_DAILY
                      - PAYMENT_FREQUENCY_WEEKLY
                      - PAYMENT_FREQUENCY_MONTHLY
                      - PAYMENT_FREQUENCY_QUARTERLY
                      - PAYMENT_FREQUENCY_ANNUALLY
                      - PAYMENT_FREQUENCY_NOT_DEFINED
                    default: PAYMENT_FREQUENCY_UNSPECIFIED
            title: QueryLedgerResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: Scope Specification ID or NFT Class ID.
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an nft, the nft class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/entries/{key.asset_class_id}/{key.nft_id}:
    get:
      summary: LedgerEntries returns the entries for a given ledger key.
      operationId: LedgerEntries
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              entries:
                type: array
                items:
                  type: object
                  properties:
                    correlation_id:
                      type: string
                      description: >-
                        The correlation ID for tracking ledger entries with
                        external systems (max 50 characters).
                    reverses_correlation_id:
                      type: string
                      description: >-
                        If this entry reverses another entry, the correlation ID
                        of the entry it reverses.
                    is_void:
                      type: boolean
                      description: >-
                        If true, this entry is a void and should not be included
                        in the ledger balance calculations.
                    sequence:
                      type: integer
                      format: int64
                      description: >-
                        Sequence number of the ledger entry (less than 300).
                        This field is used to maintain the correct

                        order of entries when multiple entries share the same
                        effective date. Entries are sorted first

                        by effective date, then by sequence.
                    entry_type_id:
                      type: integer
                      format: int32
                      description: >-
                        The type of ledger entry specified by the
                        LedgerClassEntryType.id.
                    posted_date:
                      type: integer
                      format: int32
                      description: The posted date (days since epoch).
                    effective_date:
                      type: integer
                      format: int32
                      description: The effective date (days since epoch).
                    total_amt:
                      type: string
                      description: >-
                        The total amount of the ledger entry.

                        The units of this field are defined by the denom field
                        in this ledger's class.
                    applied_amounts:
                      type: array
                      items:
                        type: object
                        properties:
                          bucket_type_id:
                            type: integer
                            format: int32
                            description: >-
                              The bucket type specified by the
                              LedgerClassBucketType.id.
                          applied_amt:
                            type: string
                            description: >-
                              The amount applied to the bucket.

                              The units of this field are defined by the denom
                              field in this ledger's class.
                        description: >-
                          LedgerBucketAmount is the amount applied to a bucket.
                          Applications to a bucket increase or

                          decrease the balance of the bucket.
                      description: >-
                        Applied amounts represent how the entry affects
                        different buckets.
                    balance_amounts:
                      type: array
                      items:
                        type: object
                        properties:
                          bucket_type_id:
                            type: integer
                            format: int32
                            description: >-
                              The bucket type specified by the
                              LedgerClassBucketType.id.
                          balance_amt:
                            type: string
                            description: >-
                              The balance of the bucket.

                              The units of this field are defined by the denom
                              field in this ledger's class.
                        description: >-
                          BucketBalance represents the balance for a specific
                          bucket type.
                      description: >-
                        Bucket balances represent the current state of funds in
                        each bucket.
                  description: >-
                    LedgerEntry is an single entry in the ledger. An entry would
                    be a payment, disbursement, adjustment, etc...
                description: The ledger entries.
            title: QueryLedgerEntriesResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: Scope Specification ID or NFT Class ID.
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an nft, the nft class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/ledger/{key.asset_class_id}/{key.nft_id}/balances/{as_of_date}:
    get:
      summary: >-
        LedgerBalancesAsOf returns the balances for a specific NFT as of a given
        date.
      operationId: LedgerBalancesAsOf
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              bucket_balances:
                type: array
                items:
                  type: object
                  properties:
                    bucket_type_id:
                      type: integer
                      format: int32
                      description: >-
                        The bucket type specified by the
                        LedgerClassBucketType.id.
                    balance_amt:
                      type: string
                      description: >-
                        The balance of the bucket.

                        The units of this field are defined by the denom field
                        in this ledger's class.
                  description: >-
                    BucketBalance represents the balance for a specific bucket
                    type.
                description: >-
                  BucketBalances represents the current balances for principal,
                  interest, and other amounts.
            title: QueryLedgerBalancesAsOfResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: Scope Specification ID or NFT Class ID.
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an nft, the nft class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
        - name: as_of_date
          description: 'The date to query balances as of (format: YYYY-MM-DD).'
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/ledger/{key.asset_class_id}/{key.nft_id}/entry/{correlation_id}:
    get:
      summary: LedgerEntry returns a specific ledger entry for an NFT.
      operationId: LedgerEntry
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              entry:
                description: The ledger entry.
                type: object
                properties:
                  correlation_id:
                    type: string
                    description: >-
                      The correlation ID for tracking ledger entries with
                      external systems (max 50 characters).
                  reverses_correlation_id:
                    type: string
                    description: >-
                      If this entry reverses another entry, the correlation ID
                      of the entry it reverses.
                  is_void:
                    type: boolean
                    description: >-
                      If true, this entry is a void and should not be included
                      in the ledger balance calculations.
                  sequence:
                    type: integer
                    format: int64
                    description: >-
                      Sequence number of the ledger entry (less than 300). This
                      field is used to maintain the correct

                      order of entries when multiple entries share the same
                      effective date. Entries are sorted first

                      by effective date, then by sequence.
                  entry_type_id:
                    type: integer
                    format: int32
                    description: >-
                      The type of ledger entry specified by the
                      LedgerClassEntryType.id.
                  posted_date:
                    type: integer
                    format: int32
                    description: The posted date (days since epoch).
                  effective_date:
                    type: integer
                    format: int32
                    description: The effective date (days since epoch).
                  total_amt:
                    type: string
                    description: >-
                      The total amount of the ledger entry.

                      The units of this field are defined by the denom field in
                      this ledger's class.
                  applied_amounts:
                    type: array
                    items:
                      type: object
                      properties:
                        bucket_type_id:
                          type: integer
                          format: int32
                          description: >-
                            The bucket type specified by the
                            LedgerClassBucketType.id.
                        applied_amt:
                          type: string
                          description: >-
                            The amount applied to the bucket.

                            The units of this field are defined by the denom
                            field in this ledger's class.
                      description: >-
                        LedgerBucketAmount is the amount applied to a bucket.
                        Applications to a bucket increase or

                        decrease the balance of the bucket.
                    description: >-
                      Applied amounts represent how the entry affects different
                      buckets.
                  balance_amounts:
                    type: array
                    items:
                      type: object
                      properties:
                        bucket_type_id:
                          type: integer
                          format: int32
                          description: >-
                            The bucket type specified by the
                            LedgerClassBucketType.id.
                        balance_amt:
                          type: string
                          description: >-
                            The balance of the bucket.

                            The units of this field are defined by the denom
                            field in this ledger's class.
                      description: >-
                        BucketBalance represents the balance for a specific
                        bucket type.
                    description: >-
                      Bucket balances represent the current state of funds in
                      each bucket.
            title: QueryLedgerEntryResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: Scope Specification ID or NFT Class ID.
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an nft, the nft class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
        - name: correlation_id
          description: >-
            The correlation ID of the entry to query (free-form string up to 50
            characters).
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/ledger/{key.asset_class_id}/{key.nft_id}/settlements:
    get:
      summary: LedgerSettlements returns all settlements for a ledger.
      operationId: LedgerSettlements
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              settlements:
                type: array
                items:
                  type: object
                  properties:
                    amount:
                      description: The amount to be transferred.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    recipient_address:
                      type: string
                      description: The recipient's blockchain address.
                    status:
                      description: The current status of the funding transfer.
                      type: string
                      enum:
                        - FUNDING_TRANSFER_STATUS_UNSPECIFIED
                        - FUNDING_TRANSFER_STATUS_PENDING
                        - FUNDING_TRANSFER_STATUS_PROCESSING
                        - FUNDING_TRANSFER_STATUS_COMPLETED
                        - FUNDING_TRANSFER_STATUS_FAILED
                      default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
                    memo:
                      type: string
                      description: An optional memo or note for the transaction.
                  description: >-
                    SettlementInstruction represents blockchain-specific
                    settlement instructions.
            title: QueryLedgerSettlementsResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: Scope Specification ID or NFT Class ID.
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an nft, the nft class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/ledger/v1/ledger/{key.asset_class_id}/{key.nft_id}/settlements/{correlation_id}:
    get:
      summary: LedgerSettlementsByCorrelationID returns settlements by correlation id.
      operationId: LedgerSettlementsByCorrelationID
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              settlements:
                type: array
                items:
                  type: object
                  properties:
                    amount:
                      description: The amount to be transferred.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    recipient_address:
                      type: string
                      description: The recipient's blockchain address.
                    status:
                      description: The current status of the funding transfer.
                      type: string
                      enum:
                        - FUNDING_TRANSFER_STATUS_UNSPECIFIED
                        - FUNDING_TRANSFER_STATUS_PENDING
                        - FUNDING_TRANSFER_STATUS_PROCESSING
                        - FUNDING_TRANSFER_STATUS_COMPLETED
                        - FUNDING_TRANSFER_STATUS_FAILED
                      default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
                    memo:
                      type: string
                      description: An optional memo or note for the transaction.
                  description: >-
                    SettlementInstruction represents blockchain-specific
                    settlement instructions.
            title: QueryLedgerSettlementsByCorrelationIDResponse
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: Scope Specification ID or NFT Class ID.
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an nft, the nft class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
        - name: correlation_id
          description: The correlation ID to query settlements for.
          in: path
          required: true
          type: string
      tags:
        - Ledger
  /provenance/marker/v1/accesscontrol/{id}:
    get:
      summary: query for access records on an account
      operationId: Access
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              accounts:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                    permissions:
                      type: array
                      items:
                        type: string
                        enum:
                          - ACCESS_UNSPECIFIED
                          - ACCESS_MINT
                          - ACCESS_BURN
                          - ACCESS_DEPOSIT
                          - ACCESS_WITHDRAW
                          - ACCESS_DELETE
                          - ACCESS_ADMIN
                          - ACCESS_TRANSFER
                          - ACCESS_FORCE_TRANSFER
                        default: ACCESS_UNSPECIFIED
                        description: >-
                          Access defines the different types of permissions that
                          a marker supports granting to an address.

                           - ACCESS_UNSPECIFIED: ACCESS_UNSPECIFIED defines a no-op vote option.
                           - ACCESS_MINT: ACCESS_MINT is the ability to increase the supply of a marker.
                           - ACCESS_BURN: ACCESS_BURN is the ability to decrease the supply of the marker using coin held by the marker.
                           - ACCESS_DEPOSIT: ACCESS_DEPOSIT is the ability to transfer funds from another account to this marker account
                          or to set a reference to this marker in the
                          metadata/scopes module.
                           - ACCESS_WITHDRAW: ACCESS_WITHDRAW is the ability to transfer funds from this marker account to another account
                          or to remove a reference to this marker in the
                          metadata/scopes module.
                           - ACCESS_DELETE: ACCESS_DELETE is the ability to move a proposed, finalized or active marker into the cancelled state.
                          This access also allows cancelled markers to be marked
                          for deletion.
                           - ACCESS_ADMIN: ACCESS_ADMIN is the ability to add access grants for accounts to the list of marker permissions.
                          This access also gives the ability to update the
                          marker's denom metadata.
                           - ACCESS_TRANSFER: ACCESS_TRANSFER is the ability to manage transfer settings and broker transfers of the marker.
                          Accounts with this access can:
                           - Update the marker's required attributes.
                           - Update the send-deny list.
                           - Use the transfer or bank send endpoints to move marker funds out of their own account.
                          This access right is only supported on RESTRICTED
                          markers.
                           - ACCESS_FORCE_TRANSFER: ACCESS_FORCE_TRANSFER is the ability to transfer restricted coins from a 3rd-party account without their signature.
                          This access right is only supported on RESTRICTED
                          markers and only has meaning when
                          allow_forced_transfer is true.
                  description: >-
                    AccessGrant associates a collection of permissions with an
                    address for delegated marker account control.
            description: >-
              QueryAccessResponse is the response type for the
              Query/MarkerAccess method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: address or denom for the marker
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/marker/v1/accountdata/{denom}:
    get:
      summary: query for account data associated with a denom
      operationId: MarkerAccountData
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              value:
                type: string
                description: The accountdata for the requested denom.
            title: >-
              QueryAccountDataResponse is the response type for the
              Query/AccountData
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: denom
          description: The denomination to look up.
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/marker/v1/all:
    get:
      summary: Returns a list of all markers on the blockchain
      operationId: AllMarkers
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              markers:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAllMarkersResponse is the response type for the
              Query/AllMarkers method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: status
          description: |-
            Optional status to filter request

             - MARKER_STATUS_UNSPECIFIED: MARKER_STATUS_UNSPECIFIED - Unknown/Invalid Marker Status
             - MARKER_STATUS_PROPOSED: MARKER_STATUS_PROPOSED - Initial configuration period, updates allowed, token supply not created.
             - MARKER_STATUS_FINALIZED: MARKER_STATUS_FINALIZED - Configuration finalized, ready for supply creation
             - MARKER_STATUS_ACTIVE: MARKER_STATUS_ACTIVE - Supply is created, rules are in force.
             - MARKER_STATUS_CANCELLED: MARKER_STATUS_CANCELLED - Marker has been cancelled, pending destroy
             - MARKER_STATUS_DESTROYED: MARKER_STATUS_DESTROYED - Marker supply has all been recalled, marker is considered destroyed and no further
            actions allowed.
          in: query
          required: false
          type: string
          enum:
            - MARKER_STATUS_UNSPECIFIED
            - MARKER_STATUS_PROPOSED
            - MARKER_STATUS_FINALIZED
            - MARKER_STATUS_ACTIVE
            - MARKER_STATUS_CANCELLED
            - MARKER_STATUS_DESTROYED
          default: MARKER_STATUS_UNSPECIFIED
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Marker
  /provenance/marker/v1/detail/{id}:
    get:
      summary: query for a single marker by denom or address
      operationId: Marker
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              marker:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := ptypes.MarshalAny(foo)
                       ...
                       foo := &pb.Foo{}
                       if err := ptypes.UnmarshalAny(any, foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".



                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
            description: >-
              QueryMarkerResponse is the response type for the Query/Marker
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: the address or denom of the marker
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/marker/v1/escrow/{id}:
    get:
      summary: query for coins on a marker account
      operationId: Escrow
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              escrow:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
            description: >-
              QueryEscrowResponse is the response type for the
              Query/MarkerEscrow method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: address or denom for the marker
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/marker/v1/getdenommetadata/{denom}:
    get:
      summary: query for access records on an account
      operationId: MarkerDenomMetadata
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              metadata:
                type: object
                properties:
                  description:
                    type: string
                  denom_units:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                          description: >-
                            denom represents the string name of the given denom
                            unit (e.g uatom).
                        exponent:
                          type: integer
                          format: int64
                          description: >-
                            exponent represents power of 10 exponent that one
                            must

                            raise the base_denom to in order to equal the given
                            DenomUnit's denom

                            1 denom = 10^exponent base_denom

                            (e.g. with a base_denom of uatom, one can create a
                            DenomUnit of 'atom' with

                            exponent = 6, thus: 1 atom = 10^6 uatom).
                        aliases:
                          type: array
                          items:
                            type: string
                          title: >-
                            aliases is a list of string aliases for the given
                            denom
                      description: |-
                        DenomUnit represents a struct that describes a given
                        denomination unit of the basic token.
                    title: >-
                      denom_units represents the list of DenomUnit's for a given
                      coin
                  base:
                    type: string
                    description: >-
                      base represents the base denom (should be the DenomUnit
                      with exponent = 0).
                  display:
                    type: string
                    description: |-
                      display indicates the suggested denom that should be
                      displayed in clients.
                  name:
                    type: string
                    title: 'name defines the name of the token (eg: Cosmos Atom)'
                  symbol:
                    type: string
                    description: >-
                      symbol is the token symbol usually shown on exchanges (eg:
                      ATOM). This can

                      be the same as the display.
                  uri:
                    type: string
                    description: >-
                      URI to a document (on or off-chain) that contains
                      additional information. Optional.
                  uri_hash:
                    type: string
                    description: >-
                      URIHash is a sha256 hash of a document pointed by URI.
                      It's used to verify that

                      the document didn't change. Optional.
                description: |-
                  Metadata represents a struct that describes
                  a basic token.
            title: >-
              QueryDenomMetadataResponse is the response type for the
              Query/DenomMetadata
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: denom
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/marker/v1/holding/{id}:
    get:
      summary: query for all accounts holding the given marker coins
      operationId: Holding
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              balances:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: address is the address of the balance holder.
                    coins:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: coins defines the different coins this balance holds.
                  title: >-
                    Balance defines an account address and balance pair used in
                    queries for accounts holding a marker
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryHoldingResponse is the response type for the
              Query/MarkerHolders method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: the address or denom of the marker
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Marker
  /provenance/marker/v1/netassetvalues/{id}:
    get:
      summary: NetAssetValues returns net asset values for marker
      operationId: NetAssetValues
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              net_asset_values:
                type: array
                items:
                  type: object
                  properties:
                    price:
                      title: price is the complete value of the asset's volume
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    volume:
                      type: string
                      format: uint64
                      title: >-
                        volume is the number of tokens of the marker that were
                        purchased for the price
                    updated_block_height:
                      type: string
                      format: uint64
                      title: updated_block_height is the block height of last update
                  title: NetAssetValue defines a marker's net asset value
                title: net asset values for marker denom
            description: >-
              QueryNetAssetValuesRequest is the response type for the
              Query/NetAssetValues method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: address or denom for the marker
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/marker/v1/params:
    get:
      summary: Params queries the parameters of x/bank module.
      operationId: MarkerParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  max_total_supply:
                    type: string
                    format: uint64
                    title: >-
                      Deprecated: Prefer to use `max_supply` instead. Maximum
                      amount of supply to allow a marker to be created with
                  enable_governance:
                    type: boolean
                    description: >-
                      indicates if governance based controls of markers is
                      allowed.
                  unrestricted_denom_regex:
                    type: string
                    title: >-
                      a regular expression used to validate marker denom values
                      from normal create requests (governance

                      requests are only subject to platform coin validation
                      denom expression)
                  max_supply:
                    type: string
                    title: >-
                      maximum amount of supply to allow a marker to be created
                      with
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Marker
  /provenance/marker/v1/supply/{id}:
    get:
      summary: >-
        query for supply of coin on a marker account

        This endpoint returns the initial/target supply from marker account, not
        actual circulating supply.

        Deprecated: This query is deprecated in favor of the bank module's
        SupplyOf query.
      operationId: Supply
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              amount:
                description: amount is the supply of the marker.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
            description: >-
              QuerySupplyResponse is the response type for the
              Query/MarkerSupply method.

              Deprecated: This returns initial/target supply. Use the bank
              module's SupplyOf query for actual circulating supply.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: address or denom for the marker
          in: path
          required: true
          type: string
      tags:
        - Marker
  /provenance/metadata/v1/accountdata/{metadata_addr}:
    get:
      summary: |-
        AccountData gets the account data associated with a metadata address.
        Currently, only scope ids are supported.
      operationId: MetadataAccountData
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              value:
                type: string
                description: The accountdata for the requested metadata address.
            description: >-
              AccountDataResponse is the response type for the Query/AccountData
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: metadata_addr
          description: |-
            The metadata address to look up.
            Currently, only scope ids are supported.
          in: path
          required: true
          type: string
          format: byte
      tags:
        - Metadata
  /provenance/metadata/v1/addr/{addrs}:
    get:
      summary: GetByAddr retrieves metadata given any address(es).
      operationId: GetByAddr
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scopes:
                type: array
                items:
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      title: >-
                        Unique ID for this scope.  Implements sdk.Address
                        interface for use where addresses are required in Cosmos
                    specification_id:
                      type: string
                      format: byte
                      title: >-
                        the scope specification that contains the specifications
                        for data elements allowed within this scope
                    owners:
                      type: array
                      items:
                        type: object
                        properties:
                          address:
                            type: string
                            title: address of the account (on chain)
                          role:
                            title: >-
                              a role for this account within the context of the
                              processes used
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                          optional:
                            type: boolean
                            title: whether this party's signature is optional
                        title: >-
                          A Party is an address with/in a given role associated
                          with a contract
                      description: >-
                        These parties represent top level owners of the records
                        within.  These parties must sign any requests that
                        modify

                        the data within the scope.  These addresses are in union
                        with parties listed on the sessions.
                    data_access:
                      type: array
                      items:
                        type: string
                      description: >-
                        Addresses in this list are authorized to receive
                        off-chain data associated with this scope.
                    value_owner_address:
                      type: string
                      description: >-
                        The address that controls the value associated with this
                        scope.


                        The value owner is actually tracked by the bank module
                        using a coin with the denom "nft/<scope_id>".

                        The value owner can be changed using WriteScope or
                        anything that transfers funds, e.g. MsgSend.


                        During WriteScope:
                         - If this field is empty, it indicates that there should not be a change to the value owner.
                           I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                         - If this field has a value, the existing value owner will be looked up, and
                           - If there's already an existing value owner, they must be a signer,
                             and the coin will be transferred to the new value owner.
                           - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                             If the scope already exists, the owners must be signers (just like changing other fields).
                             If it's a new scope, there's no special signer limitations related to the value owner.
                    require_party_rollup:
                      type: boolean
                      description: >-
                        Whether all parties in this scope and its sessions must
                        be present in this scope's owners field.

                        This also enables use of optional=true scope owners and
                        session parties.
                  description: >-
                    Scope defines a root reference for a collection of records
                    owned by one or more parties.
                description: scopes contains any scopes that were requested and found.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session_id:
                      type: string
                      format: byte
                    specification_id:
                      type: string
                      format: byte
                      description: >-
                        unique id of the contract specification that was used to
                        create this session.
                    parties:
                      type: array
                      items:
                        type: object
                        properties:
                          address:
                            type: string
                            title: address of the account (on chain)
                          role:
                            title: >-
                              a role for this account within the context of the
                              processes used
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                          optional:
                            type: boolean
                            title: whether this party's signature is optional
                        title: >-
                          A Party is an address with/in a given role associated
                          with a contract
                      title: >-
                        parties is the set of identities that signed this
                        contract
                    name:
                      type: string
                      title: >-
                        name to associate with this session execution context,
                        typically classname
                    context:
                      type: string
                      format: byte
                      description: >-
                        context is a field for storing client specific data
                        associated with a session.
                    audit:
                      description: >-
                        Created by, updated by, timestamps, version number, and
                        related info.
                      type: object
                      properties:
                        created_date:
                          type: string
                          format: date-time
                          title: the date/time when this entry was created
                        created_by:
                          type: string
                          title: the address of the account that created this record
                        updated_date:
                          type: string
                          format: date-time
                          title: the date/time when this entry was last updated
                        updated_by:
                          type: string
                          title: the address of the account that modified this record
                        version:
                          type: integer
                          format: int64
                          title: >-
                            an optional version number that is incremented with
                            each update
                        message:
                          type: string
                          title: >-
                            an optional message associated with the
                            creation/update event
                      title: >-
                        AuditFields capture information about the last account
                        to make modifications and when they were made
                  description: >-
                    Session defines an execution context against a specific
                    specification instance.

                    The context will have a specification and set of parties
                    involved.


                    NOTE: When there are no more Records within a Scope that
                    reference a Session, the Session is removed.
                description: sessions contains any sessions that were requested and found.
              records:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      title: >-
                        name/identifier for this record.  Value must be unique
                        within the scope.  Also known as a Fact name
                    session_id:
                      type: string
                      format: byte
                      title: >-
                        id of the session context that was used to create this
                        record (use with filtered kvprefix iterator)
                    process:
                      title: >-
                        process contain information used to uniquely identify an
                        execution on or off chain that generated this record
                      type: object
                      properties:
                        address:
                          type: string
                          title: >-
                            the address of a smart contract used for this
                            process
                        hash:
                          type: string
                          title: the hash of an off-chain process used
                        name:
                          type: string
                          title: >-
                            a name associated with the process (type_name,
                            classname or smart contract common name)
                        method:
                          type: string
                          title: >-
                            method is a name or reference to a specific
                            operation (method) within a class/contract that was
                            invoked
                    inputs:
                      type: array
                      items:
                        type: object
                        properties:
                          name:
                            type: string
                            description: >-
                              Name value included to link back to the definition
                              spec.
                          record_id:
                            type: string
                            format: byte
                            title: >-
                              the address of a record on chain (For Established
                              Records)
                          hash:
                            type: string
                            title: >-
                              the hash of an off-chain piece of information (For
                              Proposed Records)
                          type_name:
                            type: string
                            title: from proposed fact structure to unmarshal
                          status:
                            title: >-
                              Indicates if this input was a recorded fact on
                              chain or just a given hashed input
                            type: string
                            enum:
                              - RECORD_INPUT_STATUS_UNSPECIFIED
                              - RECORD_INPUT_STATUS_PROPOSED
                              - RECORD_INPUT_STATUS_RECORD
                            default: RECORD_INPUT_STATUS_UNSPECIFIED
                            description: >-
                              - RECORD_INPUT_STATUS_UNSPECIFIED:
                              RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                              invalid/unknown input type
                               - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                               - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                        title: Tracks the inputs used to establish this record
                      title: >-
                        inputs used with the process to achieve the output on
                        this record
                    outputs:
                      type: array
                      items:
                        type: object
                        properties:
                          hash:
                            type: string
                            title: >-
                              Hash of the data output that was output/generated
                              for this record
                          status:
                            title: >-
                              Status of the process execution associated with
                              this output indicating success,failure, or pending
                            type: string
                            enum:
                              - RESULT_STATUS_UNSPECIFIED
                              - RESULT_STATUS_PASS
                              - RESULT_STATUS_SKIP
                              - RESULT_STATUS_FAIL
                            default: RESULT_STATUS_UNSPECIFIED
                            description: >-
                              - RESULT_STATUS_UNSPECIFIED:
                              RESULT_STATUS_UNSPECIFIED indicates an unset
                              condition
                               - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                               - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                               - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                        title: >-
                          RecordOutput encapsulates the output of a process
                          recorded on chain
                      title: >-
                        output(s) is the results of executing the process on the
                        given process indicated in this record
                    specification_id:
                      type: string
                      format: byte
                      description: >-
                        specification_id is the id of the record specification
                        that was used to create this record.
                  title: >-
                    A record (of fact) is attached to a session or each
                    consideration output from a contract
                description: records contains any records that were requested and found.
              scope_specs:
                type: array
                items:
                  type: object
                  properties:
                    specification_id:
                      type: string
                      format: byte
                      title: unique identifier for this specification on chain
                    description:
                      description: General information about this scope specification.
                      type: object
                      properties:
                        name:
                          type: string
                          description: A Name for this thing.
                        description:
                          type: string
                          description: A description of this thing.
                        website_url:
                          type: string
                          description: URL to find even more info.
                        icon_url:
                          type: string
                          description: URL of an icon.
                    owner_addresses:
                      type: array
                      items:
                        type: string
                      description: Addresses of the owners of this scope specification.
                    parties_involved:
                      type: array
                      items:
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                        title: >-
                          PartyType are the different roles parties on a
                          contract may use
                      title: >-
                        A list of parties that must be present on a scope (and
                        their associated roles)
                    contract_spec_ids:
                      type: array
                      items:
                        type: string
                        format: byte
                      description: >-
                        A list of contract specification ids allowed for a scope
                        based on this specification.
                  title: >-
                    ScopeSpecification defines the required parties, resources,
                    conditions, and consideration outputs for a contract
                description: >-
                  scope_specs contains any scope specifications that were
                  requested and found.
              contract_specs:
                type: array
                items:
                  type: object
                  properties:
                    specification_id:
                      type: string
                      format: byte
                      title: unique identifier for this specification on chain
                    description:
                      title: Description information for this contract specification
                      type: object
                      properties:
                        name:
                          type: string
                          description: A Name for this thing.
                        description:
                          type: string
                          description: A description of this thing.
                        website_url:
                          type: string
                          description: URL to find even more info.
                        icon_url:
                          type: string
                          description: URL of an icon.
                      description: >-
                        Description holds general information that is handy to
                        associate with a structure.
                    owner_addresses:
                      type: array
                      items:
                        type: string
                      title: Address of the account that owns this specificaiton
                    parties_involved:
                      type: array
                      items:
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                        title: >-
                          PartyType are the different roles parties on a
                          contract may use
                      title: >-
                        a list of party roles that must be fullfilled when
                        signing a transaction for this contract specification
                    resource_id:
                      type: string
                      format: byte
                      title: >-
                        the address of a record on chain that represents this
                        contract
                    hash:
                      type: string
                      title: the hash of contract binary (off-chain instance)
                    class_name:
                      type: string
                      title: name of the class/type of this contract executable
                  title: >-
                    ContractSpecification defines the required parties,
                    resources, conditions, and consideration outputs for a
                    contract
                description: >-
                  contract_specs contains any contract specifications that were
                  requested and found.
              record_specs:
                type: array
                items:
                  type: object
                  properties:
                    specification_id:
                      type: string
                      format: byte
                      title: unique identifier for this specification on chain
                    name:
                      type: string
                      title: >-
                        Name of Record that will be created when this
                        specification is used
                    inputs:
                      type: array
                      items:
                        type: object
                        properties:
                          name:
                            type: string
                            title: name for this input
                          type_name:
                            type: string
                            title: a type_name (typically a proto name or class_name)
                          record_id:
                            type: string
                            format: byte
                            title: >-
                              the address of a record on chain (For Established
                              Records)
                          hash:
                            type: string
                            title: >-
                              the hash of an off-chain piece of information (For
                              Proposed Records)
                        title: >-
                          InputSpecification defines a name, type_name, and
                          source reference (either on or off chain) to define an
                          input

                          parameter
                      title: >-
                        A set of inputs that must be satisified to apply this
                        RecordSpecification and create a Record
                    type_name:
                      type: string
                      title: >-
                        A type name for data associated with this record
                        (typically a class or proto name)
                    result_type:
                      title: >-
                        Type of result for this record specification (must be
                        RECORD or RECORD_LIST)
                      type: string
                      enum:
                        - DEFINITION_TYPE_UNSPECIFIED
                        - DEFINITION_TYPE_PROPOSED
                        - DEFINITION_TYPE_RECORD
                        - DEFINITION_TYPE_RECORD_LIST
                      default: DEFINITION_TYPE_UNSPECIFIED
                      description: >-
                        - DEFINITION_TYPE_UNSPECIFIED:
                        DEFINITION_TYPE_UNSPECIFIED indicates an unknown/invalid
                        value
                         - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                         - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                         - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                        the same name
                    responsible_parties:
                      type: array
                      items:
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                        title: >-
                          PartyType are the different roles parties on a
                          contract may use
                      title: Type of party responsible for this record
                  title: >-
                    RecordSpecification defines the specification for a Record
                    including allowed/required inputs/outputs
                description: >-
                  record_specs contains any record specifications that were
                  requested and found.
              not_found:
                type: array
                items:
                  type: string
                description: not_found contains any addrs requested but not found.
            description: >-
              GetByAddrResponse is the response type for the Query/GetByAddr RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: addrs
          description: ids are the metadata addresses of the things to look up.
          in: path
          required: true
          type: array
          items:
            type: string
          collectionFormat: csv
          minItems: 1
      tags:
        - Metadata
  /provenance/metadata/v1/contractspec/{specification_id}:
    get:
      summary: >-
        ContractSpecification returns a contract specification for the given
        specification id.
      description: >-
        The specification_id can either be a uuid, e.g.
        def6bc0a-c9dd-4874-948f-5206e6060a84, a bech32 contract

        specification address, e.g.
        contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn, or a bech32 record
        specification

        address, e.g.
        recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44. If
        it is a record specification

        address, then the contract specification that contains that record
        specification is looked up.


        By default, the record specifications for this contract specification
        are not included.

        Set include_record_specs to true to include them in the result.
      operationId: ContractSpecification
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              contract_specification:
                description: contract_specification is the wrapped contract specification.
                type: object
                properties:
                  specification:
                    description: >-
                      specification is the on-chain contract specification
                      message.
                    type: object
                    properties:
                      specification_id:
                        type: string
                        format: byte
                        title: unique identifier for this specification on chain
                      description:
                        title: >-
                          Description information for this contract
                          specification
                        type: object
                        properties:
                          name:
                            type: string
                            description: A Name for this thing.
                          description:
                            type: string
                            description: A description of this thing.
                          website_url:
                            type: string
                            description: URL to find even more info.
                          icon_url:
                            type: string
                            description: URL of an icon.
                        description: >-
                          Description holds general information that is handy to
                          associate with a structure.
                      owner_addresses:
                        type: array
                        items:
                          type: string
                        title: Address of the account that owns this specificaiton
                      parties_involved:
                        type: array
                        items:
                          type: string
                          enum:
                            - PARTY_TYPE_UNSPECIFIED
                            - PARTY_TYPE_ORIGINATOR
                            - PARTY_TYPE_SERVICER
                            - PARTY_TYPE_INVESTOR
                            - PARTY_TYPE_CUSTODIAN
                            - PARTY_TYPE_OWNER
                            - PARTY_TYPE_AFFILIATE
                            - PARTY_TYPE_OMNIBUS
                            - PARTY_TYPE_PROVENANCE
                            - PARTY_TYPE_CONTROLLER
                            - PARTY_TYPE_VALIDATOR
                            - PARTY_TYPE_GENERIC_1
                            - PARTY_TYPE_GENERIC_2
                            - PARTY_TYPE_GENERIC_3
                            - PARTY_TYPE_GENERIC_4
                            - PARTY_TYPE_GENERIC_5
                            - PARTY_TYPE_GENERIC_6
                          default: PARTY_TYPE_UNSPECIFIED
                          description: >-
                            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is
                            an error condition
                             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                          title: >-
                            PartyType are the different roles parties on a
                            contract may use
                        title: >-
                          a list of party roles that must be fullfilled when
                          signing a transaction for this contract specification
                      resource_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain that represents this
                          contract
                      hash:
                        type: string
                        title: the hash of contract binary (off-chain instance)
                      class_name:
                        type: string
                        title: name of the class/type of this contract executable
                    title: >-
                      ContractSpecification defines the required parties,
                      resources, conditions, and consideration outputs for a
                      contract
                  contract_spec_id_info:
                    description: >-
                      contract_spec_id_info contains information about the
                      id/address of the contract specification.
                    type: object
                    properties:
                      contract_spec_id:
                        type: string
                        format: byte
                        description: >-
                          contract_spec_id is the raw bytes of the contract
                          specification address.
                      contract_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          contract_spec_id_prefix is the prefix portion of the
                          contract_spec_id.
                      contract_spec_id_contract_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          contract_spec_id_contract_spec_uuid is the
                          contract_spec_uuid portion of the contract_spec_id.
                      contract_spec_addr:
                        type: string
                        description: >-
                          contract_spec_addr is the bech32 string version of the
                          contract_spec_id.
                      contract_spec_uuid:
                        type: string
                        description: >-
                          contract_spec_uuid is the uuid hex string of the
                          contract_spec_id_contract_spec_uuid.
              record_specifications:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain record specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        name:
                          type: string
                          title: >-
                            Name of Record that will be created when this
                            specification is used
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                title: name for this input
                              type_name:
                                type: string
                                title: >-
                                  a type_name (typically a proto name or
                                  class_name)
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                            title: >-
                              InputSpecification defines a name, type_name, and
                              source reference (either on or off chain) to
                              define an input

                              parameter
                          title: >-
                            A set of inputs that must be satisified to apply
                            this RecordSpecification and create a Record
                        type_name:
                          type: string
                          title: >-
                            A type name for data associated with this record
                            (typically a class or proto name)
                        result_type:
                          title: >-
                            Type of result for this record specification (must
                            be RECORD or RECORD_LIST)
                          type: string
                          enum:
                            - DEFINITION_TYPE_UNSPECIFIED
                            - DEFINITION_TYPE_PROPOSED
                            - DEFINITION_TYPE_RECORD
                            - DEFINITION_TYPE_RECORD_LIST
                          default: DEFINITION_TYPE_UNSPECIFIED
                          description: >-
                            - DEFINITION_TYPE_UNSPECIFIED:
                            DEFINITION_TYPE_UNSPECIFIED indicates an
                            unknown/invalid value
                             - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                             - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                             - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                            the same name
                        responsible_parties:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: Type of party responsible for this record
                      title: >-
                        RecordSpecification defines the specification for a
                        Record including allowed/required inputs/outputs
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordSpecificationWrapper contains a single record
                    specification and some extra identifiers for it.
                description: >-
                  record_specifications is any number or wrapped record
                  specifications associated with this contract_specification

                  (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  specification_id:
                    type: string
                    description: >-
                      specification_id can either be a uuid, e.g.
                      def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
                      specification

                      address, e.g.
                      contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

                      It can also be a record specification address, e.g.

                      recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
                  include_record_specs:
                    type: boolean
                    description: >-
                      include_record_specs is a flag for whether to include the
                      the record specifications of this contract specification

                      in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              ContractSpecificationResponse is the response type for the
              Query/ContractSpecification RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: specification_id
          description: >-
            specification_id can either be a uuid, e.g.
            def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
            specification

            address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

            It can also be a record specification address, e.g.

            recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          in: path
          required: true
          type: string
        - name: include_record_specs
          description: >-
            include_record_specs is a flag for whether to include the the record
            specifications of this contract specification

            in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/contractspec/{specification_id}/recordspec/{name}:
    get:
      summary: RecordSpecification returns a record specification for the given input.
      operationId: RecordSpecification2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              record_specification:
                description: record_specification is the wrapped record specification.
                type: object
                properties:
                  specification:
                    description: >-
                      specification is the on-chain record specification
                      message.
                    type: object
                    properties:
                      specification_id:
                        type: string
                        format: byte
                        title: unique identifier for this specification on chain
                      name:
                        type: string
                        title: >-
                          Name of Record that will be created when this
                          specification is used
                      inputs:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              title: name for this input
                            type_name:
                              type: string
                              title: >-
                                a type_name (typically a proto name or
                                class_name)
                            record_id:
                              type: string
                              format: byte
                              title: >-
                                the address of a record on chain (For
                                Established Records)
                            hash:
                              type: string
                              title: >-
                                the hash of an off-chain piece of information
                                (For Proposed Records)
                          title: >-
                            InputSpecification defines a name, type_name, and
                            source reference (either on or off chain) to define
                            an input

                            parameter
                        title: >-
                          A set of inputs that must be satisified to apply this
                          RecordSpecification and create a Record
                      type_name:
                        type: string
                        title: >-
                          A type name for data associated with this record
                          (typically a class or proto name)
                      result_type:
                        title: >-
                          Type of result for this record specification (must be
                          RECORD or RECORD_LIST)
                        type: string
                        enum:
                          - DEFINITION_TYPE_UNSPECIFIED
                          - DEFINITION_TYPE_PROPOSED
                          - DEFINITION_TYPE_RECORD
                          - DEFINITION_TYPE_RECORD_LIST
                        default: DEFINITION_TYPE_UNSPECIFIED
                        description: >-
                          - DEFINITION_TYPE_UNSPECIFIED:
                          DEFINITION_TYPE_UNSPECIFIED indicates an
                          unknown/invalid value
                           - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                           - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                           - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                          the same name
                      responsible_parties:
                        type: array
                        items:
                          type: string
                          enum:
                            - PARTY_TYPE_UNSPECIFIED
                            - PARTY_TYPE_ORIGINATOR
                            - PARTY_TYPE_SERVICER
                            - PARTY_TYPE_INVESTOR
                            - PARTY_TYPE_CUSTODIAN
                            - PARTY_TYPE_OWNER
                            - PARTY_TYPE_AFFILIATE
                            - PARTY_TYPE_OMNIBUS
                            - PARTY_TYPE_PROVENANCE
                            - PARTY_TYPE_CONTROLLER
                            - PARTY_TYPE_VALIDATOR
                            - PARTY_TYPE_GENERIC_1
                            - PARTY_TYPE_GENERIC_2
                            - PARTY_TYPE_GENERIC_3
                            - PARTY_TYPE_GENERIC_4
                            - PARTY_TYPE_GENERIC_5
                            - PARTY_TYPE_GENERIC_6
                          default: PARTY_TYPE_UNSPECIFIED
                          description: >-
                            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is
                            an error condition
                             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                          title: >-
                            PartyType are the different roles parties on a
                            contract may use
                        title: Type of party responsible for this record
                    title: >-
                      RecordSpecification defines the specification for a Record
                      including allowed/required inputs/outputs
                  record_spec_id_info:
                    description: >-
                      record_spec_id_info contains information about the
                      id/address of the record specification.
                    type: object
                    properties:
                      record_spec_id:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id is the raw bytes of the record
                          specification address.
                      record_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id_prefix is the prefix portion of the
                          record_spec_id.
                      record_spec_id_contract_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id_contract_spec_uuid is the
                          contract_spec_uuid portion of the record_spec_id.
                      record_spec_id_hashed_name:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id_hashed_name is the hashed name portion
                          of the record_spec_id.
                      record_spec_addr:
                        type: string
                        description: >-
                          record_spec_addr is the bech32 string version of the
                          record_spec_id.
                      contract_spec_id_info:
                        description: >-
                          contract_spec_id_info is information about the
                          contract spec id referenced in the record_spec_id.
                        type: object
                        properties:
                          contract_spec_id:
                            type: string
                            format: byte
                            description: >-
                              contract_spec_id is the raw bytes of the contract
                              specification address.
                          contract_spec_id_prefix:
                            type: string
                            format: byte
                            description: >-
                              contract_spec_id_prefix is the prefix portion of
                              the contract_spec_id.
                          contract_spec_id_contract_spec_uuid:
                            type: string
                            format: byte
                            description: >-
                              contract_spec_id_contract_spec_uuid is the
                              contract_spec_uuid portion of the
                              contract_spec_id.
                          contract_spec_addr:
                            type: string
                            description: >-
                              contract_spec_addr is the bech32 string version of
                              the contract_spec_id.
                          contract_spec_uuid:
                            type: string
                            description: >-
                              contract_spec_uuid is the uuid hex string of the
                              contract_spec_id_contract_spec_uuid.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  specification_id:
                    type: string
                    description: >-
                      specification_id can either be a uuid, e.g.
                      def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
                      specification

                      address, e.g.
                      contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

                      It can also be a record specification address, e.g.

                      recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
                  name:
                    type: string
                    description: >-
                      name is the name of the record to look up.

                      It is required if the specification_id is a uuid or
                      contract specification address.

                      It is ignored if the specification_id is a record
                      specification address.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordSpecificationResponse is the response type for the
              Query/RecordSpecification RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: specification_id
          description: >-
            specification_id can either be a uuid, e.g.
            def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
            specification

            address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

            It can also be a record specification address, e.g.

            recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          in: path
          required: true
          type: string
        - name: name
          description: >-
            name is the name of the record to look up.

            It is required if the specification_id is a uuid or contract
            specification address.

            It is ignored if the specification_id is a record specification
            address.
          in: path
          required: true
          type: string
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/contractspec/{specification_id}/recordspecs:
    get:
      summary: >-
        RecordSpecificationsForContractSpecification returns the record
        specifications for the given input.
      description: >-
        The specification_id can either be a uuid, e.g.
        def6bc0a-c9dd-4874-948f-5206e6060a84, a bech32 contract

        specification address, e.g.
        contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn, or a bech32 record
        specification

        address, e.g.
        recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44. If
        it is a record specification

        address, then the contract specification that contains that record
        specification is used.
      operationId: RecordSpecificationsForContractSpecification
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              record_specifications:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain record specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        name:
                          type: string
                          title: >-
                            Name of Record that will be created when this
                            specification is used
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                title: name for this input
                              type_name:
                                type: string
                                title: >-
                                  a type_name (typically a proto name or
                                  class_name)
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                            title: >-
                              InputSpecification defines a name, type_name, and
                              source reference (either on or off chain) to
                              define an input

                              parameter
                          title: >-
                            A set of inputs that must be satisified to apply
                            this RecordSpecification and create a Record
                        type_name:
                          type: string
                          title: >-
                            A type name for data associated with this record
                            (typically a class or proto name)
                        result_type:
                          title: >-
                            Type of result for this record specification (must
                            be RECORD or RECORD_LIST)
                          type: string
                          enum:
                            - DEFINITION_TYPE_UNSPECIFIED
                            - DEFINITION_TYPE_PROPOSED
                            - DEFINITION_TYPE_RECORD
                            - DEFINITION_TYPE_RECORD_LIST
                          default: DEFINITION_TYPE_UNSPECIFIED
                          description: >-
                            - DEFINITION_TYPE_UNSPECIFIED:
                            DEFINITION_TYPE_UNSPECIFIED indicates an
                            unknown/invalid value
                             - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                             - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                             - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                            the same name
                        responsible_parties:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: Type of party responsible for this record
                      title: >-
                        RecordSpecification defines the specification for a
                        Record including allowed/required inputs/outputs
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordSpecificationWrapper contains a single record
                    specification and some extra identifiers for it.
                description: >-
                  record_specifications is any number of wrapped record
                  specifications associated with this contract_specification.
              contract_specification_uuid:
                type: string
                description: >-
                  contract_specification_uuid is the uuid of this contract
                  specification.
              contract_specification_addr:
                type: string
                description: >-
                  contract_specification_addr is the contract specification
                  address as a bech32 encoded string.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  specification_id:
                    type: string
                    description: >-
                      specification_id can either be a uuid, e.g.
                      def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
                      specification

                      address, e.g.
                      contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

                      It can also be a record specification address, e.g.

                      recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordSpecificationsForContractSpecificationResponse is the
              response type for the

              Query/RecordSpecificationsForContractSpecification RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: specification_id
          description: >-
            specification_id can either be a uuid, e.g.
            def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
            specification

            address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

            It can also be a record specification address, e.g.

            recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          in: path
          required: true
          type: string
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/contractspecs/all:
    get:
      summary: ContractSpecificationsAll retrieves all contract specifications.
      operationId: ContractSpecificationsAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              contract_specifications:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain contract specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        description:
                          title: >-
                            Description information for this contract
                            specification
                          type: object
                          properties:
                            name:
                              type: string
                              description: A Name for this thing.
                            description:
                              type: string
                              description: A description of this thing.
                            website_url:
                              type: string
                              description: URL to find even more info.
                            icon_url:
                              type: string
                              description: URL of an icon.
                          description: >-
                            Description holds general information that is handy
                            to associate with a structure.
                        owner_addresses:
                          type: array
                          items:
                            type: string
                          title: Address of the account that owns this specificaiton
                        parties_involved:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: >-
                            a list of party roles that must be fullfilled when
                            signing a transaction for this contract
                            specification
                        resource_id:
                          type: string
                          format: byte
                          title: >-
                            the address of a record on chain that represents
                            this contract
                        hash:
                          type: string
                          title: the hash of contract binary (off-chain instance)
                        class_name:
                          type: string
                          title: name of the class/type of this contract executable
                      title: >-
                        ContractSpecification defines the required parties,
                        resources, conditions, and consideration outputs for a
                        contract
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    ContractSpecificationWrapper contains a single contract
                    specification and some extra identifiers for it.
                description: >-
                  contract_specifications are the wrapped contract
                  specifications.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              ContractSpecificationsAllResponse is the response type for the
              Query/ContractSpecificationsAll RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/locator/params:
    get:
      summary: >-
        OSLocatorParams returns all parameters for the object store locator sub
        module.
      operationId: OSLocatorParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  max_uri_length:
                    type: integer
                    format: int64
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              OSLocatorParamsResponse is the response type for the
              Query/OSLocatorParams RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/locator/scope/{scope_id}:
    get:
      summary: >-
        OSLocatorsByScope returns all ObjectStoreLocator entries for a for all
        signer's present in the specified scope.
      operationId: OSLocatorsByScope
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              locators:
                type: array
                items:
                  type: object
                  properties:
                    owner:
                      type: string
                      title: account address the endpoint is owned by
                    locator_uri:
                      type: string
                      title: locator endpoint uri
                    encryption_key:
                      type: string
                      title: owners encryption key address
                  description: >-
                    Defines an Locator object stored on chain, which represents
                    a owner( blockchain address) associated with a endpoint

                    uri for it's associated object store.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              OSLocatorsByScopeResponse is the response type for the
              Query/OSLocatorsByScope RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          in: path
          required: true
          type: string
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/locator/uri/{uri}:
    get:
      summary: >-
        OSLocatorsByURI returns all ObjectStoreLocator entries for a locator
        uri.
      operationId: OSLocatorsByURI
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              locators:
                type: array
                items:
                  type: object
                  properties:
                    owner:
                      type: string
                      title: account address the endpoint is owned by
                    locator_uri:
                      type: string
                      title: locator endpoint uri
                    encryption_key:
                      type: string
                      title: owners encryption key address
                  description: >-
                    Defines an Locator object stored on chain, which represents
                    a owner( blockchain address) associated with a endpoint

                    uri for it's associated object store.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  uri:
                    type: string
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              OSLocatorsByURIResponse is the response type for the
              Query/OSLocatorsByURI RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: uri
          in: path
          required: true
          type: string
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/locator/{owner}:
    get:
      summary: OSLocator returns an ObjectStoreLocator by its owner's address.
      operationId: OSLocator
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              locator:
                type: object
                properties:
                  owner:
                    type: string
                    title: account address the endpoint is owned by
                  locator_uri:
                    type: string
                    title: locator endpoint uri
                  encryption_key:
                    type: string
                    title: owners encryption key address
                description: >-
                  Defines an Locator object stored on chain, which represents a
                  owner( blockchain address) associated with a endpoint

                  uri for it's associated object store.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  owner:
                    type: string
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              OSLocatorResponse is the response type for the Query/OSLocator RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: owner
          in: path
          required: true
          type: string
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/locators/all:
    get:
      summary: OSAllLocators returns all ObjectStoreLocator entries.
      operationId: OSAllLocators
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              locators:
                type: array
                items:
                  type: object
                  properties:
                    owner:
                      type: string
                      title: account address the endpoint is owned by
                    locator_uri:
                      type: string
                      title: locator endpoint uri
                    encryption_key:
                      type: string
                      title: owners encryption key address
                  description: >-
                    Defines an Locator object stored on chain, which represents
                    a owner( blockchain address) associated with a endpoint

                    uri for it's associated object store.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              OSAllLocatorsResponse is the response type for the
              Query/OSAllLocators RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/netassetvalues/{id}:
    get:
      summary: ScopeNetAssetValues returns net asset values for scope
      operationId: ScopeNetAssetValues
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              net_asset_values:
                type: array
                items:
                  type: object
                  properties:
                    price:
                      title: price is the complete value of the asset's volume
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    updated_block_height:
                      type: string
                      format: uint64
                      title: updated_block_height is the block height of last update
                    volume:
                      type: string
                      format: uint64
                      title: >-
                        volume is the number of scope instances that were
                        purchased for the price

                        Typically this will be null (equivalent to one) or one. 
                        The only reason this would be more than

                        one is for cases where the precision of the price denom
                        is insufficient to represent the actual price
                  title: NetAssetValue defines a scope's net asset value
                title: net asset values for scope
            description: >-
              QueryNetAssetValuesRequest is the response type for the
              Query/NetAssetValues method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          description: scopeid metadata address
          in: path
          required: true
          type: string
      tags:
        - Metadata
  /provenance/metadata/v1/ownership/{address}:
    get:
      summary: >-
        Ownership returns the scope identifiers that have the given address in
        the owners list.
      operationId: Ownership
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope_uuids:
                type: array
                items:
                  type: string
                description: A list of scope ids (uuid) associated with the given address.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  address:
                    type: string
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              OwnershipResponse is the response type for the Query/Ownership RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/params:
    get:
      summary: Params queries the parameters of x/metadata module.
      operationId: MetadataParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/record/{record_addr}:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: path
          required: true
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: query
          required: false
          type: string
        - name: name
          description: name is the name of the record to look for
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/record/{record_addr}/scope:
    get:
      summary: Scope searches for a scope.
      description: >-
        The scope id, if provided, must either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address,

        e.g. scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. The session addr, if
        provided, must be a bech32 session address,

        e.g.
        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, that scope is returned.

        * If only a session_addr is provided, the scope containing that session
        is returned.

        * If only a record_addr is provided, the scope containing that record is
        returned.

        * If more than one of scope_id, session_addr, and record_addr are
        provided, and they don't refer to the same scope,

        a bad request is returned.


        Providing a session addr or record addr does not limit the sessions and
        records returned (if requested).

        Those parameters are only used to find the scope.


        By default, sessions and records are not included.

        Set include_sessions and/or include_records to true to include sessions
        and/or records.
      operationId: Scope3
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: scope is the wrapped scope result.
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions in this scope (if
                  requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records in this scope (if
                  requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_addr:
                    type: string
                    description: >-
                      session_addr is a bech32 session address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions of the scope in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of the scope in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: ScopeResponse is the response type for the Query/Scope RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: path
          required: true
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: session_addr
          description: |-
            session_addr is a bech32 session address, e.g.
            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
          in: query
          required: false
          type: string
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions of
            the scope in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of the
            scope in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/record/{record_addr}/session:
    get:
      summary: Sessions searches for sessions.
      description: >-
        The scope_id can either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.

        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly, the session_id can
        either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, all sessions in that scope are
        returned.

        * If only a session_id is provided, it must be an address, and that
        single session is returned.

        * If the session_id is a uuid, then either a scope_id or record_addr
        must also be provided, and that single session

        is returned.

        * If only a record_addr is provided, the session containing that record
        will be returned.

        * If a record_name is provided then either a scope_id, session_id as an
        address, or record_addr must also be

        provided, and the session containing that record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and is provided without a scope_id or
        record_addr.

        * A record_name is provided without any way to identify the scope (e.g.
        a scope_id, a session_id as an address, or

        a record_addr).

        * Two or more of scope_id, session_id as an address, and record_addr are
        provided and don't all refer to the same

        scope.

        * A record_addr (or scope_id and record_name) is provided with a
        session_id and that session does not contain such

        a record.

        * A record_addr and record_name are both provided, but reference
        different records.


        By default, the scope and records are not included.

        Set include_scope and/or include_records to true to include the scope
        and/or records.
      operationId: Sessions4
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these sessions (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: sessions is any number of wrapped session results.
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records contained in these
                  sessions (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  record_name:
                    type: string
                    description: >-
                      record_name is the name of the record to find the session
                      for in the provided scope.
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the scope
                      containing these sessions in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of these sessions in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              SessionsResponse is the response type for the Query/Sessions RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: path
          required: true
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: query
          required: false
          type: string
        - name: record_name
          description: >-
            record_name is the name of the record to find the session for in the
            provided scope.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the scope containing
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/records/all:
    get:
      summary: RecordsAll retrieves all records.
      operationId: RecordsAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records are the wrapped records.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              RecordsAllResponse is the response type for the Query/RecordsAll
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/recordspec/{specification_id}:
    get:
      summary: RecordSpecification returns a record specification for the given input.
      operationId: RecordSpecification
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              record_specification:
                description: record_specification is the wrapped record specification.
                type: object
                properties:
                  specification:
                    description: >-
                      specification is the on-chain record specification
                      message.
                    type: object
                    properties:
                      specification_id:
                        type: string
                        format: byte
                        title: unique identifier for this specification on chain
                      name:
                        type: string
                        title: >-
                          Name of Record that will be created when this
                          specification is used
                      inputs:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                              title: name for this input
                            type_name:
                              type: string
                              title: >-
                                a type_name (typically a proto name or
                                class_name)
                            record_id:
                              type: string
                              format: byte
                              title: >-
                                the address of a record on chain (For
                                Established Records)
                            hash:
                              type: string
                              title: >-
                                the hash of an off-chain piece of information
                                (For Proposed Records)
                          title: >-
                            InputSpecification defines a name, type_name, and
                            source reference (either on or off chain) to define
                            an input

                            parameter
                        title: >-
                          A set of inputs that must be satisified to apply this
                          RecordSpecification and create a Record
                      type_name:
                        type: string
                        title: >-
                          A type name for data associated with this record
                          (typically a class or proto name)
                      result_type:
                        title: >-
                          Type of result for this record specification (must be
                          RECORD or RECORD_LIST)
                        type: string
                        enum:
                          - DEFINITION_TYPE_UNSPECIFIED
                          - DEFINITION_TYPE_PROPOSED
                          - DEFINITION_TYPE_RECORD
                          - DEFINITION_TYPE_RECORD_LIST
                        default: DEFINITION_TYPE_UNSPECIFIED
                        description: >-
                          - DEFINITION_TYPE_UNSPECIFIED:
                          DEFINITION_TYPE_UNSPECIFIED indicates an
                          unknown/invalid value
                           - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                           - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                           - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                          the same name
                      responsible_parties:
                        type: array
                        items:
                          type: string
                          enum:
                            - PARTY_TYPE_UNSPECIFIED
                            - PARTY_TYPE_ORIGINATOR
                            - PARTY_TYPE_SERVICER
                            - PARTY_TYPE_INVESTOR
                            - PARTY_TYPE_CUSTODIAN
                            - PARTY_TYPE_OWNER
                            - PARTY_TYPE_AFFILIATE
                            - PARTY_TYPE_OMNIBUS
                            - PARTY_TYPE_PROVENANCE
                            - PARTY_TYPE_CONTROLLER
                            - PARTY_TYPE_VALIDATOR
                            - PARTY_TYPE_GENERIC_1
                            - PARTY_TYPE_GENERIC_2
                            - PARTY_TYPE_GENERIC_3
                            - PARTY_TYPE_GENERIC_4
                            - PARTY_TYPE_GENERIC_5
                            - PARTY_TYPE_GENERIC_6
                          default: PARTY_TYPE_UNSPECIFIED
                          description: >-
                            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is
                            an error condition
                             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                          title: >-
                            PartyType are the different roles parties on a
                            contract may use
                        title: Type of party responsible for this record
                    title: >-
                      RecordSpecification defines the specification for a Record
                      including allowed/required inputs/outputs
                  record_spec_id_info:
                    description: >-
                      record_spec_id_info contains information about the
                      id/address of the record specification.
                    type: object
                    properties:
                      record_spec_id:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id is the raw bytes of the record
                          specification address.
                      record_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id_prefix is the prefix portion of the
                          record_spec_id.
                      record_spec_id_contract_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id_contract_spec_uuid is the
                          contract_spec_uuid portion of the record_spec_id.
                      record_spec_id_hashed_name:
                        type: string
                        format: byte
                        description: >-
                          record_spec_id_hashed_name is the hashed name portion
                          of the record_spec_id.
                      record_spec_addr:
                        type: string
                        description: >-
                          record_spec_addr is the bech32 string version of the
                          record_spec_id.
                      contract_spec_id_info:
                        description: >-
                          contract_spec_id_info is information about the
                          contract spec id referenced in the record_spec_id.
                        type: object
                        properties:
                          contract_spec_id:
                            type: string
                            format: byte
                            description: >-
                              contract_spec_id is the raw bytes of the contract
                              specification address.
                          contract_spec_id_prefix:
                            type: string
                            format: byte
                            description: >-
                              contract_spec_id_prefix is the prefix portion of
                              the contract_spec_id.
                          contract_spec_id_contract_spec_uuid:
                            type: string
                            format: byte
                            description: >-
                              contract_spec_id_contract_spec_uuid is the
                              contract_spec_uuid portion of the
                              contract_spec_id.
                          contract_spec_addr:
                            type: string
                            description: >-
                              contract_spec_addr is the bech32 string version of
                              the contract_spec_id.
                          contract_spec_uuid:
                            type: string
                            description: >-
                              contract_spec_uuid is the uuid hex string of the
                              contract_spec_id_contract_spec_uuid.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  specification_id:
                    type: string
                    description: >-
                      specification_id can either be a uuid, e.g.
                      def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
                      specification

                      address, e.g.
                      contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

                      It can also be a record specification address, e.g.

                      recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
                  name:
                    type: string
                    description: >-
                      name is the name of the record to look up.

                      It is required if the specification_id is a uuid or
                      contract specification address.

                      It is ignored if the specification_id is a record
                      specification address.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordSpecificationResponse is the response type for the
              Query/RecordSpecification RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: specification_id
          description: >-
            specification_id can either be a uuid, e.g.
            def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
            specification

            address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

            It can also be a record specification address, e.g.

            recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          in: path
          required: true
          type: string
        - name: name
          description: >-
            name is the name of the record to look up.

            It is required if the specification_id is a uuid or contract
            specification address.

            It is ignored if the specification_id is a record specification
            address.
          in: query
          required: false
          type: string
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/recordspecs/all:
    get:
      summary: RecordSpecificationsAll retrieves all record specifications.
      operationId: RecordSpecificationsAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              record_specifications:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain record specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        name:
                          type: string
                          title: >-
                            Name of Record that will be created when this
                            specification is used
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                title: name for this input
                              type_name:
                                type: string
                                title: >-
                                  a type_name (typically a proto name or
                                  class_name)
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                            title: >-
                              InputSpecification defines a name, type_name, and
                              source reference (either on or off chain) to
                              define an input

                              parameter
                          title: >-
                            A set of inputs that must be satisified to apply
                            this RecordSpecification and create a Record
                        type_name:
                          type: string
                          title: >-
                            A type name for data associated with this record
                            (typically a class or proto name)
                        result_type:
                          title: >-
                            Type of result for this record specification (must
                            be RECORD or RECORD_LIST)
                          type: string
                          enum:
                            - DEFINITION_TYPE_UNSPECIFIED
                            - DEFINITION_TYPE_PROPOSED
                            - DEFINITION_TYPE_RECORD
                            - DEFINITION_TYPE_RECORD_LIST
                          default: DEFINITION_TYPE_UNSPECIFIED
                          description: >-
                            - DEFINITION_TYPE_UNSPECIFIED:
                            DEFINITION_TYPE_UNSPECIFIED indicates an
                            unknown/invalid value
                             - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                             - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                             - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                            the same name
                        responsible_parties:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: Type of party responsible for this record
                      title: >-
                        RecordSpecification defines the specification for a
                        Record including allowed/required inputs/outputs
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordSpecificationWrapper contains a single record
                    specification and some extra identifiers for it.
                description: record_specifications are the wrapped record specifications.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              RecordSpecificationsAllResponse is the response type for the
              Query/RecordSpecificationsAll RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}:
    get:
      summary: Scope searches for a scope.
      description: >-
        The scope id, if provided, must either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address,

        e.g. scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. The session addr, if
        provided, must be a bech32 session address,

        e.g.
        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, that scope is returned.

        * If only a session_addr is provided, the scope containing that session
        is returned.

        * If only a record_addr is provided, the scope containing that record is
        returned.

        * If more than one of scope_id, session_addr, and record_addr are
        provided, and they don't refer to the same scope,

        a bad request is returned.


        Providing a session addr or record addr does not limit the sessions and
        records returned (if requested).

        Those parameters are only used to find the scope.


        By default, sessions and records are not included.

        Set include_sessions and/or include_records to true to include sessions
        and/or records.
      operationId: Scope
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: scope is the wrapped scope result.
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions in this scope (if
                  requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records in this scope (if
                  requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_addr:
                    type: string
                    description: >-
                      session_addr is a bech32 session address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions of the scope in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of the scope in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: ScopeResponse is the response type for the Query/Scope RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: session_addr
          description: |-
            session_addr is a bech32 session address, e.g.
            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
          in: query
          required: false
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions of
            the scope in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of the
            scope in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/record/{name}:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records3
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: name
          description: name is the name of the record to look for
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/record/{record_name}/session:
    get:
      summary: Sessions searches for sessions.
      description: >-
        The scope_id can either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.

        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly, the session_id can
        either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, all sessions in that scope are
        returned.

        * If only a session_id is provided, it must be an address, and that
        single session is returned.

        * If the session_id is a uuid, then either a scope_id or record_addr
        must also be provided, and that single session

        is returned.

        * If only a record_addr is provided, the session containing that record
        will be returned.

        * If a record_name is provided then either a scope_id, session_id as an
        address, or record_addr must also be

        provided, and the session containing that record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and is provided without a scope_id or
        record_addr.

        * A record_name is provided without any way to identify the scope (e.g.
        a scope_id, a session_id as an address, or

        a record_addr).

        * Two or more of scope_id, session_id as an address, and record_addr are
        provided and don't all refer to the same

        scope.

        * A record_addr (or scope_id and record_name) is provided with a
        session_id and that session does not contain such

        a record.

        * A record_addr and record_name are both provided, but reference
        different records.


        By default, the scope and records are not included.

        Set include_scope and/or include_records to true to include the scope
        and/or records.
      operationId: Sessions5
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these sessions (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: sessions is any number of wrapped session results.
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records contained in these
                  sessions (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  record_name:
                    type: string
                    description: >-
                      record_name is the name of the record to find the session
                      for in the provided scope.
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the scope
                      containing these sessions in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of these sessions in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              SessionsResponse is the response type for the Query/Sessions RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: record_name
          description: >-
            record_name is the name of the record to find the session for in the
            provided scope.
          in: path
          required: true
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: query
          required: false
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the scope containing
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/records:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: query
          required: false
          type: string
        - name: name
          description: name is the name of the record to look for
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/session/{session_id}:
    get:
      summary: Sessions searches for sessions.
      description: >-
        The scope_id can either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.

        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly, the session_id can
        either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, all sessions in that scope are
        returned.

        * If only a session_id is provided, it must be an address, and that
        single session is returned.

        * If the session_id is a uuid, then either a scope_id or record_addr
        must also be provided, and that single session

        is returned.

        * If only a record_addr is provided, the session containing that record
        will be returned.

        * If a record_name is provided then either a scope_id, session_id as an
        address, or record_addr must also be

        provided, and the session containing that record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and is provided without a scope_id or
        record_addr.

        * A record_name is provided without any way to identify the scope (e.g.
        a scope_id, a session_id as an address, or

        a record_addr).

        * Two or more of scope_id, session_id as an address, and record_addr are
        provided and don't all refer to the same

        scope.

        * A record_addr (or scope_id and record_name) is provided with a
        session_id and that session does not contain such

        a record.

        * A record_addr and record_name are both provided, but reference
        different records.


        By default, the scope and records are not included.

        Set include_scope and/or include_records to true to include the scope
        and/or records.
      operationId: Sessions3
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these sessions (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: sessions is any number of wrapped session results.
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records contained in these
                  sessions (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  record_name:
                    type: string
                    description: >-
                      record_name is the name of the record to find the session
                      for in the provided scope.
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the scope
                      containing these sessions in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of these sessions in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              SessionsResponse is the response type for the Query/Sessions RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: record_name
          description: >-
            record_name is the name of the record to find the session for in the
            provided scope.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the scope containing
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/session/{session_id}/record/{name}:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records5
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: path
          required: true
          type: string
        - name: name
          description: name is the name of the record to look for
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/session/{session_id}/records:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records4
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: name
          description: name is the name of the record to look for
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scope/{scope_id}/sessions:
    get:
      summary: Sessions searches for sessions.
      description: >-
        The scope_id can either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.

        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly, the session_id can
        either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, all sessions in that scope are
        returned.

        * If only a session_id is provided, it must be an address, and that
        single session is returned.

        * If the session_id is a uuid, then either a scope_id or record_addr
        must also be provided, and that single session

        is returned.

        * If only a record_addr is provided, the session containing that record
        will be returned.

        * If a record_name is provided then either a scope_id, session_id as an
        address, or record_addr must also be

        provided, and the session containing that record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and is provided without a scope_id or
        record_addr.

        * A record_name is provided without any way to identify the scope (e.g.
        a scope_id, a session_id as an address, or

        a record_addr).

        * Two or more of scope_id, session_id as an address, and record_addr are
        provided and don't all refer to the same

        scope.

        * A record_addr (or scope_id and record_name) is provided with a
        session_id and that session does not contain such

        a record.

        * A record_addr and record_name are both provided, but reference
        different records.


        By default, the scope and records are not included.

        Set include_scope and/or include_records to true to include the scope
        and/or records.
      operationId: Sessions2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these sessions (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: sessions is any number of wrapped session results.
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records contained in these
                  sessions (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  record_name:
                    type: string
                    description: >-
                      record_name is the name of the record to find the session
                      for in the provided scope.
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the scope
                      containing these sessions in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of these sessions in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              SessionsResponse is the response type for the Query/Sessions RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: path
          required: true
          type: string
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: query
          required: false
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: record_name
          description: >-
            record_name is the name of the record to find the session for in the
            provided scope.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the scope containing
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scopes/all:
    get:
      summary: ScopesAll retrieves all scopes.
      operationId: ScopesAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scopes:
                type: array
                items:
                  type: object
                  properties:
                    scope:
                      description: scope is the on-chain scope message.
                      type: object
                      properties:
                        scope_id:
                          type: string
                          format: byte
                          title: >-
                            Unique ID for this scope.  Implements sdk.Address
                            interface for use where addresses are required in
                            Cosmos
                        specification_id:
                          type: string
                          format: byte
                          title: >-
                            the scope specification that contains the
                            specifications for data elements allowed within this
                            scope
                        owners:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          description: >-
                            These parties represent top level owners of the
                            records within.  These parties must sign any
                            requests that modify

                            the data within the scope.  These addresses are in
                            union with parties listed on the sessions.
                        data_access:
                          type: array
                          items:
                            type: string
                          description: >-
                            Addresses in this list are authorized to receive
                            off-chain data associated with this scope.
                        value_owner_address:
                          type: string
                          description: >-
                            The address that controls the value associated with
                            this scope.


                            The value owner is actually tracked by the bank
                            module using a coin with the denom "nft/<scope_id>".

                            The value owner can be changed using WriteScope or
                            anything that transfers funds, e.g. MsgSend.


                            During WriteScope:
                             - If this field is empty, it indicates that there should not be a change to the value owner.
                               I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                             - If this field has a value, the existing value owner will be looked up, and
                               - If there's already an existing value owner, they must be a signer,
                                 and the coin will be transferred to the new value owner.
                               - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                                 If the scope already exists, the owners must be signers (just like changing other fields).
                                 If it's a new scope, there's no special signer limitations related to the value owner.
                        require_party_rollup:
                          type: boolean
                          description: >-
                            Whether all parties in this scope and its sessions
                            must be present in this scope's owners field.

                            This also enables use of optional=true scope owners
                            and session parties.
                    scope_id_info:
                      description: >-
                        scope_id_info contains information about the id/address
                        of the scope.
                      type: object
                      properties:
                        scope_id:
                          type: string
                          format: byte
                          description: scope_id is the raw bytes of the scope address.
                        scope_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            scope_id_prefix is the prefix portion of the
                            scope_id.
                        scope_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            scope_id_scope_uuid is the scope_uuid portion of the
                            scope_id.
                        scope_addr:
                          type: string
                          description: >-
                            scope_addr is the bech32 string version of the
                            scope_id.
                        scope_uuid:
                          type: string
                          description: >-
                            scope_uuid is the uuid hex string of the
                            scope_id_scope_uuid.
                    scope_spec_id_info:
                      description: >-
                        scope_spec_id_info contains information about the
                        id/address of the scope specification.
                      type: object
                      properties:
                        scope_spec_id:
                          type: string
                          format: byte
                          description: >-
                            scope_spec_id is the raw bytes of the scope
                            specification address.
                        scope_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            scope_spec_id_prefix is the prefix portion of the
                            scope_spec_id.
                        scope_spec_id_scope_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                            portion of the scope_spec_id.
                        scope_spec_addr:
                          type: string
                          description: >-
                            scope_spec_addr is the bech32 string version of the
                            scope_spec_id.
                        scope_spec_uuid:
                          type: string
                          description: >-
                            scope_spec_uuid is the uuid hex string of the
                            scope_spec_id_scope_spec_uuid.
                  description: SessionWrapper contains a single scope and its uuid.
                description: scopes are the wrapped scopes.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              ScopesAllResponse is the response type for the Query/ScopesAll RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scopespec/{specification_id}:
    get:
      summary: >-
        ScopeSpecification returns a scope specification for the given
        specification id.
      description: >-
        The specification_id can either be a uuid, e.g.
        dc83ea70-eacd-40fe-9adf-1cf6148bf8a2 or a bech32 scope

        specification address, e.g.
        scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m.


        By default, the contract and record specifications are not included.

        Set include_contract_specs and/or include_record_specs to true to
        include contract and/or record specifications.
      operationId: ScopeSpecification
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope_specification:
                description: scope_specification is the wrapped scope specification.
                type: object
                properties:
                  specification:
                    description: specification is the on-chain scope specification message.
                    type: object
                    properties:
                      specification_id:
                        type: string
                        format: byte
                        title: unique identifier for this specification on chain
                      description:
                        description: General information about this scope specification.
                        type: object
                        properties:
                          name:
                            type: string
                            description: A Name for this thing.
                          description:
                            type: string
                            description: A description of this thing.
                          website_url:
                            type: string
                            description: URL to find even more info.
                          icon_url:
                            type: string
                            description: URL of an icon.
                      owner_addresses:
                        type: array
                        items:
                          type: string
                        description: Addresses of the owners of this scope specification.
                      parties_involved:
                        type: array
                        items:
                          type: string
                          enum:
                            - PARTY_TYPE_UNSPECIFIED
                            - PARTY_TYPE_ORIGINATOR
                            - PARTY_TYPE_SERVICER
                            - PARTY_TYPE_INVESTOR
                            - PARTY_TYPE_CUSTODIAN
                            - PARTY_TYPE_OWNER
                            - PARTY_TYPE_AFFILIATE
                            - PARTY_TYPE_OMNIBUS
                            - PARTY_TYPE_PROVENANCE
                            - PARTY_TYPE_CONTROLLER
                            - PARTY_TYPE_VALIDATOR
                            - PARTY_TYPE_GENERIC_1
                            - PARTY_TYPE_GENERIC_2
                            - PARTY_TYPE_GENERIC_3
                            - PARTY_TYPE_GENERIC_4
                            - PARTY_TYPE_GENERIC_5
                            - PARTY_TYPE_GENERIC_6
                          default: PARTY_TYPE_UNSPECIFIED
                          description: >-
                            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is
                            an error condition
                             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                          title: >-
                            PartyType are the different roles parties on a
                            contract may use
                        title: >-
                          A list of parties that must be present on a scope (and
                          their associated roles)
                      contract_spec_ids:
                        type: array
                        items:
                          type: string
                          format: byte
                        description: >-
                          A list of contract specification ids allowed for a
                          scope based on this specification.
                    title: >-
                      ScopeSpecification defines the required parties,
                      resources, conditions, and consideration outputs for a
                      contract
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              contract_specs:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain contract specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        description:
                          title: >-
                            Description information for this contract
                            specification
                          type: object
                          properties:
                            name:
                              type: string
                              description: A Name for this thing.
                            description:
                              type: string
                              description: A description of this thing.
                            website_url:
                              type: string
                              description: URL to find even more info.
                            icon_url:
                              type: string
                              description: URL of an icon.
                          description: >-
                            Description holds general information that is handy
                            to associate with a structure.
                        owner_addresses:
                          type: array
                          items:
                            type: string
                          title: Address of the account that owns this specificaiton
                        parties_involved:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: >-
                            a list of party roles that must be fullfilled when
                            signing a transaction for this contract
                            specification
                        resource_id:
                          type: string
                          format: byte
                          title: >-
                            the address of a record on chain that represents
                            this contract
                        hash:
                          type: string
                          title: the hash of contract binary (off-chain instance)
                        class_name:
                          type: string
                          title: name of the class/type of this contract executable
                      title: >-
                        ContractSpecification defines the required parties,
                        resources, conditions, and consideration outputs for a
                        contract
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    ContractSpecificationWrapper contains a single contract
                    specification and some extra identifiers for it.
                description: >-
                  contract_specs is any number of wrapped contract
                  specifications in this scope specification (if requested).
              record_specs:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain record specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        name:
                          type: string
                          title: >-
                            Name of Record that will be created when this
                            specification is used
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                title: name for this input
                              type_name:
                                type: string
                                title: >-
                                  a type_name (typically a proto name or
                                  class_name)
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                            title: >-
                              InputSpecification defines a name, type_name, and
                              source reference (either on or off chain) to
                              define an input

                              parameter
                          title: >-
                            A set of inputs that must be satisified to apply
                            this RecordSpecification and create a Record
                        type_name:
                          type: string
                          title: >-
                            A type name for data associated with this record
                            (typically a class or proto name)
                        result_type:
                          title: >-
                            Type of result for this record specification (must
                            be RECORD or RECORD_LIST)
                          type: string
                          enum:
                            - DEFINITION_TYPE_UNSPECIFIED
                            - DEFINITION_TYPE_PROPOSED
                            - DEFINITION_TYPE_RECORD
                            - DEFINITION_TYPE_RECORD_LIST
                          default: DEFINITION_TYPE_UNSPECIFIED
                          description: >-
                            - DEFINITION_TYPE_UNSPECIFIED:
                            DEFINITION_TYPE_UNSPECIFIED indicates an
                            unknown/invalid value
                             - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                             - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                             - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                            the same name
                        responsible_parties:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: Type of party responsible for this record
                      title: >-
                        RecordSpecification defines the specification for a
                        Record including allowed/required inputs/outputs
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordSpecificationWrapper contains a single record
                    specification and some extra identifiers for it.
                description: >-
                  record_specs is any number of wrapped record specifications in
                  this scope specification (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  specification_id:
                    type: string
                    description: >-
                      specification_id can either be a uuid, e.g.
                      dc83ea70-eacd-40fe-9adf-1cf6148bf8a2 or a bech32 scope
                      specification

                      address, e.g.
                      scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m.
                  include_contract_specs:
                    type: boolean
                    description: >-
                      include_contract_specs is a flag for whether to include
                      the contract specifications of the scope specification in

                      the response.
                  include_record_specs:
                    type: boolean
                    description: >-
                      include_record_specs is a flag for whether to include the
                      record specifications of the scope specification in the

                      response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              ScopeSpecificationResponse is the response type for the
              Query/ScopeSpecification RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: specification_id
          description: >-
            specification_id can either be a uuid, e.g.
            dc83ea70-eacd-40fe-9adf-1cf6148bf8a2 or a bech32 scope specification

            address, e.g. scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m.
          in: path
          required: true
          type: string
        - name: include_contract_specs
          description: >-
            include_contract_specs is a flag for whether to include the contract
            specifications of the scope specification in

            the response.
          in: query
          required: false
          type: boolean
        - name: include_record_specs
          description: >-
            include_record_specs is a flag for whether to include the record
            specifications of the scope specification in the

            response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/scopespecs/all:
    get:
      summary: ScopeSpecificationsAll retrieves all scope specifications.
      operationId: ScopeSpecificationsAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope_specifications:
                type: array
                items:
                  type: object
                  properties:
                    specification:
                      description: >-
                        specification is the on-chain scope specification
                        message.
                      type: object
                      properties:
                        specification_id:
                          type: string
                          format: byte
                          title: unique identifier for this specification on chain
                        description:
                          description: General information about this scope specification.
                          type: object
                          properties:
                            name:
                              type: string
                              description: A Name for this thing.
                            description:
                              type: string
                              description: A description of this thing.
                            website_url:
                              type: string
                              description: URL to find even more info.
                            icon_url:
                              type: string
                              description: URL of an icon.
                        owner_addresses:
                          type: array
                          items:
                            type: string
                          description: Addresses of the owners of this scope specification.
                        parties_involved:
                          type: array
                          items:
                            type: string
                            enum:
                              - PARTY_TYPE_UNSPECIFIED
                              - PARTY_TYPE_ORIGINATOR
                              - PARTY_TYPE_SERVICER
                              - PARTY_TYPE_INVESTOR
                              - PARTY_TYPE_CUSTODIAN
                              - PARTY_TYPE_OWNER
                              - PARTY_TYPE_AFFILIATE
                              - PARTY_TYPE_OMNIBUS
                              - PARTY_TYPE_PROVENANCE
                              - PARTY_TYPE_CONTROLLER
                              - PARTY_TYPE_VALIDATOR
                              - PARTY_TYPE_GENERIC_1
                              - PARTY_TYPE_GENERIC_2
                              - PARTY_TYPE_GENERIC_3
                              - PARTY_TYPE_GENERIC_4
                              - PARTY_TYPE_GENERIC_5
                              - PARTY_TYPE_GENERIC_6
                            default: PARTY_TYPE_UNSPECIFIED
                            description: >-
                              - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                              is an error condition
                               - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                               - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                               - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                               - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                               - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                               - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                               - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                               - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                               - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                               - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                               - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                               - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            title: >-
                              PartyType are the different roles parties on a
                              contract may use
                          title: >-
                            A list of parties that must be present on a scope
                            (and their associated roles)
                        contract_spec_ids:
                          type: array
                          items:
                            type: string
                            format: byte
                          description: >-
                            A list of contract specification ids allowed for a
                            scope based on this specification.
                      title: >-
                        ScopeSpecification defines the required parties,
                        resources, conditions, and consideration outputs for a
                        contract
                    scope_spec_id_info:
                      description: >-
                        scope_spec_id_info contains information about the
                        id/address of the scope specification.
                      type: object
                      properties:
                        scope_spec_id:
                          type: string
                          format: byte
                          description: >-
                            scope_spec_id is the raw bytes of the scope
                            specification address.
                        scope_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            scope_spec_id_prefix is the prefix portion of the
                            scope_spec_id.
                        scope_spec_id_scope_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                            portion of the scope_spec_id.
                        scope_spec_addr:
                          type: string
                          description: >-
                            scope_spec_addr is the bech32 string version of the
                            scope_spec_id.
                        scope_spec_uuid:
                          type: string
                          description: >-
                            scope_spec_uuid is the uuid hex string of the
                            scope_spec_id_scope_spec_uuid.
                  description: >-
                    ScopeSpecificationWrapper contains a single scope
                    specification and some extra identifiers for it.
                description: scope_specifications are the wrapped scope specifications.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              ScopeSpecificationsAllResponse is the response type for the
              Query/ScopeSpecificationsAll RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/session/{session_addr}/scope:
    get:
      summary: Scope searches for a scope.
      description: >-
        The scope id, if provided, must either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address,

        e.g. scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. The session addr, if
        provided, must be a bech32 session address,

        e.g.
        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, that scope is returned.

        * If only a session_addr is provided, the scope containing that session
        is returned.

        * If only a record_addr is provided, the scope containing that record is
        returned.

        * If more than one of scope_id, session_addr, and record_addr are
        provided, and they don't refer to the same scope,

        a bad request is returned.


        Providing a session addr or record addr does not limit the sessions and
        records returned (if requested).

        Those parameters are only used to find the scope.


        By default, sessions and records are not included.

        Set include_sessions and/or include_records to true to include sessions
        and/or records.
      operationId: Scope2
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: scope is the wrapped scope result.
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions in this scope (if
                  requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records in this scope (if
                  requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_addr:
                    type: string
                    description: >-
                      session_addr is a bech32 session address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions of the scope in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of the scope in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: ScopeResponse is the response type for the Query/Scope RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: session_addr
          description: |-
            session_addr is a bech32 session address, e.g.
            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
          in: path
          required: true
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions of
            the scope in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of the
            scope in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/session/{session_id}:
    get:
      summary: Sessions searches for sessions.
      description: >-
        The scope_id can either be scope uuid, e.g.
        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.

        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly, the session_id can
        either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        record_addr, if provided, must be a

        bech32 record address, e.g.
        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.


        * If only a scope_id is provided, all sessions in that scope are
        returned.

        * If only a session_id is provided, it must be an address, and that
        single session is returned.

        * If the session_id is a uuid, then either a scope_id or record_addr
        must also be provided, and that single session

        is returned.

        * If only a record_addr is provided, the session containing that record
        will be returned.

        * If a record_name is provided then either a scope_id, session_id as an
        address, or record_addr must also be

        provided, and the session containing that record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and is provided without a scope_id or
        record_addr.

        * A record_name is provided without any way to identify the scope (e.g.
        a scope_id, a session_id as an address, or

        a record_addr).

        * Two or more of scope_id, session_id as an address, and record_addr are
        provided and don't all refer to the same

        scope.

        * A record_addr (or scope_id and record_name) is provided with a
        session_id and that session does not contain such

        a record.

        * A record_addr and record_name are both provided, but reference
        different records.


        By default, the scope and records are not included.

        Set include_scope and/or include_records to true to include the scope
        and/or records.
      operationId: Sessions
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these sessions (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: sessions is any number of wrapped session results.
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: >-
                  records is any number of wrapped records contained in these
                  sessions (if requested).
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  record_name:
                    type: string
                    description: >-
                      record_name is the name of the record to find the session
                      for in the provided scope.
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the scope
                      containing these sessions in the response.
                  include_records:
                    type: boolean
                    description: >-
                      include_records is a flag for whether to include the
                      records of these sessions in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              SessionsResponse is the response type for the Query/Sessions RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: path
          required: true
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: record_name
          description: >-
            record_name is the name of the record to find the session for in the
            provided scope.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the scope containing
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: include_records
          description: >-
            include_records is a flag for whether to include the records of
            these sessions in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/session/{session_id}/record/{name}:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records7
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: path
          required: true
          type: string
        - name: name
          description: name is the name of the record to look for
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/session/{session_id}/records:
    get:
      summary: Records searches for records.
      description: >-
        The record_addr, if provided, must be a bech32 record address, e.g.

        record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3. The
        scope-id can either be scope uuid, e.g.

        91978ba2-5f35-459a-86a7-feca1b0512e0 or a scope address, e.g.
        scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel. Similarly,

        the session_id can either be a uuid or session address, e.g.

        session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr. The
        name is the name of the record you're

        interested in.


        * If only a record_addr is provided, that single record will be
        returned.

        * If only a scope_id is provided, all records in that scope will be
        returned.

        * If only a session_id (or scope_id/session_id), all records in that
        session will be returned.

        * If a name is provided with a scope_id and/or session_id, that single
        record will be returned.


        A bad request is returned if:

        * The session_id is a uuid and no scope_id is provided.

        * There are two or more of record_addr, session_id, and scope_id, and
        they don't all refer to the same scope.

        * A name is provided, but not a scope_id and/or a session_id.

        * A name and record_addr are provided and the name doesn't match the
        record_addr.


        By default, the scope and sessions are not included.

        Set include_scope and/or include_sessions to true to include the scope
        and/or sessions.
      operationId: Records6
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope:
                description: >-
                  scope is the wrapped scope that holds these records (if
                  requested).
                type: object
                properties:
                  scope:
                    description: scope is the on-chain scope message.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        title: >-
                          Unique ID for this scope.  Implements sdk.Address
                          interface for use where addresses are required in
                          Cosmos
                      specification_id:
                        type: string
                        format: byte
                        title: >-
                          the scope specification that contains the
                          specifications for data elements allowed within this
                          scope
                      owners:
                        type: array
                        items:
                          type: object
                          properties:
                            address:
                              type: string
                              title: address of the account (on chain)
                            role:
                              title: >-
                                a role for this account within the context of
                                the processes used
                              type: string
                              enum:
                                - PARTY_TYPE_UNSPECIFIED
                                - PARTY_TYPE_ORIGINATOR
                                - PARTY_TYPE_SERVICER
                                - PARTY_TYPE_INVESTOR
                                - PARTY_TYPE_CUSTODIAN
                                - PARTY_TYPE_OWNER
                                - PARTY_TYPE_AFFILIATE
                                - PARTY_TYPE_OMNIBUS
                                - PARTY_TYPE_PROVENANCE
                                - PARTY_TYPE_CONTROLLER
                                - PARTY_TYPE_VALIDATOR
                                - PARTY_TYPE_GENERIC_1
                                - PARTY_TYPE_GENERIC_2
                                - PARTY_TYPE_GENERIC_3
                                - PARTY_TYPE_GENERIC_4
                                - PARTY_TYPE_GENERIC_5
                                - PARTY_TYPE_GENERIC_6
                              default: PARTY_TYPE_UNSPECIFIED
                              description: >-
                                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED
                                is an error condition
                                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                            optional:
                              type: boolean
                              title: whether this party's signature is optional
                          title: >-
                            A Party is an address with/in a given role
                            associated with a contract
                        description: >-
                          These parties represent top level owners of the
                          records within.  These parties must sign any requests
                          that modify

                          the data within the scope.  These addresses are in
                          union with parties listed on the sessions.
                      data_access:
                        type: array
                        items:
                          type: string
                        description: >-
                          Addresses in this list are authorized to receive
                          off-chain data associated with this scope.
                      value_owner_address:
                        type: string
                        description: >-
                          The address that controls the value associated with
                          this scope.


                          The value owner is actually tracked by the bank module
                          using a coin with the denom "nft/<scope_id>".

                          The value owner can be changed using WriteScope or
                          anything that transfers funds, e.g. MsgSend.


                          During WriteScope:
                           - If this field is empty, it indicates that there should not be a change to the value owner.
                             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                           - If this field has a value, the existing value owner will be looked up, and
                             - If there's already an existing value owner, they must be a signer,
                               and the coin will be transferred to the new value owner.
                             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                               If the scope already exists, the owners must be signers (just like changing other fields).
                               If it's a new scope, there's no special signer limitations related to the value owner.
                      require_party_rollup:
                        type: boolean
                        description: >-
                          Whether all parties in this scope and its sessions
                          must be present in this scope's owners field.

                          This also enables use of optional=true scope owners
                          and session parties.
                  scope_id_info:
                    description: >-
                      scope_id_info contains information about the id/address of
                      the scope.
                    type: object
                    properties:
                      scope_id:
                        type: string
                        format: byte
                        description: scope_id is the raw bytes of the scope address.
                      scope_id_prefix:
                        type: string
                        format: byte
                        description: scope_id_prefix is the prefix portion of the scope_id.
                      scope_id_scope_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_id_scope_uuid is the scope_uuid portion of the
                          scope_id.
                      scope_addr:
                        type: string
                        description: >-
                          scope_addr is the bech32 string version of the
                          scope_id.
                      scope_uuid:
                        type: string
                        description: >-
                          scope_uuid is the uuid hex string of the
                          scope_id_scope_uuid.
                  scope_spec_id_info:
                    description: >-
                      scope_spec_id_info contains information about the
                      id/address of the scope specification.
                    type: object
                    properties:
                      scope_spec_id:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id is the raw bytes of the scope
                          specification address.
                      scope_spec_id_prefix:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_prefix is the prefix portion of the
                          scope_spec_id.
                      scope_spec_id_scope_spec_uuid:
                        type: string
                        format: byte
                        description: >-
                          scope_spec_id_scope_spec_uuid is the scope_spec_uuid
                          portion of the scope_spec_id.
                      scope_spec_addr:
                        type: string
                        description: >-
                          scope_spec_addr is the bech32 string version of the
                          scope_spec_id.
                      scope_spec_uuid:
                        type: string
                        description: >-
                          scope_spec_uuid is the uuid hex string of the
                          scope_spec_id_scope_spec_uuid.
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: >-
                  sessions is any number of wrapped sessions that hold these
                  records (if requested).
              records:
                type: array
                items:
                  type: object
                  properties:
                    record:
                      description: record is the on-chain record message.
                      type: object
                      properties:
                        name:
                          type: string
                          title: >-
                            name/identifier for this record.  Value must be
                            unique within the scope.  Also known as a Fact name
                        session_id:
                          type: string
                          format: byte
                          title: >-
                            id of the session context that was used to create
                            this record (use with filtered kvprefix iterator)
                        process:
                          title: >-
                            process contain information used to uniquely
                            identify an execution on or off chain that generated
                            this record
                          type: object
                          properties:
                            address:
                              type: string
                              title: >-
                                the address of a smart contract used for this
                                process
                            hash:
                              type: string
                              title: the hash of an off-chain process used
                            name:
                              type: string
                              title: >-
                                a name associated with the process (type_name,
                                classname or smart contract common name)
                            method:
                              type: string
                              title: >-
                                method is a name or reference to a specific
                                operation (method) within a class/contract that
                                was invoked
                        inputs:
                          type: array
                          items:
                            type: object
                            properties:
                              name:
                                type: string
                                description: >-
                                  Name value included to link back to the
                                  definition spec.
                              record_id:
                                type: string
                                format: byte
                                title: >-
                                  the address of a record on chain (For
                                  Established Records)
                              hash:
                                type: string
                                title: >-
                                  the hash of an off-chain piece of information
                                  (For Proposed Records)
                              type_name:
                                type: string
                                title: from proposed fact structure to unmarshal
                              status:
                                title: >-
                                  Indicates if this input was a recorded fact on
                                  chain or just a given hashed input
                                type: string
                                enum:
                                  - RECORD_INPUT_STATUS_UNSPECIFIED
                                  - RECORD_INPUT_STATUS_PROPOSED
                                  - RECORD_INPUT_STATUS_RECORD
                                default: RECORD_INPUT_STATUS_UNSPECIFIED
                                description: >-
                                  - RECORD_INPUT_STATUS_UNSPECIFIED:
                                  RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                                  invalid/unknown input type
                                   - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                                   - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                            title: Tracks the inputs used to establish this record
                          title: >-
                            inputs used with the process to achieve the output
                            on this record
                        outputs:
                          type: array
                          items:
                            type: object
                            properties:
                              hash:
                                type: string
                                title: >-
                                  Hash of the data output that was
                                  output/generated for this record
                              status:
                                title: >-
                                  Status of the process execution associated
                                  with this output indicating success,failure,
                                  or pending
                                type: string
                                enum:
                                  - RESULT_STATUS_UNSPECIFIED
                                  - RESULT_STATUS_PASS
                                  - RESULT_STATUS_SKIP
                                  - RESULT_STATUS_FAIL
                                default: RESULT_STATUS_UNSPECIFIED
                                description: >-
                                  - RESULT_STATUS_UNSPECIFIED:
                                  RESULT_STATUS_UNSPECIFIED indicates an unset
                                  condition
                                   - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                                   - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                                   - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                            title: >-
                              RecordOutput encapsulates the output of a process
                              recorded on chain
                          title: >-
                            output(s) is the results of executing the process on
                            the given process indicated in this record
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            specification_id is the id of the record
                            specification that was used to create this record.
                      title: >-
                        A record (of fact) is attached to a session or each
                        consideration output from a contract
                    record_id_info:
                      description: >-
                        record_id_info contains information about the id/address
                        of the record.
                      type: object
                      properties:
                        record_id:
                          type: string
                          format: byte
                          description: record_id is the raw bytes of the record address.
                        record_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_id_prefix is the prefix portion of the
                            record_id.
                        record_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_id_scope_uuid is the scope_uuid portion of
                            the record_id.
                        record_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_id_hashed_name is the hashed name portion of
                            the record_id.
                        record_addr:
                          type: string
                          description: >-
                            record_addr is the bech32 string version of the
                            record_id.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the record_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    record_spec_id_info:
                      description: >-
                        record_spec_id_info contains information about the
                        id/address of the record specification.
                      type: object
                      properties:
                        record_spec_id:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id is the raw bytes of the record
                            specification address.
                        record_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_prefix is the prefix portion of the
                            record_spec_id.
                        record_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the record_spec_id.
                        record_spec_id_hashed_name:
                          type: string
                          format: byte
                          description: >-
                            record_spec_id_hashed_name is the hashed name
                            portion of the record_spec_id.
                        record_spec_addr:
                          type: string
                          description: >-
                            record_spec_addr is the bech32 string version of the
                            record_spec_id.
                        contract_spec_id_info:
                          description: >-
                            contract_spec_id_info is information about the
                            contract spec id referenced in the record_spec_id.
                          type: object
                          properties:
                            contract_spec_id:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id is the raw bytes of the
                                contract specification address.
                            contract_spec_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_prefix is the prefix portion of
                                the contract_spec_id.
                            contract_spec_id_contract_spec_uuid:
                              type: string
                              format: byte
                              description: >-
                                contract_spec_id_contract_spec_uuid is the
                                contract_spec_uuid portion of the
                                contract_spec_id.
                            contract_spec_addr:
                              type: string
                              description: >-
                                contract_spec_addr is the bech32 string version
                                of the contract_spec_id.
                            contract_spec_uuid:
                              type: string
                              description: >-
                                contract_spec_uuid is the uuid hex string of the
                                contract_spec_id_contract_spec_uuid.
                  description: >-
                    RecordWrapper contains a single record and some extra
                    identifiers for it.
                description: records is any number of wrapped record results.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  record_addr:
                    type: string
                    description: >-
                      record_addr is a bech32 record address, e.g.
                      record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
                  scope_id:
                    type: string
                    description: >-
                      scope_id can either be a uuid, e.g.
                      91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope
                      address, e.g.

                      scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
                  session_id:
                    type: string
                    description: >-
                      session_id can either be a uuid, e.g.
                      5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session
                      address, e.g.

                      session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
                      This can only be a uuid if a scope_id is also

                      provided.
                  name:
                    type: string
                    title: name is the name of the record to look for
                  include_scope:
                    type: boolean
                    description: >-
                      include_scope is a flag for whether to include the the
                      scope containing these records in the response.
                  include_sessions:
                    type: boolean
                    description: >-
                      include_sessions is a flag for whether to include the
                      sessions containing these records in the response.
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
            description: >-
              RecordsResponse is the response type for the Query/Records RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: session_id
          description: >-
            session_id can either be a uuid, e.g.
            5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
            e.g.

            session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
            This can only be a uuid if a scope_id is also

            provided.
          in: path
          required: true
          type: string
        - name: record_addr
          description: >-
            record_addr is a bech32 record address, e.g.
            record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          in: query
          required: false
          type: string
        - name: scope_id
          description: >-
            scope_id can either be a uuid, e.g.
            91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

            scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          in: query
          required: false
          type: string
        - name: name
          description: name is the name of the record to look for
          in: query
          required: false
          type: string
        - name: include_scope
          description: >-
            include_scope is a flag for whether to include the the scope
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: include_sessions
          description: >-
            include_sessions is a flag for whether to include the sessions
            containing these records in the response.
          in: query
          required: false
          type: boolean
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/sessions/all:
    get:
      summary: SessionsAll retrieves all sessions.
      operationId: SessionsAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              sessions:
                type: array
                items:
                  type: object
                  properties:
                    session:
                      description: session is the on-chain session message.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                        specification_id:
                          type: string
                          format: byte
                          description: >-
                            unique id of the contract specification that was
                            used to create this session.
                        parties:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                title: address of the account (on chain)
                              role:
                                title: >-
                                  a role for this account within the context of
                                  the processes used
                                type: string
                                enum:
                                  - PARTY_TYPE_UNSPECIFIED
                                  - PARTY_TYPE_ORIGINATOR
                                  - PARTY_TYPE_SERVICER
                                  - PARTY_TYPE_INVESTOR
                                  - PARTY_TYPE_CUSTODIAN
                                  - PARTY_TYPE_OWNER
                                  - PARTY_TYPE_AFFILIATE
                                  - PARTY_TYPE_OMNIBUS
                                  - PARTY_TYPE_PROVENANCE
                                  - PARTY_TYPE_CONTROLLER
                                  - PARTY_TYPE_VALIDATOR
                                  - PARTY_TYPE_GENERIC_1
                                  - PARTY_TYPE_GENERIC_2
                                  - PARTY_TYPE_GENERIC_3
                                  - PARTY_TYPE_GENERIC_4
                                  - PARTY_TYPE_GENERIC_5
                                  - PARTY_TYPE_GENERIC_6
                                default: PARTY_TYPE_UNSPECIFIED
                                description: >-
                                  - PARTY_TYPE_UNSPECIFIED:
                                  PARTY_TYPE_UNSPECIFIED is an error condition
                                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                              optional:
                                type: boolean
                                title: whether this party's signature is optional
                            title: >-
                              A Party is an address with/in a given role
                              associated with a contract
                          title: >-
                            parties is the set of identities that signed this
                            contract
                        name:
                          type: string
                          title: >-
                            name to associate with this session execution
                            context, typically classname
                        context:
                          type: string
                          format: byte
                          description: >-
                            context is a field for storing client specific data
                            associated with a session.
                        audit:
                          description: >-
                            Created by, updated by, timestamps, version number,
                            and related info.
                          type: object
                          properties:
                            created_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was created
                            created_by:
                              type: string
                              title: >-
                                the address of the account that created this
                                record
                            updated_date:
                              type: string
                              format: date-time
                              title: the date/time when this entry was last updated
                            updated_by:
                              type: string
                              title: >-
                                the address of the account that modified this
                                record
                            version:
                              type: integer
                              format: int64
                              title: >-
                                an optional version number that is incremented
                                with each update
                            message:
                              type: string
                              title: >-
                                an optional message associated with the
                                creation/update event
                          title: >-
                            AuditFields capture information about the last
                            account to make modifications and when they were
                            made
                    session_id_info:
                      description: >-
                        session_id_info contains information about the
                        id/address of the session.
                      type: object
                      properties:
                        session_id:
                          type: string
                          format: byte
                          description: session_id is the raw bytes of the session address.
                        session_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            session_id_prefix is the prefix portion of the
                            session_id.
                        session_id_scope_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_scope_uuid is the scope_uuid portion of
                            the session_id.
                        session_id_session_uuid:
                          type: string
                          format: byte
                          description: >-
                            session_id_session_uuid is the session_uuid portion
                            of the session_id.
                        session_addr:
                          type: string
                          description: >-
                            session_addr is the bech32 string version of the
                            session_id.
                        session_uuid:
                          type: string
                          description: >-
                            session_uuid is the uuid hex string of the
                            session_id_session_uuid.
                        scope_id_info:
                          description: >-
                            scope_id_info is information about the scope id
                            referenced in the session_id.
                          type: object
                          properties:
                            scope_id:
                              type: string
                              format: byte
                              description: scope_id is the raw bytes of the scope address.
                            scope_id_prefix:
                              type: string
                              format: byte
                              description: >-
                                scope_id_prefix is the prefix portion of the
                                scope_id.
                            scope_id_scope_uuid:
                              type: string
                              format: byte
                              description: >-
                                scope_id_scope_uuid is the scope_uuid portion of
                                the scope_id.
                            scope_addr:
                              type: string
                              description: >-
                                scope_addr is the bech32 string version of the
                                scope_id.
                            scope_uuid:
                              type: string
                              description: >-
                                scope_uuid is the uuid hex string of the
                                scope_id_scope_uuid.
                    contract_spec_id_info:
                      description: >-
                        contract_spec_id_info contains information about the
                        id/address of the contract specification.
                      type: object
                      properties:
                        contract_spec_id:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id is the raw bytes of the contract
                            specification address.
                        contract_spec_id_prefix:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_prefix is the prefix portion of the
                            contract_spec_id.
                        contract_spec_id_contract_spec_uuid:
                          type: string
                          format: byte
                          description: >-
                            contract_spec_id_contract_spec_uuid is the
                            contract_spec_uuid portion of the contract_spec_id.
                        contract_spec_addr:
                          type: string
                          description: >-
                            contract_spec_addr is the bech32 string version of
                            the contract_spec_id.
                        contract_spec_uuid:
                          type: string
                          description: >-
                            contract_spec_uuid is the uuid hex string of the
                            contract_spec_id_contract_spec_uuid.
                  description: >-
                    SessionWrapper contains a single session and some extra
                    identifiers for it.
                description: sessions are the wrapped sessions.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  exclude_id_info:
                    type: boolean
                    description: >-
                      exclude_id_info is a flag for whether to exclude the id
                      info from the response.
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              SessionsAllResponse is the response type for the Query/SessionsAll
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: exclude_id_info
          description: >-
            exclude_id_info is a flag for whether to exclude the id info from
            the response.
          in: query
          required: false
          type: boolean
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/metadata/v1/valueownership/{address}:
    get:
      summary: >-
        ValueOwnership returns the scope identifiers that list the given address
        as the value owner.
      operationId: ValueOwnership
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              scope_uuids:
                type: array
                items:
                  type: string
                description: A list of scope ids (uuid) associated with the given address.
              request:
                description: request is a copy of the request that generated these results.
                type: object
                properties:
                  address:
                    type: string
                  include_request:
                    type: boolean
                    description: >-
                      include_request is a flag for whether to include this
                      request in your result.
                  pagination:
                    description: >-
                      pagination defines optional pagination parameters for the
                      request.
                    type: object
                    properties:
                      key:
                        type: string
                        format: byte
                        description: >-
                          key is a value returned in PageResponse.next_key to
                          begin

                          querying the next page most efficiently. Only one of
                          offset or key

                          should be set.
                      offset:
                        type: string
                        format: uint64
                        description: >-
                          offset is a numeric offset that can be used when key
                          is unavailable.

                          It is less efficient than using key. Only one of
                          offset or key should

                          be set.
                      limit:
                        type: string
                        format: uint64
                        description: >-
                          limit is the total number of results to be returned in
                          the result page.

                          If left empty it will default to a value to be set by
                          each app.
                      count_total:
                        type: boolean
                        description: >-
                          count_total is set to true  to indicate that the
                          result set should include

                          a count of the total number of items available for
                          pagination in UIs.

                          count_total is only respected when offset is used. It
                          is ignored when key

                          is set.
                      reverse:
                        type: boolean
                        description: >-
                          reverse is set to true if results are to be returned
                          in the descending order.
                    title: >-
                      PageRequest is to be embedded in gRPC request messages for
                      efficient

                      pagination. Ex:
              pagination:
                description: >-
                  pagination provides the pagination information of this
                  response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              ValueOwnershipResponse is the response type for the
              Query/ValueOwnership RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
        - name: include_request
          description: >-
            include_request is a flag for whether to include this request in
            your result.
          in: query
          required: false
          type: boolean
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Metadata
  /provenance/tx/v1/calculate_msg_based_fee:
    post:
      summary: >-
        CalculateTxFees simulates executing a transaction for estimating gas
        usage and additional fees.

        Deprecated: This query is deprecated. It is replaced by the
        CalculateTxFees query in the x/flatfees module.

        This query endpoint will be removed in a future release.
      operationId: CalculateTxFeesDeprecated
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              additional_fees:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                title: >-
                  additional_fees are the amount of coins to be for addition msg
                  fees

                  Deprecated: This field will always be empty
              total_fees:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  total_fees are the total amount of fees needed for the
                  transactions (msg fees + gas fee)

                  note: the gas fee is calculated with the floor gas price
                  module param.
              estimated_gas:
                type: string
                format: uint64
                title: estimated_gas is the amount of gas needed for the transaction
            description: >-
              CalculateTxFeesResponse is the response type for the Query RPC
              method.

              Deprecated: This query is deprecated. It is replaced by the
              CalculateTxFees query in the x/flatfees module.

              This query endpoint will be removed in a future release.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: body
          description: >-
            CalculateTxFeesRequest is the request type for the Query RPC method.

            Deprecated: This query is deprecated. It is replaced by the
            CalculateTxFees query in the x/flatfees module.

            This query endpoint will be removed in a future release.
          in: body
          required: true
          schema:
            type: object
            properties:
              tx_bytes:
                type: string
                format: byte
                description: tx_bytes is the transaction to simulate.
              default_base_denom:
                type: string
                description: >-
                  default_base_denom used to be the denom used for gas fees.

                  Deprecated: This field is now ignored. The fees will be in the
                  appropriate denomination(s) automatically.
              gas_adjustment:
                type: number
                format: float
                description: >-
                  gas_adjustment is the adjustment factor to be multiplied
                  against the estimate gas returned by the tx simulation.

                  This only affects the returned gas (since the fee is flat).
            description: >-
              CalculateTxFeesRequest is the request type for the Query RPC
              method.

              Deprecated: This query is deprecated. It is replaced by the
              CalculateTxFees query in the x/flatfees module.

              This query endpoint will be removed in a future release.
      tags:
        - Fees
  /provenance/name/v1/lookup/{address}:
    get:
      summary: ReverseLookup queries for all names bound against a given address
      operationId: ReverseLookup
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              name:
                type: array
                items:
                  type: string
                title: an array of names bound against a given address
              pagination:
                description: pagination defines an optional pagination for the request.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryReverseLookupResponse is the response type for the
              Query/Resolve method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address to find name records for
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Name
  /provenance/name/v1/params:
    get:
      summary: Params queries params of the name module.
      operationId: NameParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  max_segment_length:
                    type: integer
                    format: int64
                    title: maximum length of name segment to allow
                  min_segment_length:
                    type: integer
                    format: int64
                    title: minimum length of name segment to allow
                  max_name_levels:
                    type: integer
                    format: int64
                    title: >-
                      maximum number of name segments to allow.  Example:
                      `foo.bar.baz` would be 3
                  allow_unrestricted_names:
                    type: boolean
                    title: determines if unrestricted name keys are allowed or not
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Name
  /provenance/name/v1/resolve/{name}:
    get:
      summary: Resolve queries for the address associated with a given name
      operationId: Resolve
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              address:
                type: string
                title: a string containing the address the name resolves to
              restricted:
                type: boolean
                description: Whether owner signature is required to add sub-names.
            description: >-
              QueryResolveResponse is the response type for the Query/Resolve
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: name
          description: name to resolve the address for
          in: path
          required: true
          type: string
      tags:
        - Name
  /cosmos/sanction/v1beta1/all:
    get:
      summary: SanctionedAddresses returns a list of sanctioned addresses.
      operationId: SanctionedAddresses
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              addresses:
                type: array
                items:
                  type: string
                description: addresses is the list of sanctioned account addresses.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QuerySanctionedAddressesResponse defines the RPC response of a
              SanctionedAddresses query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Sanction
  /cosmos/sanction/v1beta1/check/{address}:
    get:
      summary: IsSanctioned checks if an account has been sanctioned.
      operationId: IsSanctioned
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              is_sanctioned:
                type: boolean
                description: is_sanctioned is true if the address is sanctioned.
            description: >-
              QueryIsSanctionedResponse defines the RPC response of an
              IsSanctioned query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
      tags:
        - Sanction
  /cosmos/sanction/v1beta1/params:
    get:
      summary: Params returns the sanction module's params.
      operationId: SanctionParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params are the sanction module parameters.
                type: object
                properties:
                  immediate_sanction_min_deposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      immediate_sanction_min_deposit is the minimum deposit for
                      a sanction to happen immediately.

                      If this is zero, immediate sanctioning is not available.

                      Otherwise, if a sanction governance proposal is issued
                      with a deposit at least this large, a temporary sanction

                      will be immediately issued that will expire when voting
                      ends on the governance proposal.
                  immediate_unsanction_min_deposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      immediate_unsanction_min_deposit is the minimum deposit
                      for an unsanction to happen immediately.

                      If this is zero, immediate unsanctioning is not available.

                      Otherwise, if an unsanction governance proposal is issued
                      with a deposit at least this large, a temporary

                      unsanction will be immediately issued that will expire
                      when voting ends on the governance proposal.
            description: QueryParamsResponse defines the RPC response of a Params query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Sanction
  /cosmos/sanction/v1beta1/temp:
    get:
      summary: TemporaryEntries returns temporary sanction/unsanction info.
      operationId: TemporaryEntries
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              entries:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: address is the address of this temporary entry.
                    proposal_id:
                      type: string
                      format: uint64
                      description: >-
                        proposal_id is the governance proposal id associated
                        with this temporary entry.
                    status:
                      description: status is whether the entry is a sanction or unsanction.
                      type: string
                      enum:
                        - TEMP_STATUS_UNSPECIFIED
                        - TEMP_STATUS_SANCTIONED
                        - TEMP_STATUS_UNSANCTIONED
                      default: TEMP_STATUS_UNSPECIFIED
                  description: >-
                    TemporaryEntry defines the information involved in a
                    temporary sanction or unsanction.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryTemporaryEntriesResponse defines the RPC response of a
              TemporaryEntries query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address is an optional address to restrict results to.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Sanction
  /provenance/registry/v1/has_role/{key.asset_class_id}/{key.nft_id}/{address}/{role}:
    get:
      summary: >-
        HasRole returns true if the address has the specified role for the given
        key.
      operationId: HasRole
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              has_role:
                type: boolean
                description: >-
                  has_role is true if the address has the specified role for the
                  given key.

                  This boolean value indicates whether the role verification was
                  successful.
            description: >-
              QueryHasRoleResponse is the response type for the Query/HasRole
              RPC method.

              It provides a boolean result indicating whether the address has
              the specified role.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: >-
            asset_class_id is the Scope Specification ID or NFT Class ID.

            This identifies the class or specification that the NFT belongs to.

            If it is a Scope Specification, the address is used,
            e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

            Otherwise, it is the NFT Class ID, e.g. "heloc-loan-class".
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an NFT, the NFT class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
        - name: address
          description: |-
            address is the blockchain address to check for the role.
            This is the address whose role permissions are being verified.
          in: path
          required: true
          type: string
        - name: role
          description: |-
            role is the specific role to check for.
            This determines which role permission is being verified.
          in: path
          required: true
          type: string
          enum:
            - REGISTRY_ROLE_UNSPECIFIED
            - REGISTRY_ROLE_SERVICER
            - REGISTRY_ROLE_SUBSERVICER
            - REGISTRY_ROLE_CONTROLLER
            - REGISTRY_ROLE_CUSTODIAN
            - REGISTRY_ROLE_BORROWER
            - REGISTRY_ROLE_ORIGINATOR
      tags:
        - Registry
  /provenance/registry/v1/registry:
    get:
      summary: >-
        GetRegistries returns all registry entries, optionally filtered by asset
        class ID.

        This method retrieves the complete registry entries including all roles
        and addresses.
      operationId: GetRegistries
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              registries:
                type: array
                items:
                  type: object
                  properties:
                    key:
                      description: >-
                        key ties the registry entry to an asset class and NFT
                        id.

                        This provides the unique identifier for the registry
                        entry.
                      type: object
                      properties:
                        nft_id:
                          type: string
                          description: >-
                            Identifier for the nft that this ledger is linked
                            to.

                            This could be a `x/metadata` scope id or an `x/nft`
                            nft id.

                            In order to create a ledger for an NFT, the NFT
                            class must be registered in the ledger module as a
                            LedgerClass.
                        asset_class_id:
                          type: string
                          description: >-
                            asset_class_id is the Scope Specification ID or NFT
                            Class ID.

                            This identifies the class or specification that the
                            NFT belongs to.

                            If it is a Scope Specification, the address is used,
                            e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

                            Otherwise, it is the NFT Class ID, e.g.
                            "heloc-loan-class".
                    roles:
                      type: array
                      items:
                        type: object
                        properties:
                          role:
                            description: role is the type of role being assigned.
                            type: string
                            enum:
                              - REGISTRY_ROLE_UNSPECIFIED
                              - REGISTRY_ROLE_SERVICER
                              - REGISTRY_ROLE_SUBSERVICER
                              - REGISTRY_ROLE_CONTROLLER
                              - REGISTRY_ROLE_CUSTODIAN
                              - REGISTRY_ROLE_BORROWER
                              - REGISTRY_ROLE_ORIGINATOR
                            default: REGISTRY_ROLE_UNSPECIFIED
                          addresses:
                            type: array
                            items:
                              type: string
                            description: >-
                              addresses is the list of blockchain addresses that
                              can perform this role.

                              These addresses have the permissions associated
                              with the specified role.
                        description: >-
                          RolesEntry represents a mapping between a role and the
                          addresses that can perform that role.

                          This allows multiple addresses to be assigned the same
                          role for a given registry entry.
                      description: >-
                        roles is a list of roles and addresses that can perform
                        that role.

                        Each role entry contains a role type and the addresses
                        authorized for that role.
                  description: >-
                    RegistryEntry represents a single entry in the registry,
                    mapping a blockchain address to its roles.

                    Each entry contains the key information and the roles
                    assigned to various addresses.
                description: >-
                  registries is the collection of registry entries.

                  This includes all roles and addresses associated with the
                  specified NFT and asset class.
              pagination:
                description: pagination is the pagination details for this response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGetRegistriesResponse is the paginated response type for the
              Query/GetRegistries RPC method.

              It contains the complete registry entry for the requested key.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: asset_class_id
          description: |-
            asset_class_id is the Scope Specification ID or NFT Class ID.
            This identifies the class or specification that the NFTs belong to.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Registry
  /provenance/registry/v1/registry/{key.asset_class_id}/{key.nft_id}:
    get:
      summary: >-
        GetRegistry returns the registry entry for a given key.

        This method retrieves the complete registry entry including all roles
        and addresses.
      operationId: GetRegistry
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              registry:
                description: >-
                  registry is the registry entry for the requested key.

                  This includes all roles and addresses associated with the
                  specified NFT and asset class.
                type: object
                properties:
                  key:
                    description: >-
                      key ties the registry entry to an asset class and NFT id.

                      This provides the unique identifier for the registry
                      entry.
                    type: object
                    properties:
                      nft_id:
                        type: string
                        description: >-
                          Identifier for the nft that this ledger is linked to.

                          This could be a `x/metadata` scope id or an `x/nft`
                          nft id.

                          In order to create a ledger for an NFT, the NFT class
                          must be registered in the ledger module as a
                          LedgerClass.
                      asset_class_id:
                        type: string
                        description: >-
                          asset_class_id is the Scope Specification ID or NFT
                          Class ID.

                          This identifies the class or specification that the
                          NFT belongs to.

                          If it is a Scope Specification, the address is used,
                          e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

                          Otherwise, it is the NFT Class ID, e.g.
                          "heloc-loan-class".
                  roles:
                    type: array
                    items:
                      type: object
                      properties:
                        role:
                          description: role is the type of role being assigned.
                          type: string
                          enum:
                            - REGISTRY_ROLE_UNSPECIFIED
                            - REGISTRY_ROLE_SERVICER
                            - REGISTRY_ROLE_SUBSERVICER
                            - REGISTRY_ROLE_CONTROLLER
                            - REGISTRY_ROLE_CUSTODIAN
                            - REGISTRY_ROLE_BORROWER
                            - REGISTRY_ROLE_ORIGINATOR
                          default: REGISTRY_ROLE_UNSPECIFIED
                        addresses:
                          type: array
                          items:
                            type: string
                          description: >-
                            addresses is the list of blockchain addresses that
                            can perform this role.

                            These addresses have the permissions associated with
                            the specified role.
                      description: >-
                        RolesEntry represents a mapping between a role and the
                        addresses that can perform that role.

                        This allows multiple addresses to be assigned the same
                        role for a given registry entry.
                    description: >-
                      roles is a list of roles and addresses that can perform
                      that role.

                      Each role entry contains a role type and the addresses
                      authorized for that role.
            description: >-
              QueryGetRegistryResponse is the response type for the
              Query/GetRegistry RPC method.

              It contains the complete registry entry for the requested key.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: key.asset_class_id
          description: >-
            asset_class_id is the Scope Specification ID or NFT Class ID.

            This identifies the class or specification that the NFT belongs to.

            If it is a Scope Specification, the address is used,
            e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

            Otherwise, it is the NFT Class ID, e.g. "heloc-loan-class".
          in: path
          required: true
          type: string
        - name: key.nft_id
          description: >-
            Identifier for the nft that this ledger is linked to.

            This could be a `x/metadata` scope id or an `x/nft` nft id.

            In order to create a ledger for an NFT, the NFT class must be
            registered in the ledger module as a LedgerClass.
          in: path
          required: true
          type: string
      tags:
        - Registry
  /provenance/trigger/v1/triggers:
    get:
      summary: Triggers returns the list of triggers.
      operationId: Triggers
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              triggers:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                      description: An integer to uniquely identify the trigger.
                    owner:
                      type: string
                      description: The owner of the trigger.
                    event:
                      description: The event that must be detected for the trigger to fire.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                    actions:
                      type: array
                      items:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := ptypes.MarshalAny(foo)
                               ...
                               foo := &pb.Foo{}
                               if err := ptypes.UnmarshalAny(any, foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".



                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                      description: The messages to run when the trigger fires.
                  title: Trigger
                description: List of Trigger objects.
              pagination:
                description: pagination defines an optional pagination for the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: QueryTriggersResponse contains the list of Triggers.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Trigger
  /provenance/trigger/v1/triggers/{id}:
    get:
      summary: TriggerByID returns a trigger matching the ID.
      operationId: TriggerByID
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              trigger:
                description: The trigger object that was queried for.
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                    description: An integer to uniquely identify the trigger.
                  owner:
                    type: string
                    description: The owner of the trigger.
                  event:
                    description: The event that must be detected for the trigger to fire.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                  actions:
                    type: array
                    items:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := ptypes.MarshalAny(foo)
                             ...
                             foo := &pb.Foo{}
                             if err := ptypes.UnmarshalAny(any, foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".



                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    description: The messages to run when the trigger fires.
                title: Trigger
            description: QueryTriggerByIDResponse contains the requested Trigger.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: The id of the trigger to query.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Trigger
  /vault/v1/params:
    get:
      summary: Params returns the current module parameters.
      operationId: Params
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the vault module parameters.
                type: object
                properties:
                  tech_fee_address:
                    type: string
                    description: >-
                      tech_fee_address is the address where all AUM fees are
                      collected and which holds

                      the authority to update per-vault bips.
                  default_aum_fee_bips:
                    type: integer
                    format: int64
                    description: >-
                      default_aum_fee_bips is the default fee rate (in basis
                      points) applied to new vaults upon creation.
                  gov_only_vault_creation:
                    type: boolean
                    description: >-
                      gov_only_vault_creation restricts CreateVault to the
                      governance module account.

                      When true, a vault can only come into existence through a
                      passed governance proposal.

                      When false (the default), any account may sign CreateVault
                      directly, which is how

                      development, docker, and testnet chains are expected to
                      run.
            description: >-
              QueryParamsResponse is the response message for the Query/Params
              endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Vault
  /vault/v1/pending_swap_outs:
    get:
      summary: PendingSwapOuts returns a paginated list of all pending swap outs.
      operationId: PendingSwapOuts
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              pending_swap_outs:
                type: array
                items:
                  type: object
                  properties:
                    request_id:
                      type: string
                      format: uint64
                      description: >-
                        request_id is the unique identifier for the pending swap
                        out request.
                    pending_swap_out:
                      description: >-
                        pending_swap_out contains the details of the swap out
                        request.
                      type: object
                      properties:
                        owner:
                          type: string
                          description: owner is the address initiating the swap out.
                        vault_address:
                          type: string
                          description: >-
                            vault_address is the address of the vault processing
                            the withdrawal.
                        shares:
                          description: >-
                            shares are the shares that were escrowed by the
                            user.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        redeem_denom:
                          type: string
                          description: >-
                            redeem_denom is the denomination of the asset to be
                            redeemed. Always the

                            vault's underlying_asset; retained for wire
                            compatibility.
                        failure_count:
                          type: integer
                          format: int64
                          description: >-
                            failure_count is the number of consecutive failed
                            processing attempts that

                            left this request queued. Each failure re-keys the
                            request to a later retry

                            time so it cannot hold the front of the queue. Reset
                            when expedited.
                    timeout:
                      type: string
                      format: date-time
                      description: >-
                        timeout is the time at which the pending swap out will
                        expire if not processed.
                  description: >-
                    PendingSwapOutWithTimeout is a pending swap out with its
                    timeout.
                description: pending_swap_outs is a list of all pending swap outs.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryPendingSwapOutsResponse is the response message for the
              Query/PendingSwapOuts endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Vault
  /vault/v1/vaults:
    get:
      summary: Vaults returns a paginated list of all vaults.
      operationId: Vaults
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              vaults:
                type: array
                items:
                  type: object
                  properties:
                    base_account:
                      description: >-
                        base_account cosmos account information including
                        address and coin holdings.
                      type: object
                      properties:
                        address:
                          type: string
                        pub_key:
                          type: object
                          properties:
                            '@type':
                              type: string
                              description: >-
                                A URL/resource name that uniquely identifies the
                                type of the serialized

                                protocol buffer message. This string must
                                contain at least

                                one "/" character. The last segment of the URL's
                                path must represent

                                the fully qualified name of the type (as in

                                `path/google.protobuf.Duration`). The name
                                should be in a canonical form

                                (e.g., leading "." is not accepted).


                                In practice, teams usually precompile into the
                                binary all types that they

                                expect it to use in the context of Any. However,
                                for URLs which use the

                                scheme `http`, `https`, or no scheme, one can
                                optionally set up a type

                                server that maps type URLs to message
                                definitions as follows:


                                * If no scheme is provided, `https` is assumed.

                                * An HTTP GET on the URL must yield a
                                [google.protobuf.Type][]
                                  value in binary format, or produce an error.
                                * Applications are allowed to cache lookup
                                results based on the
                                  URL, or have them precompiled into a binary to avoid any
                                  lookup. Therefore, binary compatibility needs to be preserved
                                  on changes to types. (Use versioned type names to manage
                                  breaking changes.)

                                Note: this functionality is not currently
                                available in the official

                                protobuf release, and it is not used for type
                                URLs beginning with

                                type.googleapis.com.


                                Schemes other than `http`, `https` (or the empty
                                scheme) might be

                                used with implementation specific semantics.
                          additionalProperties: {}
                          description: >-
                            `Any` contains an arbitrary serialized protocol
                            buffer message along with a

                            URL that describes the type of the serialized
                            message.


                            Protobuf library provides support to pack/unpack Any
                            values in the form

                            of utility functions or additional generated methods
                            of the Any type.


                            Example 1: Pack and unpack a message in C++.

                                Foo foo = ...;
                                Any any;
                                any.PackFrom(foo);
                                ...
                                if (any.UnpackTo(&foo)) {
                                  ...
                                }

                            Example 2: Pack and unpack a message in Java.

                                Foo foo = ...;
                                Any any = Any.pack(foo);
                                ...
                                if (any.is(Foo.class)) {
                                  foo = any.unpack(Foo.class);
                                }

                             Example 3: Pack and unpack a message in Python.

                                foo = Foo(...)
                                any = Any()
                                any.Pack(foo)
                                ...
                                if any.Is(Foo.DESCRIPTOR):
                                  any.Unpack(foo)
                                  ...

                             Example 4: Pack and unpack a message in Go

                                 foo := &pb.Foo{...}
                                 any, err := ptypes.MarshalAny(foo)
                                 ...
                                 foo := &pb.Foo{}
                                 if err := ptypes.UnmarshalAny(any, foo); err != nil {
                                   ...
                                 }

                            The pack methods provided by protobuf library will
                            by default use

                            'type.googleapis.com/full.type.name' as the type URL
                            and the unpack

                            methods only use the fully qualified type name after
                            the last '/'

                            in the type URL, for example "foo.bar.com/x/y.z"
                            will yield type

                            name "y.z".



                            JSON

                            ====

                            The JSON representation of an `Any` value uses the
                            regular

                            representation of the deserialized, embedded
                            message, with an

                            additional field `@type` which contains the type
                            URL. Example:

                                package google.profile;
                                message Person {
                                  string first_name = 1;
                                  string last_name = 2;
                                }

                                {
                                  "@type": "type.googleapis.com/google.profile.Person",
                                  "firstName": <string>,
                                  "lastName": <string>
                                }

                            If the embedded message type is well-known and has a
                            custom JSON

                            representation, that representation will be embedded
                            adding a field

                            `value` which holds the custom JSON in addition to
                            the `@type`

                            field. Example (for message
                            [google.protobuf.Duration][]):

                                {
                                  "@type": "type.googleapis.com/google.protobuf.Duration",
                                  "value": "1.212s"
                                }
                        account_number:
                          type: string
                          format: uint64
                        sequence:
                          type: string
                          format: uint64
                    total_shares:
                      description: >-
                        total_shares is the total number of shares that have
                        ever been issued by the vault.

                        It serves as the canonical supply-of-record for all
                        shares, regardless of whether

                        they are held locally on Provenance or externally (e.g.,
                        bridged).
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    underlying_asset:
                      type: string
                      description: >-
                        underlying_asset is the vault’s single principal
                        collateral AND valuation/base unit.

                        - Exactly one denom.

                        - Total Vault Value (TVV) and NAV-per-share are computed
                        and reported in this denom.

                        - Interest accrual, fees, and internal accounting are
                        measured in this denom.

                        - Held assets acquired via settlement must have an
                        internal NAV record priced INTO this denom.
                    payment_denom:
                      type: string
                      description: >-
                        payment_denom always equals underlying_asset. Vaults are
                        single-denom; the

                        field is inert and retained only for wire compatibility.


                        Deprecated: The mixed-denom vault concept has been
                        flattened into a single

                        underlying denom. Deletion of this field is deferred to
                        a future major release.
                    admin:
                      type: string
                      description: >-
                        admin is the address that has administrative privileges
                        over the vault.
                    current_interest_rate:
                      type: string
                      description: >-
                        current_interest_rate is a decimal string (e.g., "0.9"
                        for 90% and "0.9001353" for 90.01353%) representing the
                        actual annual interest rate currently being applied.

                        This may be adjusted programmatically (e.g., due to lack
                        of funds).
                    desired_interest_rate:
                      type: string
                      description: >-
                        desired_interest_rate is a decimal string (e.g., "0.9"
                        for 90% and "0.9001353" for 90.01353%) representing the
                        target annual interest rate that the vault intends to
                        apply.
                    min_interest_rate:
                      type: string
                      description: >-
                        min_interest_rate is a decimal string (e.g., "0.9" for
                        90% and "0.9001353" for 90.01353%) representing the
                        lowest annual interest rate the admin is allowed to set.

                        If unset (empty string), there is no lower limit.
                    max_interest_rate:
                      type: string
                      description: >-
                        max_interest_rate is a decimal string (e.g., "0.9" for
                        90% and "0.9001353" for 90.01353%) representing the
                        highest annual interest rate the admin is allowed to
                        set.

                        If unset (empty string), there is no upper limit.
                    period_start:
                      type: string
                      format: int64
                      description: >-
                        The start time (in Unix seconds) of the current interest
                        accrual period.
                    period_timeout:
                      type: string
                      format: int64
                      description: >-
                        The expire time (in Unix seconds) of the current
                        interest accrual period.
                    swap_in_enabled:
                      type: boolean
                      description: >-
                        swap_in_enabled indicates whether users are allowed to
                        deposit into the vault.
                    swap_out_enabled:
                      type: boolean
                      description: >-
                        swap_out_enabled indicates whether users are allowed to
                        withdraw from the vault.
                    withdrawal_delay_seconds:
                      type: string
                      format: uint64
                      description: >-
                        withdrawal_delay_seconds is the configured time period
                        (in seconds) that a withdrawal

                        request must wait in the pending queue before being
                        processed.
                    paused:
                      type: boolean
                      description: >-
                        paused indicates that all user-facing swap-in and
                        swap-out operations are disabled.
                    paused_balance:
                      description: >-
                        paused_balance is the total vault value snapshot taken
                        at the moment of pausing.

                        This value is used for all NAV calculations while the
                        vault is paused to prevent

                        apparent devaluation during collateral rebalancing. It
                        is cleared upon unpausing.

                        Nothing done during the pause moves it, neither a
                        principal deposit or withdrawal

                        nor a NAV repricing; those all take effect together when
                        the vault is unpaused.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    paused_reason:
                      type: string
                      description: >-
                        paused_reason is a human-readable string explaining why
                        the vault was paused, particularly for automatic pauses.
                    paused_by:
                      type: string
                      description: >-
                        paused_by is the address that initiated the current
                        pause, empty for an automatic

                        pause and cleared on unpause. MsgRepriceVault consults
                        it so the NAV authority

                        can only resume a pause it took itself.
                    paused_forced:
                      type: boolean
                      description: >-
                        paused_forced reports whether the current pause waived
                        the strict reconcile and

                        valuation gate, true for a forced MsgPauseVault and
                        every automatic pause. A forced

                        pause records a tolerated failure, so MsgRepriceVault
                        refuses to resume one.
                    bridge_address:
                      type: string
                      description: >-
                        bridge_address is the single external address allowed to
                        mint or burn shares on behalf

                        of this vault (e.g., for bridging to another chain). All
                        mint/burn must flow through the

                        vault keeper, which enforces that marker supply never
                        exceeds total_shares.
                    bridge_enabled:
                      type: boolean
                      description: >-
                        bridge_enabled indicates whether the bridge
                        functionality is active. If false, the

                        bridge_address has no effect and cannot mint or burn.
                    asset_manager:
                      type: string
                      description: >-
                        asset_manager is an optional address that, when set, is
                        authorized to manage certain

                        collateral operations alongside the admin (e.g.,
                        pausing/unpausing, depositing/withdrawing

                        principal or interest funds). If unset (empty string),
                        only the admin may perform those actions.
                    fee_period_start:
                      type: string
                      format: int64
                      description: >-
                        fee_period_start is the start time (in Unix seconds) of
                        the current AUM fee collection period.

                        This is a module-managed timestamp kept in sync with the
                        fee timeout queue machinery.
                    fee_period_timeout:
                      type: string
                      format: int64
                      description: >-
                        fee_period_timeout is the end time (in Unix seconds) of
                        the current AUM fee collection period.

                        This is a module-managed timestamp kept in sync with the
                        fee timeout queue machinery.
                    outstanding_aum_fee:
                      description: >-
                        outstanding_aum_fee is the amount of AUM fee that has
                        been calculated but not yet collected

                        due to insufficient liquidity in the principal marker.
                        This amount is always denominated

                        in the vault's underlying_asset, must be preserved, and
                        is carried into valuation computations.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    aum_fee_bips:
                      type: integer
                      format: int64
                      description: >-
                        aum_fee_bips is the AUM fee rate (in basis points) for
                        this specific vault.

                        Units: Basis Points (1 bps = 0.01%).

                        Valid Range: 0 to 10000 (0% to 100%).

                        A value of 0 disables AUM fee collection for this vault.

                        Negative values are not supported (uint32).
                    min_swap_in_value:
                      type: string
                      description: >-
                        min_swap_in_value is a string representing the minimum
                        value required for a deposit.

                        - The value is measured in the underlying_asset.

                        - Values must be non-negative (>= 0).

                        - If empty ("") or "0", there is no minimum limit.
                    min_swap_out_value:
                      type: string
                      description: >-
                        min_swap_out_value is a string representing the minimum
                        value required for a withdrawal.

                        - The value is measured in the underlying_asset.

                        - Outgoing redemptions are converted to this unit before
                        checking.

                        - Values must be non-negative (>= 0).

                        - If empty ("") or "0", there is no minimum limit.
                    max_swap_in_value:
                      type: string
                      description: >-
                        max_swap_in_value is a string representing the maximum
                        value allowed for a deposit.

                        - The value is measured in the underlying_asset.

                        - Values must be positive (> 0).

                        - An empty string "" indicates no maximum limit.
                    max_swap_out_value:
                      type: string
                      description: >-
                        max_swap_out_value is a string representing the maximum
                        value allowed for a withdrawal.

                        - The value is measured in the underlying_asset.

                        - Outgoing redemptions are converted to this unit before
                        checking.

                        - Values must be positive (> 0).

                        - An empty string "" indicates no maximum limit.
                    nav_authority:
                      type: string
                      description: >-
                        nav_authority is the address authorized to mutate this
                        vault's internal NAV

                        table via MsgUpdateVaultNAV. If empty, the vault admin
                        is treated as the NAV

                        authority. It is rotated only via MsgUpdateNAVAuthority.
                  description: >-
                    VaultAccount represents a central holding place for assets,
                    governed by a set of rules.

                    It is based on the ERC-4626 standard and builds upon the
                    Provenance Marker module.
                description: vaults is a list of all vaults.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVaultsResponse is the response message for the Query/Vaults
              endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Vault
  /vault/v1/vaults/{id}:
    get:
      summary: Vault returns the configuration and state of a specific vault.
      operationId: Vault
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              vault:
                description: vault is the requested vault.
                type: object
                properties:
                  base_account:
                    description: >-
                      base_account cosmos account information including address
                      and coin holdings.
                    type: object
                    properties:
                      address:
                        type: string
                      pub_key:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := ptypes.MarshalAny(foo)
                               ...
                               foo := &pb.Foo{}
                               if err := ptypes.UnmarshalAny(any, foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".



                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                      account_number:
                        type: string
                        format: uint64
                      sequence:
                        type: string
                        format: uint64
                  total_shares:
                    description: >-
                      total_shares is the total number of shares that have ever
                      been issued by the vault.

                      It serves as the canonical supply-of-record for all
                      shares, regardless of whether

                      they are held locally on Provenance or externally (e.g.,
                      bridged).
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                  underlying_asset:
                    type: string
                    description: >-
                      underlying_asset is the vault’s single principal
                      collateral AND valuation/base unit.

                      - Exactly one denom.

                      - Total Vault Value (TVV) and NAV-per-share are computed
                      and reported in this denom.

                      - Interest accrual, fees, and internal accounting are
                      measured in this denom.

                      - Held assets acquired via settlement must have an
                      internal NAV record priced INTO this denom.
                  payment_denom:
                    type: string
                    description: >-
                      payment_denom always equals underlying_asset. Vaults are
                      single-denom; the

                      field is inert and retained only for wire compatibility.


                      Deprecated: The mixed-denom vault concept has been
                      flattened into a single

                      underlying denom. Deletion of this field is deferred to a
                      future major release.
                  admin:
                    type: string
                    description: >-
                      admin is the address that has administrative privileges
                      over the vault.
                  current_interest_rate:
                    type: string
                    description: >-
                      current_interest_rate is a decimal string (e.g., "0.9" for
                      90% and "0.9001353" for 90.01353%) representing the actual
                      annual interest rate currently being applied.

                      This may be adjusted programmatically (e.g., due to lack
                      of funds).
                  desired_interest_rate:
                    type: string
                    description: >-
                      desired_interest_rate is a decimal string (e.g., "0.9" for
                      90% and "0.9001353" for 90.01353%) representing the target
                      annual interest rate that the vault intends to apply.
                  min_interest_rate:
                    type: string
                    description: >-
                      min_interest_rate is a decimal string (e.g., "0.9" for 90%
                      and "0.9001353" for 90.01353%) representing the lowest
                      annual interest rate the admin is allowed to set.

                      If unset (empty string), there is no lower limit.
                  max_interest_rate:
                    type: string
                    description: >-
                      max_interest_rate is a decimal string (e.g., "0.9" for 90%
                      and "0.9001353" for 90.01353%) representing the highest
                      annual interest rate the admin is allowed to set.

                      If unset (empty string), there is no upper limit.
                  period_start:
                    type: string
                    format: int64
                    description: >-
                      The start time (in Unix seconds) of the current interest
                      accrual period.
                  period_timeout:
                    type: string
                    format: int64
                    description: >-
                      The expire time (in Unix seconds) of the current interest
                      accrual period.
                  swap_in_enabled:
                    type: boolean
                    description: >-
                      swap_in_enabled indicates whether users are allowed to
                      deposit into the vault.
                  swap_out_enabled:
                    type: boolean
                    description: >-
                      swap_out_enabled indicates whether users are allowed to
                      withdraw from the vault.
                  withdrawal_delay_seconds:
                    type: string
                    format: uint64
                    description: >-
                      withdrawal_delay_seconds is the configured time period (in
                      seconds) that a withdrawal

                      request must wait in the pending queue before being
                      processed.
                  paused:
                    type: boolean
                    description: >-
                      paused indicates that all user-facing swap-in and swap-out
                      operations are disabled.
                  paused_balance:
                    description: >-
                      paused_balance is the total vault value snapshot taken at
                      the moment of pausing.

                      This value is used for all NAV calculations while the
                      vault is paused to prevent

                      apparent devaluation during collateral rebalancing. It is
                      cleared upon unpausing.

                      Nothing done during the pause moves it, neither a
                      principal deposit or withdrawal

                      nor a NAV repricing; those all take effect together when
                      the vault is unpaused.
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                  paused_reason:
                    type: string
                    description: >-
                      paused_reason is a human-readable string explaining why
                      the vault was paused, particularly for automatic pauses.
                  paused_by:
                    type: string
                    description: >-
                      paused_by is the address that initiated the current pause,
                      empty for an automatic

                      pause and cleared on unpause. MsgRepriceVault consults it
                      so the NAV authority

                      can only resume a pause it took itself.
                  paused_forced:
                    type: boolean
                    description: >-
                      paused_forced reports whether the current pause waived the
                      strict reconcile and

                      valuation gate, true for a forced MsgPauseVault and every
                      automatic pause. A forced

                      pause records a tolerated failure, so MsgRepriceVault
                      refuses to resume one.
                  bridge_address:
                    type: string
                    description: >-
                      bridge_address is the single external address allowed to
                      mint or burn shares on behalf

                      of this vault (e.g., for bridging to another chain). All
                      mint/burn must flow through the

                      vault keeper, which enforces that marker supply never
                      exceeds total_shares.
                  bridge_enabled:
                    type: boolean
                    description: >-
                      bridge_enabled indicates whether the bridge functionality
                      is active. If false, the

                      bridge_address has no effect and cannot mint or burn.
                  asset_manager:
                    type: string
                    description: >-
                      asset_manager is an optional address that, when set, is
                      authorized to manage certain

                      collateral operations alongside the admin (e.g.,
                      pausing/unpausing, depositing/withdrawing

                      principal or interest funds). If unset (empty string),
                      only the admin may perform those actions.
                  fee_period_start:
                    type: string
                    format: int64
                    description: >-
                      fee_period_start is the start time (in Unix seconds) of
                      the current AUM fee collection period.

                      This is a module-managed timestamp kept in sync with the
                      fee timeout queue machinery.
                  fee_period_timeout:
                    type: string
                    format: int64
                    description: >-
                      fee_period_timeout is the end time (in Unix seconds) of
                      the current AUM fee collection period.

                      This is a module-managed timestamp kept in sync with the
                      fee timeout queue machinery.
                  outstanding_aum_fee:
                    description: >-
                      outstanding_aum_fee is the amount of AUM fee that has been
                      calculated but not yet collected

                      due to insufficient liquidity in the principal marker.
                      This amount is always denominated

                      in the vault's underlying_asset, must be preserved, and is
                      carried into valuation computations.
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                  aum_fee_bips:
                    type: integer
                    format: int64
                    description: >-
                      aum_fee_bips is the AUM fee rate (in basis points) for
                      this specific vault.

                      Units: Basis Points (1 bps = 0.01%).

                      Valid Range: 0 to 10000 (0% to 100%).

                      A value of 0 disables AUM fee collection for this vault.

                      Negative values are not supported (uint32).
                  min_swap_in_value:
                    type: string
                    description: >-
                      min_swap_in_value is a string representing the minimum
                      value required for a deposit.

                      - The value is measured in the underlying_asset.

                      - Values must be non-negative (>= 0).

                      - If empty ("") or "0", there is no minimum limit.
                  min_swap_out_value:
                    type: string
                    description: >-
                      min_swap_out_value is a string representing the minimum
                      value required for a withdrawal.

                      - The value is measured in the underlying_asset.

                      - Outgoing redemptions are converted to this unit before
                      checking.

                      - Values must be non-negative (>= 0).

                      - If empty ("") or "0", there is no minimum limit.
                  max_swap_in_value:
                    type: string
                    description: >-
                      max_swap_in_value is a string representing the maximum
                      value allowed for a deposit.

                      - The value is measured in the underlying_asset.

                      - Values must be positive (> 0).

                      - An empty string "" indicates no maximum limit.
                  max_swap_out_value:
                    type: string
                    description: >-
                      max_swap_out_value is a string representing the maximum
                      value allowed for a withdrawal.

                      - The value is measured in the underlying_asset.

                      - Outgoing redemptions are converted to this unit before
                      checking.

                      - Values must be positive (> 0).

                      - An empty string "" indicates no maximum limit.
                  nav_authority:
                    type: string
                    description: >-
                      nav_authority is the address authorized to mutate this
                      vault's internal NAV

                      table via MsgUpdateVaultNAV. If empty, the vault admin is
                      treated as the NAV

                      authority. It is rotated only via MsgUpdateNAVAuthority.
              principal:
                description: >-
                  principal is the total amount of principal held in the vault's
                  marker.
                type: object
                properties:
                  address:
                    type: string
                    description: address is the account address.
                  coins:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: coins is the balance of the account.
              reserves:
                description: >-
                  reserves is the total amount of reserves held in the vault
                  account for interest payments.
                type: object
                properties:
                  address:
                    type: string
                    description: address is the account address.
                  coins:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: coins is the balance of the account.
              total_vault_value:
                description: >-
                  total_vault_value is the estimated total value of the vault in
                  its

                  underlying asset. It includes current principal and estimated
                  unpaid

                  interest (at query block height), but excludes reserves. The
                  value is approximate and may differ

                  from the reconciled amount.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
            description: >-
              QueryVaultResponse is the response message for the Query/Vault
              endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: >-
            id is the bech32 address of the vault or the vault's share denom to
            query.
          in: path
          required: true
          type: string
      tags:
        - Vault
  /vault/v1/vaults/{id}/navs:
    get:
      summary: >-
        VaultNavs returns a paginated list of all internal NAV entries for a
        vault.
      operationId: VaultNavs
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              navs:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                      description: denom is the asset denomination this entry prices.
                    price:
                      description: >-
                        price is the total value of `volume` units of the denom,
                        denominated in the

                        vault's underlying asset. The per-unit value is price
                        divided by volume.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                    volume:
                      type: string
                      description: >-
                        volume is the number of units of the denom that price
                        covers. It must be

                        positive; the per-unit value of the denom is price
                        divided by volume.
                    source:
                      type: string
                      description: >-
                        source identifies the origin of this NAV entry (for
                        example an oracle name

                        or the settlement path), mirroring the Marker module's
                        NAV source attribution.
                    updated_block_height:
                      type: string
                      format: int64
                      description: >-
                        updated_block_height is the block height at which this
                        entry was last updated.
                    updated_time:
                      type: string
                      format: date-time
                      description: >-
                        updated_time is the block time at which this entry was
                        last updated.
                  description: >-
                    VaultNAV is a single internal net asset value entry
                    recording the price of one

                    asset denom held by a vault. The vault module is the sole
                    source of truth for

                    these values; the NAV authority maintains them via
                    MsgUpdateVaultNAV.
                description: navs is the list of internal NAV entries held by the vault.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVaultNavsResponse is the response message for the
              Query/VaultNavs endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: >-
            id is the bech32 address of the vault or the vault's share denom to
            query.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Vault
  /vault/v1/vaults/{id}/navs/{denom}:
    get:
      summary: NavValue returns a single internal NAV entry for a vault and denom.
      operationId: NavValue
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              nav:
                description: >-
                  nav is the internal NAV entry for the requested vault and
                  denom.
                type: object
                properties:
                  denom:
                    type: string
                    description: denom is the asset denomination this entry prices.
                  price:
                    description: >-
                      price is the total value of `volume` units of the denom,
                      denominated in the

                      vault's underlying asset. The per-unit value is price
                      divided by volume.
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                  volume:
                    type: string
                    description: >-
                      volume is the number of units of the denom that price
                      covers. It must be

                      positive; the per-unit value of the denom is price divided
                      by volume.
                  source:
                    type: string
                    description: >-
                      source identifies the origin of this NAV entry (for
                      example an oracle name

                      or the settlement path), mirroring the Marker module's NAV
                      source attribution.
                  updated_block_height:
                    type: string
                    format: int64
                    description: >-
                      updated_block_height is the block height at which this
                      entry was last updated.
                  updated_time:
                    type: string
                    format: date-time
                    description: >-
                      updated_time is the block time at which this entry was
                      last updated.
            description: >-
              QueryNavValueResponse is the response message for the
              Query/NavValue endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: >-
            id is the bech32 address of the vault or the vault's share denom to
            query.
          in: path
          required: true
          type: string
        - name: denom
          description: denom is the asset denomination whose NAV entry is being queried.
          in: path
          required: true
          type: string
          pattern: .+
      tags:
        - Vault
  /vault/v1/vaults/{id}/payment:
    get:
      summary: >-
        VaultPayment returns a single pending exchange-module payment targeting
        a vault,

        identified by the payment's source account and external id.
      operationId: VaultPayment
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payment:
                description: >-
                  payment is the pending exchange-module payment targeting the
                  vault.
                type: object
                properties:
                  source:
                    type: string
                    description: >-
                      source is the account that created the payment and owns
                      the escrowed source_amount.
                  source_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      source_amount is the funds the source pays the target. A
                      hold is placed on this

                      amount in the source account until the payment is
                      accepted, rejected, or cancelled.
                  target:
                    type: string
                    description: >-
                      target is the account that can accept the payment; for the
                      vault's settlement

                      workflow it is the vault.
                  target_amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      target_amount is the funds the target pays the source in
                      exchange for source_amount.
                  external_id:
                    type: string
                    description: >-
                      external_id, together with source, uniquely identifies the
                      payment.
            description: >-
              QueryVaultPaymentResponse is the response message for the
              Query/VaultPayment endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: >-
            id is the bech32 address of the vault or the vault's share denom to
            query.
          in: path
          required: true
          type: string
        - name: source
          description: >-
            source is the bech32 address of the account that created the
            payment.
          in: query
          required: false
          type: string
        - name: external_id
          description: external_id, together with source, uniquely identifies the payment.
          in: query
          required: false
          type: string
      tags:
        - Vault
  /vault/v1/vaults/{id}/payments:
    get:
      summary: >-
        VaultPayments returns a paginated list of all pending exchange-module
        payments

        targeting a vault.
      operationId: VaultPayments
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              payments:
                type: array
                items:
                  type: object
                  properties:
                    source:
                      type: string
                      description: >-
                        source is the account that created the payment and owns
                        the escrowed source_amount.
                    source_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: >-
                        source_amount is the funds the source pays the target. A
                        hold is placed on this

                        amount in the source account until the payment is
                        accepted, rejected, or cancelled.
                    target:
                      type: string
                      description: >-
                        target is the account that can accept the payment; for
                        the vault's settlement

                        workflow it is the vault.
                    target_amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: >-
                        target_amount is the funds the target pays the source in
                        exchange for source_amount.
                    external_id:
                      type: string
                      description: >-
                        external_id, together with source, uniquely identifies
                        the payment.
                  description: >-
                    Payment is the vault module's view of a Provenance
                    exchange-module payment. It

                    mirrors the exchange Payment, exposing only the fields
                    relevant to the vault's

                    asset settlement workflow.


                    It serves two roles. As a query result it reports a pending
                    payment targeting the

                    vault. In MsgAcceptAssetRequest it carries the terms the
                    asset manager reviewed, and

                    settlement proceeds only if they still match the stored
                    payment exactly.
                description: >-
                  payments is the list of pending exchange-module payments
                  targeting the vault.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVaultPaymentsResponse is the response message for the
              Query/VaultPayments endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: >-
            id is the bech32 address of the vault or the vault's share denom to
            query.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Vault
  /vault/v1/vaults/{id}/pending_swap_outs:
    get:
      summary: >-
        VaultPendingSwapOuts returns a paginated list of all pending swap outs
        for a specific vault.
      operationId: VaultPendingSwapOuts
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              pending_swap_outs:
                type: array
                items:
                  type: object
                  properties:
                    request_id:
                      type: string
                      format: uint64
                      description: >-
                        request_id is the unique identifier for the pending swap
                        out request.
                    pending_swap_out:
                      description: >-
                        pending_swap_out contains the details of the swap out
                        request.
                      type: object
                      properties:
                        owner:
                          type: string
                          description: owner is the address initiating the swap out.
                        vault_address:
                          type: string
                          description: >-
                            vault_address is the address of the vault processing
                            the withdrawal.
                        shares:
                          description: >-
                            shares are the shares that were escrowed by the
                            user.
                          type: object
                          properties:
                            denom:
                              type: string
                            amount:
                              type: string
                        redeem_denom:
                          type: string
                          description: >-
                            redeem_denom is the denomination of the asset to be
                            redeemed. Always the

                            vault's underlying_asset; retained for wire
                            compatibility.
                        failure_count:
                          type: integer
                          format: int64
                          description: >-
                            failure_count is the number of consecutive failed
                            processing attempts that

                            left this request queued. Each failure re-keys the
                            request to a later retry

                            time so it cannot hold the front of the queue. Reset
                            when expedited.
                    timeout:
                      type: string
                      format: date-time
                      description: >-
                        timeout is the time at which the pending swap out will
                        expire if not processed.
                  description: >-
                    PendingSwapOutWithTimeout is a pending swap out with its
                    timeout.
                description: pending_swap_outs is a list of all pending swap outs.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVaultPendingSwapOutsResponse is the response message for the
              Query/VaultPendingSwapOuts endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: >-
            id is the bech32 address of the vault or the vault's share denom to
            query.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Vault
  /vault/v1/vaults/{vault_address}/estimate_swap_in:
    get:
      summary: >-
        EstimateSwapIn estimates the amount of shares that would be received for
        a given amount of underlying assets.
      operationId: EstimateSwapIn
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              assets:
                description: >-
                  assets is the estimated amount of shares that would be
                  received.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              height:
                type: string
                format: int64
                description: The block height when the estimate occurred.
              time:
                type: string
                format: date-time
                description: The UTC block time when the estimate occurred.
            description: >-
              QueryEstimateSwapInResponse is the response message for the
              Query/EstimateSwapIn endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: vault_address
          description: vault_address is the bech32 address of the vault to query.
          in: path
          required: true
          type: string
        - name: assets.denom
          in: query
          required: false
          type: string
        - name: assets.amount
          in: query
          required: false
          type: string
      tags:
        - Vault
  /vault/v1/vaults/{vault_address}/estimate_swap_out:
    get:
      summary: >-
        EstimateSwapOut estimates the amount of underlying assets that would be
        received for a given amount of shares.
      operationId: EstimateSwapOut
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              assets:
                description: >-
                  assets is the estimated amount of underlying assets that would
                  be received.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              height:
                type: string
                format: int64
                description: The block height when the estimate occurred.
              time:
                type: string
                format: date-time
                description: The UTC block time when the estimate occurred.
            description: >-
              QueryEstimateSwapOutResponse is the response message for the
              Query/EstimateSwapOut endpoint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: vault_address
          description: vault_address is the bech32 address of the vault to query.
          in: path
          required: true
          type: string
        - name: shares
          description: >-
            shares is the amount of shares to swap out, as a non-negative
            integer of at most 80 characters.
          in: query
          required: false
          type: string
        - name: redeem_denom
          description: >-
            redeem_denom previously selected the payout denom to estimate. The
            estimate

            is always in the vault's underlying_asset; if set, this must equal

            underlying_asset. Retained for wire compatibility with released
            clients.
          in: query
          required: false
          type: string
      tags:
        - Vault
  /cosmos/auth/v1beta1/account_info/{address}:
    get:
      summary: AccountInfo queries account info which is common to all account types.
      operationId: AccountInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              info:
                description: info is the account info which is represented by BaseAccount.
                type: object
                properties:
                  address:
                    type: string
                  pubKey:
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                    description: >-
                      `Any` contains an arbitrary serialized protocol buffer
                      message along with a

                      URL that describes the type of the serialized message.


                      Protobuf library provides support to pack/unpack Any
                      values in the form

                      of utility functions or additional generated methods of
                      the Any type.


                      Example 1: Pack and unpack a message in C++.

                          Foo foo = ...;
                          Any any;
                          any.PackFrom(foo);
                          ...
                          if (any.UnpackTo(&foo)) {
                            ...
                          }

                      Example 2: Pack and unpack a message in Java.

                          Foo foo = ...;
                          Any any = Any.pack(foo);
                          ...
                          if (any.is(Foo.class)) {
                            foo = any.unpack(Foo.class);
                          }
                          // or ...
                          if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                            foo = any.unpack(Foo.getDefaultInstance());
                          }

                       Example 3: Pack and unpack a message in Python.

                          foo = Foo(...)
                          any = Any()
                          any.Pack(foo)
                          ...
                          if any.Is(Foo.DESCRIPTOR):
                            any.Unpack(foo)
                            ...

                       Example 4: Pack and unpack a message in Go

                           foo := &pb.Foo{...}
                           any, err := anypb.New(foo)
                           if err != nil {
                             ...
                           }
                           ...
                           foo := &pb.Foo{}
                           if err := any.UnmarshalTo(foo); err != nil {
                             ...
                           }

                      The pack methods provided by protobuf library will by
                      default use

                      'type.googleapis.com/full.type.name' as the type URL and
                      the unpack

                      methods only use the fully qualified type name after the
                      last '/'

                      in the type URL, for example "foo.bar.com/x/y.z" will
                      yield type

                      name "y.z".


                      JSON

                      ====

                      The JSON representation of an `Any` value uses the regular

                      representation of the deserialized, embedded message, with
                      an

                      additional field `@type` which contains the type URL.
                      Example:

                          package google.profile;
                          message Person {
                            string first_name = 1;
                            string last_name = 2;
                          }

                          {
                            "@type": "type.googleapis.com/google.profile.Person",
                            "firstName": <string>,
                            "lastName": <string>
                          }

                      If the embedded message type is well-known and has a
                      custom JSON

                      representation, that representation will be embedded
                      adding a field

                      `value` which holds the custom JSON in addition to the
                      `@type`

                      field. Example (for message [google.protobuf.Duration][]):

                          {
                            "@type": "type.googleapis.com/google.protobuf.Duration",
                            "value": "1.212s"
                          }
                  accountNumber:
                    type: string
                    format: uint64
                  sequence:
                    type: string
                    format: uint64
            description: QueryAccountInfoResponse is the Query/AccountInfo response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: address
          description: address is the account address string.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/accounts:
    get:
      summary: Accounts returns all the existing accounts.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: Accounts
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              accounts:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
                title: accounts are the existing accounts
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAccountsResponse is the response type for the Query/Accounts
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/accounts/{address}:
    get:
      summary: Account returns account details based on address.
      operationId: Account
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              account:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
            description: >-
              QueryAccountResponse is the response type for the Query/Account
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: address
          description: address defines the address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/address_by_id/{id}:
    get:
      summary: AccountAddressByID returns account address based on account number.
      operationId: AccountAddressByID
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              accountAddress:
                type: string
            title: >-
              QueryAccountAddressByIDResponse is the response type for
              AccountAddressByID rpc method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: id
          description: |-
            Deprecated, use account_id instead

            id is the account number of the address to be queried. This field
            should have been an uint64 (like all account numbers), and will be
            updated to uint64 in a future version of the auth query.
          in: path
          required: true
          type: string
          format: int64
        - name: accountId
          description: account_id is the account number of the address to be queried.
          in: query
          required: false
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/bech32:
    get:
      summary: Bech32Prefix queries bech32Prefix
      operationId: Bech32Prefix
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              bech32Prefix:
                type: string
            description: >-
              Bech32PrefixResponse is the response type for Bech32Prefix rpc
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/bech32/{addressBytes}:
    get:
      summary: AddressBytesToString converts Account Address bytes to string
      operationId: AddressBytesToString
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              addressString:
                type: string
            description: >-
              AddressBytesToStringResponse is the response type for
              AddressString rpc method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: addressBytes
          in: path
          required: true
          type: string
          format: byte
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/bech32/{addressString}:
    get:
      summary: AddressStringToBytes converts Address string to bytes
      operationId: AddressStringToBytes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              addressBytes:
                type: string
                format: byte
            description: >-
              AddressStringToBytesResponse is the response type for AddressBytes
              rpc method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: addressString
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/module_accounts:
    get:
      summary: ModuleAccounts returns all the existing module accounts.
      operationId: ModuleAccounts
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              accounts:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
            description: >-
              QueryModuleAccountsResponse is the response type for the
              Query/ModuleAccounts RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/module_accounts/{name}:
    get:
      summary: ModuleAccountByName returns the module account info by module name
      operationId: ModuleAccountByName
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              account:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
            description: >-
              QueryModuleAccountByNameResponse is the response type for the
              Query/ModuleAccountByName RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: name
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/auth/v1beta1/params:
    get:
      summary: Params queries all parameters.
      operationId: AuthParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  maxMemoCharacters:
                    type: string
                    format: uint64
                  txSigLimit:
                    type: string
                    format: uint64
                  txSizeCostPerByte:
                    type: string
                    format: uint64
                  sigVerifyCostEd25519:
                    type: string
                    format: uint64
                  sigVerifyCostSecp256k1:
                    type: string
                    format: uint64
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/authz/v1beta1/grants:
    get:
      summary: Returns list of `Authorization`, granted to the grantee by the granter.
      operationId: Grants
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              grants:
                type: array
                items:
                  type: object
                  properties:
                    authorization:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    expiration:
                      type: string
                      format: date-time
                      title: >-
                        time when the grant will expire and will be pruned. If
                        null, then the grant

                        doesn't have a time expiration (other conditions  in
                        `authorization`

                        may apply to invalidate the grant)
                  description: |-
                    Grant gives permissions to execute
                    the provide method with expiration time.
                description: >-
                  authorizations is a list of grants granted for grantee by
                  granter.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGrantsResponse is the response type for the
              Query/Authorizations RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: granter
          in: query
          required: false
          type: string
        - name: grantee
          in: query
          required: false
          type: string
        - name: msgTypeUrl
          description: >-
            Optional, msg_type_url, when set, will query only grants matching
            given msg type.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/authz/v1beta1/grants/grantee/{grantee}:
    get:
      summary: GranteeGrants returns a list of `GrantAuthorization` by grantee.
      operationId: GranteeGrants
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              grants:
                type: array
                items:
                  type: object
                  properties:
                    granter:
                      type: string
                    grantee:
                      type: string
                    authorization:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    expiration:
                      type: string
                      format: date-time
                  title: >-
                    GrantAuthorization extends a grant with both the addresses
                    of the grantee and granter.

                    It is used in genesis.proto and query.proto
                description: grants is a list of grants granted to the grantee.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGranteeGrantsResponse is the response type for the
              Query/GranteeGrants RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: grantee
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/authz/v1beta1/grants/granter/{granter}:
    get:
      summary: GranterGrants returns list of `GrantAuthorization`, granted by granter.
      operationId: GranterGrants
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              grants:
                type: array
                items:
                  type: object
                  properties:
                    granter:
                      type: string
                    grantee:
                      type: string
                    authorization:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    expiration:
                      type: string
                      format: date-time
                  title: >-
                    GrantAuthorization extends a grant with both the addresses
                    of the grantee and granter.

                    It is used in genesis.proto and query.proto
                description: grants is a list of grants granted by the granter.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGranterGrantsResponse is the response type for the
              Query/GranterGrants RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: granter
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/balances/{address}:
    get:
      summary: AllBalances queries the balance of all coins for a single account.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: AllBalances
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              balances:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: balances is the balances of all the coins.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAllBalancesResponse is the response type for the
              Query/AllBalances RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address is the address to query balances for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
        - name: resolveDenom
          description: >-
            resolve_denom is the flag to resolve the denom into a human-readable
            form from the metadata.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/balances/{address}/by_denom:
    get:
      summary: Balance queries the balance of a single coin for a single account.
      operationId: Balance
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              balance:
                description: balance is the balance of the coin.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
            description: >-
              QueryBalanceResponse is the response type for the Query/Balance
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address is the address to query balances for.
          in: path
          required: true
          type: string
        - name: denom
          description: denom is the coin denom to query balances for.
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/denom_owners/{denom}:
    get:
      summary: >-
        DenomOwners queries for all account addresses that own a particular
        token

        denomination.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: DenomOwners
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              denomOwners:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: >-
                        address defines the address that owns a particular
                        denomination.
                    balance:
                      description: >-
                        balance is the balance of the denominated coin for an
                        account.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                  description: >-
                    DenomOwner defines structure representing an account that
                    owns or holds a

                    particular denominated token. It contains the account
                    address and account

                    balance of the denominated token.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryDenomOwnersResponse defines the RPC response of a DenomOwners
              RPC query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: denom
          description: >-
            denom defines the coin denomination to query all account holders
            for.
          in: path
          required: true
          type: string
          pattern: .+
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/denom_owners_by_query:
    get:
      summary: >-
        DenomOwnersByQuery queries for all account addresses that own a
        particular token

        denomination.
      operationId: DenomOwnersByQuery
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              denomOwners:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: >-
                        address defines the address that owns a particular
                        denomination.
                    balance:
                      description: >-
                        balance is the balance of the denominated coin for an
                        account.
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                  description: >-
                    DenomOwner defines structure representing an account that
                    owns or holds a

                    particular denominated token. It contains the account
                    address and account

                    balance of the denominated token.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryDenomOwnersByQueryResponse defines the RPC response of a
              DenomOwnersByQuery RPC query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: denom
          description: >-
            denom defines the coin denomination to query all account holders
            for.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/denoms_metadata:
    get:
      summary: |-
        DenomsMetadata queries the client metadata for all registered coin
        denominations.
      operationId: DenomsMetadata
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              metadatas:
                type: array
                items:
                  type: object
                  properties:
                    description:
                      type: string
                    denomUnits:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                            description: >-
                              denom represents the string name of the given
                              denom unit (e.g uatom).
                          exponent:
                            type: integer
                            format: int64
                            description: >-
                              exponent represents power of 10 exponent that one
                              must

                              raise the base_denom to in order to equal the
                              given DenomUnit's denom

                              1 denom = 10^exponent base_denom

                              (e.g. with a base_denom of uatom, one can create a
                              DenomUnit of 'atom' with

                              exponent = 6, thus: 1 atom = 10^6 uatom).
                          aliases:
                            type: array
                            items:
                              type: string
                            title: >-
                              aliases is a list of string aliases for the given
                              denom
                        description: |-
                          DenomUnit represents a struct that describes a given
                          denomination unit of the basic token.
                      title: >-
                        denom_units represents the list of DenomUnit's for a
                        given coin
                    base:
                      type: string
                      description: >-
                        base represents the base denom (should be the DenomUnit
                        with exponent = 0).
                    display:
                      type: string
                      description: |-
                        display indicates the suggested denom that should be
                        displayed in clients.
                    name:
                      type: string
                      title: 'name defines the name of the token (eg: Cosmos Atom)'
                    symbol:
                      type: string
                      description: >-
                        symbol is the token symbol usually shown on exchanges
                        (eg: ATOM). This can

                        be the same as the display.
                    uri:
                      type: string
                      description: >-
                        URI to a document (on or off-chain) that contains
                        additional information. Optional.
                    uriHash:
                      type: string
                      description: >-
                        URIHash is a sha256 hash of a document pointed by URI.
                        It's used to verify that

                        the document didn't change. Optional.
                  description: |-
                    Metadata represents a struct that describes
                    a basic token.
                description: >-
                  metadata provides the client information for all the
                  registered tokens.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryDenomsMetadataResponse is the response type for the
              Query/DenomsMetadata RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/denoms_metadata/{denom}:
    get:
      summary: DenomMetadata queries the client metadata of a given coin denomination.
      operationId: DenomMetadata
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              metadata:
                description: >-
                  metadata describes and provides all the client information for
                  the requested token.
                type: object
                properties:
                  description:
                    type: string
                  denomUnits:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                          description: >-
                            denom represents the string name of the given denom
                            unit (e.g uatom).
                        exponent:
                          type: integer
                          format: int64
                          description: >-
                            exponent represents power of 10 exponent that one
                            must

                            raise the base_denom to in order to equal the given
                            DenomUnit's denom

                            1 denom = 10^exponent base_denom

                            (e.g. with a base_denom of uatom, one can create a
                            DenomUnit of 'atom' with

                            exponent = 6, thus: 1 atom = 10^6 uatom).
                        aliases:
                          type: array
                          items:
                            type: string
                          title: >-
                            aliases is a list of string aliases for the given
                            denom
                      description: |-
                        DenomUnit represents a struct that describes a given
                        denomination unit of the basic token.
                    title: >-
                      denom_units represents the list of DenomUnit's for a given
                      coin
                  base:
                    type: string
                    description: >-
                      base represents the base denom (should be the DenomUnit
                      with exponent = 0).
                  display:
                    type: string
                    description: |-
                      display indicates the suggested denom that should be
                      displayed in clients.
                  name:
                    type: string
                    title: 'name defines the name of the token (eg: Cosmos Atom)'
                  symbol:
                    type: string
                    description: >-
                      symbol is the token symbol usually shown on exchanges (eg:
                      ATOM). This can

                      be the same as the display.
                  uri:
                    type: string
                    description: >-
                      URI to a document (on or off-chain) that contains
                      additional information. Optional.
                  uriHash:
                    type: string
                    description: >-
                      URIHash is a sha256 hash of a document pointed by URI.
                      It's used to verify that

                      the document didn't change. Optional.
            description: >-
              QueryDenomMetadataResponse is the response type for the
              Query/DenomMetadata RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: denom
          description: denom is the coin denom to query the metadata for.
          in: path
          required: true
          type: string
          pattern: .+
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/denoms_metadata_by_query_string:
    get:
      summary: >-
        DenomMetadataByQueryString queries the client metadata of a given coin
        denomination.
      operationId: DenomMetadataByQueryString
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              metadata:
                description: >-
                  metadata describes and provides all the client information for
                  the requested token.
                type: object
                properties:
                  description:
                    type: string
                  denomUnits:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                          description: >-
                            denom represents the string name of the given denom
                            unit (e.g uatom).
                        exponent:
                          type: integer
                          format: int64
                          description: >-
                            exponent represents power of 10 exponent that one
                            must

                            raise the base_denom to in order to equal the given
                            DenomUnit's denom

                            1 denom = 10^exponent base_denom

                            (e.g. with a base_denom of uatom, one can create a
                            DenomUnit of 'atom' with

                            exponent = 6, thus: 1 atom = 10^6 uatom).
                        aliases:
                          type: array
                          items:
                            type: string
                          title: >-
                            aliases is a list of string aliases for the given
                            denom
                      description: |-
                        DenomUnit represents a struct that describes a given
                        denomination unit of the basic token.
                    title: >-
                      denom_units represents the list of DenomUnit's for a given
                      coin
                  base:
                    type: string
                    description: >-
                      base represents the base denom (should be the DenomUnit
                      with exponent = 0).
                  display:
                    type: string
                    description: |-
                      display indicates the suggested denom that should be
                      displayed in clients.
                  name:
                    type: string
                    title: 'name defines the name of the token (eg: Cosmos Atom)'
                  symbol:
                    type: string
                    description: >-
                      symbol is the token symbol usually shown on exchanges (eg:
                      ATOM). This can

                      be the same as the display.
                  uri:
                    type: string
                    description: >-
                      URI to a document (on or off-chain) that contains
                      additional information. Optional.
                  uriHash:
                    type: string
                    description: >-
                      URIHash is a sha256 hash of a document pointed by URI.
                      It's used to verify that

                      the document didn't change. Optional.
            description: >-
              QueryDenomMetadataByQueryStringResponse is the response type for
              the Query/DenomMetadata RPC

              method. Identical with QueryDenomMetadataResponse but receives
              denom as query string in request.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: denom
          description: denom is the coin denom to query the metadata for.
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/params:
    get:
      summary: Params queries the parameters of x/bank module.
      operationId: BankParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params provides the parameters of the bank module.
                type: object
                properties:
                  sendEnabled:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        enabled:
                          type: boolean
                      description: >-
                        SendEnabled maps coin denom to a send_enabled status
                        (whether a denom is

                        sendable).
                    description: >-
                      Deprecated: Use of SendEnabled in params is deprecated.

                      For genesis, use the newly added send_enabled field in the
                      genesis object.

                      Storage, lookup, and manipulation of this information is
                      now in the keeper.


                      As of cosmos-sdk 0.47, this only exists for backwards
                      compatibility of genesis files.
                  defaultSendEnabled:
                    type: boolean
            description: >-
              QueryParamsResponse defines the response type for querying x/bank
              parameters.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/send_enabled:
    get:
      summary: SendEnabled queries for SendEnabled entries.
      description: >-
        This query only returns denominations that have specific SendEnabled
        settings.

        Any denomination that does not have a specific setting will use the
        default

        params.default_send_enabled, and will not be returned by this query.
      operationId: SendEnabled
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              sendEnabled:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    enabled:
                      type: boolean
                  description: >-
                    SendEnabled maps coin denom to a send_enabled status
                    (whether a denom is

                    sendable).
              pagination:
                description: >-
                  pagination defines the pagination in the response. This field
                  is only

                  populated if the denoms field in the request is empty.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QuerySendEnabledResponse defines the RPC response of a SendEnable
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: denoms
          description: >-
            denoms is the specific denoms you want look up. Leave empty to get
            all entries.
          in: query
          required: false
          type: array
          items:
            type: string
          collectionFormat: multi
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/spendable_balances/{address}:
    get:
      summary: >-
        SpendableBalances queries the spendable balance of all coins for a
        single

        account.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: SpendableBalances
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              balances:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: balances is the spendable balances of all the coins.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QuerySpendableBalancesResponse defines the gRPC response structure
              for querying

              an account's spendable balances.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address is the address to query spendable balances for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/spendable_balances/{address}/by_denom:
    get:
      summary: >-
        SpendableBalanceByDenom queries the spendable balance of a single denom
        for

        a single account.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: SpendableBalanceByDenom
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              balance:
                description: balance is the balance of the coin.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
            description: >-
              QuerySpendableBalanceByDenomResponse defines the gRPC response
              structure for

              querying an account's spendable balance for a specific denom.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          description: address is the address to query balances for.
          in: path
          required: true
          type: string
        - name: denom
          description: denom is the coin denom to query balances for.
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/supply:
    get:
      summary: TotalSupply queries the total supply of all coins.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: TotalSupply
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              supply:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                title: supply is the supply of the coins
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: >-
              QueryTotalSupplyResponse is the response type for the
              Query/TotalSupply RPC

              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/bank/v1beta1/supply/by_denom:
    get:
      summary: SupplyOf queries the supply of a single coin.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: SupplyOf
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              amount:
                description: amount is the supply of the coin.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
            description: >-
              QuerySupplyOfResponse is the response type for the Query/SupplyOf
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: denom
          description: denom is the coin denom to query balances for.
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
  /cosmos/base/node/v1beta1/config:
    get:
      summary: Config queries for the operator configuration.
      operationId: Config
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              minimumGasPrice:
                type: string
              pruningKeepRecent:
                type: string
              pruningInterval:
                type: string
              haltHeight:
                type: string
                format: uint64
            description: >-
              ConfigResponse defines the response structure for the Config gRPC
              query.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/base/node/v1beta1/status:
    get:
      summary: Status queries for the node status.
      operationId: Status
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              earliestStoreHeight:
                type: string
                format: uint64
                title: earliest block height available in the store
              height:
                type: string
                format: uint64
                title: current block height
              timestamp:
                type: string
                format: date-time
                title: block height timestamp
              appHash:
                type: string
                format: byte
                title: app hash of the current block
              validatorHash:
                type: string
                format: byte
                title: validator hash provided by the consensus header
            description: >-
              StateResponse defines the response structure for the status of a
              node.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/abci_query:
    get:
      summary: >-
        ABCIQuery defines a query handler that supports ABCI queries directly to
        the

        application, bypassing Tendermint completely. The ABCI query must
        contain

        a valid and supported path, including app, custom, p2p, and store.
      operationId: ABCIQuery
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int64
              log:
                type: string
                title: nondeterministic
              info:
                type: string
                title: nondeterministic
              index:
                type: string
                format: int64
              key:
                type: string
                format: byte
              value:
                type: string
                format: byte
              proofOps:
                type: object
                properties:
                  ops:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        key:
                          type: string
                          format: byte
                        data:
                          type: string
                          format: byte
                      description: >-
                        ProofOp defines an operation used for calculating Merkle
                        root. The data could

                        be arbitrary format, providing necessary data for
                        example neighbouring node

                        hash.


                        Note: This type is a duplicate of the ProofOp proto type
                        defined in Tendermint.
                description: >-
                  ProofOps is Merkle proof defined by the list of ProofOps.


                  Note: This type is a duplicate of the ProofOps proto type
                  defined in Tendermint.
              height:
                type: string
                format: int64
              codespace:
                type: string
            description: >-
              ABCIQueryResponse defines the response structure for the ABCIQuery
              gRPC query.


              Note: This type is a duplicate of the ResponseQuery proto type
              defined in

              Tendermint.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: data
          in: query
          required: false
          type: string
          format: byte
        - name: path
          in: query
          required: false
          type: string
        - name: height
          in: query
          required: false
          type: string
          format: int64
        - name: prove
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/blocks/latest:
    get:
      summary: GetLatestBlock returns the latest block.
      operationId: GetLatestBlock
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              block:
                title: 'Deprecated: please use `sdk_block` instead'
                type: object
                properties:
                  header:
                    type: object
                    properties:
                      version:
                        title: basic block info
                        type: object
                        properties:
                          block:
                            type: string
                            format: uint64
                          app:
                            type: string
                            format: uint64
                        description: >-
                          Consensus captures the consensus rules for processing
                          a block in the blockchain,

                          including all blockchain data structures and the rules
                          of the application's

                          state transition machine.
                      chainId:
                        type: string
                      height:
                        type: string
                        format: int64
                      time:
                        type: string
                        format: date-time
                      lastBlockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      lastCommitHash:
                        type: string
                        format: byte
                        description: commit from validators from the last block
                        title: hashes of block data
                      dataHash:
                        type: string
                        format: byte
                        title: transactions
                      validatorsHash:
                        type: string
                        format: byte
                        description: validators for the current block
                        title: hashes from the app output from the prev block
                      nextValidatorsHash:
                        type: string
                        format: byte
                        title: validators for the next block
                      consensusHash:
                        type: string
                        format: byte
                        title: consensus params for current block
                      appHash:
                        type: string
                        format: byte
                        title: state after txs from the previous block
                      lastResultsHash:
                        type: string
                        format: byte
                        title: >-
                          root hash of all results from the txs from the
                          previous block
                      evidenceHash:
                        type: string
                        format: byte
                        description: evidence included in the block
                        title: consensus info
                      proposerAddress:
                        type: string
                        format: byte
                        title: original proposer of the block
                    description: Header defines the structure of a block header.
                  data:
                    type: object
                    properties:
                      txs:
                        type: array
                        items:
                          type: string
                          format: byte
                        description: >-
                          Txs that will be applied by state @ block.Height+1.

                          NOTE: not all txs here are valid.  We're just agreeing
                          on the order first.

                          This means that block.AppHash does not include these
                          txs.
                    title: >-
                      Data contains the set of transactions included in the
                      block
                  evidence:
                    type: object
                    properties:
                      evidence:
                        type: array
                        items:
                          type: object
                          properties:
                            duplicateVoteEvidence:
                              type: object
                              properties:
                                voteA:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                voteB:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                totalVotingPower:
                                  type: string
                                  format: int64
                                validatorPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                DuplicateVoteEvidence contains evidence of a
                                validator signed two conflicting votes.
                            lightClientAttackEvidence:
                              type: object
                              properties:
                                conflictingBlock:
                                  type: object
                                  properties:
                                    signedHeader:
                                      type: object
                                      properties:
                                        header:
                                          type: object
                                          properties:
                                            version:
                                              title: basic block info
                                              type: object
                                              properties:
                                                block:
                                                  type: string
                                                  format: uint64
                                                app:
                                                  type: string
                                                  format: uint64
                                              description: >-
                                                Consensus captures the consensus rules
                                                for processing a block in the
                                                blockchain,

                                                including all blockchain data structures
                                                and the rules of the application's

                                                state transition machine.
                                            chainId:
                                              type: string
                                            height:
                                              type: string
                                              format: int64
                                            time:
                                              type: string
                                              format: date-time
                                            lastBlockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            lastCommitHash:
                                              type: string
                                              format: byte
                                              description: >-
                                                commit from validators from the last
                                                block
                                              title: hashes of block data
                                            dataHash:
                                              type: string
                                              format: byte
                                              title: transactions
                                            validatorsHash:
                                              type: string
                                              format: byte
                                              description: validators for the current block
                                              title: >-
                                                hashes from the app output from the prev
                                                block
                                            nextValidatorsHash:
                                              type: string
                                              format: byte
                                              title: validators for the next block
                                            consensusHash:
                                              type: string
                                              format: byte
                                              title: consensus params for current block
                                            appHash:
                                              type: string
                                              format: byte
                                              title: state after txs from the previous block
                                            lastResultsHash:
                                              type: string
                                              format: byte
                                              title: >-
                                                root hash of all results from the txs
                                                from the previous block
                                            evidenceHash:
                                              type: string
                                              format: byte
                                              description: evidence included in the block
                                              title: consensus info
                                            proposerAddress:
                                              type: string
                                              format: byte
                                              title: original proposer of the block
                                          description: >-
                                            Header defines the structure of a block
                                            header.
                                        commit:
                                          type: object
                                          properties:
                                            height:
                                              type: string
                                              format: int64
                                            round:
                                              type: integer
                                              format: int32
                                            blockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            signatures:
                                              type: array
                                              items:
                                                type: object
                                                properties:
                                                  blockIdFlag:
                                                    type: string
                                                    enum:
                                                      - BLOCK_ID_FLAG_UNKNOWN
                                                      - BLOCK_ID_FLAG_ABSENT
                                                      - BLOCK_ID_FLAG_COMMIT
                                                      - BLOCK_ID_FLAG_NIL
                                                    default: BLOCK_ID_FLAG_UNKNOWN
                                                    description: >-
                                                      - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                                      error condition
                                                       - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                                       - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                                       - BLOCK_ID_FLAG_NIL: voted for nil
                                                    title: >-
                                                      BlockIdFlag indicates which BlockID the
                                                      signature is for
                                                  validatorAddress:
                                                    type: string
                                                    format: byte
                                                  timestamp:
                                                    type: string
                                                    format: date-time
                                                  signature:
                                                    type: string
                                                    format: byte
                                                description: >-
                                                  CommitSig is a part of the Vote included
                                                  in a Commit.
                                          description: >-
                                            Commit contains the evidence that a
                                            block was committed by a set of
                                            validators.
                                    validatorSet:
                                      type: object
                                      properties:
                                        validators:
                                          type: array
                                          items:
                                            type: object
                                            properties:
                                              address:
                                                type: string
                                                format: byte
                                              pubKey:
                                                type: object
                                                properties:
                                                  ed25519:
                                                    type: string
                                                    format: byte
                                                  secp256k1:
                                                    type: string
                                                    format: byte
                                                title: >-
                                                  PublicKey defines the keys available for
                                                  use with Validators
                                              votingPower:
                                                type: string
                                                format: int64
                                              proposerPriority:
                                                type: string
                                                format: int64
                                        proposer:
                                          type: object
                                          properties:
                                            address:
                                              type: string
                                              format: byte
                                            pubKey:
                                              type: object
                                              properties:
                                                ed25519:
                                                  type: string
                                                  format: byte
                                                secp256k1:
                                                  type: string
                                                  format: byte
                                              title: >-
                                                PublicKey defines the keys available for
                                                use with Validators
                                            votingPower:
                                              type: string
                                              format: int64
                                            proposerPriority:
                                              type: string
                                              format: int64
                                        totalVotingPower:
                                          type: string
                                          format: int64
                                commonHeight:
                                  type: string
                                  format: int64
                                byzantineValidators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                LightClientAttackEvidence contains evidence of a
                                set of validators attempting to mislead a light
                                client.
                  lastCommit:
                    type: object
                    properties:
                      height:
                        type: string
                        format: int64
                      round:
                        type: integer
                        format: int32
                      blockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      signatures:
                        type: array
                        items:
                          type: object
                          properties:
                            blockIdFlag:
                              type: string
                              enum:
                                - BLOCK_ID_FLAG_UNKNOWN
                                - BLOCK_ID_FLAG_ABSENT
                                - BLOCK_ID_FLAG_COMMIT
                                - BLOCK_ID_FLAG_NIL
                              default: BLOCK_ID_FLAG_UNKNOWN
                              description: >-
                                - BLOCK_ID_FLAG_UNKNOWN: indicates an error
                                condition
                                 - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                 - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                 - BLOCK_ID_FLAG_NIL: voted for nil
                              title: >-
                                BlockIdFlag indicates which BlockID the
                                signature is for
                            validatorAddress:
                              type: string
                              format: byte
                            timestamp:
                              type: string
                              format: date-time
                            signature:
                              type: string
                              format: byte
                          description: >-
                            CommitSig is a part of the Vote included in a
                            Commit.
                    description: >-
                      Commit contains the evidence that a block was committed by
                      a set of validators.
              sdkBlock:
                type: object
                properties:
                  header:
                    type: object
                    properties:
                      version:
                        title: basic block info
                        type: object
                        properties:
                          block:
                            type: string
                            format: uint64
                          app:
                            type: string
                            format: uint64
                        description: >-
                          Consensus captures the consensus rules for processing
                          a block in the blockchain,

                          including all blockchain data structures and the rules
                          of the application's

                          state transition machine.
                      chainId:
                        type: string
                      height:
                        type: string
                        format: int64
                      time:
                        type: string
                        format: date-time
                      lastBlockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      lastCommitHash:
                        type: string
                        format: byte
                        description: commit from validators from the last block
                        title: hashes of block data
                      dataHash:
                        type: string
                        format: byte
                        title: transactions
                      validatorsHash:
                        type: string
                        format: byte
                        description: validators for the current block
                        title: hashes from the app output from the prev block
                      nextValidatorsHash:
                        type: string
                        format: byte
                        title: validators for the next block
                      consensusHash:
                        type: string
                        format: byte
                        title: consensus params for current block
                      appHash:
                        type: string
                        format: byte
                        title: state after txs from the previous block
                      lastResultsHash:
                        type: string
                        format: byte
                        title: >-
                          root hash of all results from the txs from the
                          previous block
                      evidenceHash:
                        type: string
                        format: byte
                        description: evidence included in the block
                        title: consensus info
                      proposerAddress:
                        type: string
                        description: >-
                          proposer_address is the original block proposer
                          address, formatted as a Bech32 string.

                          In Tendermint, this type is `bytes`, but in the SDK,
                          we convert it to a Bech32 string

                          for better UX.


                          original proposer of the block
                    description: Header defines the structure of a Tendermint block header.
                  data:
                    type: object
                    properties:
                      txs:
                        type: array
                        items:
                          type: string
                          format: byte
                        description: >-
                          Txs that will be applied by state @ block.Height+1.

                          NOTE: not all txs here are valid.  We're just agreeing
                          on the order first.

                          This means that block.AppHash does not include these
                          txs.
                    title: >-
                      Data contains the set of transactions included in the
                      block
                  evidence:
                    type: object
                    properties:
                      evidence:
                        type: array
                        items:
                          type: object
                          properties:
                            duplicateVoteEvidence:
                              type: object
                              properties:
                                voteA:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                voteB:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                totalVotingPower:
                                  type: string
                                  format: int64
                                validatorPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                DuplicateVoteEvidence contains evidence of a
                                validator signed two conflicting votes.
                            lightClientAttackEvidence:
                              type: object
                              properties:
                                conflictingBlock:
                                  type: object
                                  properties:
                                    signedHeader:
                                      type: object
                                      properties:
                                        header:
                                          type: object
                                          properties:
                                            version:
                                              title: basic block info
                                              type: object
                                              properties:
                                                block:
                                                  type: string
                                                  format: uint64
                                                app:
                                                  type: string
                                                  format: uint64
                                              description: >-
                                                Consensus captures the consensus rules
                                                for processing a block in the
                                                blockchain,

                                                including all blockchain data structures
                                                and the rules of the application's

                                                state transition machine.
                                            chainId:
                                              type: string
                                            height:
                                              type: string
                                              format: int64
                                            time:
                                              type: string
                                              format: date-time
                                            lastBlockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            lastCommitHash:
                                              type: string
                                              format: byte
                                              description: >-
                                                commit from validators from the last
                                                block
                                              title: hashes of block data
                                            dataHash:
                                              type: string
                                              format: byte
                                              title: transactions
                                            validatorsHash:
                                              type: string
                                              format: byte
                                              description: validators for the current block
                                              title: >-
                                                hashes from the app output from the prev
                                                block
                                            nextValidatorsHash:
                                              type: string
                                              format: byte
                                              title: validators for the next block
                                            consensusHash:
                                              type: string
                                              format: byte
                                              title: consensus params for current block
                                            appHash:
                                              type: string
                                              format: byte
                                              title: state after txs from the previous block
                                            lastResultsHash:
                                              type: string
                                              format: byte
                                              title: >-
                                                root hash of all results from the txs
                                                from the previous block
                                            evidenceHash:
                                              type: string
                                              format: byte
                                              description: evidence included in the block
                                              title: consensus info
                                            proposerAddress:
                                              type: string
                                              format: byte
                                              title: original proposer of the block
                                          description: >-
                                            Header defines the structure of a block
                                            header.
                                        commit:
                                          type: object
                                          properties:
                                            height:
                                              type: string
                                              format: int64
                                            round:
                                              type: integer
                                              format: int32
                                            blockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            signatures:
                                              type: array
                                              items:
                                                type: object
                                                properties:
                                                  blockIdFlag:
                                                    type: string
                                                    enum:
                                                      - BLOCK_ID_FLAG_UNKNOWN
                                                      - BLOCK_ID_FLAG_ABSENT
                                                      - BLOCK_ID_FLAG_COMMIT
                                                      - BLOCK_ID_FLAG_NIL
                                                    default: BLOCK_ID_FLAG_UNKNOWN
                                                    description: >-
                                                      - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                                      error condition
                                                       - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                                       - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                                       - BLOCK_ID_FLAG_NIL: voted for nil
                                                    title: >-
                                                      BlockIdFlag indicates which BlockID the
                                                      signature is for
                                                  validatorAddress:
                                                    type: string
                                                    format: byte
                                                  timestamp:
                                                    type: string
                                                    format: date-time
                                                  signature:
                                                    type: string
                                                    format: byte
                                                description: >-
                                                  CommitSig is a part of the Vote included
                                                  in a Commit.
                                          description: >-
                                            Commit contains the evidence that a
                                            block was committed by a set of
                                            validators.
                                    validatorSet:
                                      type: object
                                      properties:
                                        validators:
                                          type: array
                                          items:
                                            type: object
                                            properties:
                                              address:
                                                type: string
                                                format: byte
                                              pubKey:
                                                type: object
                                                properties:
                                                  ed25519:
                                                    type: string
                                                    format: byte
                                                  secp256k1:
                                                    type: string
                                                    format: byte
                                                title: >-
                                                  PublicKey defines the keys available for
                                                  use with Validators
                                              votingPower:
                                                type: string
                                                format: int64
                                              proposerPriority:
                                                type: string
                                                format: int64
                                        proposer:
                                          type: object
                                          properties:
                                            address:
                                              type: string
                                              format: byte
                                            pubKey:
                                              type: object
                                              properties:
                                                ed25519:
                                                  type: string
                                                  format: byte
                                                secp256k1:
                                                  type: string
                                                  format: byte
                                              title: >-
                                                PublicKey defines the keys available for
                                                use with Validators
                                            votingPower:
                                              type: string
                                              format: int64
                                            proposerPriority:
                                              type: string
                                              format: int64
                                        totalVotingPower:
                                          type: string
                                          format: int64
                                commonHeight:
                                  type: string
                                  format: int64
                                byzantineValidators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                LightClientAttackEvidence contains evidence of a
                                set of validators attempting to mislead a light
                                client.
                  lastCommit:
                    type: object
                    properties:
                      height:
                        type: string
                        format: int64
                      round:
                        type: integer
                        format: int32
                      blockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      signatures:
                        type: array
                        items:
                          type: object
                          properties:
                            blockIdFlag:
                              type: string
                              enum:
                                - BLOCK_ID_FLAG_UNKNOWN
                                - BLOCK_ID_FLAG_ABSENT
                                - BLOCK_ID_FLAG_COMMIT
                                - BLOCK_ID_FLAG_NIL
                              default: BLOCK_ID_FLAG_UNKNOWN
                              description: >-
                                - BLOCK_ID_FLAG_UNKNOWN: indicates an error
                                condition
                                 - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                 - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                 - BLOCK_ID_FLAG_NIL: voted for nil
                              title: >-
                                BlockIdFlag indicates which BlockID the
                                signature is for
                            validatorAddress:
                              type: string
                              format: byte
                            timestamp:
                              type: string
                              format: date-time
                            signature:
                              type: string
                              format: byte
                          description: >-
                            CommitSig is a part of the Vote included in a
                            Commit.
                    description: >-
                      Commit contains the evidence that a block was committed by
                      a set of validators.
                description: >-
                  Block is tendermint type Block, with the Header proposer
                  address

                  field converted to bech32 string.
            description: >-
              GetLatestBlockResponse is the response type for the
              Query/GetLatestBlock RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/blocks/{height}:
    get:
      summary: GetBlockByHeight queries block for given height.
      operationId: GetBlockByHeight
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              block:
                title: 'Deprecated: please use `sdk_block` instead'
                type: object
                properties:
                  header:
                    type: object
                    properties:
                      version:
                        title: basic block info
                        type: object
                        properties:
                          block:
                            type: string
                            format: uint64
                          app:
                            type: string
                            format: uint64
                        description: >-
                          Consensus captures the consensus rules for processing
                          a block in the blockchain,

                          including all blockchain data structures and the rules
                          of the application's

                          state transition machine.
                      chainId:
                        type: string
                      height:
                        type: string
                        format: int64
                      time:
                        type: string
                        format: date-time
                      lastBlockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      lastCommitHash:
                        type: string
                        format: byte
                        description: commit from validators from the last block
                        title: hashes of block data
                      dataHash:
                        type: string
                        format: byte
                        title: transactions
                      validatorsHash:
                        type: string
                        format: byte
                        description: validators for the current block
                        title: hashes from the app output from the prev block
                      nextValidatorsHash:
                        type: string
                        format: byte
                        title: validators for the next block
                      consensusHash:
                        type: string
                        format: byte
                        title: consensus params for current block
                      appHash:
                        type: string
                        format: byte
                        title: state after txs from the previous block
                      lastResultsHash:
                        type: string
                        format: byte
                        title: >-
                          root hash of all results from the txs from the
                          previous block
                      evidenceHash:
                        type: string
                        format: byte
                        description: evidence included in the block
                        title: consensus info
                      proposerAddress:
                        type: string
                        format: byte
                        title: original proposer of the block
                    description: Header defines the structure of a block header.
                  data:
                    type: object
                    properties:
                      txs:
                        type: array
                        items:
                          type: string
                          format: byte
                        description: >-
                          Txs that will be applied by state @ block.Height+1.

                          NOTE: not all txs here are valid.  We're just agreeing
                          on the order first.

                          This means that block.AppHash does not include these
                          txs.
                    title: >-
                      Data contains the set of transactions included in the
                      block
                  evidence:
                    type: object
                    properties:
                      evidence:
                        type: array
                        items:
                          type: object
                          properties:
                            duplicateVoteEvidence:
                              type: object
                              properties:
                                voteA:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                voteB:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                totalVotingPower:
                                  type: string
                                  format: int64
                                validatorPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                DuplicateVoteEvidence contains evidence of a
                                validator signed two conflicting votes.
                            lightClientAttackEvidence:
                              type: object
                              properties:
                                conflictingBlock:
                                  type: object
                                  properties:
                                    signedHeader:
                                      type: object
                                      properties:
                                        header:
                                          type: object
                                          properties:
                                            version:
                                              title: basic block info
                                              type: object
                                              properties:
                                                block:
                                                  type: string
                                                  format: uint64
                                                app:
                                                  type: string
                                                  format: uint64
                                              description: >-
                                                Consensus captures the consensus rules
                                                for processing a block in the
                                                blockchain,

                                                including all blockchain data structures
                                                and the rules of the application's

                                                state transition machine.
                                            chainId:
                                              type: string
                                            height:
                                              type: string
                                              format: int64
                                            time:
                                              type: string
                                              format: date-time
                                            lastBlockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            lastCommitHash:
                                              type: string
                                              format: byte
                                              description: >-
                                                commit from validators from the last
                                                block
                                              title: hashes of block data
                                            dataHash:
                                              type: string
                                              format: byte
                                              title: transactions
                                            validatorsHash:
                                              type: string
                                              format: byte
                                              description: validators for the current block
                                              title: >-
                                                hashes from the app output from the prev
                                                block
                                            nextValidatorsHash:
                                              type: string
                                              format: byte
                                              title: validators for the next block
                                            consensusHash:
                                              type: string
                                              format: byte
                                              title: consensus params for current block
                                            appHash:
                                              type: string
                                              format: byte
                                              title: state after txs from the previous block
                                            lastResultsHash:
                                              type: string
                                              format: byte
                                              title: >-
                                                root hash of all results from the txs
                                                from the previous block
                                            evidenceHash:
                                              type: string
                                              format: byte
                                              description: evidence included in the block
                                              title: consensus info
                                            proposerAddress:
                                              type: string
                                              format: byte
                                              title: original proposer of the block
                                          description: >-
                                            Header defines the structure of a block
                                            header.
                                        commit:
                                          type: object
                                          properties:
                                            height:
                                              type: string
                                              format: int64
                                            round:
                                              type: integer
                                              format: int32
                                            blockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            signatures:
                                              type: array
                                              items:
                                                type: object
                                                properties:
                                                  blockIdFlag:
                                                    type: string
                                                    enum:
                                                      - BLOCK_ID_FLAG_UNKNOWN
                                                      - BLOCK_ID_FLAG_ABSENT
                                                      - BLOCK_ID_FLAG_COMMIT
                                                      - BLOCK_ID_FLAG_NIL
                                                    default: BLOCK_ID_FLAG_UNKNOWN
                                                    description: >-
                                                      - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                                      error condition
                                                       - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                                       - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                                       - BLOCK_ID_FLAG_NIL: voted for nil
                                                    title: >-
                                                      BlockIdFlag indicates which BlockID the
                                                      signature is for
                                                  validatorAddress:
                                                    type: string
                                                    format: byte
                                                  timestamp:
                                                    type: string
                                                    format: date-time
                                                  signature:
                                                    type: string
                                                    format: byte
                                                description: >-
                                                  CommitSig is a part of the Vote included
                                                  in a Commit.
                                          description: >-
                                            Commit contains the evidence that a
                                            block was committed by a set of
                                            validators.
                                    validatorSet:
                                      type: object
                                      properties:
                                        validators:
                                          type: array
                                          items:
                                            type: object
                                            properties:
                                              address:
                                                type: string
                                                format: byte
                                              pubKey:
                                                type: object
                                                properties:
                                                  ed25519:
                                                    type: string
                                                    format: byte
                                                  secp256k1:
                                                    type: string
                                                    format: byte
                                                title: >-
                                                  PublicKey defines the keys available for
                                                  use with Validators
                                              votingPower:
                                                type: string
                                                format: int64
                                              proposerPriority:
                                                type: string
                                                format: int64
                                        proposer:
                                          type: object
                                          properties:
                                            address:
                                              type: string
                                              format: byte
                                            pubKey:
                                              type: object
                                              properties:
                                                ed25519:
                                                  type: string
                                                  format: byte
                                                secp256k1:
                                                  type: string
                                                  format: byte
                                              title: >-
                                                PublicKey defines the keys available for
                                                use with Validators
                                            votingPower:
                                              type: string
                                              format: int64
                                            proposerPriority:
                                              type: string
                                              format: int64
                                        totalVotingPower:
                                          type: string
                                          format: int64
                                commonHeight:
                                  type: string
                                  format: int64
                                byzantineValidators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                LightClientAttackEvidence contains evidence of a
                                set of validators attempting to mislead a light
                                client.
                  lastCommit:
                    type: object
                    properties:
                      height:
                        type: string
                        format: int64
                      round:
                        type: integer
                        format: int32
                      blockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      signatures:
                        type: array
                        items:
                          type: object
                          properties:
                            blockIdFlag:
                              type: string
                              enum:
                                - BLOCK_ID_FLAG_UNKNOWN
                                - BLOCK_ID_FLAG_ABSENT
                                - BLOCK_ID_FLAG_COMMIT
                                - BLOCK_ID_FLAG_NIL
                              default: BLOCK_ID_FLAG_UNKNOWN
                              description: >-
                                - BLOCK_ID_FLAG_UNKNOWN: indicates an error
                                condition
                                 - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                 - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                 - BLOCK_ID_FLAG_NIL: voted for nil
                              title: >-
                                BlockIdFlag indicates which BlockID the
                                signature is for
                            validatorAddress:
                              type: string
                              format: byte
                            timestamp:
                              type: string
                              format: date-time
                            signature:
                              type: string
                              format: byte
                          description: >-
                            CommitSig is a part of the Vote included in a
                            Commit.
                    description: >-
                      Commit contains the evidence that a block was committed by
                      a set of validators.
              sdkBlock:
                type: object
                properties:
                  header:
                    type: object
                    properties:
                      version:
                        title: basic block info
                        type: object
                        properties:
                          block:
                            type: string
                            format: uint64
                          app:
                            type: string
                            format: uint64
                        description: >-
                          Consensus captures the consensus rules for processing
                          a block in the blockchain,

                          including all blockchain data structures and the rules
                          of the application's

                          state transition machine.
                      chainId:
                        type: string
                      height:
                        type: string
                        format: int64
                      time:
                        type: string
                        format: date-time
                      lastBlockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      lastCommitHash:
                        type: string
                        format: byte
                        description: commit from validators from the last block
                        title: hashes of block data
                      dataHash:
                        type: string
                        format: byte
                        title: transactions
                      validatorsHash:
                        type: string
                        format: byte
                        description: validators for the current block
                        title: hashes from the app output from the prev block
                      nextValidatorsHash:
                        type: string
                        format: byte
                        title: validators for the next block
                      consensusHash:
                        type: string
                        format: byte
                        title: consensus params for current block
                      appHash:
                        type: string
                        format: byte
                        title: state after txs from the previous block
                      lastResultsHash:
                        type: string
                        format: byte
                        title: >-
                          root hash of all results from the txs from the
                          previous block
                      evidenceHash:
                        type: string
                        format: byte
                        description: evidence included in the block
                        title: consensus info
                      proposerAddress:
                        type: string
                        description: >-
                          proposer_address is the original block proposer
                          address, formatted as a Bech32 string.

                          In Tendermint, this type is `bytes`, but in the SDK,
                          we convert it to a Bech32 string

                          for better UX.


                          original proposer of the block
                    description: Header defines the structure of a Tendermint block header.
                  data:
                    type: object
                    properties:
                      txs:
                        type: array
                        items:
                          type: string
                          format: byte
                        description: >-
                          Txs that will be applied by state @ block.Height+1.

                          NOTE: not all txs here are valid.  We're just agreeing
                          on the order first.

                          This means that block.AppHash does not include these
                          txs.
                    title: >-
                      Data contains the set of transactions included in the
                      block
                  evidence:
                    type: object
                    properties:
                      evidence:
                        type: array
                        items:
                          type: object
                          properties:
                            duplicateVoteEvidence:
                              type: object
                              properties:
                                voteA:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                voteB:
                                  type: object
                                  properties:
                                    type:
                                      type: string
                                      enum:
                                        - SIGNED_MSG_TYPE_UNKNOWN
                                        - SIGNED_MSG_TYPE_PREVOTE
                                        - SIGNED_MSG_TYPE_PRECOMMIT
                                        - SIGNED_MSG_TYPE_PROPOSAL
                                      default: SIGNED_MSG_TYPE_UNKNOWN
                                      description: >-
                                        SignedMsgType is a type of signed
                                        message in the consensus.

                                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                      description: zero if vote is nil.
                                    timestamp:
                                      type: string
                                      format: date-time
                                    validatorAddress:
                                      type: string
                                      format: byte
                                    validatorIndex:
                                      type: integer
                                      format: int32
                                    signature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote signature by the validator if they
                                        participated in consensus for the

                                        associated block.
                                    extension:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension provided by the
                                        application. Only valid for precommit

                                        messages.
                                    extensionSignature:
                                      type: string
                                      format: byte
                                      description: >-
                                        Vote extension signature by the
                                        validator if they participated in

                                        consensus for the associated block.

                                        Only valid for precommit messages.
                                  description: >-
                                    Vote represents a prevote or precommit vote
                                    from validators for

                                    consensus.
                                totalVotingPower:
                                  type: string
                                  format: int64
                                validatorPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                DuplicateVoteEvidence contains evidence of a
                                validator signed two conflicting votes.
                            lightClientAttackEvidence:
                              type: object
                              properties:
                                conflictingBlock:
                                  type: object
                                  properties:
                                    signedHeader:
                                      type: object
                                      properties:
                                        header:
                                          type: object
                                          properties:
                                            version:
                                              title: basic block info
                                              type: object
                                              properties:
                                                block:
                                                  type: string
                                                  format: uint64
                                                app:
                                                  type: string
                                                  format: uint64
                                              description: >-
                                                Consensus captures the consensus rules
                                                for processing a block in the
                                                blockchain,

                                                including all blockchain data structures
                                                and the rules of the application's

                                                state transition machine.
                                            chainId:
                                              type: string
                                            height:
                                              type: string
                                              format: int64
                                            time:
                                              type: string
                                              format: date-time
                                            lastBlockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            lastCommitHash:
                                              type: string
                                              format: byte
                                              description: >-
                                                commit from validators from the last
                                                block
                                              title: hashes of block data
                                            dataHash:
                                              type: string
                                              format: byte
                                              title: transactions
                                            validatorsHash:
                                              type: string
                                              format: byte
                                              description: validators for the current block
                                              title: >-
                                                hashes from the app output from the prev
                                                block
                                            nextValidatorsHash:
                                              type: string
                                              format: byte
                                              title: validators for the next block
                                            consensusHash:
                                              type: string
                                              format: byte
                                              title: consensus params for current block
                                            appHash:
                                              type: string
                                              format: byte
                                              title: state after txs from the previous block
                                            lastResultsHash:
                                              type: string
                                              format: byte
                                              title: >-
                                                root hash of all results from the txs
                                                from the previous block
                                            evidenceHash:
                                              type: string
                                              format: byte
                                              description: evidence included in the block
                                              title: consensus info
                                            proposerAddress:
                                              type: string
                                              format: byte
                                              title: original proposer of the block
                                          description: >-
                                            Header defines the structure of a block
                                            header.
                                        commit:
                                          type: object
                                          properties:
                                            height:
                                              type: string
                                              format: int64
                                            round:
                                              type: integer
                                              format: int32
                                            blockId:
                                              type: object
                                              properties:
                                                hash:
                                                  type: string
                                                  format: byte
                                                partSetHeader:
                                                  type: object
                                                  properties:
                                                    total:
                                                      type: integer
                                                      format: int64
                                                    hash:
                                                      type: string
                                                      format: byte
                                                  title: PartsetHeader
                                              title: BlockID
                                            signatures:
                                              type: array
                                              items:
                                                type: object
                                                properties:
                                                  blockIdFlag:
                                                    type: string
                                                    enum:
                                                      - BLOCK_ID_FLAG_UNKNOWN
                                                      - BLOCK_ID_FLAG_ABSENT
                                                      - BLOCK_ID_FLAG_COMMIT
                                                      - BLOCK_ID_FLAG_NIL
                                                    default: BLOCK_ID_FLAG_UNKNOWN
                                                    description: >-
                                                      - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                                      error condition
                                                       - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                                       - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                                       - BLOCK_ID_FLAG_NIL: voted for nil
                                                    title: >-
                                                      BlockIdFlag indicates which BlockID the
                                                      signature is for
                                                  validatorAddress:
                                                    type: string
                                                    format: byte
                                                  timestamp:
                                                    type: string
                                                    format: date-time
                                                  signature:
                                                    type: string
                                                    format: byte
                                                description: >-
                                                  CommitSig is a part of the Vote included
                                                  in a Commit.
                                          description: >-
                                            Commit contains the evidence that a
                                            block was committed by a set of
                                            validators.
                                    validatorSet:
                                      type: object
                                      properties:
                                        validators:
                                          type: array
                                          items:
                                            type: object
                                            properties:
                                              address:
                                                type: string
                                                format: byte
                                              pubKey:
                                                type: object
                                                properties:
                                                  ed25519:
                                                    type: string
                                                    format: byte
                                                  secp256k1:
                                                    type: string
                                                    format: byte
                                                title: >-
                                                  PublicKey defines the keys available for
                                                  use with Validators
                                              votingPower:
                                                type: string
                                                format: int64
                                              proposerPriority:
                                                type: string
                                                format: int64
                                        proposer:
                                          type: object
                                          properties:
                                            address:
                                              type: string
                                              format: byte
                                            pubKey:
                                              type: object
                                              properties:
                                                ed25519:
                                                  type: string
                                                  format: byte
                                                secp256k1:
                                                  type: string
                                                  format: byte
                                              title: >-
                                                PublicKey defines the keys available for
                                                use with Validators
                                            votingPower:
                                              type: string
                                              format: int64
                                            proposerPriority:
                                              type: string
                                              format: int64
                                        totalVotingPower:
                                          type: string
                                          format: int64
                                commonHeight:
                                  type: string
                                  format: int64
                                byzantineValidators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                                timestamp:
                                  type: string
                                  format: date-time
                              description: >-
                                LightClientAttackEvidence contains evidence of a
                                set of validators attempting to mislead a light
                                client.
                  lastCommit:
                    type: object
                    properties:
                      height:
                        type: string
                        format: int64
                      round:
                        type: integer
                        format: int32
                      blockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      signatures:
                        type: array
                        items:
                          type: object
                          properties:
                            blockIdFlag:
                              type: string
                              enum:
                                - BLOCK_ID_FLAG_UNKNOWN
                                - BLOCK_ID_FLAG_ABSENT
                                - BLOCK_ID_FLAG_COMMIT
                                - BLOCK_ID_FLAG_NIL
                              default: BLOCK_ID_FLAG_UNKNOWN
                              description: >-
                                - BLOCK_ID_FLAG_UNKNOWN: indicates an error
                                condition
                                 - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                 - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                 - BLOCK_ID_FLAG_NIL: voted for nil
                              title: >-
                                BlockIdFlag indicates which BlockID the
                                signature is for
                            validatorAddress:
                              type: string
                              format: byte
                            timestamp:
                              type: string
                              format: date-time
                            signature:
                              type: string
                              format: byte
                          description: >-
                            CommitSig is a part of the Vote included in a
                            Commit.
                    description: >-
                      Commit contains the evidence that a block was committed by
                      a set of validators.
                description: >-
                  Block is tendermint type Block, with the Header proposer
                  address

                  field converted to bech32 string.
            description: >-
              GetBlockByHeightResponse is the response type for the
              Query/GetBlockByHeight RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: height
          in: path
          required: true
          type: string
          format: int64
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/node_info:
    get:
      summary: GetNodeInfo queries the current node info.
      operationId: GetNodeInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              defaultNodeInfo:
                type: object
                properties:
                  protocolVersion:
                    type: object
                    properties:
                      p2p:
                        type: string
                        format: uint64
                      block:
                        type: string
                        format: uint64
                      app:
                        type: string
                        format: uint64
                  defaultNodeId:
                    type: string
                  listenAddr:
                    type: string
                  network:
                    type: string
                  version:
                    type: string
                  channels:
                    type: string
                    format: byte
                  moniker:
                    type: string
                  other:
                    type: object
                    properties:
                      txIndex:
                        type: string
                      rpcAddress:
                        type: string
              applicationVersion:
                type: object
                properties:
                  name:
                    type: string
                  appName:
                    type: string
                  version:
                    type: string
                  gitCommit:
                    type: string
                  buildTags:
                    type: string
                  goVersion:
                    type: string
                  buildDeps:
                    type: array
                    items:
                      type: object
                      properties:
                        path:
                          type: string
                          title: module path
                        version:
                          type: string
                          title: module version
                        sum:
                          type: string
                          title: checksum
                      title: Module is the type for VersionInfo
                  cosmosSdkVersion:
                    type: string
                description: VersionInfo is the type for the GetNodeInfoResponse message.
            description: >-
              GetNodeInfoResponse is the response type for the Query/GetNodeInfo
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/syncing:
    get:
      summary: GetSyncing queries node syncing.
      operationId: GetSyncing
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              syncing:
                type: boolean
            description: >-
              GetSyncingResponse is the response type for the Query/GetSyncing
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/validatorsets/latest:
    get:
      summary: GetLatestValidatorSet queries latest validator-set.
      operationId: GetLatestValidatorSet
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              blockHeight:
                type: string
                format: int64
              validators:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                    pubKey:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    votingPower:
                      type: string
                      format: int64
                    proposerPriority:
                      type: string
                      format: int64
                  description: Validator is the type for the validator-set.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              GetLatestValidatorSetResponse is the response type for the
              Query/GetValidatorSetByHeight RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/base/tendermint/v1beta1/validatorsets/{height}:
    get:
      summary: GetValidatorSetByHeight queries validator-set at a given height.
      operationId: GetValidatorSetByHeight
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              blockHeight:
                type: string
                format: int64
              validators:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                    pubKey:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    votingPower:
                      type: string
                      format: int64
                    proposerPriority:
                      type: string
                      format: int64
                  description: Validator is the type for the validator-set.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              GetValidatorSetByHeightResponse is the response type for the
              Query/GetValidatorSetByHeight RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: height
          in: path
          required: true
          type: string
          format: int64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/circuit/v1/accounts:
    get:
      summary: Account returns account permissions.
      operationId: CircuitAccounts
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              accounts:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                    permissions:
                      type: object
                      properties:
                        level:
                          description: >-
                            level is the level of permissions granted to this
                            account.
                          type: string
                          enum:
                            - LEVEL_NONE_UNSPECIFIED
                            - LEVEL_SOME_MSGS
                            - LEVEL_ALL_MSGS
                            - LEVEL_SUPER_ADMIN
                          default: LEVEL_NONE_UNSPECIFIED
                        limitTypeUrls:
                          type: array
                          items:
                            type: string
                          description: >-
                            limit_type_urls is used with LEVEL_SOME_MSGS to
                            limit the lists of Msg type

                            URLs that the account can trip. It is an error to
                            use limit_type_urls with

                            a level other than LEVEL_SOME_MSGS.
                      description: >-
                        Permissions are the permissions that an account has to
                        trip

                        or reset the circuit breaker.
                  title: >-
                    GenesisAccountPermissions is the account permissions for the
                    circuit breaker in genesis
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              AccountsResponse is the response type for the Query/Accounts RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/circuit/v1/accounts/{address}:
    get:
      summary: Account returns account permissions.
      operationId: CircuitAccount
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              permission:
                type: object
                properties:
                  level:
                    description: level is the level of permissions granted to this account.
                    type: string
                    enum:
                      - LEVEL_NONE_UNSPECIFIED
                      - LEVEL_SOME_MSGS
                      - LEVEL_ALL_MSGS
                      - LEVEL_SUPER_ADMIN
                    default: LEVEL_NONE_UNSPECIFIED
                  limitTypeUrls:
                    type: array
                    items:
                      type: string
                    description: >-
                      limit_type_urls is used with LEVEL_SOME_MSGS to limit the
                      lists of Msg type

                      URLs that the account can trip. It is an error to use
                      limit_type_urls with

                      a level other than LEVEL_SOME_MSGS.
                description: |-
                  Permissions are the permissions that an account has to trip
                  or reset the circuit breaker.
            description: >-
              AccountResponse is the response type for the Query/Account RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/circuit/v1/disable_list:
    get:
      summary: DisabledList returns a list of disabled message urls
      operationId: DisabledList
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              disabledList:
                type: array
                items:
                  type: string
            description: >-
              DisabledListResponse is the response type for the
              Query/DisabledList RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/consensus/v1/params:
    get:
      summary: Params queries the parameters of x/consensus module.
      operationId: ConsensusParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: >-
                  params are the tendermint consensus params stored in the
                  consensus module.

                  Please note that `params.version` is not populated in this
                  response, it is

                  tracked separately in the x/upgrade module.
                type: object
                properties:
                  block:
                    type: object
                    properties:
                      maxBytes:
                        type: string
                        format: int64
                        title: |-
                          Max block size, in bytes.
                          Note: must be greater than 0
                      maxGas:
                        type: string
                        format: int64
                        title: |-
                          Max gas per block.
                          Note: must be greater or equal to -1
                    description: BlockParams contains limits on the block size.
                  evidence:
                    type: object
                    properties:
                      maxAgeNumBlocks:
                        type: string
                        format: int64
                        description: >-
                          Max age of evidence, in blocks.


                          The basic formula for calculating this is:
                          MaxAgeDuration / {average block

                          time}.
                      maxAgeDuration:
                        type: string
                        description: >-
                          Max age of evidence, in time.


                          It should correspond with an app's "unbonding period"
                          or other similar

                          mechanism for handling [Nothing-At-Stake

                          attacks](https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed).
                      maxBytes:
                        type: string
                        format: int64
                        title: >-
                          This sets the maximum size of total evidence in bytes
                          that can be committed in a single block.

                          and should fall comfortably under the max block bytes.

                          Default is 1048576 or 1MB
                    description: >-
                      EvidenceParams determine how we handle evidence of
                      malfeasance.
                  validator:
                    type: object
                    properties:
                      pubKeyTypes:
                        type: array
                        items:
                          type: string
                    description: >-
                      ValidatorParams restrict the public key types validators
                      can use.

                      NOTE: uses ABCI pubkey naming, not Amino names.
                  version:
                    type: object
                    properties:
                      app:
                        type: string
                        format: uint64
                    description: VersionParams contains the ABCI application version.
                  abci:
                    type: object
                    properties:
                      voteExtensionsEnableHeight:
                        type: string
                        format: int64
                        description: >-
                          vote_extensions_enable_height configures the first
                          height during which

                          vote extensions will be enabled. During this specified
                          height, and for all

                          subsequent heights, precommit messages that do not
                          contain valid extension data

                          will be considered invalid. Prior to this height, vote
                          extensions will not

                          be used or accepted by validators on the network.


                          Once enabled, vote extensions will be created by the
                          application in ExtendVote,

                          passed to the application for validation in
                          VerifyVoteExtension and given

                          to the application to use when proposing a block
                          during PrepareProposal.
                    description: >-
                      ABCIParams configure functionality specific to the
                      Application Blockchain Interface.
            description: >-
              QueryParamsResponse defines the response type for querying
              x/consensus parameters.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/community_pool:
    get:
      summary: CommunityPool queries the community pool coins.
      description: 'WARNING: This query will fail if an external community pool is used.'
      operationId: CommunityPool
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              pool:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    DecCoin defines a token with a denomination and a decimal
                    amount.


                    NOTE: The amount field is an Dec which implements the custom
                    method

                    signatures required by gogoproto.
                description: pool defines community pool's coins.
            description: >-
              QueryCommunityPoolResponse is the response type for the
              Query/CommunityPool

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/delegators/{delegatorAddress}/rewards:
    get:
      summary: |-
        DelegationTotalRewards queries the total rewards accrued by each
        validator.
      operationId: DelegationTotalRewards
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              rewards:
                type: array
                items:
                  type: object
                  properties:
                    validatorAddress:
                      type: string
                    reward:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          DecCoin defines a token with a denomination and a
                          decimal amount.


                          NOTE: The amount field is an Dec which implements the
                          custom method

                          signatures required by gogoproto.
                  description: |-
                    DelegationDelegatorReward represents the properties
                    of a delegator's delegation reward.
                description: rewards defines all the rewards accrued by a delegator.
              total:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    DecCoin defines a token with a denomination and a decimal
                    amount.


                    NOTE: The amount field is an Dec which implements the custom
                    method

                    signatures required by gogoproto.
                description: total defines the sum of all the rewards.
            description: |-
              QueryDelegationTotalRewardsResponse is the response type for the
              Query/DelegationTotalRewards RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: delegatorAddress
          description: delegator_address defines the delegator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/delegators/{delegatorAddress}/rewards/{validatorAddress}:
    get:
      summary: DelegationRewards queries the total rewards accrued by a delegation.
      operationId: DelegationRewards
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              rewards:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    DecCoin defines a token with a denomination and a decimal
                    amount.


                    NOTE: The amount field is an Dec which implements the custom
                    method

                    signatures required by gogoproto.
                description: rewards defines the rewards accrued by a delegation.
            description: |-
              QueryDelegationRewardsResponse is the response type for the
              Query/DelegationRewards RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: delegatorAddress
          description: delegator_address defines the delegator address to query for.
          in: path
          required: true
          type: string
        - name: validatorAddress
          description: validator_address defines the validator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/delegators/{delegatorAddress}/validators:
    get:
      summary: DelegatorValidators queries the validators of a delegator.
      operationId: DelegatorValidators
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              validators:
                type: array
                items:
                  type: string
                description: >-
                  validators defines the validators a delegator is delegating
                  for.
            description: |-
              QueryDelegatorValidatorsResponse is the response type for the
              Query/DelegatorValidators RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: delegatorAddress
          description: delegator_address defines the delegator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/delegators/{delegatorAddress}/withdraw_address:
    get:
      summary: DelegatorWithdrawAddress queries withdraw address of a delegator.
      operationId: DelegatorWithdrawAddress
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              withdrawAddress:
                type: string
                description: withdraw_address defines the delegator address to query for.
            description: |-
              QueryDelegatorWithdrawAddressResponse is the response type for the
              Query/DelegatorWithdrawAddress RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: delegatorAddress
          description: delegator_address defines the delegator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/params:
    get:
      summary: Params queries params of the distribution module.
      operationId: DistributionParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  communityTax:
                    type: string
                  baseProposerReward:
                    type: string
                    description: >-
                      Deprecated: The base_proposer_reward field is deprecated
                      and is no longer used

                      in the x/distribution module's reward mechanism.
                  bonusProposerReward:
                    type: string
                    description: >-
                      Deprecated: The bonus_proposer_reward field is deprecated
                      and is no longer used

                      in the x/distribution module's reward mechanism.
                  withdrawAddrEnabled:
                    type: boolean
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/validators/{validatorAddress}:
    get:
      summary: >-
        ValidatorDistributionInfo queries validator commission and
        self-delegation rewards for validator
      operationId: ValidatorDistributionInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              operatorAddress:
                type: string
                description: operator_address defines the validator operator address.
              selfBondRewards:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    DecCoin defines a token with a denomination and a decimal
                    amount.


                    NOTE: The amount field is an Dec which implements the custom
                    method

                    signatures required by gogoproto.
                description: self_bond_rewards defines the self delegations rewards.
              commission:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    DecCoin defines a token with a denomination and a decimal
                    amount.


                    NOTE: The amount field is an Dec which implements the custom
                    method

                    signatures required by gogoproto.
                description: commission defines the commission the validator received.
            description: >-
              QueryValidatorDistributionInfoResponse is the response type for
              the Query/ValidatorDistributionInfo RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: validatorAddress
          description: validator_address defines the validator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/validators/{validatorAddress}/commission:
    get:
      summary: ValidatorCommission queries accumulated commission for a validator.
      operationId: ValidatorCommission
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              commission:
                description: commission defines the commission the validator received.
                type: object
                properties:
                  commission:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        DecCoin defines a token with a denomination and a
                        decimal amount.


                        NOTE: The amount field is an Dec which implements the
                        custom method

                        signatures required by gogoproto.
            title: |-
              QueryValidatorCommissionResponse is the response type for the
              Query/ValidatorCommission RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: validatorAddress
          description: validator_address defines the validator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/validators/{validatorAddress}/outstanding_rewards:
    get:
      summary: ValidatorOutstandingRewards queries rewards of a validator address.
      operationId: ValidatorOutstandingRewards
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              rewards:
                type: object
                properties:
                  rewards:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        DecCoin defines a token with a denomination and a
                        decimal amount.


                        NOTE: The amount field is an Dec which implements the
                        custom method

                        signatures required by gogoproto.
                description: >-
                  ValidatorOutstandingRewards represents outstanding
                  (un-withdrawn) rewards

                  for a validator inexpensive to track, allows simple sanity
                  checks.
            description: >-
              QueryValidatorOutstandingRewardsResponse is the response type for
              the

              Query/ValidatorOutstandingRewards RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: validatorAddress
          description: validator_address defines the validator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/distribution/v1beta1/validators/{validatorAddress}/slashes:
    get:
      summary: ValidatorSlashes queries slash events of a validator.
      operationId: ValidatorSlashes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              slashes:
                type: array
                items:
                  type: object
                  properties:
                    validatorPeriod:
                      type: string
                      format: uint64
                    fraction:
                      type: string
                  description: >-
                    ValidatorSlashEvent represents a validator slash event.

                    Height is implicit within the store key.

                    This is needed to calculate appropriate amount of staking
                    tokens

                    for delegations which are withdrawn after a slash has
                    occurred.
                description: slashes defines the slashes the validator received.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: |-
              QueryValidatorSlashesResponse is the response type for the
              Query/ValidatorSlashes RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: validatorAddress
          description: validator_address defines the validator address to query for.
          in: path
          required: true
          type: string
        - name: startingHeight
          description: >-
            starting_height defines the optional starting height to query the
            slashes.
          in: query
          required: false
          type: string
          format: uint64
        - name: endingHeight
          description: >-
            starting_height defines the optional ending height to query the
            slashes.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/epochs/v1beta1/current_epoch:
    get:
      summary: CurrentEpoch provide current epoch of specified identifier
      operationId: CurrentEpoch
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              currentEpoch:
                type: string
                format: int64
            description: |-
              QueryCurrentEpochResponse defines the gRPC response structure for
              querying an epoch by its identifier.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: identifier
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
  /cosmos/epochs/v1beta1/epochs:
    get:
      summary: EpochInfos provide running epochInfos
      operationId: EpochInfos
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              epochs:
                type: array
                items:
                  type: object
                  properties:
                    identifier:
                      type: string
                      description: >-
                        identifier is a unique reference to this particular
                        timer.
                    startTime:
                      type: string
                      format: date-time
                      description: >-
                        start_time is the time at which the timer first ever
                        ticks.

                        If start_time is in the future, the epoch will not begin
                        until the start

                        time.
                    duration:
                      type: string
                      description: >-
                        duration is the time in between epoch ticks.

                        In order for intended behavior to be met, duration
                        should

                        be greater than the chains expected block time.

                        Duration must be non-zero.
                    currentEpoch:
                      type: string
                      format: int64
                      description: >-
                        current_epoch is the current epoch number, or in other
                        words,

                        how many times has the timer 'ticked'.

                        The first tick (current_epoch=1) is defined as

                        the first block whose blocktime is greater than the
                        EpochInfo start_time.
                    currentEpochStartTime:
                      type: string
                      format: date-time
                      description: >-
                        current_epoch_start_time describes the start time of the
                        current timer

                        interval. The interval is (current_epoch_start_time,

                        current_epoch_start_time + duration] When the timer
                        ticks, this is set to

                        current_epoch_start_time = last_epoch_start_time +
                        duration only one timer

                        tick for a given identifier can occur per block.


                        NOTE! The current_epoch_start_time may diverge
                        significantly from the

                        wall-clock time the epoch began at. Wall-clock time of
                        epoch start may be

                        >> current_epoch_start_time. Suppose
                        current_epoch_start_time = 10,

                        duration = 5. Suppose the chain goes offline at t=14,
                        and comes back online

                        at t=30, and produces blocks at every successive time.
                        (t=31, 32, etc.)

                        * The t=30 block will start the epoch for (10, 15]

                        * The t=31 block will start the epoch for (15, 20]

                        * The t=32 block will start the epoch for (20, 25]

                        * The t=33 block will start the epoch for (25, 30]

                        * The t=34 block will start the epoch for (30, 35]

                        * The **t=36** block will start the epoch for (35, 40]
                    epochCountingStarted:
                      type: boolean
                      description: >-
                        epoch_counting_started is a boolean, that indicates
                        whether this

                        epoch timer has began yet.
                    currentEpochStartHeight:
                      type: string
                      format: int64
                      title: >-
                        current_epoch_start_height is the block height at which
                        the current epoch

                        started. (The block height at which the timer last
                        ticked)
                  description: |-
                    EpochInfo is a struct that describes the data going into
                    a timer defined by the x/epochs module.
            description: |-
              QueryEpochInfosRequest defines the gRPC response structure for
              querying all epoch info.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/evidence/v1beta1/evidence:
    get:
      summary: AllEvidence queries all evidence.
      operationId: AllEvidence
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              evidence:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
                description: evidence returns all evidences.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAllEvidenceResponse is the response type for the
              Query/AllEvidence RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/evidence/v1beta1/evidence/{hash}:
    get:
      summary: Evidence queries evidence based on evidence hash.
      operationId: Evidence
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              evidence:
                description: evidence returns the requested evidence.
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
            description: >-
              QueryEvidenceResponse is the response type for the Query/Evidence
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: hash
          description: hash defines the evidence hash of the requested evidence.
          in: path
          required: true
          type: string
        - name: evidenceHash
          description: |-
            evidence_hash defines the hash of the requested evidence.
            Deprecated: Use hash, a HEX encoded string, instead.
          in: query
          required: false
          type: string
          format: byte
      tags:
        - Cosmos SDK
  /cosmos/feegrant/v1beta1/allowance/{granter}/{grantee}:
    get:
      summary: Allowance returns granted allwance to the grantee by the granter.
      operationId: Allowance
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              allowance:
                description: allowance is a allowance granted for grantee by granter.
                type: object
                properties:
                  granter:
                    type: string
                    description: >-
                      granter is the address of the user granting an allowance
                      of their funds.
                  grantee:
                    type: string
                    description: >-
                      grantee is the address of the user being granted an
                      allowance of another user's funds.
                  allowance:
                    description: >-
                      allowance can be any of basic, periodic, allowed fee
                      allowance.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                title: >-
                  Grant is stored in the KVStore to record a grant with full
                  context
            description: >-
              QueryAllowanceResponse is the response type for the
              Query/Allowance RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: granter
          description: >-
            granter is the address of the user granting an allowance of their
            funds.
          in: path
          required: true
          type: string
        - name: grantee
          description: >-
            grantee is the address of the user being granted an allowance of
            another user's funds.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/feegrant/v1beta1/allowances/{grantee}:
    get:
      summary: Allowances returns all the grants for the given grantee address.
      operationId: Allowances
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              allowances:
                type: array
                items:
                  type: object
                  properties:
                    granter:
                      type: string
                      description: >-
                        granter is the address of the user granting an allowance
                        of their funds.
                    grantee:
                      type: string
                      description: >-
                        grantee is the address of the user being granted an
                        allowance of another user's funds.
                    allowance:
                      description: >-
                        allowance can be any of basic, periodic, allowed fee
                        allowance.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                  title: >-
                    Grant is stored in the KVStore to record a grant with full
                    context
                description: allowances are allowance's granted for grantee by granter.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAllowancesResponse is the response type for the
              Query/Allowances RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: grantee
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/feegrant/v1beta1/issued/{granter}:
    get:
      summary: AllowancesByGranter returns all the grants given by an address
      operationId: AllowancesByGranter
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              allowances:
                type: array
                items:
                  type: object
                  properties:
                    granter:
                      type: string
                      description: >-
                        granter is the address of the user granting an allowance
                        of their funds.
                    grantee:
                      type: string
                      description: >-
                        grantee is the address of the user being granted an
                        allowance of another user's funds.
                    allowance:
                      description: >-
                        allowance can be any of basic, periodic, allowed fee
                        allowance.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                  title: >-
                    Grant is stored in the KVStore to record a grant with full
                    context
                description: allowances that have been issued by the granter.
              pagination:
                description: pagination defines an pagination for the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryAllowancesByGranterResponse is the response type for the
              Query/AllowancesByGranter RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: granter
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/constitution:
    get:
      summary: Constitution queries the chain's constitution.
      operationId: Constitution
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              constitution:
                type: string
            title: >-
              QueryConstitutionResponse is the response type for the
              Query/Constitution RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/params/{paramsType}:
    get:
      summary: Params queries all parameters of the gov module.
      operationId: GovV1Params
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              votingParams:
                description: |-
                  Deprecated: Prefer to use `params` instead.
                  voting_params defines the parameters related to voting.
                type: object
                properties:
                  votingPeriod:
                    type: string
                    description: Duration of the voting period.
              depositParams:
                description: |-
                  Deprecated: Prefer to use `params` instead.
                  deposit_params defines the parameters related to deposit.
                type: object
                properties:
                  minDeposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: Minimum deposit for a proposal to enter voting period.
                  maxDepositPeriod:
                    type: string
                    description: >-
                      Maximum period for Atom holders to deposit on a proposal.
                      Initial value: 2

                      months.
              tallyParams:
                description: |-
                  Deprecated: Prefer to use `params` instead.
                  tally_params defines the parameters related to tally.
                type: object
                properties:
                  quorum:
                    type: string
                    description: >-
                      Minimum percentage of total stake needed to vote for a
                      result to be

                      considered valid.
                  threshold:
                    type: string
                    description: >-
                      Minimum proportion of Yes votes for proposal to pass.
                      Default value: 0.5.
                  vetoThreshold:
                    type: string
                    description: >-
                      Minimum value of Veto votes to Total votes ratio for
                      proposal to be

                      vetoed. Default value: 1/3.
              params:
                description: params defines all the paramaters of x/gov module.
                type: object
                properties:
                  minDeposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: Minimum deposit for a proposal to enter voting period.
                  maxDepositPeriod:
                    type: string
                    description: >-
                      Maximum period for Atom holders to deposit on a proposal.
                      Initial value: 2

                      months.
                  votingPeriod:
                    type: string
                    description: Duration of the voting period.
                  quorum:
                    type: string
                    description: >-
                      Minimum percentage of total stake needed to vote for a
                      result to be
                       considered valid.
                  threshold:
                    type: string
                    description: >-
                      Minimum proportion of Yes votes for proposal to pass.
                      Default value: 0.5.
                  vetoThreshold:
                    type: string
                    description: >-
                      Minimum value of Veto votes to Total votes ratio for
                      proposal to be
                       vetoed. Default value: 1/3.
                  minInitialDepositRatio:
                    type: string
                    description: >-
                      The ratio representing the proportion of the deposit value
                      that must be paid at proposal submission.
                  proposalCancelRatio:
                    type: string
                    description: >-
                      The cancel ratio which will not be returned back to the
                      depositors when a proposal is cancelled.
                  proposalCancelDest:
                    type: string
                    description: >-
                      The address which will receive (proposal_cancel_ratio *
                      deposit) proposal deposits.

                      If empty, the (proposal_cancel_ratio * deposit) proposal
                      deposits will be burned.
                  expeditedVotingPeriod:
                    type: string
                    description: Duration of the voting period of an expedited proposal.
                  expeditedThreshold:
                    type: string
                    description: >-
                      Minimum proportion of Yes votes for proposal to pass.
                      Default value: 0.67.
                  expeditedMinDeposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: >-
                      Minimum expedited deposit for a proposal to enter voting
                      period.
                  burnVoteQuorum:
                    type: boolean
                    title: burn deposits if a proposal does not meet quorum
                  burnProposalDepositPrevote:
                    type: boolean
                    title: burn deposits if the proposal does not enter voting period
                  burnVoteVeto:
                    type: boolean
                    title: burn deposits if quorum with vote type no_veto is met
                  minDepositRatio:
                    type: string
                    description: >-
                      The ratio representing the proportion of the deposit value
                      minimum that must be met when making a deposit.

                      Default value: 0.01. Meaning that for a chain with a
                      min_deposit of 100stake, a deposit of 1stake would be

                      required.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: paramsType
          description: >-
            params_type defines which parameters to query for, can be one of
            "voting",

            "tallying" or "deposit".
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals:
    get:
      summary: Proposals queries all proposals based on given status.
      operationId: GovV1Proposal
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              proposals:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                      description: id defines the unique id of the proposal.
                    messages:
                      type: array
                      items:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com. As of May 2023, there are no
                              widely used type server

                              implementations and no plans to implement one.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }
                              // or ...
                              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                                foo = any.unpack(Foo.getDefaultInstance());
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := anypb.New(foo)
                               if err != nil {
                                 ...
                               }
                               ...
                               foo := &pb.Foo{}
                               if err := any.UnmarshalTo(foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".


                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                      description: >-
                        messages are the arbitrary messages to be executed if
                        the proposal passes.
                    status:
                      description: status defines the proposal status.
                      type: string
                      enum:
                        - PROPOSAL_STATUS_UNSPECIFIED
                        - PROPOSAL_STATUS_DEPOSIT_PERIOD
                        - PROPOSAL_STATUS_VOTING_PERIOD
                        - PROPOSAL_STATUS_PASSED
                        - PROPOSAL_STATUS_REJECTED
                        - PROPOSAL_STATUS_FAILED
                      default: PROPOSAL_STATUS_UNSPECIFIED
                    finalTallyResult:
                      description: >-
                        final_tally_result is the final tally result of the
                        proposal. When

                        querying a proposal via gRPC, this field is not
                        populated until the

                        proposal's voting period has ended.
                      type: object
                      properties:
                        yesCount:
                          type: string
                          description: yes_count is the number of yes votes on a proposal.
                        abstainCount:
                          type: string
                          description: >-
                            abstain_count is the number of abstain votes on a
                            proposal.
                        noCount:
                          type: string
                          description: no_count is the number of no votes on a proposal.
                        noWithVetoCount:
                          type: string
                          description: >-
                            no_with_veto_count is the number of no with veto
                            votes on a proposal.
                    submitTime:
                      type: string
                      format: date-time
                      description: submit_time is the time of proposal submission.
                    depositEndTime:
                      type: string
                      format: date-time
                      description: deposit_end_time is the end time for deposition.
                    totalDeposit:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: total_deposit is the total deposit on the proposal.
                    votingStartTime:
                      type: string
                      format: date-time
                      description: >-
                        voting_start_time is the starting time to vote on a
                        proposal.
                    votingEndTime:
                      type: string
                      format: date-time
                      description: voting_end_time is the end time of voting on a proposal.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the
                        proposal.

                        the recommended format of the metadata is to be found
                        here:

                        https://docs.cosmos.network/v0.47/modules/gov#proposal-3
                    title:
                      type: string
                      title: title is the title of the proposal
                    summary:
                      type: string
                      title: summary is a short summary of the proposal
                    proposer:
                      type: string
                      title: proposer is the address of the proposal sumbitter
                    expedited:
                      type: boolean
                      title: expedited defines if the proposal is expedited
                    failedReason:
                      type: string
                      title: failed_reason defines the reason why the proposal failed
                  description: >-
                    Proposal defines the core field members of a governance
                    proposal.
                description: proposals defines all the requested governance proposals.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryProposalsResponse is the response type for the
              Query/Proposals RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalStatus
          description: |-
            proposal_status defines the status of the proposals.

             - PROPOSAL_STATUS_UNSPECIFIED: PROPOSAL_STATUS_UNSPECIFIED defines the default proposal status.
             - PROPOSAL_STATUS_DEPOSIT_PERIOD: PROPOSAL_STATUS_DEPOSIT_PERIOD defines a proposal status during the deposit
            period.
             - PROPOSAL_STATUS_VOTING_PERIOD: PROPOSAL_STATUS_VOTING_PERIOD defines a proposal status during the voting
            period.
             - PROPOSAL_STATUS_PASSED: PROPOSAL_STATUS_PASSED defines a proposal status of a proposal that has
            passed.
             - PROPOSAL_STATUS_REJECTED: PROPOSAL_STATUS_REJECTED defines a proposal status of a proposal that has
            been rejected.
             - PROPOSAL_STATUS_FAILED: PROPOSAL_STATUS_FAILED defines a proposal status of a proposal that has
            failed.
          in: query
          required: false
          type: string
          enum:
            - PROPOSAL_STATUS_UNSPECIFIED
            - PROPOSAL_STATUS_DEPOSIT_PERIOD
            - PROPOSAL_STATUS_VOTING_PERIOD
            - PROPOSAL_STATUS_PASSED
            - PROPOSAL_STATUS_REJECTED
            - PROPOSAL_STATUS_FAILED
          default: PROPOSAL_STATUS_UNSPECIFIED
        - name: voter
          description: voter defines the voter address for the proposals.
          in: query
          required: false
          type: string
        - name: depositor
          description: depositor defines the deposit addresses from the proposals.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals/{proposalId}:
    get:
      summary: Proposal queries proposal details based on ProposalID.
      operationId: GovV1Proposal
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              proposal:
                description: proposal is the requested governance proposal.
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                    description: id defines the unique id of the proposal.
                  messages:
                    type: array
                    items:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    description: >-
                      messages are the arbitrary messages to be executed if the
                      proposal passes.
                  status:
                    description: status defines the proposal status.
                    type: string
                    enum:
                      - PROPOSAL_STATUS_UNSPECIFIED
                      - PROPOSAL_STATUS_DEPOSIT_PERIOD
                      - PROPOSAL_STATUS_VOTING_PERIOD
                      - PROPOSAL_STATUS_PASSED
                      - PROPOSAL_STATUS_REJECTED
                      - PROPOSAL_STATUS_FAILED
                    default: PROPOSAL_STATUS_UNSPECIFIED
                  finalTallyResult:
                    description: >-
                      final_tally_result is the final tally result of the
                      proposal. When

                      querying a proposal via gRPC, this field is not populated
                      until the

                      proposal's voting period has ended.
                    type: object
                    properties:
                      yesCount:
                        type: string
                        description: yes_count is the number of yes votes on a proposal.
                      abstainCount:
                        type: string
                        description: >-
                          abstain_count is the number of abstain votes on a
                          proposal.
                      noCount:
                        type: string
                        description: no_count is the number of no votes on a proposal.
                      noWithVetoCount:
                        type: string
                        description: >-
                          no_with_veto_count is the number of no with veto votes
                          on a proposal.
                  submitTime:
                    type: string
                    format: date-time
                    description: submit_time is the time of proposal submission.
                  depositEndTime:
                    type: string
                    format: date-time
                    description: deposit_end_time is the end time for deposition.
                  totalDeposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: total_deposit is the total deposit on the proposal.
                  votingStartTime:
                    type: string
                    format: date-time
                    description: >-
                      voting_start_time is the starting time to vote on a
                      proposal.
                  votingEndTime:
                    type: string
                    format: date-time
                    description: voting_end_time is the end time of voting on a proposal.
                  metadata:
                    type: string
                    title: >-
                      metadata is any arbitrary metadata attached to the
                      proposal.

                      the recommended format of the metadata is to be found
                      here:

                      https://docs.cosmos.network/v0.47/modules/gov#proposal-3
                  title:
                    type: string
                    title: title is the title of the proposal
                  summary:
                    type: string
                    title: summary is a short summary of the proposal
                  proposer:
                    type: string
                    title: proposer is the address of the proposal sumbitter
                  expedited:
                    type: boolean
                    title: expedited defines if the proposal is expedited
                  failedReason:
                    type: string
                    title: failed_reason defines the reason why the proposal failed
            description: >-
              QueryProposalResponse is the response type for the Query/Proposal
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals/{proposalId}/deposits:
    get:
      summary: Deposits queries all deposits of a single proposal.
      operationId: GovV1Deposit
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              deposits:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal_id defines the unique id of the proposal.
                    depositor:
                      type: string
                      description: >-
                        depositor defines the deposit addresses from the
                        proposals.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: amount to be deposited by depositor.
                  description: >-
                    Deposit defines an amount deposited by an account address to
                    an active

                    proposal.
                description: deposits defines the requested deposits.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryDepositsResponse is the response type for the Query/Deposits
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals/{proposalId}/deposits/{depositor}:
    get:
      summary: >-
        Deposit queries single deposit information based on proposalID,
        depositAddr.
      operationId: GovV1Deposit
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              deposit:
                description: deposit defines the requested deposit.
                type: object
                properties:
                  proposalId:
                    type: string
                    format: uint64
                    description: proposal_id defines the unique id of the proposal.
                  depositor:
                    type: string
                    description: >-
                      depositor defines the deposit addresses from the
                      proposals.
                  amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: amount to be deposited by depositor.
            description: >-
              QueryDepositResponse is the response type for the Query/Deposit
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: depositor
          description: depositor defines the deposit addresses from the proposals.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals/{proposalId}/tally:
    get:
      summary: TallyResult queries the tally of a proposal vote.
      operationId: GovV1TallyResult
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              tally:
                description: tally defines the requested tally.
                type: object
                properties:
                  yesCount:
                    type: string
                    description: yes_count is the number of yes votes on a proposal.
                  abstainCount:
                    type: string
                    description: >-
                      abstain_count is the number of abstain votes on a
                      proposal.
                  noCount:
                    type: string
                    description: no_count is the number of no votes on a proposal.
                  noWithVetoCount:
                    type: string
                    description: >-
                      no_with_veto_count is the number of no with veto votes on
                      a proposal.
            description: >-
              QueryTallyResultResponse is the response type for the Query/Tally
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals/{proposalId}/votes:
    get:
      summary: Votes queries votes of a given proposal.
      operationId: GovV1Votes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              votes:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal_id defines the unique id of the proposal.
                    voter:
                      type: string
                      description: voter is the voter address of the proposal.
                    options:
                      type: array
                      items:
                        type: object
                        properties:
                          option:
                            description: >-
                              option defines the valid vote options, it must not
                              contain duplicate vote options.
                            type: string
                            enum:
                              - VOTE_OPTION_UNSPECIFIED
                              - VOTE_OPTION_YES
                              - VOTE_OPTION_ABSTAIN
                              - VOTE_OPTION_NO
                              - VOTE_OPTION_NO_WITH_VETO
                            default: VOTE_OPTION_UNSPECIFIED
                          weight:
                            type: string
                            description: >-
                              weight is the vote weight associated with the vote
                              option.
                        description: >-
                          WeightedVoteOption defines a unit of vote for vote
                          split.
                      description: options is the weighted vote options.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the vote.

                        the recommended format of the metadata is to be found
                        here:
                        https://docs.cosmos.network/v0.47/modules/gov#vote-5
                  description: >-
                    Vote defines a vote on a governance proposal.

                    A Vote consists of a proposal ID, the voter, and the vote
                    option.
                description: votes defines the queried votes.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVotesResponse is the response type for the Query/Votes RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1/proposals/{proposalId}/votes/{voter}:
    get:
      summary: Vote queries voted information based on proposalID, voterAddr.
      operationId: GovV1Vote
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              vote:
                description: vote defines the queried vote.
                type: object
                properties:
                  proposalId:
                    type: string
                    format: uint64
                    description: proposal_id defines the unique id of the proposal.
                  voter:
                    type: string
                    description: voter is the voter address of the proposal.
                  options:
                    type: array
                    items:
                      type: object
                      properties:
                        option:
                          description: >-
                            option defines the valid vote options, it must not
                            contain duplicate vote options.
                          type: string
                          enum:
                            - VOTE_OPTION_UNSPECIFIED
                            - VOTE_OPTION_YES
                            - VOTE_OPTION_ABSTAIN
                            - VOTE_OPTION_NO
                            - VOTE_OPTION_NO_WITH_VETO
                          default: VOTE_OPTION_UNSPECIFIED
                        weight:
                          type: string
                          description: >-
                            weight is the vote weight associated with the vote
                            option.
                      description: >-
                        WeightedVoteOption defines a unit of vote for vote
                        split.
                    description: options is the weighted vote options.
                  metadata:
                    type: string
                    title: >-
                      metadata is any arbitrary metadata attached to the vote.

                      the recommended format of the metadata is to be found
                      here: https://docs.cosmos.network/v0.47/modules/gov#vote-5
            description: >-
              QueryVoteResponse is the response type for the Query/Vote RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: voter
          description: voter defines the voter address for the proposals.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/params/{paramsType}:
    get:
      summary: Params queries all parameters of the gov module.
      operationId: GovParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              votingParams:
                description: voting_params defines the parameters related to voting.
                type: object
                properties:
                  votingPeriod:
                    type: string
                    description: Duration of the voting period.
              depositParams:
                description: deposit_params defines the parameters related to deposit.
                type: object
                properties:
                  minDeposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: Minimum deposit for a proposal to enter voting period.
                  maxDepositPeriod:
                    type: string
                    description: >-
                      Maximum period for Atom holders to deposit on a proposal.
                      Initial value: 2

                      months.
              tallyParams:
                description: tally_params defines the parameters related to tally.
                type: object
                properties:
                  quorum:
                    type: string
                    format: byte
                    description: >-
                      Minimum percentage of total stake needed to vote for a
                      result to be

                      considered valid.
                  threshold:
                    type: string
                    format: byte
                    description: >-
                      Minimum proportion of Yes votes for proposal to pass.
                      Default value: 0.5.
                  vetoThreshold:
                    type: string
                    format: byte
                    description: >-
                      Minimum value of Veto votes to Total votes ratio for
                      proposal to be

                      vetoed. Default value: 1/3.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: paramsType
          description: >-
            params_type defines which parameters to query for, can be one of
            "voting",

            "tallying" or "deposit".
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals:
    get:
      summary: Proposals queries all proposals based on given status.
      operationId: Proposals
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              proposals:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal_id defines the unique id of the proposal.
                    content:
                      description: content is the proposal's content.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                    status:
                      description: status defines the proposal status.
                      type: string
                      enum:
                        - PROPOSAL_STATUS_UNSPECIFIED
                        - PROPOSAL_STATUS_DEPOSIT_PERIOD
                        - PROPOSAL_STATUS_VOTING_PERIOD
                        - PROPOSAL_STATUS_PASSED
                        - PROPOSAL_STATUS_REJECTED
                        - PROPOSAL_STATUS_FAILED
                      default: PROPOSAL_STATUS_UNSPECIFIED
                    finalTallyResult:
                      description: >-
                        final_tally_result is the final tally result of the
                        proposal. When

                        querying a proposal via gRPC, this field is not
                        populated until the

                        proposal's voting period has ended.
                      type: object
                      properties:
                        'yes':
                          type: string
                          description: yes is the number of yes votes on a proposal.
                        abstain:
                          type: string
                          description: >-
                            abstain is the number of abstain votes on a
                            proposal.
                        'no':
                          type: string
                          description: no is the number of no votes on a proposal.
                        noWithVeto:
                          type: string
                          description: >-
                            no_with_veto is the number of no with veto votes on
                            a proposal.
                    submitTime:
                      type: string
                      format: date-time
                      description: submit_time is the time of proposal submission.
                    depositEndTime:
                      type: string
                      format: date-time
                      description: deposit_end_time is the end time for deposition.
                    totalDeposit:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: total_deposit is the total deposit on the proposal.
                    votingStartTime:
                      type: string
                      format: date-time
                      description: >-
                        voting_start_time is the starting time to vote on a
                        proposal.
                    votingEndTime:
                      type: string
                      format: date-time
                      description: voting_end_time is the end time of voting on a proposal.
                  description: >-
                    Proposal defines the core field members of a governance
                    proposal.
                description: proposals defines all the requested governance proposals.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryProposalsResponse is the response type for the
              Query/Proposals RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalStatus
          description: |-
            proposal_status defines the status of the proposals.

             - PROPOSAL_STATUS_UNSPECIFIED: PROPOSAL_STATUS_UNSPECIFIED defines the default proposal status.
             - PROPOSAL_STATUS_DEPOSIT_PERIOD: PROPOSAL_STATUS_DEPOSIT_PERIOD defines a proposal status during the deposit
            period.
             - PROPOSAL_STATUS_VOTING_PERIOD: PROPOSAL_STATUS_VOTING_PERIOD defines a proposal status during the voting
            period.
             - PROPOSAL_STATUS_PASSED: PROPOSAL_STATUS_PASSED defines a proposal status of a proposal that has
            passed.
             - PROPOSAL_STATUS_REJECTED: PROPOSAL_STATUS_REJECTED defines a proposal status of a proposal that has
            been rejected.
             - PROPOSAL_STATUS_FAILED: PROPOSAL_STATUS_FAILED defines a proposal status of a proposal that has
            failed.
          in: query
          required: false
          type: string
          enum:
            - PROPOSAL_STATUS_UNSPECIFIED
            - PROPOSAL_STATUS_DEPOSIT_PERIOD
            - PROPOSAL_STATUS_VOTING_PERIOD
            - PROPOSAL_STATUS_PASSED
            - PROPOSAL_STATUS_REJECTED
            - PROPOSAL_STATUS_FAILED
          default: PROPOSAL_STATUS_UNSPECIFIED
        - name: voter
          description: voter defines the voter address for the proposals.
          in: query
          required: false
          type: string
        - name: depositor
          description: depositor defines the deposit addresses from the proposals.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals/{proposalId}:
    get:
      summary: Proposal queries proposal details based on ProposalID.
      operationId: Proposal
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              proposal:
                type: object
                properties:
                  proposalId:
                    type: string
                    format: uint64
                    description: proposal_id defines the unique id of the proposal.
                  content:
                    description: content is the proposal's content.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                  status:
                    description: status defines the proposal status.
                    type: string
                    enum:
                      - PROPOSAL_STATUS_UNSPECIFIED
                      - PROPOSAL_STATUS_DEPOSIT_PERIOD
                      - PROPOSAL_STATUS_VOTING_PERIOD
                      - PROPOSAL_STATUS_PASSED
                      - PROPOSAL_STATUS_REJECTED
                      - PROPOSAL_STATUS_FAILED
                    default: PROPOSAL_STATUS_UNSPECIFIED
                  finalTallyResult:
                    description: >-
                      final_tally_result is the final tally result of the
                      proposal. When

                      querying a proposal via gRPC, this field is not populated
                      until the

                      proposal's voting period has ended.
                    type: object
                    properties:
                      'yes':
                        type: string
                        description: yes is the number of yes votes on a proposal.
                      abstain:
                        type: string
                        description: abstain is the number of abstain votes on a proposal.
                      'no':
                        type: string
                        description: no is the number of no votes on a proposal.
                      noWithVeto:
                        type: string
                        description: >-
                          no_with_veto is the number of no with veto votes on a
                          proposal.
                  submitTime:
                    type: string
                    format: date-time
                    description: submit_time is the time of proposal submission.
                  depositEndTime:
                    type: string
                    format: date-time
                    description: deposit_end_time is the end time for deposition.
                  totalDeposit:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: total_deposit is the total deposit on the proposal.
                  votingStartTime:
                    type: string
                    format: date-time
                    description: >-
                      voting_start_time is the starting time to vote on a
                      proposal.
                  votingEndTime:
                    type: string
                    format: date-time
                    description: voting_end_time is the end time of voting on a proposal.
                description: >-
                  Proposal defines the core field members of a governance
                  proposal.
            description: >-
              QueryProposalResponse is the response type for the Query/Proposal
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals/{proposalId}/deposits:
    get:
      summary: Deposits queries all deposits of a single proposal.
      operationId: Deposits
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              deposits:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal_id defines the unique id of the proposal.
                    depositor:
                      type: string
                      description: >-
                        depositor defines the deposit addresses from the
                        proposals.
                    amount:
                      type: array
                      items:
                        type: object
                        properties:
                          denom:
                            type: string
                          amount:
                            type: string
                        description: >-
                          Coin defines a token with a denomination and an
                          amount.


                          NOTE: The amount field is an Int which implements the
                          custom method

                          signatures required by gogoproto.
                      description: amount to be deposited by depositor.
                  description: >-
                    Deposit defines an amount deposited by an account address to
                    an active

                    proposal.
                description: deposits defines the requested deposits.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryDepositsResponse is the response type for the Query/Deposits
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals/{proposalId}/deposits/{depositor}:
    get:
      summary: >-
        Deposit queries single deposit information based on proposalID,
        depositor address.
      operationId: Deposit
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              deposit:
                description: deposit defines the requested deposit.
                type: object
                properties:
                  proposalId:
                    type: string
                    format: uint64
                    description: proposal_id defines the unique id of the proposal.
                  depositor:
                    type: string
                    description: >-
                      depositor defines the deposit addresses from the
                      proposals.
                  amount:
                    type: array
                    items:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                    description: amount to be deposited by depositor.
            description: >-
              QueryDepositResponse is the response type for the Query/Deposit
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: depositor
          description: depositor defines the deposit addresses from the proposals.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals/{proposalId}/tally:
    get:
      summary: TallyResult queries the tally of a proposal vote.
      operationId: TallyResult
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              tally:
                description: tally defines the requested tally.
                type: object
                properties:
                  'yes':
                    type: string
                    description: yes is the number of yes votes on a proposal.
                  abstain:
                    type: string
                    description: abstain is the number of abstain votes on a proposal.
                  'no':
                    type: string
                    description: no is the number of no votes on a proposal.
                  noWithVeto:
                    type: string
                    description: >-
                      no_with_veto is the number of no with veto votes on a
                      proposal.
            description: >-
              QueryTallyResultResponse is the response type for the Query/Tally
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals/{proposalId}/votes:
    get:
      summary: Votes queries votes of a given proposal.
      operationId: Votes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              votes:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal_id defines the unique id of the proposal.
                    voter:
                      type: string
                      description: voter is the voter address of the proposal.
                    option:
                      description: >-
                        Deprecated: Prefer to use `options` instead. This field
                        is set in queries

                        if and only if `len(options) == 1` and that option has
                        weight 1. In all

                        other cases, this field will default to
                        VOTE_OPTION_UNSPECIFIED.
                      type: string
                      enum:
                        - VOTE_OPTION_UNSPECIFIED
                        - VOTE_OPTION_YES
                        - VOTE_OPTION_ABSTAIN
                        - VOTE_OPTION_NO
                        - VOTE_OPTION_NO_WITH_VETO
                      default: VOTE_OPTION_UNSPECIFIED
                    options:
                      type: array
                      items:
                        type: object
                        properties:
                          option:
                            description: >-
                              option defines the valid vote options, it must not
                              contain duplicate vote options.
                            type: string
                            enum:
                              - VOTE_OPTION_UNSPECIFIED
                              - VOTE_OPTION_YES
                              - VOTE_OPTION_ABSTAIN
                              - VOTE_OPTION_NO
                              - VOTE_OPTION_NO_WITH_VETO
                            default: VOTE_OPTION_UNSPECIFIED
                          weight:
                            type: string
                            description: >-
                              weight is the vote weight associated with the vote
                              option.
                        description: >-
                          WeightedVoteOption defines a unit of vote for vote
                          split.
                      description: options is the weighted vote options.
                  description: >-
                    Vote defines a vote on a governance proposal.

                    A Vote consists of a proposal ID, the voter, and the vote
                    option.
                description: votes defines the queried votes.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVotesResponse is the response type for the Query/Votes RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/gov/v1beta1/proposals/{proposalId}/votes/{voter}:
    get:
      summary: Vote queries voted information based on proposalID, voterAddr.
      operationId: Vote
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              vote:
                description: vote defines the queried vote.
                type: object
                properties:
                  proposalId:
                    type: string
                    format: uint64
                    description: proposal_id defines the unique id of the proposal.
                  voter:
                    type: string
                    description: voter is the voter address of the proposal.
                  option:
                    description: >-
                      Deprecated: Prefer to use `options` instead. This field is
                      set in queries

                      if and only if `len(options) == 1` and that option has
                      weight 1. In all

                      other cases, this field will default to
                      VOTE_OPTION_UNSPECIFIED.
                    type: string
                    enum:
                      - VOTE_OPTION_UNSPECIFIED
                      - VOTE_OPTION_YES
                      - VOTE_OPTION_ABSTAIN
                      - VOTE_OPTION_NO
                      - VOTE_OPTION_NO_WITH_VETO
                    default: VOTE_OPTION_UNSPECIFIED
                  options:
                    type: array
                    items:
                      type: object
                      properties:
                        option:
                          description: >-
                            option defines the valid vote options, it must not
                            contain duplicate vote options.
                          type: string
                          enum:
                            - VOTE_OPTION_UNSPECIFIED
                            - VOTE_OPTION_YES
                            - VOTE_OPTION_ABSTAIN
                            - VOTE_OPTION_NO
                            - VOTE_OPTION_NO_WITH_VETO
                          default: VOTE_OPTION_UNSPECIFIED
                        weight:
                          type: string
                          description: >-
                            weight is the vote weight associated with the vote
                            option.
                      description: >-
                        WeightedVoteOption defines a unit of vote for vote
                        split.
                    description: options is the weighted vote options.
            description: >-
              QueryVoteResponse is the response type for the Query/Vote RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id defines the unique id of the proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: voter
          description: voter defines the voter address for the proposals.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/group/v1/group_info/{groupId}:
    get:
      summary: GroupInfo queries group info based on group id.
      operationId: GroupInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              info:
                description: info is the GroupInfo of the group.
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                    description: id is the unique ID of the group.
                  admin:
                    type: string
                    description: admin is the account address of the group's admin.
                  metadata:
                    type: string
                    title: >-
                      metadata is any arbitrary metadata to attached to the
                      group.

                      the recommended format of the metadata is to be found
                      here:
                      https://docs.cosmos.network/v0.47/modules/group#group-1
                  version:
                    type: string
                    format: uint64
                    title: >-
                      version is used to track changes to a group's membership
                      structure that

                      would break existing proposals. Whenever any members
                      weight is changed,

                      or any member is added or removed this version is
                      incremented and will

                      cause proposals based on older versions of this group to
                      fail
                  totalWeight:
                    type: string
                    description: total_weight is the sum of the group members' weights.
                  createdAt:
                    type: string
                    format: date-time
                    description: >-
                      created_at is a timestamp specifying when a group was
                      created.
            description: QueryGroupInfoResponse is the Query/GroupInfo response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: groupId
          description: group_id is the unique ID of the group.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/group/v1/group_members/{groupId}:
    get:
      summary: GroupMembers queries members of a group by group id.
      operationId: GroupMembers
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              members:
                type: array
                items:
                  type: object
                  properties:
                    groupId:
                      type: string
                      format: uint64
                      description: group_id is the unique ID of the group.
                    member:
                      description: member is the member data.
                      type: object
                      properties:
                        address:
                          type: string
                          description: address is the member's account address.
                        weight:
                          type: string
                          description: >-
                            weight is the member's voting weight that should be
                            greater than 0.
                        metadata:
                          type: string
                          description: >-
                            metadata is any arbitrary metadata attached to the
                            member.
                        addedAt:
                          type: string
                          format: date-time
                          description: >-
                            added_at is a timestamp specifying when a member was
                            added.
                  description: >-
                    GroupMember represents the relationship between a group and
                    a member.
                description: members are the members of the group with given group_id.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGroupMembersResponse is the Query/GroupMembersResponse
              response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: groupId
          description: group_id is the unique ID of the group.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/group_policies_by_admin/{admin}:
    get:
      summary: GroupPoliciesByAdmin queries group policies by admin address.
      operationId: GroupPoliciesByAdmin
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              groupPolicies:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: address is the account address of group policy.
                    groupId:
                      type: string
                      format: uint64
                      description: group_id is the unique ID of the group.
                    admin:
                      type: string
                      description: admin is the account address of the group admin.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the group
                        policy.

                        the recommended format of the metadata is to be found
                        here:

                        https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
                    version:
                      type: string
                      format: uint64
                      description: >-
                        version is used to track changes to a group's
                        GroupPolicyInfo structure that

                        would create a different result on a running proposal.
                    decisionPolicy:
                      description: >-
                        decision_policy specifies the group policy's decision
                        policy.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                    createdAt:
                      type: string
                      format: date-time
                      description: >-
                        created_at is a timestamp specifying when a group policy
                        was created.
                  description: >-
                    GroupPolicyInfo represents the high-level on-chain
                    information for a group policy.
                description: >-
                  group_policies are the group policies info with provided
                  admin.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGroupPoliciesByAdminResponse is the
              Query/GroupPoliciesByAdmin response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: admin
          description: admin is the admin address of the group policy.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/group_policies_by_group/{groupId}:
    get:
      summary: GroupPoliciesByGroup queries group policies by group id.
      operationId: GroupPoliciesByGroup
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              groupPolicies:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      description: address is the account address of group policy.
                    groupId:
                      type: string
                      format: uint64
                      description: group_id is the unique ID of the group.
                    admin:
                      type: string
                      description: admin is the account address of the group admin.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the group
                        policy.

                        the recommended format of the metadata is to be found
                        here:

                        https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
                    version:
                      type: string
                      format: uint64
                      description: >-
                        version is used to track changes to a group's
                        GroupPolicyInfo structure that

                        would create a different result on a running proposal.
                    decisionPolicy:
                      description: >-
                        decision_policy specifies the group policy's decision
                        policy.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                    createdAt:
                      type: string
                      format: date-time
                      description: >-
                        created_at is a timestamp specifying when a group policy
                        was created.
                  description: >-
                    GroupPolicyInfo represents the high-level on-chain
                    information for a group policy.
                description: >-
                  group_policies are the group policies info associated with the
                  provided group.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGroupPoliciesByGroupResponse is the
              Query/GroupPoliciesByGroup response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: groupId
          description: group_id is the unique ID of the group policy's group.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/group_policy_info/{address}:
    get:
      summary: >-
        GroupPolicyInfo queries group policy info based on account address of
        group policy.
      operationId: GroupPolicyInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              info:
                description: info is the GroupPolicyInfo of the group policy.
                type: object
                properties:
                  address:
                    type: string
                    description: address is the account address of group policy.
                  groupId:
                    type: string
                    format: uint64
                    description: group_id is the unique ID of the group.
                  admin:
                    type: string
                    description: admin is the account address of the group admin.
                  metadata:
                    type: string
                    title: >-
                      metadata is any arbitrary metadata attached to the group
                      policy.

                      the recommended format of the metadata is to be found
                      here:

                      https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
                  version:
                    type: string
                    format: uint64
                    description: >-
                      version is used to track changes to a group's
                      GroupPolicyInfo structure that

                      would create a different result on a running proposal.
                  decisionPolicy:
                    description: >-
                      decision_policy specifies the group policy's decision
                      policy.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                  createdAt:
                    type: string
                    format: date-time
                    description: >-
                      created_at is a timestamp specifying when a group policy
                      was created.
            description: >-
              QueryGroupPolicyInfoResponse is the Query/GroupPolicyInfo response
              type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: address
          description: address is the account address of the group policy.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/group/v1/groups:
    get:
      summary: Groups queries all groups in state.
      operationId: Groups
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              groups:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                      description: id is the unique ID of the group.
                    admin:
                      type: string
                      description: admin is the account address of the group's admin.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata to attached to the
                        group.

                        the recommended format of the metadata is to be found
                        here:
                        https://docs.cosmos.network/v0.47/modules/group#group-1
                    version:
                      type: string
                      format: uint64
                      title: >-
                        version is used to track changes to a group's membership
                        structure that

                        would break existing proposals. Whenever any members
                        weight is changed,

                        or any member is added or removed this version is
                        incremented and will

                        cause proposals based on older versions of this group to
                        fail
                    totalWeight:
                      type: string
                      description: total_weight is the sum of the group members' weights.
                    createdAt:
                      type: string
                      format: date-time
                      description: >-
                        created_at is a timestamp specifying when a group was
                        created.
                  description: >-
                    GroupInfo represents the high-level on-chain information for
                    a group.
                description: '`groups` is all the groups present in state.'
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: QueryGroupsResponse is the Query/Groups response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/groups_by_admin/{admin}:
    get:
      summary: GroupsByAdmin queries groups by admin address.
      operationId: GroupsByAdmin
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              groups:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                      description: id is the unique ID of the group.
                    admin:
                      type: string
                      description: admin is the account address of the group's admin.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata to attached to the
                        group.

                        the recommended format of the metadata is to be found
                        here:
                        https://docs.cosmos.network/v0.47/modules/group#group-1
                    version:
                      type: string
                      format: uint64
                      title: >-
                        version is used to track changes to a group's membership
                        structure that

                        would break existing proposals. Whenever any members
                        weight is changed,

                        or any member is added or removed this version is
                        incremented and will

                        cause proposals based on older versions of this group to
                        fail
                    totalWeight:
                      type: string
                      description: total_weight is the sum of the group members' weights.
                    createdAt:
                      type: string
                      format: date-time
                      description: >-
                        created_at is a timestamp specifying when a group was
                        created.
                  description: >-
                    GroupInfo represents the high-level on-chain information for
                    a group.
                description: groups are the groups info with the provided admin.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGroupsByAdminResponse is the Query/GroupsByAdminResponse
              response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: admin
          description: admin is the account address of a group's admin.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/groups_by_member/{address}:
    get:
      summary: GroupsByMember queries groups by member address.
      operationId: GroupsByMember
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              groups:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                      description: id is the unique ID of the group.
                    admin:
                      type: string
                      description: admin is the account address of the group's admin.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata to attached to the
                        group.

                        the recommended format of the metadata is to be found
                        here:
                        https://docs.cosmos.network/v0.47/modules/group#group-1
                    version:
                      type: string
                      format: uint64
                      title: >-
                        version is used to track changes to a group's membership
                        structure that

                        would break existing proposals. Whenever any members
                        weight is changed,

                        or any member is added or removed this version is
                        incremented and will

                        cause proposals based on older versions of this group to
                        fail
                    totalWeight:
                      type: string
                      description: total_weight is the sum of the group members' weights.
                    createdAt:
                      type: string
                      format: date-time
                      description: >-
                        created_at is a timestamp specifying when a group was
                        created.
                  description: >-
                    GroupInfo represents the high-level on-chain information for
                    a group.
                description: groups are the groups info with the provided group member.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryGroupsByMemberResponse is the Query/GroupsByMember response
              type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: address
          description: address is the group member address.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/proposal/{proposalId}:
    get:
      summary: Proposal queries a proposal based on proposal id.
      operationId: GroupProposal
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              proposal:
                description: proposal is the proposal info.
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                    description: id is the unique id of the proposal.
                  groupPolicyAddress:
                    type: string
                    description: >-
                      group_policy_address is the account address of group
                      policy.
                  metadata:
                    type: string
                    title: >-
                      metadata is any arbitrary metadata attached to the
                      proposal.

                      the recommended format of the metadata is to be found
                      here:

                      https://docs.cosmos.network/v0.47/modules/group#proposal-4
                  proposers:
                    type: array
                    items:
                      type: string
                    description: proposers are the account addresses of the proposers.
                  submitTime:
                    type: string
                    format: date-time
                    description: >-
                      submit_time is a timestamp specifying when a proposal was
                      submitted.
                  groupVersion:
                    type: string
                    format: uint64
                    description: >-
                      group_version tracks the version of the group at proposal
                      submission.

                      This field is here for informational purposes only.
                  groupPolicyVersion:
                    type: string
                    format: uint64
                    description: >-
                      group_policy_version tracks the version of the group
                      policy at proposal submission.

                      When a decision policy is changed, existing proposals from
                      previous policy

                      versions will become invalid with the `ABORTED` status.

                      This field is here for informational purposes only.
                  status:
                    description: >-
                      status represents the high level position in the life
                      cycle of the proposal. Initial value is Submitted.
                    type: string
                    enum:
                      - PROPOSAL_STATUS_UNSPECIFIED
                      - PROPOSAL_STATUS_SUBMITTED
                      - PROPOSAL_STATUS_ACCEPTED
                      - PROPOSAL_STATUS_REJECTED
                      - PROPOSAL_STATUS_ABORTED
                      - PROPOSAL_STATUS_WITHDRAWN
                    default: PROPOSAL_STATUS_UNSPECIFIED
                  finalTallyResult:
                    description: >-
                      final_tally_result contains the sums of all weighted votes
                      for this

                      proposal for each vote option. It is empty at submission,
                      and only

                      populated after tallying, at voting period end or at
                      proposal execution,

                      whichever happens first.
                    type: object
                    properties:
                      yesCount:
                        type: string
                        description: yes_count is the weighted sum of yes votes.
                      abstainCount:
                        type: string
                        description: abstain_count is the weighted sum of abstainers.
                      noCount:
                        type: string
                        description: no_count is the weighted sum of no votes.
                      noWithVetoCount:
                        type: string
                        description: no_with_veto_count is the weighted sum of veto.
                  votingPeriodEnd:
                    type: string
                    format: date-time
                    description: >-
                      voting_period_end is the timestamp before which voting
                      must be done.

                      Unless a successful MsgExec is called before (to execute a
                      proposal whose

                      tally is successful before the voting period ends),
                      tallying will be done

                      at this point, and the `final_tally_result`and `status`
                      fields will be

                      accordingly updated.
                  executorResult:
                    description: >-
                      executor_result is the final result of the proposal
                      execution. Initial value is NotRun.
                    type: string
                    enum:
                      - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
                      - PROPOSAL_EXECUTOR_RESULT_NOT_RUN
                      - PROPOSAL_EXECUTOR_RESULT_SUCCESS
                      - PROPOSAL_EXECUTOR_RESULT_FAILURE
                    default: PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
                  messages:
                    type: array
                    items:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    description: >-
                      messages is a list of `sdk.Msg`s that will be executed if
                      the proposal passes.
                  title:
                    type: string
                    title: title is the title of the proposal
                  summary:
                    type: string
                    title: summary is a short summary of the proposal
            description: QueryProposalResponse is the Query/Proposal response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id is the unique ID of a proposal.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/group/v1/proposals/{proposalId}/tally:
    get:
      summary: >-
        TallyResult returns the tally result of a proposal. If the proposal is

        still in voting period, then this query computes the current tally
        state,

        which might not be final. On the other hand, if the proposal is final,

        then it simply returns the `final_tally_result` state stored in the

        proposal itself.
      operationId: GroupTallyResult
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              tally:
                description: tally defines the requested tally.
                type: object
                properties:
                  yesCount:
                    type: string
                    description: yes_count is the weighted sum of yes votes.
                  abstainCount:
                    type: string
                    description: abstain_count is the weighted sum of abstainers.
                  noCount:
                    type: string
                    description: no_count is the weighted sum of no votes.
                  noWithVetoCount:
                    type: string
                    description: no_with_veto_count is the weighted sum of veto.
            description: QueryTallyResultResponse is the Query/TallyResult response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id is the unique id of a proposal.
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Cosmos SDK
  /cosmos/group/v1/proposals_by_group_policy/{address}:
    get:
      summary: >-
        ProposalsByGroupPolicy queries proposals based on account address of
        group policy.
      operationId: ProposalsByGroupPolicy
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              proposals:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                      description: id is the unique id of the proposal.
                    groupPolicyAddress:
                      type: string
                      description: >-
                        group_policy_address is the account address of group
                        policy.
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the
                        proposal.

                        the recommended format of the metadata is to be found
                        here:

                        https://docs.cosmos.network/v0.47/modules/group#proposal-4
                    proposers:
                      type: array
                      items:
                        type: string
                      description: proposers are the account addresses of the proposers.
                    submitTime:
                      type: string
                      format: date-time
                      description: >-
                        submit_time is a timestamp specifying when a proposal
                        was submitted.
                    groupVersion:
                      type: string
                      format: uint64
                      description: >-
                        group_version tracks the version of the group at
                        proposal submission.

                        This field is here for informational purposes only.
                    groupPolicyVersion:
                      type: string
                      format: uint64
                      description: >-
                        group_policy_version tracks the version of the group
                        policy at proposal submission.

                        When a decision policy is changed, existing proposals
                        from previous policy

                        versions will become invalid with the `ABORTED` status.

                        This field is here for informational purposes only.
                    status:
                      description: >-
                        status represents the high level position in the life
                        cycle of the proposal. Initial value is Submitted.
                      type: string
                      enum:
                        - PROPOSAL_STATUS_UNSPECIFIED
                        - PROPOSAL_STATUS_SUBMITTED
                        - PROPOSAL_STATUS_ACCEPTED
                        - PROPOSAL_STATUS_REJECTED
                        - PROPOSAL_STATUS_ABORTED
                        - PROPOSAL_STATUS_WITHDRAWN
                      default: PROPOSAL_STATUS_UNSPECIFIED
                    finalTallyResult:
                      description: >-
                        final_tally_result contains the sums of all weighted
                        votes for this

                        proposal for each vote option. It is empty at
                        submission, and only

                        populated after tallying, at voting period end or at
                        proposal execution,

                        whichever happens first.
                      type: object
                      properties:
                        yesCount:
                          type: string
                          description: yes_count is the weighted sum of yes votes.
                        abstainCount:
                          type: string
                          description: abstain_count is the weighted sum of abstainers.
                        noCount:
                          type: string
                          description: no_count is the weighted sum of no votes.
                        noWithVetoCount:
                          type: string
                          description: no_with_veto_count is the weighted sum of veto.
                    votingPeriodEnd:
                      type: string
                      format: date-time
                      description: >-
                        voting_period_end is the timestamp before which voting
                        must be done.

                        Unless a successful MsgExec is called before (to execute
                        a proposal whose

                        tally is successful before the voting period ends),
                        tallying will be done

                        at this point, and the `final_tally_result`and `status`
                        fields will be

                        accordingly updated.
                    executorResult:
                      description: >-
                        executor_result is the final result of the proposal
                        execution. Initial value is NotRun.
                      type: string
                      enum:
                        - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
                        - PROPOSAL_EXECUTOR_RESULT_NOT_RUN
                        - PROPOSAL_EXECUTOR_RESULT_SUCCESS
                        - PROPOSAL_EXECUTOR_RESULT_FAILURE
                      default: PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
                    messages:
                      type: array
                      items:
                        type: object
                        properties:
                          '@type':
                            type: string
                            description: >-
                              A URL/resource name that uniquely identifies the
                              type of the serialized

                              protocol buffer message. This string must contain
                              at least

                              one "/" character. The last segment of the URL's
                              path must represent

                              the fully qualified name of the type (as in

                              `path/google.protobuf.Duration`). The name should
                              be in a canonical form

                              (e.g., leading "." is not accepted).


                              In practice, teams usually precompile into the
                              binary all types that they

                              expect it to use in the context of Any. However,
                              for URLs which use the

                              scheme `http`, `https`, or no scheme, one can
                              optionally set up a type

                              server that maps type URLs to message definitions
                              as follows:


                              * If no scheme is provided, `https` is assumed.

                              * An HTTP GET on the URL must yield a
                              [google.protobuf.Type][]
                                value in binary format, or produce an error.
                              * Applications are allowed to cache lookup results
                              based on the
                                URL, or have them precompiled into a binary to avoid any
                                lookup. Therefore, binary compatibility needs to be preserved
                                on changes to types. (Use versioned type names to manage
                                breaking changes.)

                              Note: this functionality is not currently
                              available in the official

                              protobuf release, and it is not used for type URLs
                              beginning with

                              type.googleapis.com. As of May 2023, there are no
                              widely used type server

                              implementations and no plans to implement one.


                              Schemes other than `http`, `https` (or the empty
                              scheme) might be

                              used with implementation specific semantics.
                        additionalProperties: {}
                        description: >-
                          `Any` contains an arbitrary serialized protocol buffer
                          message along with a

                          URL that describes the type of the serialized message.


                          Protobuf library provides support to pack/unpack Any
                          values in the form

                          of utility functions or additional generated methods
                          of the Any type.


                          Example 1: Pack and unpack a message in C++.

                              Foo foo = ...;
                              Any any;
                              any.PackFrom(foo);
                              ...
                              if (any.UnpackTo(&foo)) {
                                ...
                              }

                          Example 2: Pack and unpack a message in Java.

                              Foo foo = ...;
                              Any any = Any.pack(foo);
                              ...
                              if (any.is(Foo.class)) {
                                foo = any.unpack(Foo.class);
                              }
                              // or ...
                              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                                foo = any.unpack(Foo.getDefaultInstance());
                              }

                           Example 3: Pack and unpack a message in Python.

                              foo = Foo(...)
                              any = Any()
                              any.Pack(foo)
                              ...
                              if any.Is(Foo.DESCRIPTOR):
                                any.Unpack(foo)
                                ...

                           Example 4: Pack and unpack a message in Go

                               foo := &pb.Foo{...}
                               any, err := anypb.New(foo)
                               if err != nil {
                                 ...
                               }
                               ...
                               foo := &pb.Foo{}
                               if err := any.UnmarshalTo(foo); err != nil {
                                 ...
                               }

                          The pack methods provided by protobuf library will by
                          default use

                          'type.googleapis.com/full.type.name' as the type URL
                          and the unpack

                          methods only use the fully qualified type name after
                          the last '/'

                          in the type URL, for example "foo.bar.com/x/y.z" will
                          yield type

                          name "y.z".


                          JSON

                          ====

                          The JSON representation of an `Any` value uses the
                          regular

                          representation of the deserialized, embedded message,
                          with an

                          additional field `@type` which contains the type URL.
                          Example:

                              package google.profile;
                              message Person {
                                string first_name = 1;
                                string last_name = 2;
                              }

                              {
                                "@type": "type.googleapis.com/google.profile.Person",
                                "firstName": <string>,
                                "lastName": <string>
                              }

                          If the embedded message type is well-known and has a
                          custom JSON

                          representation, that representation will be embedded
                          adding a field

                          `value` which holds the custom JSON in addition to the
                          `@type`

                          field. Example (for message
                          [google.protobuf.Duration][]):

                              {
                                "@type": "type.googleapis.com/google.protobuf.Duration",
                                "value": "1.212s"
                              }
                      description: >-
                        messages is a list of `sdk.Msg`s that will be executed
                        if the proposal passes.
                    title:
                      type: string
                      title: title is the title of the proposal
                    summary:
                      type: string
                      title: summary is a short summary of the proposal
                  description: >-
                    Proposal defines a group proposal. Any member of a group can
                    submit a proposal

                    for a group policy to decide upon.

                    A proposal consists of a set of `sdk.Msg`s that will be
                    executed if the proposal

                    passes as well as some optional metadata associated with the
                    proposal.
                description: proposals are the proposals with given group policy.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryProposalsByGroupPolicyResponse is the
              Query/ProposalByGroupPolicy response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: address
          description: >-
            address is the account address of the group policy related to
            proposals.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/vote_by_proposal_voter/{proposalId}/{voter}:
    get:
      summary: VoteByProposalVoter queries a vote by proposal id and voter.
      operationId: VoteByProposalVoter
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              vote:
                description: vote is the vote with given proposal_id and voter.
                type: object
                properties:
                  proposalId:
                    type: string
                    format: uint64
                    description: proposal is the unique ID of the proposal.
                  voter:
                    type: string
                    description: voter is the account address of the voter.
                  option:
                    description: option is the voter's choice on the proposal.
                    type: string
                    enum:
                      - VOTE_OPTION_UNSPECIFIED
                      - VOTE_OPTION_YES
                      - VOTE_OPTION_ABSTAIN
                      - VOTE_OPTION_NO
                      - VOTE_OPTION_NO_WITH_VETO
                    default: VOTE_OPTION_UNSPECIFIED
                  metadata:
                    type: string
                    title: >-
                      metadata is any arbitrary metadata attached to the vote.

                      the recommended format of the metadata is to be found
                      here:
                      https://docs.cosmos.network/v0.47/modules/group#vote-2
                  submitTime:
                    type: string
                    format: date-time
                    description: submit_time is the timestamp when the vote was submitted.
                title: Vote represents a vote for a proposal.string metadata
            description: >-
              QueryVoteByProposalVoterResponse is the Query/VoteByProposalVoter
              response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id is the unique ID of a proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: voter
          description: voter is a proposal voter account address.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/group/v1/votes_by_proposal/{proposalId}:
    get:
      summary: VotesByProposal queries a vote by proposal id.
      operationId: VotesByProposal
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              votes:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal is the unique ID of the proposal.
                    voter:
                      type: string
                      description: voter is the account address of the voter.
                    option:
                      description: option is the voter's choice on the proposal.
                      type: string
                      enum:
                        - VOTE_OPTION_UNSPECIFIED
                        - VOTE_OPTION_YES
                        - VOTE_OPTION_ABSTAIN
                        - VOTE_OPTION_NO
                        - VOTE_OPTION_NO_WITH_VETO
                      default: VOTE_OPTION_UNSPECIFIED
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the vote.

                        the recommended format of the metadata is to be found
                        here:
                        https://docs.cosmos.network/v0.47/modules/group#vote-2
                    submitTime:
                      type: string
                      format: date-time
                      description: >-
                        submit_time is the timestamp when the vote was
                        submitted.
                  title: Vote represents a vote for a proposal.string metadata
                description: votes are the list of votes for given proposal_id.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryVotesByProposalResponse is the Query/VotesByProposal response
              type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: proposalId
          description: proposal_id is the unique ID of a proposal.
          in: path
          required: true
          type: string
          format: uint64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/group/v1/votes_by_voter/{voter}:
    get:
      summary: VotesByVoter queries a vote by voter.
      operationId: VotesByVoter
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              votes:
                type: array
                items:
                  type: object
                  properties:
                    proposalId:
                      type: string
                      format: uint64
                      description: proposal is the unique ID of the proposal.
                    voter:
                      type: string
                      description: voter is the account address of the voter.
                    option:
                      description: option is the voter's choice on the proposal.
                      type: string
                      enum:
                        - VOTE_OPTION_UNSPECIFIED
                        - VOTE_OPTION_YES
                        - VOTE_OPTION_ABSTAIN
                        - VOTE_OPTION_NO
                        - VOTE_OPTION_NO_WITH_VETO
                      default: VOTE_OPTION_UNSPECIFIED
                    metadata:
                      type: string
                      title: >-
                        metadata is any arbitrary metadata attached to the vote.

                        the recommended format of the metadata is to be found
                        here:
                        https://docs.cosmos.network/v0.47/modules/group#vote-2
                    submitTime:
                      type: string
                      format: date-time
                      description: >-
                        submit_time is the timestamp when the vote was
                        submitted.
                  title: Vote represents a vote for a proposal.string metadata
                description: votes are the list of votes by given voter.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: QueryVotesByVoterResponse is the Query/VotesByVoter response type.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: voter
          description: voter is a proposal voter account address.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/mint/v1beta1/annual_provisions:
    get:
      summary: AnnualProvisions current minting annual provisions value.
      operationId: AnnualProvisions
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              annualProvisions:
                type: string
                format: byte
                description: >-
                  annual_provisions is the current minting annual provisions
                  value.
            description: |-
              QueryAnnualProvisionsResponse is the response type for the
              Query/AnnualProvisions RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/mint/v1beta1/inflation:
    get:
      summary: Inflation returns the current minting inflation value.
      operationId: Inflation
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              inflation:
                type: string
                format: byte
                description: inflation is the current minting inflation value.
            description: >-
              QueryInflationResponse is the response type for the
              Query/Inflation RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/mint/v1beta1/params:
    get:
      summary: Params returns the total set of minting parameters.
      operationId: MintParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params defines the parameters of the module.
                type: object
                properties:
                  mintDenom:
                    type: string
                    title: type of coin to mint
                  inflationRateChange:
                    type: string
                    title: maximum annual change in inflation rate
                  inflationMax:
                    type: string
                    title: maximum inflation rate
                  inflationMin:
                    type: string
                    title: minimum inflation rate
                  goalBonded:
                    type: string
                    title: goal of percent bonded atoms
                  blocksPerYear:
                    type: string
                    format: uint64
                    title: expected blocks per year
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/balance/{owner}/{classId}:
    get:
      summary: >-
        Balance queries the number of NFTs of a given class owned by the owner,
        same as balanceOf in ERC721
      operationId: NftBalance
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              amount:
                type: string
                format: uint64
                title: >-
                  amount is the number of all NFTs of a given class owned by the
                  owner
            title: >-
              QueryBalanceResponse is the response type for the Query/Balance
              RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: owner
          description: owner is the owner address of the nft
          in: path
          required: true
          type: string
        - name: classId
          description: class_id associated with the nft
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/classes:
    get:
      summary: Classes queries all NFT classes
      operationId: Classes
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              classes:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      title: >-
                        id defines the unique identifier of the NFT
                        classification, similar to the contract address of
                        ERC721
                    name:
                      type: string
                      title: >-
                        name defines the human-readable name of the NFT
                        classification. Optional
                    symbol:
                      type: string
                      title: >-
                        symbol is an abbreviated name for nft classification.
                        Optional
                    description:
                      type: string
                      title: >-
                        description is a brief description of nft
                        classification. Optional
                    uri:
                      type: string
                      title: >-
                        uri for the class metadata stored off chain. It can
                        define schema for Class and NFT `Data` attributes.
                        Optional
                    uriHash:
                      type: string
                      title: >-
                        uri_hash is a hash of the document pointed by uri.
                        Optional
                    data:
                      title: >-
                        data is the app specific metadata of the NFT class.
                        Optional
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                  description: Class defines the class of the nft type.
                description: class defines the class of the nft type.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: >-
              QueryClassesResponse is the response type for the Query/Classes
              RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/classes/{classId}:
    get:
      summary: Class queries an NFT class based on its id
      operationId: Class
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              class:
                description: class defines the class of the nft type.
                type: object
                properties:
                  id:
                    type: string
                    title: >-
                      id defines the unique identifier of the NFT
                      classification, similar to the contract address of ERC721
                  name:
                    type: string
                    title: >-
                      name defines the human-readable name of the NFT
                      classification. Optional
                  symbol:
                    type: string
                    title: >-
                      symbol is an abbreviated name for nft classification.
                      Optional
                  description:
                    type: string
                    title: >-
                      description is a brief description of nft classification.
                      Optional
                  uri:
                    type: string
                    title: >-
                      uri for the class metadata stored off chain. It can define
                      schema for Class and NFT `Data` attributes. Optional
                  uriHash:
                    type: string
                    title: >-
                      uri_hash is a hash of the document pointed by uri.
                      Optional
                  data:
                    title: >-
                      data is the app specific metadata of the NFT class.
                      Optional
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                    description: >-
                      `Any` contains an arbitrary serialized protocol buffer
                      message along with a

                      URL that describes the type of the serialized message.


                      Protobuf library provides support to pack/unpack Any
                      values in the form

                      of utility functions or additional generated methods of
                      the Any type.


                      Example 1: Pack and unpack a message in C++.

                          Foo foo = ...;
                          Any any;
                          any.PackFrom(foo);
                          ...
                          if (any.UnpackTo(&foo)) {
                            ...
                          }

                      Example 2: Pack and unpack a message in Java.

                          Foo foo = ...;
                          Any any = Any.pack(foo);
                          ...
                          if (any.is(Foo.class)) {
                            foo = any.unpack(Foo.class);
                          }
                          // or ...
                          if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                            foo = any.unpack(Foo.getDefaultInstance());
                          }

                       Example 3: Pack and unpack a message in Python.

                          foo = Foo(...)
                          any = Any()
                          any.Pack(foo)
                          ...
                          if any.Is(Foo.DESCRIPTOR):
                            any.Unpack(foo)
                            ...

                       Example 4: Pack and unpack a message in Go

                           foo := &pb.Foo{...}
                           any, err := anypb.New(foo)
                           if err != nil {
                             ...
                           }
                           ...
                           foo := &pb.Foo{}
                           if err := any.UnmarshalTo(foo); err != nil {
                             ...
                           }

                      The pack methods provided by protobuf library will by
                      default use

                      'type.googleapis.com/full.type.name' as the type URL and
                      the unpack

                      methods only use the fully qualified type name after the
                      last '/'

                      in the type URL, for example "foo.bar.com/x/y.z" will
                      yield type

                      name "y.z".


                      JSON

                      ====

                      The JSON representation of an `Any` value uses the regular

                      representation of the deserialized, embedded message, with
                      an

                      additional field `@type` which contains the type URL.
                      Example:

                          package google.profile;
                          message Person {
                            string first_name = 1;
                            string last_name = 2;
                          }

                          {
                            "@type": "type.googleapis.com/google.profile.Person",
                            "firstName": <string>,
                            "lastName": <string>
                          }

                      If the embedded message type is well-known and has a
                      custom JSON

                      representation, that representation will be embedded
                      adding a field

                      `value` which holds the custom JSON in addition to the
                      `@type`

                      field. Example (for message [google.protobuf.Duration][]):

                          {
                            "@type": "type.googleapis.com/google.protobuf.Duration",
                            "value": "1.212s"
                          }
            title: >-
              QueryClassResponse is the response type for the Query/Class RPC
              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: classId
          description: class_id associated with the nft
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/nfts:
    get:
      summary: >-
        NFTs queries all NFTs of a given class or owner,choose at least one of
        the two, similar to tokenByIndex in

        ERC721Enumerable
      operationId: NFTs
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              nfts:
                type: array
                items:
                  type: object
                  properties:
                    classId:
                      type: string
                      title: >-
                        class_id associated with the NFT, similar to the
                        contract address of ERC721
                    id:
                      type: string
                      title: id is a unique identifier of the NFT
                    uri:
                      type: string
                      title: uri for the NFT metadata stored off chain
                    uriHash:
                      type: string
                      title: uri_hash is a hash of the document pointed by uri
                    data:
                      title: data is an app specific data of the NFT. Optional
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                  description: NFT defines the NFT.
                title: NFT defines the NFT
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: >-
              QueryNFTsResponse is the response type for the Query/NFTs RPC
              methods
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: classId
          description: class_id associated with the nft
          in: query
          required: false
          type: string
        - name: owner
          description: owner is the owner address of the nft
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/nfts/{classId}/{id}:
    get:
      summary: NFT queries an NFT based on its class and id.
      operationId: NFT
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              nft:
                title: owner is the owner address of the nft
                type: object
                properties:
                  classId:
                    type: string
                    title: >-
                      class_id associated with the NFT, similar to the contract
                      address of ERC721
                  id:
                    type: string
                    title: id is a unique identifier of the NFT
                  uri:
                    type: string
                    title: uri for the NFT metadata stored off chain
                  uriHash:
                    type: string
                    title: uri_hash is a hash of the document pointed by uri
                  data:
                    title: data is an app specific data of the NFT. Optional
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                    description: >-
                      `Any` contains an arbitrary serialized protocol buffer
                      message along with a

                      URL that describes the type of the serialized message.


                      Protobuf library provides support to pack/unpack Any
                      values in the form

                      of utility functions or additional generated methods of
                      the Any type.


                      Example 1: Pack and unpack a message in C++.

                          Foo foo = ...;
                          Any any;
                          any.PackFrom(foo);
                          ...
                          if (any.UnpackTo(&foo)) {
                            ...
                          }

                      Example 2: Pack and unpack a message in Java.

                          Foo foo = ...;
                          Any any = Any.pack(foo);
                          ...
                          if (any.is(Foo.class)) {
                            foo = any.unpack(Foo.class);
                          }
                          // or ...
                          if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                            foo = any.unpack(Foo.getDefaultInstance());
                          }

                       Example 3: Pack and unpack a message in Python.

                          foo = Foo(...)
                          any = Any()
                          any.Pack(foo)
                          ...
                          if any.Is(Foo.DESCRIPTOR):
                            any.Unpack(foo)
                            ...

                       Example 4: Pack and unpack a message in Go

                           foo := &pb.Foo{...}
                           any, err := anypb.New(foo)
                           if err != nil {
                             ...
                           }
                           ...
                           foo := &pb.Foo{}
                           if err := any.UnmarshalTo(foo); err != nil {
                             ...
                           }

                      The pack methods provided by protobuf library will by
                      default use

                      'type.googleapis.com/full.type.name' as the type URL and
                      the unpack

                      methods only use the fully qualified type name after the
                      last '/'

                      in the type URL, for example "foo.bar.com/x/y.z" will
                      yield type

                      name "y.z".


                      JSON

                      ====

                      The JSON representation of an `Any` value uses the regular

                      representation of the deserialized, embedded message, with
                      an

                      additional field `@type` which contains the type URL.
                      Example:

                          package google.profile;
                          message Person {
                            string first_name = 1;
                            string last_name = 2;
                          }

                          {
                            "@type": "type.googleapis.com/google.profile.Person",
                            "firstName": <string>,
                            "lastName": <string>
                          }

                      If the embedded message type is well-known and has a
                      custom JSON

                      representation, that representation will be embedded
                      adding a field

                      `value` which holds the custom JSON in addition to the
                      `@type`

                      field. Example (for message [google.protobuf.Duration][]):

                          {
                            "@type": "type.googleapis.com/google.protobuf.Duration",
                            "value": "1.212s"
                          }
                description: NFT defines the NFT.
            title: QueryNFTResponse is the response type for the Query/NFT RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: classId
          description: class_id associated with the nft
          in: path
          required: true
          type: string
        - name: id
          description: id is a unique identifier of the NFT
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/owner/{classId}/{id}:
    get:
      summary: >-
        Owner queries the owner of the NFT based on its class and id, same as
        ownerOf in ERC721
      operationId: Owner
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              owner:
                type: string
                title: owner is the owner address of the nft
            title: >-
              QueryOwnerResponse is the response type for the Query/Owner RPC
              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: classId
          description: class_id associated with the nft
          in: path
          required: true
          type: string
        - name: id
          description: id is a unique identifier of the NFT
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/nft/v1beta1/supply/{classId}:
    get:
      summary: >-
        Supply queries the number of NFTs from the given class, same as
        totalSupply of ERC721.
      operationId: NftSupply
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              amount:
                type: string
                format: uint64
                title: amount is the number of all NFTs from the given class
            title: >-
              QuerySupplyResponse is the response type for the Query/Supply RPC
              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: classId
          description: class_id associated with the nft
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/protocolpool/v1/community_pool:
    get:
      summary: CommunityPool queries the community pool coins.
      operationId: ProtocolpoolCommunityPool
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              pool:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: pool defines community pool's coins.
            description: >-
              QueryCommunityPoolResponse is the response type for the
              Query/CommunityPool

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/protocolpool/v1/continuous_funds:
    get:
      summary: ContinuousFunds queries all continuous funds in the store.
      operationId: ContinuousFunds
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              continuousFunds:
                type: array
                items:
                  type: object
                  properties:
                    recipient:
                      type: string
                      description: >-
                        Recipient is the address string of the account receiving
                        funds.
                    percentage:
                      type: string
                      description: >-
                        Percentage is the percentage of funds to be allocated
                        from Community pool.
                    expiry:
                      type: string
                      format: date-time
                      description: >-
                        Optional, if expiry is set, removes the state object
                        when expired.
                  description: >-
                    ContinuousFund defines the fields of continuous fund
                    proposal.
                description: ContinuousFunds defines all continuous funds in state.
            description: >-
              QueryUnclaimedBudgetResponse is the response type for the
              Query/ContinuousFunds

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/protocolpool/v1/continuous_funds/{recipient}:
    get:
      summary: >-
        ContinuousFund queries a continuous fund by the recipient is is
        associated with.
      operationId: ContinuousFund
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              continuousFund:
                description: >-
                  ContinuousFunds is the given continuous fund returned in the
                  query.
                type: object
                properties:
                  recipient:
                    type: string
                    description: >-
                      Recipient is the address string of the account receiving
                      funds.
                  percentage:
                    type: string
                    description: >-
                      Percentage is the percentage of funds to be allocated from
                      Community pool.
                  expiry:
                    type: string
                    format: date-time
                    description: >-
                      Optional, if expiry is set, removes the state object when
                      expired.
            description: >-
              QueryUnclaimedBudgetResponse is the response type for the
              Query/ContinuousFund

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: recipient
          description: >-
            recipient is the recipient address to query unclaimed budget amount
            for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/protocolpool/v1/params:
    get:
      summary: Params returns the total set of x/protocolpool parameters.
      operationId: ProtocolpoolParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                type: object
                properties:
                  enabledDistributionDenoms:
                    type: array
                    items:
                      type: string
                    description: >-
                      EnabledDistributionDenoms lists the denoms that are
                      allowed to be distributed.

                      This is to avoid spending time distributing undesired
                      tokens to continuous funds and budgets.
                  distributionFrequency:
                    type: string
                    format: uint64
                    description: >-
                      DistributionFrequency is the frequency (in terms of
                      blocks) that funds are distributed out from the

                      x/protocolpool module.
                description: Params defines the parameters for the protocolpool module.
            description: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/slashing/v1beta1/params:
    get:
      summary: Params queries the parameters of slashing module
      operationId: SlashingParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                type: object
                properties:
                  signedBlocksWindow:
                    type: string
                    format: int64
                  minSignedPerWindow:
                    type: string
                    format: byte
                  downtimeJailDuration:
                    type: string
                  slashFractionDoubleSign:
                    type: string
                    format: byte
                  slashFractionDowntime:
                    type: string
                    format: byte
                description: >-
                  Params represents the parameters used for by the slashing
                  module.
            title: >-
              QueryParamsResponse is the response type for the Query/Params RPC
              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      tags:
        - Cosmos SDK
  /cosmos/slashing/v1beta1/signing_infos:
    get:
      summary: SigningInfos queries signing info of all validators
      operationId: SigningInfos
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              info:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                    startHeight:
                      type: string
                      format: int64
                      title: >-
                        Height at which validator was first a candidate OR was
                        un-jailed
                    indexOffset:
                      type: string
                      format: int64
                      description: >-
                        Index which is incremented every time a validator is
                        bonded in a block and

                        _may_ have signed a pre-commit or not. This in
                        conjunction with the

                        signed_blocks_window param determines the index in the
                        missed block bitmap.
                    jailedUntil:
                      type: string
                      format: date-time
                      description: >-
                        Timestamp until which the validator is jailed due to
                        liveness downtime.
                    tombstoned:
                      type: boolean
                      description: >-
                        Whether or not a validator has been tombstoned (killed
                        out of validator

                        set). It is set once the validator commits an
                        equivocation or for any other

                        configured misbehavior.
                    missedBlocksCounter:
                      type: string
                      format: int64
                      description: >-
                        A counter of missed (unsigned) blocks. It is used to
                        avoid unnecessary

                        reads in the missed block bitmap.
                  description: >-
                    ValidatorSigningInfo defines a validator's signing info for
                    monitoring their

                    liveness activity.
                title: info is the signing info of all validators
              pagination:
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
            title: >-
              QuerySigningInfosResponse is the response type for the
              Query/SigningInfos RPC

              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/slashing/v1beta1/signing_infos/{consAddress}:
    get:
      summary: SigningInfo queries the signing info of given cons address
      operationId: SigningInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              valSigningInfo:
                title: >-
                  val_signing_info is the signing info of requested val cons
                  address
                type: object
                properties:
                  address:
                    type: string
                  startHeight:
                    type: string
                    format: int64
                    title: >-
                      Height at which validator was first a candidate OR was
                      un-jailed
                  indexOffset:
                    type: string
                    format: int64
                    description: >-
                      Index which is incremented every time a validator is
                      bonded in a block and

                      _may_ have signed a pre-commit or not. This in conjunction
                      with the

                      signed_blocks_window param determines the index in the
                      missed block bitmap.
                  jailedUntil:
                    type: string
                    format: date-time
                    description: >-
                      Timestamp until which the validator is jailed due to
                      liveness downtime.
                  tombstoned:
                    type: boolean
                    description: >-
                      Whether or not a validator has been tombstoned (killed out
                      of validator

                      set). It is set once the validator commits an equivocation
                      or for any other

                      configured misbehavior.
                  missedBlocksCounter:
                    type: string
                    format: int64
                    description: >-
                      A counter of missed (unsigned) blocks. It is used to avoid
                      unnecessary

                      reads in the missed block bitmap.
                description: >-
                  ValidatorSigningInfo defines a validator's signing info for
                  monitoring their

                  liveness activity.
            title: >-
              QuerySigningInfoResponse is the response type for the
              Query/SigningInfo RPC

              method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: consAddress
          description: cons_address is the address to query signing info of
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/delegations/{delegatorAddr}:
    get:
      summary: >-
        DelegatorDelegations queries all delegations of a given delegator
        address.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: DelegatorDelegations
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              delegationResponses:
                type: array
                items:
                  type: object
                  properties:
                    delegation:
                      type: object
                      properties:
                        delegatorAddress:
                          type: string
                          description: >-
                            delegator_address is the encoded address of the
                            delegator.
                        validatorAddress:
                          type: string
                          description: >-
                            validator_address is the encoded address of the
                            validator.
                        shares:
                          type: string
                          description: shares define the delegation shares received.
                      description: >-
                        Delegation represents the bond with tokens held by an
                        account. It is

                        owned by one delegator, and is associated with the
                        voting power of one

                        validator.
                    balance:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                  description: >-
                    DelegationResponse is equivalent to Delegation except that
                    it contains a

                    balance in addition to shares which is more suitable for
                    client responses.
                description: >-
                  delegation_responses defines all the delegations' info of a
                  delegator.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: |-
              QueryDelegatorDelegationsResponse is response type for the
              Query/DelegatorDelegations RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/delegators/{delegatorAddr}/redelegations:
    get:
      summary: Redelegations queries redelegations of given address.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: Redelegations
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              redelegationResponses:
                type: array
                items:
                  type: object
                  properties:
                    redelegation:
                      type: object
                      properties:
                        delegatorAddress:
                          type: string
                          description: >-
                            delegator_address is the bech32-encoded address of
                            the delegator.
                        validatorSrcAddress:
                          type: string
                          description: >-
                            validator_src_address is the validator redelegation
                            source operator address.
                        validatorDstAddress:
                          type: string
                          description: >-
                            validator_dst_address is the validator redelegation
                            destination operator address.
                        entries:
                          type: array
                          items:
                            type: object
                            properties:
                              creationHeight:
                                type: string
                                format: int64
                                description: >-
                                  creation_height  defines the height which the
                                  redelegation took place.
                              completionTime:
                                type: string
                                format: date-time
                                description: >-
                                  completion_time defines the unix time for
                                  redelegation completion.
                              initialBalance:
                                type: string
                                description: >-
                                  initial_balance defines the initial balance
                                  when redelegation started.
                              sharesDst:
                                type: string
                                description: >-
                                  shares_dst is the amount of
                                  destination-validator shares created by
                                  redelegation.
                              unbondingId:
                                type: string
                                format: uint64
                                title: >-
                                  Incrementing id that uniquely identifies this
                                  entry
                              unbondingOnHoldRefCount:
                                type: string
                                format: int64
                                title: >-
                                  Strictly positive if this entry's unbonding
                                  has been stopped by external modules
                            description: >-
                              RedelegationEntry defines a redelegation object
                              with relevant metadata.
                          description: |-
                            entries are the redelegation entries.

                            redelegation entries
                      description: >-
                        Redelegation contains the list of a particular
                        delegator's redelegating bonds

                        from a particular source validator to a particular
                        destination validator.
                    entries:
                      type: array
                      items:
                        type: object
                        properties:
                          redelegationEntry:
                            type: object
                            properties:
                              creationHeight:
                                type: string
                                format: int64
                                description: >-
                                  creation_height  defines the height which the
                                  redelegation took place.
                              completionTime:
                                type: string
                                format: date-time
                                description: >-
                                  completion_time defines the unix time for
                                  redelegation completion.
                              initialBalance:
                                type: string
                                description: >-
                                  initial_balance defines the initial balance
                                  when redelegation started.
                              sharesDst:
                                type: string
                                description: >-
                                  shares_dst is the amount of
                                  destination-validator shares created by
                                  redelegation.
                              unbondingId:
                                type: string
                                format: uint64
                                title: >-
                                  Incrementing id that uniquely identifies this
                                  entry
                              unbondingOnHoldRefCount:
                                type: string
                                format: int64
                                title: >-
                                  Strictly positive if this entry's unbonding
                                  has been stopped by external modules
                            description: >-
                              RedelegationEntry defines a redelegation object
                              with relevant metadata.
                          balance:
                            type: string
                        description: >-
                          RedelegationEntryResponse is equivalent to a
                          RedelegationEntry except that it

                          contains a balance in addition to shares which is more
                          suitable for client

                          responses.
                  description: >-
                    RedelegationResponse is equivalent to a Redelegation except
                    that its entries

                    contain a balance in addition to shares which is more
                    suitable for client

                    responses.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryRedelegationsResponse is response type for the
              Query/Redelegations RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
        - name: srcValidatorAddr
          description: src_validator_addr defines the validator address to redelegate from.
          in: query
          required: false
          type: string
        - name: dstValidatorAddr
          description: dst_validator_addr defines the validator address to redelegate to.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/delegators/{delegatorAddr}/unbonding_delegations:
    get:
      summary: >-
        DelegatorUnbondingDelegations queries all unbonding delegations of a
        given

        delegator address.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: DelegatorUnbondingDelegations
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              unbondingResponses:
                type: array
                items:
                  type: object
                  properties:
                    delegatorAddress:
                      type: string
                      description: >-
                        delegator_address is the encoded address of the
                        delegator.
                    validatorAddress:
                      type: string
                      description: >-
                        validator_address is the encoded address of the
                        validator.
                    entries:
                      type: array
                      items:
                        type: object
                        properties:
                          creationHeight:
                            type: string
                            format: int64
                            description: >-
                              creation_height is the height which the unbonding
                              took place.
                          completionTime:
                            type: string
                            format: date-time
                            description: >-
                              completion_time is the unix time for unbonding
                              completion.
                          initialBalance:
                            type: string
                            description: >-
                              initial_balance defines the tokens initially
                              scheduled to receive at completion.
                          balance:
                            type: string
                            description: >-
                              balance defines the tokens to receive at
                              completion.
                          unbondingId:
                            type: string
                            format: uint64
                            title: >-
                              Incrementing id that uniquely identifies this
                              entry
                          unbondingOnHoldRefCount:
                            type: string
                            format: int64
                            title: >-
                              Strictly positive if this entry's unbonding has
                              been stopped by external modules
                        description: >-
                          UnbondingDelegationEntry defines an unbonding object
                          with relevant metadata.
                      description: |-
                        entries are the unbonding delegation entries.

                        unbonding delegation entries
                  description: >-
                    UnbondingDelegation stores all of a single delegator's
                    unbonding bonds

                    for a single validator in an time-ordered list.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryUnbondingDelegatorDelegationsResponse is response type for
              the

              Query/UnbondingDelegatorDelegations RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/delegators/{delegatorAddr}/validators:
    get:
      summary: |-
        DelegatorValidators queries all validators info for given delegator
        address.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: StakingDelegatorValidators
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              validators:
                type: array
                items:
                  type: object
                  properties:
                    operatorAddress:
                      type: string
                      description: >-
                        operator_address defines the address of the validator's
                        operator; bech encoded in JSON.
                    consensusPubkey:
                      description: >-
                        consensus_pubkey is the consensus public key of the
                        validator, as a Protobuf Any.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                    jailed:
                      type: boolean
                      description: >-
                        jailed defined whether the validator has been jailed
                        from bonded status or not.
                    status:
                      description: >-
                        status is the validator status
                        (bonded/unbonding/unbonded).
                      type: string
                      enum:
                        - BOND_STATUS_UNSPECIFIED
                        - BOND_STATUS_UNBONDED
                        - BOND_STATUS_UNBONDING
                        - BOND_STATUS_BONDED
                      default: BOND_STATUS_UNSPECIFIED
                    tokens:
                      type: string
                      description: >-
                        tokens define the delegated tokens (incl.
                        self-delegation).
                    delegatorShares:
                      type: string
                      description: >-
                        delegator_shares defines total shares issued to a
                        validator's delegators.
                    description:
                      description: >-
                        description defines the description terms for the
                        validator.
                      type: object
                      properties:
                        moniker:
                          type: string
                          description: >-
                            moniker defines a human-readable name for the
                            validator.
                        identity:
                          type: string
                          description: >-
                            identity defines an optional identity signature (ex.
                            UPort or Keybase).
                        website:
                          type: string
                          description: website defines an optional website link.
                        securityContact:
                          type: string
                          description: >-
                            security_contact defines an optional email for
                            security contact.
                        details:
                          type: string
                          description: details define other optional details.
                    unbondingHeight:
                      type: string
                      format: int64
                      description: >-
                        unbonding_height defines, if unbonding, the height at
                        which this validator has begun unbonding.
                    unbondingTime:
                      type: string
                      format: date-time
                      description: >-
                        unbonding_time defines, if unbonding, the min time for
                        the validator to complete unbonding.
                    commission:
                      description: commission defines the commission parameters.
                      type: object
                      properties:
                        commissionRates:
                          description: >-
                            commission_rates defines the initial commission
                            rates to be used for creating a validator.
                          type: object
                          properties:
                            rate:
                              type: string
                              description: >-
                                rate is the commission rate charged to
                                delegators, as a fraction.
                            maxRate:
                              type: string
                              description: >-
                                max_rate defines the maximum commission rate
                                which validator can ever charge, as a fraction.
                            maxChangeRate:
                              type: string
                              description: >-
                                max_change_rate defines the maximum daily
                                increase of the validator commission, as a
                                fraction.
                        updateTime:
                          type: string
                          format: date-time
                          description: >-
                            update_time is the last time the commission rate was
                            changed.
                    minSelfDelegation:
                      type: string
                      description: >-
                        min_self_delegation is the validator's self declared
                        minimum self delegation.
                    unbondingOnHoldRefCount:
                      type: string
                      format: int64
                      title: >-
                        strictly positive if this validator's unbonding has been
                        stopped by external modules
                    unbondingIds:
                      type: array
                      items:
                        type: string
                        format: uint64
                      title: >-
                        list of unbonding ids, each uniquely identifing an
                        unbonding of this validator
                  description: >-
                    Validator defines a validator, together with the total
                    amount of the

                    Validator's bond shares and their exchange rate to coins.
                    Slashing results in

                    a decrease in the exchange rate, allowing correct
                    calculation of future

                    undelegations without iterating over delegators. When coins
                    are delegated to

                    this validator, the validator is credited with a delegation
                    whose number of

                    bond shares is based on the amount of coins delegated
                    divided by the current

                    exchange rate. Voting power can be calculated as total
                    bonded shares

                    multiplied by exchange rate.
                description: validators defines the validators' info of a delegator.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: |-
              QueryDelegatorValidatorsResponse is response type for the
              Query/DelegatorValidators RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/delegators/{delegatorAddr}/validators/{validatorAddr}:
    get:
      summary: |-
        DelegatorValidator queries validator info for given delegator validator
        pair.
      operationId: DelegatorValidator
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              validator:
                description: validator defines the validator info.
                type: object
                properties:
                  operatorAddress:
                    type: string
                    description: >-
                      operator_address defines the address of the validator's
                      operator; bech encoded in JSON.
                  consensusPubkey:
                    description: >-
                      consensus_pubkey is the consensus public key of the
                      validator, as a Protobuf Any.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                  jailed:
                    type: boolean
                    description: >-
                      jailed defined whether the validator has been jailed from
                      bonded status or not.
                  status:
                    description: >-
                      status is the validator status
                      (bonded/unbonding/unbonded).
                    type: string
                    enum:
                      - BOND_STATUS_UNSPECIFIED
                      - BOND_STATUS_UNBONDED
                      - BOND_STATUS_UNBONDING
                      - BOND_STATUS_BONDED
                    default: BOND_STATUS_UNSPECIFIED
                  tokens:
                    type: string
                    description: >-
                      tokens define the delegated tokens (incl.
                      self-delegation).
                  delegatorShares:
                    type: string
                    description: >-
                      delegator_shares defines total shares issued to a
                      validator's delegators.
                  description:
                    description: >-
                      description defines the description terms for the
                      validator.
                    type: object
                    properties:
                      moniker:
                        type: string
                        description: >-
                          moniker defines a human-readable name for the
                          validator.
                      identity:
                        type: string
                        description: >-
                          identity defines an optional identity signature (ex.
                          UPort or Keybase).
                      website:
                        type: string
                        description: website defines an optional website link.
                      securityContact:
                        type: string
                        description: >-
                          security_contact defines an optional email for
                          security contact.
                      details:
                        type: string
                        description: details define other optional details.
                  unbondingHeight:
                    type: string
                    format: int64
                    description: >-
                      unbonding_height defines, if unbonding, the height at
                      which this validator has begun unbonding.
                  unbondingTime:
                    type: string
                    format: date-time
                    description: >-
                      unbonding_time defines, if unbonding, the min time for the
                      validator to complete unbonding.
                  commission:
                    description: commission defines the commission parameters.
                    type: object
                    properties:
                      commissionRates:
                        description: >-
                          commission_rates defines the initial commission rates
                          to be used for creating a validator.
                        type: object
                        properties:
                          rate:
                            type: string
                            description: >-
                              rate is the commission rate charged to delegators,
                              as a fraction.
                          maxRate:
                            type: string
                            description: >-
                              max_rate defines the maximum commission rate which
                              validator can ever charge, as a fraction.
                          maxChangeRate:
                            type: string
                            description: >-
                              max_change_rate defines the maximum daily increase
                              of the validator commission, as a fraction.
                      updateTime:
                        type: string
                        format: date-time
                        description: >-
                          update_time is the last time the commission rate was
                          changed.
                  minSelfDelegation:
                    type: string
                    description: >-
                      min_self_delegation is the validator's self declared
                      minimum self delegation.
                  unbondingOnHoldRefCount:
                    type: string
                    format: int64
                    title: >-
                      strictly positive if this validator's unbonding has been
                      stopped by external modules
                  unbondingIds:
                    type: array
                    items:
                      type: string
                      format: uint64
                    title: >-
                      list of unbonding ids, each uniquely identifing an
                      unbonding of this validator
            description: |-
              QueryDelegatorValidatorResponse response type for the
              Query/DelegatorValidator RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
        - name: validatorAddr
          description: validator_addr defines the validator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/historical_info/{height}:
    get:
      summary: HistoricalInfo queries the historical info for given height.
      operationId: HistoricalInfo
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              hist:
                description: hist defines the historical info at the given height.
                type: object
                properties:
                  header:
                    type: object
                    properties:
                      version:
                        title: basic block info
                        type: object
                        properties:
                          block:
                            type: string
                            format: uint64
                          app:
                            type: string
                            format: uint64
                        description: >-
                          Consensus captures the consensus rules for processing
                          a block in the blockchain,

                          including all blockchain data structures and the rules
                          of the application's

                          state transition machine.
                      chainId:
                        type: string
                      height:
                        type: string
                        format: int64
                      time:
                        type: string
                        format: date-time
                      lastBlockId:
                        title: prev block info
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                      lastCommitHash:
                        type: string
                        format: byte
                        description: commit from validators from the last block
                        title: hashes of block data
                      dataHash:
                        type: string
                        format: byte
                        title: transactions
                      validatorsHash:
                        type: string
                        format: byte
                        description: validators for the current block
                        title: hashes from the app output from the prev block
                      nextValidatorsHash:
                        type: string
                        format: byte
                        title: validators for the next block
                      consensusHash:
                        type: string
                        format: byte
                        title: consensus params for current block
                      appHash:
                        type: string
                        format: byte
                        title: state after txs from the previous block
                      lastResultsHash:
                        type: string
                        format: byte
                        title: >-
                          root hash of all results from the txs from the
                          previous block
                      evidenceHash:
                        type: string
                        format: byte
                        description: evidence included in the block
                        title: consensus info
                      proposerAddress:
                        type: string
                        format: byte
                        title: original proposer of the block
                    description: Header defines the structure of a block header.
                  valset:
                    type: array
                    items:
                      type: object
                      properties:
                        operatorAddress:
                          type: string
                          description: >-
                            operator_address defines the address of the
                            validator's operator; bech encoded in JSON.
                        consensusPubkey:
                          description: >-
                            consensus_pubkey is the consensus public key of the
                            validator, as a Protobuf Any.
                          type: object
                          properties:
                            '@type':
                              type: string
                              description: >-
                                A URL/resource name that uniquely identifies the
                                type of the serialized

                                protocol buffer message. This string must
                                contain at least

                                one "/" character. The last segment of the URL's
                                path must represent

                                the fully qualified name of the type (as in

                                `path/google.protobuf.Duration`). The name
                                should be in a canonical form

                                (e.g., leading "." is not accepted).


                                In practice, teams usually precompile into the
                                binary all types that they

                                expect it to use in the context of Any. However,
                                for URLs which use the

                                scheme `http`, `https`, or no scheme, one can
                                optionally set up a type

                                server that maps type URLs to message
                                definitions as follows:


                                * If no scheme is provided, `https` is assumed.

                                * An HTTP GET on the URL must yield a
                                [google.protobuf.Type][]
                                  value in binary format, or produce an error.
                                * Applications are allowed to cache lookup
                                results based on the
                                  URL, or have them precompiled into a binary to avoid any
                                  lookup. Therefore, binary compatibility needs to be preserved
                                  on changes to types. (Use versioned type names to manage
                                  breaking changes.)

                                Note: this functionality is not currently
                                available in the official

                                protobuf release, and it is not used for type
                                URLs beginning with

                                type.googleapis.com. As of May 2023, there are
                                no widely used type server

                                implementations and no plans to implement one.


                                Schemes other than `http`, `https` (or the empty
                                scheme) might be

                                used with implementation specific semantics.
                          additionalProperties: {}
                        jailed:
                          type: boolean
                          description: >-
                            jailed defined whether the validator has been jailed
                            from bonded status or not.
                        status:
                          description: >-
                            status is the validator status
                            (bonded/unbonding/unbonded).
                          type: string
                          enum:
                            - BOND_STATUS_UNSPECIFIED
                            - BOND_STATUS_UNBONDED
                            - BOND_STATUS_UNBONDING
                            - BOND_STATUS_BONDED
                          default: BOND_STATUS_UNSPECIFIED
                        tokens:
                          type: string
                          description: >-
                            tokens define the delegated tokens (incl.
                            self-delegation).
                        delegatorShares:
                          type: string
                          description: >-
                            delegator_shares defines total shares issued to a
                            validator's delegators.
                        description:
                          description: >-
                            description defines the description terms for the
                            validator.
                          type: object
                          properties:
                            moniker:
                              type: string
                              description: >-
                                moniker defines a human-readable name for the
                                validator.
                            identity:
                              type: string
                              description: >-
                                identity defines an optional identity signature
                                (ex. UPort or Keybase).
                            website:
                              type: string
                              description: website defines an optional website link.
                            securityContact:
                              type: string
                              description: >-
                                security_contact defines an optional email for
                                security contact.
                            details:
                              type: string
                              description: details define other optional details.
                        unbondingHeight:
                          type: string
                          format: int64
                          description: >-
                            unbonding_height defines, if unbonding, the height
                            at which this validator has begun unbonding.
                        unbondingTime:
                          type: string
                          format: date-time
                          description: >-
                            unbonding_time defines, if unbonding, the min time
                            for the validator to complete unbonding.
                        commission:
                          description: commission defines the commission parameters.
                          type: object
                          properties:
                            commissionRates:
                              description: >-
                                commission_rates defines the initial commission
                                rates to be used for creating a validator.
                              type: object
                              properties:
                                rate:
                                  type: string
                                  description: >-
                                    rate is the commission rate charged to
                                    delegators, as a fraction.
                                maxRate:
                                  type: string
                                  description: >-
                                    max_rate defines the maximum commission rate
                                    which validator can ever charge, as a
                                    fraction.
                                maxChangeRate:
                                  type: string
                                  description: >-
                                    max_change_rate defines the maximum daily
                                    increase of the validator commission, as a
                                    fraction.
                            updateTime:
                              type: string
                              format: date-time
                              description: >-
                                update_time is the last time the commission rate
                                was changed.
                        minSelfDelegation:
                          type: string
                          description: >-
                            min_self_delegation is the validator's self declared
                            minimum self delegation.
                        unbondingOnHoldRefCount:
                          type: string
                          format: int64
                          title: >-
                            strictly positive if this validator's unbonding has
                            been stopped by external modules
                        unbondingIds:
                          type: array
                          items:
                            type: string
                            format: uint64
                          title: >-
                            list of unbonding ids, each uniquely identifing an
                            unbonding of this validator
                      description: >-
                        Validator defines a validator, together with the total
                        amount of the

                        Validator's bond shares and their exchange rate to
                        coins. Slashing results in

                        a decrease in the exchange rate, allowing correct
                        calculation of future

                        undelegations without iterating over delegators. When
                        coins are delegated to

                        this validator, the validator is credited with a
                        delegation whose number of

                        bond shares is based on the amount of coins delegated
                        divided by the current

                        exchange rate. Voting power can be calculated as total
                        bonded shares

                        multiplied by exchange rate.
            description: >-
              QueryHistoricalInfoResponse is response type for the
              Query/HistoricalInfo RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: height
          description: height defines at which height to query the historical info.
          in: path
          required: true
          type: string
          format: int64
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/params:
    get:
      summary: Parameters queries the staking parameters.
      operationId: StakingParams
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              params:
                description: params holds all the parameters of this module.
                type: object
                properties:
                  unbondingTime:
                    type: string
                    description: unbonding_time is the time duration of unbonding.
                  maxValidators:
                    type: integer
                    format: int64
                    description: max_validators is the maximum number of validators.
                  maxEntries:
                    type: integer
                    format: int64
                    description: >-
                      max_entries is the max entries for either unbonding
                      delegation or redelegation (per pair/trio).
                  historicalEntries:
                    type: integer
                    format: int64
                    description: >-
                      historical_entries is the number of historical entries to
                      persist.
                  bondDenom:
                    type: string
                    description: bond_denom defines the bondable coin denomination.
                  minCommissionRate:
                    type: string
                    title: >-
                      min_commission_rate is the chain-wide minimum commission
                      rate that a validator can charge their delegators
            description: >-
              QueryParamsResponse is response type for the Query/Params RPC
              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/pool:
    get:
      summary: Pool queries the pool info.
      operationId: Pool
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              pool:
                description: pool defines the pool info.
                type: object
                properties:
                  notBondedTokens:
                    type: string
                  bondedTokens:
                    type: string
            description: QueryPoolResponse is response type for the Query/Pool RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/validators:
    get:
      summary: Validators queries all validators that match the given status.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: Validators
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              validators:
                type: array
                items:
                  type: object
                  properties:
                    operatorAddress:
                      type: string
                      description: >-
                        operator_address defines the address of the validator's
                        operator; bech encoded in JSON.
                    consensusPubkey:
                      description: >-
                        consensus_pubkey is the consensus public key of the
                        validator, as a Protobuf Any.
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                    jailed:
                      type: boolean
                      description: >-
                        jailed defined whether the validator has been jailed
                        from bonded status or not.
                    status:
                      description: >-
                        status is the validator status
                        (bonded/unbonding/unbonded).
                      type: string
                      enum:
                        - BOND_STATUS_UNSPECIFIED
                        - BOND_STATUS_UNBONDED
                        - BOND_STATUS_UNBONDING
                        - BOND_STATUS_BONDED
                      default: BOND_STATUS_UNSPECIFIED
                    tokens:
                      type: string
                      description: >-
                        tokens define the delegated tokens (incl.
                        self-delegation).
                    delegatorShares:
                      type: string
                      description: >-
                        delegator_shares defines total shares issued to a
                        validator's delegators.
                    description:
                      description: >-
                        description defines the description terms for the
                        validator.
                      type: object
                      properties:
                        moniker:
                          type: string
                          description: >-
                            moniker defines a human-readable name for the
                            validator.
                        identity:
                          type: string
                          description: >-
                            identity defines an optional identity signature (ex.
                            UPort or Keybase).
                        website:
                          type: string
                          description: website defines an optional website link.
                        securityContact:
                          type: string
                          description: >-
                            security_contact defines an optional email for
                            security contact.
                        details:
                          type: string
                          description: details define other optional details.
                    unbondingHeight:
                      type: string
                      format: int64
                      description: >-
                        unbonding_height defines, if unbonding, the height at
                        which this validator has begun unbonding.
                    unbondingTime:
                      type: string
                      format: date-time
                      description: >-
                        unbonding_time defines, if unbonding, the min time for
                        the validator to complete unbonding.
                    commission:
                      description: commission defines the commission parameters.
                      type: object
                      properties:
                        commissionRates:
                          description: >-
                            commission_rates defines the initial commission
                            rates to be used for creating a validator.
                          type: object
                          properties:
                            rate:
                              type: string
                              description: >-
                                rate is the commission rate charged to
                                delegators, as a fraction.
                            maxRate:
                              type: string
                              description: >-
                                max_rate defines the maximum commission rate
                                which validator can ever charge, as a fraction.
                            maxChangeRate:
                              type: string
                              description: >-
                                max_change_rate defines the maximum daily
                                increase of the validator commission, as a
                                fraction.
                        updateTime:
                          type: string
                          format: date-time
                          description: >-
                            update_time is the last time the commission rate was
                            changed.
                    minSelfDelegation:
                      type: string
                      description: >-
                        min_self_delegation is the validator's self declared
                        minimum self delegation.
                    unbondingOnHoldRefCount:
                      type: string
                      format: int64
                      title: >-
                        strictly positive if this validator's unbonding has been
                        stopped by external modules
                    unbondingIds:
                      type: array
                      items:
                        type: string
                        format: uint64
                      title: >-
                        list of unbonding ids, each uniquely identifing an
                        unbonding of this validator
                  description: >-
                    Validator defines a validator, together with the total
                    amount of the

                    Validator's bond shares and their exchange rate to coins.
                    Slashing results in

                    a decrease in the exchange rate, allowing correct
                    calculation of future

                    undelegations without iterating over delegators. When coins
                    are delegated to

                    this validator, the validator is credited with a delegation
                    whose number of

                    bond shares is based on the amount of coins delegated
                    divided by the current

                    exchange rate. Voting power can be calculated as total
                    bonded shares

                    multiplied by exchange rate.
                description: validators contains all the queried validators.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: >-
              QueryValidatorsResponse is response type for the Query/Validators
              RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: status
          description: status enables to query for validators matching a given status.
          in: query
          required: false
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/validators/{validatorAddr}:
    get:
      summary: Validator queries validator info for given validator address.
      operationId: Validator
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              validator:
                description: validator defines the validator info.
                type: object
                properties:
                  operatorAddress:
                    type: string
                    description: >-
                      operator_address defines the address of the validator's
                      operator; bech encoded in JSON.
                  consensusPubkey:
                    description: >-
                      consensus_pubkey is the consensus public key of the
                      validator, as a Protobuf Any.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                  jailed:
                    type: boolean
                    description: >-
                      jailed defined whether the validator has been jailed from
                      bonded status or not.
                  status:
                    description: >-
                      status is the validator status
                      (bonded/unbonding/unbonded).
                    type: string
                    enum:
                      - BOND_STATUS_UNSPECIFIED
                      - BOND_STATUS_UNBONDED
                      - BOND_STATUS_UNBONDING
                      - BOND_STATUS_BONDED
                    default: BOND_STATUS_UNSPECIFIED
                  tokens:
                    type: string
                    description: >-
                      tokens define the delegated tokens (incl.
                      self-delegation).
                  delegatorShares:
                    type: string
                    description: >-
                      delegator_shares defines total shares issued to a
                      validator's delegators.
                  description:
                    description: >-
                      description defines the description terms for the
                      validator.
                    type: object
                    properties:
                      moniker:
                        type: string
                        description: >-
                          moniker defines a human-readable name for the
                          validator.
                      identity:
                        type: string
                        description: >-
                          identity defines an optional identity signature (ex.
                          UPort or Keybase).
                      website:
                        type: string
                        description: website defines an optional website link.
                      securityContact:
                        type: string
                        description: >-
                          security_contact defines an optional email for
                          security contact.
                      details:
                        type: string
                        description: details define other optional details.
                  unbondingHeight:
                    type: string
                    format: int64
                    description: >-
                      unbonding_height defines, if unbonding, the height at
                      which this validator has begun unbonding.
                  unbondingTime:
                    type: string
                    format: date-time
                    description: >-
                      unbonding_time defines, if unbonding, the min time for the
                      validator to complete unbonding.
                  commission:
                    description: commission defines the commission parameters.
                    type: object
                    properties:
                      commissionRates:
                        description: >-
                          commission_rates defines the initial commission rates
                          to be used for creating a validator.
                        type: object
                        properties:
                          rate:
                            type: string
                            description: >-
                              rate is the commission rate charged to delegators,
                              as a fraction.
                          maxRate:
                            type: string
                            description: >-
                              max_rate defines the maximum commission rate which
                              validator can ever charge, as a fraction.
                          maxChangeRate:
                            type: string
                            description: >-
                              max_change_rate defines the maximum daily increase
                              of the validator commission, as a fraction.
                      updateTime:
                        type: string
                        format: date-time
                        description: >-
                          update_time is the last time the commission rate was
                          changed.
                  minSelfDelegation:
                    type: string
                    description: >-
                      min_self_delegation is the validator's self declared
                      minimum self delegation.
                  unbondingOnHoldRefCount:
                    type: string
                    format: int64
                    title: >-
                      strictly positive if this validator's unbonding has been
                      stopped by external modules
                  unbondingIds:
                    type: array
                    items:
                      type: string
                      format: uint64
                    title: >-
                      list of unbonding ids, each uniquely identifing an
                      unbonding of this validator
            title: >-
              QueryValidatorResponse is response type for the Query/Validator
              RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: validatorAddr
          description: validator_addr defines the validator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/validators/{validatorAddr}/delegations:
    get:
      summary: ValidatorDelegations queries delegate info for given validator.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: ValidatorDelegations
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              delegationResponses:
                type: array
                items:
                  type: object
                  properties:
                    delegation:
                      type: object
                      properties:
                        delegatorAddress:
                          type: string
                          description: >-
                            delegator_address is the encoded address of the
                            delegator.
                        validatorAddress:
                          type: string
                          description: >-
                            validator_address is the encoded address of the
                            validator.
                        shares:
                          type: string
                          description: shares define the delegation shares received.
                      description: >-
                        Delegation represents the bond with tokens held by an
                        account. It is

                        owned by one delegator, and is associated with the
                        voting power of one

                        validator.
                    balance:
                      type: object
                      properties:
                        denom:
                          type: string
                        amount:
                          type: string
                      description: >-
                        Coin defines a token with a denomination and an amount.


                        NOTE: The amount field is an Int which implements the
                        custom method

                        signatures required by gogoproto.
                  description: >-
                    DelegationResponse is equivalent to Delegation except that
                    it contains a

                    balance in addition to shares which is more suitable for
                    client responses.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            title: |-
              QueryValidatorDelegationsResponse is response type for the
              Query/ValidatorDelegations RPC method
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: validatorAddr
          description: validator_addr defines the validator address to query for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/validators/{validatorAddr}/delegations/{delegatorAddr}:
    get:
      summary: Delegation queries delegate info for given validator delegator pair.
      operationId: Delegation
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              delegationResponse:
                description: >-
                  delegation_responses defines the delegation info of a
                  delegation.
                type: object
                properties:
                  delegation:
                    type: object
                    properties:
                      delegatorAddress:
                        type: string
                        description: >-
                          delegator_address is the encoded address of the
                          delegator.
                      validatorAddress:
                        type: string
                        description: >-
                          validator_address is the encoded address of the
                          validator.
                      shares:
                        type: string
                        description: shares define the delegation shares received.
                    description: >-
                      Delegation represents the bond with tokens held by an
                      account. It is

                      owned by one delegator, and is associated with the voting
                      power of one

                      validator.
                  balance:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
            description: >-
              QueryDelegationResponse is response type for the Query/Delegation
              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: validatorAddr
          description: validator_addr defines the validator address to query for.
          in: path
          required: true
          type: string
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/validators/{validatorAddr}/delegations/{delegatorAddr}/unbonding_delegation:
    get:
      summary: |-
        UnbondingDelegation queries unbonding info for given validator delegator
        pair.
      operationId: UnbondingDelegation
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              unbond:
                description: unbond defines the unbonding information of a delegation.
                type: object
                properties:
                  delegatorAddress:
                    type: string
                    description: delegator_address is the encoded address of the delegator.
                  validatorAddress:
                    type: string
                    description: validator_address is the encoded address of the validator.
                  entries:
                    type: array
                    items:
                      type: object
                      properties:
                        creationHeight:
                          type: string
                          format: int64
                          description: >-
                            creation_height is the height which the unbonding
                            took place.
                        completionTime:
                          type: string
                          format: date-time
                          description: >-
                            completion_time is the unix time for unbonding
                            completion.
                        initialBalance:
                          type: string
                          description: >-
                            initial_balance defines the tokens initially
                            scheduled to receive at completion.
                        balance:
                          type: string
                          description: balance defines the tokens to receive at completion.
                        unbondingId:
                          type: string
                          format: uint64
                          title: Incrementing id that uniquely identifies this entry
                        unbondingOnHoldRefCount:
                          type: string
                          format: int64
                          title: >-
                            Strictly positive if this entry's unbonding has been
                            stopped by external modules
                      description: >-
                        UnbondingDelegationEntry defines an unbonding object
                        with relevant metadata.
                    description: |-
                      entries are the unbonding delegation entries.

                      unbonding delegation entries
            description: >-
              QueryDelegationResponse is response type for the
              Query/UnbondingDelegation

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: validatorAddr
          description: validator_addr defines the validator address to query for.
          in: path
          required: true
          type: string
        - name: delegatorAddr
          description: delegator_addr defines the delegator address to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/staking/v1beta1/validators/{validatorAddr}/unbonding_delegations:
    get:
      summary: >-
        ValidatorUnbondingDelegations queries unbonding delegations of a
        validator.
      description: >-
        When called from another module, this query might consume a high amount
        of

        gas if the pagination field is incorrectly set.
      operationId: ValidatorUnbondingDelegations
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              unbondingResponses:
                type: array
                items:
                  type: object
                  properties:
                    delegatorAddress:
                      type: string
                      description: >-
                        delegator_address is the encoded address of the
                        delegator.
                    validatorAddress:
                      type: string
                      description: >-
                        validator_address is the encoded address of the
                        validator.
                    entries:
                      type: array
                      items:
                        type: object
                        properties:
                          creationHeight:
                            type: string
                            format: int64
                            description: >-
                              creation_height is the height which the unbonding
                              took place.
                          completionTime:
                            type: string
                            format: date-time
                            description: >-
                              completion_time is the unix time for unbonding
                              completion.
                          initialBalance:
                            type: string
                            description: >-
                              initial_balance defines the tokens initially
                              scheduled to receive at completion.
                          balance:
                            type: string
                            description: >-
                              balance defines the tokens to receive at
                              completion.
                          unbondingId:
                            type: string
                            format: uint64
                            title: >-
                              Incrementing id that uniquely identifies this
                              entry
                          unbondingOnHoldRefCount:
                            type: string
                            format: int64
                            title: >-
                              Strictly positive if this entry's unbonding has
                              been stopped by external modules
                        description: >-
                          UnbondingDelegationEntry defines an unbonding object
                          with relevant metadata.
                      description: |-
                        entries are the unbonding delegation entries.

                        unbonding delegation entries
                  description: >-
                    UnbondingDelegation stores all of a single delegator's
                    unbonding bonds

                    for a single validator in an time-ordered list.
              pagination:
                description: pagination defines the pagination in the response.
                type: object
                properties:
                  nextKey:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
            description: >-
              QueryValidatorUnbondingDelegationsResponse is response type for
              the

              Query/ValidatorUnbondingDelegations RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: validatorAddr
          description: validator_addr defines the validator address to query for.
          in: path
          required: true
          type: string
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/decode:
    post:
      summary: TxDecode decodes the transaction.
      operationId: TxDecode
      responses:
        '200':
          description: A successful response.
          schema:
            $ref: '#/definitions/cosmos.tx.v1beta1.TxDecodeResponse'
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: body
          description: |-
            TxDecodeRequest is the request type for the Service.TxDecode
            RPC method.
          in: body
          required: true
          schema:
            type: object
            properties:
              txBytes:
                type: string
                format: byte
                description: tx_bytes is the raw transaction.
            description: |-
              TxDecodeRequest is the request type for the Service.TxDecode
              RPC method.
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/decode/amino:
    post:
      summary: TxDecodeAmino decodes an Amino transaction from encoded bytes to JSON.
      operationId: TxDecodeAmino
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              aminoJson:
                type: string
            description: >-
              TxDecodeAminoResponse is the response type for the
              Service.TxDecodeAmino

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: body
          description: >-
            TxDecodeAminoRequest is the request type for the
            Service.TxDecodeAmino

            RPC method.
          in: body
          required: true
          schema:
            type: object
            properties:
              aminoBinary:
                type: string
                format: byte
            description: >-
              TxDecodeAminoRequest is the request type for the
              Service.TxDecodeAmino

              RPC method.
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/encode:
    post:
      summary: TxEncode encodes the transaction.
      operationId: TxEncode
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              txBytes:
                type: string
                format: byte
                description: tx_bytes is the encoded transaction bytes.
            description: |-
              TxEncodeResponse is the response type for the
              Service.TxEncode method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: body
          description: |-
            TxEncodeRequest is the request type for the Service.TxEncode
            RPC method.
          in: body
          required: true
          schema:
            $ref: '#/definitions/cosmos.tx.v1beta1.TxEncodeRequest'
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/encode/amino:
    post:
      summary: TxEncodeAmino encodes an Amino transaction from JSON to encoded bytes.
      operationId: TxEncodeAmino
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              aminoBinary:
                type: string
                format: byte
            description: >-
              TxEncodeAminoResponse is the response type for the
              Service.TxEncodeAmino

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: body
          description: >-
            TxEncodeAminoRequest is the request type for the
            Service.TxEncodeAmino

            RPC method.
          in: body
          required: true
          schema:
            type: object
            properties:
              aminoJson:
                type: string
            description: >-
              TxEncodeAminoRequest is the request type for the
              Service.TxEncodeAmino

              RPC method.
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/simulate:
    post:
      summary: Simulate simulates executing a transaction for estimating gas usage.
      operationId: Simulate
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              gasInfo:
                description: gas_info is the information about gas used in the simulation.
                type: object
                properties:
                  gasWanted:
                    type: string
                    format: uint64
                    description: >-
                      GasWanted is the maximum units of work we allow this tx to
                      perform.
                  gasUsed:
                    type: string
                    format: uint64
                    description: GasUsed is the amount of gas actually consumed.
              result:
                description: result is the result of the simulation.
                type: object
                properties:
                  data:
                    type: string
                    format: byte
                    description: >-
                      Data is any data returned from message or handler
                      execution. It MUST be

                      length prefixed in order to separate data from multiple
                      message executions.

                      Deprecated. This field is still populated, but prefer
                      msg_response instead

                      because it also contains the Msg response typeURL.
                  log:
                    type: string
                    description: >-
                      Log contains the log information from message or handler
                      execution.
                  events:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        attributes:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                              index:
                                type: boolean
                                title: nondeterministic
                            description: >-
                              EventAttribute is a single key-value pair,
                              associated with an event.
                      description: >-
                        Event allows application developers to attach additional
                        information to

                        ResponseFinalizeBlock and ResponseCheckTx.

                        Later, transactions may be queried using these events.
                    description: >-
                      Events contains a slice of Event objects that were emitted
                      during message

                      or handler execution.
                  msgResponses:
                    type: array
                    items:
                      type: object
                      properties:
                        '@type':
                          type: string
                          description: >-
                            A URL/resource name that uniquely identifies the
                            type of the serialized

                            protocol buffer message. This string must contain at
                            least

                            one "/" character. The last segment of the URL's
                            path must represent

                            the fully qualified name of the type (as in

                            `path/google.protobuf.Duration`). The name should be
                            in a canonical form

                            (e.g., leading "." is not accepted).


                            In practice, teams usually precompile into the
                            binary all types that they

                            expect it to use in the context of Any. However, for
                            URLs which use the

                            scheme `http`, `https`, or no scheme, one can
                            optionally set up a type

                            server that maps type URLs to message definitions as
                            follows:


                            * If no scheme is provided, `https` is assumed.

                            * An HTTP GET on the URL must yield a
                            [google.protobuf.Type][]
                              value in binary format, or produce an error.
                            * Applications are allowed to cache lookup results
                            based on the
                              URL, or have them precompiled into a binary to avoid any
                              lookup. Therefore, binary compatibility needs to be preserved
                              on changes to types. (Use versioned type names to manage
                              breaking changes.)

                            Note: this functionality is not currently available
                            in the official

                            protobuf release, and it is not used for type URLs
                            beginning with

                            type.googleapis.com. As of May 2023, there are no
                            widely used type server

                            implementations and no plans to implement one.


                            Schemes other than `http`, `https` (or the empty
                            scheme) might be

                            used with implementation specific semantics.
                      additionalProperties: {}
                      description: >-
                        `Any` contains an arbitrary serialized protocol buffer
                        message along with a

                        URL that describes the type of the serialized message.


                        Protobuf library provides support to pack/unpack Any
                        values in the form

                        of utility functions or additional generated methods of
                        the Any type.


                        Example 1: Pack and unpack a message in C++.

                            Foo foo = ...;
                            Any any;
                            any.PackFrom(foo);
                            ...
                            if (any.UnpackTo(&foo)) {
                              ...
                            }

                        Example 2: Pack and unpack a message in Java.

                            Foo foo = ...;
                            Any any = Any.pack(foo);
                            ...
                            if (any.is(Foo.class)) {
                              foo = any.unpack(Foo.class);
                            }
                            // or ...
                            if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                              foo = any.unpack(Foo.getDefaultInstance());
                            }

                         Example 3: Pack and unpack a message in Python.

                            foo = Foo(...)
                            any = Any()
                            any.Pack(foo)
                            ...
                            if any.Is(Foo.DESCRIPTOR):
                              any.Unpack(foo)
                              ...

                         Example 4: Pack and unpack a message in Go

                             foo := &pb.Foo{...}
                             any, err := anypb.New(foo)
                             if err != nil {
                               ...
                             }
                             ...
                             foo := &pb.Foo{}
                             if err := any.UnmarshalTo(foo); err != nil {
                               ...
                             }

                        The pack methods provided by protobuf library will by
                        default use

                        'type.googleapis.com/full.type.name' as the type URL and
                        the unpack

                        methods only use the fully qualified type name after the
                        last '/'

                        in the type URL, for example "foo.bar.com/x/y.z" will
                        yield type

                        name "y.z".


                        JSON

                        ====

                        The JSON representation of an `Any` value uses the
                        regular

                        representation of the deserialized, embedded message,
                        with an

                        additional field `@type` which contains the type URL.
                        Example:

                            package google.profile;
                            message Person {
                              string first_name = 1;
                              string last_name = 2;
                            }

                            {
                              "@type": "type.googleapis.com/google.profile.Person",
                              "firstName": <string>,
                              "lastName": <string>
                            }

                        If the embedded message type is well-known and has a
                        custom JSON

                        representation, that representation will be embedded
                        adding a field

                        `value` which holds the custom JSON in addition to the
                        `@type`

                        field. Example (for message
                        [google.protobuf.Duration][]):

                            {
                              "@type": "type.googleapis.com/google.protobuf.Duration",
                              "value": "1.212s"
                            }
                    description: >-
                      msg_responses contains the Msg handler responses type
                      packed in Anys.
            description: |-
              SimulateResponse is the response type for the
              Service.SimulateRPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: body
          description: |-
            SimulateRequest is the request type for the Service.Simulate
            RPC method.
          in: body
          required: true
          schema:
            $ref: '#/definitions/cosmos.tx.v1beta1.SimulateRequest'
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/txs:
    get:
      summary: GetTxsEvent fetches txs by event.
      operationId: GetTxsEvent
      responses:
        '200':
          description: A successful response.
          schema:
            $ref: '#/definitions/cosmos.tx.v1beta1.GetTxsEventResponse'
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: events
          description: >-
            events is the list of transaction event type.

            Deprecated post v0.47.x: use query instead, which should contain a
            valid

            events query.
          in: query
          required: false
          type: array
          items:
            type: string
          collectionFormat: multi
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
        - name: orderBy
          description: |2-
             - ORDER_BY_UNSPECIFIED: ORDER_BY_UNSPECIFIED specifies an unknown sorting order. OrderBy defaults
            to ASC in this case.
             - ORDER_BY_ASC: ORDER_BY_ASC defines ascending order
             - ORDER_BY_DESC: ORDER_BY_DESC defines descending order
          in: query
          required: false
          type: string
          enum:
            - ORDER_BY_UNSPECIFIED
            - ORDER_BY_ASC
            - ORDER_BY_DESC
          default: ORDER_BY_UNSPECIFIED
        - name: page
          description: |-
            page is the page number to query, starts at 1. If not provided, will
            default to first page.
          in: query
          required: false
          type: string
          format: uint64
        - name: limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: query
          description: >-
            query defines the transaction event query that is proxied to
            Tendermint's

            TxSearch RPC method. The query must be valid.
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
    post:
      summary: BroadcastTx broadcast transaction.
      operationId: BroadcastTx
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              txResponse:
                description: tx_response is the queried TxResponses.
                type: object
                properties:
                  height:
                    type: string
                    format: int64
                    title: The block height
                  txhash:
                    type: string
                    description: The transaction hash.
                  codespace:
                    type: string
                    title: Namespace for the Code
                  code:
                    type: integer
                    format: int64
                    description: Response code.
                  data:
                    type: string
                    description: Result bytes, if any.
                  rawLog:
                    type: string
                    description: >-
                      The output of the application's logger (raw string). May
                      be

                      non-deterministic.
                  logs:
                    type: array
                    items:
                      type: object
                      properties:
                        msgIndex:
                          type: integer
                          format: int64
                        log:
                          type: string
                        events:
                          type: array
                          items:
                            type: object
                            properties:
                              type:
                                type: string
                              attributes:
                                type: array
                                items:
                                  type: object
                                  properties:
                                    key:
                                      type: string
                                    value:
                                      type: string
                                  description: >-
                                    Attribute defines an attribute wrapper where
                                    the key and value are

                                    strings instead of raw bytes.
                            description: >-
                              StringEvent defines en Event object wrapper where
                              all the attributes

                              contain key/value pairs that are strings instead
                              of raw bytes.
                          description: >-
                            Events contains a slice of Event objects that were
                            emitted during some

                            execution.
                      description: >-
                        ABCIMessageLog defines a structure containing an indexed
                        tx ABCI message log.
                    description: >-
                      The output of the application's logger (typed). May be
                      non-deterministic.
                  info:
                    type: string
                    description: Additional information. May be non-deterministic.
                  gasWanted:
                    type: string
                    format: int64
                    description: Amount of gas requested for transaction.
                  gasUsed:
                    type: string
                    format: int64
                    description: Amount of gas consumed by transaction.
                  tx:
                    description: The request transaction bytes.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
                  timestamp:
                    type: string
                    description: >-
                      Time of the previous block. For heights > 1, it's the
                      weighted median of

                      the timestamps of the valid votes in the block.LastCommit.
                      For height == 1,

                      it's genesis time.
                  events:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        attributes:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                              index:
                                type: boolean
                                title: nondeterministic
                            description: >-
                              EventAttribute is a single key-value pair,
                              associated with an event.
                      description: >-
                        Event allows application developers to attach additional
                        information to

                        ResponseFinalizeBlock and ResponseCheckTx.

                        Later, transactions may be queried using these events.
                    description: >-
                      Events defines all the events emitted by processing a
                      transaction. Note,

                      these events include those emitted by processing all the
                      messages and those

                      emitted from the ante. Whereas Logs contains the events,
                      with

                      additional metadata, emitted only by processing the
                      messages.
            description: |-
              BroadcastTxResponse is the response type for the
              Service.BroadcastTx method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: body
          description: >-
            BroadcastTxRequest is the request type for the
            Service.BroadcastTxRequest

            RPC method.
          in: body
          required: true
          schema:
            type: object
            properties:
              txBytes:
                type: string
                format: byte
                description: tx_bytes is the raw transaction.
              mode:
                type: string
                enum:
                  - BROADCAST_MODE_UNSPECIFIED
                  - BROADCAST_MODE_BLOCK
                  - BROADCAST_MODE_SYNC
                  - BROADCAST_MODE_ASYNC
                default: BROADCAST_MODE_UNSPECIFIED
                description: >-
                  BroadcastMode specifies the broadcast mode for the
                  TxService.Broadcast RPC

                  method.

                   - BROADCAST_MODE_UNSPECIFIED: zero-value for mode ordering
                   - BROADCAST_MODE_BLOCK: DEPRECATED: use BROADCAST_MODE_SYNC instead,
                  BROADCAST_MODE_BLOCK is not supported by the SDK from v0.47.x
                  onwards.
                   - BROADCAST_MODE_SYNC: BROADCAST_MODE_SYNC defines a tx broadcasting mode where the client waits
                  for a CheckTx execution response only.
                   - BROADCAST_MODE_ASYNC: BROADCAST_MODE_ASYNC defines a tx broadcasting mode where the client
                  returns immediately.
            description: >-
              BroadcastTxRequest is the request type for the
              Service.BroadcastTxRequest

              RPC method.
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/txs/block/{height}:
    get:
      summary: GetBlockWithTxs fetches a block with decoded txs.
      operationId: GetBlockWithTxs
      responses:
        '200':
          description: A successful response.
          schema:
            $ref: '#/definitions/cosmos.tx.v1beta1.GetBlockWithTxsResponse'
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: height
          description: height is the height of the block to query.
          in: path
          required: true
          type: string
          format: int64
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.countTotal
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.
          in: query
          required: false
          type: boolean
      tags:
        - Cosmos SDK
  /cosmos/tx/v1beta1/txs/{hash}:
    get:
      summary: GetTx fetches a tx by hash.
      operationId: GetTx
      responses:
        '200':
          description: A successful response.
          schema:
            $ref: '#/definitions/cosmos.tx.v1beta1.GetTxResponse'
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: hash
          description: hash is the tx hash to query, encoded as a hex string.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/upgrade/v1beta1/applied_plan/{name}:
    get:
      summary: AppliedPlan queries a previously applied upgrade plan by its name.
      operationId: AppliedPlan
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              height:
                type: string
                format: int64
                description: height is the block height at which the plan was applied.
            description: >-
              QueryAppliedPlanResponse is the response type for the
              Query/AppliedPlan RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: name
          description: name is the name of the applied plan to query for.
          in: path
          required: true
          type: string
      tags:
        - Cosmos SDK
  /cosmos/upgrade/v1beta1/authority:
    get:
      summary: Returns the account with authority to conduct upgrades
      operationId: Authority
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              address:
                type: string
            title: QueryAuthorityResponse is the response type for Query/Authority
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/upgrade/v1beta1/current_plan:
    get:
      summary: CurrentPlan queries the current upgrade plan.
      operationId: CurrentPlan
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              plan:
                description: plan is the current upgrade plan.
                type: object
                properties:
                  name:
                    type: string
                    description: >-
                      Sets the name for the upgrade. This name will be used by
                      the upgraded

                      version of the software to apply any special "on-upgrade"
                      commands during

                      the first BeginBlock method after the upgrade is applied.
                      It is also used

                      to detect whether a software version can handle a given
                      upgrade. If no

                      upgrade handler with this name has been set in the
                      software, it will be

                      assumed that the software is out-of-date when the upgrade
                      Time or Height is

                      reached and the software will exit.
                  time:
                    type: string
                    format: date-time
                    description: >-
                      Deprecated: Time based upgrades have been deprecated. Time
                      based upgrade logic

                      has been removed from the SDK.

                      If this field is not empty, an error will be thrown.
                  height:
                    type: string
                    format: int64
                    description: The height at which the upgrade must be performed.
                  info:
                    type: string
                    title: >-
                      Any application specific upgrade info to be included
                      on-chain

                      such as a git commit that validators could automatically
                      upgrade to
                  upgradedClientState:
                    description: >-
                      Deprecated: UpgradedClientState field has been deprecated.
                      IBC upgrade logic has been

                      moved to the IBC module in the sub module 02-client.

                      If this field is not empty, an error will be thrown.
                    type: object
                    properties:
                      '@type':
                        type: string
                        description: >-
                          A URL/resource name that uniquely identifies the type
                          of the serialized

                          protocol buffer message. This string must contain at
                          least

                          one "/" character. The last segment of the URL's path
                          must represent

                          the fully qualified name of the type (as in

                          `path/google.protobuf.Duration`). The name should be
                          in a canonical form

                          (e.g., leading "." is not accepted).


                          In practice, teams usually precompile into the binary
                          all types that they

                          expect it to use in the context of Any. However, for
                          URLs which use the

                          scheme `http`, `https`, or no scheme, one can
                          optionally set up a type

                          server that maps type URLs to message definitions as
                          follows:


                          * If no scheme is provided, `https` is assumed.

                          * An HTTP GET on the URL must yield a
                          [google.protobuf.Type][]
                            value in binary format, or produce an error.
                          * Applications are allowed to cache lookup results
                          based on the
                            URL, or have them precompiled into a binary to avoid any
                            lookup. Therefore, binary compatibility needs to be preserved
                            on changes to types. (Use versioned type names to manage
                            breaking changes.)

                          Note: this functionality is not currently available in
                          the official

                          protobuf release, and it is not used for type URLs
                          beginning with

                          type.googleapis.com. As of May 2023, there are no
                          widely used type server

                          implementations and no plans to implement one.


                          Schemes other than `http`, `https` (or the empty
                          scheme) might be

                          used with implementation specific semantics.
                    additionalProperties: {}
            description: >-
              QueryCurrentPlanResponse is the response type for the
              Query/CurrentPlan RPC

              method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      tags:
        - Cosmos SDK
  /cosmos/upgrade/v1beta1/module_versions:
    get:
      summary: ModuleVersions queries the list of module versions from state.
      operationId: ModuleVersions
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              moduleVersions:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      title: name of the app module
                    version:
                      type: string
                      format: uint64
                      title: consensus version of the app module
                  description: ModuleVersion specifies a module and its consensus version.
                description: >-
                  module_versions is a list of module names with their consensus
                  versions.
            description: >-
              QueryModuleVersionsResponse is the response type for the
              Query/ModuleVersions

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: moduleName
          description: |-
            module_name is a field to query a specific module
            consensus version from state. Leaving this empty will
            fetch the full list of module versions from state
          in: query
          required: false
          type: string
      tags:
        - Cosmos SDK
  /cosmos/upgrade/v1beta1/upgraded_consensus_state/{lastHeight}:
    get:
      summary: >-
        UpgradedConsensusState queries the consensus state that will serve

        as a trusted kernel for the next version of this chain. It will only be

        stored at the last height of this chain.

        UpgradedConsensusState RPC not supported with legacy querier

        This rpc is deprecated now that IBC has its own replacement

        (https://github.com/cosmos/ibc-go/blob/2c880a22e9f9cc75f62b527ca94aa75ce1106001/proto/ibc/core/client/v1/query.proto#L54)
      operationId: UpgradedConsensusState
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              upgradedConsensusState:
                type: string
                format: byte
            description: >-
              QueryUpgradedConsensusStateResponse is the response type for the
              Query/UpgradedConsensusState

              RPC method.
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }
                        // or ...
                        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                          foo = any.unpack(Foo.getDefaultInstance());
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := anypb.New(foo)
                         if err != nil {
                           ...
                         }
                         ...
                         foo := &pb.Foo{}
                         if err := any.UnmarshalTo(foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".


                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
      parameters:
        - name: lastHeight
          description: |-
            last height of the current chain must be sent in request
            as this is the height under which next consensus state is stored
          in: path
          required: true
          type: string
          format: int64
      tags:
        - Cosmos SDK
definitions:
  cosmos.base.query.v1beta1.PageRequest:
    type: object
    properties:
      key:
        type: string
        format: byte
        description: |-
          key is a value returned in PageResponse.next_key to begin
          querying the next page most efficiently. Only one of offset or key
          should be set.
      offset:
        type: string
        format: uint64
        description: |-
          offset is a numeric offset that can be used when key is unavailable.
          It is less efficient than using key. Only one of offset or key should
          be set.
      limit:
        type: string
        format: uint64
        description: >-
          limit is the total number of results to be returned in the result
          page.

          If left empty it will default to a value to be set by each app.
      count_total:
        type: boolean
        description: >-
          count_total is set to true  to indicate that the result set should
          include

          a count of the total number of items available for pagination in UIs.

          count_total is only respected when offset is used. It is ignored when
          key

          is set.
      reverse:
        type: boolean
        description: >-
          reverse is set to true if results are to be returned in the descending
          order.
      countTotal:
        type: boolean
        description: >-
          count_total is set to true  to indicate that the result set should
          include

          a count of the total number of items available for pagination in UIs.

          count_total is only respected when offset is used. It is ignored when
          key

          is set.
    description: |-
      message SomeRequest {
               Foo some_parameter = 1;
               PageRequest pagination = 2;
       }
    title: |-
      PageRequest is to be embedded in gRPC request messages for efficient
      pagination. Ex:
  cosmos.base.query.v1beta1.PageResponse:
    type: object
    properties:
      next_key:
        type: string
        format: byte
        description: |-
          next_key is the key to be passed to PageRequest.key to
          query the next page most efficiently. It will be empty if
          there are no more results.
      total:
        type: string
        format: uint64
        title: |-
          total is total number of results available if PageRequest.count_total
          was set, its value is undefined otherwise
      nextKey:
        type: string
        format: byte
        description: |-
          next_key is the key to be passed to PageRequest.key to
          query the next page most efficiently. It will be empty if
          there are no more results.
    description: |-
      PageResponse is to be embedded in gRPC response messages where the
      corresponding request message has used PageRequest.

       message SomeResponse {
               repeated Bar results = 1;
               PageResponse page = 2;
       }
  google.protobuf.Any:
    type: object
    properties:
      '@type':
        type: string
        description: >-
          A URL/resource name that uniquely identifies the type of the
          serialized

          protocol buffer message. This string must contain at least

          one "/" character. The last segment of the URL's path must represent

          the fully qualified name of the type (as in

          `path/google.protobuf.Duration`). The name should be in a canonical
          form

          (e.g., leading "." is not accepted).


          In practice, teams usually precompile into the binary all types that
          they

          expect it to use in the context of Any. However, for URLs which use
          the

          scheme `http`, `https`, or no scheme, one can optionally set up a type

          server that maps type URLs to message definitions as follows:


          * If no scheme is provided, `https` is assumed.

          * An HTTP GET on the URL must yield a [google.protobuf.Type][]
            value in binary format, or produce an error.
          * Applications are allowed to cache lookup results based on the
            URL, or have them precompiled into a binary to avoid any
            lookup. Therefore, binary compatibility needs to be preserved
            on changes to types. (Use versioned type names to manage
            breaking changes.)

          Note: this functionality is not currently available in the official

          protobuf release, and it is not used for type URLs beginning with

          type.googleapis.com.


          Schemes other than `http`, `https` (or the empty scheme) might be

          used with implementation specific semantics.
    additionalProperties: {}
    description: >-
      `Any` contains an arbitrary serialized protocol buffer message along with
      a

      URL that describes the type of the serialized message.


      Protobuf library provides support to pack/unpack Any values in the form

      of utility functions or additional generated methods of the Any type.


      Example 1: Pack and unpack a message in C++.

          Foo foo = ...;
          Any any;
          any.PackFrom(foo);
          ...
          if (any.UnpackTo(&foo)) {
            ...
          }

      Example 2: Pack and unpack a message in Java.

          Foo foo = ...;
          Any any = Any.pack(foo);
          ...
          if (any.is(Foo.class)) {
            foo = any.unpack(Foo.class);
          }

       Example 3: Pack and unpack a message in Python.

          foo = Foo(...)
          any = Any()
          any.Pack(foo)
          ...
          if any.Is(Foo.DESCRIPTOR):
            any.Unpack(foo)
            ...

       Example 4: Pack and unpack a message in Go

           foo := &pb.Foo{...}
           any, err := ptypes.MarshalAny(foo)
           ...
           foo := &pb.Foo{}
           if err := ptypes.UnmarshalAny(any, foo); err != nil {
             ...
           }

      The pack methods provided by protobuf library will by default use

      'type.googleapis.com/full.type.name' as the type URL and the unpack

      methods only use the fully qualified type name after the last '/'

      in the type URL, for example "foo.bar.com/x/y.z" will yield type

      name "y.z".



      JSON

      ====

      The JSON representation of an `Any` value uses the regular

      representation of the deserialized, embedded message, with an

      additional field `@type` which contains the type URL. Example:

          package google.profile;
          message Person {
            string first_name = 1;
            string last_name = 2;
          }

          {
            "@type": "type.googleapis.com/google.profile.Person",
            "firstName": <string>,
            "lastName": <string>
          }

      If the embedded message type is well-known and has a custom JSON

      representation, that representation will be embedded adding a field

      `value` which holds the custom JSON in addition to the `@type`

      field. Example (for message [google.protobuf.Duration][]):

          {
            "@type": "type.googleapis.com/google.protobuf.Duration",
            "value": "1.212s"
          }
  google.rpc.Status:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := ptypes.MarshalAny(foo)
                 ...
                 foo := &pb.Foo{}
                 if err := ptypes.UnmarshalAny(any, foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".



            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
  provenance.asset.v1.Asset:
    type: object
    properties:
      class_id:
        type: string
        description: >-
          class_id associated with the asset, similar to the contract address of
          ERC721.
      id:
        type: string
        description: id is a unique identifier of the asset.
      uri:
        type: string
        description: uri for the asset metadata stored off chain.
      uri_hash:
        type: string
        description: uri_hash is a hash of the document pointed by uri.
      data:
        type: string
        description: data is an app specific json data of the asset.
    description: Asset defines the asset. Similar to Nft.
  provenance.asset.v1.AssetClass:
    type: object
    properties:
      id:
        type: string
        description: >-
          id defines the unique identifier of the asset classification, similar
          to the contract address of ERC721.
      name:
        type: string
        description: name defines the human-readable name of the asset classification.
      symbol:
        type: string
        description: symbol is an abbreviated name for asset classification.
      description:
        type: string
        description: description is a brief description of asset classification.
      uri:
        type: string
        description: >-
          uri for the class metadata stored off chain. It can define schema for
          Class and asset `Data` attributes.
      uri_hash:
        type: string
        description: uri_hash is a hash of the document pointed by uri.
      data:
        type: string
        description: data is the app specific json schema of the asset class.
    description: AssetClass defines the class type data for an Asset. Similar to NftClass.
  provenance.asset.v1.QueryAssetClassResponse:
    type: object
    properties:
      asset_class:
        description: asset_class is the asset class returned from the query.
        type: object
        properties:
          id:
            type: string
            description: >-
              id defines the unique identifier of the asset classification,
              similar to the contract address of ERC721.
          name:
            type: string
            description: name defines the human-readable name of the asset classification.
          symbol:
            type: string
            description: symbol is an abbreviated name for asset classification.
          description:
            type: string
            description: description is a brief description of asset classification.
          uri:
            type: string
            description: >-
              uri for the class metadata stored off chain. It can define schema
              for Class and asset `Data` attributes.
          uri_hash:
            type: string
            description: uri_hash is a hash of the document pointed by uri.
          data:
            type: string
            description: data is the app specific json schema of the asset class.
    description: >-
      QueryAssetClassResponse is the response type for the Query/Class RPC
      method.
  provenance.asset.v1.QueryAssetClassesResponse:
    type: object
    properties:
      asset_classes:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              description: >-
                id defines the unique identifier of the asset classification,
                similar to the contract address of ERC721.
            name:
              type: string
              description: >-
                name defines the human-readable name of the asset
                classification.
            symbol:
              type: string
              description: symbol is an abbreviated name for asset classification.
            description:
              type: string
              description: description is a brief description of asset classification.
            uri:
              type: string
              description: >-
                uri for the class metadata stored off chain. It can define
                schema for Class and asset `Data` attributes.
            uri_hash:
              type: string
              description: uri_hash is a hash of the document pointed by uri.
            data:
              type: string
              description: data is the app specific json schema of the asset class.
          description: >-
            AssetClass defines the class type data for an Asset. Similar to
            NftClass.
        description: asset_classes are the asset classes returned from the query.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAssetClassesResponse is the response type for the Query/Classes RPC
      method.
  provenance.asset.v1.QueryAssetResponse:
    type: object
    properties:
      asset:
        description: asset is the asset returned from the query.
        type: object
        properties:
          class_id:
            type: string
            description: >-
              class_id associated with the asset, similar to the contract
              address of ERC721.
          id:
            type: string
            description: id is a unique identifier of the asset.
          uri:
            type: string
            description: uri for the asset metadata stored off chain.
          uri_hash:
            type: string
            description: uri_hash is a hash of the document pointed by uri.
          data:
            type: string
            description: data is an app specific json data of the asset.
    description: QueryAssetResponse is the response type for the Query/Asset RPC method.
  provenance.asset.v1.QueryAssetsResponse:
    type: object
    properties:
      assets:
        type: array
        items:
          type: object
          properties:
            class_id:
              type: string
              description: >-
                class_id associated with the asset, similar to the contract
                address of ERC721.
            id:
              type: string
              description: id is a unique identifier of the asset.
            uri:
              type: string
              description: uri for the asset metadata stored off chain.
            uri_hash:
              type: string
              description: uri_hash is a hash of the document pointed by uri.
            data:
              type: string
              description: data is an app specific json data of the asset.
          description: Asset defines the asset. Similar to Nft.
        description: assets are the assets returned from the query.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryAssetsResponse is the response type for the Query/Assets RPC methods.
  cosmos.base.v1beta1.Coin:
    type: object
    properties:
      denom:
        type: string
      amount:
        type: string
    description: |-
      Coin defines a token with a denomination and an amount.

      NOTE: The amount field is an Int which implements the custom method
      signatures required by gogoproto.
  provenance.asset.v1.AssetKey:
    type: object
    properties:
      class_id:
        type: string
        description: >-
          class_id associated with the asset, similar to the contract address of
          ERC721.
      id:
        type: string
        description: id is a unique identifier of the asset.
    description: AssetKey defines the lookup key for an Asset.
  provenance.asset.v1.MsgBurnAssetResponse:
    type: object
    description: MsgBurnAssetResponse is the response type for the BurnAsset RPC method.
  provenance.asset.v1.MsgCreateAssetClassResponse:
    type: object
    description: >-
      MsgCreateAssetClassResponse is the response type for the CreateAssetClass
      RPC method.
  provenance.asset.v1.MsgCreateAssetResponse:
    type: object
    description: >-
      MsgCreateAssetResponse is the response type for the CreateAsset RPC
      method.
  provenance.asset.v1.MsgCreatePoolResponse:
    type: object
    description: MsgCreatePoolResponse is the response type for the CreatePool RPC method.
  provenance.asset.v1.MsgCreateSecuritizationResponse:
    type: object
    description: >-
      MsgCreateSecuritizationResponse is the response type for the
      CreateSecuritization RPC method.
  provenance.asset.v1.MsgCreateTokenizationResponse:
    type: object
    description: >-
      MsgCreateTokenizationResponse is the response type for the
      CreateTokenization RPC method.
  provenance.attribute.v1.Attribute:
    type: object
    properties:
      name:
        type: string
        description: The attribute name.
      value:
        type: string
        format: byte
        description: The attribute value.
      attribute_type:
        description: The attribute value type.
        type: string
        enum:
          - ATTRIBUTE_TYPE_UNSPECIFIED
          - ATTRIBUTE_TYPE_UUID
          - ATTRIBUTE_TYPE_JSON
          - ATTRIBUTE_TYPE_STRING
          - ATTRIBUTE_TYPE_URI
          - ATTRIBUTE_TYPE_INT
          - ATTRIBUTE_TYPE_FLOAT
          - ATTRIBUTE_TYPE_PROTO
          - ATTRIBUTE_TYPE_BYTES
        default: ATTRIBUTE_TYPE_UNSPECIFIED
        title: >-
          AttributeType defines the type of the data stored in the attribute
          value
      address:
        type: string
        title: The address the attribute is bound to
      expiration_date:
        type: string
        format: date-time
        description: Time that an attribute will expire.
      concrete_type:
        type: string
        description: >-
          concrete_type defines the specific type of data for PROTO or JSON
          attribute.

          Optional. Max 200 characters.
    title: >-
      Attribute holds a typed key/value structure for data associated with an
      account
  provenance.attribute.v1.AttributeType:
    type: string
    enum:
      - ATTRIBUTE_TYPE_UNSPECIFIED
      - ATTRIBUTE_TYPE_UUID
      - ATTRIBUTE_TYPE_JSON
      - ATTRIBUTE_TYPE_STRING
      - ATTRIBUTE_TYPE_URI
      - ATTRIBUTE_TYPE_INT
      - ATTRIBUTE_TYPE_FLOAT
      - ATTRIBUTE_TYPE_PROTO
      - ATTRIBUTE_TYPE_BYTES
    default: ATTRIBUTE_TYPE_UNSPECIFIED
    description: >-
      - ATTRIBUTE_TYPE_UNSPECIFIED: ATTRIBUTE_TYPE_UNSPECIFIED defines an
      unknown/invalid type
       - ATTRIBUTE_TYPE_UUID: ATTRIBUTE_TYPE_UUID defines an attribute value that contains a string value representation of a V4 uuid
       - ATTRIBUTE_TYPE_JSON: ATTRIBUTE_TYPE_JSON defines an attribute value that contains a byte string containing json data
       - ATTRIBUTE_TYPE_STRING: ATTRIBUTE_TYPE_STRING defines an attribute value that contains a generic string value
       - ATTRIBUTE_TYPE_URI: ATTRIBUTE_TYPE_URI defines an attribute value that contains a URI
       - ATTRIBUTE_TYPE_INT: ATTRIBUTE_TYPE_INT defines an attribute value that contains an integer (cast as int64)
       - ATTRIBUTE_TYPE_FLOAT: ATTRIBUTE_TYPE_FLOAT defines an attribute value that contains a float
       - ATTRIBUTE_TYPE_PROTO: ATTRIBUTE_TYPE_PROTO defines an attribute value that contains a serialized proto value in bytes
       - ATTRIBUTE_TYPE_BYTES: ATTRIBUTE_TYPE_BYTES defines an attribute value that contains an untyped array of bytes
    title: AttributeType defines the type of the data stored in the attribute value
  provenance.attribute.v1.Params:
    type: object
    properties:
      max_value_length:
        type: integer
        format: int64
        title: maximum length of data to allow in an attribute value
    description: Params defines the set of params for the attribute module.
  provenance.attribute.v1.QueryAccountDataResponse:
    type: object
    properties:
      value:
        type: string
        description: value is the accountdata attribute value for the requested account.
    description: >-
      QueryAccountDataResponse is the response type for the Query/AccountData
      method.
  provenance.attribute.v1.QueryAttributeAccountsResponse:
    type: object
    properties:
      accounts:
        type: array
        items:
          type: string
        title: list of account addresses that have attributes of request name
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAttributeAccountsResponse is the response type for the
      Query/AttributeAccounts method.
  provenance.attribute.v1.QueryAttributeResponse:
    type: object
    properties:
      account:
        type: string
        description: >-
          a string containing the address of the account the attributes are
          assigned to.
      attributes:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              description: The attribute name.
            value:
              type: string
              format: byte
              description: The attribute value.
            attribute_type:
              description: The attribute value type.
              type: string
              enum:
                - ATTRIBUTE_TYPE_UNSPECIFIED
                - ATTRIBUTE_TYPE_UUID
                - ATTRIBUTE_TYPE_JSON
                - ATTRIBUTE_TYPE_STRING
                - ATTRIBUTE_TYPE_URI
                - ATTRIBUTE_TYPE_INT
                - ATTRIBUTE_TYPE_FLOAT
                - ATTRIBUTE_TYPE_PROTO
                - ATTRIBUTE_TYPE_BYTES
              default: ATTRIBUTE_TYPE_UNSPECIFIED
              title: >-
                AttributeType defines the type of the data stored in the
                attribute value
            address:
              type: string
              title: The address the attribute is bound to
            expiration_date:
              type: string
              format: date-time
              description: Time that an attribute will expire.
            concrete_type:
              type: string
              description: >-
                concrete_type defines the specific type of data for PROTO or
                JSON attribute.

                Optional. Max 200 characters.
          title: >-
            Attribute holds a typed key/value structure for data associated with
            an account
        title: a list of attribute values
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAttributeResponse is the response type for the Query/Attribute
      method.
  provenance.attribute.v1.QueryAttributesResponse:
    type: object
    properties:
      account:
        type: string
        title: >-
          a string containing the address of the account the attributes are
          assigned to=
      attributes:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              description: The attribute name.
            value:
              type: string
              format: byte
              description: The attribute value.
            attribute_type:
              description: The attribute value type.
              type: string
              enum:
                - ATTRIBUTE_TYPE_UNSPECIFIED
                - ATTRIBUTE_TYPE_UUID
                - ATTRIBUTE_TYPE_JSON
                - ATTRIBUTE_TYPE_STRING
                - ATTRIBUTE_TYPE_URI
                - ATTRIBUTE_TYPE_INT
                - ATTRIBUTE_TYPE_FLOAT
                - ATTRIBUTE_TYPE_PROTO
                - ATTRIBUTE_TYPE_BYTES
              default: ATTRIBUTE_TYPE_UNSPECIFIED
              title: >-
                AttributeType defines the type of the data stored in the
                attribute value
            address:
              type: string
              title: The address the attribute is bound to
            expiration_date:
              type: string
              format: date-time
              description: Time that an attribute will expire.
            concrete_type:
              type: string
              description: >-
                concrete_type defines the specific type of data for PROTO or
                JSON attribute.

                Optional. Max 200 characters.
          title: >-
            Attribute holds a typed key/value structure for data associated with
            an account
        title: a list of attribute values
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAttributesResponse is the response type for the Query/Attributes
      method.
  provenance.attribute.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          max_value_length:
            type: integer
            format: int64
            title: maximum length of data to allow in an attribute value
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  provenance.attribute.v1.QueryScanResponse:
    type: object
    properties:
      account:
        type: string
        title: >-
          a string containing the address of the account the attributes are
          assigned to=
      attributes:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              description: The attribute name.
            value:
              type: string
              format: byte
              description: The attribute value.
            attribute_type:
              description: The attribute value type.
              type: string
              enum:
                - ATTRIBUTE_TYPE_UNSPECIFIED
                - ATTRIBUTE_TYPE_UUID
                - ATTRIBUTE_TYPE_JSON
                - ATTRIBUTE_TYPE_STRING
                - ATTRIBUTE_TYPE_URI
                - ATTRIBUTE_TYPE_INT
                - ATTRIBUTE_TYPE_FLOAT
                - ATTRIBUTE_TYPE_PROTO
                - ATTRIBUTE_TYPE_BYTES
              default: ATTRIBUTE_TYPE_UNSPECIFIED
              title: >-
                AttributeType defines the type of the data stored in the
                attribute value
            address:
              type: string
              title: The address the attribute is bound to
            expiration_date:
              type: string
              format: date-time
              description: Time that an attribute will expire.
            concrete_type:
              type: string
              description: >-
                concrete_type defines the specific type of data for PROTO or
                JSON attribute.

                Optional. Max 200 characters.
          title: >-
            Attribute holds a typed key/value structure for data associated with
            an account
        title: a list of attribute values
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryScanResponse is the response type for the Query/Scan method.
  provenance.attribute.v1.MsgAddAttributeResponse:
    type: object
    description: MsgAddAttributeResponse defines the Msg/AddAttribute response type.
  provenance.attribute.v1.MsgDeleteAttributeResponse:
    type: object
    description: MsgDeleteAttributeResponse defines the Msg/DeleteAttribute response type.
  provenance.attribute.v1.MsgDeleteDistinctAttributeResponse:
    type: object
    description: >-
      MsgDeleteDistinctAttributeResponse defines the Msg/DeleteDistinctAttribute
      response type.
  provenance.attribute.v1.MsgSetAccountDataResponse:
    type: object
    description: MsgSetAccountDataResponse defines the Msg/SetAccountData response type.
  provenance.attribute.v1.MsgUpdateAttributeExpirationResponse:
    type: object
    description: MsgUpdateAttributeExpirationResponse defines the Msg/Vote response type.
  provenance.attribute.v1.MsgUpdateAttributeResponse:
    type: object
    description: MsgUpdateAttributeResponse defines the Msg/UpdateAttribute response type.
  provenance.attribute.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is a response message for the UpdateParams
      endpoint.
  provenance.exchange.v1.AccessGrant:
    type: object
    properties:
      address:
        type: string
        description: address is the address that these permissions apply to.
      permissions:
        type: array
        items:
          type: string
          enum:
            - PERMISSION_UNSPECIFIED
            - PERMISSION_SETTLE
            - PERMISSION_SET_IDS
            - PERMISSION_CANCEL
            - PERMISSION_WITHDRAW
            - PERMISSION_UPDATE
            - PERMISSION_PERMISSIONS
            - PERMISSION_ATTRIBUTES
          default: PERMISSION_UNSPECIFIED
          description: >-
            Permission defines the different types of permission that can be
            given to an account for a market.

             - PERMISSION_UNSPECIFIED: PERMISSION_UNSPECIFIED is the zero-value Permission; it is an error to use it.
             - PERMISSION_SETTLE: PERMISSION_SETTLE is the ability to use the Settle Tx endpoint on behalf of a market.
             - PERMISSION_SET_IDS: PERMISSION_SET_IDS is the ability to use the SetOrderExternalID Tx endpoint on behalf of a market.
             - PERMISSION_CANCEL: PERMISSION_CANCEL is the ability to use the Cancel Tx endpoint on behalf of a market.
             - PERMISSION_WITHDRAW: PERMISSION_WITHDRAW is the ability to use the MarketWithdraw Tx endpoint.
             - PERMISSION_UPDATE: PERMISSION_UPDATE is the ability to use the MarketUpdate* Tx endpoints.
             - PERMISSION_PERMISSIONS: PERMISSION_PERMISSIONS is the ability to use the MarketManagePermissions Tx endpoint.
             - PERMISSION_ATTRIBUTES: PERMISSION_ATTRIBUTES is the ability to use the MarketManageReqAttrs Tx endpoint.
        description: allowed is the list of permissions available for the address.
    description: >-
      AddrPermissions associates an address with a list of permissions available
      for that address.
  provenance.exchange.v1.AccountAmount:
    type: object
    properties:
      account:
        type: string
        description: >-
          account is the bech32 address string of the account associated with
          the amount.
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount is the funds associated with the address.
    description: AccountAmount associates an account with a coins amount.
  provenance.exchange.v1.AskOrder:
    type: object
    properties:
      market_id:
        type: integer
        format: int64
        description: market_id identifies the market that this order belongs to.
      seller:
        type: string
        description: >-
          seller is the address of the account that owns this order and has the
          assets to sell.
      assets:
        description: |-
          assets are the things that the seller wishes to sell.
          A hold is placed on this until the order is filled or cancelled.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      price:
        description: >-
          price is the minimum amount that the seller is willing to accept for
          the assets. The seller's settlement

          proportional fee (and possibly the settlement flat fee) is taken out
          of the amount the seller receives,

          so it's possible that the seller will still receive less than this
          price.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      seller_settlement_flat_fee:
        description: >-
          seller_settlement_flat_fee is the flat fee for sellers that will be
          charged during settlement. If this denom is the

          same denom as the price, it will come out of the actual price
          received. If this denom is different, the amount must

          be in the seller's account and a hold is placed on it until the order
          is filled or cancelled.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      allow_partial:
        type: boolean
        description: >-
          allow_partial should be true if partial fulfillment of this order
          should be allowed, and should be false if the

          order must be either filled in full or not filled at all.
      external_id:
        type: string
        description: >-
          external_id is an optional string used to externally identify this
          order. Max length is 100 characters.

          If an order in this market with this external id already exists, this
          order will be rejected.
    description: AskOrder represents someone's desire to sell something at a minimum price.
  provenance.exchange.v1.BidOrder:
    type: object
    properties:
      market_id:
        type: integer
        format: int64
        description: market_id identifies the market that this order belongs to.
      buyer:
        type: string
        description: >-
          buyer is the address of the account that owns this order and has the
          price to spend.
      assets:
        description: assets are the things that the buyer wishes to buy.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      price:
        description: |-
          price is the amount that the buyer will pay for the assets.
          A hold is placed on this until the order is filled or cancelled.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      buyer_settlement_fees:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          buyer_settlement_fees are the fees (both flat and proportional) that
          the buyer will pay (in addition to the price)

          when the order is settled. A hold is placed on this until the order is
          filled or cancelled.
      allow_partial:
        type: boolean
        description: >-
          allow_partial should be true if partial fulfillment of this order
          should be allowed, and should be false if the

          order must be either filled in full or not filled at all.
      external_id:
        type: string
        description: >-
          external_id is an optional string used to externally identify this
          order. Max length is 100 characters.

          If an order in this market with this external id already exists, this
          order will be rejected.
    description: BidOrder represents someone's desire to buy something at a specific price.
  provenance.exchange.v1.Commitment:
    type: object
    properties:
      account:
        type: string
        description: account is the bech32 address string with the committed funds.
      market_id:
        type: integer
        format: int64
        description: >-
          market_id is the numeric identifier of the market the funds are
          committed to.
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          amount is the funds that have been committed by the account to the
          market.
    description: Commitment contains information on committed funds.
  provenance.exchange.v1.DenomSplit:
    type: object
    properties:
      denom:
        type: string
        description: denom is the coin denomination this split applies to.
      split:
        type: integer
        format: int64
        description: >-
          split is the proportion of fees the exchange receives for this denom
          in basis points.

          E.g. 100 = 1%. Min = 0, Max = 10000.
    description: >-
      DenomSplit associates a coin denomination with an amount the exchange
      receives for that denom.
  provenance.exchange.v1.FeeRatio:
    type: object
    properties:
      price:
        description: >-
          price is the unit the order price is divided by to get how much of the
          fee should apply.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      fee:
        description: fee is the amount to charge per price unit.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      FeeRatio defines a ratio of price amount to fee amount.

      For an order to be valid, its price must be evenly divisible by a
      FeeRatio's price.
  provenance.exchange.v1.Market:
    type: object
    properties:
      market_id:
        type: integer
        format: int64
        description: market_id is the numerical identifier for this market.
      market_details:
        description: market_details is some information about this market.
        type: object
        properties:
          name:
            type: string
            description: name is a moniker that people can use to refer to this market.
          description:
            type: string
            description: >-
              description extra information about this market. The field is
              meant to be human-readable.
          website_url:
            type: string
            description: >-
              website_url is a url people can use to get to this market, or at
              least get more information about this market.
          icon_uri:
            type: string
            description: icon_uri is a uri for an icon to associate with this market.
      fee_create_ask_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_create_ask_flat is the flat fee charged for creating an ask order.

          Each coin entry is a separate option. When an ask is created, one of
          these must be paid.

          If empty, no fee is required to create an ask order.
      fee_create_bid_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_create_bid_flat is the flat fee charged for creating a bid order.

          Each coin entry is a separate option. When a bid is created, one of
          these must be paid.

          If empty, no fee is required to create a bid order.
      fee_seller_settlement_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_seller_settlement_flat is the flat fee charged to the seller
          during settlement.

          Each coin entry is a separate option.

          When an ask is settled, the seller will pay the amount in the denom
          that matches the price they received.
      fee_seller_settlement_ratios:
        type: array
        items:
          type: object
          properties:
            price:
              description: >-
                price is the unit the order price is divided by to get how much
                of the fee should apply.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            fee:
              description: fee is the amount to charge per price unit.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            FeeRatio defines a ratio of price amount to fee amount.

            For an order to be valid, its price must be evenly divisible by a
            FeeRatio's price.
        description: >-
          fee_seller_settlement_ratios is the fee to charge a seller during
          settlement based on the price they are receiving.

          The price and fee denoms must be equal for each entry, and only one
          entry for any given denom is allowed.
      fee_buyer_settlement_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_buyer_settlement_flat is the flat fee charged to the buyer during
          settlement.

          Each coin entry is a separate option.

          When a bid is created, the settlement fees provided must contain one
          of these.
      fee_buyer_settlement_ratios:
        type: array
        items:
          type: object
          properties:
            price:
              description: >-
                price is the unit the order price is divided by to get how much
                of the fee should apply.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            fee:
              description: fee is the amount to charge per price unit.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            FeeRatio defines a ratio of price amount to fee amount.

            For an order to be valid, its price must be evenly divisible by a
            FeeRatio's price.
        description: >-
          fee_buyer_settlement_ratios is the fee to charge a buyer during
          settlement based on the price they are spending.

          The price and fee denoms do not have to equal. Multiple entries for
          any given price or fee denom are allowed, but

          each price denom to fee denom pair can only have one entry.
      accepting_orders:
        type: boolean
        description: >-
          accepting_orders is whether this market is allowing orders to be
          created for it.
      allow_user_settlement:
        type: boolean
        description: >-
          allow_user_settlement is whether this market allows users to initiate
          their own settlements.

          For example, the FillBids and FillAsks endpoints are available if and
          only if this is true.

          The MarketSettle endpoint is only available to market actors
          regardless of the value of this field.
      access_grants:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address is the address that these permissions apply to.
            permissions:
              type: array
              items:
                type: string
                enum:
                  - PERMISSION_UNSPECIFIED
                  - PERMISSION_SETTLE
                  - PERMISSION_SET_IDS
                  - PERMISSION_CANCEL
                  - PERMISSION_WITHDRAW
                  - PERMISSION_UPDATE
                  - PERMISSION_PERMISSIONS
                  - PERMISSION_ATTRIBUTES
                default: PERMISSION_UNSPECIFIED
                description: >-
                  Permission defines the different types of permission that can
                  be given to an account for a market.

                   - PERMISSION_UNSPECIFIED: PERMISSION_UNSPECIFIED is the zero-value Permission; it is an error to use it.
                   - PERMISSION_SETTLE: PERMISSION_SETTLE is the ability to use the Settle Tx endpoint on behalf of a market.
                   - PERMISSION_SET_IDS: PERMISSION_SET_IDS is the ability to use the SetOrderExternalID Tx endpoint on behalf of a market.
                   - PERMISSION_CANCEL: PERMISSION_CANCEL is the ability to use the Cancel Tx endpoint on behalf of a market.
                   - PERMISSION_WITHDRAW: PERMISSION_WITHDRAW is the ability to use the MarketWithdraw Tx endpoint.
                   - PERMISSION_UPDATE: PERMISSION_UPDATE is the ability to use the MarketUpdate* Tx endpoints.
                   - PERMISSION_PERMISSIONS: PERMISSION_PERMISSIONS is the ability to use the MarketManagePermissions Tx endpoint.
                   - PERMISSION_ATTRIBUTES: PERMISSION_ATTRIBUTES is the ability to use the MarketManageReqAttrs Tx endpoint.
              description: allowed is the list of permissions available for the address.
          description: >-
            AddrPermissions associates an address with a list of permissions
            available for that address.
        description: >-
          access_grants is the list of addresses and permissions granted for
          this market.
      req_attr_create_ask:
        type: array
        items:
          type: string
        description: >-
          req_attr_create_ask is a list of attributes required on an account for
          it to be allowed to create an ask order.

          An account must have all of these attributes in order to create an ask
          order in this market.

          If the list is empty, any account can create ask orders in this
          market.


          An entry that starts with "*." will match any attributes that end with
          the rest of it.

          E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a"; but
          not "b.a", "xb.a", "b.x.a", or "c.b.a.x".
      req_attr_create_bid:
        type: array
        items:
          type: string
        description: >-
          req_attr_create_ask is a list of attributes required on an account for
          it to be allowed to create a bid order.

          An account must have all of these attributes in order to create a bid
          order in this market.

          If the list is empty, any account can create bid orders in this
          market.


          An entry that starts with "*." will match any attributes that end with
          the rest of it.

          E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a"; but
          not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
      accepting_commitments:
        type: boolean
        description: >-
          accepting_commitments is whether the market is allowing users to
          commit funds to it.
      fee_create_commitment_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_create_commitment_flat is the flat fee charged for creating a
          commitment.

          Each coin entry is a separate option. When a commitment is created,
          one of these must be paid.

          If empty, no fee is required to create a commitment.
      commitment_settlement_bips:
        type: integer
        format: int64
        description: >-
          commitment_settlement_bips is the fraction of a commitment settlement
          that will be paid to the exchange.

          It is represented in basis points (1/100th of 1%, e.g. 0.0001) and is
          limited to 0 to 10,000 inclusive.

          During a commitment settlement, the inputs are summed and NAVs are
          used to convert that total to the

          intermediary denom, then to the fee denom. That is then multiplied by
          this value to get the fee amount

          that will be transferred out of the market's account into the exchange
          for that settlement.


          Summing the inputs effectively doubles the value of the settlement
          from what what is usually thought of

          as the value of a trade. That should be taken into account when
          setting this value.

          E.g. if two accounts are trading 10apples for 100grapes, the inputs
          total will be 10apples,100grapes

          (which might then be converted to USD then nhash before applying this
          ratio); Usually, though, the value

          of that trade would be viewed as either just 10apples or just
          100grapes.
      intermediary_denom:
        type: string
        description: >-
          intermediary_denom is the denom that funds get converted to (before
          being converted to the chain's fee denom)

          when calculating the fees that are paid to the exchange. NAVs are used
          for this conversion and actions will fail

          if a NAV is needed but not available.
      req_attr_create_commitment:
        type: array
        items:
          type: string
        description: >-
          req_attr_create_commitment is a list of attributes required on an
          account for it to be allowed to create a

          commitment. An account must have all of these attributes in order to
          create a commitment in this market.

          If the list is empty, any account can create commitments in this
          market.


          An entry that starts with "*." will match any attributes that end with
          the rest of it.

          E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a"; but
          not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
    description: Market contains all information about a market.
  provenance.exchange.v1.MarketAmount:
    type: object
    properties:
      market_id:
        type: integer
        format: int64
        description: market_id is the numeric identifier the amount has been committed to.
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount is the funds associated with the address.
    description: MarketAmount associates a market with a coins amount.
  provenance.exchange.v1.MarketBrief:
    type: object
    properties:
      market_id:
        type: integer
        format: int64
        description: market_id is the numerical identifier for this market.
      market_address:
        type: string
        description: market_address is the bech32 address string of this market's account.
      market_details:
        description: market_details is some information about this market.
        type: object
        properties:
          name:
            type: string
            description: name is a moniker that people can use to refer to this market.
          description:
            type: string
            description: >-
              description extra information about this market. The field is
              meant to be human-readable.
          website_url:
            type: string
            description: >-
              website_url is a url people can use to get to this market, or at
              least get more information about this market.
          icon_uri:
            type: string
            description: icon_uri is a uri for an icon to associate with this market.
    description: >-
      MarketBrief is a message containing brief, superficial information about a
      market.
  provenance.exchange.v1.MarketDetails:
    type: object
    properties:
      name:
        type: string
        description: name is a moniker that people can use to refer to this market.
      description:
        type: string
        description: >-
          description extra information about this market. The field is meant to
          be human-readable.
      website_url:
        type: string
        description: >-
          website_url is a url people can use to get to this market, or at least
          get more information about this market.
      icon_uri:
        type: string
        description: icon_uri is a uri for an icon to associate with this market.
    description: MarketDetails contains information about a market.
  provenance.exchange.v1.MsgGovCreateMarketRequest:
    type: object
    properties:
      authority:
        type: string
        description: authority should be the governance module account address.
      market:
        description: >-
          market is the initial market configuration.

          If the market_id is 0, the next available market_id will be used (once
          voting ends).

          If it is not zero, it must not yet be in use when the voting period
          ends.
        type: object
        properties:
          market_id:
            type: integer
            format: int64
            description: market_id is the numerical identifier for this market.
          market_details:
            description: market_details is some information about this market.
            type: object
            properties:
              name:
                type: string
                description: name is a moniker that people can use to refer to this market.
              description:
                type: string
                description: >-
                  description extra information about this market. The field is
                  meant to be human-readable.
              website_url:
                type: string
                description: >-
                  website_url is a url people can use to get to this market, or
                  at least get more information about this market.
              icon_uri:
                type: string
                description: icon_uri is a uri for an icon to associate with this market.
          fee_create_ask_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_ask_flat is the flat fee charged for creating an ask
              order.

              Each coin entry is a separate option. When an ask is created, one
              of these must be paid.

              If empty, no fee is required to create an ask order.
          fee_create_bid_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_bid_flat is the flat fee charged for creating a bid
              order.

              Each coin entry is a separate option. When a bid is created, one
              of these must be paid.

              If empty, no fee is required to create a bid order.
          fee_seller_settlement_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_seller_settlement_flat is the flat fee charged to the seller
              during settlement.

              Each coin entry is a separate option.

              When an ask is settled, the seller will pay the amount in the
              denom that matches the price they received.
          fee_seller_settlement_ratios:
            type: array
            items:
              type: object
              properties:
                price:
                  description: >-
                    price is the unit the order price is divided by to get how
                    much of the fee should apply.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                fee:
                  description: fee is the amount to charge per price unit.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
              description: >-
                FeeRatio defines a ratio of price amount to fee amount.

                For an order to be valid, its price must be evenly divisible by
                a FeeRatio's price.
            description: >-
              fee_seller_settlement_ratios is the fee to charge a seller during
              settlement based on the price they are receiving.

              The price and fee denoms must be equal for each entry, and only
              one entry for any given denom is allowed.
          fee_buyer_settlement_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_buyer_settlement_flat is the flat fee charged to the buyer
              during settlement.

              Each coin entry is a separate option.

              When a bid is created, the settlement fees provided must contain
              one of these.
          fee_buyer_settlement_ratios:
            type: array
            items:
              type: object
              properties:
                price:
                  description: >-
                    price is the unit the order price is divided by to get how
                    much of the fee should apply.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                fee:
                  description: fee is the amount to charge per price unit.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
              description: >-
                FeeRatio defines a ratio of price amount to fee amount.

                For an order to be valid, its price must be evenly divisible by
                a FeeRatio's price.
            description: >-
              fee_buyer_settlement_ratios is the fee to charge a buyer during
              settlement based on the price they are spending.

              The price and fee denoms do not have to equal. Multiple entries
              for any given price or fee denom are allowed, but

              each price denom to fee denom pair can only have one entry.
          accepting_orders:
            type: boolean
            description: >-
              accepting_orders is whether this market is allowing orders to be
              created for it.
          allow_user_settlement:
            type: boolean
            description: >-
              allow_user_settlement is whether this market allows users to
              initiate their own settlements.

              For example, the FillBids and FillAsks endpoints are available if
              and only if this is true.

              The MarketSettle endpoint is only available to market actors
              regardless of the value of this field.
          access_grants:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                  description: address is the address that these permissions apply to.
                permissions:
                  type: array
                  items:
                    type: string
                    enum:
                      - PERMISSION_UNSPECIFIED
                      - PERMISSION_SETTLE
                      - PERMISSION_SET_IDS
                      - PERMISSION_CANCEL
                      - PERMISSION_WITHDRAW
                      - PERMISSION_UPDATE
                      - PERMISSION_PERMISSIONS
                      - PERMISSION_ATTRIBUTES
                    default: PERMISSION_UNSPECIFIED
                    description: >-
                      Permission defines the different types of permission that
                      can be given to an account for a market.

                       - PERMISSION_UNSPECIFIED: PERMISSION_UNSPECIFIED is the zero-value Permission; it is an error to use it.
                       - PERMISSION_SETTLE: PERMISSION_SETTLE is the ability to use the Settle Tx endpoint on behalf of a market.
                       - PERMISSION_SET_IDS: PERMISSION_SET_IDS is the ability to use the SetOrderExternalID Tx endpoint on behalf of a market.
                       - PERMISSION_CANCEL: PERMISSION_CANCEL is the ability to use the Cancel Tx endpoint on behalf of a market.
                       - PERMISSION_WITHDRAW: PERMISSION_WITHDRAW is the ability to use the MarketWithdraw Tx endpoint.
                       - PERMISSION_UPDATE: PERMISSION_UPDATE is the ability to use the MarketUpdate* Tx endpoints.
                       - PERMISSION_PERMISSIONS: PERMISSION_PERMISSIONS is the ability to use the MarketManagePermissions Tx endpoint.
                       - PERMISSION_ATTRIBUTES: PERMISSION_ATTRIBUTES is the ability to use the MarketManageReqAttrs Tx endpoint.
                  description: >-
                    allowed is the list of permissions available for the
                    address.
              description: >-
                AddrPermissions associates an address with a list of permissions
                available for that address.
            description: >-
              access_grants is the list of addresses and permissions granted for
              this market.
          req_attr_create_ask:
            type: array
            items:
              type: string
            description: >-
              req_attr_create_ask is a list of attributes required on an account
              for it to be allowed to create an ask order.

              An account must have all of these attributes in order to create an
              ask order in this market.

              If the list is empty, any account can create ask orders in this
              market.


              An entry that starts with "*." will match any attributes that end
              with the rest of it.

              E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
              but not "b.a", "xb.a", "b.x.a", or "c.b.a.x".
          req_attr_create_bid:
            type: array
            items:
              type: string
            description: >-
              req_attr_create_ask is a list of attributes required on an account
              for it to be allowed to create a bid order.

              An account must have all of these attributes in order to create a
              bid order in this market.

              If the list is empty, any account can create bid orders in this
              market.


              An entry that starts with "*." will match any attributes that end
              with the rest of it.

              E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
              but not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
          accepting_commitments:
            type: boolean
            description: >-
              accepting_commitments is whether the market is allowing users to
              commit funds to it.
          fee_create_commitment_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_commitment_flat is the flat fee charged for creating a
              commitment.

              Each coin entry is a separate option. When a commitment is
              created, one of these must be paid.

              If empty, no fee is required to create a commitment.
          commitment_settlement_bips:
            type: integer
            format: int64
            description: >-
              commitment_settlement_bips is the fraction of a commitment
              settlement that will be paid to the exchange.

              It is represented in basis points (1/100th of 1%, e.g. 0.0001) and
              is limited to 0 to 10,000 inclusive.

              During a commitment settlement, the inputs are summed and NAVs are
              used to convert that total to the

              intermediary denom, then to the fee denom. That is then multiplied
              by this value to get the fee amount

              that will be transferred out of the market's account into the
              exchange for that settlement.


              Summing the inputs effectively doubles the value of the settlement
              from what what is usually thought of

              as the value of a trade. That should be taken into account when
              setting this value.

              E.g. if two accounts are trading 10apples for 100grapes, the
              inputs total will be 10apples,100grapes

              (which might then be converted to USD then nhash before applying
              this ratio); Usually, though, the value

              of that trade would be viewed as either just 10apples or just
              100grapes.
          intermediary_denom:
            type: string
            description: >-
              intermediary_denom is the denom that funds get converted to
              (before being converted to the chain's fee denom)

              when calculating the fees that are paid to the exchange. NAVs are
              used for this conversion and actions will fail

              if a NAV is needed but not available.
          req_attr_create_commitment:
            type: array
            items:
              type: string
            description: >-
              req_attr_create_commitment is a list of attributes required on an
              account for it to be allowed to create a

              commitment. An account must have all of these attributes in order
              to create a commitment in this market.

              If the list is empty, any account can create commitments in this
              market.


              An entry that starts with "*." will match any attributes that end
              with the rest of it.

              E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
              but not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
    description: >-
      MsgGovCreateMarketRequest is a request message for the GovCreateMarket
      endpoint.
  provenance.exchange.v1.MsgGovManageFeesRequest:
    type: object
    properties:
      authority:
        type: string
        description: authority should be the governance module account address.
      market_id:
        type: integer
        format: int64
        description: market_id is the market id that will get these fee updates.
      add_fee_create_ask_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: add_fee_create_ask_flat are the create-ask flat fee options to add.
      remove_fee_create_ask_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          remove_fee_create_ask_flat are the create-ask flat fee options to
          remove.
      add_fee_create_bid_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: add_fee_create_bid_flat are the create-bid flat fee options to add.
      remove_fee_create_bid_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          remove_fee_create_bid_flat are the create-bid flat fee options to
          remove.
      add_fee_seller_settlement_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          add_fee_seller_settlement_flat are the seller settlement flat fee
          options to add.
      remove_fee_seller_settlement_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          remove_fee_seller_settlement_flat are the seller settlement flat fee
          options to remove.
      add_fee_seller_settlement_ratios:
        type: array
        items:
          type: object
          properties:
            price:
              description: >-
                price is the unit the order price is divided by to get how much
                of the fee should apply.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            fee:
              description: fee is the amount to charge per price unit.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            FeeRatio defines a ratio of price amount to fee amount.

            For an order to be valid, its price must be evenly divisible by a
            FeeRatio's price.
        description: >-
          add_fee_seller_settlement_ratios are the seller settlement fee ratios
          to add.
      remove_fee_seller_settlement_ratios:
        type: array
        items:
          type: object
          properties:
            price:
              description: >-
                price is the unit the order price is divided by to get how much
                of the fee should apply.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            fee:
              description: fee is the amount to charge per price unit.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            FeeRatio defines a ratio of price amount to fee amount.

            For an order to be valid, its price must be evenly divisible by a
            FeeRatio's price.
        description: >-
          remove_fee_seller_settlement_ratios are the seller settlement fee
          ratios to remove.
      add_fee_buyer_settlement_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          add_fee_buyer_settlement_flat are the buyer settlement flat fee
          options to add.
      remove_fee_buyer_settlement_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          remove_fee_buyer_settlement_flat are the buyer settlement flat fee
          options to remove.
      add_fee_buyer_settlement_ratios:
        type: array
        items:
          type: object
          properties:
            price:
              description: >-
                price is the unit the order price is divided by to get how much
                of the fee should apply.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            fee:
              description: fee is the amount to charge per price unit.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            FeeRatio defines a ratio of price amount to fee amount.

            For an order to be valid, its price must be evenly divisible by a
            FeeRatio's price.
        description: >-
          add_fee_buyer_settlement_ratios are the buyer settlement fee ratios to
          add.
      remove_fee_buyer_settlement_ratios:
        type: array
        items:
          type: object
          properties:
            price:
              description: >-
                price is the unit the order price is divided by to get how much
                of the fee should apply.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            fee:
              description: fee is the amount to charge per price unit.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            FeeRatio defines a ratio of price amount to fee amount.

            For an order to be valid, its price must be evenly divisible by a
            FeeRatio's price.
        description: >-
          remove_fee_buyer_settlement_ratios are the buyer settlement fee ratios
          to remove.
      add_fee_create_commitment_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          add_fee_create_commitment_flat are the create-commitment flat fee
          options to add.
      remove_fee_create_commitment_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          remove_fee_create_commitment_flat are the create-commitment flat fee
          options to remove.
      set_fee_commitment_settlement_bips:
        type: integer
        format: int64
        description: >-
          set_fee_commitment_settlement_bips is the new
          fee_commitment_settlement_bips for the market.

          It is ignored if it is zero. To set it to zero set
          unset_fee_commitment_settlement_bips to true.
      unset_fee_commitment_settlement_bips:
        type: boolean
        description: >-
          unset_fee_commitment_settlement_bips, if true, sets the
          fee_commitment_settlement_bips to zero.

          If false, it is ignored.
    description: >-
      MsgGovManageFeesRequest is a request message for the GovManageFees
      endpoint.
  provenance.exchange.v1.MsgMarketCommitmentSettleRequest:
    type: object
    properties:
      admin:
        type: string
        description: >-
          admin is the account with "settle" permission requesting this
          settlement.
      market_id:
        type: integer
        format: int64
        description: >-
          market_id is the numerical identifier of the market requesting this
          settlement.
      inputs:
        type: array
        items:
          type: object
          properties:
            account:
              type: string
              description: >-
                account is the bech32 address string of the account associated
                with the amount.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount is the funds associated with the address.
          description: AccountAmount associates an account with a coins amount.
        description: >-
          inputs defines where the funds are coming from. All of these funds
          must be already committed to the market.
      outputs:
        type: array
        items:
          type: object
          properties:
            account:
              type: string
              description: >-
                account is the bech32 address string of the account associated
                with the amount.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount is the funds associated with the address.
          description: AccountAmount associates an account with a coins amount.
        description: >-
          outputs defines how the funds are to be distributed. These funds will
          be re-committed in the destination accounts.
      fees:
        type: array
        items:
          type: object
          properties:
            account:
              type: string
              description: >-
                account is the bech32 address string of the account associated
                with the amount.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount is the funds associated with the address.
          description: AccountAmount associates an account with a coins amount.
        description: >-
          fees is the funds that the market is collecting as part of this
          settlement.

          All of these funds must be already committed to the market.
      navs:
        type: array
        items:
          type: object
          properties:
            assets:
              description: assets is the volume and denom that has been bought or sold.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            price:
              description: price is what was paid for the assets.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            NetAssetPrice is an association of assets and price used to record
            the value of things.

            It is related to the NetAssetValue message from the x/marker module,
            and is therefore often referred to as "a NAV".
        description: >-
          navs are any NAV info that should be updated at the beginning of this
          settlement.
      event_tag:
        type: string
        description: >-
          event_tag is a string that is included in the funds-committed/released
          events. Max length is 100 characters.
    description: >-
      MsgMarketCommitmentSettleRequest is a request message for the
      MarketCommitmentSettle endpoint.
  provenance.exchange.v1.NetAssetPrice:
    type: object
    properties:
      assets:
        description: assets is the volume and denom that has been bought or sold.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      price:
        description: price is what was paid for the assets.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      NetAssetPrice is an association of assets and price used to record the
      value of things.

      It is related to the NetAssetValue message from the x/marker module, and
      is therefore often referred to as "a NAV".
  provenance.exchange.v1.Order:
    type: object
    properties:
      order_id:
        type: string
        format: uint64
        description: order_id is the numerical identifier for this order.
      ask_order:
        description: >-
          ask_order is the information about this order if it represents an ask
          order.
        type: object
        properties:
          market_id:
            type: integer
            format: int64
            description: market_id identifies the market that this order belongs to.
          seller:
            type: string
            description: >-
              seller is the address of the account that owns this order and has
              the assets to sell.
          assets:
            description: |-
              assets are the things that the seller wishes to sell.
              A hold is placed on this until the order is filled or cancelled.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          price:
            description: >-
              price is the minimum amount that the seller is willing to accept
              for the assets. The seller's settlement

              proportional fee (and possibly the settlement flat fee) is taken
              out of the amount the seller receives,

              so it's possible that the seller will still receive less than this
              price.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          seller_settlement_flat_fee:
            description: >-
              seller_settlement_flat_fee is the flat fee for sellers that will
              be charged during settlement. If this denom is the

              same denom as the price, it will come out of the actual price
              received. If this denom is different, the amount must

              be in the seller's account and a hold is placed on it until the
              order is filled or cancelled.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          allow_partial:
            type: boolean
            description: >-
              allow_partial should be true if partial fulfillment of this order
              should be allowed, and should be false if the

              order must be either filled in full or not filled at all.
          external_id:
            type: string
            description: >-
              external_id is an optional string used to externally identify this
              order. Max length is 100 characters.

              If an order in this market with this external id already exists,
              this order will be rejected.
      bid_order:
        description: >-
          bid_order is the information about this order if it represents a bid
          order.
        type: object
        properties:
          market_id:
            type: integer
            format: int64
            description: market_id identifies the market that this order belongs to.
          buyer:
            type: string
            description: >-
              buyer is the address of the account that owns this order and has
              the price to spend.
          assets:
            description: assets are the things that the buyer wishes to buy.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          price:
            description: |-
              price is the amount that the buyer will pay for the assets.
              A hold is placed on this until the order is filled or cancelled.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          buyer_settlement_fees:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              buyer_settlement_fees are the fees (both flat and proportional)
              that the buyer will pay (in addition to the price)

              when the order is settled. A hold is placed on this until the
              order is filled or cancelled.
          allow_partial:
            type: boolean
            description: >-
              allow_partial should be true if partial fulfillment of this order
              should be allowed, and should be false if the

              order must be either filled in full or not filled at all.
          external_id:
            type: string
            description: >-
              external_id is an optional string used to externally identify this
              order. Max length is 100 characters.

              If an order in this market with this external id already exists,
              this order will be rejected.
    description: Order associates an order id with one of the order types.
  provenance.exchange.v1.Params:
    type: object
    properties:
      default_split:
        type: integer
        format: int64
        description: >-
          default_split is the default proportion of fees the exchange receives
          in basis points.

          It is used if there isn't an applicable denom-specific split defined.

          E.g. 100 = 1%. Min = 0, Max = 10000.
      denom_splits:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
              description: denom is the coin denomination this split applies to.
            split:
              type: integer
              format: int64
              description: >-
                split is the proportion of fees the exchange receives for this
                denom in basis points.

                E.g. 100 = 1%. Min = 0, Max = 10000.
          description: >-
            DenomSplit associates a coin denomination with an amount the
            exchange receives for that denom.
        description: denom_splits are the denom-specific amounts the exchange receives.
      fee_create_payment_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_create_payment_flat is the flat fee options for creating a
          payment.

          If the source amount is not zero then one of these fee entries is
          required to create the payment.

          This field is currently limited to zero or one entries.
      fee_accept_payment_flat:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          fee_accept_payment_flat is the flat fee options for accepting a
          payment.

          If the target amount is not zero then one of these fee entries is
          required to accept the payment.

          This field is currently limited to zero or one entries.
    description: Params is a representation of the exchange module parameters.
  provenance.exchange.v1.Payment:
    type: object
    properties:
      source:
        type: string
        description: >-
          source is the account that created this Payment. It is considered the
          owner of the payment.
      source_amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: >-
          source_amount is the funds that the source is will pay the target in
          exchange for the target_amount.

          A hold will be placed on this amount in the source account until this
          Payment is accepted, rejected or cancelled.

          If the source_amount is zero, this Payment can be considered a
          "payment request."
      target:
        type: string
        description: |-
          target is the account that can accept this Payment.
          The target is the only thing allowed to change in a payment.
          I.e. it can be empty initially and updated later as needed.
      target_amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: >-
          target_amount is the funds that the target will pay the source in
          exchange for the source_amount.

          If the target_amount is zero, this Payment can be considered a
          "peer-to-peer (P2P) payment."
      external_id:
        type: string
        description: >-
          external_id is used along with the source to uniquely identify this
          Payment.


          A source can only have one Payment with any given external id.

          A source can have two payments with two different external ids.

          Two different sources can each have a payment with the same external
          id.

          But a source cannot have two different payments each with the same
          external id.


          An external id can be reused by a source once the payment is accepted,
          rejected, or cancelled.


          The external id is limited to 100 bytes. An empty string is a valid
          external id.
    description: >-
      Payment represents one account's desire to trade funds with another
      account.
  provenance.exchange.v1.Permission:
    type: string
    enum:
      - PERMISSION_UNSPECIFIED
      - PERMISSION_SETTLE
      - PERMISSION_SET_IDS
      - PERMISSION_CANCEL
      - PERMISSION_WITHDRAW
      - PERMISSION_UPDATE
      - PERMISSION_PERMISSIONS
      - PERMISSION_ATTRIBUTES
    default: PERMISSION_UNSPECIFIED
    description: >-
      Permission defines the different types of permission that can be given to
      an account for a market.

       - PERMISSION_UNSPECIFIED: PERMISSION_UNSPECIFIED is the zero-value Permission; it is an error to use it.
       - PERMISSION_SETTLE: PERMISSION_SETTLE is the ability to use the Settle Tx endpoint on behalf of a market.
       - PERMISSION_SET_IDS: PERMISSION_SET_IDS is the ability to use the SetOrderExternalID Tx endpoint on behalf of a market.
       - PERMISSION_CANCEL: PERMISSION_CANCEL is the ability to use the Cancel Tx endpoint on behalf of a market.
       - PERMISSION_WITHDRAW: PERMISSION_WITHDRAW is the ability to use the MarketWithdraw Tx endpoint.
       - PERMISSION_UPDATE: PERMISSION_UPDATE is the ability to use the MarketUpdate* Tx endpoints.
       - PERMISSION_PERMISSIONS: PERMISSION_PERMISSIONS is the ability to use the MarketManagePermissions Tx endpoint.
       - PERMISSION_ATTRIBUTES: PERMISSION_ATTRIBUTES is the ability to use the MarketManageReqAttrs Tx endpoint.
  provenance.exchange.v1.QueryCommitmentSettlementFeeCalcResponse:
    type: object
    properties:
      exchange_fees:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          exchange_fees is the total that the exchange would currently pay for
          the provided settlement.
      input_total:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: input_total is the sum of all the inputs in the provided settlement.
      converted_total:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          converted_total is the input_total converted to a single intermediary
          denom or left as the fee denom.
      conversion_navs:
        type: array
        items:
          type: object
          properties:
            assets:
              description: assets is the volume and denom that has been bought or sold.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            price:
              description: price is what was paid for the assets.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            NetAssetPrice is an association of assets and price used to record
            the value of things.

            It is related to the NetAssetValue message from the x/marker module,
            and is therefore often referred to as "a NAV".
        description: >-
          conversion_navs are the NAVs used to convert the input_total to the
          converted_total.
      to_fee_nav:
        description: >-
          to_fee_nav is the NAV used to convert the converted_total into the fee
          denom.
        type: object
        properties:
          assets:
            description: assets is the volume and denom that has been bought or sold.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          price:
            description: price is what was paid for the assets.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
    description: >-
      QueryCommitmentSettlementFeeCalcResponse is a response message for the
      CommitmentSettlementFeeCalc query.
  provenance.exchange.v1.QueryGetAccountCommitmentsResponse:
    type: object
    properties:
      commitments:
        type: array
        items:
          type: object
          properties:
            market_id:
              type: integer
              format: int64
              description: >-
                market_id is the numeric identifier the amount has been
                committed to.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount is the funds associated with the address.
          description: MarketAmount associates a market with a coins amount.
        description: >-
          commitments is the amounts committed from the account to the any
          market.
    description: >-
      QueryGetAccountCommitmentsResponse is a response message for the
      GetAccountCommitments query.
  provenance.exchange.v1.QueryGetAllCommitmentsResponse:
    type: object
    properties:
      commitments:
        type: array
        items:
          type: object
          properties:
            account:
              type: string
              description: account is the bech32 address string with the committed funds.
            market_id:
              type: integer
              format: int64
              description: >-
                market_id is the numeric identifier of the market the funds are
                committed to.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: >-
                amount is the funds that have been committed by the account to
                the market.
          description: Commitment contains information on committed funds.
        description: commitments is the requested commitment information.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetAllCommitmentsResponse is a response message for the
      GetAllCommitments query.
  provenance.exchange.v1.QueryGetAllMarketsResponse:
    type: object
    properties:
      markets:
        type: array
        items:
          type: object
          properties:
            market_id:
              type: integer
              format: int64
              description: market_id is the numerical identifier for this market.
            market_address:
              type: string
              description: >-
                market_address is the bech32 address string of this market's
                account.
            market_details:
              description: market_details is some information about this market.
              type: object
              properties:
                name:
                  type: string
                  description: >-
                    name is a moniker that people can use to refer to this
                    market.
                description:
                  type: string
                  description: >-
                    description extra information about this market. The field
                    is meant to be human-readable.
                website_url:
                  type: string
                  description: >-
                    website_url is a url people can use to get to this market,
                    or at least get more information about this market.
                icon_uri:
                  type: string
                  description: icon_uri is a uri for an icon to associate with this market.
          description: >-
            MarketBrief is a message containing brief, superficial information
            about a market.
        description: markets are a page of the briefs for all markets.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetAllMarketsResponse is a response message for the GetAllMarkets
      query.
  provenance.exchange.v1.QueryGetAllOrdersResponse:
    type: object
    properties:
      orders:
        type: array
        items:
          type: object
          properties:
            order_id:
              type: string
              format: uint64
              description: order_id is the numerical identifier for this order.
            ask_order:
              description: >-
                ask_order is the information about this order if it represents
                an ask order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                seller:
                  type: string
                  description: >-
                    seller is the address of the account that owns this order
                    and has the assets to sell.
                assets:
                  description: >-
                    assets are the things that the seller wishes to sell.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the minimum amount that the seller is willing to
                    accept for the assets. The seller's settlement

                    proportional fee (and possibly the settlement flat fee) is
                    taken out of the amount the seller receives,

                    so it's possible that the seller will still receive less
                    than this price.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                seller_settlement_flat_fee:
                  description: >-
                    seller_settlement_flat_fee is the flat fee for sellers that
                    will be charged during settlement. If this denom is the

                    same denom as the price, it will come out of the actual
                    price received. If this denom is different, the amount must

                    be in the seller's account and a hold is placed on it until
                    the order is filled or cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
            bid_order:
              description: >-
                bid_order is the information about this order if it represents a
                bid order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                buyer:
                  type: string
                  description: >-
                    buyer is the address of the account that owns this order and
                    has the price to spend.
                assets:
                  description: assets are the things that the buyer wishes to buy.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the amount that the buyer will pay for the assets.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                buyer_settlement_fees:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  description: >-
                    buyer_settlement_fees are the fees (both flat and
                    proportional) that the buyer will pay (in addition to the
                    price)

                    when the order is settled. A hold is placed on this until
                    the order is filled or cancelled.
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
          description: Order associates an order id with one of the order types.
        description: orders are a page of the all orders.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetAllOrdersResponse is a response message for the GetAllOrders
      query.
  provenance.exchange.v1.QueryGetAllPaymentsResponse:
    type: object
    properties:
      payments:
        type: array
        items:
          type: object
          properties:
            source:
              type: string
              description: >-
                source is the account that created this Payment. It is
                considered the owner of the payment.
            source_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              title: >-
                source_amount is the funds that the source is will pay the
                target in exchange for the target_amount.

                A hold will be placed on this amount in the source account until
                this Payment is accepted, rejected or cancelled.

                If the source_amount is zero, this Payment can be considered a
                "payment request."
            target:
              type: string
              description: |-
                target is the account that can accept this Payment.
                The target is the only thing allowed to change in a payment.
                I.e. it can be empty initially and updated later as needed.
            target_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              title: >-
                target_amount is the funds that the target will pay the source
                in exchange for the source_amount.

                If the target_amount is zero, this Payment can be considered a
                "peer-to-peer (P2P) payment."
            external_id:
              type: string
              description: >-
                external_id is used along with the source to uniquely identify
                this Payment.


                A source can only have one Payment with any given external id.

                A source can have two payments with two different external ids.

                Two different sources can each have a payment with the same
                external id.

                But a source cannot have two different payments each with the
                same external id.


                An external id can be reused by a source once the payment is
                accepted, rejected, or cancelled.


                The external id is limited to 100 bytes. An empty string is a
                valid external id.
          description: >-
            Payment represents one account's desire to trade funds with another
            account.
        description: payments is all the payments on this page of results.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetAllPaymentsResponse is a response message for the GetAllPayments
      query.
  provenance.exchange.v1.QueryGetAssetOrdersResponse:
    type: object
    properties:
      orders:
        type: array
        items:
          type: object
          properties:
            order_id:
              type: string
              format: uint64
              description: order_id is the numerical identifier for this order.
            ask_order:
              description: >-
                ask_order is the information about this order if it represents
                an ask order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                seller:
                  type: string
                  description: >-
                    seller is the address of the account that owns this order
                    and has the assets to sell.
                assets:
                  description: >-
                    assets are the things that the seller wishes to sell.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the minimum amount that the seller is willing to
                    accept for the assets. The seller's settlement

                    proportional fee (and possibly the settlement flat fee) is
                    taken out of the amount the seller receives,

                    so it's possible that the seller will still receive less
                    than this price.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                seller_settlement_flat_fee:
                  description: >-
                    seller_settlement_flat_fee is the flat fee for sellers that
                    will be charged during settlement. If this denom is the

                    same denom as the price, it will come out of the actual
                    price received. If this denom is different, the amount must

                    be in the seller's account and a hold is placed on it until
                    the order is filled or cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
            bid_order:
              description: >-
                bid_order is the information about this order if it represents a
                bid order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                buyer:
                  type: string
                  description: >-
                    buyer is the address of the account that owns this order and
                    has the price to spend.
                assets:
                  description: assets are the things that the buyer wishes to buy.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the amount that the buyer will pay for the assets.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                buyer_settlement_fees:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  description: >-
                    buyer_settlement_fees are the fees (both flat and
                    proportional) that the buyer will pay (in addition to the
                    price)

                    when the order is settled. A hold is placed on this until
                    the order is filled or cancelled.
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
          description: Order associates an order id with one of the order types.
        description: orders are a page of the orders for the provided asset.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetAssetOrdersResponse is a response message for the GetAssetOrders
      query.
  provenance.exchange.v1.QueryGetCommitmentResponse:
    type: object
    properties:
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount is the total funds committed to the market by the account.
    description: >-
      QueryGetCommitmentResponse is a response message for the GetCommitment
      query.
  provenance.exchange.v1.QueryGetMarketCommitmentsResponse:
    type: object
    properties:
      commitments:
        type: array
        items:
          type: object
          properties:
            account:
              type: string
              description: >-
                account is the bech32 address string of the account associated
                with the amount.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount is the funds associated with the address.
          description: AccountAmount associates an account with a coins amount.
        description: commitments is the amounts committed to the market from any account.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetMarketCommitmentsResponse is a response message for the
      GetMarketCommitments query.
  provenance.exchange.v1.QueryGetMarketOrdersResponse:
    type: object
    properties:
      orders:
        type: array
        items:
          type: object
          properties:
            order_id:
              type: string
              format: uint64
              description: order_id is the numerical identifier for this order.
            ask_order:
              description: >-
                ask_order is the information about this order if it represents
                an ask order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                seller:
                  type: string
                  description: >-
                    seller is the address of the account that owns this order
                    and has the assets to sell.
                assets:
                  description: >-
                    assets are the things that the seller wishes to sell.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the minimum amount that the seller is willing to
                    accept for the assets. The seller's settlement

                    proportional fee (and possibly the settlement flat fee) is
                    taken out of the amount the seller receives,

                    so it's possible that the seller will still receive less
                    than this price.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                seller_settlement_flat_fee:
                  description: >-
                    seller_settlement_flat_fee is the flat fee for sellers that
                    will be charged during settlement. If this denom is the

                    same denom as the price, it will come out of the actual
                    price received. If this denom is different, the amount must

                    be in the seller's account and a hold is placed on it until
                    the order is filled or cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
            bid_order:
              description: >-
                bid_order is the information about this order if it represents a
                bid order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                buyer:
                  type: string
                  description: >-
                    buyer is the address of the account that owns this order and
                    has the price to spend.
                assets:
                  description: assets are the things that the buyer wishes to buy.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the amount that the buyer will pay for the assets.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                buyer_settlement_fees:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  description: >-
                    buyer_settlement_fees are the fees (both flat and
                    proportional) that the buyer will pay (in addition to the
                    price)

                    when the order is settled. A hold is placed on this until
                    the order is filled or cancelled.
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
          description: Order associates an order id with one of the order types.
        description: orders are a page of the orders in the provided market.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetMarketOrdersResponse is a response message for the GetMarketOrders
      query.
  provenance.exchange.v1.QueryGetMarketResponse:
    type: object
    properties:
      address:
        type: string
        description: address is the bech32 address string of this market's account.
      market:
        description: market is all information and details of the market.
        type: object
        properties:
          market_id:
            type: integer
            format: int64
            description: market_id is the numerical identifier for this market.
          market_details:
            description: market_details is some information about this market.
            type: object
            properties:
              name:
                type: string
                description: name is a moniker that people can use to refer to this market.
              description:
                type: string
                description: >-
                  description extra information about this market. The field is
                  meant to be human-readable.
              website_url:
                type: string
                description: >-
                  website_url is a url people can use to get to this market, or
                  at least get more information about this market.
              icon_uri:
                type: string
                description: icon_uri is a uri for an icon to associate with this market.
          fee_create_ask_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_ask_flat is the flat fee charged for creating an ask
              order.

              Each coin entry is a separate option. When an ask is created, one
              of these must be paid.

              If empty, no fee is required to create an ask order.
          fee_create_bid_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_bid_flat is the flat fee charged for creating a bid
              order.

              Each coin entry is a separate option. When a bid is created, one
              of these must be paid.

              If empty, no fee is required to create a bid order.
          fee_seller_settlement_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_seller_settlement_flat is the flat fee charged to the seller
              during settlement.

              Each coin entry is a separate option.

              When an ask is settled, the seller will pay the amount in the
              denom that matches the price they received.
          fee_seller_settlement_ratios:
            type: array
            items:
              type: object
              properties:
                price:
                  description: >-
                    price is the unit the order price is divided by to get how
                    much of the fee should apply.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                fee:
                  description: fee is the amount to charge per price unit.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
              description: >-
                FeeRatio defines a ratio of price amount to fee amount.

                For an order to be valid, its price must be evenly divisible by
                a FeeRatio's price.
            description: >-
              fee_seller_settlement_ratios is the fee to charge a seller during
              settlement based on the price they are receiving.

              The price and fee denoms must be equal for each entry, and only
              one entry for any given denom is allowed.
          fee_buyer_settlement_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_buyer_settlement_flat is the flat fee charged to the buyer
              during settlement.

              Each coin entry is a separate option.

              When a bid is created, the settlement fees provided must contain
              one of these.
          fee_buyer_settlement_ratios:
            type: array
            items:
              type: object
              properties:
                price:
                  description: >-
                    price is the unit the order price is divided by to get how
                    much of the fee should apply.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                fee:
                  description: fee is the amount to charge per price unit.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
              description: >-
                FeeRatio defines a ratio of price amount to fee amount.

                For an order to be valid, its price must be evenly divisible by
                a FeeRatio's price.
            description: >-
              fee_buyer_settlement_ratios is the fee to charge a buyer during
              settlement based on the price they are spending.

              The price and fee denoms do not have to equal. Multiple entries
              for any given price or fee denom are allowed, but

              each price denom to fee denom pair can only have one entry.
          accepting_orders:
            type: boolean
            description: >-
              accepting_orders is whether this market is allowing orders to be
              created for it.
          allow_user_settlement:
            type: boolean
            description: >-
              allow_user_settlement is whether this market allows users to
              initiate their own settlements.

              For example, the FillBids and FillAsks endpoints are available if
              and only if this is true.

              The MarketSettle endpoint is only available to market actors
              regardless of the value of this field.
          access_grants:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                  description: address is the address that these permissions apply to.
                permissions:
                  type: array
                  items:
                    type: string
                    enum:
                      - PERMISSION_UNSPECIFIED
                      - PERMISSION_SETTLE
                      - PERMISSION_SET_IDS
                      - PERMISSION_CANCEL
                      - PERMISSION_WITHDRAW
                      - PERMISSION_UPDATE
                      - PERMISSION_PERMISSIONS
                      - PERMISSION_ATTRIBUTES
                    default: PERMISSION_UNSPECIFIED
                    description: >-
                      Permission defines the different types of permission that
                      can be given to an account for a market.

                       - PERMISSION_UNSPECIFIED: PERMISSION_UNSPECIFIED is the zero-value Permission; it is an error to use it.
                       - PERMISSION_SETTLE: PERMISSION_SETTLE is the ability to use the Settle Tx endpoint on behalf of a market.
                       - PERMISSION_SET_IDS: PERMISSION_SET_IDS is the ability to use the SetOrderExternalID Tx endpoint on behalf of a market.
                       - PERMISSION_CANCEL: PERMISSION_CANCEL is the ability to use the Cancel Tx endpoint on behalf of a market.
                       - PERMISSION_WITHDRAW: PERMISSION_WITHDRAW is the ability to use the MarketWithdraw Tx endpoint.
                       - PERMISSION_UPDATE: PERMISSION_UPDATE is the ability to use the MarketUpdate* Tx endpoints.
                       - PERMISSION_PERMISSIONS: PERMISSION_PERMISSIONS is the ability to use the MarketManagePermissions Tx endpoint.
                       - PERMISSION_ATTRIBUTES: PERMISSION_ATTRIBUTES is the ability to use the MarketManageReqAttrs Tx endpoint.
                  description: >-
                    allowed is the list of permissions available for the
                    address.
              description: >-
                AddrPermissions associates an address with a list of permissions
                available for that address.
            description: >-
              access_grants is the list of addresses and permissions granted for
              this market.
          req_attr_create_ask:
            type: array
            items:
              type: string
            description: >-
              req_attr_create_ask is a list of attributes required on an account
              for it to be allowed to create an ask order.

              An account must have all of these attributes in order to create an
              ask order in this market.

              If the list is empty, any account can create ask orders in this
              market.


              An entry that starts with "*." will match any attributes that end
              with the rest of it.

              E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
              but not "b.a", "xb.a", "b.x.a", or "c.b.a.x".
          req_attr_create_bid:
            type: array
            items:
              type: string
            description: >-
              req_attr_create_ask is a list of attributes required on an account
              for it to be allowed to create a bid order.

              An account must have all of these attributes in order to create a
              bid order in this market.

              If the list is empty, any account can create bid orders in this
              market.


              An entry that starts with "*." will match any attributes that end
              with the rest of it.

              E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
              but not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
          accepting_commitments:
            type: boolean
            description: >-
              accepting_commitments is whether the market is allowing users to
              commit funds to it.
          fee_create_commitment_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_commitment_flat is the flat fee charged for creating a
              commitment.

              Each coin entry is a separate option. When a commitment is
              created, one of these must be paid.

              If empty, no fee is required to create a commitment.
          commitment_settlement_bips:
            type: integer
            format: int64
            description: >-
              commitment_settlement_bips is the fraction of a commitment
              settlement that will be paid to the exchange.

              It is represented in basis points (1/100th of 1%, e.g. 0.0001) and
              is limited to 0 to 10,000 inclusive.

              During a commitment settlement, the inputs are summed and NAVs are
              used to convert that total to the

              intermediary denom, then to the fee denom. That is then multiplied
              by this value to get the fee amount

              that will be transferred out of the market's account into the
              exchange for that settlement.


              Summing the inputs effectively doubles the value of the settlement
              from what what is usually thought of

              as the value of a trade. That should be taken into account when
              setting this value.

              E.g. if two accounts are trading 10apples for 100grapes, the
              inputs total will be 10apples,100grapes

              (which might then be converted to USD then nhash before applying
              this ratio); Usually, though, the value

              of that trade would be viewed as either just 10apples or just
              100grapes.
          intermediary_denom:
            type: string
            description: >-
              intermediary_denom is the denom that funds get converted to
              (before being converted to the chain's fee denom)

              when calculating the fees that are paid to the exchange. NAVs are
              used for this conversion and actions will fail

              if a NAV is needed but not available.
          req_attr_create_commitment:
            type: array
            items:
              type: string
            description: >-
              req_attr_create_commitment is a list of attributes required on an
              account for it to be allowed to create a

              commitment. An account must have all of these attributes in order
              to create a commitment in this market.

              If the list is empty, any account can create commitments in this
              market.


              An entry that starts with "*." will match any attributes that end
              with the rest of it.

              E.g. "*.b.a" will match all of "c.b.a", "x.b.a", and "e.d.c.b.a";
              but not "b.a", "xb.a", "c.b.x.a", or "c.b.a.x".
    description: QueryGetMarketResponse is a response message for the GetMarket query.
  provenance.exchange.v1.QueryGetOrderByExternalIDResponse:
    type: object
    properties:
      order:
        description: order is the requested order.
        type: object
        properties:
          order_id:
            type: string
            format: uint64
            description: order_id is the numerical identifier for this order.
          ask_order:
            description: >-
              ask_order is the information about this order if it represents an
              ask order.
            type: object
            properties:
              market_id:
                type: integer
                format: int64
                description: market_id identifies the market that this order belongs to.
              seller:
                type: string
                description: >-
                  seller is the address of the account that owns this order and
                  has the assets to sell.
              assets:
                description: >-
                  assets are the things that the seller wishes to sell.

                  A hold is placed on this until the order is filled or
                  cancelled.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              price:
                description: >-
                  price is the minimum amount that the seller is willing to
                  accept for the assets. The seller's settlement

                  proportional fee (and possibly the settlement flat fee) is
                  taken out of the amount the seller receives,

                  so it's possible that the seller will still receive less than
                  this price.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              seller_settlement_flat_fee:
                description: >-
                  seller_settlement_flat_fee is the flat fee for sellers that
                  will be charged during settlement. If this denom is the

                  same denom as the price, it will come out of the actual price
                  received. If this denom is different, the amount must

                  be in the seller's account and a hold is placed on it until
                  the order is filled or cancelled.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              allow_partial:
                type: boolean
                description: >-
                  allow_partial should be true if partial fulfillment of this
                  order should be allowed, and should be false if the

                  order must be either filled in full or not filled at all.
              external_id:
                type: string
                description: >-
                  external_id is an optional string used to externally identify
                  this order. Max length is 100 characters.

                  If an order in this market with this external id already
                  exists, this order will be rejected.
          bid_order:
            description: >-
              bid_order is the information about this order if it represents a
              bid order.
            type: object
            properties:
              market_id:
                type: integer
                format: int64
                description: market_id identifies the market that this order belongs to.
              buyer:
                type: string
                description: >-
                  buyer is the address of the account that owns this order and
                  has the price to spend.
              assets:
                description: assets are the things that the buyer wishes to buy.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              price:
                description: >-
                  price is the amount that the buyer will pay for the assets.

                  A hold is placed on this until the order is filled or
                  cancelled.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              buyer_settlement_fees:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  buyer_settlement_fees are the fees (both flat and
                  proportional) that the buyer will pay (in addition to the
                  price)

                  when the order is settled. A hold is placed on this until the
                  order is filled or cancelled.
              allow_partial:
                type: boolean
                description: >-
                  allow_partial should be true if partial fulfillment of this
                  order should be allowed, and should be false if the

                  order must be either filled in full or not filled at all.
              external_id:
                type: string
                description: >-
                  external_id is an optional string used to externally identify
                  this order. Max length is 100 characters.

                  If an order in this market with this external id already
                  exists, this order will be rejected.
    description: >-
      QueryGetOrderByExternalIDResponse is a response message for the
      GetOrderByExternalID query.
  provenance.exchange.v1.QueryGetOrderResponse:
    type: object
    properties:
      order:
        description: order is the requested order.
        type: object
        properties:
          order_id:
            type: string
            format: uint64
            description: order_id is the numerical identifier for this order.
          ask_order:
            description: >-
              ask_order is the information about this order if it represents an
              ask order.
            type: object
            properties:
              market_id:
                type: integer
                format: int64
                description: market_id identifies the market that this order belongs to.
              seller:
                type: string
                description: >-
                  seller is the address of the account that owns this order and
                  has the assets to sell.
              assets:
                description: >-
                  assets are the things that the seller wishes to sell.

                  A hold is placed on this until the order is filled or
                  cancelled.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              price:
                description: >-
                  price is the minimum amount that the seller is willing to
                  accept for the assets. The seller's settlement

                  proportional fee (and possibly the settlement flat fee) is
                  taken out of the amount the seller receives,

                  so it's possible that the seller will still receive less than
                  this price.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              seller_settlement_flat_fee:
                description: >-
                  seller_settlement_flat_fee is the flat fee for sellers that
                  will be charged during settlement. If this denom is the

                  same denom as the price, it will come out of the actual price
                  received. If this denom is different, the amount must

                  be in the seller's account and a hold is placed on it until
                  the order is filled or cancelled.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              allow_partial:
                type: boolean
                description: >-
                  allow_partial should be true if partial fulfillment of this
                  order should be allowed, and should be false if the

                  order must be either filled in full or not filled at all.
              external_id:
                type: string
                description: >-
                  external_id is an optional string used to externally identify
                  this order. Max length is 100 characters.

                  If an order in this market with this external id already
                  exists, this order will be rejected.
          bid_order:
            description: >-
              bid_order is the information about this order if it represents a
              bid order.
            type: object
            properties:
              market_id:
                type: integer
                format: int64
                description: market_id identifies the market that this order belongs to.
              buyer:
                type: string
                description: >-
                  buyer is the address of the account that owns this order and
                  has the price to spend.
              assets:
                description: assets are the things that the buyer wishes to buy.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              price:
                description: >-
                  price is the amount that the buyer will pay for the assets.

                  A hold is placed on this until the order is filled or
                  cancelled.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              buyer_settlement_fees:
                type: array
                items:
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                  description: >-
                    Coin defines a token with a denomination and an amount.


                    NOTE: The amount field is an Int which implements the custom
                    method

                    signatures required by gogoproto.
                description: >-
                  buyer_settlement_fees are the fees (both flat and
                  proportional) that the buyer will pay (in addition to the
                  price)

                  when the order is settled. A hold is placed on this until the
                  order is filled or cancelled.
              allow_partial:
                type: boolean
                description: >-
                  allow_partial should be true if partial fulfillment of this
                  order should be allowed, and should be false if the

                  order must be either filled in full or not filled at all.
              external_id:
                type: string
                description: >-
                  external_id is an optional string used to externally identify
                  this order. Max length is 100 characters.

                  If an order in this market with this external id already
                  exists, this order will be rejected.
    description: QueryGetOrderResponse is a response message for the GetOrder query.
  provenance.exchange.v1.QueryGetOwnerOrdersResponse:
    type: object
    properties:
      orders:
        type: array
        items:
          type: object
          properties:
            order_id:
              type: string
              format: uint64
              description: order_id is the numerical identifier for this order.
            ask_order:
              description: >-
                ask_order is the information about this order if it represents
                an ask order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                seller:
                  type: string
                  description: >-
                    seller is the address of the account that owns this order
                    and has the assets to sell.
                assets:
                  description: >-
                    assets are the things that the seller wishes to sell.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the minimum amount that the seller is willing to
                    accept for the assets. The seller's settlement

                    proportional fee (and possibly the settlement flat fee) is
                    taken out of the amount the seller receives,

                    so it's possible that the seller will still receive less
                    than this price.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                seller_settlement_flat_fee:
                  description: >-
                    seller_settlement_flat_fee is the flat fee for sellers that
                    will be charged during settlement. If this denom is the

                    same denom as the price, it will come out of the actual
                    price received. If this denom is different, the amount must

                    be in the seller's account and a hold is placed on it until
                    the order is filled or cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
            bid_order:
              description: >-
                bid_order is the information about this order if it represents a
                bid order.
              type: object
              properties:
                market_id:
                  type: integer
                  format: int64
                  description: market_id identifies the market that this order belongs to.
                buyer:
                  type: string
                  description: >-
                    buyer is the address of the account that owns this order and
                    has the price to spend.
                assets:
                  description: assets are the things that the buyer wishes to buy.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                price:
                  description: >-
                    price is the amount that the buyer will pay for the assets.

                    A hold is placed on this until the order is filled or
                    cancelled.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                buyer_settlement_fees:
                  type: array
                  items:
                    type: object
                    properties:
                      denom:
                        type: string
                      amount:
                        type: string
                    description: >-
                      Coin defines a token with a denomination and an amount.


                      NOTE: The amount field is an Int which implements the
                      custom method

                      signatures required by gogoproto.
                  description: >-
                    buyer_settlement_fees are the fees (both flat and
                    proportional) that the buyer will pay (in addition to the
                    price)

                    when the order is settled. A hold is placed on this until
                    the order is filled or cancelled.
                allow_partial:
                  type: boolean
                  description: >-
                    allow_partial should be true if partial fulfillment of this
                    order should be allowed, and should be false if the

                    order must be either filled in full or not filled at all.
                external_id:
                  type: string
                  description: >-
                    external_id is an optional string used to externally
                    identify this order. Max length is 100 characters.

                    If an order in this market with this external id already
                    exists, this order will be rejected.
          description: Order associates an order id with one of the order types.
        description: orders are a page of the orders for the provided address.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetOwnerOrdersResponse is a response message for the GetOwnerOrders
      query.
  provenance.exchange.v1.QueryGetPaymentResponse:
    type: object
    properties:
      payment:
        description: payment is the info on the requested payment.
        type: object
        properties:
          source:
            type: string
            description: >-
              source is the account that created this Payment. It is considered
              the owner of the payment.
          source_amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: >-
              source_amount is the funds that the source is will pay the target
              in exchange for the target_amount.

              A hold will be placed on this amount in the source account until
              this Payment is accepted, rejected or cancelled.

              If the source_amount is zero, this Payment can be considered a
              "payment request."
          target:
            type: string
            description: |-
              target is the account that can accept this Payment.
              The target is the only thing allowed to change in a payment.
              I.e. it can be empty initially and updated later as needed.
          target_amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: >-
              target_amount is the funds that the target will pay the source in
              exchange for the source_amount.

              If the target_amount is zero, this Payment can be considered a
              "peer-to-peer (P2P) payment."
          external_id:
            type: string
            description: >-
              external_id is used along with the source to uniquely identify
              this Payment.


              A source can only have one Payment with any given external id.

              A source can have two payments with two different external ids.

              Two different sources can each have a payment with the same
              external id.

              But a source cannot have two different payments each with the same
              external id.


              An external id can be reused by a source once the payment is
              accepted, rejected, or cancelled.


              The external id is limited to 100 bytes. An empty string is a
              valid external id.
    description: QueryGetPaymentResponse is a response message for the GetPayment query.
  provenance.exchange.v1.QueryGetPaymentsWithSourceResponse:
    type: object
    properties:
      payments:
        type: array
        items:
          type: object
          properties:
            source:
              type: string
              description: >-
                source is the account that created this Payment. It is
                considered the owner of the payment.
            source_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              title: >-
                source_amount is the funds that the source is will pay the
                target in exchange for the target_amount.

                A hold will be placed on this amount in the source account until
                this Payment is accepted, rejected or cancelled.

                If the source_amount is zero, this Payment can be considered a
                "payment request."
            target:
              type: string
              description: |-
                target is the account that can accept this Payment.
                The target is the only thing allowed to change in a payment.
                I.e. it can be empty initially and updated later as needed.
            target_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              title: >-
                target_amount is the funds that the target will pay the source
                in exchange for the source_amount.

                If the target_amount is zero, this Payment can be considered a
                "peer-to-peer (P2P) payment."
            external_id:
              type: string
              description: >-
                external_id is used along with the source to uniquely identify
                this Payment.


                A source can only have one Payment with any given external id.

                A source can have two payments with two different external ids.

                Two different sources can each have a payment with the same
                external id.

                But a source cannot have two different payments each with the
                same external id.


                An external id can be reused by a source once the payment is
                accepted, rejected, or cancelled.


                The external id is limited to 100 bytes. An empty string is a
                valid external id.
          description: >-
            Payment represents one account's desire to trade funds with another
            account.
        description: payments is all the payments with the requested source.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetPaymentsWithSourceResponse is a response message for the
      GetPaymentsWithSource query.
  provenance.exchange.v1.QueryGetPaymentsWithTargetResponse:
    type: object
    properties:
      payments:
        type: array
        items:
          type: object
          properties:
            source:
              type: string
              description: >-
                source is the account that created this Payment. It is
                considered the owner of the payment.
            source_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              title: >-
                source_amount is the funds that the source is will pay the
                target in exchange for the target_amount.

                A hold will be placed on this amount in the source account until
                this Payment is accepted, rejected or cancelled.

                If the source_amount is zero, this Payment can be considered a
                "payment request."
            target:
              type: string
              description: |-
                target is the account that can accept this Payment.
                The target is the only thing allowed to change in a payment.
                I.e. it can be empty initially and updated later as needed.
            target_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              title: >-
                target_amount is the funds that the target will pay the source
                in exchange for the source_amount.

                If the target_amount is zero, this Payment can be considered a
                "peer-to-peer (P2P) payment."
            external_id:
              type: string
              description: >-
                external_id is used along with the source to uniquely identify
                this Payment.


                A source can only have one Payment with any given external id.

                A source can have two payments with two different external ids.

                Two different sources can each have a payment with the same
                external id.

                But a source cannot have two different payments each with the
                same external id.


                An external id can be reused by a source once the payment is
                accepted, rejected, or cancelled.


                The external id is limited to 100 bytes. An empty string is a
                valid external id.
          description: >-
            Payment represents one account's desire to trade funds with another
            account.
        description: payments is all the payments with the requested target.
      pagination:
        description: pagination is the resulting pagination parameters.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetPaymentsWithTargetResponse is a response message for the
      GetPaymentsWithTarget query.
  provenance.exchange.v1.QueryOrderFeeCalcResponse:
    type: object
    properties:
      creation_fee_options:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          creation_fee_options are the order creation flat fee options available
          for creating the provided order.

          If it's empty, no order creation fee is required.

          When creating the order, you should include exactly one of these.
      settlement_flat_fee_options:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          settlement_flat_fee_options are the settlement flat fee options
          available for the provided order.

          If it's empty, no settlement flat fee is required.

          When creating an order, you should include exactly one of these in the
          settlement fees field.
      settlement_ratio_fee_options:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          settlement_ratio_fee_options are the settlement ratio fee options
          available for the provided order.

          If it's empty, no settlement ratio fee is required.


          If the provided order was a bid order, you should include exactly one
          of these in the settlement fees field.

          If the flat and ratio options you've chose have the same denom, a
          single entry should be included with their sum.


          If the provided order was an ask order, these are purely informational
          and represent how much will be removed

          from your price if it settles at that price. If it settles for more,
          the actual amount will probably be larger.
    description: >-
      QueryOrderFeeCalcResponse is a response message for the OrderFeeCalc
      query.
  provenance.exchange.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params are the exchange module parameter values.
        type: object
        properties:
          default_split:
            type: integer
            format: int64
            description: >-
              default_split is the default proportion of fees the exchange
              receives in basis points.

              It is used if there isn't an applicable denom-specific split
              defined.

              E.g. 100 = 1%. Min = 0, Max = 10000.
          denom_splits:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                  description: denom is the coin denomination this split applies to.
                split:
                  type: integer
                  format: int64
                  description: >-
                    split is the proportion of fees the exchange receives for
                    this denom in basis points.

                    E.g. 100 = 1%. Min = 0, Max = 10000.
              description: >-
                DenomSplit associates a coin denomination with an amount the
                exchange receives for that denom.
            description: denom_splits are the denom-specific amounts the exchange receives.
          fee_create_payment_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_create_payment_flat is the flat fee options for creating a
              payment.

              If the source amount is not zero then one of these fee entries is
              required to create the payment.

              This field is currently limited to zero or one entries.
          fee_accept_payment_flat:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              fee_accept_payment_flat is the flat fee options for accepting a
              payment.

              If the target amount is not zero then one of these fee entries is
              required to accept the payment.

              This field is currently limited to zero or one entries.
    description: QueryParamsResponse is a response message for the Params query.
  provenance.exchange.v1.QueryPaymentFeeCalcResponse:
    type: object
    properties:
      fee_create:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: fee_create is the fee required to create the provided payment.
      fee_accept:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: fee_accept is the fee required to accept the provided payment.
    description: >-
      QueryPaymentFeeCalcResponse is a response message for the PaymentFeeCalc
      query.
  provenance.exchange.v1.QueryValidateCreateMarketResponse:
    type: object
    properties:
      error:
        type: string
        description: >-
          error is any problems or inconsistencies in the provided gov prop msg.

          This goes above and beyond the validation done when actually
          processing the governance proposal.

          If an error is returned, and gov_prop_will_pass is true, it means the
          error is more of an

          inconsistency that might cause certain aspects of the market to behave
          unexpectedly.
      gov_prop_will_pass:
        type: boolean
        description: >-
          gov_prop_will_pass will be true if the the provided msg will be
          successfully processed at the end of it's voting

          period (assuming it passes).
    description: >-
      QueryValidateCreateMarketResponse is a response message for the
      ValidateCreateMarket query.
  provenance.exchange.v1.QueryValidateManageFeesResponse:
    type: object
    properties:
      error:
        type: string
        description: >-
          error is any problems or inconsistencies in the provided gov prop msg.

          This goes above and beyond the validation done when actually
          processing the governance proposal.

          If an error is returned, and gov_prop_will_pass is true, it means the
          error is more of an

          inconsistency that might cause certain aspects of the market to behave
          unexpectedly.
      gov_prop_will_pass:
        type: boolean
        description: >-
          gov_prop_will_pass will be true if the the provided msg will be
          successfully processed at the end of it's voting

          period (assuming it passes).
    description: >-
      QueryValidateManageFeesResponse is a response message for the
      ValidateManageFees query.
  provenance.exchange.v1.QueryValidateMarketResponse:
    type: object
    properties:
      error:
        type: string
        description: error is any problems or inconsistencies in the provided market.
    description: >-
      QueryValidateMarketResponse is a response message for the ValidateMarket
      query.
  provenance.exchange.v1.MsgAcceptPaymentResponse:
    type: object
    description: >-
      MsgAcceptPaymentResponse is a response message for the AcceptPayment
      endpoint.
  provenance.exchange.v1.MsgCancelOrderResponse:
    type: object
    description: MsgCancelOrderResponse is a response message for the CancelOrder endpoint.
  provenance.exchange.v1.MsgCancelPaymentsResponse:
    type: object
    description: >-
      MsgCancelPaymentsResponse is a response message for the CancelPayments
      endpoint.
  provenance.exchange.v1.MsgChangePaymentTargetResponse:
    type: object
    description: >-
      MsgChangePaymentTargetResponse is a response message for the
      ChangePaymentTarget endpoint.
  provenance.exchange.v1.MsgCommitFundsResponse:
    type: object
    description: MsgCommitFundsResponse is a response message for the CommitFunds endpoint.
  provenance.exchange.v1.MsgCreateAskResponse:
    type: object
    properties:
      order_id:
        type: string
        format: uint64
        description: order_id is the id of the order created.
    description: MsgCreateAskResponse is a response message for the CreateAsk endpoint.
  provenance.exchange.v1.MsgCreateBidResponse:
    type: object
    properties:
      order_id:
        type: string
        format: uint64
        description: order_id is the id of the order created.
    description: MsgCreateBidResponse is a response message for the CreateBid endpoint.
  provenance.exchange.v1.MsgCreatePaymentResponse:
    type: object
    description: >-
      MsgCreatePaymentResponse is a response message for the CreatePayment
      endpoint.
  provenance.exchange.v1.MsgFillAsksResponse:
    type: object
    description: MsgFillAsksResponse is a response message for the FillAsks endpoint.
  provenance.exchange.v1.MsgFillBidsResponse:
    type: object
    description: MsgFillBidsResponse is a response message for the FillBids endpoint.
  provenance.exchange.v1.MsgGovCloseMarketResponse:
    type: object
    description: >-
      MsgGovCloseMarketResponse is a response message for the GovCloseMarket
      endpoint.
  provenance.exchange.v1.MsgGovCreateMarketResponse:
    type: object
    description: >-
      MsgGovCreateMarketResponse is a response message for the GovCreateMarket
      endpoint.
  provenance.exchange.v1.MsgGovManageFeesResponse:
    type: object
    description: >-
      MsgGovManageFeesResponse is a response message for the GovManageFees
      endpoint.
  provenance.exchange.v1.MsgGovUpdateParamsResponse:
    type: object
    description: >-
      MsgGovUpdateParamsResponse is a response message for the GovUpdateParams
      endpoint.

      Deprecated: Use MsgUpdateParamsResponse instead.
  provenance.exchange.v1.MsgMarketCommitmentSettleResponse:
    type: object
    description: >-
      MsgMarketCommitmentSettleResponse is a response message for the
      MarketCommitmentSettle endpoint.
  provenance.exchange.v1.MsgMarketManagePermissionsResponse:
    type: object
    description: >-
      MsgMarketManagePermissionsResponse is a response message for the
      MarketManagePermissions endpoint.
  provenance.exchange.v1.MsgMarketManageReqAttrsResponse:
    type: object
    description: >-
      MsgMarketManageReqAttrsResponse is a response message for the
      MarketManageReqAttrs endpoint.
  provenance.exchange.v1.MsgMarketReleaseCommitmentsResponse:
    type: object
    description: >-
      MsgMarketReleaseCommitmentsResponse is a response message for the
      MarketReleaseCommitments endpoint.
  provenance.exchange.v1.MsgMarketSetOrderExternalIDResponse:
    type: object
    description: >-
      MsgMarketSetOrderExternalIDResponse is a response message for the
      MarketSetOrderExternalID endpoint.
  provenance.exchange.v1.MsgMarketSettleResponse:
    type: object
    description: >-
      MsgMarketSettleResponse is a response message for the MarketSettle
      endpoint.
  provenance.exchange.v1.MsgMarketTransferCommitmentResponse:
    type: object
    description: >-
      MsgMarketTransferCommitmentResponse is a response message for the
      MarketTransferCommitment endpoint.
  provenance.exchange.v1.MsgMarketUpdateAcceptingCommitmentsResponse:
    type: object
    description: >-
      MsgMarketUpdateAcceptingCommitmentsResponse is a response message for the
      MarketUpdateAcceptingCommitments endpoint.
  provenance.exchange.v1.MsgMarketUpdateAcceptingOrdersResponse:
    type: object
    description: >-
      MsgMarketUpdateAcceptingOrdersResponse is a response message for the
      MarketUpdateAcceptingOrders endpoint.
  provenance.exchange.v1.MsgMarketUpdateDetailsResponse:
    type: object
    description: >-
      MsgMarketUpdateDetailsResponse is a response message for the
      MarketUpdateDetails endpoint.
  provenance.exchange.v1.MsgMarketUpdateEnabledResponse:
    type: object
    description: >-
      MsgMarketUpdateEnabledResponse is a response message for the
      MarketUpdateEnabled endpoint.

      Deprecated: This endpoint is no longer usable. It is replaced by
      MarketUpdateAcceptingOrders.
  provenance.exchange.v1.MsgMarketUpdateIntermediaryDenomResponse:
    type: object
    description: >-
      MsgMarketUpdateIntermediaryDenomResponse is a response message for the
      MarketUpdateIntermediaryDenom endpoint.
  provenance.exchange.v1.MsgMarketUpdateUserSettleResponse:
    type: object
    description: >-
      MsgMarketUpdateUserSettleResponse is a response message for the
      MarketUpdateUserSettle endpoint.
  provenance.exchange.v1.MsgMarketWithdrawResponse:
    type: object
    description: >-
      MsgMarketWithdrawResponse is a response message for the MarketWithdraw
      endpoint.
  provenance.exchange.v1.MsgRejectPaymentResponse:
    type: object
    description: >-
      MsgRejectPaymentResponse is a response message for the RejectPayment
      endpoint.
  provenance.exchange.v1.MsgRejectPaymentsResponse:
    type: object
    description: >-
      MsgRejectPaymentsResponse is a response message for the RejectPayments
      endpoint.
  provenance.exchange.v1.MsgSendAndCommitResponse:
    type: object
    description: >-
      MsgSendAndCommitResponse is a response message for the SendAndCommit
      endpoint.
  provenance.exchange.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is a response message for the GovUpdateParams
      endpoint.
  provenance.flatfees.v1.ConversionFactor:
    type: object
    properties:
      definition_amount:
        description: >-
          definition_amount is an amount (in the denomination used to define
          fees) that is equal to the converted_amount.

          This cannot have an amount of zero.

          If this has the same denomination as the converted_amount, then the
          amounts must also be equal.

          The denom of this field should be the same as the default cost, e.g.
          musd.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      converted_amount:
        description: >-
          converted_amount is an amount in the fee denomination equal to the
          definition_amount.

          If this is zero, all msgs will be free.

          If this has the same denomination as the definition_amount, then the
          amounts must also be equal.

          The denom of this field should be the fee denom, e.g. nhash.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      ConversionFactor equates the values of two coins in different
      denominations.

      It is used to determine how much of the fee denomination is due.

      actual cost = defined cost * converted_amount / definition_amount
      (truncated to an integer).
  provenance.flatfees.v1.MsgFee:
    type: object
    properties:
      msg_type_url:
        type: string
        description: >-
          msg_type_url is the type-url of the message, e.g.
          "/cosmos.bank.v1beta1.MsgSend".
      cost:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          cost is the Tx fee required for this msg_type_url.

          It should have the same denomination as the default cost and as the
          conversion factor's

          definition_amount, e.g. musdc. Any other denomination will be charged
          as defined.
    description: MsgFee defines the cost to use a specific message type.
  provenance.flatfees.v1.Params:
    type: object
    properties:
      default_cost:
        description: >-
          default_cost is the amount a msg costs when there is no specific
          msg-fee defined for it.

          The denom used here should be the same as used to define the specific
          msg costs.

          The recommended denom is musd.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      conversion_factor:
        description: >-
          conversion_factor is the ratio used to convert the msg-fees from their
          defined amounts into the fee denomination.

          The definition_amount should have the same denom as the default cost.

          The denom of the converted amount should be the denom that fees are
          paid in, e.g. nhash.
        type: object
        properties:
          definition_amount:
            description: >-
              definition_amount is an amount (in the denomination used to define
              fees) that is equal to the converted_amount.

              This cannot have an amount of zero.

              If this has the same denomination as the converted_amount, then
              the amounts must also be equal.

              The denom of this field should be the same as the default cost,
              e.g. musd.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          converted_amount:
            description: >-
              converted_amount is an amount in the fee denomination equal to the
              definition_amount.

              If this is zero, all msgs will be free.

              If this has the same denomination as the definition_amount, then
              the amounts must also be equal.

              The denom of this field should be the fee denom, e.g. nhash.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
      oracle_addresses:
        type: array
        items:
          type: string
        description: >-
          oracle_addresses is the list of addresses allowed to update the
          conversion factor without governance.
    description: Params defines the set of params for the flatfees module.
  provenance.flatfees.v1.QueryAllMsgFeesResponse:
    type: object
    properties:
      msg_fees:
        type: array
        items:
          type: object
          properties:
            msg_type_url:
              type: string
              description: >-
                msg_type_url is the type-url of the message, e.g.
                "/cosmos.bank.v1beta1.MsgSend".
            cost:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: >-
                cost is the Tx fee required for this msg_type_url.

                It should have the same denomination as the default cost and as
                the conversion factor's

                definition_amount, e.g. musdc. Any other denomination will be
                charged as defined.
          description: MsgFee defines the cost to use a specific message type.
        description: msg_fees contains the requested information.
      default_cost:
        description: >-
          default_cost is the amount a msg costs when there is no specific
          msg-fee defined for it.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      pagination:
        description: pagination defines the pagination parameters of the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryAllMsgFeesResponse is the response type for the AllMsgFees query.
  provenance.flatfees.v1.QueryCalculateTxFeesRequest:
    type: object
    properties:
      tx_bytes:
        type: string
        format: byte
        description: tx_bytes is the transaction to simulate.
      gas_adjustment:
        type: number
        format: float
        description: >-
          gas_adjustment is a multiplier applied to the gas used while
          simulating the tx.

          This only affects the estimated gas (since the fee is flat).

          The default is 1.0. Valid range is 0.0 (exclusive) to 10.0
          (inclusive).
    description: >-
      QueryCalculateTxFeesRequest is the request type for the CalculateTxFees
      query.
  provenance.flatfees.v1.QueryCalculateTxFeesResponse:
    type: object
    properties:
      total_fees:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: total_fees is the amount of fees needed for the Tx.
      estimated_gas:
        type: string
        format: uint64
        description: estimated_gas is the amount of gas needed for the Tx.
    description: >-
      QueryCalculateTxFeesResponse is the response type for the CalculateTxFees
      query.
  provenance.flatfees.v1.QueryMsgFeeResponse:
    type: object
    properties:
      msg_fee:
        description: msg_fee is the requested entry.
        type: object
        properties:
          msg_type_url:
            type: string
            description: >-
              msg_type_url is the type-url of the message, e.g.
              "/cosmos.bank.v1beta1.MsgSend".
          cost:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              cost is the Tx fee required for this msg_type_url.

              It should have the same denomination as the default cost and as
              the conversion factor's

              definition_amount, e.g. musdc. Any other denomination will be
              charged as defined.
    description: QueryMsgFeeResponse is the response type for the MsgFee query.
  provenance.flatfees.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the x/flatfees module.
        type: object
        properties:
          default_cost:
            description: >-
              default_cost is the amount a msg costs when there is no specific
              msg-fee defined for it.

              The denom used here should be the same as used to define the
              specific msg costs.

              The recommended denom is musd.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          conversion_factor:
            description: >-
              conversion_factor is the ratio used to convert the msg-fees from
              their defined amounts into the fee denomination.

              The definition_amount should have the same denom as the default
              cost.

              The denom of the converted amount should be the denom that fees
              are paid in, e.g. nhash.
            type: object
            properties:
              definition_amount:
                description: >-
                  definition_amount is an amount (in the denomination used to
                  define fees) that is equal to the converted_amount.

                  This cannot have an amount of zero.

                  If this has the same denomination as the converted_amount,
                  then the amounts must also be equal.

                  The denom of this field should be the same as the default
                  cost, e.g. musd.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
              converted_amount:
                description: >-
                  converted_amount is an amount in the fee denomination equal to
                  the definition_amount.

                  If this is zero, all msgs will be free.

                  If this has the same denomination as the definition_amount,
                  then the amounts must also be equal.

                  The denom of this field should be the fee denom, e.g. nhash.
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
          oracle_addresses:
            type: array
            items:
              type: string
            description: >-
              oracle_addresses is the list of addresses allowed to update the
              conversion factor without governance.
    description: QueryParamsResponse is the response type for the Params query.
  provenance.flatfees.v1.MsgAddOracleAddressResponse:
    type: object
    description: >-
      MsgAddOracleAddressResponse is the response for the AddOracleAddress
      governance endpoint.
  provenance.flatfees.v1.MsgRemoveOracleAddressResponse:
    type: object
    description: >-
      MsgRemoveOracleAddressResponse is the response for the RemoveOracleAddress
      governance endpoint.
  provenance.flatfees.v1.MsgUpdateConversionFactorResponse:
    type: object
    description: >-
      MsgUpdateConversionFactorResponse is the response for the
      UpdateConversionFactor governance endpoint.
  provenance.flatfees.v1.MsgUpdateMsgFeesResponse:
    type: object
    description: >-
      MsgUpdateMsgFeesResponse is the response for the UpdateMsgFees governance
      endpoint.
  provenance.flatfees.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is the response for the UpdateParams governance
      endpoint.
  provenance.hold.v1.AccountHold:
    type: object
    properties:
      address:
        type: string
        description: address is the account address that holds the funds on hold.
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount is the balances that are on hold for the address.
    description: AccountHold associates an address with an amount on hold for that address.
  provenance.hold.v1.GetAllHoldsResponse:
    type: object
    properties:
      holds:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address is the account address that holds the funds on hold.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount is the balances that are on hold for the address.
          description: >-
            AccountHold associates an address with an amount on hold for that
            address.
        description: >-
          holds is a list of addresses with funds on hold and the amounts being
          held.
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: GetAllHoldsResponse is the response type for the Query/GetAllHolds query.
  provenance.hold.v1.GetHoldsResponse:
    type: object
    properties:
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount is the total on hold for the requested address.
    description: GetHoldsResponse is the response type for the Query/GetHolds query.
  provenance.hold.v1.MsgUnlockVestingAccountsResponse:
    type: object
    title: >-
      MsgUnlockVestingAccountsResponse defines the response for unlocking
      vesting accounts
  provenance.ibchooks.v1.Params:
    type: object
    properties:
      allowed_async_ack_contracts:
        type: array
        items:
          type: string
        description: |-
          Deprecated: This field has been deprecated and is ignored.
          Async ack contracts are no longer possible in IBC v10.
    title: Params defines the allowed async ack contracts
  provenance.ibchooks.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          allowed_async_ack_contracts:
            type: array
            items:
              type: string
            description: |-
              Deprecated: This field has been deprecated and is ignored.
              Async ack contracts are no longer possible in IBC v10.
        title: Params defines the allowed async ack contracts
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  provenance.ibchooks.v1.MsgEmitIBCAckResponse:
    type: object
    properties:
      contract_result:
        type: string
      ibc_ack:
        type: string
    title: MsgEmitIBCAckResponse is the IBC Acknowledgement response
  provenance.ibchooks.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is a response message for the UpdateParams
      endpoint.
  provenance.ibcratelimit.v1.Params:
    type: object
    properties:
      contract_address:
        type: string
        description: contract_address is the address of the rate limiter contract.
    description: Params defines the parameters for the ibcratelimit module.
  provenance.ibcratelimit.v1.ParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          contract_address:
            type: string
            description: contract_address is the address of the rate limiter contract.
    description: ParamsResponse is the response type for the Query/Params RPC method.
  provenance.ibcratelimit.v1.MsgGovUpdateParamsResponse:
    type: object
    description: >-
      MsgGovUpdateParamsResponse is a response message for the GovUpdateParams
      endpoint.

      Deprecated: Use MsgUpdateParamsResponse instead.
  provenance.ibcratelimit.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is a response message for the UpdateParams
      endpoint.
  provenance.ledger.v1.BucketBalance:
    type: object
    properties:
      bucket_type_id:
        type: integer
        format: int32
        description: The bucket type specified by the LedgerClassBucketType.id.
      balance_amt:
        type: string
        description: >-
          The balance of the bucket.

          The units of this field are defined by the denom field in this
          ledger's class.
    description: BucketBalance represents the balance for a specific bucket type.
  provenance.ledger.v1.DayCountConvention:
    type: string
    enum:
      - DAY_COUNT_CONVENTION_UNSPECIFIED
      - DAY_COUNT_CONVENTION_ACTUAL_365
      - DAY_COUNT_CONVENTION_ACTUAL_360
      - DAY_COUNT_CONVENTION_THIRTY_360
      - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
      - DAY_COUNT_CONVENTION_DAYS_365
      - DAY_COUNT_CONVENTION_DAYS_360
      - DAY_COUNT_CONVENTION_NOT_DEFINED
    default: DAY_COUNT_CONVENTION_UNSPECIFIED
    description: >-
      Day Count Conventions used in interest calculations.

       - DAY_COUNT_CONVENTION_UNSPECIFIED: Unspecified day count convention.

      This value is never a field's actual value, it just indicates that, for a
      given request, the day count convention

      is not being provided. E.g. if this value is provided in a
      MsgUpdateInterestRateRequest, it indicates that the day

      count convention is not being provided, and so should not be updated.


      DAY_COUNT_CONVENTION_UNSPECIFIED indicates that the day count convention
      is not being provided.

      DAY_COUNT_CONVENTION_NOT_DEFINED indicates that the day count convention
      of the entry does not have a value.
       - DAY_COUNT_CONVENTION_ACTUAL_365: Actual/365: Uses the actual number of days in the period with a fixed denominator of 365
      (or sometimes 365.25 to adjust for leap years).
       - DAY_COUNT_CONVENTION_ACTUAL_360: Actual/360: Uses the actual number of days in the period but divides by 360.
       - DAY_COUNT_CONVENTION_THIRTY_360: 30/360: Assumes each month has 30 days and the year has 360 days.
       - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL: Actual/Actual: Uses the actual number of days in the period and the actual days in the year
      (365 or 366, depending on the year).
       - DAY_COUNT_CONVENTION_DAYS_365: 365/365: Always uses 365 days in the denominator regardless of leap years.
       - DAY_COUNT_CONVENTION_DAYS_360: 360/360: Always uses 360 days in both the numerator and denominator.
       - DAY_COUNT_CONVENTION_NOT_DEFINED: Not Defined: The day count convention is not defined.

      This value indicates that the day count convention should be on record as
      not having a value. It could mean that

      the day count convention is not one of the other values in this enum, or
      it could mean that it doesn't make sense

      for the entry to have a day count convention. It could also just mean that
      the day count convention has not yet

      been defined for the entry.


      DAY_COUNT_CONVENTION_UNSPECIFIED indicates that the day count convention
      is not being provided.

      DAY_COUNT_CONVENTION_NOT_DEFINED indicates that the day count convention
      of the entry does not have a value.
  provenance.ledger.v1.FundingTransferStatus:
    type: string
    enum:
      - FUNDING_TRANSFER_STATUS_UNSPECIFIED
      - FUNDING_TRANSFER_STATUS_PENDING
      - FUNDING_TRANSFER_STATUS_PROCESSING
      - FUNDING_TRANSFER_STATUS_COMPLETED
      - FUNDING_TRANSFER_STATUS_FAILED
    default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
    description: |-
      FundingTransferStatus represents the current status of a funding transfer.

       - FUNDING_TRANSFER_STATUS_UNSPECIFIED: Unspecified funding transfer status.
       - FUNDING_TRANSFER_STATUS_PENDING: Pending funding transfer status.
       - FUNDING_TRANSFER_STATUS_PROCESSING: Processing funding transfer status.
       - FUNDING_TRANSFER_STATUS_COMPLETED: Completed funding transfer status.
       - FUNDING_TRANSFER_STATUS_FAILED: Failed funding transfer status.
  provenance.ledger.v1.InterestAccrualMethod:
    type: string
    enum:
      - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
      - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
      - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
      - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
      - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
      - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
      - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
      - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
      - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
    default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
    description: >-
      Interest Accrual Methods describing how interest is calculated over time.

       - INTEREST_ACCRUAL_METHOD_UNSPECIFIED: Unspecified interest accrual method.

      This value is never a field's actual value, it just indicates that, for a
      given request, the interest accrual

      method is not being provided. E.g. if this value is provided in a
      MsgUpdateInterestRateRequest, it indicates that

      the interest accrual method is not being provided, and so should not be
      updated.


      INTEREST_ACCRUAL_METHOD_UNSPECIFIED indicates that the interest accrual
      method is not being provided.

      INTEREST_ACCRUAL_METHOD_NOT_DEFINED indicates that the interest accrual
      method of the entry does not have a value.
       - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST: Simple Interest: Calculated only on the principal amount.
       - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST: Compound Interest: Calculated on both the principal and on previously accumulated interest.
       - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING: Daily Compounding: Interest is compounded on a daily basis.
       - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING: Monthly Compounding: Interest is compounded each month.
       - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING: Quarterly Compounding: Interest is compounded every quarter.
       - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING: Annually Compounding: Interest is compounded once per year.
       - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING: Continuous Compounding: The theoretical limit of compounding frequency where interest is compounded continuously.
       - INTEREST_ACCRUAL_METHOD_NOT_DEFINED: Not Defined: The interest accrual method is not defined.

      This value indicates that the interest accrual method should be on record
      as not having a value. It could mean that

      the interest accrual method is not one of the other values in this enum,
      or it could mean that it doesn't make

      sense for the entry to have an interest accrual method. It could also just
      mean that the interest accrual method

      has not yet been defined for the entry.


      INTEREST_ACCRUAL_METHOD_UNSPECIFIED indicates that the interest accrual
      method is not being provided.

      INTEREST_ACCRUAL_METHOD_NOT_DEFINED indicates that the interest accrual
      method of the entry does not have a value.
  provenance.ledger.v1.Ledger:
    type: object
    properties:
      key:
        description: The ledger key identifying the ledger.
        type: object
        properties:
          nft_id:
            type: string
            description: >-
              Identifier for the nft that this ledger is linked to.

              This could be a `x/metadata` scope id or an `x/nft` nft id.

              In order to create a ledger for an nft, the nft class must be
              registered in the ledger module as a LedgerClass.
          asset_class_id:
            type: string
            description: Scope Specification ID or NFT Class ID.
      ledger_class_id:
        type: string
        description: The ledger class ID for the ledger.
      status_type_id:
        type: integer
        format: int32
        description: The status of the ledger.
      next_pmt_date:
        type: integer
        format: int32
        description: The next payment date (days since epoch).
      next_pmt_amt:
        type: string
        description: >-
          The next payment amount.

          The units of this field are defined by the denom field in this
          ledger's class.
      interest_rate:
        type: integer
        format: int32
        description: >-
          The interest rate. Min = 0, Max = 100,000,000 = 100%, e.g. 4,321,987 =
          4.321987%.
      maturity_date:
        type: integer
        format: int32
        description: The maturity date (days since epoch).
      interest_day_count_convention:
        description: The day count convention for interest calculations.
        type: string
        enum:
          - DAY_COUNT_CONVENTION_UNSPECIFIED
          - DAY_COUNT_CONVENTION_ACTUAL_365
          - DAY_COUNT_CONVENTION_ACTUAL_360
          - DAY_COUNT_CONVENTION_THIRTY_360
          - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
          - DAY_COUNT_CONVENTION_DAYS_365
          - DAY_COUNT_CONVENTION_DAYS_360
          - DAY_COUNT_CONVENTION_NOT_DEFINED
        default: DAY_COUNT_CONVENTION_UNSPECIFIED
      interest_accrual_method:
        description: The interest accrual method for interest calculations.
        type: string
        enum:
          - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
          - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
          - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
          - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
          - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
          - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
          - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
          - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
          - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
        default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
      payment_frequency:
        description: The payment frequency.
        type: string
        enum:
          - PAYMENT_FREQUENCY_UNSPECIFIED
          - PAYMENT_FREQUENCY_DAILY
          - PAYMENT_FREQUENCY_WEEKLY
          - PAYMENT_FREQUENCY_MONTHLY
          - PAYMENT_FREQUENCY_QUARTERLY
          - PAYMENT_FREQUENCY_ANNUALLY
          - PAYMENT_FREQUENCY_NOT_DEFINED
        default: PAYMENT_FREQUENCY_UNSPECIFIED
    description: Ledger defines an servicing ledger for an asset.
  provenance.ledger.v1.LedgerBucketAmount:
    type: object
    properties:
      bucket_type_id:
        type: integer
        format: int32
        description: The bucket type specified by the LedgerClassBucketType.id.
      applied_amt:
        type: string
        description: >-
          The amount applied to the bucket.

          The units of this field are defined by the denom field in this
          ledger's class.
    description: >-
      LedgerBucketAmount is the amount applied to a bucket. Applications to a
      bucket increase or

      decrease the balance of the bucket.
  provenance.ledger.v1.LedgerClass:
    type: object
    properties:
      ledger_class_id:
        type: string
        description: |-
          Unique ID for the ledger class (eg. 1, 2, 3, etc.).
          This is necessary since the nft class does not have an owner.
      asset_class_id:
        type: string
        description: Scope Specification ID or NFT Class ID.
      denom:
        type: string
        description: Denom that this class of asset will be ledgered in.
      maintainer_address:
        type: string
        description: Address of the maintainer for the ledger class.
    description: >-
      LedgerClass contains the configuration for a ledger related to a
      particular class of asset. The asset class

      is defined by the either a scope spec `x/metadata`, or nft class `x/nft`.
      Ultimately, the configuration will

      assist in verifying the types that are associated with particular ledger
      entries.
  provenance.ledger.v1.LedgerClassBucketType:
    type: object
    properties:
      id:
        type: integer
        format: int32
        description: Unique ID for the bucket type (eg. 1, 2, 3, etc.).
      code:
        type: string
        description: >-
          Code for the bucket type (eg. "PRINCIPAL", "INTEREST", "FEE",
          "OTHER").
      description:
        type: string
        description: >-
          Description of the bucket type (eg. "Principal", "Interest", "Fee",
          "Other").
    description: LedgerClassBucketType represents a bucket type for a ledger class.
  provenance.ledger.v1.LedgerClassEntryType:
    type: object
    properties:
      id:
        type: integer
        format: int32
        description: Unique ID for the entry type (eg. 1, 2, 3, etc.).
      code:
        type: string
        description: >-
          Code for the entry type (eg. "DISBURSEMENT", "PAYMENT", "ADJUSTMENT",
          "INTEREST", "FEE", "OTHER").
      description:
        type: string
        description: >-
          Description of the entry type (eg. "Disbursement", "Payment",
          "Adjustment", "Interest", "Fee", "Other").
    description: >-
      LedgerClassEntryType defines the types of possible ledger entries for a
      given asset class. These type codes allow

      for minimal data storage while providing a human readable description of
      the entry type.
  provenance.ledger.v1.LedgerClassStatusType:
    type: object
    properties:
      id:
        type: integer
        format: int32
        description: Unique ID for the status type (eg. 1, 2, 3, etc.).
      code:
        type: string
        description: >-
          Code for the status type (eg. "IN_REPAYMENT", "IN_FORECLOSURE",
          "FORBEARANCE", "DEFERMENT", "BANKRUPTCY",

          "CLOSED", "CANCELLED", "SUSPENDED", "OTHER").
      description:
        type: string
        description: >-
          Description of the status type (eg. "In Repayment", "In Foreclosure",
          "Forbearance", "Deferment", "Bankruptcy",

          "Closed", "Cancelled", "Suspended", "Other").
    description: >-
      LedgerClassStatusType defines the status types for a ledger class.

      These status types are used to track the status of underlying loan
      throughout the loan life cycle.
  provenance.ledger.v1.LedgerEntry:
    type: object
    properties:
      correlation_id:
        type: string
        description: >-
          The correlation ID for tracking ledger entries with external systems
          (max 50 characters).
      reverses_correlation_id:
        type: string
        description: >-
          If this entry reverses another entry, the correlation ID of the entry
          it reverses.
      is_void:
        type: boolean
        description: >-
          If true, this entry is a void and should not be included in the ledger
          balance calculations.
      sequence:
        type: integer
        format: int64
        description: >-
          Sequence number of the ledger entry (less than 300). This field is
          used to maintain the correct

          order of entries when multiple entries share the same effective date.
          Entries are sorted first

          by effective date, then by sequence.
      entry_type_id:
        type: integer
        format: int32
        description: The type of ledger entry specified by the LedgerClassEntryType.id.
      posted_date:
        type: integer
        format: int32
        description: The posted date (days since epoch).
      effective_date:
        type: integer
        format: int32
        description: The effective date (days since epoch).
      total_amt:
        type: string
        description: >-
          The total amount of the ledger entry.

          The units of this field are defined by the denom field in this
          ledger's class.
      applied_amounts:
        type: array
        items:
          type: object
          properties:
            bucket_type_id:
              type: integer
              format: int32
              description: The bucket type specified by the LedgerClassBucketType.id.
            applied_amt:
              type: string
              description: >-
                The amount applied to the bucket.

                The units of this field are defined by the denom field in this
                ledger's class.
          description: >-
            LedgerBucketAmount is the amount applied to a bucket. Applications
            to a bucket increase or

            decrease the balance of the bucket.
        description: Applied amounts represent how the entry affects different buckets.
      balance_amounts:
        type: array
        items:
          type: object
          properties:
            bucket_type_id:
              type: integer
              format: int32
              description: The bucket type specified by the LedgerClassBucketType.id.
            balance_amt:
              type: string
              description: >-
                The balance of the bucket.

                The units of this field are defined by the denom field in this
                ledger's class.
          description: BucketBalance represents the balance for a specific bucket type.
        description: Bucket balances represent the current state of funds in each bucket.
    description: >-
      LedgerEntry is an single entry in the ledger. An entry would be a payment,
      disbursement, adjustment, etc...
  provenance.ledger.v1.LedgerKey:
    type: object
    properties:
      nft_id:
        type: string
        description: >-
          Identifier for the nft that this ledger is linked to.

          This could be a `x/metadata` scope id or an `x/nft` nft id.

          In order to create a ledger for an nft, the nft class must be
          registered in the ledger module as a LedgerClass.
      asset_class_id:
        type: string
        description: Scope Specification ID or NFT Class ID.
    description: LedgerKey is used as the unique key for an asset's ledger in the keeper.
  provenance.ledger.v1.PaymentFrequency:
    type: string
    enum:
      - PAYMENT_FREQUENCY_UNSPECIFIED
      - PAYMENT_FREQUENCY_DAILY
      - PAYMENT_FREQUENCY_WEEKLY
      - PAYMENT_FREQUENCY_MONTHLY
      - PAYMENT_FREQUENCY_QUARTERLY
      - PAYMENT_FREQUENCY_ANNUALLY
      - PAYMENT_FREQUENCY_NOT_DEFINED
    default: PAYMENT_FREQUENCY_UNSPECIFIED
    description: >-
      Payment frequencies for loan repayments.

       - PAYMENT_FREQUENCY_UNSPECIFIED: Unspecified payment frequency.

      This value is never a field's actual value, it just indicates that, for a
      given request, the payment frequency is

      not being provided. E.g. if this value is provided in a
      MsgUpdatePaymentRequest, it indicates that the payment

      frequency is not being provided, and so should not be updated.


      PAYMENT_FREQUENCY_UNSPECIFIED indicates that the payment frequency is not
      being provided.

      PAYMENT_FREQUENCY_NOT_DEFINED indicates that the payment frequency of the
      entry does not have a value.
       - PAYMENT_FREQUENCY_DAILY: Daily payments.
       - PAYMENT_FREQUENCY_WEEKLY: Weekly or biweekly payments.
       - PAYMENT_FREQUENCY_MONTHLY: Monthly payments (most common for consumer loans and mortgages).
       - PAYMENT_FREQUENCY_QUARTERLY: Quarterly payments.
       - PAYMENT_FREQUENCY_ANNUALLY: Annual payments.
       - PAYMENT_FREQUENCY_NOT_DEFINED: Not Defined: The payment frequency is not defined.

      This value indicates that the payment frequency should be on record as not
      having a value. It could mean that the

      payment frequency is not one of the other values in this enum, or it could
      mean that it doesn't make sense for the

      entry to have a payment frequency. It could also just mean that the
      payment frequency has not yet been defined for

      the entry.


      PAYMENT_FREQUENCY_UNSPECIFIED indicates that the payment frequency is not
      being provided.

      PAYMENT_FREQUENCY_NOT_DEFINED indicates that the payment frequency of the
      entry does not have a value.
  provenance.ledger.v1.QueryLedgerBalancesAsOfResponse:
    type: object
    properties:
      bucket_balances:
        type: array
        items:
          type: object
          properties:
            bucket_type_id:
              type: integer
              format: int32
              description: The bucket type specified by the LedgerClassBucketType.id.
            balance_amt:
              type: string
              description: >-
                The balance of the bucket.

                The units of this field are defined by the denom field in this
                ledger's class.
          description: BucketBalance represents the balance for a specific bucket type.
        description: >-
          BucketBalances represents the current balances for principal,
          interest, and other amounts.
    title: QueryLedgerBalancesAsOfResponse
  provenance.ledger.v1.QueryLedgerClassBucketTypesResponse:
    type: object
    properties:
      bucket_types:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
              format: int32
              description: Unique ID for the bucket type (eg. 1, 2, 3, etc.).
            code:
              type: string
              description: >-
                Code for the bucket type (eg. "PRINCIPAL", "INTEREST", "FEE",
                "OTHER").
            description:
              type: string
              description: >-
                Description of the bucket type (eg. "Principal", "Interest",
                "Fee", "Other").
          description: LedgerClassBucketType represents a bucket type for a ledger class.
    title: QueryLedgerClassBucketTypesResponse
  provenance.ledger.v1.QueryLedgerClassEntryTypesResponse:
    type: object
    properties:
      entry_types:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
              format: int32
              description: Unique ID for the entry type (eg. 1, 2, 3, etc.).
            code:
              type: string
              description: >-
                Code for the entry type (eg. "DISBURSEMENT", "PAYMENT",
                "ADJUSTMENT", "INTEREST", "FEE", "OTHER").
            description:
              type: string
              description: >-
                Description of the entry type (eg. "Disbursement", "Payment",
                "Adjustment", "Interest", "Fee", "Other").
          description: >-
            LedgerClassEntryType defines the types of possible ledger entries
            for a given asset class. These type codes allow

            for minimal data storage while providing a human readable
            description of the entry type.
    title: QueryLedgerClassEntryTypesResponse
  provenance.ledger.v1.QueryLedgerClassResponse:
    type: object
    properties:
      ledger_class:
        type: object
        properties:
          ledger_class_id:
            type: string
            description: |-
              Unique ID for the ledger class (eg. 1, 2, 3, etc.).
              This is necessary since the nft class does not have an owner.
          asset_class_id:
            type: string
            description: Scope Specification ID or NFT Class ID.
          denom:
            type: string
            description: Denom that this class of asset will be ledgered in.
          maintainer_address:
            type: string
            description: Address of the maintainer for the ledger class.
        description: >-
          LedgerClass contains the configuration for a ledger related to a
          particular class of asset. The asset class

          is defined by the either a scope spec `x/metadata`, or nft class
          `x/nft`. Ultimately, the configuration will

          assist in verifying the types that are associated with particular
          ledger entries.
    title: QueryLedgerClassResponse
  provenance.ledger.v1.QueryLedgerClassStatusTypesResponse:
    type: object
    properties:
      status_types:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
              format: int32
              description: Unique ID for the status type (eg. 1, 2, 3, etc.).
            code:
              type: string
              description: >-
                Code for the status type (eg. "IN_REPAYMENT", "IN_FORECLOSURE",
                "FORBEARANCE", "DEFERMENT", "BANKRUPTCY",

                "CLOSED", "CANCELLED", "SUSPENDED", "OTHER").
            description:
              type: string
              description: >-
                Description of the status type (eg. "In Repayment", "In
                Foreclosure", "Forbearance", "Deferment", "Bankruptcy",

                "Closed", "Cancelled", "Suspended", "Other").
          description: >-
            LedgerClassStatusType defines the status types for a ledger class.

            These status types are used to track the status of underlying loan
            throughout the loan life cycle.
    title: QueryLedgerClassStatusTypesResponse
  provenance.ledger.v1.QueryLedgerClassesResponse:
    type: object
    properties:
      ledger_classes:
        type: array
        items:
          type: object
          properties:
            ledger_class_id:
              type: string
              description: |-
                Unique ID for the ledger class (eg. 1, 2, 3, etc.).
                This is necessary since the nft class does not have an owner.
            asset_class_id:
              type: string
              description: Scope Specification ID or NFT Class ID.
            denom:
              type: string
              description: Denom that this class of asset will be ledgered in.
            maintainer_address:
              type: string
              description: Address of the maintainer for the ledger class.
          description: >-
            LedgerClass contains the configuration for a ledger related to a
            particular class of asset. The asset class

            is defined by the either a scope spec `x/metadata`, or nft class
            `x/nft`. Ultimately, the configuration will

            assist in verifying the types that are associated with particular
            ledger entries.
        description: List of ledger classes.
      pagination:
        description: pagination is the pagination details for this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: QueryLedgerClassesResponse
  provenance.ledger.v1.QueryLedgerEntriesResponse:
    type: object
    properties:
      entries:
        type: array
        items:
          type: object
          properties:
            correlation_id:
              type: string
              description: >-
                The correlation ID for tracking ledger entries with external
                systems (max 50 characters).
            reverses_correlation_id:
              type: string
              description: >-
                If this entry reverses another entry, the correlation ID of the
                entry it reverses.
            is_void:
              type: boolean
              description: >-
                If true, this entry is a void and should not be included in the
                ledger balance calculations.
            sequence:
              type: integer
              format: int64
              description: >-
                Sequence number of the ledger entry (less than 300). This field
                is used to maintain the correct

                order of entries when multiple entries share the same effective
                date. Entries are sorted first

                by effective date, then by sequence.
            entry_type_id:
              type: integer
              format: int32
              description: >-
                The type of ledger entry specified by the
                LedgerClassEntryType.id.
            posted_date:
              type: integer
              format: int32
              description: The posted date (days since epoch).
            effective_date:
              type: integer
              format: int32
              description: The effective date (days since epoch).
            total_amt:
              type: string
              description: >-
                The total amount of the ledger entry.

                The units of this field are defined by the denom field in this
                ledger's class.
            applied_amounts:
              type: array
              items:
                type: object
                properties:
                  bucket_type_id:
                    type: integer
                    format: int32
                    description: The bucket type specified by the LedgerClassBucketType.id.
                  applied_amt:
                    type: string
                    description: >-
                      The amount applied to the bucket.

                      The units of this field are defined by the denom field in
                      this ledger's class.
                description: >-
                  LedgerBucketAmount is the amount applied to a bucket.
                  Applications to a bucket increase or

                  decrease the balance of the bucket.
              description: >-
                Applied amounts represent how the entry affects different
                buckets.
            balance_amounts:
              type: array
              items:
                type: object
                properties:
                  bucket_type_id:
                    type: integer
                    format: int32
                    description: The bucket type specified by the LedgerClassBucketType.id.
                  balance_amt:
                    type: string
                    description: >-
                      The balance of the bucket.

                      The units of this field are defined by the denom field in
                      this ledger's class.
                description: >-
                  BucketBalance represents the balance for a specific bucket
                  type.
              description: >-
                Bucket balances represent the current state of funds in each
                bucket.
          description: >-
            LedgerEntry is an single entry in the ledger. An entry would be a
            payment, disbursement, adjustment, etc...
        description: The ledger entries.
    title: QueryLedgerEntriesResponse
  provenance.ledger.v1.QueryLedgerEntryResponse:
    type: object
    properties:
      entry:
        description: The ledger entry.
        type: object
        properties:
          correlation_id:
            type: string
            description: >-
              The correlation ID for tracking ledger entries with external
              systems (max 50 characters).
          reverses_correlation_id:
            type: string
            description: >-
              If this entry reverses another entry, the correlation ID of the
              entry it reverses.
          is_void:
            type: boolean
            description: >-
              If true, this entry is a void and should not be included in the
              ledger balance calculations.
          sequence:
            type: integer
            format: int64
            description: >-
              Sequence number of the ledger entry (less than 300). This field is
              used to maintain the correct

              order of entries when multiple entries share the same effective
              date. Entries are sorted first

              by effective date, then by sequence.
          entry_type_id:
            type: integer
            format: int32
            description: The type of ledger entry specified by the LedgerClassEntryType.id.
          posted_date:
            type: integer
            format: int32
            description: The posted date (days since epoch).
          effective_date:
            type: integer
            format: int32
            description: The effective date (days since epoch).
          total_amt:
            type: string
            description: >-
              The total amount of the ledger entry.

              The units of this field are defined by the denom field in this
              ledger's class.
          applied_amounts:
            type: array
            items:
              type: object
              properties:
                bucket_type_id:
                  type: integer
                  format: int32
                  description: The bucket type specified by the LedgerClassBucketType.id.
                applied_amt:
                  type: string
                  description: >-
                    The amount applied to the bucket.

                    The units of this field are defined by the denom field in
                    this ledger's class.
              description: >-
                LedgerBucketAmount is the amount applied to a bucket.
                Applications to a bucket increase or

                decrease the balance of the bucket.
            description: Applied amounts represent how the entry affects different buckets.
          balance_amounts:
            type: array
            items:
              type: object
              properties:
                bucket_type_id:
                  type: integer
                  format: int32
                  description: The bucket type specified by the LedgerClassBucketType.id.
                balance_amt:
                  type: string
                  description: >-
                    The balance of the bucket.

                    The units of this field are defined by the denom field in
                    this ledger's class.
              description: BucketBalance represents the balance for a specific bucket type.
            description: >-
              Bucket balances represent the current state of funds in each
              bucket.
    title: QueryLedgerEntryResponse
  provenance.ledger.v1.QueryLedgerResponse:
    type: object
    properties:
      ledger:
        description: The ledger data.
        type: object
        properties:
          key:
            description: The ledger key identifying the ledger.
            type: object
            properties:
              nft_id:
                type: string
                description: >-
                  Identifier for the nft that this ledger is linked to.

                  This could be a `x/metadata` scope id or an `x/nft` nft id.

                  In order to create a ledger for an nft, the nft class must be
                  registered in the ledger module as a LedgerClass.
              asset_class_id:
                type: string
                description: Scope Specification ID or NFT Class ID.
          ledger_class_id:
            type: string
            description: The ledger class ID for the ledger.
          status_type_id:
            type: integer
            format: int32
            description: The status of the ledger.
          next_pmt_date:
            type: integer
            format: int32
            description: The next payment date (days since epoch).
          next_pmt_amt:
            type: string
            description: >-
              The next payment amount.

              The units of this field are defined by the denom field in this
              ledger's class.
          interest_rate:
            type: integer
            format: int32
            description: >-
              The interest rate. Min = 0, Max = 100,000,000 = 100%, e.g.
              4,321,987 = 4.321987%.
          maturity_date:
            type: integer
            format: int32
            description: The maturity date (days since epoch).
          interest_day_count_convention:
            description: The day count convention for interest calculations.
            type: string
            enum:
              - DAY_COUNT_CONVENTION_UNSPECIFIED
              - DAY_COUNT_CONVENTION_ACTUAL_365
              - DAY_COUNT_CONVENTION_ACTUAL_360
              - DAY_COUNT_CONVENTION_THIRTY_360
              - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
              - DAY_COUNT_CONVENTION_DAYS_365
              - DAY_COUNT_CONVENTION_DAYS_360
              - DAY_COUNT_CONVENTION_NOT_DEFINED
            default: DAY_COUNT_CONVENTION_UNSPECIFIED
          interest_accrual_method:
            description: The interest accrual method for interest calculations.
            type: string
            enum:
              - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
              - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
              - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
              - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
            default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
          payment_frequency:
            description: The payment frequency.
            type: string
            enum:
              - PAYMENT_FREQUENCY_UNSPECIFIED
              - PAYMENT_FREQUENCY_DAILY
              - PAYMENT_FREQUENCY_WEEKLY
              - PAYMENT_FREQUENCY_MONTHLY
              - PAYMENT_FREQUENCY_QUARTERLY
              - PAYMENT_FREQUENCY_ANNUALLY
              - PAYMENT_FREQUENCY_NOT_DEFINED
            default: PAYMENT_FREQUENCY_UNSPECIFIED
    title: QueryLedgerResponse
  provenance.ledger.v1.QueryLedgerSettlementsByCorrelationIDResponse:
    type: object
    properties:
      settlements:
        type: array
        items:
          type: object
          properties:
            amount:
              description: The amount to be transferred.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            recipient_address:
              type: string
              description: The recipient's blockchain address.
            status:
              description: The current status of the funding transfer.
              type: string
              enum:
                - FUNDING_TRANSFER_STATUS_UNSPECIFIED
                - FUNDING_TRANSFER_STATUS_PENDING
                - FUNDING_TRANSFER_STATUS_PROCESSING
                - FUNDING_TRANSFER_STATUS_COMPLETED
                - FUNDING_TRANSFER_STATUS_FAILED
              default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
            memo:
              type: string
              description: An optional memo or note for the transaction.
          description: >-
            SettlementInstruction represents blockchain-specific settlement
            instructions.
    title: QueryLedgerSettlementsByCorrelationIDResponse
  provenance.ledger.v1.QueryLedgerSettlementsResponse:
    type: object
    properties:
      settlements:
        type: array
        items:
          type: object
          properties:
            amount:
              description: The amount to be transferred.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            recipient_address:
              type: string
              description: The recipient's blockchain address.
            status:
              description: The current status of the funding transfer.
              type: string
              enum:
                - FUNDING_TRANSFER_STATUS_UNSPECIFIED
                - FUNDING_TRANSFER_STATUS_PENDING
                - FUNDING_TRANSFER_STATUS_PROCESSING
                - FUNDING_TRANSFER_STATUS_COMPLETED
                - FUNDING_TRANSFER_STATUS_FAILED
              default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
            memo:
              type: string
              description: An optional memo or note for the transaction.
          description: >-
            SettlementInstruction represents blockchain-specific settlement
            instructions.
    title: QueryLedgerSettlementsResponse
  provenance.ledger.v1.QueryLedgersResponse:
    type: object
    properties:
      ledgers:
        type: array
        items:
          type: object
          properties:
            key:
              description: The ledger key identifying the ledger.
              type: object
              properties:
                nft_id:
                  type: string
                  description: >-
                    Identifier for the nft that this ledger is linked to.

                    This could be a `x/metadata` scope id or an `x/nft` nft id.

                    In order to create a ledger for an nft, the nft class must
                    be registered in the ledger module as a LedgerClass.
                asset_class_id:
                  type: string
                  description: Scope Specification ID or NFT Class ID.
            ledger_class_id:
              type: string
              description: The ledger class ID for the ledger.
            status_type_id:
              type: integer
              format: int32
              description: The status of the ledger.
            next_pmt_date:
              type: integer
              format: int32
              description: The next payment date (days since epoch).
            next_pmt_amt:
              type: string
              description: >-
                The next payment amount.

                The units of this field are defined by the denom field in this
                ledger's class.
            interest_rate:
              type: integer
              format: int32
              description: >-
                The interest rate. Min = 0, Max = 100,000,000 = 100%, e.g.
                4,321,987 = 4.321987%.
            maturity_date:
              type: integer
              format: int32
              description: The maturity date (days since epoch).
            interest_day_count_convention:
              description: The day count convention for interest calculations.
              type: string
              enum:
                - DAY_COUNT_CONVENTION_UNSPECIFIED
                - DAY_COUNT_CONVENTION_ACTUAL_365
                - DAY_COUNT_CONVENTION_ACTUAL_360
                - DAY_COUNT_CONVENTION_THIRTY_360
                - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
                - DAY_COUNT_CONVENTION_DAYS_365
                - DAY_COUNT_CONVENTION_DAYS_360
                - DAY_COUNT_CONVENTION_NOT_DEFINED
              default: DAY_COUNT_CONVENTION_UNSPECIFIED
            interest_accrual_method:
              description: The interest accrual method for interest calculations.
              type: string
              enum:
                - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
                - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
                - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
                - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
                - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
                - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
                - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
                - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
                - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
              default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
            payment_frequency:
              description: The payment frequency.
              type: string
              enum:
                - PAYMENT_FREQUENCY_UNSPECIFIED
                - PAYMENT_FREQUENCY_DAILY
                - PAYMENT_FREQUENCY_WEEKLY
                - PAYMENT_FREQUENCY_MONTHLY
                - PAYMENT_FREQUENCY_QUARTERLY
                - PAYMENT_FREQUENCY_ANNUALLY
                - PAYMENT_FREQUENCY_NOT_DEFINED
              default: PAYMENT_FREQUENCY_UNSPECIFIED
          description: Ledger defines an servicing ledger for an asset.
        description: List of ledgers.
      pagination:
        description: pagination is the pagination details for this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: QueryLedgersResponse
  provenance.ledger.v1.SettlementInstruction:
    type: object
    properties:
      amount:
        description: The amount to be transferred.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      recipient_address:
        type: string
        description: The recipient's blockchain address.
      status:
        description: The current status of the funding transfer.
        type: string
        enum:
          - FUNDING_TRANSFER_STATUS_UNSPECIFIED
          - FUNDING_TRANSFER_STATUS_PENDING
          - FUNDING_TRANSFER_STATUS_PROCESSING
          - FUNDING_TRANSFER_STATUS_COMPLETED
          - FUNDING_TRANSFER_STATUS_FAILED
        default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
      memo:
        type: string
        description: An optional memo or note for the transaction.
    description: >-
      SettlementInstruction represents blockchain-specific settlement
      instructions.
  provenance.ledger.v1.FundTransferWithSettlement:
    type: object
    properties:
      key:
        description: The ledger key identifying the ledger for this transfer.
        type: object
        properties:
          nft_id:
            type: string
            description: >-
              Identifier for the nft that this ledger is linked to.

              This could be a `x/metadata` scope id or an `x/nft` nft id.

              In order to create a ledger for an nft, the nft class must be
              registered in the ledger module as a LedgerClass.
          asset_class_id:
            type: string
            description: Scope Specification ID or NFT Class ID.
      ledger_entry_correlation_id:
        type: string
        description: The correlation ID of the ledger entry associated with this transfer.
      settlement_instructions:
        type: array
        items:
          type: object
          properties:
            amount:
              description: The amount to be transferred.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            recipient_address:
              type: string
              description: The recipient's blockchain address.
            status:
              description: The current status of the funding transfer.
              type: string
              enum:
                - FUNDING_TRANSFER_STATUS_UNSPECIFIED
                - FUNDING_TRANSFER_STATUS_PENDING
                - FUNDING_TRANSFER_STATUS_PROCESSING
                - FUNDING_TRANSFER_STATUS_COMPLETED
                - FUNDING_TRANSFER_STATUS_FAILED
              default: FUNDING_TRANSFER_STATUS_UNSPECIFIED
            memo:
              type: string
              description: An optional memo or note for the transaction.
          description: >-
            SettlementInstruction represents blockchain-specific settlement
            instructions.
    description: >-
      FundTransferWithSettlement represents a fund transfer with settlement
      instructions.
  provenance.ledger.v1.LedgerAndEntries:
    type: object
    properties:
      ledger_key:
        description: The ledger key identifying the ledger.
        type: object
        properties:
          nft_id:
            type: string
            description: >-
              Identifier for the nft that this ledger is linked to.

              This could be a `x/metadata` scope id or an `x/nft` nft id.

              In order to create a ledger for an nft, the nft class must be
              registered in the ledger module as a LedgerClass.
          asset_class_id:
            type: string
            description: Scope Specification ID or NFT Class ID.
      ledger:
        description: The ledger data.
        type: object
        properties:
          key:
            description: The ledger key identifying the ledger.
            type: object
            properties:
              nft_id:
                type: string
                description: >-
                  Identifier for the nft that this ledger is linked to.

                  This could be a `x/metadata` scope id or an `x/nft` nft id.

                  In order to create a ledger for an nft, the nft class must be
                  registered in the ledger module as a LedgerClass.
              asset_class_id:
                type: string
                description: Scope Specification ID or NFT Class ID.
          ledger_class_id:
            type: string
            description: The ledger class ID for the ledger.
          status_type_id:
            type: integer
            format: int32
            description: The status of the ledger.
          next_pmt_date:
            type: integer
            format: int32
            description: The next payment date (days since epoch).
          next_pmt_amt:
            type: string
            description: >-
              The next payment amount.

              The units of this field are defined by the denom field in this
              ledger's class.
          interest_rate:
            type: integer
            format: int32
            description: >-
              The interest rate. Min = 0, Max = 100,000,000 = 100%, e.g.
              4,321,987 = 4.321987%.
          maturity_date:
            type: integer
            format: int32
            description: The maturity date (days since epoch).
          interest_day_count_convention:
            description: The day count convention for interest calculations.
            type: string
            enum:
              - DAY_COUNT_CONVENTION_UNSPECIFIED
              - DAY_COUNT_CONVENTION_ACTUAL_365
              - DAY_COUNT_CONVENTION_ACTUAL_360
              - DAY_COUNT_CONVENTION_THIRTY_360
              - DAY_COUNT_CONVENTION_ACTUAL_ACTUAL
              - DAY_COUNT_CONVENTION_DAYS_365
              - DAY_COUNT_CONVENTION_DAYS_360
              - DAY_COUNT_CONVENTION_NOT_DEFINED
            default: DAY_COUNT_CONVENTION_UNSPECIFIED
          interest_accrual_method:
            description: The interest accrual method for interest calculations.
            type: string
            enum:
              - INTEREST_ACCRUAL_METHOD_UNSPECIFIED
              - INTEREST_ACCRUAL_METHOD_SIMPLE_INTEREST
              - INTEREST_ACCRUAL_METHOD_COMPOUND_INTEREST
              - INTEREST_ACCRUAL_METHOD_DAILY_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_MONTHLY_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_QUARTERLY_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_ANNUAL_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_CONTINUOUS_COMPOUNDING
              - INTEREST_ACCRUAL_METHOD_NOT_DEFINED
            default: INTEREST_ACCRUAL_METHOD_UNSPECIFIED
          payment_frequency:
            description: The payment frequency.
            type: string
            enum:
              - PAYMENT_FREQUENCY_UNSPECIFIED
              - PAYMENT_FREQUENCY_DAILY
              - PAYMENT_FREQUENCY_WEEKLY
              - PAYMENT_FREQUENCY_MONTHLY
              - PAYMENT_FREQUENCY_QUARTERLY
              - PAYMENT_FREQUENCY_ANNUALLY
              - PAYMENT_FREQUENCY_NOT_DEFINED
            default: PAYMENT_FREQUENCY_UNSPECIFIED
      entries:
        type: array
        items:
          type: object
          properties:
            correlation_id:
              type: string
              description: >-
                The correlation ID for tracking ledger entries with external
                systems (max 50 characters).
            reverses_correlation_id:
              type: string
              description: >-
                If this entry reverses another entry, the correlation ID of the
                entry it reverses.
            is_void:
              type: boolean
              description: >-
                If true, this entry is a void and should not be included in the
                ledger balance calculations.
            sequence:
              type: integer
              format: int64
              description: >-
                Sequence number of the ledger entry (less than 300). This field
                is used to maintain the correct

                order of entries when multiple entries share the same effective
                date. Entries are sorted first

                by effective date, then by sequence.
            entry_type_id:
              type: integer
              format: int32
              description: >-
                The type of ledger entry specified by the
                LedgerClassEntryType.id.
            posted_date:
              type: integer
              format: int32
              description: The posted date (days since epoch).
            effective_date:
              type: integer
              format: int32
              description: The effective date (days since epoch).
            total_amt:
              type: string
              description: >-
                The total amount of the ledger entry.

                The units of this field are defined by the denom field in this
                ledger's class.
            applied_amounts:
              type: array
              items:
                type: object
                properties:
                  bucket_type_id:
                    type: integer
                    format: int32
                    description: The bucket type specified by the LedgerClassBucketType.id.
                  applied_amt:
                    type: string
                    description: >-
                      The amount applied to the bucket.

                      The units of this field are defined by the denom field in
                      this ledger's class.
                description: >-
                  LedgerBucketAmount is the amount applied to a bucket.
                  Applications to a bucket increase or

                  decrease the balance of the bucket.
              description: >-
                Applied amounts represent how the entry affects different
                buckets.
            balance_amounts:
              type: array
              items:
                type: object
                properties:
                  bucket_type_id:
                    type: integer
                    format: int32
                    description: The bucket type specified by the LedgerClassBucketType.id.
                  balance_amt:
                    type: string
                    description: >-
                      The balance of the bucket.

                      The units of this field are defined by the denom field in
                      this ledger's class.
                description: >-
                  BucketBalance represents the balance for a specific bucket
                  type.
              description: >-
                Bucket balances represent the current state of funds in each
                bucket.
          description: >-
            LedgerEntry is an single entry in the ledger. An entry would be a
            payment, disbursement, adjustment, etc...
        description: The ledger entries.
    description: LedgerAndEntries represents a ledger with its associated entries.
  provenance.ledger.v1.MsgAddLedgerClassBucketTypeResponse:
    type: object
    description: >-
      MsgAddLedgerClassBucketTypeResponse represents the response from adding a
      bucket type.
  provenance.ledger.v1.MsgAddLedgerClassEntryTypeResponse:
    type: object
    description: >-
      MsgAddLedgerClassEntryTypeResponse represents the response from adding an
      entry type.
  provenance.ledger.v1.MsgAddLedgerClassStatusTypeResponse:
    type: object
    description: >-
      MsgAddLedgerClassStatusTypeResponse represents the response from adding a
      status type.
  provenance.ledger.v1.MsgAppendResponse:
    type: object
    description: >-
      MsgAppendResponse represents the response from appending entries to a
      ledger.
  provenance.ledger.v1.MsgBulkCreateResponse:
    type: object
    description: >-
      MsgBulkCreateResponse represents the response from bulk creating ledgers
      and entries.
  provenance.ledger.v1.MsgCreateLedgerClassResponse:
    type: object
    description: >-
      MsgCreateLedgerClassResponse represents the response from creating a
      ledger class.
  provenance.ledger.v1.MsgCreateLedgerResponse:
    type: object
    description: MsgCreateLedgerResponse represents the response from creating a ledger.
  provenance.ledger.v1.MsgDestroyResponse:
    type: object
    description: MsgDestroyResponse represents the response from destroying a ledger.
  provenance.ledger.v1.MsgTransferFundsWithSettlementResponse:
    type: object
    description: >-
      MsgTransferFundsWithSettlementResponse represents the response from
      transferring funds with settlement.
  provenance.ledger.v1.MsgUpdateBalancesResponse:
    type: object
    description: >-
      MsgUpdateBalancesResponse represents the response from updating ledger
      balances.
  provenance.ledger.v1.MsgUpdateInterestRateResponse:
    type: object
    description: >-
      MsgUpdateInterestRateResponse represents the response from updating a
      ledger interest rate.
  provenance.ledger.v1.MsgUpdateMaturityDateResponse:
    type: object
    description: >-
      MsgUpdateMaturityDateResponse represents the response from updating a
      ledger maturity date.
  provenance.ledger.v1.MsgUpdatePaymentResponse:
    type: object
    description: >-
      MsgUpdatePaymentResponse represents the response from updating a ledger
      payment configuration.
  provenance.ledger.v1.MsgUpdateStatusResponse:
    type: object
    description: >-
      MsgUpdateStatusResponse represents the response from updating a ledger
      status.
  cosmos.bank.v1beta1.DenomUnit:
    type: object
    properties:
      denom:
        type: string
        description: denom represents the string name of the given denom unit (e.g uatom).
      exponent:
        type: integer
        format: int64
        description: >-
          exponent represents power of 10 exponent that one must

          raise the base_denom to in order to equal the given DenomUnit's denom

          1 denom = 10^exponent base_denom

          (e.g. with a base_denom of uatom, one can create a DenomUnit of 'atom'
          with

          exponent = 6, thus: 1 atom = 10^6 uatom).
      aliases:
        type: array
        items:
          type: string
        title: aliases is a list of string aliases for the given denom
    description: |-
      DenomUnit represents a struct that describes a given
      denomination unit of the basic token.
  cosmos.bank.v1beta1.Metadata:
    type: object
    properties:
      description:
        type: string
      denom_units:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
              description: >-
                denom represents the string name of the given denom unit (e.g
                uatom).
            exponent:
              type: integer
              format: int64
              description: >-
                exponent represents power of 10 exponent that one must

                raise the base_denom to in order to equal the given DenomUnit's
                denom

                1 denom = 10^exponent base_denom

                (e.g. with a base_denom of uatom, one can create a DenomUnit of
                'atom' with

                exponent = 6, thus: 1 atom = 10^6 uatom).
            aliases:
              type: array
              items:
                type: string
              title: aliases is a list of string aliases for the given denom
          description: |-
            DenomUnit represents a struct that describes a given
            denomination unit of the basic token.
        title: denom_units represents the list of DenomUnit's for a given coin
      base:
        type: string
        description: >-
          base represents the base denom (should be the DenomUnit with exponent
          = 0).
      display:
        type: string
        description: |-
          display indicates the suggested denom that should be
          displayed in clients.
      name:
        type: string
        title: 'name defines the name of the token (eg: Cosmos Atom)'
      symbol:
        type: string
        description: >-
          symbol is the token symbol usually shown on exchanges (eg: ATOM). This
          can

          be the same as the display.
      uri:
        type: string
        description: >-
          URI to a document (on or off-chain) that contains additional
          information. Optional.
      uri_hash:
        type: string
        description: >-
          URIHash is a sha256 hash of a document pointed by URI. It's used to
          verify that

          the document didn't change. Optional.
      denomUnits:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
              description: >-
                denom represents the string name of the given denom unit (e.g
                uatom).
            exponent:
              type: integer
              format: int64
              description: >-
                exponent represents power of 10 exponent that one must

                raise the base_denom to in order to equal the given DenomUnit's
                denom

                1 denom = 10^exponent base_denom

                (e.g. with a base_denom of uatom, one can create a DenomUnit of
                'atom' with

                exponent = 6, thus: 1 atom = 10^6 uatom).
            aliases:
              type: array
              items:
                type: string
              title: aliases is a list of string aliases for the given denom
          description: |-
            DenomUnit represents a struct that describes a given
            denomination unit of the basic token.
        title: denom_units represents the list of DenomUnit's for a given coin
      uriHash:
        type: string
        description: >-
          URIHash is a sha256 hash of a document pointed by URI. It's used to
          verify that

          the document didn't change. Optional.
    description: |-
      Metadata represents a struct that describes
      a basic token.
  provenance.marker.v1.Access:
    type: string
    enum:
      - ACCESS_UNSPECIFIED
      - ACCESS_MINT
      - ACCESS_BURN
      - ACCESS_DEPOSIT
      - ACCESS_WITHDRAW
      - ACCESS_DELETE
      - ACCESS_ADMIN
      - ACCESS_TRANSFER
      - ACCESS_FORCE_TRANSFER
    default: ACCESS_UNSPECIFIED
    description: >-
      Access defines the different types of permissions that a marker supports
      granting to an address.

       - ACCESS_UNSPECIFIED: ACCESS_UNSPECIFIED defines a no-op vote option.
       - ACCESS_MINT: ACCESS_MINT is the ability to increase the supply of a marker.
       - ACCESS_BURN: ACCESS_BURN is the ability to decrease the supply of the marker using coin held by the marker.
       - ACCESS_DEPOSIT: ACCESS_DEPOSIT is the ability to transfer funds from another account to this marker account
      or to set a reference to this marker in the metadata/scopes module.
       - ACCESS_WITHDRAW: ACCESS_WITHDRAW is the ability to transfer funds from this marker account to another account
      or to remove a reference to this marker in the metadata/scopes module.
       - ACCESS_DELETE: ACCESS_DELETE is the ability to move a proposed, finalized or active marker into the cancelled state.
      This access also allows cancelled markers to be marked for deletion.
       - ACCESS_ADMIN: ACCESS_ADMIN is the ability to add access grants for accounts to the list of marker permissions.
      This access also gives the ability to update the marker's denom metadata.
       - ACCESS_TRANSFER: ACCESS_TRANSFER is the ability to manage transfer settings and broker transfers of the marker.
      Accounts with this access can:
       - Update the marker's required attributes.
       - Update the send-deny list.
       - Use the transfer or bank send endpoints to move marker funds out of their own account.
      This access right is only supported on RESTRICTED markers.
       - ACCESS_FORCE_TRANSFER: ACCESS_FORCE_TRANSFER is the ability to transfer restricted coins from a 3rd-party account without their signature.
      This access right is only supported on RESTRICTED markers and only has
      meaning when allow_forced_transfer is true.
  provenance.marker.v1.AccessGrant:
    type: object
    properties:
      address:
        type: string
      permissions:
        type: array
        items:
          type: string
          enum:
            - ACCESS_UNSPECIFIED
            - ACCESS_MINT
            - ACCESS_BURN
            - ACCESS_DEPOSIT
            - ACCESS_WITHDRAW
            - ACCESS_DELETE
            - ACCESS_ADMIN
            - ACCESS_TRANSFER
            - ACCESS_FORCE_TRANSFER
          default: ACCESS_UNSPECIFIED
          description: >-
            Access defines the different types of permissions that a marker
            supports granting to an address.

             - ACCESS_UNSPECIFIED: ACCESS_UNSPECIFIED defines a no-op vote option.
             - ACCESS_MINT: ACCESS_MINT is the ability to increase the supply of a marker.
             - ACCESS_BURN: ACCESS_BURN is the ability to decrease the supply of the marker using coin held by the marker.
             - ACCESS_DEPOSIT: ACCESS_DEPOSIT is the ability to transfer funds from another account to this marker account
            or to set a reference to this marker in the metadata/scopes module.
             - ACCESS_WITHDRAW: ACCESS_WITHDRAW is the ability to transfer funds from this marker account to another account
            or to remove a reference to this marker in the metadata/scopes
            module.
             - ACCESS_DELETE: ACCESS_DELETE is the ability to move a proposed, finalized or active marker into the cancelled state.
            This access also allows cancelled markers to be marked for deletion.
             - ACCESS_ADMIN: ACCESS_ADMIN is the ability to add access grants for accounts to the list of marker permissions.
            This access also gives the ability to update the marker's denom
            metadata.
             - ACCESS_TRANSFER: ACCESS_TRANSFER is the ability to manage transfer settings and broker transfers of the marker.
            Accounts with this access can:
             - Update the marker's required attributes.
             - Update the send-deny list.
             - Use the transfer or bank send endpoints to move marker funds out of their own account.
            This access right is only supported on RESTRICTED markers.
             - ACCESS_FORCE_TRANSFER: ACCESS_FORCE_TRANSFER is the ability to transfer restricted coins from a 3rd-party account without their signature.
            This access right is only supported on RESTRICTED markers and only
            has meaning when allow_forced_transfer is true.
    description: >-
      AccessGrant associates a collection of permissions with an address for
      delegated marker account control.
  provenance.marker.v1.Balance:
    type: object
    properties:
      address:
        type: string
        description: address is the address of the balance holder.
      coins:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: coins defines the different coins this balance holds.
    title: >-
      Balance defines an account address and balance pair used in queries for
      accounts holding a marker
  provenance.marker.v1.MarkerStatus:
    type: string
    enum:
      - MARKER_STATUS_UNSPECIFIED
      - MARKER_STATUS_PROPOSED
      - MARKER_STATUS_FINALIZED
      - MARKER_STATUS_ACTIVE
      - MARKER_STATUS_CANCELLED
      - MARKER_STATUS_DESTROYED
    default: MARKER_STATUS_UNSPECIFIED
    description: |-
      MarkerStatus defines the various states a marker account can be in.

       - MARKER_STATUS_UNSPECIFIED: MARKER_STATUS_UNSPECIFIED - Unknown/Invalid Marker Status
       - MARKER_STATUS_PROPOSED: MARKER_STATUS_PROPOSED - Initial configuration period, updates allowed, token supply not created.
       - MARKER_STATUS_FINALIZED: MARKER_STATUS_FINALIZED - Configuration finalized, ready for supply creation
       - MARKER_STATUS_ACTIVE: MARKER_STATUS_ACTIVE - Supply is created, rules are in force.
       - MARKER_STATUS_CANCELLED: MARKER_STATUS_CANCELLED - Marker has been cancelled, pending destroy
       - MARKER_STATUS_DESTROYED: MARKER_STATUS_DESTROYED - Marker supply has all been recalled, marker is considered destroyed and no further
      actions allowed.
  provenance.marker.v1.NetAssetValue:
    type: object
    properties:
      price:
        title: price is the complete value of the asset's volume
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
      volume:
        type: string
        format: uint64
        title: >-
          volume is the number of tokens of the marker that were purchased for
          the price
      updated_block_height:
        type: string
        format: uint64
        title: updated_block_height is the block height of last update
    title: NetAssetValue defines a marker's net asset value
  provenance.marker.v1.Params:
    type: object
    properties:
      max_total_supply:
        type: string
        format: uint64
        title: >-
          Deprecated: Prefer to use `max_supply` instead. Maximum amount of
          supply to allow a marker to be created with
      enable_governance:
        type: boolean
        description: indicates if governance based controls of markers is allowed.
      unrestricted_denom_regex:
        type: string
        title: >-
          a regular expression used to validate marker denom values from normal
          create requests (governance

          requests are only subject to platform coin validation denom
          expression)
      max_supply:
        type: string
        title: maximum amount of supply to allow a marker to be created with
    description: Params defines the set of params for the account module.
  provenance.marker.v1.QueryAccessResponse:
    type: object
    properties:
      accounts:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            permissions:
              type: array
              items:
                type: string
                enum:
                  - ACCESS_UNSPECIFIED
                  - ACCESS_MINT
                  - ACCESS_BURN
                  - ACCESS_DEPOSIT
                  - ACCESS_WITHDRAW
                  - ACCESS_DELETE
                  - ACCESS_ADMIN
                  - ACCESS_TRANSFER
                  - ACCESS_FORCE_TRANSFER
                default: ACCESS_UNSPECIFIED
                description: >-
                  Access defines the different types of permissions that a
                  marker supports granting to an address.

                   - ACCESS_UNSPECIFIED: ACCESS_UNSPECIFIED defines a no-op vote option.
                   - ACCESS_MINT: ACCESS_MINT is the ability to increase the supply of a marker.
                   - ACCESS_BURN: ACCESS_BURN is the ability to decrease the supply of the marker using coin held by the marker.
                   - ACCESS_DEPOSIT: ACCESS_DEPOSIT is the ability to transfer funds from another account to this marker account
                  or to set a reference to this marker in the metadata/scopes
                  module.
                   - ACCESS_WITHDRAW: ACCESS_WITHDRAW is the ability to transfer funds from this marker account to another account
                  or to remove a reference to this marker in the metadata/scopes
                  module.
                   - ACCESS_DELETE: ACCESS_DELETE is the ability to move a proposed, finalized or active marker into the cancelled state.
                  This access also allows cancelled markers to be marked for
                  deletion.
                   - ACCESS_ADMIN: ACCESS_ADMIN is the ability to add access grants for accounts to the list of marker permissions.
                  This access also gives the ability to update the marker's
                  denom metadata.
                   - ACCESS_TRANSFER: ACCESS_TRANSFER is the ability to manage transfer settings and broker transfers of the marker.
                  Accounts with this access can:
                   - Update the marker's required attributes.
                   - Update the send-deny list.
                   - Use the transfer or bank send endpoints to move marker funds out of their own account.
                  This access right is only supported on RESTRICTED markers.
                   - ACCESS_FORCE_TRANSFER: ACCESS_FORCE_TRANSFER is the ability to transfer restricted coins from a 3rd-party account without their signature.
                  This access right is only supported on RESTRICTED markers and
                  only has meaning when allow_forced_transfer is true.
          description: >-
            AccessGrant associates a collection of permissions with an address
            for delegated marker account control.
    description: >-
      QueryAccessResponse is the response type for the Query/MarkerAccess
      method.
  provenance.marker.v1.QueryAccountDataResponse:
    type: object
    properties:
      value:
        type: string
        description: The accountdata for the requested denom.
    title: QueryAccountDataResponse is the response type for the Query/AccountData
  provenance.marker.v1.QueryAllMarkersResponse:
    type: object
    properties:
      markers:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := ptypes.MarshalAny(foo)
                 ...
                 foo := &pb.Foo{}
                 if err := ptypes.UnmarshalAny(any, foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".



            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAllMarkersResponse is the response type for the Query/AllMarkers
      method.
  provenance.marker.v1.QueryDenomMetadataResponse:
    type: object
    properties:
      metadata:
        type: object
        properties:
          description:
            type: string
          denom_units:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                  description: >-
                    denom represents the string name of the given denom unit
                    (e.g uatom).
                exponent:
                  type: integer
                  format: int64
                  description: >-
                    exponent represents power of 10 exponent that one must

                    raise the base_denom to in order to equal the given
                    DenomUnit's denom

                    1 denom = 10^exponent base_denom

                    (e.g. with a base_denom of uatom, one can create a DenomUnit
                    of 'atom' with

                    exponent = 6, thus: 1 atom = 10^6 uatom).
                aliases:
                  type: array
                  items:
                    type: string
                  title: aliases is a list of string aliases for the given denom
              description: |-
                DenomUnit represents a struct that describes a given
                denomination unit of the basic token.
            title: denom_units represents the list of DenomUnit's for a given coin
          base:
            type: string
            description: >-
              base represents the base denom (should be the DenomUnit with
              exponent = 0).
          display:
            type: string
            description: |-
              display indicates the suggested denom that should be
              displayed in clients.
          name:
            type: string
            title: 'name defines the name of the token (eg: Cosmos Atom)'
          symbol:
            type: string
            description: >-
              symbol is the token symbol usually shown on exchanges (eg: ATOM).
              This can

              be the same as the display.
          uri:
            type: string
            description: >-
              URI to a document (on or off-chain) that contains additional
              information. Optional.
          uri_hash:
            type: string
            description: >-
              URIHash is a sha256 hash of a document pointed by URI. It's used
              to verify that

              the document didn't change. Optional.
        description: |-
          Metadata represents a struct that describes
          a basic token.
    title: >-
      QueryDenomMetadataResponse is the response type for the
      Query/DenomMetadata
  provenance.marker.v1.QueryEscrowResponse:
    type: object
    properties:
      escrow:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
    description: >-
      QueryEscrowResponse is the response type for the Query/MarkerEscrow
      method.
  provenance.marker.v1.QueryHoldingResponse:
    type: object
    properties:
      balances:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address is the address of the balance holder.
            coins:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: coins defines the different coins this balance holds.
          title: >-
            Balance defines an account address and balance pair used in queries
            for accounts holding a marker
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryHoldingResponse is the response type for the Query/MarkerHolders
      method.
  provenance.marker.v1.QueryMarkerResponse:
    type: object
    properties:
      marker:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := ptypes.MarshalAny(foo)
               ...
               foo := &pb.Foo{}
               if err := ptypes.UnmarshalAny(any, foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".



          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
    description: QueryMarkerResponse is the response type for the Query/Marker method.
  provenance.marker.v1.QueryNetAssetValuesResponse:
    type: object
    properties:
      net_asset_values:
        type: array
        items:
          type: object
          properties:
            price:
              title: price is the complete value of the asset's volume
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            volume:
              type: string
              format: uint64
              title: >-
                volume is the number of tokens of the marker that were purchased
                for the price
            updated_block_height:
              type: string
              format: uint64
              title: updated_block_height is the block height of last update
          title: NetAssetValue defines a marker's net asset value
        title: net asset values for marker denom
    description: >-
      QueryNetAssetValuesRequest is the response type for the
      Query/NetAssetValues method.
  provenance.marker.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          max_total_supply:
            type: string
            format: uint64
            title: >-
              Deprecated: Prefer to use `max_supply` instead. Maximum amount of
              supply to allow a marker to be created with
          enable_governance:
            type: boolean
            description: indicates if governance based controls of markers is allowed.
          unrestricted_denom_regex:
            type: string
            title: >-
              a regular expression used to validate marker denom values from
              normal create requests (governance

              requests are only subject to platform coin validation denom
              expression)
          max_supply:
            type: string
            title: maximum amount of supply to allow a marker to be created with
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  provenance.marker.v1.QuerySupplyResponse:
    type: object
    properties:
      amount:
        description: amount is the supply of the marker.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      QuerySupplyResponse is the response type for the Query/MarkerSupply
      method.

      Deprecated: This returns initial/target supply. Use the bank module's
      SupplyOf query for actual circulating supply.
  ibc.applications.transfer.v1.MsgTransfer:
    type: object
    properties:
      source_port:
        type: string
        title: the port on which the packet will be sent
      source_channel:
        type: string
        title: the channel by which the packet will be sent
      token:
        title: token to be transferred
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
      sender:
        type: string
        title: the sender address
      receiver:
        type: string
        title: the recipient address on the destination chain
      timeout_height:
        description: >-
          Timeout height relative to the current block height.

          If you are sending with IBC v1 protocol, either timeout_height or
          timeout_timestamp must be set.

          If you are sending with IBC v2 protocol, timeout_timestamp must be
          set, and timeout_height must be omitted.
        type: object
        properties:
          revision_number:
            type: string
            format: uint64
            title: the revision that the client is currently on
          revision_height:
            type: string
            format: uint64
            title: the height within the given revision
        title: >-
          Height is a monotonically increasing data type

          that can be compared against another Height for the purposes of
          updating and

          freezing clients
      timeout_timestamp:
        type: string
        format: uint64
        description: >-
          Timeout timestamp in absolute nanoseconds since unix epoch.

          If you are sending with IBC v1 protocol, either timeout_height or
          timeout_timestamp must be set.

          If you are sending with IBC v2 protocol, timeout_timestamp must be
          set.
      memo:
        type: string
        title: optional memo
      encoding:
        type: string
        title: optional encoding
    title: >-
      MsgTransfer defines a msg to transfer fungible tokens (i.e Coins) between

      ICS20 enabled chains. See ICS Spec here:

      https://github.com/cosmos/ibc/tree/master/spec/app/ics-020-fungible-token-transfer#data-structures
  ibc.core.client.v1.Height:
    type: object
    properties:
      revision_number:
        type: string
        format: uint64
        title: the revision that the client is currently on
      revision_height:
        type: string
        format: uint64
        title: the height within the given revision
    description: >-
      Normally the RevisionHeight is incremented at each height while keeping

      RevisionNumber the same. However some consensus algorithms may choose to

      reset the height in certain conditions e.g. hard forks, state-machine

      breaking changes In these cases, the RevisionNumber is incremented so that

      height continues to be monitonically increasing even as the RevisionHeight

      gets reset


      Please note that json tags for generated Go code are overridden to
      explicitly exclude the omitempty jsontag.

      This enforces the Go json marshaller to always emit zero values for both
      revision_number and revision_height.
    title: >-
      Height is a monotonically increasing data type

      that can be compared against another Height for the purposes of updating
      and

      freezing clients
  provenance.marker.v1.MarkerType:
    type: string
    enum:
      - MARKER_TYPE_UNSPECIFIED
      - MARKER_TYPE_COIN
      - MARKER_TYPE_RESTRICTED
    default: MARKER_TYPE_UNSPECIFIED
    description: >-
      - MARKER_TYPE_UNSPECIFIED: MARKER_TYPE_UNSPECIFIED is an invalid/unknown
      marker type.
       - MARKER_TYPE_COIN: MARKER_TYPE_COIN is a marker that represents a standard fungible coin (default).
       - MARKER_TYPE_RESTRICTED: MARKER_TYPE_RESTRICTED is a marker that represents a denom with send_enabled = false.
    title: MarkerType defines the types of marker
  provenance.marker.v1.MsgActivateResponse:
    type: object
    title: MsgActivateResponse defines the Msg/Activate response type
  provenance.marker.v1.MsgAddAccessResponse:
    type: object
    title: MsgAddAccessResponse defines the Msg/AddAccess response type
  provenance.marker.v1.MsgAddFinalizeActivateMarkerResponse:
    type: object
    title: >-
      MsgAddFinalizeActivateMarkerResponse defines the
      Msg/AddFinalizeActivateMarker response type
  provenance.marker.v1.MsgAddMarkerResponse:
    type: object
    title: MsgAddMarkerResponse defines the Msg/AddMarker response type
  provenance.marker.v1.MsgAddNetAssetValuesResponse:
    type: object
    title: >-
      MsgAddNetAssetValuesResponse defines the Msg/AddNetAssetValue response
      type
  provenance.marker.v1.MsgBurnResponse:
    type: object
    title: MsgBurnResponse defines the Msg/Burn response type
  provenance.marker.v1.MsgCancelResponse:
    type: object
    title: MsgCancelResponse defines the Msg/Cancel response type
  provenance.marker.v1.MsgChangeStatusProposalResponse:
    type: object
    title: >-
      MsgChangeStatusProposalResponse defines the Msg/ChangeStatusProposal
      response type
  provenance.marker.v1.MsgDeleteAccessResponse:
    type: object
    title: MsgDeleteAccessResponse defines the Msg/DeleteAccess response type
  provenance.marker.v1.MsgDeleteResponse:
    type: object
    title: MsgDeleteResponse defines the Msg/Delete response type
  provenance.marker.v1.MsgFinalizeResponse:
    type: object
    title: MsgFinalizeResponse defines the Msg/Finalize response type
  provenance.marker.v1.MsgGrantAllowanceResponse:
    type: object
    description: >-
      MsgGrantAllowanceResponse defines the Msg/GrantAllowanceResponse response
      type.
  provenance.marker.v1.MsgIbcTransferResponse:
    type: object
    title: MsgIbcTransferResponse defines the Msg/IbcTransfer response type
  provenance.marker.v1.MsgMintResponse:
    type: object
    title: MsgMintResponse defines the Msg/Mint response type
  provenance.marker.v1.MsgRemoveAdministratorProposalResponse:
    type: object
    title: >-
      MsgRemoveAdministratorProposalResponse defines the
      Msg/RemoveAdministratorProposal response type
  provenance.marker.v1.MsgRevokeGrantAllowanceResponse:
    type: object
    description: >-
      MsgRevokeGrantResponse is a response message for the
      RevokeFeeGrantAllowance endpoint.
  provenance.marker.v1.MsgSetAccountDataResponse:
    type: object
    title: MsgSetAccountDataResponse defines the Msg/SetAccountData response type
  provenance.marker.v1.MsgSetAdministratorProposalResponse:
    type: object
    title: >-
      MsgSetAdministratorProposalResponse defines the
      Msg/SetAdministratorProposal response type
  provenance.marker.v1.MsgSetDenomMetadataProposalResponse:
    type: object
    title: >-
      MsgSetDenomMetadataProposalResponse defines the
      Msg/SetDenomMetadataProposal response type
  provenance.marker.v1.MsgSetDenomMetadataResponse:
    type: object
    title: MsgSetDenomMetadataResponse defines the Msg/SetDenomMetadata response type
  provenance.marker.v1.MsgSupplyDecreaseProposalResponse:
    type: object
    title: >-
      MsgSupplyIncreaseProposalResponse defines the Msg/SupplyDecreaseProposal
      response type
  provenance.marker.v1.MsgSupplyIncreaseProposalResponse:
    type: object
    title: >-
      MsgSupplyIncreaseProposalResponse defines the Msg/SupplyIncreaseProposal
      response type
  provenance.marker.v1.MsgTransferResponse:
    type: object
    title: MsgTransferResponse defines the Msg/Transfer response type
  provenance.marker.v1.MsgUpdateForcedTransferResponse:
    type: object
    title: >-
      MsgUpdateForcedTransferResponse defines the Msg/UpdateForcedTransfer
      response type
  provenance.marker.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is a response message for the UpdateParams
      endpoint.
  provenance.marker.v1.MsgUpdateRequireDepositAccessResponse:
    type: object
    title: >-
      MsgUpdateRequireDepositAccessResponse defines the
      Msg/UpdateRequireDepositAccess response type
  provenance.marker.v1.MsgUpdateRequiredAttributesResponse:
    type: object
    title: >-
      MsgUpdateRequiredAttributesResponse defines the
      Msg/UpdateRequiredAttributes response type
  provenance.marker.v1.MsgUpdateSendDenyListResponse:
    type: object
    title: >-
      MsgUpdateSendDenyListResponse defines the Msg/UpdateSendDenyList response
      type
  provenance.marker.v1.MsgWithdrawEscrowProposalResponse:
    type: object
    title: >-
      MsgWithdrawEscrowProposalResponse defines the Msg/WithdrawEscrowProposal
      response type
  provenance.marker.v1.MsgWithdrawResponse:
    type: object
    title: MsgWithdrawResponse defines the Msg/Withdraw response type
  provenance.metadata.v1.AccountDataResponse:
    type: object
    properties:
      value:
        type: string
        description: The accountdata for the requested metadata address.
    description: >-
      AccountDataResponse is the response type for the Query/AccountData RPC
      method.
  provenance.metadata.v1.AuditFields:
    type: object
    properties:
      created_date:
        type: string
        format: date-time
        title: the date/time when this entry was created
      created_by:
        type: string
        title: the address of the account that created this record
      updated_date:
        type: string
        format: date-time
        title: the date/time when this entry was last updated
      updated_by:
        type: string
        title: the address of the account that modified this record
      version:
        type: integer
        format: int64
        title: an optional version number that is incremented with each update
      message:
        type: string
        title: an optional message associated with the creation/update event
    title: >-
      AuditFields capture information about the last account to make
      modifications and when they were made
  provenance.metadata.v1.ContractSpecIdInfo:
    type: object
    properties:
      contract_spec_id:
        type: string
        format: byte
        description: >-
          contract_spec_id is the raw bytes of the contract specification
          address.
      contract_spec_id_prefix:
        type: string
        format: byte
        description: contract_spec_id_prefix is the prefix portion of the contract_spec_id.
      contract_spec_id_contract_spec_uuid:
        type: string
        format: byte
        description: >-
          contract_spec_id_contract_spec_uuid is the contract_spec_uuid portion
          of the contract_spec_id.
      contract_spec_addr:
        type: string
        description: >-
          contract_spec_addr is the bech32 string version of the
          contract_spec_id.
      contract_spec_uuid:
        type: string
        description: >-
          contract_spec_uuid is the uuid hex string of the
          contract_spec_id_contract_spec_uuid.
    description: >-
      ContractSpecIdInfo contains various info regarding a contract
      specification id.
  provenance.metadata.v1.ContractSpecification:
    type: object
    properties:
      specification_id:
        type: string
        format: byte
        title: unique identifier for this specification on chain
      description:
        title: Description information for this contract specification
        type: object
        properties:
          name:
            type: string
            description: A Name for this thing.
          description:
            type: string
            description: A description of this thing.
          website_url:
            type: string
            description: URL to find even more info.
          icon_url:
            type: string
            description: URL of an icon.
        description: >-
          Description holds general information that is handy to associate with
          a structure.
      owner_addresses:
        type: array
        items:
          type: string
        title: Address of the account that owns this specificaiton
      parties_involved:
        type: array
        items:
          type: string
          enum:
            - PARTY_TYPE_UNSPECIFIED
            - PARTY_TYPE_ORIGINATOR
            - PARTY_TYPE_SERVICER
            - PARTY_TYPE_INVESTOR
            - PARTY_TYPE_CUSTODIAN
            - PARTY_TYPE_OWNER
            - PARTY_TYPE_AFFILIATE
            - PARTY_TYPE_OMNIBUS
            - PARTY_TYPE_PROVENANCE
            - PARTY_TYPE_CONTROLLER
            - PARTY_TYPE_VALIDATOR
            - PARTY_TYPE_GENERIC_1
            - PARTY_TYPE_GENERIC_2
            - PARTY_TYPE_GENERIC_3
            - PARTY_TYPE_GENERIC_4
            - PARTY_TYPE_GENERIC_5
            - PARTY_TYPE_GENERIC_6
          default: PARTY_TYPE_UNSPECIFIED
          description: >-
            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
            condition
             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
          title: PartyType are the different roles parties on a contract may use
        title: >-
          a list of party roles that must be fullfilled when signing a
          transaction for this contract specification
      resource_id:
        type: string
        format: byte
        title: the address of a record on chain that represents this contract
      hash:
        type: string
        title: the hash of contract binary (off-chain instance)
      class_name:
        type: string
        title: name of the class/type of this contract executable
    title: >-
      ContractSpecification defines the required parties, resources, conditions,
      and consideration outputs for a contract
  provenance.metadata.v1.ContractSpecificationRequest:
    type: object
    properties:
      specification_id:
        type: string
        description: >-
          specification_id can either be a uuid, e.g.
          def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
          specification

          address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

          It can also be a record specification address, e.g.

          recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
      include_record_specs:
        type: boolean
        description: >-
          include_record_specs is a flag for whether to include the the record
          specifications of this contract specification

          in the response.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: >-
      ContractSpecificationRequest is the request type for the
      Query/ContractSpecification RPC method.
  provenance.metadata.v1.ContractSpecificationResponse:
    type: object
    properties:
      contract_specification:
        description: contract_specification is the wrapped contract specification.
        type: object
        properties:
          specification:
            description: specification is the on-chain contract specification message.
            type: object
            properties:
              specification_id:
                type: string
                format: byte
                title: unique identifier for this specification on chain
              description:
                title: Description information for this contract specification
                type: object
                properties:
                  name:
                    type: string
                    description: A Name for this thing.
                  description:
                    type: string
                    description: A description of this thing.
                  website_url:
                    type: string
                    description: URL to find even more info.
                  icon_url:
                    type: string
                    description: URL of an icon.
                description: >-
                  Description holds general information that is handy to
                  associate with a structure.
              owner_addresses:
                type: array
                items:
                  type: string
                title: Address of the account that owns this specificaiton
              parties_involved:
                type: array
                items:
                  type: string
                  enum:
                    - PARTY_TYPE_UNSPECIFIED
                    - PARTY_TYPE_ORIGINATOR
                    - PARTY_TYPE_SERVICER
                    - PARTY_TYPE_INVESTOR
                    - PARTY_TYPE_CUSTODIAN
                    - PARTY_TYPE_OWNER
                    - PARTY_TYPE_AFFILIATE
                    - PARTY_TYPE_OMNIBUS
                    - PARTY_TYPE_PROVENANCE
                    - PARTY_TYPE_CONTROLLER
                    - PARTY_TYPE_VALIDATOR
                    - PARTY_TYPE_GENERIC_1
                    - PARTY_TYPE_GENERIC_2
                    - PARTY_TYPE_GENERIC_3
                    - PARTY_TYPE_GENERIC_4
                    - PARTY_TYPE_GENERIC_5
                    - PARTY_TYPE_GENERIC_6
                  default: PARTY_TYPE_UNSPECIFIED
                  description: >-
                    - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                    condition
                     - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                     - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                     - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                     - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                     - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                     - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                     - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                     - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                     - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                     - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                     - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                  title: >-
                    PartyType are the different roles parties on a contract may
                    use
                title: >-
                  a list of party roles that must be fullfilled when signing a
                  transaction for this contract specification
              resource_id:
                type: string
                format: byte
                title: the address of a record on chain that represents this contract
              hash:
                type: string
                title: the hash of contract binary (off-chain instance)
              class_name:
                type: string
                title: name of the class/type of this contract executable
            title: >-
              ContractSpecification defines the required parties, resources,
              conditions, and consideration outputs for a contract
          contract_spec_id_info:
            description: >-
              contract_spec_id_info contains information about the id/address of
              the contract specification.
            type: object
            properties:
              contract_spec_id:
                type: string
                format: byte
                description: >-
                  contract_spec_id is the raw bytes of the contract
                  specification address.
              contract_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  contract_spec_id_prefix is the prefix portion of the
                  contract_spec_id.
              contract_spec_id_contract_spec_uuid:
                type: string
                format: byte
                description: >-
                  contract_spec_id_contract_spec_uuid is the contract_spec_uuid
                  portion of the contract_spec_id.
              contract_spec_addr:
                type: string
                description: >-
                  contract_spec_addr is the bech32 string version of the
                  contract_spec_id.
              contract_spec_uuid:
                type: string
                description: >-
                  contract_spec_uuid is the uuid hex string of the
                  contract_spec_id_contract_spec_uuid.
      record_specifications:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain record specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                name:
                  type: string
                  title: >-
                    Name of Record that will be created when this specification
                    is used
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        title: name for this input
                      type_name:
                        type: string
                        title: a type_name (typically a proto name or class_name)
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                    title: >-
                      InputSpecification defines a name, type_name, and source
                      reference (either on or off chain) to define an input

                      parameter
                  title: >-
                    A set of inputs that must be satisified to apply this
                    RecordSpecification and create a Record
                type_name:
                  type: string
                  title: >-
                    A type name for data associated with this record (typically
                    a class or proto name)
                result_type:
                  title: >-
                    Type of result for this record specification (must be RECORD
                    or RECORD_LIST)
                  type: string
                  enum:
                    - DEFINITION_TYPE_UNSPECIFIED
                    - DEFINITION_TYPE_PROPOSED
                    - DEFINITION_TYPE_RECORD
                    - DEFINITION_TYPE_RECORD_LIST
                  default: DEFINITION_TYPE_UNSPECIFIED
                  description: >-
                    - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
                    indicates an unknown/invalid value
                     - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                     - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                     - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                    the same name
                responsible_parties:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: Type of party responsible for this record
              title: >-
                RecordSpecification defines the specification for a Record
                including allowed/required inputs/outputs
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordSpecificationWrapper contains a single record specification
            and some extra identifiers for it.
        description: >-
          record_specifications is any number or wrapped record specifications
          associated with this contract_specification

          (if requested).
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          specification_id:
            type: string
            description: >-
              specification_id can either be a uuid, e.g.
              def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
              specification

              address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

              It can also be a record specification address, e.g.

              recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          include_record_specs:
            type: boolean
            description: >-
              include_record_specs is a flag for whether to include the the
              record specifications of this contract specification

              in the response.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: >-
      ContractSpecificationResponse is the response type for the
      Query/ContractSpecification RPC method.
  provenance.metadata.v1.ContractSpecificationWrapper:
    type: object
    properties:
      specification:
        description: specification is the on-chain contract specification message.
        type: object
        properties:
          specification_id:
            type: string
            format: byte
            title: unique identifier for this specification on chain
          description:
            title: Description information for this contract specification
            type: object
            properties:
              name:
                type: string
                description: A Name for this thing.
              description:
                type: string
                description: A description of this thing.
              website_url:
                type: string
                description: URL to find even more info.
              icon_url:
                type: string
                description: URL of an icon.
            description: >-
              Description holds general information that is handy to associate
              with a structure.
          owner_addresses:
            type: array
            items:
              type: string
            title: Address of the account that owns this specificaiton
          parties_involved:
            type: array
            items:
              type: string
              enum:
                - PARTY_TYPE_UNSPECIFIED
                - PARTY_TYPE_ORIGINATOR
                - PARTY_TYPE_SERVICER
                - PARTY_TYPE_INVESTOR
                - PARTY_TYPE_CUSTODIAN
                - PARTY_TYPE_OWNER
                - PARTY_TYPE_AFFILIATE
                - PARTY_TYPE_OMNIBUS
                - PARTY_TYPE_PROVENANCE
                - PARTY_TYPE_CONTROLLER
                - PARTY_TYPE_VALIDATOR
                - PARTY_TYPE_GENERIC_1
                - PARTY_TYPE_GENERIC_2
                - PARTY_TYPE_GENERIC_3
                - PARTY_TYPE_GENERIC_4
                - PARTY_TYPE_GENERIC_5
                - PARTY_TYPE_GENERIC_6
              default: PARTY_TYPE_UNSPECIFIED
              description: >-
                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                condition
                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
              title: PartyType are the different roles parties on a contract may use
            title: >-
              a list of party roles that must be fullfilled when signing a
              transaction for this contract specification
          resource_id:
            type: string
            format: byte
            title: the address of a record on chain that represents this contract
          hash:
            type: string
            title: the hash of contract binary (off-chain instance)
          class_name:
            type: string
            title: name of the class/type of this contract executable
        title: >-
          ContractSpecification defines the required parties, resources,
          conditions, and consideration outputs for a contract
      contract_spec_id_info:
        description: >-
          contract_spec_id_info contains information about the id/address of the
          contract specification.
        type: object
        properties:
          contract_spec_id:
            type: string
            format: byte
            description: >-
              contract_spec_id is the raw bytes of the contract specification
              address.
          contract_spec_id_prefix:
            type: string
            format: byte
            description: >-
              contract_spec_id_prefix is the prefix portion of the
              contract_spec_id.
          contract_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              contract_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the contract_spec_id.
          contract_spec_addr:
            type: string
            description: >-
              contract_spec_addr is the bech32 string version of the
              contract_spec_id.
          contract_spec_uuid:
            type: string
            description: >-
              contract_spec_uuid is the uuid hex string of the
              contract_spec_id_contract_spec_uuid.
    description: >-
      ContractSpecificationWrapper contains a single contract specification and
      some extra identifiers for it.
  provenance.metadata.v1.ContractSpecificationsAllRequest:
    type: object
    properties:
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      ContractSpecificationsAllRequest is the request type for the
      Query/ContractSpecificationsAll RPC method.
  provenance.metadata.v1.ContractSpecificationsAllResponse:
    type: object
    properties:
      contract_specifications:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain contract specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                description:
                  title: Description information for this contract specification
                  type: object
                  properties:
                    name:
                      type: string
                      description: A Name for this thing.
                    description:
                      type: string
                      description: A description of this thing.
                    website_url:
                      type: string
                      description: URL to find even more info.
                    icon_url:
                      type: string
                      description: URL of an icon.
                  description: >-
                    Description holds general information that is handy to
                    associate with a structure.
                owner_addresses:
                  type: array
                  items:
                    type: string
                  title: Address of the account that owns this specificaiton
                parties_involved:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: >-
                    a list of party roles that must be fullfilled when signing a
                    transaction for this contract specification
                resource_id:
                  type: string
                  format: byte
                  title: >-
                    the address of a record on chain that represents this
                    contract
                hash:
                  type: string
                  title: the hash of contract binary (off-chain instance)
                class_name:
                  type: string
                  title: name of the class/type of this contract executable
              title: >-
                ContractSpecification defines the required parties, resources,
                conditions, and consideration outputs for a contract
            contract_spec_id_info:
              description: >-
                contract_spec_id_info contains information about the id/address
                of the contract specification.
              type: object
              properties:
                contract_spec_id:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id is the raw bytes of the contract
                    specification address.
                contract_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_prefix is the prefix portion of the
                    contract_spec_id.
                contract_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_contract_spec_uuid is the
                    contract_spec_uuid portion of the contract_spec_id.
                contract_spec_addr:
                  type: string
                  description: >-
                    contract_spec_addr is the bech32 string version of the
                    contract_spec_id.
                contract_spec_uuid:
                  type: string
                  description: >-
                    contract_spec_uuid is the uuid hex string of the
                    contract_spec_id_contract_spec_uuid.
          description: >-
            ContractSpecificationWrapper contains a single contract
            specification and some extra identifiers for it.
        description: contract_specifications are the wrapped contract specifications.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      ContractSpecificationsAllResponse is the response type for the
      Query/ContractSpecificationsAll RPC method.
  provenance.metadata.v1.DefinitionType:
    type: string
    enum:
      - DEFINITION_TYPE_UNSPECIFIED
      - DEFINITION_TYPE_PROPOSED
      - DEFINITION_TYPE_RECORD
      - DEFINITION_TYPE_RECORD_LIST
    default: DEFINITION_TYPE_UNSPECIFIED
    description: >-
      - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED indicates an
      unknown/invalid value
       - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
       - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
       - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
      the same name
    title: DefinitionType indicates the required definition type for this value
  provenance.metadata.v1.Description:
    type: object
    properties:
      name:
        type: string
        description: A Name for this thing.
      description:
        type: string
        description: A description of this thing.
      website_url:
        type: string
        description: URL to find even more info.
      icon_url:
        type: string
        description: URL of an icon.
    description: >-
      Description holds general information that is handy to associate with a
      structure.
  provenance.metadata.v1.GetByAddrResponse:
    type: object
    properties:
      scopes:
        type: array
        items:
          type: object
          properties:
            scope_id:
              type: string
              format: byte
              title: >-
                Unique ID for this scope.  Implements sdk.Address interface for
                use where addresses are required in Cosmos
            specification_id:
              type: string
              format: byte
              title: >-
                the scope specification that contains the specifications for
                data elements allowed within this scope
            owners:
              type: array
              items:
                type: object
                properties:
                  address:
                    type: string
                    title: address of the account (on chain)
                  role:
                    title: >-
                      a role for this account within the context of the
                      processes used
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                  optional:
                    type: boolean
                    title: whether this party's signature is optional
                title: >-
                  A Party is an address with/in a given role associated with a
                  contract
              description: >-
                These parties represent top level owners of the records within. 
                These parties must sign any requests that modify

                the data within the scope.  These addresses are in union with
                parties listed on the sessions.
            data_access:
              type: array
              items:
                type: string
              description: >-
                Addresses in this list are authorized to receive off-chain data
                associated with this scope.
            value_owner_address:
              type: string
              description: >-
                The address that controls the value associated with this scope.


                The value owner is actually tracked by the bank module using a
                coin with the denom "nft/<scope_id>".

                The value owner can be changed using WriteScope or anything that
                transfers funds, e.g. MsgSend.


                During WriteScope:
                 - If this field is empty, it indicates that there should not be a change to the value owner.
                   I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                 - If this field has a value, the existing value owner will be looked up, and
                   - If there's already an existing value owner, they must be a signer,
                     and the coin will be transferred to the new value owner.
                   - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                     If the scope already exists, the owners must be signers (just like changing other fields).
                     If it's a new scope, there's no special signer limitations related to the value owner.
            require_party_rollup:
              type: boolean
              description: >-
                Whether all parties in this scope and its sessions must be
                present in this scope's owners field.

                This also enables use of optional=true scope owners and session
                parties.
          description: >-
            Scope defines a root reference for a collection of records owned by
            one or more parties.
        description: scopes contains any scopes that were requested and found.
      sessions:
        type: array
        items:
          type: object
          properties:
            session_id:
              type: string
              format: byte
            specification_id:
              type: string
              format: byte
              description: >-
                unique id of the contract specification that was used to create
                this session.
            parties:
              type: array
              items:
                type: object
                properties:
                  address:
                    type: string
                    title: address of the account (on chain)
                  role:
                    title: >-
                      a role for this account within the context of the
                      processes used
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                  optional:
                    type: boolean
                    title: whether this party's signature is optional
                title: >-
                  A Party is an address with/in a given role associated with a
                  contract
              title: parties is the set of identities that signed this contract
            name:
              type: string
              title: >-
                name to associate with this session execution context, typically
                classname
            context:
              type: string
              format: byte
              description: >-
                context is a field for storing client specific data associated
                with a session.
            audit:
              description: >-
                Created by, updated by, timestamps, version number, and related
                info.
              type: object
              properties:
                created_date:
                  type: string
                  format: date-time
                  title: the date/time when this entry was created
                created_by:
                  type: string
                  title: the address of the account that created this record
                updated_date:
                  type: string
                  format: date-time
                  title: the date/time when this entry was last updated
                updated_by:
                  type: string
                  title: the address of the account that modified this record
                version:
                  type: integer
                  format: int64
                  title: >-
                    an optional version number that is incremented with each
                    update
                message:
                  type: string
                  title: >-
                    an optional message associated with the creation/update
                    event
              title: >-
                AuditFields capture information about the last account to make
                modifications and when they were made
          description: >-
            Session defines an execution context against a specific
            specification instance.

            The context will have a specification and set of parties involved.


            NOTE: When there are no more Records within a Scope that reference a
            Session, the Session is removed.
        description: sessions contains any sessions that were requested and found.
      records:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              title: >-
                name/identifier for this record.  Value must be unique within
                the scope.  Also known as a Fact name
            session_id:
              type: string
              format: byte
              title: >-
                id of the session context that was used to create this record
                (use with filtered kvprefix iterator)
            process:
              title: >-
                process contain information used to uniquely identify an
                execution on or off chain that generated this record
              type: object
              properties:
                address:
                  type: string
                  title: the address of a smart contract used for this process
                hash:
                  type: string
                  title: the hash of an off-chain process used
                name:
                  type: string
                  title: >-
                    a name associated with the process (type_name, classname or
                    smart contract common name)
                method:
                  type: string
                  title: >-
                    method is a name or reference to a specific operation
                    (method) within a class/contract that was invoked
            inputs:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    description: Name value included to link back to the definition spec.
                  record_id:
                    type: string
                    format: byte
                    title: the address of a record on chain (For Established Records)
                  hash:
                    type: string
                    title: >-
                      the hash of an off-chain piece of information (For
                      Proposed Records)
                  type_name:
                    type: string
                    title: from proposed fact structure to unmarshal
                  status:
                    title: >-
                      Indicates if this input was a recorded fact on chain or
                      just a given hashed input
                    type: string
                    enum:
                      - RECORD_INPUT_STATUS_UNSPECIFIED
                      - RECORD_INPUT_STATUS_PROPOSED
                      - RECORD_INPUT_STATUS_RECORD
                    default: RECORD_INPUT_STATUS_UNSPECIFIED
                    description: >-
                      - RECORD_INPUT_STATUS_UNSPECIFIED:
                      RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                      invalid/unknown input type
                       - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                       - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                title: Tracks the inputs used to establish this record
              title: >-
                inputs used with the process to achieve the output on this
                record
            outputs:
              type: array
              items:
                type: object
                properties:
                  hash:
                    type: string
                    title: >-
                      Hash of the data output that was output/generated for this
                      record
                  status:
                    title: >-
                      Status of the process execution associated with this
                      output indicating success,failure, or pending
                    type: string
                    enum:
                      - RESULT_STATUS_UNSPECIFIED
                      - RESULT_STATUS_PASS
                      - RESULT_STATUS_SKIP
                      - RESULT_STATUS_FAIL
                    default: RESULT_STATUS_UNSPECIFIED
                    description: >-
                      - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED
                      indicates an unset condition
                       - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                       - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                       - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                title: >-
                  RecordOutput encapsulates the output of a process recorded on
                  chain
              title: >-
                output(s) is the results of executing the process on the given
                process indicated in this record
            specification_id:
              type: string
              format: byte
              description: >-
                specification_id is the id of the record specification that was
                used to create this record.
          title: >-
            A record (of fact) is attached to a session or each consideration
            output from a contract
        description: records contains any records that were requested and found.
      scope_specs:
        type: array
        items:
          type: object
          properties:
            specification_id:
              type: string
              format: byte
              title: unique identifier for this specification on chain
            description:
              description: General information about this scope specification.
              type: object
              properties:
                name:
                  type: string
                  description: A Name for this thing.
                description:
                  type: string
                  description: A description of this thing.
                website_url:
                  type: string
                  description: URL to find even more info.
                icon_url:
                  type: string
                  description: URL of an icon.
            owner_addresses:
              type: array
              items:
                type: string
              description: Addresses of the owners of this scope specification.
            parties_involved:
              type: array
              items:
                type: string
                enum:
                  - PARTY_TYPE_UNSPECIFIED
                  - PARTY_TYPE_ORIGINATOR
                  - PARTY_TYPE_SERVICER
                  - PARTY_TYPE_INVESTOR
                  - PARTY_TYPE_CUSTODIAN
                  - PARTY_TYPE_OWNER
                  - PARTY_TYPE_AFFILIATE
                  - PARTY_TYPE_OMNIBUS
                  - PARTY_TYPE_PROVENANCE
                  - PARTY_TYPE_CONTROLLER
                  - PARTY_TYPE_VALIDATOR
                  - PARTY_TYPE_GENERIC_1
                  - PARTY_TYPE_GENERIC_2
                  - PARTY_TYPE_GENERIC_3
                  - PARTY_TYPE_GENERIC_4
                  - PARTY_TYPE_GENERIC_5
                  - PARTY_TYPE_GENERIC_6
                default: PARTY_TYPE_UNSPECIFIED
                description: >-
                  - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                  condition
                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                title: >-
                  PartyType are the different roles parties on a contract may
                  use
              title: >-
                A list of parties that must be present on a scope (and their
                associated roles)
            contract_spec_ids:
              type: array
              items:
                type: string
                format: byte
              description: >-
                A list of contract specification ids allowed for a scope based
                on this specification.
          title: >-
            ScopeSpecification defines the required parties, resources,
            conditions, and consideration outputs for a contract
        description: >-
          scope_specs contains any scope specifications that were requested and
          found.
      contract_specs:
        type: array
        items:
          type: object
          properties:
            specification_id:
              type: string
              format: byte
              title: unique identifier for this specification on chain
            description:
              title: Description information for this contract specification
              type: object
              properties:
                name:
                  type: string
                  description: A Name for this thing.
                description:
                  type: string
                  description: A description of this thing.
                website_url:
                  type: string
                  description: URL to find even more info.
                icon_url:
                  type: string
                  description: URL of an icon.
              description: >-
                Description holds general information that is handy to associate
                with a structure.
            owner_addresses:
              type: array
              items:
                type: string
              title: Address of the account that owns this specificaiton
            parties_involved:
              type: array
              items:
                type: string
                enum:
                  - PARTY_TYPE_UNSPECIFIED
                  - PARTY_TYPE_ORIGINATOR
                  - PARTY_TYPE_SERVICER
                  - PARTY_TYPE_INVESTOR
                  - PARTY_TYPE_CUSTODIAN
                  - PARTY_TYPE_OWNER
                  - PARTY_TYPE_AFFILIATE
                  - PARTY_TYPE_OMNIBUS
                  - PARTY_TYPE_PROVENANCE
                  - PARTY_TYPE_CONTROLLER
                  - PARTY_TYPE_VALIDATOR
                  - PARTY_TYPE_GENERIC_1
                  - PARTY_TYPE_GENERIC_2
                  - PARTY_TYPE_GENERIC_3
                  - PARTY_TYPE_GENERIC_4
                  - PARTY_TYPE_GENERIC_5
                  - PARTY_TYPE_GENERIC_6
                default: PARTY_TYPE_UNSPECIFIED
                description: >-
                  - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                  condition
                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                title: >-
                  PartyType are the different roles parties on a contract may
                  use
              title: >-
                a list of party roles that must be fullfilled when signing a
                transaction for this contract specification
            resource_id:
              type: string
              format: byte
              title: the address of a record on chain that represents this contract
            hash:
              type: string
              title: the hash of contract binary (off-chain instance)
            class_name:
              type: string
              title: name of the class/type of this contract executable
          title: >-
            ContractSpecification defines the required parties, resources,
            conditions, and consideration outputs for a contract
        description: >-
          contract_specs contains any contract specifications that were
          requested and found.
      record_specs:
        type: array
        items:
          type: object
          properties:
            specification_id:
              type: string
              format: byte
              title: unique identifier for this specification on chain
            name:
              type: string
              title: >-
                Name of Record that will be created when this specification is
                used
            inputs:
              type: array
              items:
                type: object
                properties:
                  name:
                    type: string
                    title: name for this input
                  type_name:
                    type: string
                    title: a type_name (typically a proto name or class_name)
                  record_id:
                    type: string
                    format: byte
                    title: the address of a record on chain (For Established Records)
                  hash:
                    type: string
                    title: >-
                      the hash of an off-chain piece of information (For
                      Proposed Records)
                title: >-
                  InputSpecification defines a name, type_name, and source
                  reference (either on or off chain) to define an input

                  parameter
              title: >-
                A set of inputs that must be satisified to apply this
                RecordSpecification and create a Record
            type_name:
              type: string
              title: >-
                A type name for data associated with this record (typically a
                class or proto name)
            result_type:
              title: >-
                Type of result for this record specification (must be RECORD or
                RECORD_LIST)
              type: string
              enum:
                - DEFINITION_TYPE_UNSPECIFIED
                - DEFINITION_TYPE_PROPOSED
                - DEFINITION_TYPE_RECORD
                - DEFINITION_TYPE_RECORD_LIST
              default: DEFINITION_TYPE_UNSPECIFIED
              description: >-
                - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
                indicates an unknown/invalid value
                 - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                 - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                 - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                the same name
            responsible_parties:
              type: array
              items:
                type: string
                enum:
                  - PARTY_TYPE_UNSPECIFIED
                  - PARTY_TYPE_ORIGINATOR
                  - PARTY_TYPE_SERVICER
                  - PARTY_TYPE_INVESTOR
                  - PARTY_TYPE_CUSTODIAN
                  - PARTY_TYPE_OWNER
                  - PARTY_TYPE_AFFILIATE
                  - PARTY_TYPE_OMNIBUS
                  - PARTY_TYPE_PROVENANCE
                  - PARTY_TYPE_CONTROLLER
                  - PARTY_TYPE_VALIDATOR
                  - PARTY_TYPE_GENERIC_1
                  - PARTY_TYPE_GENERIC_2
                  - PARTY_TYPE_GENERIC_3
                  - PARTY_TYPE_GENERIC_4
                  - PARTY_TYPE_GENERIC_5
                  - PARTY_TYPE_GENERIC_6
                default: PARTY_TYPE_UNSPECIFIED
                description: >-
                  - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                  condition
                   - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                   - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                   - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                   - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                   - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                   - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                   - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                   - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                   - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                   - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                   - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                   - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                title: >-
                  PartyType are the different roles parties on a contract may
                  use
              title: Type of party responsible for this record
          title: >-
            RecordSpecification defines the specification for a Record including
            allowed/required inputs/outputs
        description: >-
          record_specs contains any record specifications that were requested
          and found.
      not_found:
        type: array
        items:
          type: string
        description: not_found contains any addrs requested but not found.
    description: GetByAddrResponse is the response type for the Query/GetByAddr RPC method.
  provenance.metadata.v1.InputSpecification:
    type: object
    properties:
      name:
        type: string
        title: name for this input
      type_name:
        type: string
        title: a type_name (typically a proto name or class_name)
      record_id:
        type: string
        format: byte
        title: the address of a record on chain (For Established Records)
      hash:
        type: string
        title: the hash of an off-chain piece of information (For Proposed Records)
    title: >-
      InputSpecification defines a name, type_name, and source reference (either
      on or off chain) to define an input

      parameter
  provenance.metadata.v1.NetAssetValue:
    type: object
    properties:
      price:
        title: price is the complete value of the asset's volume
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
      updated_block_height:
        type: string
        format: uint64
        title: updated_block_height is the block height of last update
      volume:
        type: string
        format: uint64
        title: >-
          volume is the number of scope instances that were purchased for the
          price

          Typically this will be null (equivalent to one) or one.  The only
          reason this would be more than

          one is for cases where the precision of the price denom is
          insufficient to represent the actual price
    title: NetAssetValue defines a scope's net asset value
  provenance.metadata.v1.OSAllLocatorsRequest:
    type: object
    properties:
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      OSAllLocatorsRequest is the request type for the Query/OSAllLocators RPC
      method.
  provenance.metadata.v1.OSAllLocatorsResponse:
    type: object
    properties:
      locators:
        type: array
        items:
          type: object
          properties:
            owner:
              type: string
              title: account address the endpoint is owned by
            locator_uri:
              type: string
              title: locator endpoint uri
            encryption_key:
              type: string
              title: owners encryption key address
          description: >-
            Defines an Locator object stored on chain, which represents a owner(
            blockchain address) associated with a endpoint

            uri for it's associated object store.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      OSAllLocatorsResponse is the response type for the Query/OSAllLocators RPC
      method.
  provenance.metadata.v1.OSLocatorParams:
    type: object
    properties:
      max_uri_length:
        type: integer
        format: int64
    description: Params defines the parameters for the metadata-locator module methods.
  provenance.metadata.v1.OSLocatorParamsRequest:
    type: object
    properties:
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: >-
      OSLocatorParamsRequest is the request type for the Query/OSLocatorParams
      RPC method.
  provenance.metadata.v1.OSLocatorParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          max_uri_length:
            type: integer
            format: int64
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: >-
      OSLocatorParamsResponse is the response type for the Query/OSLocatorParams
      RPC method.
  provenance.metadata.v1.OSLocatorRequest:
    type: object
    properties:
      owner:
        type: string
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: OSLocatorRequest is the request type for the Query/OSLocator RPC method.
  provenance.metadata.v1.OSLocatorResponse:
    type: object
    properties:
      locator:
        type: object
        properties:
          owner:
            type: string
            title: account address the endpoint is owned by
          locator_uri:
            type: string
            title: locator endpoint uri
          encryption_key:
            type: string
            title: owners encryption key address
        description: >-
          Defines an Locator object stored on chain, which represents a owner(
          blockchain address) associated with a endpoint

          uri for it's associated object store.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          owner:
            type: string
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: OSLocatorResponse is the response type for the Query/OSLocator RPC method.
  provenance.metadata.v1.OSLocatorsByScopeRequest:
    type: object
    properties:
      scope_id:
        type: string
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: >-
      OSLocatorsByScopeRequest is the request type for the
      Query/OSLocatorsByScope RPC method.
  provenance.metadata.v1.OSLocatorsByScopeResponse:
    type: object
    properties:
      locators:
        type: array
        items:
          type: object
          properties:
            owner:
              type: string
              title: account address the endpoint is owned by
            locator_uri:
              type: string
              title: locator endpoint uri
            encryption_key:
              type: string
              title: owners encryption key address
          description: >-
            Defines an Locator object stored on chain, which represents a owner(
            blockchain address) associated with a endpoint

            uri for it's associated object store.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          scope_id:
            type: string
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: >-
      OSLocatorsByScopeResponse is the response type for the
      Query/OSLocatorsByScope RPC method.
  provenance.metadata.v1.OSLocatorsByURIRequest:
    type: object
    properties:
      uri:
        type: string
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      OSLocatorsByURIRequest is the request type for the Query/OSLocatorsByURI
      RPC method.
  provenance.metadata.v1.OSLocatorsByURIResponse:
    type: object
    properties:
      locators:
        type: array
        items:
          type: object
          properties:
            owner:
              type: string
              title: account address the endpoint is owned by
            locator_uri:
              type: string
              title: locator endpoint uri
            encryption_key:
              type: string
              title: owners encryption key address
          description: >-
            Defines an Locator object stored on chain, which represents a owner(
            blockchain address) associated with a endpoint

            uri for it's associated object store.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          uri:
            type: string
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      OSLocatorsByURIResponse is the response type for the Query/OSLocatorsByURI
      RPC method.
  provenance.metadata.v1.ObjectStoreLocator:
    type: object
    properties:
      owner:
        type: string
        title: account address the endpoint is owned by
      locator_uri:
        type: string
        title: locator endpoint uri
      encryption_key:
        type: string
        title: owners encryption key address
    description: >-
      Defines an Locator object stored on chain, which represents a owner(
      blockchain address) associated with a endpoint

      uri for it's associated object store.
  provenance.metadata.v1.OwnershipRequest:
    type: object
    properties:
      address:
        type: string
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: OwnershipRequest is the request type for the Query/Ownership RPC method.
  provenance.metadata.v1.OwnershipResponse:
    type: object
    properties:
      scope_uuids:
        type: array
        items:
          type: string
        description: A list of scope ids (uuid) associated with the given address.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          address:
            type: string
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: OwnershipResponse is the response type for the Query/Ownership RPC method.
  provenance.metadata.v1.Params:
    type: object
    description: Params defines the set of params for the metadata module.
  provenance.metadata.v1.Party:
    type: object
    properties:
      address:
        type: string
        title: address of the account (on chain)
      role:
        title: a role for this account within the context of the processes used
        type: string
        enum:
          - PARTY_TYPE_UNSPECIFIED
          - PARTY_TYPE_ORIGINATOR
          - PARTY_TYPE_SERVICER
          - PARTY_TYPE_INVESTOR
          - PARTY_TYPE_CUSTODIAN
          - PARTY_TYPE_OWNER
          - PARTY_TYPE_AFFILIATE
          - PARTY_TYPE_OMNIBUS
          - PARTY_TYPE_PROVENANCE
          - PARTY_TYPE_CONTROLLER
          - PARTY_TYPE_VALIDATOR
          - PARTY_TYPE_GENERIC_1
          - PARTY_TYPE_GENERIC_2
          - PARTY_TYPE_GENERIC_3
          - PARTY_TYPE_GENERIC_4
          - PARTY_TYPE_GENERIC_5
          - PARTY_TYPE_GENERIC_6
        default: PARTY_TYPE_UNSPECIFIED
        description: |-
          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error condition
           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
      optional:
        type: boolean
        title: whether this party's signature is optional
    title: A Party is an address with/in a given role associated with a contract
  provenance.metadata.v1.PartyType:
    type: string
    enum:
      - PARTY_TYPE_UNSPECIFIED
      - PARTY_TYPE_ORIGINATOR
      - PARTY_TYPE_SERVICER
      - PARTY_TYPE_INVESTOR
      - PARTY_TYPE_CUSTODIAN
      - PARTY_TYPE_OWNER
      - PARTY_TYPE_AFFILIATE
      - PARTY_TYPE_OMNIBUS
      - PARTY_TYPE_PROVENANCE
      - PARTY_TYPE_CONTROLLER
      - PARTY_TYPE_VALIDATOR
      - PARTY_TYPE_GENERIC_1
      - PARTY_TYPE_GENERIC_2
      - PARTY_TYPE_GENERIC_3
      - PARTY_TYPE_GENERIC_4
      - PARTY_TYPE_GENERIC_5
      - PARTY_TYPE_GENERIC_6
    default: PARTY_TYPE_UNSPECIFIED
    description: |-
      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error condition
       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
    title: PartyType are the different roles parties on a contract may use
  provenance.metadata.v1.Process:
    type: object
    properties:
      address:
        type: string
        title: the address of a smart contract used for this process
      hash:
        type: string
        title: the hash of an off-chain process used
      name:
        type: string
        title: >-
          a name associated with the process (type_name, classname or smart
          contract common name)
      method:
        type: string
        title: >-
          method is a name or reference to a specific operation (method) within
          a class/contract that was invoked
    title: >-
      Process contains information used to uniquely identify what was used to
      generate this record
  provenance.metadata.v1.QueryParamsRequest:
    type: object
    properties:
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: QueryParamsRequest is the request type for the Query/Params RPC method.
  provenance.metadata.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  provenance.metadata.v1.QueryScopeNetAssetValuesResponse:
    type: object
    properties:
      net_asset_values:
        type: array
        items:
          type: object
          properties:
            price:
              title: price is the complete value of the asset's volume
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            updated_block_height:
              type: string
              format: uint64
              title: updated_block_height is the block height of last update
            volume:
              type: string
              format: uint64
              title: >-
                volume is the number of scope instances that were purchased for
                the price

                Typically this will be null (equivalent to one) or one.  The
                only reason this would be more than

                one is for cases where the precision of the price denom is
                insufficient to represent the actual price
          title: NetAssetValue defines a scope's net asset value
        title: net asset values for scope
    description: >-
      QueryNetAssetValuesRequest is the response type for the
      Query/NetAssetValues method.
  provenance.metadata.v1.Record:
    type: object
    properties:
      name:
        type: string
        title: >-
          name/identifier for this record.  Value must be unique within the
          scope.  Also known as a Fact name
      session_id:
        type: string
        format: byte
        title: >-
          id of the session context that was used to create this record (use
          with filtered kvprefix iterator)
      process:
        title: >-
          process contain information used to uniquely identify an execution on
          or off chain that generated this record
        type: object
        properties:
          address:
            type: string
            title: the address of a smart contract used for this process
          hash:
            type: string
            title: the hash of an off-chain process used
          name:
            type: string
            title: >-
              a name associated with the process (type_name, classname or smart
              contract common name)
          method:
            type: string
            title: >-
              method is a name or reference to a specific operation (method)
              within a class/contract that was invoked
      inputs:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              description: Name value included to link back to the definition spec.
            record_id:
              type: string
              format: byte
              title: the address of a record on chain (For Established Records)
            hash:
              type: string
              title: >-
                the hash of an off-chain piece of information (For Proposed
                Records)
            type_name:
              type: string
              title: from proposed fact structure to unmarshal
            status:
              title: >-
                Indicates if this input was a recorded fact on chain or just a
                given hashed input
              type: string
              enum:
                - RECORD_INPUT_STATUS_UNSPECIFIED
                - RECORD_INPUT_STATUS_PROPOSED
                - RECORD_INPUT_STATUS_RECORD
              default: RECORD_INPUT_STATUS_UNSPECIFIED
              description: >-
                - RECORD_INPUT_STATUS_UNSPECIFIED:
                RECORD_INPUT_STATUS_UNSPECIFIED indicates an invalid/unknown
                input type
                 - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                 - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
          title: Tracks the inputs used to establish this record
        title: inputs used with the process to achieve the output on this record
      outputs:
        type: array
        items:
          type: object
          properties:
            hash:
              type: string
              title: >-
                Hash of the data output that was output/generated for this
                record
            status:
              title: >-
                Status of the process execution associated with this output
                indicating success,failure, or pending
              type: string
              enum:
                - RESULT_STATUS_UNSPECIFIED
                - RESULT_STATUS_PASS
                - RESULT_STATUS_SKIP
                - RESULT_STATUS_FAIL
              default: RESULT_STATUS_UNSPECIFIED
              description: >-
                - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED indicates
                an unset condition
                 - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                 - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                 - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
          title: RecordOutput encapsulates the output of a process recorded on chain
        title: >-
          output(s) is the results of executing the process on the given process
          indicated in this record
      specification_id:
        type: string
        format: byte
        description: >-
          specification_id is the id of the record specification that was used
          to create this record.
    title: >-
      A record (of fact) is attached to a session or each consideration output
      from a contract
  provenance.metadata.v1.RecordIdInfo:
    type: object
    properties:
      record_id:
        type: string
        format: byte
        description: record_id is the raw bytes of the record address.
      record_id_prefix:
        type: string
        format: byte
        description: record_id_prefix is the prefix portion of the record_id.
      record_id_scope_uuid:
        type: string
        format: byte
        description: record_id_scope_uuid is the scope_uuid portion of the record_id.
      record_id_hashed_name:
        type: string
        format: byte
        description: record_id_hashed_name is the hashed name portion of the record_id.
      record_addr:
        type: string
        description: record_addr is the bech32 string version of the record_id.
      scope_id_info:
        description: >-
          scope_id_info is information about the scope id referenced in the
          record_id.
        type: object
        properties:
          scope_id:
            type: string
            format: byte
            description: scope_id is the raw bytes of the scope address.
          scope_id_prefix:
            type: string
            format: byte
            description: scope_id_prefix is the prefix portion of the scope_id.
          scope_id_scope_uuid:
            type: string
            format: byte
            description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
          scope_addr:
            type: string
            description: scope_addr is the bech32 string version of the scope_id.
          scope_uuid:
            type: string
            description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
    description: RecordIdInfo contains various info regarding a record id.
  provenance.metadata.v1.RecordInput:
    type: object
    properties:
      name:
        type: string
        description: Name value included to link back to the definition spec.
      record_id:
        type: string
        format: byte
        title: the address of a record on chain (For Established Records)
      hash:
        type: string
        title: the hash of an off-chain piece of information (For Proposed Records)
      type_name:
        type: string
        title: from proposed fact structure to unmarshal
      status:
        title: >-
          Indicates if this input was a recorded fact on chain or just a given
          hashed input
        type: string
        enum:
          - RECORD_INPUT_STATUS_UNSPECIFIED
          - RECORD_INPUT_STATUS_PROPOSED
          - RECORD_INPUT_STATUS_RECORD
        default: RECORD_INPUT_STATUS_UNSPECIFIED
        description: >-
          - RECORD_INPUT_STATUS_UNSPECIFIED: RECORD_INPUT_STATUS_UNSPECIFIED
          indicates an invalid/unknown input type
           - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
           - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
    title: Tracks the inputs used to establish this record
  provenance.metadata.v1.RecordInputStatus:
    type: string
    enum:
      - RECORD_INPUT_STATUS_UNSPECIFIED
      - RECORD_INPUT_STATUS_PROPOSED
      - RECORD_INPUT_STATUS_RECORD
    default: RECORD_INPUT_STATUS_UNSPECIFIED
    description: >-
      - RECORD_INPUT_STATUS_UNSPECIFIED: RECORD_INPUT_STATUS_UNSPECIFIED
      indicates an invalid/unknown input type
       - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
       - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
    title: A set of types for inputs on a record (of fact)
  provenance.metadata.v1.RecordOutput:
    type: object
    properties:
      hash:
        type: string
        title: Hash of the data output that was output/generated for this record
      status:
        title: >-
          Status of the process execution associated with this output indicating
          success,failure, or pending
        type: string
        enum:
          - RESULT_STATUS_UNSPECIFIED
          - RESULT_STATUS_PASS
          - RESULT_STATUS_SKIP
          - RESULT_STATUS_FAIL
        default: RESULT_STATUS_UNSPECIFIED
        description: >-
          - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED indicates an
          unset condition
           - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
           - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
           - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
    title: RecordOutput encapsulates the output of a process recorded on chain
  provenance.metadata.v1.RecordSpecIdInfo:
    type: object
    properties:
      record_spec_id:
        type: string
        format: byte
        description: record_spec_id is the raw bytes of the record specification address.
      record_spec_id_prefix:
        type: string
        format: byte
        description: record_spec_id_prefix is the prefix portion of the record_spec_id.
      record_spec_id_contract_spec_uuid:
        type: string
        format: byte
        description: >-
          record_spec_id_contract_spec_uuid is the contract_spec_uuid portion of
          the record_spec_id.
      record_spec_id_hashed_name:
        type: string
        format: byte
        description: >-
          record_spec_id_hashed_name is the hashed name portion of the
          record_spec_id.
      record_spec_addr:
        type: string
        description: record_spec_addr is the bech32 string version of the record_spec_id.
      contract_spec_id_info:
        description: >-
          contract_spec_id_info is information about the contract spec id
          referenced in the record_spec_id.
        type: object
        properties:
          contract_spec_id:
            type: string
            format: byte
            description: >-
              contract_spec_id is the raw bytes of the contract specification
              address.
          contract_spec_id_prefix:
            type: string
            format: byte
            description: >-
              contract_spec_id_prefix is the prefix portion of the
              contract_spec_id.
          contract_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              contract_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the contract_spec_id.
          contract_spec_addr:
            type: string
            description: >-
              contract_spec_addr is the bech32 string version of the
              contract_spec_id.
          contract_spec_uuid:
            type: string
            description: >-
              contract_spec_uuid is the uuid hex string of the
              contract_spec_id_contract_spec_uuid.
    description: >-
      RecordSpecIdInfo contains various info regarding a record specification
      id.
  provenance.metadata.v1.RecordSpecification:
    type: object
    properties:
      specification_id:
        type: string
        format: byte
        title: unique identifier for this specification on chain
      name:
        type: string
        title: Name of Record that will be created when this specification is used
      inputs:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              title: name for this input
            type_name:
              type: string
              title: a type_name (typically a proto name or class_name)
            record_id:
              type: string
              format: byte
              title: the address of a record on chain (For Established Records)
            hash:
              type: string
              title: >-
                the hash of an off-chain piece of information (For Proposed
                Records)
          title: >-
            InputSpecification defines a name, type_name, and source reference
            (either on or off chain) to define an input

            parameter
        title: >-
          A set of inputs that must be satisified to apply this
          RecordSpecification and create a Record
      type_name:
        type: string
        title: >-
          A type name for data associated with this record (typically a class or
          proto name)
      result_type:
        title: >-
          Type of result for this record specification (must be RECORD or
          RECORD_LIST)
        type: string
        enum:
          - DEFINITION_TYPE_UNSPECIFIED
          - DEFINITION_TYPE_PROPOSED
          - DEFINITION_TYPE_RECORD
          - DEFINITION_TYPE_RECORD_LIST
        default: DEFINITION_TYPE_UNSPECIFIED
        description: >-
          - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED indicates
          an unknown/invalid value
           - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
           - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
           - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
          the same name
      responsible_parties:
        type: array
        items:
          type: string
          enum:
            - PARTY_TYPE_UNSPECIFIED
            - PARTY_TYPE_ORIGINATOR
            - PARTY_TYPE_SERVICER
            - PARTY_TYPE_INVESTOR
            - PARTY_TYPE_CUSTODIAN
            - PARTY_TYPE_OWNER
            - PARTY_TYPE_AFFILIATE
            - PARTY_TYPE_OMNIBUS
            - PARTY_TYPE_PROVENANCE
            - PARTY_TYPE_CONTROLLER
            - PARTY_TYPE_VALIDATOR
            - PARTY_TYPE_GENERIC_1
            - PARTY_TYPE_GENERIC_2
            - PARTY_TYPE_GENERIC_3
            - PARTY_TYPE_GENERIC_4
            - PARTY_TYPE_GENERIC_5
            - PARTY_TYPE_GENERIC_6
          default: PARTY_TYPE_UNSPECIFIED
          description: >-
            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
            condition
             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
          title: PartyType are the different roles parties on a contract may use
        title: Type of party responsible for this record
    title: >-
      RecordSpecification defines the specification for a Record including
      allowed/required inputs/outputs
  provenance.metadata.v1.RecordSpecificationRequest:
    type: object
    properties:
      specification_id:
        type: string
        description: >-
          specification_id can either be a uuid, e.g.
          def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
          specification

          address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

          It can also be a record specification address, e.g.

          recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
      name:
        type: string
        description: >-
          name is the name of the record to look up.

          It is required if the specification_id is a uuid or contract
          specification address.

          It is ignored if the specification_id is a record specification
          address.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: >-
      RecordSpecificationRequest is the request type for the
      Query/RecordSpecification RPC method.
  provenance.metadata.v1.RecordSpecificationResponse:
    type: object
    properties:
      record_specification:
        description: record_specification is the wrapped record specification.
        type: object
        properties:
          specification:
            description: specification is the on-chain record specification message.
            type: object
            properties:
              specification_id:
                type: string
                format: byte
                title: unique identifier for this specification on chain
              name:
                type: string
                title: >-
                  Name of Record that will be created when this specification is
                  used
              inputs:
                type: array
                items:
                  type: object
                  properties:
                    name:
                      type: string
                      title: name for this input
                    type_name:
                      type: string
                      title: a type_name (typically a proto name or class_name)
                    record_id:
                      type: string
                      format: byte
                      title: >-
                        the address of a record on chain (For Established
                        Records)
                    hash:
                      type: string
                      title: >-
                        the hash of an off-chain piece of information (For
                        Proposed Records)
                  title: >-
                    InputSpecification defines a name, type_name, and source
                    reference (either on or off chain) to define an input

                    parameter
                title: >-
                  A set of inputs that must be satisified to apply this
                  RecordSpecification and create a Record
              type_name:
                type: string
                title: >-
                  A type name for data associated with this record (typically a
                  class or proto name)
              result_type:
                title: >-
                  Type of result for this record specification (must be RECORD
                  or RECORD_LIST)
                type: string
                enum:
                  - DEFINITION_TYPE_UNSPECIFIED
                  - DEFINITION_TYPE_PROPOSED
                  - DEFINITION_TYPE_RECORD
                  - DEFINITION_TYPE_RECORD_LIST
                default: DEFINITION_TYPE_UNSPECIFIED
                description: >-
                  - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
                  indicates an unknown/invalid value
                   - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                   - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                   - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                  the same name
              responsible_parties:
                type: array
                items:
                  type: string
                  enum:
                    - PARTY_TYPE_UNSPECIFIED
                    - PARTY_TYPE_ORIGINATOR
                    - PARTY_TYPE_SERVICER
                    - PARTY_TYPE_INVESTOR
                    - PARTY_TYPE_CUSTODIAN
                    - PARTY_TYPE_OWNER
                    - PARTY_TYPE_AFFILIATE
                    - PARTY_TYPE_OMNIBUS
                    - PARTY_TYPE_PROVENANCE
                    - PARTY_TYPE_CONTROLLER
                    - PARTY_TYPE_VALIDATOR
                    - PARTY_TYPE_GENERIC_1
                    - PARTY_TYPE_GENERIC_2
                    - PARTY_TYPE_GENERIC_3
                    - PARTY_TYPE_GENERIC_4
                    - PARTY_TYPE_GENERIC_5
                    - PARTY_TYPE_GENERIC_6
                  default: PARTY_TYPE_UNSPECIFIED
                  description: >-
                    - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                    condition
                     - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                     - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                     - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                     - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                     - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                     - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                     - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                     - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                     - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                     - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                     - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                  title: >-
                    PartyType are the different roles parties on a contract may
                    use
                title: Type of party responsible for this record
            title: >-
              RecordSpecification defines the specification for a Record
              including allowed/required inputs/outputs
          record_spec_id_info:
            description: >-
              record_spec_id_info contains information about the id/address of
              the record specification.
            type: object
            properties:
              record_spec_id:
                type: string
                format: byte
                description: >-
                  record_spec_id is the raw bytes of the record specification
                  address.
              record_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  record_spec_id_prefix is the prefix portion of the
                  record_spec_id.
              record_spec_id_contract_spec_uuid:
                type: string
                format: byte
                description: >-
                  record_spec_id_contract_spec_uuid is the contract_spec_uuid
                  portion of the record_spec_id.
              record_spec_id_hashed_name:
                type: string
                format: byte
                description: >-
                  record_spec_id_hashed_name is the hashed name portion of the
                  record_spec_id.
              record_spec_addr:
                type: string
                description: >-
                  record_spec_addr is the bech32 string version of the
                  record_spec_id.
              contract_spec_id_info:
                description: >-
                  contract_spec_id_info is information about the contract spec
                  id referenced in the record_spec_id.
                type: object
                properties:
                  contract_spec_id:
                    type: string
                    format: byte
                    description: >-
                      contract_spec_id is the raw bytes of the contract
                      specification address.
                  contract_spec_id_prefix:
                    type: string
                    format: byte
                    description: >-
                      contract_spec_id_prefix is the prefix portion of the
                      contract_spec_id.
                  contract_spec_id_contract_spec_uuid:
                    type: string
                    format: byte
                    description: >-
                      contract_spec_id_contract_spec_uuid is the
                      contract_spec_uuid portion of the contract_spec_id.
                  contract_spec_addr:
                    type: string
                    description: >-
                      contract_spec_addr is the bech32 string version of the
                      contract_spec_id.
                  contract_spec_uuid:
                    type: string
                    description: >-
                      contract_spec_uuid is the uuid hex string of the
                      contract_spec_id_contract_spec_uuid.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          specification_id:
            type: string
            description: >-
              specification_id can either be a uuid, e.g.
              def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
              specification

              address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

              It can also be a record specification address, e.g.

              recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          name:
            type: string
            description: >-
              name is the name of the record to look up.

              It is required if the specification_id is a uuid or contract
              specification address.

              It is ignored if the specification_id is a record specification
              address.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: >-
      RecordSpecificationResponse is the response type for the
      Query/RecordSpecification RPC method.
  provenance.metadata.v1.RecordSpecificationWrapper:
    type: object
    properties:
      specification:
        description: specification is the on-chain record specification message.
        type: object
        properties:
          specification_id:
            type: string
            format: byte
            title: unique identifier for this specification on chain
          name:
            type: string
            title: >-
              Name of Record that will be created when this specification is
              used
          inputs:
            type: array
            items:
              type: object
              properties:
                name:
                  type: string
                  title: name for this input
                type_name:
                  type: string
                  title: a type_name (typically a proto name or class_name)
                record_id:
                  type: string
                  format: byte
                  title: the address of a record on chain (For Established Records)
                hash:
                  type: string
                  title: >-
                    the hash of an off-chain piece of information (For Proposed
                    Records)
              title: >-
                InputSpecification defines a name, type_name, and source
                reference (either on or off chain) to define an input

                parameter
            title: >-
              A set of inputs that must be satisified to apply this
              RecordSpecification and create a Record
          type_name:
            type: string
            title: >-
              A type name for data associated with this record (typically a
              class or proto name)
          result_type:
            title: >-
              Type of result for this record specification (must be RECORD or
              RECORD_LIST)
            type: string
            enum:
              - DEFINITION_TYPE_UNSPECIFIED
              - DEFINITION_TYPE_PROPOSED
              - DEFINITION_TYPE_RECORD
              - DEFINITION_TYPE_RECORD_LIST
            default: DEFINITION_TYPE_UNSPECIFIED
            description: >-
              - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
              indicates an unknown/invalid value
               - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
               - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
               - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
              the same name
          responsible_parties:
            type: array
            items:
              type: string
              enum:
                - PARTY_TYPE_UNSPECIFIED
                - PARTY_TYPE_ORIGINATOR
                - PARTY_TYPE_SERVICER
                - PARTY_TYPE_INVESTOR
                - PARTY_TYPE_CUSTODIAN
                - PARTY_TYPE_OWNER
                - PARTY_TYPE_AFFILIATE
                - PARTY_TYPE_OMNIBUS
                - PARTY_TYPE_PROVENANCE
                - PARTY_TYPE_CONTROLLER
                - PARTY_TYPE_VALIDATOR
                - PARTY_TYPE_GENERIC_1
                - PARTY_TYPE_GENERIC_2
                - PARTY_TYPE_GENERIC_3
                - PARTY_TYPE_GENERIC_4
                - PARTY_TYPE_GENERIC_5
                - PARTY_TYPE_GENERIC_6
              default: PARTY_TYPE_UNSPECIFIED
              description: >-
                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                condition
                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
              title: PartyType are the different roles parties on a contract may use
            title: Type of party responsible for this record
        title: >-
          RecordSpecification defines the specification for a Record including
          allowed/required inputs/outputs
      record_spec_id_info:
        description: >-
          record_spec_id_info contains information about the id/address of the
          record specification.
        type: object
        properties:
          record_spec_id:
            type: string
            format: byte
            description: >-
              record_spec_id is the raw bytes of the record specification
              address.
          record_spec_id_prefix:
            type: string
            format: byte
            description: record_spec_id_prefix is the prefix portion of the record_spec_id.
          record_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              record_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the record_spec_id.
          record_spec_id_hashed_name:
            type: string
            format: byte
            description: >-
              record_spec_id_hashed_name is the hashed name portion of the
              record_spec_id.
          record_spec_addr:
            type: string
            description: >-
              record_spec_addr is the bech32 string version of the
              record_spec_id.
          contract_spec_id_info:
            description: >-
              contract_spec_id_info is information about the contract spec id
              referenced in the record_spec_id.
            type: object
            properties:
              contract_spec_id:
                type: string
                format: byte
                description: >-
                  contract_spec_id is the raw bytes of the contract
                  specification address.
              contract_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  contract_spec_id_prefix is the prefix portion of the
                  contract_spec_id.
              contract_spec_id_contract_spec_uuid:
                type: string
                format: byte
                description: >-
                  contract_spec_id_contract_spec_uuid is the contract_spec_uuid
                  portion of the contract_spec_id.
              contract_spec_addr:
                type: string
                description: >-
                  contract_spec_addr is the bech32 string version of the
                  contract_spec_id.
              contract_spec_uuid:
                type: string
                description: >-
                  contract_spec_uuid is the uuid hex string of the
                  contract_spec_id_contract_spec_uuid.
    description: >-
      RecordSpecificationWrapper contains a single record specification and some
      extra identifiers for it.
  provenance.metadata.v1.RecordSpecificationsAllRequest:
    type: object
    properties:
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      RecordSpecificationsAllRequest is the request type for the
      Query/RecordSpecificationsAll RPC method.
  provenance.metadata.v1.RecordSpecificationsAllResponse:
    type: object
    properties:
      record_specifications:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain record specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                name:
                  type: string
                  title: >-
                    Name of Record that will be created when this specification
                    is used
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        title: name for this input
                      type_name:
                        type: string
                        title: a type_name (typically a proto name or class_name)
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                    title: >-
                      InputSpecification defines a name, type_name, and source
                      reference (either on or off chain) to define an input

                      parameter
                  title: >-
                    A set of inputs that must be satisified to apply this
                    RecordSpecification and create a Record
                type_name:
                  type: string
                  title: >-
                    A type name for data associated with this record (typically
                    a class or proto name)
                result_type:
                  title: >-
                    Type of result for this record specification (must be RECORD
                    or RECORD_LIST)
                  type: string
                  enum:
                    - DEFINITION_TYPE_UNSPECIFIED
                    - DEFINITION_TYPE_PROPOSED
                    - DEFINITION_TYPE_RECORD
                    - DEFINITION_TYPE_RECORD_LIST
                  default: DEFINITION_TYPE_UNSPECIFIED
                  description: >-
                    - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
                    indicates an unknown/invalid value
                     - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                     - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                     - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                    the same name
                responsible_parties:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: Type of party responsible for this record
              title: >-
                RecordSpecification defines the specification for a Record
                including allowed/required inputs/outputs
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordSpecificationWrapper contains a single record specification
            and some extra identifiers for it.
        description: record_specifications are the wrapped record specifications.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      RecordSpecificationsAllResponse is the response type for the
      Query/RecordSpecificationsAll RPC method.
  provenance.metadata.v1.RecordSpecificationsForContractSpecificationRequest:
    type: object
    properties:
      specification_id:
        type: string
        description: >-
          specification_id can either be a uuid, e.g.
          def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
          specification

          address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

          It can also be a record specification address, e.g.

          recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: >-
      RecordSpecificationsForContractSpecificationRequest is the request type
      for the

      Query/RecordSpecificationsForContractSpecification RPC method.
  provenance.metadata.v1.RecordSpecificationsForContractSpecificationResponse:
    type: object
    properties:
      record_specifications:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain record specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                name:
                  type: string
                  title: >-
                    Name of Record that will be created when this specification
                    is used
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        title: name for this input
                      type_name:
                        type: string
                        title: a type_name (typically a proto name or class_name)
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                    title: >-
                      InputSpecification defines a name, type_name, and source
                      reference (either on or off chain) to define an input

                      parameter
                  title: >-
                    A set of inputs that must be satisified to apply this
                    RecordSpecification and create a Record
                type_name:
                  type: string
                  title: >-
                    A type name for data associated with this record (typically
                    a class or proto name)
                result_type:
                  title: >-
                    Type of result for this record specification (must be RECORD
                    or RECORD_LIST)
                  type: string
                  enum:
                    - DEFINITION_TYPE_UNSPECIFIED
                    - DEFINITION_TYPE_PROPOSED
                    - DEFINITION_TYPE_RECORD
                    - DEFINITION_TYPE_RECORD_LIST
                  default: DEFINITION_TYPE_UNSPECIFIED
                  description: >-
                    - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
                    indicates an unknown/invalid value
                     - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                     - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                     - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                    the same name
                responsible_parties:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: Type of party responsible for this record
              title: >-
                RecordSpecification defines the specification for a Record
                including allowed/required inputs/outputs
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordSpecificationWrapper contains a single record specification
            and some extra identifiers for it.
        description: >-
          record_specifications is any number of wrapped record specifications
          associated with this contract_specification.
      contract_specification_uuid:
        type: string
        description: >-
          contract_specification_uuid is the uuid of this contract
          specification.
      contract_specification_addr:
        type: string
        description: >-
          contract_specification_addr is the contract specification address as a
          bech32 encoded string.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          specification_id:
            type: string
            description: >-
              specification_id can either be a uuid, e.g.
              def6bc0a-c9dd-4874-948f-5206e6060a84 or a bech32 contract
              specification

              address, e.g. contractspec1q000d0q2e8w5say53afqdesxp2zqzkr4fn.

              It can also be a record specification address, e.g.

              recspec1qh00d0q2e8w5say53afqdesxp2zw42dq2jdvmdazuwzcaddhh8gmuqhez44.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: >-
      RecordSpecificationsForContractSpecificationResponse is the response type
      for the

      Query/RecordSpecificationsForContractSpecification RPC method.
  provenance.metadata.v1.RecordWrapper:
    type: object
    properties:
      record:
        description: record is the on-chain record message.
        type: object
        properties:
          name:
            type: string
            title: >-
              name/identifier for this record.  Value must be unique within the
              scope.  Also known as a Fact name
          session_id:
            type: string
            format: byte
            title: >-
              id of the session context that was used to create this record (use
              with filtered kvprefix iterator)
          process:
            title: >-
              process contain information used to uniquely identify an execution
              on or off chain that generated this record
            type: object
            properties:
              address:
                type: string
                title: the address of a smart contract used for this process
              hash:
                type: string
                title: the hash of an off-chain process used
              name:
                type: string
                title: >-
                  a name associated with the process (type_name, classname or
                  smart contract common name)
              method:
                type: string
                title: >-
                  method is a name or reference to a specific operation (method)
                  within a class/contract that was invoked
          inputs:
            type: array
            items:
              type: object
              properties:
                name:
                  type: string
                  description: Name value included to link back to the definition spec.
                record_id:
                  type: string
                  format: byte
                  title: the address of a record on chain (For Established Records)
                hash:
                  type: string
                  title: >-
                    the hash of an off-chain piece of information (For Proposed
                    Records)
                type_name:
                  type: string
                  title: from proposed fact structure to unmarshal
                status:
                  title: >-
                    Indicates if this input was a recorded fact on chain or just
                    a given hashed input
                  type: string
                  enum:
                    - RECORD_INPUT_STATUS_UNSPECIFIED
                    - RECORD_INPUT_STATUS_PROPOSED
                    - RECORD_INPUT_STATUS_RECORD
                  default: RECORD_INPUT_STATUS_UNSPECIFIED
                  description: >-
                    - RECORD_INPUT_STATUS_UNSPECIFIED:
                    RECORD_INPUT_STATUS_UNSPECIFIED indicates an invalid/unknown
                    input type
                     - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                     - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
              title: Tracks the inputs used to establish this record
            title: inputs used with the process to achieve the output on this record
          outputs:
            type: array
            items:
              type: object
              properties:
                hash:
                  type: string
                  title: >-
                    Hash of the data output that was output/generated for this
                    record
                status:
                  title: >-
                    Status of the process execution associated with this output
                    indicating success,failure, or pending
                  type: string
                  enum:
                    - RESULT_STATUS_UNSPECIFIED
                    - RESULT_STATUS_PASS
                    - RESULT_STATUS_SKIP
                    - RESULT_STATUS_FAIL
                  default: RESULT_STATUS_UNSPECIFIED
                  description: >-
                    - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED
                    indicates an unset condition
                     - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                     - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                     - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
              title: >-
                RecordOutput encapsulates the output of a process recorded on
                chain
            title: >-
              output(s) is the results of executing the process on the given
              process indicated in this record
          specification_id:
            type: string
            format: byte
            description: >-
              specification_id is the id of the record specification that was
              used to create this record.
        title: >-
          A record (of fact) is attached to a session or each consideration
          output from a contract
      record_id_info:
        description: >-
          record_id_info contains information about the id/address of the
          record.
        type: object
        properties:
          record_id:
            type: string
            format: byte
            description: record_id is the raw bytes of the record address.
          record_id_prefix:
            type: string
            format: byte
            description: record_id_prefix is the prefix portion of the record_id.
          record_id_scope_uuid:
            type: string
            format: byte
            description: record_id_scope_uuid is the scope_uuid portion of the record_id.
          record_id_hashed_name:
            type: string
            format: byte
            description: record_id_hashed_name is the hashed name portion of the record_id.
          record_addr:
            type: string
            description: record_addr is the bech32 string version of the record_id.
          scope_id_info:
            description: >-
              scope_id_info is information about the scope id referenced in the
              record_id.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
      record_spec_id_info:
        description: >-
          record_spec_id_info contains information about the id/address of the
          record specification.
        type: object
        properties:
          record_spec_id:
            type: string
            format: byte
            description: >-
              record_spec_id is the raw bytes of the record specification
              address.
          record_spec_id_prefix:
            type: string
            format: byte
            description: record_spec_id_prefix is the prefix portion of the record_spec_id.
          record_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              record_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the record_spec_id.
          record_spec_id_hashed_name:
            type: string
            format: byte
            description: >-
              record_spec_id_hashed_name is the hashed name portion of the
              record_spec_id.
          record_spec_addr:
            type: string
            description: >-
              record_spec_addr is the bech32 string version of the
              record_spec_id.
          contract_spec_id_info:
            description: >-
              contract_spec_id_info is information about the contract spec id
              referenced in the record_spec_id.
            type: object
            properties:
              contract_spec_id:
                type: string
                format: byte
                description: >-
                  contract_spec_id is the raw bytes of the contract
                  specification address.
              contract_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  contract_spec_id_prefix is the prefix portion of the
                  contract_spec_id.
              contract_spec_id_contract_spec_uuid:
                type: string
                format: byte
                description: >-
                  contract_spec_id_contract_spec_uuid is the contract_spec_uuid
                  portion of the contract_spec_id.
              contract_spec_addr:
                type: string
                description: >-
                  contract_spec_addr is the bech32 string version of the
                  contract_spec_id.
              contract_spec_uuid:
                type: string
                description: >-
                  contract_spec_uuid is the uuid hex string of the
                  contract_spec_id_contract_spec_uuid.
    description: RecordWrapper contains a single record and some extra identifiers for it.
  provenance.metadata.v1.RecordsAllRequest:
    type: object
    properties:
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: RecordsAllRequest is the request type for the Query/RecordsAll RPC method.
  provenance.metadata.v1.RecordsAllResponse:
    type: object
    properties:
      records:
        type: array
        items:
          type: object
          properties:
            record:
              description: record is the on-chain record message.
              type: object
              properties:
                name:
                  type: string
                  title: >-
                    name/identifier for this record.  Value must be unique
                    within the scope.  Also known as a Fact name
                session_id:
                  type: string
                  format: byte
                  title: >-
                    id of the session context that was used to create this
                    record (use with filtered kvprefix iterator)
                process:
                  title: >-
                    process contain information used to uniquely identify an
                    execution on or off chain that generated this record
                  type: object
                  properties:
                    address:
                      type: string
                      title: the address of a smart contract used for this process
                    hash:
                      type: string
                      title: the hash of an off-chain process used
                    name:
                      type: string
                      title: >-
                        a name associated with the process (type_name, classname
                        or smart contract common name)
                    method:
                      type: string
                      title: >-
                        method is a name or reference to a specific operation
                        (method) within a class/contract that was invoked
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: >-
                          Name value included to link back to the definition
                          spec.
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                      type_name:
                        type: string
                        title: from proposed fact structure to unmarshal
                      status:
                        title: >-
                          Indicates if this input was a recorded fact on chain
                          or just a given hashed input
                        type: string
                        enum:
                          - RECORD_INPUT_STATUS_UNSPECIFIED
                          - RECORD_INPUT_STATUS_PROPOSED
                          - RECORD_INPUT_STATUS_RECORD
                        default: RECORD_INPUT_STATUS_UNSPECIFIED
                        description: >-
                          - RECORD_INPUT_STATUS_UNSPECIFIED:
                          RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                          invalid/unknown input type
                           - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                           - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                    title: Tracks the inputs used to establish this record
                  title: >-
                    inputs used with the process to achieve the output on this
                    record
                outputs:
                  type: array
                  items:
                    type: object
                    properties:
                      hash:
                        type: string
                        title: >-
                          Hash of the data output that was output/generated for
                          this record
                      status:
                        title: >-
                          Status of the process execution associated with this
                          output indicating success,failure, or pending
                        type: string
                        enum:
                          - RESULT_STATUS_UNSPECIFIED
                          - RESULT_STATUS_PASS
                          - RESULT_STATUS_SKIP
                          - RESULT_STATUS_FAIL
                        default: RESULT_STATUS_UNSPECIFIED
                        description: >-
                          - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED
                          indicates an unset condition
                           - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                           - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                           - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                    title: >-
                      RecordOutput encapsulates the output of a process recorded
                      on chain
                  title: >-
                    output(s) is the results of executing the process on the
                    given process indicated in this record
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    specification_id is the id of the record specification that
                    was used to create this record.
              title: >-
                A record (of fact) is attached to a session or each
                consideration output from a contract
            record_id_info:
              description: >-
                record_id_info contains information about the id/address of the
                record.
              type: object
              properties:
                record_id:
                  type: string
                  format: byte
                  description: record_id is the raw bytes of the record address.
                record_id_prefix:
                  type: string
                  format: byte
                  description: record_id_prefix is the prefix portion of the record_id.
                record_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_id_scope_uuid is the scope_uuid portion of the
                    record_id.
                record_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_id_hashed_name is the hashed name portion of the
                    record_id.
                record_addr:
                  type: string
                  description: record_addr is the bech32 string version of the record_id.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the record_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordWrapper contains a single record and some extra identifiers
            for it.
        description: records are the wrapped records.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      RecordsAllResponse is the response type for the Query/RecordsAll RPC
      method.
  provenance.metadata.v1.RecordsRequest:
    type: object
    properties:
      record_addr:
        type: string
        description: >-
          record_addr is a bech32 record address, e.g.
          record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
      scope_id:
        type: string
        description: >-
          scope_id can either be a uuid, e.g.
          91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

          scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
      session_id:
        type: string
        description: >-
          session_id can either be a uuid, e.g.
          5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address, e.g.

          session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
          This can only be a uuid if a scope_id is also

          provided.
      name:
        type: string
        title: name is the name of the record to look for
      include_scope:
        type: boolean
        description: >-
          include_scope is a flag for whether to include the the scope
          containing these records in the response.
      include_sessions:
        type: boolean
        description: >-
          include_sessions is a flag for whether to include the sessions
          containing these records in the response.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: RecordsRequest is the request type for the Query/Records RPC method.
  provenance.metadata.v1.RecordsResponse:
    type: object
    properties:
      scope:
        description: scope is the wrapped scope that holds these records (if requested).
        type: object
        properties:
          scope:
            description: scope is the on-chain scope message.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                title: >-
                  Unique ID for this scope.  Implements sdk.Address interface
                  for use where addresses are required in Cosmos
              specification_id:
                type: string
                format: byte
                title: >-
                  the scope specification that contains the specifications for
                  data elements allowed within this scope
              owners:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      title: address of the account (on chain)
                    role:
                      title: >-
                        a role for this account within the context of the
                        processes used
                      type: string
                      enum:
                        - PARTY_TYPE_UNSPECIFIED
                        - PARTY_TYPE_ORIGINATOR
                        - PARTY_TYPE_SERVICER
                        - PARTY_TYPE_INVESTOR
                        - PARTY_TYPE_CUSTODIAN
                        - PARTY_TYPE_OWNER
                        - PARTY_TYPE_AFFILIATE
                        - PARTY_TYPE_OMNIBUS
                        - PARTY_TYPE_PROVENANCE
                        - PARTY_TYPE_CONTROLLER
                        - PARTY_TYPE_VALIDATOR
                        - PARTY_TYPE_GENERIC_1
                        - PARTY_TYPE_GENERIC_2
                        - PARTY_TYPE_GENERIC_3
                        - PARTY_TYPE_GENERIC_4
                        - PARTY_TYPE_GENERIC_5
                        - PARTY_TYPE_GENERIC_6
                      default: PARTY_TYPE_UNSPECIFIED
                      description: >-
                        - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                        error condition
                         - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                         - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                         - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                         - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                         - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                         - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                         - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                         - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                         - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                         - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                         - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    optional:
                      type: boolean
                      title: whether this party's signature is optional
                  title: >-
                    A Party is an address with/in a given role associated with a
                    contract
                description: >-
                  These parties represent top level owners of the records
                  within.  These parties must sign any requests that modify

                  the data within the scope.  These addresses are in union with
                  parties listed on the sessions.
              data_access:
                type: array
                items:
                  type: string
                description: >-
                  Addresses in this list are authorized to receive off-chain
                  data associated with this scope.
              value_owner_address:
                type: string
                description: >-
                  The address that controls the value associated with this
                  scope.


                  The value owner is actually tracked by the bank module using a
                  coin with the denom "nft/<scope_id>".

                  The value owner can be changed using WriteScope or anything
                  that transfers funds, e.g. MsgSend.


                  During WriteScope:
                   - If this field is empty, it indicates that there should not be a change to the value owner.
                     I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                   - If this field has a value, the existing value owner will be looked up, and
                     - If there's already an existing value owner, they must be a signer,
                       and the coin will be transferred to the new value owner.
                     - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                       If the scope already exists, the owners must be signers (just like changing other fields).
                       If it's a new scope, there's no special signer limitations related to the value owner.
              require_party_rollup:
                type: boolean
                description: >-
                  Whether all parties in this scope and its sessions must be
                  present in this scope's owners field.

                  This also enables use of optional=true scope owners and
                  session parties.
          scope_id_info:
            description: >-
              scope_id_info contains information about the id/address of the
              scope.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
          scope_spec_id_info:
            description: >-
              scope_spec_id_info contains information about the id/address of
              the scope specification.
            type: object
            properties:
              scope_spec_id:
                type: string
                format: byte
                description: >-
                  scope_spec_id is the raw bytes of the scope specification
                  address.
              scope_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  scope_spec_id_prefix is the prefix portion of the
                  scope_spec_id.
              scope_spec_id_scope_spec_uuid:
                type: string
                format: byte
                description: >-
                  scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion
                  of the scope_spec_id.
              scope_spec_addr:
                type: string
                description: >-
                  scope_spec_addr is the bech32 string version of the
                  scope_spec_id.
              scope_spec_uuid:
                type: string
                description: >-
                  scope_spec_uuid is the uuid hex string of the
                  scope_spec_id_scope_spec_uuid.
      sessions:
        type: array
        items:
          type: object
          properties:
            session:
              description: session is the on-chain session message.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    unique id of the contract specification that was used to
                    create this session.
                parties:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        title: address of the account (on chain)
                      role:
                        title: >-
                          a role for this account within the context of the
                          processes used
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                      optional:
                        type: boolean
                        title: whether this party's signature is optional
                    title: >-
                      A Party is an address with/in a given role associated with
                      a contract
                  title: parties is the set of identities that signed this contract
                name:
                  type: string
                  title: >-
                    name to associate with this session execution context,
                    typically classname
                context:
                  type: string
                  format: byte
                  description: >-
                    context is a field for storing client specific data
                    associated with a session.
                audit:
                  description: >-
                    Created by, updated by, timestamps, version number, and
                    related info.
                  type: object
                  properties:
                    created_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was created
                    created_by:
                      type: string
                      title: the address of the account that created this record
                    updated_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was last updated
                    updated_by:
                      type: string
                      title: the address of the account that modified this record
                    version:
                      type: integer
                      format: int64
                      title: >-
                        an optional version number that is incremented with each
                        update
                    message:
                      type: string
                      title: >-
                        an optional message associated with the creation/update
                        event
                  title: >-
                    AuditFields capture information about the last account to
                    make modifications and when they were made
            session_id_info:
              description: >-
                session_id_info contains information about the id/address of the
                session.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                  description: session_id is the raw bytes of the session address.
                session_id_prefix:
                  type: string
                  format: byte
                  description: session_id_prefix is the prefix portion of the session_id.
                session_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_scope_uuid is the scope_uuid portion of the
                    session_id.
                session_id_session_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_session_uuid is the session_uuid portion of the
                    session_id.
                session_addr:
                  type: string
                  description: session_addr is the bech32 string version of the session_id.
                session_uuid:
                  type: string
                  description: >-
                    session_uuid is the uuid hex string of the
                    session_id_session_uuid.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the session_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            contract_spec_id_info:
              description: >-
                contract_spec_id_info contains information about the id/address
                of the contract specification.
              type: object
              properties:
                contract_spec_id:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id is the raw bytes of the contract
                    specification address.
                contract_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_prefix is the prefix portion of the
                    contract_spec_id.
                contract_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_contract_spec_uuid is the
                    contract_spec_uuid portion of the contract_spec_id.
                contract_spec_addr:
                  type: string
                  description: >-
                    contract_spec_addr is the bech32 string version of the
                    contract_spec_id.
                contract_spec_uuid:
                  type: string
                  description: >-
                    contract_spec_uuid is the uuid hex string of the
                    contract_spec_id_contract_spec_uuid.
          description: >-
            SessionWrapper contains a single session and some extra identifiers
            for it.
        description: >-
          sessions is any number of wrapped sessions that hold these records (if
          requested).
      records:
        type: array
        items:
          type: object
          properties:
            record:
              description: record is the on-chain record message.
              type: object
              properties:
                name:
                  type: string
                  title: >-
                    name/identifier for this record.  Value must be unique
                    within the scope.  Also known as a Fact name
                session_id:
                  type: string
                  format: byte
                  title: >-
                    id of the session context that was used to create this
                    record (use with filtered kvprefix iterator)
                process:
                  title: >-
                    process contain information used to uniquely identify an
                    execution on or off chain that generated this record
                  type: object
                  properties:
                    address:
                      type: string
                      title: the address of a smart contract used for this process
                    hash:
                      type: string
                      title: the hash of an off-chain process used
                    name:
                      type: string
                      title: >-
                        a name associated with the process (type_name, classname
                        or smart contract common name)
                    method:
                      type: string
                      title: >-
                        method is a name or reference to a specific operation
                        (method) within a class/contract that was invoked
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: >-
                          Name value included to link back to the definition
                          spec.
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                      type_name:
                        type: string
                        title: from proposed fact structure to unmarshal
                      status:
                        title: >-
                          Indicates if this input was a recorded fact on chain
                          or just a given hashed input
                        type: string
                        enum:
                          - RECORD_INPUT_STATUS_UNSPECIFIED
                          - RECORD_INPUT_STATUS_PROPOSED
                          - RECORD_INPUT_STATUS_RECORD
                        default: RECORD_INPUT_STATUS_UNSPECIFIED
                        description: >-
                          - RECORD_INPUT_STATUS_UNSPECIFIED:
                          RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                          invalid/unknown input type
                           - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                           - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                    title: Tracks the inputs used to establish this record
                  title: >-
                    inputs used with the process to achieve the output on this
                    record
                outputs:
                  type: array
                  items:
                    type: object
                    properties:
                      hash:
                        type: string
                        title: >-
                          Hash of the data output that was output/generated for
                          this record
                      status:
                        title: >-
                          Status of the process execution associated with this
                          output indicating success,failure, or pending
                        type: string
                        enum:
                          - RESULT_STATUS_UNSPECIFIED
                          - RESULT_STATUS_PASS
                          - RESULT_STATUS_SKIP
                          - RESULT_STATUS_FAIL
                        default: RESULT_STATUS_UNSPECIFIED
                        description: >-
                          - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED
                          indicates an unset condition
                           - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                           - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                           - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                    title: >-
                      RecordOutput encapsulates the output of a process recorded
                      on chain
                  title: >-
                    output(s) is the results of executing the process on the
                    given process indicated in this record
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    specification_id is the id of the record specification that
                    was used to create this record.
              title: >-
                A record (of fact) is attached to a session or each
                consideration output from a contract
            record_id_info:
              description: >-
                record_id_info contains information about the id/address of the
                record.
              type: object
              properties:
                record_id:
                  type: string
                  format: byte
                  description: record_id is the raw bytes of the record address.
                record_id_prefix:
                  type: string
                  format: byte
                  description: record_id_prefix is the prefix portion of the record_id.
                record_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_id_scope_uuid is the scope_uuid portion of the
                    record_id.
                record_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_id_hashed_name is the hashed name portion of the
                    record_id.
                record_addr:
                  type: string
                  description: record_addr is the bech32 string version of the record_id.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the record_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordWrapper contains a single record and some extra identifiers
            for it.
        description: records is any number of wrapped record results.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          record_addr:
            type: string
            description: >-
              record_addr is a bech32 record address, e.g.
              record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          scope_id:
            type: string
            description: >-
              scope_id can either be a uuid, e.g.
              91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address,
              e.g.

              scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          session_id:
            type: string
            description: >-
              session_id can either be a uuid, e.g.
              5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
              e.g.

              session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
              This can only be a uuid if a scope_id is also

              provided.
          name:
            type: string
            title: name is the name of the record to look for
          include_scope:
            type: boolean
            description: >-
              include_scope is a flag for whether to include the the scope
              containing these records in the response.
          include_sessions:
            type: boolean
            description: >-
              include_sessions is a flag for whether to include the sessions
              containing these records in the response.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: RecordsResponse is the response type for the Query/Records RPC method.
  provenance.metadata.v1.ResultStatus:
    type: string
    enum:
      - RESULT_STATUS_UNSPECIFIED
      - RESULT_STATUS_PASS
      - RESULT_STATUS_SKIP
      - RESULT_STATUS_FAIL
    default: RESULT_STATUS_UNSPECIFIED
    description: >-
      - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED indicates an unset
      condition
       - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
       - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
       - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
    title: ResultStatus indicates the various states of execution of a record
  provenance.metadata.v1.Scope:
    type: object
    properties:
      scope_id:
        type: string
        format: byte
        title: >-
          Unique ID for this scope.  Implements sdk.Address interface for use
          where addresses are required in Cosmos
      specification_id:
        type: string
        format: byte
        title: >-
          the scope specification that contains the specifications for data
          elements allowed within this scope
      owners:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              title: address of the account (on chain)
            role:
              title: a role for this account within the context of the processes used
              type: string
              enum:
                - PARTY_TYPE_UNSPECIFIED
                - PARTY_TYPE_ORIGINATOR
                - PARTY_TYPE_SERVICER
                - PARTY_TYPE_INVESTOR
                - PARTY_TYPE_CUSTODIAN
                - PARTY_TYPE_OWNER
                - PARTY_TYPE_AFFILIATE
                - PARTY_TYPE_OMNIBUS
                - PARTY_TYPE_PROVENANCE
                - PARTY_TYPE_CONTROLLER
                - PARTY_TYPE_VALIDATOR
                - PARTY_TYPE_GENERIC_1
                - PARTY_TYPE_GENERIC_2
                - PARTY_TYPE_GENERIC_3
                - PARTY_TYPE_GENERIC_4
                - PARTY_TYPE_GENERIC_5
                - PARTY_TYPE_GENERIC_6
              default: PARTY_TYPE_UNSPECIFIED
              description: >-
                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                condition
                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
            optional:
              type: boolean
              title: whether this party's signature is optional
          title: >-
            A Party is an address with/in a given role associated with a
            contract
        description: >-
          These parties represent top level owners of the records within.  These
          parties must sign any requests that modify

          the data within the scope.  These addresses are in union with parties
          listed on the sessions.
      data_access:
        type: array
        items:
          type: string
        description: >-
          Addresses in this list are authorized to receive off-chain data
          associated with this scope.
      value_owner_address:
        type: string
        description: >-
          The address that controls the value associated with this scope.


          The value owner is actually tracked by the bank module using a coin
          with the denom "nft/<scope_id>".

          The value owner can be changed using WriteScope or anything that
          transfers funds, e.g. MsgSend.


          During WriteScope:
           - If this field is empty, it indicates that there should not be a change to the value owner.
             I.e. Once a scope has a value owner, it will always have one (until it's deleted).
           - If this field has a value, the existing value owner will be looked up, and
             - If there's already an existing value owner, they must be a signer,
               and the coin will be transferred to the new value owner.
             - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
               If the scope already exists, the owners must be signers (just like changing other fields).
               If it's a new scope, there's no special signer limitations related to the value owner.
      require_party_rollup:
        type: boolean
        description: >-
          Whether all parties in this scope and its sessions must be present in
          this scope's owners field.

          This also enables use of optional=true scope owners and session
          parties.
    description: >-
      Scope defines a root reference for a collection of records owned by one or
      more parties.
  provenance.metadata.v1.ScopeIdInfo:
    type: object
    properties:
      scope_id:
        type: string
        format: byte
        description: scope_id is the raw bytes of the scope address.
      scope_id_prefix:
        type: string
        format: byte
        description: scope_id_prefix is the prefix portion of the scope_id.
      scope_id_scope_uuid:
        type: string
        format: byte
        description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
      scope_addr:
        type: string
        description: scope_addr is the bech32 string version of the scope_id.
      scope_uuid:
        type: string
        description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
    description: ScopeIdInfo contains various info regarding a scope id.
  provenance.metadata.v1.ScopeRequest:
    type: object
    properties:
      scope_id:
        type: string
        description: >-
          scope_id can either be a uuid, e.g.
          91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

          scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
      session_addr:
        type: string
        description: |-
          session_addr is a bech32 session address, e.g.
          session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
      record_addr:
        type: string
        description: >-
          record_addr is a bech32 record address, e.g.
          record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
      include_sessions:
        type: boolean
        description: >-
          include_sessions is a flag for whether to include the sessions of the
          scope in the response.
      include_records:
        type: boolean
        description: >-
          include_records is a flag for whether to include the records of the
          scope in the response.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: ScopeRequest is the request type for the Query/Scope RPC method.
  provenance.metadata.v1.ScopeResponse:
    type: object
    properties:
      scope:
        description: scope is the wrapped scope result.
        type: object
        properties:
          scope:
            description: scope is the on-chain scope message.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                title: >-
                  Unique ID for this scope.  Implements sdk.Address interface
                  for use where addresses are required in Cosmos
              specification_id:
                type: string
                format: byte
                title: >-
                  the scope specification that contains the specifications for
                  data elements allowed within this scope
              owners:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      title: address of the account (on chain)
                    role:
                      title: >-
                        a role for this account within the context of the
                        processes used
                      type: string
                      enum:
                        - PARTY_TYPE_UNSPECIFIED
                        - PARTY_TYPE_ORIGINATOR
                        - PARTY_TYPE_SERVICER
                        - PARTY_TYPE_INVESTOR
                        - PARTY_TYPE_CUSTODIAN
                        - PARTY_TYPE_OWNER
                        - PARTY_TYPE_AFFILIATE
                        - PARTY_TYPE_OMNIBUS
                        - PARTY_TYPE_PROVENANCE
                        - PARTY_TYPE_CONTROLLER
                        - PARTY_TYPE_VALIDATOR
                        - PARTY_TYPE_GENERIC_1
                        - PARTY_TYPE_GENERIC_2
                        - PARTY_TYPE_GENERIC_3
                        - PARTY_TYPE_GENERIC_4
                        - PARTY_TYPE_GENERIC_5
                        - PARTY_TYPE_GENERIC_6
                      default: PARTY_TYPE_UNSPECIFIED
                      description: >-
                        - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                        error condition
                         - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                         - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                         - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                         - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                         - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                         - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                         - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                         - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                         - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                         - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                         - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    optional:
                      type: boolean
                      title: whether this party's signature is optional
                  title: >-
                    A Party is an address with/in a given role associated with a
                    contract
                description: >-
                  These parties represent top level owners of the records
                  within.  These parties must sign any requests that modify

                  the data within the scope.  These addresses are in union with
                  parties listed on the sessions.
              data_access:
                type: array
                items:
                  type: string
                description: >-
                  Addresses in this list are authorized to receive off-chain
                  data associated with this scope.
              value_owner_address:
                type: string
                description: >-
                  The address that controls the value associated with this
                  scope.


                  The value owner is actually tracked by the bank module using a
                  coin with the denom "nft/<scope_id>".

                  The value owner can be changed using WriteScope or anything
                  that transfers funds, e.g. MsgSend.


                  During WriteScope:
                   - If this field is empty, it indicates that there should not be a change to the value owner.
                     I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                   - If this field has a value, the existing value owner will be looked up, and
                     - If there's already an existing value owner, they must be a signer,
                       and the coin will be transferred to the new value owner.
                     - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                       If the scope already exists, the owners must be signers (just like changing other fields).
                       If it's a new scope, there's no special signer limitations related to the value owner.
              require_party_rollup:
                type: boolean
                description: >-
                  Whether all parties in this scope and its sessions must be
                  present in this scope's owners field.

                  This also enables use of optional=true scope owners and
                  session parties.
          scope_id_info:
            description: >-
              scope_id_info contains information about the id/address of the
              scope.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
          scope_spec_id_info:
            description: >-
              scope_spec_id_info contains information about the id/address of
              the scope specification.
            type: object
            properties:
              scope_spec_id:
                type: string
                format: byte
                description: >-
                  scope_spec_id is the raw bytes of the scope specification
                  address.
              scope_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  scope_spec_id_prefix is the prefix portion of the
                  scope_spec_id.
              scope_spec_id_scope_spec_uuid:
                type: string
                format: byte
                description: >-
                  scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion
                  of the scope_spec_id.
              scope_spec_addr:
                type: string
                description: >-
                  scope_spec_addr is the bech32 string version of the
                  scope_spec_id.
              scope_spec_uuid:
                type: string
                description: >-
                  scope_spec_uuid is the uuid hex string of the
                  scope_spec_id_scope_spec_uuid.
      sessions:
        type: array
        items:
          type: object
          properties:
            session:
              description: session is the on-chain session message.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    unique id of the contract specification that was used to
                    create this session.
                parties:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        title: address of the account (on chain)
                      role:
                        title: >-
                          a role for this account within the context of the
                          processes used
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                      optional:
                        type: boolean
                        title: whether this party's signature is optional
                    title: >-
                      A Party is an address with/in a given role associated with
                      a contract
                  title: parties is the set of identities that signed this contract
                name:
                  type: string
                  title: >-
                    name to associate with this session execution context,
                    typically classname
                context:
                  type: string
                  format: byte
                  description: >-
                    context is a field for storing client specific data
                    associated with a session.
                audit:
                  description: >-
                    Created by, updated by, timestamps, version number, and
                    related info.
                  type: object
                  properties:
                    created_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was created
                    created_by:
                      type: string
                      title: the address of the account that created this record
                    updated_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was last updated
                    updated_by:
                      type: string
                      title: the address of the account that modified this record
                    version:
                      type: integer
                      format: int64
                      title: >-
                        an optional version number that is incremented with each
                        update
                    message:
                      type: string
                      title: >-
                        an optional message associated with the creation/update
                        event
                  title: >-
                    AuditFields capture information about the last account to
                    make modifications and when they were made
            session_id_info:
              description: >-
                session_id_info contains information about the id/address of the
                session.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                  description: session_id is the raw bytes of the session address.
                session_id_prefix:
                  type: string
                  format: byte
                  description: session_id_prefix is the prefix portion of the session_id.
                session_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_scope_uuid is the scope_uuid portion of the
                    session_id.
                session_id_session_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_session_uuid is the session_uuid portion of the
                    session_id.
                session_addr:
                  type: string
                  description: session_addr is the bech32 string version of the session_id.
                session_uuid:
                  type: string
                  description: >-
                    session_uuid is the uuid hex string of the
                    session_id_session_uuid.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the session_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            contract_spec_id_info:
              description: >-
                contract_spec_id_info contains information about the id/address
                of the contract specification.
              type: object
              properties:
                contract_spec_id:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id is the raw bytes of the contract
                    specification address.
                contract_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_prefix is the prefix portion of the
                    contract_spec_id.
                contract_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_contract_spec_uuid is the
                    contract_spec_uuid portion of the contract_spec_id.
                contract_spec_addr:
                  type: string
                  description: >-
                    contract_spec_addr is the bech32 string version of the
                    contract_spec_id.
                contract_spec_uuid:
                  type: string
                  description: >-
                    contract_spec_uuid is the uuid hex string of the
                    contract_spec_id_contract_spec_uuid.
          description: >-
            SessionWrapper contains a single session and some extra identifiers
            for it.
        description: >-
          sessions is any number of wrapped sessions in this scope (if
          requested).
      records:
        type: array
        items:
          type: object
          properties:
            record:
              description: record is the on-chain record message.
              type: object
              properties:
                name:
                  type: string
                  title: >-
                    name/identifier for this record.  Value must be unique
                    within the scope.  Also known as a Fact name
                session_id:
                  type: string
                  format: byte
                  title: >-
                    id of the session context that was used to create this
                    record (use with filtered kvprefix iterator)
                process:
                  title: >-
                    process contain information used to uniquely identify an
                    execution on or off chain that generated this record
                  type: object
                  properties:
                    address:
                      type: string
                      title: the address of a smart contract used for this process
                    hash:
                      type: string
                      title: the hash of an off-chain process used
                    name:
                      type: string
                      title: >-
                        a name associated with the process (type_name, classname
                        or smart contract common name)
                    method:
                      type: string
                      title: >-
                        method is a name or reference to a specific operation
                        (method) within a class/contract that was invoked
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: >-
                          Name value included to link back to the definition
                          spec.
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                      type_name:
                        type: string
                        title: from proposed fact structure to unmarshal
                      status:
                        title: >-
                          Indicates if this input was a recorded fact on chain
                          or just a given hashed input
                        type: string
                        enum:
                          - RECORD_INPUT_STATUS_UNSPECIFIED
                          - RECORD_INPUT_STATUS_PROPOSED
                          - RECORD_INPUT_STATUS_RECORD
                        default: RECORD_INPUT_STATUS_UNSPECIFIED
                        description: >-
                          - RECORD_INPUT_STATUS_UNSPECIFIED:
                          RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                          invalid/unknown input type
                           - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                           - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                    title: Tracks the inputs used to establish this record
                  title: >-
                    inputs used with the process to achieve the output on this
                    record
                outputs:
                  type: array
                  items:
                    type: object
                    properties:
                      hash:
                        type: string
                        title: >-
                          Hash of the data output that was output/generated for
                          this record
                      status:
                        title: >-
                          Status of the process execution associated with this
                          output indicating success,failure, or pending
                        type: string
                        enum:
                          - RESULT_STATUS_UNSPECIFIED
                          - RESULT_STATUS_PASS
                          - RESULT_STATUS_SKIP
                          - RESULT_STATUS_FAIL
                        default: RESULT_STATUS_UNSPECIFIED
                        description: >-
                          - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED
                          indicates an unset condition
                           - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                           - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                           - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                    title: >-
                      RecordOutput encapsulates the output of a process recorded
                      on chain
                  title: >-
                    output(s) is the results of executing the process on the
                    given process indicated in this record
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    specification_id is the id of the record specification that
                    was used to create this record.
              title: >-
                A record (of fact) is attached to a session or each
                consideration output from a contract
            record_id_info:
              description: >-
                record_id_info contains information about the id/address of the
                record.
              type: object
              properties:
                record_id:
                  type: string
                  format: byte
                  description: record_id is the raw bytes of the record address.
                record_id_prefix:
                  type: string
                  format: byte
                  description: record_id_prefix is the prefix portion of the record_id.
                record_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_id_scope_uuid is the scope_uuid portion of the
                    record_id.
                record_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_id_hashed_name is the hashed name portion of the
                    record_id.
                record_addr:
                  type: string
                  description: record_addr is the bech32 string version of the record_id.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the record_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordWrapper contains a single record and some extra identifiers
            for it.
        description: records is any number of wrapped records in this scope (if requested).
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          scope_id:
            type: string
            description: >-
              scope_id can either be a uuid, e.g.
              91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address,
              e.g.

              scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          session_addr:
            type: string
            description: >-
              session_addr is a bech32 session address, e.g.

              session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
          record_addr:
            type: string
            description: >-
              record_addr is a bech32 record address, e.g.
              record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          include_sessions:
            type: boolean
            description: >-
              include_sessions is a flag for whether to include the sessions of
              the scope in the response.
          include_records:
            type: boolean
            description: >-
              include_records is a flag for whether to include the records of
              the scope in the response.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: ScopeResponse is the response type for the Query/Scope RPC method.
  provenance.metadata.v1.ScopeSpecIdInfo:
    type: object
    properties:
      scope_spec_id:
        type: string
        format: byte
        description: scope_spec_id is the raw bytes of the scope specification address.
      scope_spec_id_prefix:
        type: string
        format: byte
        description: scope_spec_id_prefix is the prefix portion of the scope_spec_id.
      scope_spec_id_scope_spec_uuid:
        type: string
        format: byte
        description: >-
          scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion of the
          scope_spec_id.
      scope_spec_addr:
        type: string
        description: scope_spec_addr is the bech32 string version of the scope_spec_id.
      scope_spec_uuid:
        type: string
        description: >-
          scope_spec_uuid is the uuid hex string of the
          scope_spec_id_scope_spec_uuid.
    description: ScopeSpecIdInfo contains various info regarding a scope specification id.
  provenance.metadata.v1.ScopeSpecification:
    type: object
    properties:
      specification_id:
        type: string
        format: byte
        title: unique identifier for this specification on chain
      description:
        description: General information about this scope specification.
        type: object
        properties:
          name:
            type: string
            description: A Name for this thing.
          description:
            type: string
            description: A description of this thing.
          website_url:
            type: string
            description: URL to find even more info.
          icon_url:
            type: string
            description: URL of an icon.
      owner_addresses:
        type: array
        items:
          type: string
        description: Addresses of the owners of this scope specification.
      parties_involved:
        type: array
        items:
          type: string
          enum:
            - PARTY_TYPE_UNSPECIFIED
            - PARTY_TYPE_ORIGINATOR
            - PARTY_TYPE_SERVICER
            - PARTY_TYPE_INVESTOR
            - PARTY_TYPE_CUSTODIAN
            - PARTY_TYPE_OWNER
            - PARTY_TYPE_AFFILIATE
            - PARTY_TYPE_OMNIBUS
            - PARTY_TYPE_PROVENANCE
            - PARTY_TYPE_CONTROLLER
            - PARTY_TYPE_VALIDATOR
            - PARTY_TYPE_GENERIC_1
            - PARTY_TYPE_GENERIC_2
            - PARTY_TYPE_GENERIC_3
            - PARTY_TYPE_GENERIC_4
            - PARTY_TYPE_GENERIC_5
            - PARTY_TYPE_GENERIC_6
          default: PARTY_TYPE_UNSPECIFIED
          description: >-
            - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
            condition
             - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
             - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
             - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
             - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
             - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
             - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
             - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
             - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
             - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
             - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
             - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
             - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
          title: PartyType are the different roles parties on a contract may use
        title: >-
          A list of parties that must be present on a scope (and their
          associated roles)
      contract_spec_ids:
        type: array
        items:
          type: string
          format: byte
        description: >-
          A list of contract specification ids allowed for a scope based on this
          specification.
    title: >-
      ScopeSpecification defines the required parties, resources, conditions,
      and consideration outputs for a contract
  provenance.metadata.v1.ScopeSpecificationRequest:
    type: object
    properties:
      specification_id:
        type: string
        description: >-
          specification_id can either be a uuid, e.g.
          dc83ea70-eacd-40fe-9adf-1cf6148bf8a2 or a bech32 scope specification

          address, e.g. scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m.
      include_contract_specs:
        type: boolean
        description: >-
          include_contract_specs is a flag for whether to include the contract
          specifications of the scope specification in

          the response.
      include_record_specs:
        type: boolean
        description: >-
          include_record_specs is a flag for whether to include the record
          specifications of the scope specification in the

          response.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: >-
      ScopeSpecificationRequest is the request type for the
      Query/ScopeSpecification RPC method.
  provenance.metadata.v1.ScopeSpecificationResponse:
    type: object
    properties:
      scope_specification:
        description: scope_specification is the wrapped scope specification.
        type: object
        properties:
          specification:
            description: specification is the on-chain scope specification message.
            type: object
            properties:
              specification_id:
                type: string
                format: byte
                title: unique identifier for this specification on chain
              description:
                description: General information about this scope specification.
                type: object
                properties:
                  name:
                    type: string
                    description: A Name for this thing.
                  description:
                    type: string
                    description: A description of this thing.
                  website_url:
                    type: string
                    description: URL to find even more info.
                  icon_url:
                    type: string
                    description: URL of an icon.
              owner_addresses:
                type: array
                items:
                  type: string
                description: Addresses of the owners of this scope specification.
              parties_involved:
                type: array
                items:
                  type: string
                  enum:
                    - PARTY_TYPE_UNSPECIFIED
                    - PARTY_TYPE_ORIGINATOR
                    - PARTY_TYPE_SERVICER
                    - PARTY_TYPE_INVESTOR
                    - PARTY_TYPE_CUSTODIAN
                    - PARTY_TYPE_OWNER
                    - PARTY_TYPE_AFFILIATE
                    - PARTY_TYPE_OMNIBUS
                    - PARTY_TYPE_PROVENANCE
                    - PARTY_TYPE_CONTROLLER
                    - PARTY_TYPE_VALIDATOR
                    - PARTY_TYPE_GENERIC_1
                    - PARTY_TYPE_GENERIC_2
                    - PARTY_TYPE_GENERIC_3
                    - PARTY_TYPE_GENERIC_4
                    - PARTY_TYPE_GENERIC_5
                    - PARTY_TYPE_GENERIC_6
                  default: PARTY_TYPE_UNSPECIFIED
                  description: >-
                    - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                    condition
                     - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                     - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                     - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                     - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                     - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                     - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                     - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                     - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                     - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                     - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                     - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                  title: >-
                    PartyType are the different roles parties on a contract may
                    use
                title: >-
                  A list of parties that must be present on a scope (and their
                  associated roles)
              contract_spec_ids:
                type: array
                items:
                  type: string
                  format: byte
                description: >-
                  A list of contract specification ids allowed for a scope based
                  on this specification.
            title: >-
              ScopeSpecification defines the required parties, resources,
              conditions, and consideration outputs for a contract
          scope_spec_id_info:
            description: >-
              scope_spec_id_info contains information about the id/address of
              the scope specification.
            type: object
            properties:
              scope_spec_id:
                type: string
                format: byte
                description: >-
                  scope_spec_id is the raw bytes of the scope specification
                  address.
              scope_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  scope_spec_id_prefix is the prefix portion of the
                  scope_spec_id.
              scope_spec_id_scope_spec_uuid:
                type: string
                format: byte
                description: >-
                  scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion
                  of the scope_spec_id.
              scope_spec_addr:
                type: string
                description: >-
                  scope_spec_addr is the bech32 string version of the
                  scope_spec_id.
              scope_spec_uuid:
                type: string
                description: >-
                  scope_spec_uuid is the uuid hex string of the
                  scope_spec_id_scope_spec_uuid.
      contract_specs:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain contract specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                description:
                  title: Description information for this contract specification
                  type: object
                  properties:
                    name:
                      type: string
                      description: A Name for this thing.
                    description:
                      type: string
                      description: A description of this thing.
                    website_url:
                      type: string
                      description: URL to find even more info.
                    icon_url:
                      type: string
                      description: URL of an icon.
                  description: >-
                    Description holds general information that is handy to
                    associate with a structure.
                owner_addresses:
                  type: array
                  items:
                    type: string
                  title: Address of the account that owns this specificaiton
                parties_involved:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: >-
                    a list of party roles that must be fullfilled when signing a
                    transaction for this contract specification
                resource_id:
                  type: string
                  format: byte
                  title: >-
                    the address of a record on chain that represents this
                    contract
                hash:
                  type: string
                  title: the hash of contract binary (off-chain instance)
                class_name:
                  type: string
                  title: name of the class/type of this contract executable
              title: >-
                ContractSpecification defines the required parties, resources,
                conditions, and consideration outputs for a contract
            contract_spec_id_info:
              description: >-
                contract_spec_id_info contains information about the id/address
                of the contract specification.
              type: object
              properties:
                contract_spec_id:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id is the raw bytes of the contract
                    specification address.
                contract_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_prefix is the prefix portion of the
                    contract_spec_id.
                contract_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_contract_spec_uuid is the
                    contract_spec_uuid portion of the contract_spec_id.
                contract_spec_addr:
                  type: string
                  description: >-
                    contract_spec_addr is the bech32 string version of the
                    contract_spec_id.
                contract_spec_uuid:
                  type: string
                  description: >-
                    contract_spec_uuid is the uuid hex string of the
                    contract_spec_id_contract_spec_uuid.
          description: >-
            ContractSpecificationWrapper contains a single contract
            specification and some extra identifiers for it.
        description: >-
          contract_specs is any number of wrapped contract specifications in
          this scope specification (if requested).
      record_specs:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain record specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                name:
                  type: string
                  title: >-
                    Name of Record that will be created when this specification
                    is used
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        title: name for this input
                      type_name:
                        type: string
                        title: a type_name (typically a proto name or class_name)
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                    title: >-
                      InputSpecification defines a name, type_name, and source
                      reference (either on or off chain) to define an input

                      parameter
                  title: >-
                    A set of inputs that must be satisified to apply this
                    RecordSpecification and create a Record
                type_name:
                  type: string
                  title: >-
                    A type name for data associated with this record (typically
                    a class or proto name)
                result_type:
                  title: >-
                    Type of result for this record specification (must be RECORD
                    or RECORD_LIST)
                  type: string
                  enum:
                    - DEFINITION_TYPE_UNSPECIFIED
                    - DEFINITION_TYPE_PROPOSED
                    - DEFINITION_TYPE_RECORD
                    - DEFINITION_TYPE_RECORD_LIST
                  default: DEFINITION_TYPE_UNSPECIFIED
                  description: >-
                    - DEFINITION_TYPE_UNSPECIFIED: DEFINITION_TYPE_UNSPECIFIED
                    indicates an unknown/invalid value
                     - DEFINITION_TYPE_PROPOSED: DEFINITION_TYPE_PROPOSED indicates a proposed value is used here (a record that is not on-chain)
                     - DEFINITION_TYPE_RECORD: DEFINITION_TYPE_RECORD indicates the value must be a reference to a record on chain
                     - DEFINITION_TYPE_RECORD_LIST: DEFINITION_TYPE_RECORD_LIST indicates the value maybe a reference to a collection of values on chain having
                    the same name
                responsible_parties:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: Type of party responsible for this record
              title: >-
                RecordSpecification defines the specification for a Record
                including allowed/required inputs/outputs
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordSpecificationWrapper contains a single record specification
            and some extra identifiers for it.
        description: >-
          record_specs is any number of wrapped record specifications in this
          scope specification (if requested).
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          specification_id:
            type: string
            description: >-
              specification_id can either be a uuid, e.g.
              dc83ea70-eacd-40fe-9adf-1cf6148bf8a2 or a bech32 scope
              specification

              address, e.g. scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m.
          include_contract_specs:
            type: boolean
            description: >-
              include_contract_specs is a flag for whether to include the
              contract specifications of the scope specification in

              the response.
          include_record_specs:
            type: boolean
            description: >-
              include_record_specs is a flag for whether to include the record
              specifications of the scope specification in the

              response.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: >-
      ScopeSpecificationResponse is the response type for the
      Query/ScopeSpecification RPC method.
  provenance.metadata.v1.ScopeSpecificationWrapper:
    type: object
    properties:
      specification:
        description: specification is the on-chain scope specification message.
        type: object
        properties:
          specification_id:
            type: string
            format: byte
            title: unique identifier for this specification on chain
          description:
            description: General information about this scope specification.
            type: object
            properties:
              name:
                type: string
                description: A Name for this thing.
              description:
                type: string
                description: A description of this thing.
              website_url:
                type: string
                description: URL to find even more info.
              icon_url:
                type: string
                description: URL of an icon.
          owner_addresses:
            type: array
            items:
              type: string
            description: Addresses of the owners of this scope specification.
          parties_involved:
            type: array
            items:
              type: string
              enum:
                - PARTY_TYPE_UNSPECIFIED
                - PARTY_TYPE_ORIGINATOR
                - PARTY_TYPE_SERVICER
                - PARTY_TYPE_INVESTOR
                - PARTY_TYPE_CUSTODIAN
                - PARTY_TYPE_OWNER
                - PARTY_TYPE_AFFILIATE
                - PARTY_TYPE_OMNIBUS
                - PARTY_TYPE_PROVENANCE
                - PARTY_TYPE_CONTROLLER
                - PARTY_TYPE_VALIDATOR
                - PARTY_TYPE_GENERIC_1
                - PARTY_TYPE_GENERIC_2
                - PARTY_TYPE_GENERIC_3
                - PARTY_TYPE_GENERIC_4
                - PARTY_TYPE_GENERIC_5
                - PARTY_TYPE_GENERIC_6
              default: PARTY_TYPE_UNSPECIFIED
              description: >-
                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                condition
                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
              title: PartyType are the different roles parties on a contract may use
            title: >-
              A list of parties that must be present on a scope (and their
              associated roles)
          contract_spec_ids:
            type: array
            items:
              type: string
              format: byte
            description: >-
              A list of contract specification ids allowed for a scope based on
              this specification.
        title: >-
          ScopeSpecification defines the required parties, resources,
          conditions, and consideration outputs for a contract
      scope_spec_id_info:
        description: >-
          scope_spec_id_info contains information about the id/address of the
          scope specification.
        type: object
        properties:
          scope_spec_id:
            type: string
            format: byte
            description: scope_spec_id is the raw bytes of the scope specification address.
          scope_spec_id_prefix:
            type: string
            format: byte
            description: scope_spec_id_prefix is the prefix portion of the scope_spec_id.
          scope_spec_id_scope_spec_uuid:
            type: string
            format: byte
            description: >-
              scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion of
              the scope_spec_id.
          scope_spec_addr:
            type: string
            description: scope_spec_addr is the bech32 string version of the scope_spec_id.
          scope_spec_uuid:
            type: string
            description: >-
              scope_spec_uuid is the uuid hex string of the
              scope_spec_id_scope_spec_uuid.
    description: >-
      ScopeSpecificationWrapper contains a single scope specification and some
      extra identifiers for it.
  provenance.metadata.v1.ScopeSpecificationsAllRequest:
    type: object
    properties:
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      ScopeSpecificationsAllRequest is the request type for the
      Query/ScopeSpecificationsAll RPC method.
  provenance.metadata.v1.ScopeSpecificationsAllResponse:
    type: object
    properties:
      scope_specifications:
        type: array
        items:
          type: object
          properties:
            specification:
              description: specification is the on-chain scope specification message.
              type: object
              properties:
                specification_id:
                  type: string
                  format: byte
                  title: unique identifier for this specification on chain
                description:
                  description: General information about this scope specification.
                  type: object
                  properties:
                    name:
                      type: string
                      description: A Name for this thing.
                    description:
                      type: string
                      description: A description of this thing.
                    website_url:
                      type: string
                      description: URL to find even more info.
                    icon_url:
                      type: string
                      description: URL of an icon.
                owner_addresses:
                  type: array
                  items:
                    type: string
                  description: Addresses of the owners of this scope specification.
                parties_involved:
                  type: array
                  items:
                    type: string
                    enum:
                      - PARTY_TYPE_UNSPECIFIED
                      - PARTY_TYPE_ORIGINATOR
                      - PARTY_TYPE_SERVICER
                      - PARTY_TYPE_INVESTOR
                      - PARTY_TYPE_CUSTODIAN
                      - PARTY_TYPE_OWNER
                      - PARTY_TYPE_AFFILIATE
                      - PARTY_TYPE_OMNIBUS
                      - PARTY_TYPE_PROVENANCE
                      - PARTY_TYPE_CONTROLLER
                      - PARTY_TYPE_VALIDATOR
                      - PARTY_TYPE_GENERIC_1
                      - PARTY_TYPE_GENERIC_2
                      - PARTY_TYPE_GENERIC_3
                      - PARTY_TYPE_GENERIC_4
                      - PARTY_TYPE_GENERIC_5
                      - PARTY_TYPE_GENERIC_6
                    default: PARTY_TYPE_UNSPECIFIED
                    description: >-
                      - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                      error condition
                       - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                       - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                       - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                       - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                       - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                       - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                       - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                       - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                       - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                       - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                       - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                       - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    title: >-
                      PartyType are the different roles parties on a contract
                      may use
                  title: >-
                    A list of parties that must be present on a scope (and their
                    associated roles)
                contract_spec_ids:
                  type: array
                  items:
                    type: string
                    format: byte
                  description: >-
                    A list of contract specification ids allowed for a scope
                    based on this specification.
              title: >-
                ScopeSpecification defines the required parties, resources,
                conditions, and consideration outputs for a contract
            scope_spec_id_info:
              description: >-
                scope_spec_id_info contains information about the id/address of
                the scope specification.
              type: object
              properties:
                scope_spec_id:
                  type: string
                  format: byte
                  description: >-
                    scope_spec_id is the raw bytes of the scope specification
                    address.
                scope_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    scope_spec_id_prefix is the prefix portion of the
                    scope_spec_id.
                scope_spec_id_scope_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion
                    of the scope_spec_id.
                scope_spec_addr:
                  type: string
                  description: >-
                    scope_spec_addr is the bech32 string version of the
                    scope_spec_id.
                scope_spec_uuid:
                  type: string
                  description: >-
                    scope_spec_uuid is the uuid hex string of the
                    scope_spec_id_scope_spec_uuid.
          description: >-
            ScopeSpecificationWrapper contains a single scope specification and
            some extra identifiers for it.
        description: scope_specifications are the wrapped scope specifications.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      ScopeSpecificationsAllResponse is the response type for the
      Query/ScopeSpecificationsAll RPC method.
  provenance.metadata.v1.ScopeWrapper:
    type: object
    properties:
      scope:
        description: scope is the on-chain scope message.
        type: object
        properties:
          scope_id:
            type: string
            format: byte
            title: >-
              Unique ID for this scope.  Implements sdk.Address interface for
              use where addresses are required in Cosmos
          specification_id:
            type: string
            format: byte
            title: >-
              the scope specification that contains the specifications for data
              elements allowed within this scope
          owners:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                  title: address of the account (on chain)
                role:
                  title: >-
                    a role for this account within the context of the processes
                    used
                  type: string
                  enum:
                    - PARTY_TYPE_UNSPECIFIED
                    - PARTY_TYPE_ORIGINATOR
                    - PARTY_TYPE_SERVICER
                    - PARTY_TYPE_INVESTOR
                    - PARTY_TYPE_CUSTODIAN
                    - PARTY_TYPE_OWNER
                    - PARTY_TYPE_AFFILIATE
                    - PARTY_TYPE_OMNIBUS
                    - PARTY_TYPE_PROVENANCE
                    - PARTY_TYPE_CONTROLLER
                    - PARTY_TYPE_VALIDATOR
                    - PARTY_TYPE_GENERIC_1
                    - PARTY_TYPE_GENERIC_2
                    - PARTY_TYPE_GENERIC_3
                    - PARTY_TYPE_GENERIC_4
                    - PARTY_TYPE_GENERIC_5
                    - PARTY_TYPE_GENERIC_6
                  default: PARTY_TYPE_UNSPECIFIED
                  description: >-
                    - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                    condition
                     - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                     - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                     - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                     - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                     - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                     - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                     - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                     - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                     - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                     - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                     - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                optional:
                  type: boolean
                  title: whether this party's signature is optional
              title: >-
                A Party is an address with/in a given role associated with a
                contract
            description: >-
              These parties represent top level owners of the records within. 
              These parties must sign any requests that modify

              the data within the scope.  These addresses are in union with
              parties listed on the sessions.
          data_access:
            type: array
            items:
              type: string
            description: >-
              Addresses in this list are authorized to receive off-chain data
              associated with this scope.
          value_owner_address:
            type: string
            description: >-
              The address that controls the value associated with this scope.


              The value owner is actually tracked by the bank module using a
              coin with the denom "nft/<scope_id>".

              The value owner can be changed using WriteScope or anything that
              transfers funds, e.g. MsgSend.


              During WriteScope:
               - If this field is empty, it indicates that there should not be a change to the value owner.
                 I.e. Once a scope has a value owner, it will always have one (until it's deleted).
               - If this field has a value, the existing value owner will be looked up, and
                 - If there's already an existing value owner, they must be a signer,
                   and the coin will be transferred to the new value owner.
                 - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                   If the scope already exists, the owners must be signers (just like changing other fields).
                   If it's a new scope, there's no special signer limitations related to the value owner.
          require_party_rollup:
            type: boolean
            description: >-
              Whether all parties in this scope and its sessions must be present
              in this scope's owners field.

              This also enables use of optional=true scope owners and session
              parties.
      scope_id_info:
        description: scope_id_info contains information about the id/address of the scope.
        type: object
        properties:
          scope_id:
            type: string
            format: byte
            description: scope_id is the raw bytes of the scope address.
          scope_id_prefix:
            type: string
            format: byte
            description: scope_id_prefix is the prefix portion of the scope_id.
          scope_id_scope_uuid:
            type: string
            format: byte
            description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
          scope_addr:
            type: string
            description: scope_addr is the bech32 string version of the scope_id.
          scope_uuid:
            type: string
            description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
      scope_spec_id_info:
        description: >-
          scope_spec_id_info contains information about the id/address of the
          scope specification.
        type: object
        properties:
          scope_spec_id:
            type: string
            format: byte
            description: scope_spec_id is the raw bytes of the scope specification address.
          scope_spec_id_prefix:
            type: string
            format: byte
            description: scope_spec_id_prefix is the prefix portion of the scope_spec_id.
          scope_spec_id_scope_spec_uuid:
            type: string
            format: byte
            description: >-
              scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion of
              the scope_spec_id.
          scope_spec_addr:
            type: string
            description: scope_spec_addr is the bech32 string version of the scope_spec_id.
          scope_spec_uuid:
            type: string
            description: >-
              scope_spec_uuid is the uuid hex string of the
              scope_spec_id_scope_spec_uuid.
    description: SessionWrapper contains a single scope and its uuid.
  provenance.metadata.v1.ScopesAllRequest:
    type: object
    properties:
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: ScopesAllRequest is the request type for the Query/ScopesAll RPC method.
  provenance.metadata.v1.ScopesAllResponse:
    type: object
    properties:
      scopes:
        type: array
        items:
          type: object
          properties:
            scope:
              description: scope is the on-chain scope message.
              type: object
              properties:
                scope_id:
                  type: string
                  format: byte
                  title: >-
                    Unique ID for this scope.  Implements sdk.Address interface
                    for use where addresses are required in Cosmos
                specification_id:
                  type: string
                  format: byte
                  title: >-
                    the scope specification that contains the specifications for
                    data elements allowed within this scope
                owners:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        title: address of the account (on chain)
                      role:
                        title: >-
                          a role for this account within the context of the
                          processes used
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                      optional:
                        type: boolean
                        title: whether this party's signature is optional
                    title: >-
                      A Party is an address with/in a given role associated with
                      a contract
                  description: >-
                    These parties represent top level owners of the records
                    within.  These parties must sign any requests that modify

                    the data within the scope.  These addresses are in union
                    with parties listed on the sessions.
                data_access:
                  type: array
                  items:
                    type: string
                  description: >-
                    Addresses in this list are authorized to receive off-chain
                    data associated with this scope.
                value_owner_address:
                  type: string
                  description: >-
                    The address that controls the value associated with this
                    scope.


                    The value owner is actually tracked by the bank module using
                    a coin with the denom "nft/<scope_id>".

                    The value owner can be changed using WriteScope or anything
                    that transfers funds, e.g. MsgSend.


                    During WriteScope:
                     - If this field is empty, it indicates that there should not be a change to the value owner.
                       I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                     - If this field has a value, the existing value owner will be looked up, and
                       - If there's already an existing value owner, they must be a signer,
                         and the coin will be transferred to the new value owner.
                       - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                         If the scope already exists, the owners must be signers (just like changing other fields).
                         If it's a new scope, there's no special signer limitations related to the value owner.
                require_party_rollup:
                  type: boolean
                  description: >-
                    Whether all parties in this scope and its sessions must be
                    present in this scope's owners field.

                    This also enables use of optional=true scope owners and
                    session parties.
            scope_id_info:
              description: >-
                scope_id_info contains information about the id/address of the
                scope.
              type: object
              properties:
                scope_id:
                  type: string
                  format: byte
                  description: scope_id is the raw bytes of the scope address.
                scope_id_prefix:
                  type: string
                  format: byte
                  description: scope_id_prefix is the prefix portion of the scope_id.
                scope_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    scope_id_scope_uuid is the scope_uuid portion of the
                    scope_id.
                scope_addr:
                  type: string
                  description: scope_addr is the bech32 string version of the scope_id.
                scope_uuid:
                  type: string
                  description: >-
                    scope_uuid is the uuid hex string of the
                    scope_id_scope_uuid.
            scope_spec_id_info:
              description: >-
                scope_spec_id_info contains information about the id/address of
                the scope specification.
              type: object
              properties:
                scope_spec_id:
                  type: string
                  format: byte
                  description: >-
                    scope_spec_id is the raw bytes of the scope specification
                    address.
                scope_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    scope_spec_id_prefix is the prefix portion of the
                    scope_spec_id.
                scope_spec_id_scope_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion
                    of the scope_spec_id.
                scope_spec_addr:
                  type: string
                  description: >-
                    scope_spec_addr is the bech32 string version of the
                    scope_spec_id.
                scope_spec_uuid:
                  type: string
                  description: >-
                    scope_spec_uuid is the uuid hex string of the
                    scope_spec_id_scope_spec_uuid.
          description: SessionWrapper contains a single scope and its uuid.
        description: scopes are the wrapped scopes.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: ScopesAllResponse is the response type for the Query/ScopesAll RPC method.
  provenance.metadata.v1.Session:
    type: object
    properties:
      session_id:
        type: string
        format: byte
      specification_id:
        type: string
        format: byte
        description: >-
          unique id of the contract specification that was used to create this
          session.
      parties:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              title: address of the account (on chain)
            role:
              title: a role for this account within the context of the processes used
              type: string
              enum:
                - PARTY_TYPE_UNSPECIFIED
                - PARTY_TYPE_ORIGINATOR
                - PARTY_TYPE_SERVICER
                - PARTY_TYPE_INVESTOR
                - PARTY_TYPE_CUSTODIAN
                - PARTY_TYPE_OWNER
                - PARTY_TYPE_AFFILIATE
                - PARTY_TYPE_OMNIBUS
                - PARTY_TYPE_PROVENANCE
                - PARTY_TYPE_CONTROLLER
                - PARTY_TYPE_VALIDATOR
                - PARTY_TYPE_GENERIC_1
                - PARTY_TYPE_GENERIC_2
                - PARTY_TYPE_GENERIC_3
                - PARTY_TYPE_GENERIC_4
                - PARTY_TYPE_GENERIC_5
                - PARTY_TYPE_GENERIC_6
              default: PARTY_TYPE_UNSPECIFIED
              description: >-
                - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                condition
                 - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                 - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                 - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                 - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                 - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                 - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                 - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                 - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                 - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                 - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                 - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                 - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
            optional:
              type: boolean
              title: whether this party's signature is optional
          title: >-
            A Party is an address with/in a given role associated with a
            contract
        title: parties is the set of identities that signed this contract
      name:
        type: string
        title: >-
          name to associate with this session execution context, typically
          classname
      context:
        type: string
        format: byte
        description: >-
          context is a field for storing client specific data associated with a
          session.
      audit:
        description: Created by, updated by, timestamps, version number, and related info.
        type: object
        properties:
          created_date:
            type: string
            format: date-time
            title: the date/time when this entry was created
          created_by:
            type: string
            title: the address of the account that created this record
          updated_date:
            type: string
            format: date-time
            title: the date/time when this entry was last updated
          updated_by:
            type: string
            title: the address of the account that modified this record
          version:
            type: integer
            format: int64
            title: an optional version number that is incremented with each update
          message:
            type: string
            title: an optional message associated with the creation/update event
        title: >-
          AuditFields capture information about the last account to make
          modifications and when they were made
    description: >-
      Session defines an execution context against a specific specification
      instance.

      The context will have a specification and set of parties involved.


      NOTE: When there are no more Records within a Scope that reference a
      Session, the Session is removed.
  provenance.metadata.v1.SessionIdInfo:
    type: object
    properties:
      session_id:
        type: string
        format: byte
        description: session_id is the raw bytes of the session address.
      session_id_prefix:
        type: string
        format: byte
        description: session_id_prefix is the prefix portion of the session_id.
      session_id_scope_uuid:
        type: string
        format: byte
        description: session_id_scope_uuid is the scope_uuid portion of the session_id.
      session_id_session_uuid:
        type: string
        format: byte
        description: session_id_session_uuid is the session_uuid portion of the session_id.
      session_addr:
        type: string
        description: session_addr is the bech32 string version of the session_id.
      session_uuid:
        type: string
        description: session_uuid is the uuid hex string of the session_id_session_uuid.
      scope_id_info:
        description: >-
          scope_id_info is information about the scope id referenced in the
          session_id.
        type: object
        properties:
          scope_id:
            type: string
            format: byte
            description: scope_id is the raw bytes of the scope address.
          scope_id_prefix:
            type: string
            format: byte
            description: scope_id_prefix is the prefix portion of the scope_id.
          scope_id_scope_uuid:
            type: string
            format: byte
            description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
          scope_addr:
            type: string
            description: scope_addr is the bech32 string version of the scope_id.
          scope_uuid:
            type: string
            description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
    description: SessionIdInfo contains various info regarding a session id.
  provenance.metadata.v1.SessionWrapper:
    type: object
    properties:
      session:
        description: session is the on-chain session message.
        type: object
        properties:
          session_id:
            type: string
            format: byte
          specification_id:
            type: string
            format: byte
            description: >-
              unique id of the contract specification that was used to create
              this session.
          parties:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                  title: address of the account (on chain)
                role:
                  title: >-
                    a role for this account within the context of the processes
                    used
                  type: string
                  enum:
                    - PARTY_TYPE_UNSPECIFIED
                    - PARTY_TYPE_ORIGINATOR
                    - PARTY_TYPE_SERVICER
                    - PARTY_TYPE_INVESTOR
                    - PARTY_TYPE_CUSTODIAN
                    - PARTY_TYPE_OWNER
                    - PARTY_TYPE_AFFILIATE
                    - PARTY_TYPE_OMNIBUS
                    - PARTY_TYPE_PROVENANCE
                    - PARTY_TYPE_CONTROLLER
                    - PARTY_TYPE_VALIDATOR
                    - PARTY_TYPE_GENERIC_1
                    - PARTY_TYPE_GENERIC_2
                    - PARTY_TYPE_GENERIC_3
                    - PARTY_TYPE_GENERIC_4
                    - PARTY_TYPE_GENERIC_5
                    - PARTY_TYPE_GENERIC_6
                  default: PARTY_TYPE_UNSPECIFIED
                  description: >-
                    - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an error
                    condition
                     - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                     - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                     - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                     - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                     - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                     - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                     - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                     - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                     - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                     - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                     - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                     - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                optional:
                  type: boolean
                  title: whether this party's signature is optional
              title: >-
                A Party is an address with/in a given role associated with a
                contract
            title: parties is the set of identities that signed this contract
          name:
            type: string
            title: >-
              name to associate with this session execution context, typically
              classname
          context:
            type: string
            format: byte
            description: >-
              context is a field for storing client specific data associated
              with a session.
          audit:
            description: >-
              Created by, updated by, timestamps, version number, and related
              info.
            type: object
            properties:
              created_date:
                type: string
                format: date-time
                title: the date/time when this entry was created
              created_by:
                type: string
                title: the address of the account that created this record
              updated_date:
                type: string
                format: date-time
                title: the date/time when this entry was last updated
              updated_by:
                type: string
                title: the address of the account that modified this record
              version:
                type: integer
                format: int64
                title: >-
                  an optional version number that is incremented with each
                  update
              message:
                type: string
                title: an optional message associated with the creation/update event
            title: >-
              AuditFields capture information about the last account to make
              modifications and when they were made
      session_id_info:
        description: >-
          session_id_info contains information about the id/address of the
          session.
        type: object
        properties:
          session_id:
            type: string
            format: byte
            description: session_id is the raw bytes of the session address.
          session_id_prefix:
            type: string
            format: byte
            description: session_id_prefix is the prefix portion of the session_id.
          session_id_scope_uuid:
            type: string
            format: byte
            description: session_id_scope_uuid is the scope_uuid portion of the session_id.
          session_id_session_uuid:
            type: string
            format: byte
            description: >-
              session_id_session_uuid is the session_uuid portion of the
              session_id.
          session_addr:
            type: string
            description: session_addr is the bech32 string version of the session_id.
          session_uuid:
            type: string
            description: >-
              session_uuid is the uuid hex string of the
              session_id_session_uuid.
          scope_id_info:
            description: >-
              scope_id_info is information about the scope id referenced in the
              session_id.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
      contract_spec_id_info:
        description: >-
          contract_spec_id_info contains information about the id/address of the
          contract specification.
        type: object
        properties:
          contract_spec_id:
            type: string
            format: byte
            description: >-
              contract_spec_id is the raw bytes of the contract specification
              address.
          contract_spec_id_prefix:
            type: string
            format: byte
            description: >-
              contract_spec_id_prefix is the prefix portion of the
              contract_spec_id.
          contract_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              contract_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the contract_spec_id.
          contract_spec_addr:
            type: string
            description: >-
              contract_spec_addr is the bech32 string version of the
              contract_spec_id.
          contract_spec_uuid:
            type: string
            description: >-
              contract_spec_uuid is the uuid hex string of the
              contract_spec_id_contract_spec_uuid.
    description: >-
      SessionWrapper contains a single session and some extra identifiers for
      it.
  provenance.metadata.v1.SessionsAllRequest:
    type: object
    properties:
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      SessionsAllRequest is the request type for the Query/SessionsAll RPC
      method.
  provenance.metadata.v1.SessionsAllResponse:
    type: object
    properties:
      sessions:
        type: array
        items:
          type: object
          properties:
            session:
              description: session is the on-chain session message.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    unique id of the contract specification that was used to
                    create this session.
                parties:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        title: address of the account (on chain)
                      role:
                        title: >-
                          a role for this account within the context of the
                          processes used
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                      optional:
                        type: boolean
                        title: whether this party's signature is optional
                    title: >-
                      A Party is an address with/in a given role associated with
                      a contract
                  title: parties is the set of identities that signed this contract
                name:
                  type: string
                  title: >-
                    name to associate with this session execution context,
                    typically classname
                context:
                  type: string
                  format: byte
                  description: >-
                    context is a field for storing client specific data
                    associated with a session.
                audit:
                  description: >-
                    Created by, updated by, timestamps, version number, and
                    related info.
                  type: object
                  properties:
                    created_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was created
                    created_by:
                      type: string
                      title: the address of the account that created this record
                    updated_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was last updated
                    updated_by:
                      type: string
                      title: the address of the account that modified this record
                    version:
                      type: integer
                      format: int64
                      title: >-
                        an optional version number that is incremented with each
                        update
                    message:
                      type: string
                      title: >-
                        an optional message associated with the creation/update
                        event
                  title: >-
                    AuditFields capture information about the last account to
                    make modifications and when they were made
            session_id_info:
              description: >-
                session_id_info contains information about the id/address of the
                session.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                  description: session_id is the raw bytes of the session address.
                session_id_prefix:
                  type: string
                  format: byte
                  description: session_id_prefix is the prefix portion of the session_id.
                session_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_scope_uuid is the scope_uuid portion of the
                    session_id.
                session_id_session_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_session_uuid is the session_uuid portion of the
                    session_id.
                session_addr:
                  type: string
                  description: session_addr is the bech32 string version of the session_id.
                session_uuid:
                  type: string
                  description: >-
                    session_uuid is the uuid hex string of the
                    session_id_session_uuid.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the session_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            contract_spec_id_info:
              description: >-
                contract_spec_id_info contains information about the id/address
                of the contract specification.
              type: object
              properties:
                contract_spec_id:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id is the raw bytes of the contract
                    specification address.
                contract_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_prefix is the prefix portion of the
                    contract_spec_id.
                contract_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_contract_spec_uuid is the
                    contract_spec_uuid portion of the contract_spec_id.
                contract_spec_addr:
                  type: string
                  description: >-
                    contract_spec_addr is the bech32 string version of the
                    contract_spec_id.
                contract_spec_uuid:
                  type: string
                  description: >-
                    contract_spec_uuid is the uuid hex string of the
                    contract_spec_id_contract_spec_uuid.
          description: >-
            SessionWrapper contains a single session and some extra identifiers
            for it.
        description: sessions are the wrapped sessions.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      SessionsAllResponse is the response type for the Query/SessionsAll RPC
      method.
  provenance.metadata.v1.SessionsRequest:
    type: object
    properties:
      scope_id:
        type: string
        description: >-
          scope_id can either be a uuid, e.g.
          91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address, e.g.

          scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
      session_id:
        type: string
        description: >-
          session_id can either be a uuid, e.g.
          5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address, e.g.

          session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
          This can only be a uuid if a scope_id is also

          provided.
      record_addr:
        type: string
        description: >-
          record_addr is a bech32 record address, e.g.
          record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
      record_name:
        type: string
        description: >-
          record_name is the name of the record to find the session for in the
          provided scope.
      include_scope:
        type: boolean
        description: >-
          include_scope is a flag for whether to include the scope containing
          these sessions in the response.
      include_records:
        type: boolean
        description: >-
          include_records is a flag for whether to include the records of these
          sessions in the response.
      exclude_id_info:
        type: boolean
        description: >-
          exclude_id_info is a flag for whether to exclude the id info from the
          response.
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
    description: SessionsRequest is the request type for the Query/Sessions RPC method.
  provenance.metadata.v1.SessionsResponse:
    type: object
    properties:
      scope:
        description: scope is the wrapped scope that holds these sessions (if requested).
        type: object
        properties:
          scope:
            description: scope is the on-chain scope message.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                title: >-
                  Unique ID for this scope.  Implements sdk.Address interface
                  for use where addresses are required in Cosmos
              specification_id:
                type: string
                format: byte
                title: >-
                  the scope specification that contains the specifications for
                  data elements allowed within this scope
              owners:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      title: address of the account (on chain)
                    role:
                      title: >-
                        a role for this account within the context of the
                        processes used
                      type: string
                      enum:
                        - PARTY_TYPE_UNSPECIFIED
                        - PARTY_TYPE_ORIGINATOR
                        - PARTY_TYPE_SERVICER
                        - PARTY_TYPE_INVESTOR
                        - PARTY_TYPE_CUSTODIAN
                        - PARTY_TYPE_OWNER
                        - PARTY_TYPE_AFFILIATE
                        - PARTY_TYPE_OMNIBUS
                        - PARTY_TYPE_PROVENANCE
                        - PARTY_TYPE_CONTROLLER
                        - PARTY_TYPE_VALIDATOR
                        - PARTY_TYPE_GENERIC_1
                        - PARTY_TYPE_GENERIC_2
                        - PARTY_TYPE_GENERIC_3
                        - PARTY_TYPE_GENERIC_4
                        - PARTY_TYPE_GENERIC_5
                        - PARTY_TYPE_GENERIC_6
                      default: PARTY_TYPE_UNSPECIFIED
                      description: >-
                        - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                        error condition
                         - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                         - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                         - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                         - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                         - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                         - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                         - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                         - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                         - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                         - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                         - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                         - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                    optional:
                      type: boolean
                      title: whether this party's signature is optional
                  title: >-
                    A Party is an address with/in a given role associated with a
                    contract
                description: >-
                  These parties represent top level owners of the records
                  within.  These parties must sign any requests that modify

                  the data within the scope.  These addresses are in union with
                  parties listed on the sessions.
              data_access:
                type: array
                items:
                  type: string
                description: >-
                  Addresses in this list are authorized to receive off-chain
                  data associated with this scope.
              value_owner_address:
                type: string
                description: >-
                  The address that controls the value associated with this
                  scope.


                  The value owner is actually tracked by the bank module using a
                  coin with the denom "nft/<scope_id>".

                  The value owner can be changed using WriteScope or anything
                  that transfers funds, e.g. MsgSend.


                  During WriteScope:
                   - If this field is empty, it indicates that there should not be a change to the value owner.
                     I.e. Once a scope has a value owner, it will always have one (until it's deleted).
                   - If this field has a value, the existing value owner will be looked up, and
                     - If there's already an existing value owner, they must be a signer,
                       and the coin will be transferred to the new value owner.
                     - If there isn't yet a value owner, the coin will be minted and sent to the new value owner.
                       If the scope already exists, the owners must be signers (just like changing other fields).
                       If it's a new scope, there's no special signer limitations related to the value owner.
              require_party_rollup:
                type: boolean
                description: >-
                  Whether all parties in this scope and its sessions must be
                  present in this scope's owners field.

                  This also enables use of optional=true scope owners and
                  session parties.
          scope_id_info:
            description: >-
              scope_id_info contains information about the id/address of the
              scope.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
          scope_spec_id_info:
            description: >-
              scope_spec_id_info contains information about the id/address of
              the scope specification.
            type: object
            properties:
              scope_spec_id:
                type: string
                format: byte
                description: >-
                  scope_spec_id is the raw bytes of the scope specification
                  address.
              scope_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  scope_spec_id_prefix is the prefix portion of the
                  scope_spec_id.
              scope_spec_id_scope_spec_uuid:
                type: string
                format: byte
                description: >-
                  scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion
                  of the scope_spec_id.
              scope_spec_addr:
                type: string
                description: >-
                  scope_spec_addr is the bech32 string version of the
                  scope_spec_id.
              scope_spec_uuid:
                type: string
                description: >-
                  scope_spec_uuid is the uuid hex string of the
                  scope_spec_id_scope_spec_uuid.
      sessions:
        type: array
        items:
          type: object
          properties:
            session:
              description: session is the on-chain session message.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    unique id of the contract specification that was used to
                    create this session.
                parties:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        title: address of the account (on chain)
                      role:
                        title: >-
                          a role for this account within the context of the
                          processes used
                        type: string
                        enum:
                          - PARTY_TYPE_UNSPECIFIED
                          - PARTY_TYPE_ORIGINATOR
                          - PARTY_TYPE_SERVICER
                          - PARTY_TYPE_INVESTOR
                          - PARTY_TYPE_CUSTODIAN
                          - PARTY_TYPE_OWNER
                          - PARTY_TYPE_AFFILIATE
                          - PARTY_TYPE_OMNIBUS
                          - PARTY_TYPE_PROVENANCE
                          - PARTY_TYPE_CONTROLLER
                          - PARTY_TYPE_VALIDATOR
                          - PARTY_TYPE_GENERIC_1
                          - PARTY_TYPE_GENERIC_2
                          - PARTY_TYPE_GENERIC_3
                          - PARTY_TYPE_GENERIC_4
                          - PARTY_TYPE_GENERIC_5
                          - PARTY_TYPE_GENERIC_6
                        default: PARTY_TYPE_UNSPECIFIED
                        description: >-
                          - PARTY_TYPE_UNSPECIFIED: PARTY_TYPE_UNSPECIFIED is an
                          error condition
                           - PARTY_TYPE_ORIGINATOR: PARTY_TYPE_ORIGINATOR is an asset originator
                           - PARTY_TYPE_SERVICER: PARTY_TYPE_SERVICER provides debt servicing functions
                           - PARTY_TYPE_INVESTOR: PARTY_TYPE_INVESTOR is a generic investor
                           - PARTY_TYPE_CUSTODIAN: PARTY_TYPE_CUSTODIAN is an entity that provides custodian services for assets
                           - PARTY_TYPE_OWNER: PARTY_TYPE_OWNER indicates this party is an owner of the item
                           - PARTY_TYPE_AFFILIATE: PARTY_TYPE_AFFILIATE is a party with an affiliate agreement
                           - PARTY_TYPE_OMNIBUS: PARTY_TYPE_OMNIBUS is a special type of party that controls an omnibus bank account
                           - PARTY_TYPE_PROVENANCE: PARTY_TYPE_PROVENANCE is used to indicate this party represents the blockchain or a smart contract action
                           - PARTY_TYPE_CONTROLLER: PARTY_TYPE_CONTROLLER is an entity which controls a specific asset on chain (ie enote)
                           - PARTY_TYPE_VALIDATOR: PARTY_TYPE_VALIDATOR is an entity which validates given assets on chain
                           - PARTY_TYPE_GENERIC_1: PARTY_TYPE_GENERIC_1 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_2: PARTY_TYPE_GENERIC_2 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_3: PARTY_TYPE_GENERIC_3 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_4: PARTY_TYPE_GENERIC_4 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_5: PARTY_TYPE_GENERIC_5 are placeholder types for the user-defined role.
                           - PARTY_TYPE_GENERIC_6: PARTY_TYPE_GENERIC_6 are placeholder types for the user-defined role.
                      optional:
                        type: boolean
                        title: whether this party's signature is optional
                    title: >-
                      A Party is an address with/in a given role associated with
                      a contract
                  title: parties is the set of identities that signed this contract
                name:
                  type: string
                  title: >-
                    name to associate with this session execution context,
                    typically classname
                context:
                  type: string
                  format: byte
                  description: >-
                    context is a field for storing client specific data
                    associated with a session.
                audit:
                  description: >-
                    Created by, updated by, timestamps, version number, and
                    related info.
                  type: object
                  properties:
                    created_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was created
                    created_by:
                      type: string
                      title: the address of the account that created this record
                    updated_date:
                      type: string
                      format: date-time
                      title: the date/time when this entry was last updated
                    updated_by:
                      type: string
                      title: the address of the account that modified this record
                    version:
                      type: integer
                      format: int64
                      title: >-
                        an optional version number that is incremented with each
                        update
                    message:
                      type: string
                      title: >-
                        an optional message associated with the creation/update
                        event
                  title: >-
                    AuditFields capture information about the last account to
                    make modifications and when they were made
            session_id_info:
              description: >-
                session_id_info contains information about the id/address of the
                session.
              type: object
              properties:
                session_id:
                  type: string
                  format: byte
                  description: session_id is the raw bytes of the session address.
                session_id_prefix:
                  type: string
                  format: byte
                  description: session_id_prefix is the prefix portion of the session_id.
                session_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_scope_uuid is the scope_uuid portion of the
                    session_id.
                session_id_session_uuid:
                  type: string
                  format: byte
                  description: >-
                    session_id_session_uuid is the session_uuid portion of the
                    session_id.
                session_addr:
                  type: string
                  description: session_addr is the bech32 string version of the session_id.
                session_uuid:
                  type: string
                  description: >-
                    session_uuid is the uuid hex string of the
                    session_id_session_uuid.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the session_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            contract_spec_id_info:
              description: >-
                contract_spec_id_info contains information about the id/address
                of the contract specification.
              type: object
              properties:
                contract_spec_id:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id is the raw bytes of the contract
                    specification address.
                contract_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_prefix is the prefix portion of the
                    contract_spec_id.
                contract_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    contract_spec_id_contract_spec_uuid is the
                    contract_spec_uuid portion of the contract_spec_id.
                contract_spec_addr:
                  type: string
                  description: >-
                    contract_spec_addr is the bech32 string version of the
                    contract_spec_id.
                contract_spec_uuid:
                  type: string
                  description: >-
                    contract_spec_uuid is the uuid hex string of the
                    contract_spec_id_contract_spec_uuid.
          description: >-
            SessionWrapper contains a single session and some extra identifiers
            for it.
        description: sessions is any number of wrapped session results.
      records:
        type: array
        items:
          type: object
          properties:
            record:
              description: record is the on-chain record message.
              type: object
              properties:
                name:
                  type: string
                  title: >-
                    name/identifier for this record.  Value must be unique
                    within the scope.  Also known as a Fact name
                session_id:
                  type: string
                  format: byte
                  title: >-
                    id of the session context that was used to create this
                    record (use with filtered kvprefix iterator)
                process:
                  title: >-
                    process contain information used to uniquely identify an
                    execution on or off chain that generated this record
                  type: object
                  properties:
                    address:
                      type: string
                      title: the address of a smart contract used for this process
                    hash:
                      type: string
                      title: the hash of an off-chain process used
                    name:
                      type: string
                      title: >-
                        a name associated with the process (type_name, classname
                        or smart contract common name)
                    method:
                      type: string
                      title: >-
                        method is a name or reference to a specific operation
                        (method) within a class/contract that was invoked
                inputs:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                        description: >-
                          Name value included to link back to the definition
                          spec.
                      record_id:
                        type: string
                        format: byte
                        title: >-
                          the address of a record on chain (For Established
                          Records)
                      hash:
                        type: string
                        title: >-
                          the hash of an off-chain piece of information (For
                          Proposed Records)
                      type_name:
                        type: string
                        title: from proposed fact structure to unmarshal
                      status:
                        title: >-
                          Indicates if this input was a recorded fact on chain
                          or just a given hashed input
                        type: string
                        enum:
                          - RECORD_INPUT_STATUS_UNSPECIFIED
                          - RECORD_INPUT_STATUS_PROPOSED
                          - RECORD_INPUT_STATUS_RECORD
                        default: RECORD_INPUT_STATUS_UNSPECIFIED
                        description: >-
                          - RECORD_INPUT_STATUS_UNSPECIFIED:
                          RECORD_INPUT_STATUS_UNSPECIFIED indicates an
                          invalid/unknown input type
                           - RECORD_INPUT_STATUS_PROPOSED: RECORD_INPUT_STATUS_PROPOSED indicates this input was an arbitrary piece of data that was hashed
                           - RECORD_INPUT_STATUS_RECORD: RECORD_INPUT_STATUS_RECORD indicates this input is a reference to a previously recorded fact on blockchain
                    title: Tracks the inputs used to establish this record
                  title: >-
                    inputs used with the process to achieve the output on this
                    record
                outputs:
                  type: array
                  items:
                    type: object
                    properties:
                      hash:
                        type: string
                        title: >-
                          Hash of the data output that was output/generated for
                          this record
                      status:
                        title: >-
                          Status of the process execution associated with this
                          output indicating success,failure, or pending
                        type: string
                        enum:
                          - RESULT_STATUS_UNSPECIFIED
                          - RESULT_STATUS_PASS
                          - RESULT_STATUS_SKIP
                          - RESULT_STATUS_FAIL
                        default: RESULT_STATUS_UNSPECIFIED
                        description: >-
                          - RESULT_STATUS_UNSPECIFIED: RESULT_STATUS_UNSPECIFIED
                          indicates an unset condition
                           - RESULT_STATUS_PASS: RESULT_STATUS_PASS indicates the execution was successful
                           - RESULT_STATUS_SKIP: RESULT_STATUS_SKIP indicates condition/consideration was skipped due to missing inputs or delayed execution
                           - RESULT_STATUS_FAIL: RESULT_STATUS_FAIL indicates the execution of the condition/consideration failed.
                    title: >-
                      RecordOutput encapsulates the output of a process recorded
                      on chain
                  title: >-
                    output(s) is the results of executing the process on the
                    given process indicated in this record
                specification_id:
                  type: string
                  format: byte
                  description: >-
                    specification_id is the id of the record specification that
                    was used to create this record.
              title: >-
                A record (of fact) is attached to a session or each
                consideration output from a contract
            record_id_info:
              description: >-
                record_id_info contains information about the id/address of the
                record.
              type: object
              properties:
                record_id:
                  type: string
                  format: byte
                  description: record_id is the raw bytes of the record address.
                record_id_prefix:
                  type: string
                  format: byte
                  description: record_id_prefix is the prefix portion of the record_id.
                record_id_scope_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_id_scope_uuid is the scope_uuid portion of the
                    record_id.
                record_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_id_hashed_name is the hashed name portion of the
                    record_id.
                record_addr:
                  type: string
                  description: record_addr is the bech32 string version of the record_id.
                scope_id_info:
                  description: >-
                    scope_id_info is information about the scope id referenced
                    in the record_id.
                  type: object
                  properties:
                    scope_id:
                      type: string
                      format: byte
                      description: scope_id is the raw bytes of the scope address.
                    scope_id_prefix:
                      type: string
                      format: byte
                      description: scope_id_prefix is the prefix portion of the scope_id.
                    scope_id_scope_uuid:
                      type: string
                      format: byte
                      description: >-
                        scope_id_scope_uuid is the scope_uuid portion of the
                        scope_id.
                    scope_addr:
                      type: string
                      description: scope_addr is the bech32 string version of the scope_id.
                    scope_uuid:
                      type: string
                      description: >-
                        scope_uuid is the uuid hex string of the
                        scope_id_scope_uuid.
            record_spec_id_info:
              description: >-
                record_spec_id_info contains information about the id/address of
                the record specification.
              type: object
              properties:
                record_spec_id:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id is the raw bytes of the record specification
                    address.
                record_spec_id_prefix:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_prefix is the prefix portion of the
                    record_spec_id.
                record_spec_id_contract_spec_uuid:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_contract_spec_uuid is the contract_spec_uuid
                    portion of the record_spec_id.
                record_spec_id_hashed_name:
                  type: string
                  format: byte
                  description: >-
                    record_spec_id_hashed_name is the hashed name portion of the
                    record_spec_id.
                record_spec_addr:
                  type: string
                  description: >-
                    record_spec_addr is the bech32 string version of the
                    record_spec_id.
                contract_spec_id_info:
                  description: >-
                    contract_spec_id_info is information about the contract spec
                    id referenced in the record_spec_id.
                  type: object
                  properties:
                    contract_spec_id:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id is the raw bytes of the contract
                        specification address.
                    contract_spec_id_prefix:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_prefix is the prefix portion of the
                        contract_spec_id.
                    contract_spec_id_contract_spec_uuid:
                      type: string
                      format: byte
                      description: >-
                        contract_spec_id_contract_spec_uuid is the
                        contract_spec_uuid portion of the contract_spec_id.
                    contract_spec_addr:
                      type: string
                      description: >-
                        contract_spec_addr is the bech32 string version of the
                        contract_spec_id.
                    contract_spec_uuid:
                      type: string
                      description: >-
                        contract_spec_uuid is the uuid hex string of the
                        contract_spec_id_contract_spec_uuid.
          description: >-
            RecordWrapper contains a single record and some extra identifiers
            for it.
        description: >-
          records is any number of wrapped records contained in these sessions
          (if requested).
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          scope_id:
            type: string
            description: >-
              scope_id can either be a uuid, e.g.
              91978ba2-5f35-459a-86a7-feca1b0512e0 or a bech32 scope address,
              e.g.

              scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel.
          session_id:
            type: string
            description: >-
              session_id can either be a uuid, e.g.
              5803f8bc-6067-4eb5-951f-2121671c2ec0 or a bech32 session address,
              e.g.

              session1qxge0zaztu65tx5x5llv5xc9zts9sqlch3sxwn44j50jzgt8rshvqyfrjcr.
              This can only be a uuid if a scope_id is also

              provided.
          record_addr:
            type: string
            description: >-
              record_addr is a bech32 record address, e.g.
              record1q2ge0zaztu65tx5x5llv5xc9ztsw42dq2jdvmdazuwzcaddhh8gmu3mcze3.
          record_name:
            type: string
            description: >-
              record_name is the name of the record to find the session for in
              the provided scope.
          include_scope:
            type: boolean
            description: >-
              include_scope is a flag for whether to include the scope
              containing these sessions in the response.
          include_records:
            type: boolean
            description: >-
              include_records is a flag for whether to include the records of
              these sessions in the response.
          exclude_id_info:
            type: boolean
            description: >-
              exclude_id_info is a flag for whether to exclude the id info from
              the response.
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
    description: SessionsResponse is the response type for the Query/Sessions RPC method.
  provenance.metadata.v1.ValueOwnershipRequest:
    type: object
    properties:
      address:
        type: string
      include_request:
        type: boolean
        description: >-
          include_request is a flag for whether to include this request in your
          result.
      pagination:
        description: pagination defines optional pagination parameters for the request.
        type: object
        properties:
          key:
            type: string
            format: byte
            description: |-
              key is a value returned in PageResponse.next_key to begin
              querying the next page most efficiently. Only one of offset or key
              should be set.
          offset:
            type: string
            format: uint64
            description: >-
              offset is a numeric offset that can be used when key is
              unavailable.

              It is less efficient than using key. Only one of offset or key
              should

              be set.
          limit:
            type: string
            format: uint64
            description: >-
              limit is the total number of results to be returned in the result
              page.

              If left empty it will default to a value to be set by each app.
          count_total:
            type: boolean
            description: >-
              count_total is set to true  to indicate that the result set should
              include

              a count of the total number of items available for pagination in
              UIs.

              count_total is only respected when offset is used. It is ignored
              when key

              is set.
          reverse:
            type: boolean
            description: >-
              reverse is set to true if results are to be returned in the
              descending order.
        title: |-
          PageRequest is to be embedded in gRPC request messages for efficient
          pagination. Ex:
    description: >-
      ValueOwnershipRequest is the request type for the Query/ValueOwnership RPC
      method.
  provenance.metadata.v1.ValueOwnershipResponse:
    type: object
    properties:
      scope_uuids:
        type: array
        items:
          type: string
        description: A list of scope ids (uuid) associated with the given address.
      request:
        description: request is a copy of the request that generated these results.
        type: object
        properties:
          address:
            type: string
          include_request:
            type: boolean
            description: >-
              include_request is a flag for whether to include this request in
              your result.
          pagination:
            description: pagination defines optional pagination parameters for the request.
            type: object
            properties:
              key:
                type: string
                format: byte
                description: >-
                  key is a value returned in PageResponse.next_key to begin

                  querying the next page most efficiently. Only one of offset or
                  key

                  should be set.
              offset:
                type: string
                format: uint64
                description: >-
                  offset is a numeric offset that can be used when key is
                  unavailable.

                  It is less efficient than using key. Only one of offset or key
                  should

                  be set.
              limit:
                type: string
                format: uint64
                description: >-
                  limit is the total number of results to be returned in the
                  result page.

                  If left empty it will default to a value to be set by each
                  app.
              count_total:
                type: boolean
                description: >-
                  count_total is set to true  to indicate that the result set
                  should include

                  a count of the total number of items available for pagination
                  in UIs.

                  count_total is only respected when offset is used. It is
                  ignored when key

                  is set.
              reverse:
                type: boolean
                description: >-
                  reverse is set to true if results are to be returned in the
                  descending order.
            title: >-
              PageRequest is to be embedded in gRPC request messages for
              efficient

              pagination. Ex:
      pagination:
        description: pagination provides the pagination information of this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      ValueOwnershipResponse is the response type for the Query/ValueOwnership
      RPC method.
  provenance.metadata.v1.MsgAddContractSpecToScopeSpecResponse:
    type: object
    description: >-
      MsgAddContractSpecToScopeSpecResponse is the response type for the
      Msg/AddContractSpecToScopeSpec RPC method.
  provenance.metadata.v1.MsgAddNetAssetValuesResponse:
    type: object
    title: >-
      MsgAddNetAssetValuesResponse defines the Msg/AddNetAssetValue response
      type
  provenance.metadata.v1.MsgAddScopeDataAccessResponse:
    type: object
    title: >-
      MsgAddScopeDataAccessResponse is the response for adding data access
      AccAddress to scope
  provenance.metadata.v1.MsgAddScopeOwnerResponse:
    type: object
    title: >-
      MsgAddScopeOwnerResponse is the response for adding owner AccAddresses to
      scope
  provenance.metadata.v1.MsgBindOSLocatorResponse:
    type: object
    properties:
      locator:
        type: object
        properties:
          owner:
            type: string
            title: account address the endpoint is owned by
          locator_uri:
            type: string
            title: locator endpoint uri
          encryption_key:
            type: string
            title: owners encryption key address
        description: >-
          Defines an Locator object stored on chain, which represents a owner(
          blockchain address) associated with a endpoint

          uri for it's associated object store.
    description: >-
      MsgBindOSLocatorResponse is the response type for the Msg/BindOSLocator
      RPC method.
  provenance.metadata.v1.MsgDeleteContractSpecFromScopeSpecResponse:
    type: object
    description: >-
      MsgDeleteContractSpecFromScopeSpecResponse is the response type for the
      Msg/DeleteContractSpecFromScopeSpec RPC

      method.
  provenance.metadata.v1.MsgDeleteContractSpecificationResponse:
    type: object
    description: >-
      MsgDeleteContractSpecificationResponse is the response type for the
      Msg/DeleteContractSpecification RPC method.
  provenance.metadata.v1.MsgDeleteOSLocatorResponse:
    type: object
    properties:
      locator:
        type: object
        properties:
          owner:
            type: string
            title: account address the endpoint is owned by
          locator_uri:
            type: string
            title: locator endpoint uri
          encryption_key:
            type: string
            title: owners encryption key address
        description: >-
          Defines an Locator object stored on chain, which represents a owner(
          blockchain address) associated with a endpoint

          uri for it's associated object store.
    description: >-
      MsgDeleteOSLocatorResponse is the response type for the
      Msg/DeleteOSLocator RPC method.
  provenance.metadata.v1.MsgDeleteRecordResponse:
    type: object
    description: >-
      MsgDeleteRecordResponse is the response type for the Msg/DeleteRecord RPC
      method.
  provenance.metadata.v1.MsgDeleteRecordSpecificationResponse:
    type: object
    description: >-
      MsgDeleteRecordSpecificationResponse is the response type for the
      Msg/DeleteRecordSpecification RPC method.
  provenance.metadata.v1.MsgDeleteScopeDataAccessResponse:
    type: object
    title: >-
      MsgDeleteScopeDataAccessResponse is the response from removing data access
      AccAddress to scope
  provenance.metadata.v1.MsgDeleteScopeOwnerResponse:
    type: object
    title: >-
      MsgDeleteScopeOwnerResponse is the response from removing owner AccAddress
      to scope
  provenance.metadata.v1.MsgDeleteScopeResponse:
    type: object
    description: >-
      MsgDeleteScopeResponse is the response type for the Msg/DeleteScope RPC
      method.
  provenance.metadata.v1.MsgDeleteScopeSpecificationResponse:
    type: object
    description: >-
      MsgDeleteScopeSpecificationResponse is the response type for the
      Msg/DeleteScopeSpecification RPC method.
  provenance.metadata.v1.MsgMigrateValueOwnerResponse:
    type: object
    description: >-
      MsgMigrateValueOwnerResponse is the response from migrating a value owner
      address.
  provenance.metadata.v1.MsgModifyOSLocatorResponse:
    type: object
    properties:
      locator:
        type: object
        properties:
          owner:
            type: string
            title: account address the endpoint is owned by
          locator_uri:
            type: string
            title: locator endpoint uri
          encryption_key:
            type: string
            title: owners encryption key address
        description: >-
          Defines an Locator object stored on chain, which represents a owner(
          blockchain address) associated with a endpoint

          uri for it's associated object store.
    description: >-
      MsgModifyOSLocatorResponse is the response type for the
      Msg/ModifyOSLocator RPC method.
  provenance.metadata.v1.MsgSetAccountDataResponse:
    type: object
    description: >-
      MsgSetAccountDataResponse is the response from setting/updating/deleting a
      scope's account data.
  provenance.metadata.v1.MsgUpdateValueOwnersResponse:
    type: object
    description: >-
      MsgUpdateValueOwnersResponse is the response from updating value owner
      addresses in one or more scopes.
  provenance.metadata.v1.MsgWriteContractSpecificationResponse:
    type: object
    properties:
      contract_spec_id_info:
        description: >-
          contract_spec_id_info contains information about the id/address of the
          contract specification that was added or

          updated.
        type: object
        properties:
          contract_spec_id:
            type: string
            format: byte
            description: >-
              contract_spec_id is the raw bytes of the contract specification
              address.
          contract_spec_id_prefix:
            type: string
            format: byte
            description: >-
              contract_spec_id_prefix is the prefix portion of the
              contract_spec_id.
          contract_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              contract_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the contract_spec_id.
          contract_spec_addr:
            type: string
            description: >-
              contract_spec_addr is the bech32 string version of the
              contract_spec_id.
          contract_spec_uuid:
            type: string
            description: >-
              contract_spec_uuid is the uuid hex string of the
              contract_spec_id_contract_spec_uuid.
    description: >-
      MsgWriteContractSpecificationResponse is the response type for the
      Msg/WriteContractSpecification RPC method.
  provenance.metadata.v1.MsgWriteRecordResponse:
    type: object
    properties:
      record_id_info:
        description: >-
          record_id_info contains information about the id/address of the record
          that was added or updated.
        type: object
        properties:
          record_id:
            type: string
            format: byte
            description: record_id is the raw bytes of the record address.
          record_id_prefix:
            type: string
            format: byte
            description: record_id_prefix is the prefix portion of the record_id.
          record_id_scope_uuid:
            type: string
            format: byte
            description: record_id_scope_uuid is the scope_uuid portion of the record_id.
          record_id_hashed_name:
            type: string
            format: byte
            description: record_id_hashed_name is the hashed name portion of the record_id.
          record_addr:
            type: string
            description: record_addr is the bech32 string version of the record_id.
          scope_id_info:
            description: >-
              scope_id_info is information about the scope id referenced in the
              record_id.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
    description: >-
      MsgWriteRecordResponse is the response type for the Msg/WriteRecord RPC
      method.
  provenance.metadata.v1.MsgWriteRecordSpecificationResponse:
    type: object
    properties:
      record_spec_id_info:
        description: >-
          record_spec_id_info contains information about the id/address of the
          record specification that was added or

          updated.
        type: object
        properties:
          record_spec_id:
            type: string
            format: byte
            description: >-
              record_spec_id is the raw bytes of the record specification
              address.
          record_spec_id_prefix:
            type: string
            format: byte
            description: record_spec_id_prefix is the prefix portion of the record_spec_id.
          record_spec_id_contract_spec_uuid:
            type: string
            format: byte
            description: >-
              record_spec_id_contract_spec_uuid is the contract_spec_uuid
              portion of the record_spec_id.
          record_spec_id_hashed_name:
            type: string
            format: byte
            description: >-
              record_spec_id_hashed_name is the hashed name portion of the
              record_spec_id.
          record_spec_addr:
            type: string
            description: >-
              record_spec_addr is the bech32 string version of the
              record_spec_id.
          contract_spec_id_info:
            description: >-
              contract_spec_id_info is information about the contract spec id
              referenced in the record_spec_id.
            type: object
            properties:
              contract_spec_id:
                type: string
                format: byte
                description: >-
                  contract_spec_id is the raw bytes of the contract
                  specification address.
              contract_spec_id_prefix:
                type: string
                format: byte
                description: >-
                  contract_spec_id_prefix is the prefix portion of the
                  contract_spec_id.
              contract_spec_id_contract_spec_uuid:
                type: string
                format: byte
                description: >-
                  contract_spec_id_contract_spec_uuid is the contract_spec_uuid
                  portion of the contract_spec_id.
              contract_spec_addr:
                type: string
                description: >-
                  contract_spec_addr is the bech32 string version of the
                  contract_spec_id.
              contract_spec_uuid:
                type: string
                description: >-
                  contract_spec_uuid is the uuid hex string of the
                  contract_spec_id_contract_spec_uuid.
    description: >-
      MsgWriteRecordSpecificationResponse is the response type for the
      Msg/WriteRecordSpecification RPC method.
  provenance.metadata.v1.MsgWriteScopeResponse:
    type: object
    properties:
      scope_id_info:
        description: >-
          scope_id_info contains information about the id/address of the scope
          that was added or updated.
        type: object
        properties:
          scope_id:
            type: string
            format: byte
            description: scope_id is the raw bytes of the scope address.
          scope_id_prefix:
            type: string
            format: byte
            description: scope_id_prefix is the prefix portion of the scope_id.
          scope_id_scope_uuid:
            type: string
            format: byte
            description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
          scope_addr:
            type: string
            description: scope_addr is the bech32 string version of the scope_id.
          scope_uuid:
            type: string
            description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
    description: >-
      MsgWriteScopeResponse is the response type for the Msg/WriteScope RPC
      method.
  provenance.metadata.v1.MsgWriteScopeSpecificationResponse:
    type: object
    properties:
      scope_spec_id_info:
        description: >-
          scope_spec_id_info contains information about the id/address of the
          scope specification that was added or updated.
        type: object
        properties:
          scope_spec_id:
            type: string
            format: byte
            description: scope_spec_id is the raw bytes of the scope specification address.
          scope_spec_id_prefix:
            type: string
            format: byte
            description: scope_spec_id_prefix is the prefix portion of the scope_spec_id.
          scope_spec_id_scope_spec_uuid:
            type: string
            format: byte
            description: >-
              scope_spec_id_scope_spec_uuid is the scope_spec_uuid portion of
              the scope_spec_id.
          scope_spec_addr:
            type: string
            description: scope_spec_addr is the bech32 string version of the scope_spec_id.
          scope_spec_uuid:
            type: string
            description: >-
              scope_spec_uuid is the uuid hex string of the
              scope_spec_id_scope_spec_uuid.
    description: >-
      MsgWriteScopeSpecificationResponse is the response type for the
      Msg/WriteScopeSpecification RPC method.
  provenance.metadata.v1.MsgWriteSessionResponse:
    type: object
    properties:
      session_id_info:
        description: >-
          session_id_info contains information about the id/address of the
          session that was added or updated.
        type: object
        properties:
          session_id:
            type: string
            format: byte
            description: session_id is the raw bytes of the session address.
          session_id_prefix:
            type: string
            format: byte
            description: session_id_prefix is the prefix portion of the session_id.
          session_id_scope_uuid:
            type: string
            format: byte
            description: session_id_scope_uuid is the scope_uuid portion of the session_id.
          session_id_session_uuid:
            type: string
            format: byte
            description: >-
              session_id_session_uuid is the session_uuid portion of the
              session_id.
          session_addr:
            type: string
            description: session_addr is the bech32 string version of the session_id.
          session_uuid:
            type: string
            description: >-
              session_uuid is the uuid hex string of the
              session_id_session_uuid.
          scope_id_info:
            description: >-
              scope_id_info is information about the scope id referenced in the
              session_id.
            type: object
            properties:
              scope_id:
                type: string
                format: byte
                description: scope_id is the raw bytes of the scope address.
              scope_id_prefix:
                type: string
                format: byte
                description: scope_id_prefix is the prefix portion of the scope_id.
              scope_id_scope_uuid:
                type: string
                format: byte
                description: scope_id_scope_uuid is the scope_uuid portion of the scope_id.
              scope_addr:
                type: string
                description: scope_addr is the bech32 string version of the scope_id.
              scope_uuid:
                type: string
                description: scope_uuid is the uuid hex string of the scope_id_scope_uuid.
    description: >-
      MsgWriteSessionResponse is the response type for the Msg/WriteSession RPC
      method.
  provenance.metadata.v1.SessionIdComponents:
    type: object
    properties:
      scope_uuid:
        type: string
        title: >-
          scope_uuid is the uuid string for the scope, e.g.
          "91978ba2-5f35-459a-86a7-feca1b0512e0"
      scope_addr:
        type: string
        title: >-
          scope_addr is the bech32 address string for the scope, g.g.
          "scope1qzge0zaztu65tx5x5llv5xc9ztsqxlkwel"
      session_uuid:
        type: string
        title: >-
          session_uuid is a uuid string for identifying this session, e.g.
          "5803f8bc-6067-4eb5-951f-2121671c2ec0"
    description: >-
      SessionIDComponents contains fields for the components that make up a
      session id.
  provenance.msgfees.v1.CalculateTxFeesRequest:
    type: object
    properties:
      tx_bytes:
        type: string
        format: byte
        description: tx_bytes is the transaction to simulate.
      default_base_denom:
        type: string
        description: >-
          default_base_denom used to be the denom used for gas fees.

          Deprecated: This field is now ignored. The fees will be in the
          appropriate denomination(s) automatically.
      gas_adjustment:
        type: number
        format: float
        description: >-
          gas_adjustment is the adjustment factor to be multiplied against the
          estimate gas returned by the tx simulation.

          This only affects the returned gas (since the fee is flat).
    description: >-
      CalculateTxFeesRequest is the request type for the Query RPC method.

      Deprecated: This query is deprecated. It is replaced by the
      CalculateTxFees query in the x/flatfees module.

      This query endpoint will be removed in a future release.
  provenance.msgfees.v1.CalculateTxFeesResponse:
    type: object
    properties:
      additional_fees:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: |-
          additional_fees are the amount of coins to be for addition msg fees
          Deprecated: This field will always be empty
      total_fees:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          total_fees are the total amount of fees needed for the transactions
          (msg fees + gas fee)

          note: the gas fee is calculated with the floor gas price module param.
      estimated_gas:
        type: string
        format: uint64
        title: estimated_gas is the amount of gas needed for the transaction
    description: >-
      CalculateTxFeesResponse is the response type for the Query RPC method.

      Deprecated: This query is deprecated. It is replaced by the
      CalculateTxFees query in the x/flatfees module.

      This query endpoint will be removed in a future release.
  provenance.name.v1.Params:
    type: object
    properties:
      max_segment_length:
        type: integer
        format: int64
        title: maximum length of name segment to allow
      min_segment_length:
        type: integer
        format: int64
        title: minimum length of name segment to allow
      max_name_levels:
        type: integer
        format: int64
        title: >-
          maximum number of name segments to allow.  Example: `foo.bar.baz`
          would be 3
      allow_unrestricted_names:
        type: boolean
        title: determines if unrestricted name keys are allowed or not
    description: Params defines the set of params for the name module.
  provenance.name.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          max_segment_length:
            type: integer
            format: int64
            title: maximum length of name segment to allow
          min_segment_length:
            type: integer
            format: int64
            title: minimum length of name segment to allow
          max_name_levels:
            type: integer
            format: int64
            title: >-
              maximum number of name segments to allow.  Example: `foo.bar.baz`
              would be 3
          allow_unrestricted_names:
            type: boolean
            title: determines if unrestricted name keys are allowed or not
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  provenance.name.v1.QueryResolveResponse:
    type: object
    properties:
      address:
        type: string
        title: a string containing the address the name resolves to
      restricted:
        type: boolean
        description: Whether owner signature is required to add sub-names.
    description: QueryResolveResponse is the response type for the Query/Resolve method.
  provenance.name.v1.QueryReverseLookupResponse:
    type: object
    properties:
      name:
        type: array
        items:
          type: string
        title: an array of names bound against a given address
      pagination:
        description: pagination defines an optional pagination for the request.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryReverseLookupResponse is the response type for the Query/Resolve
      method.
  provenance.name.v1.MsgBindNameResponse:
    type: object
    description: MsgBindNameResponse defines the Msg/BindName response type.
  provenance.name.v1.MsgCreateRootNameResponse:
    type: object
    description: MsgCreateRootNameResponse defines Msg/CreateRootName response type.
  provenance.name.v1.MsgDeleteNameResponse:
    type: object
    description: MsgDeleteNameResponse defines the Msg/DeleteName response type.
  provenance.name.v1.MsgModifyNameResponse:
    type: object
    description: MsgModifyNameResponse defines the Msg/ModifyName response type.
  provenance.name.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is a response message for the UpdateParams
      endpoint.
  provenance.name.v1.NameRecord:
    type: object
    properties:
      name:
        type: string
        title: the bound name
      address:
        type: string
        title: the address the name resolved to
      restricted:
        type: boolean
        title: whether owner signature is required to add sub-names
    title: >-
      NameRecord is a structure used to bind ownership of a name hierarchy to a
      collection of addresses
  cosmos.sanction.v1beta1.Params:
    type: object
    properties:
      immediate_sanction_min_deposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          immediate_sanction_min_deposit is the minimum deposit for a sanction
          to happen immediately.

          If this is zero, immediate sanctioning is not available.

          Otherwise, if a sanction governance proposal is issued with a deposit
          at least this large, a temporary sanction

          will be immediately issued that will expire when voting ends on the
          governance proposal.
      immediate_unsanction_min_deposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          immediate_unsanction_min_deposit is the minimum deposit for an
          unsanction to happen immediately.

          If this is zero, immediate unsanctioning is not available.

          Otherwise, if an unsanction governance proposal is issued with a
          deposit at least this large, a temporary

          unsanction will be immediately issued that will expire when voting
          ends on the governance proposal.
    description: Params defines the configurable parameters of the sanction module.
  cosmos.sanction.v1beta1.QueryIsSanctionedResponse:
    type: object
    properties:
      is_sanctioned:
        type: boolean
        description: is_sanctioned is true if the address is sanctioned.
    description: >-
      QueryIsSanctionedResponse defines the RPC response of an IsSanctioned
      query.
  cosmos.sanction.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params are the sanction module parameters.
        type: object
        properties:
          immediate_sanction_min_deposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              immediate_sanction_min_deposit is the minimum deposit for a
              sanction to happen immediately.

              If this is zero, immediate sanctioning is not available.

              Otherwise, if a sanction governance proposal is issued with a
              deposit at least this large, a temporary sanction

              will be immediately issued that will expire when voting ends on
              the governance proposal.
          immediate_unsanction_min_deposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              immediate_unsanction_min_deposit is the minimum deposit for an
              unsanction to happen immediately.

              If this is zero, immediate unsanctioning is not available.

              Otherwise, if an unsanction governance proposal is issued with a
              deposit at least this large, a temporary

              unsanction will be immediately issued that will expire when voting
              ends on the governance proposal.
    description: QueryParamsResponse defines the RPC response of a Params query.
  cosmos.sanction.v1beta1.QuerySanctionedAddressesResponse:
    type: object
    properties:
      addresses:
        type: array
        items:
          type: string
        description: addresses is the list of sanctioned account addresses.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QuerySanctionedAddressesResponse defines the RPC response of a
      SanctionedAddresses query.
  cosmos.sanction.v1beta1.QueryTemporaryEntriesResponse:
    type: object
    properties:
      entries:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address is the address of this temporary entry.
            proposal_id:
              type: string
              format: uint64
              description: >-
                proposal_id is the governance proposal id associated with this
                temporary entry.
            status:
              description: status is whether the entry is a sanction or unsanction.
              type: string
              enum:
                - TEMP_STATUS_UNSPECIFIED
                - TEMP_STATUS_SANCTIONED
                - TEMP_STATUS_UNSANCTIONED
              default: TEMP_STATUS_UNSPECIFIED
          description: >-
            TemporaryEntry defines the information involved in a temporary
            sanction or unsanction.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryTemporaryEntriesResponse defines the RPC response of a
      TemporaryEntries query.
  cosmos.sanction.v1beta1.TempStatus:
    type: string
    enum:
      - TEMP_STATUS_UNSPECIFIED
      - TEMP_STATUS_SANCTIONED
      - TEMP_STATUS_UNSANCTIONED
    default: TEMP_STATUS_UNSPECIFIED
    description: |-
      TempStatus is whether a temporary entry is a sanction or unsanction.

       - TEMP_STATUS_UNSPECIFIED: TEMP_STATUS_UNSPECIFIED represents and unspecified status value.
       - TEMP_STATUS_SANCTIONED: TEMP_STATUS_SANCTIONED indicates a sanction is in place.
       - TEMP_STATUS_UNSANCTIONED: TEMP_STATUS_UNSANCTIONED indicates an unsanctioned is in place.
  cosmos.sanction.v1beta1.TemporaryEntry:
    type: object
    properties:
      address:
        type: string
        description: address is the address of this temporary entry.
      proposal_id:
        type: string
        format: uint64
        description: >-
          proposal_id is the governance proposal id associated with this
          temporary entry.
      status:
        description: status is whether the entry is a sanction or unsanction.
        type: string
        enum:
          - TEMP_STATUS_UNSPECIFIED
          - TEMP_STATUS_SANCTIONED
          - TEMP_STATUS_UNSANCTIONED
        default: TEMP_STATUS_UNSPECIFIED
    description: >-
      TemporaryEntry defines the information involved in a temporary sanction or
      unsanction.
  cosmos.sanction.v1beta1.MsgSanctionResponse:
    type: object
    description: MsgOptInResponse defines the Msg/Sanction response type.
  cosmos.sanction.v1beta1.MsgUnsanctionResponse:
    type: object
    description: MsgOptInResponse defines the Msg/Unsanction response type.
  cosmos.sanction.v1beta1.MsgUpdateParamsResponse:
    type: object
    description: MsgUpdateParamsResponse defined the Msg/UpdateParams response type.
  provenance.registry.v1.QueryGetRegistriesResponse:
    type: object
    properties:
      registries:
        type: array
        items:
          type: object
          properties:
            key:
              description: |-
                key ties the registry entry to an asset class and NFT id.
                This provides the unique identifier for the registry entry.
              type: object
              properties:
                nft_id:
                  type: string
                  description: >-
                    Identifier for the nft that this ledger is linked to.

                    This could be a `x/metadata` scope id or an `x/nft` nft id.

                    In order to create a ledger for an NFT, the NFT class must
                    be registered in the ledger module as a LedgerClass.
                asset_class_id:
                  type: string
                  description: >-
                    asset_class_id is the Scope Specification ID or NFT Class
                    ID.

                    This identifies the class or specification that the NFT
                    belongs to.

                    If it is a Scope Specification, the address is used,
                    e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

                    Otherwise, it is the NFT Class ID, e.g. "heloc-loan-class".
            roles:
              type: array
              items:
                type: object
                properties:
                  role:
                    description: role is the type of role being assigned.
                    type: string
                    enum:
                      - REGISTRY_ROLE_UNSPECIFIED
                      - REGISTRY_ROLE_SERVICER
                      - REGISTRY_ROLE_SUBSERVICER
                      - REGISTRY_ROLE_CONTROLLER
                      - REGISTRY_ROLE_CUSTODIAN
                      - REGISTRY_ROLE_BORROWER
                      - REGISTRY_ROLE_ORIGINATOR
                    default: REGISTRY_ROLE_UNSPECIFIED
                  addresses:
                    type: array
                    items:
                      type: string
                    description: >-
                      addresses is the list of blockchain addresses that can
                      perform this role.

                      These addresses have the permissions associated with the
                      specified role.
                description: >-
                  RolesEntry represents a mapping between a role and the
                  addresses that can perform that role.

                  This allows multiple addresses to be assigned the same role
                  for a given registry entry.
              description: >-
                roles is a list of roles and addresses that can perform that
                role.

                Each role entry contains a role type and the addresses
                authorized for that role.
          description: >-
            RegistryEntry represents a single entry in the registry, mapping a
            blockchain address to its roles.

            Each entry contains the key information and the roles assigned to
            various addresses.
        description: >-
          registries is the collection of registry entries.

          This includes all roles and addresses associated with the specified
          NFT and asset class.
      pagination:
        description: pagination is the pagination details for this response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGetRegistriesResponse is the paginated response type for the
      Query/GetRegistries RPC method.

      It contains the complete registry entry for the requested key.
  provenance.registry.v1.QueryGetRegistryResponse:
    type: object
    properties:
      registry:
        description: >-
          registry is the registry entry for the requested key.

          This includes all roles and addresses associated with the specified
          NFT and asset class.
        type: object
        properties:
          key:
            description: |-
              key ties the registry entry to an asset class and NFT id.
              This provides the unique identifier for the registry entry.
            type: object
            properties:
              nft_id:
                type: string
                description: >-
                  Identifier for the nft that this ledger is linked to.

                  This could be a `x/metadata` scope id or an `x/nft` nft id.

                  In order to create a ledger for an NFT, the NFT class must be
                  registered in the ledger module as a LedgerClass.
              asset_class_id:
                type: string
                description: >-
                  asset_class_id is the Scope Specification ID or NFT Class ID.

                  This identifies the class or specification that the NFT
                  belongs to.

                  If it is a Scope Specification, the address is used,
                  e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

                  Otherwise, it is the NFT Class ID, e.g. "heloc-loan-class".
          roles:
            type: array
            items:
              type: object
              properties:
                role:
                  description: role is the type of role being assigned.
                  type: string
                  enum:
                    - REGISTRY_ROLE_UNSPECIFIED
                    - REGISTRY_ROLE_SERVICER
                    - REGISTRY_ROLE_SUBSERVICER
                    - REGISTRY_ROLE_CONTROLLER
                    - REGISTRY_ROLE_CUSTODIAN
                    - REGISTRY_ROLE_BORROWER
                    - REGISTRY_ROLE_ORIGINATOR
                  default: REGISTRY_ROLE_UNSPECIFIED
                addresses:
                  type: array
                  items:
                    type: string
                  description: >-
                    addresses is the list of blockchain addresses that can
                    perform this role.

                    These addresses have the permissions associated with the
                    specified role.
              description: >-
                RolesEntry represents a mapping between a role and the addresses
                that can perform that role.

                This allows multiple addresses to be assigned the same role for
                a given registry entry.
            description: >-
              roles is a list of roles and addresses that can perform that role.

              Each role entry contains a role type and the addresses authorized
              for that role.
    description: >-
      QueryGetRegistryResponse is the response type for the Query/GetRegistry
      RPC method.

      It contains the complete registry entry for the requested key.
  provenance.registry.v1.QueryHasRoleResponse:
    type: object
    properties:
      has_role:
        type: boolean
        description: >-
          has_role is true if the address has the specified role for the given
          key.

          This boolean value indicates whether the role verification was
          successful.
    description: >-
      QueryHasRoleResponse is the response type for the Query/HasRole RPC
      method.

      It provides a boolean result indicating whether the address has the
      specified role.
  provenance.registry.v1.RegistryEntry:
    type: object
    properties:
      key:
        description: |-
          key ties the registry entry to an asset class and NFT id.
          This provides the unique identifier for the registry entry.
        type: object
        properties:
          nft_id:
            type: string
            description: >-
              Identifier for the nft that this ledger is linked to.

              This could be a `x/metadata` scope id or an `x/nft` nft id.

              In order to create a ledger for an NFT, the NFT class must be
              registered in the ledger module as a LedgerClass.
          asset_class_id:
            type: string
            description: >-
              asset_class_id is the Scope Specification ID or NFT Class ID.

              This identifies the class or specification that the NFT belongs
              to.

              If it is a Scope Specification, the address is used,
              e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

              Otherwise, it is the NFT Class ID, e.g. "heloc-loan-class".
      roles:
        type: array
        items:
          type: object
          properties:
            role:
              description: role is the type of role being assigned.
              type: string
              enum:
                - REGISTRY_ROLE_UNSPECIFIED
                - REGISTRY_ROLE_SERVICER
                - REGISTRY_ROLE_SUBSERVICER
                - REGISTRY_ROLE_CONTROLLER
                - REGISTRY_ROLE_CUSTODIAN
                - REGISTRY_ROLE_BORROWER
                - REGISTRY_ROLE_ORIGINATOR
              default: REGISTRY_ROLE_UNSPECIFIED
            addresses:
              type: array
              items:
                type: string
              description: >-
                addresses is the list of blockchain addresses that can perform
                this role.

                These addresses have the permissions associated with the
                specified role.
          description: >-
            RolesEntry represents a mapping between a role and the addresses
            that can perform that role.

            This allows multiple addresses to be assigned the same role for a
            given registry entry.
        description: >-
          roles is a list of roles and addresses that can perform that role.

          Each role entry contains a role type and the addresses authorized for
          that role.
    description: >-
      RegistryEntry represents a single entry in the registry, mapping a
      blockchain address to its roles.

      Each entry contains the key information and the roles assigned to various
      addresses.
  provenance.registry.v1.RegistryKey:
    type: object
    properties:
      nft_id:
        type: string
        description: >-
          Identifier for the nft that this ledger is linked to.

          This could be a `x/metadata` scope id or an `x/nft` nft id.

          In order to create a ledger for an NFT, the NFT class must be
          registered in the ledger module as a LedgerClass.
      asset_class_id:
        type: string
        description: >-
          asset_class_id is the Scope Specification ID or NFT Class ID.

          This identifies the class or specification that the NFT belongs to.

          If it is a Scope Specification, the address is used,
          e.g."scopespec1qnwg86nsatx5pl56muw0v9ytlz3qu3jx6m".

          Otherwise, it is the NFT Class ID, e.g. "heloc-loan-class".
    description: >-
      RegistryKey represents a unique identifier for registry entries.

      It links registry entries to specific NFT assets and their associated
      asset classes.
  provenance.registry.v1.RegistryRole:
    type: string
    enum:
      - REGISTRY_ROLE_UNSPECIFIED
      - REGISTRY_ROLE_SERVICER
      - REGISTRY_ROLE_SUBSERVICER
      - REGISTRY_ROLE_CONTROLLER
      - REGISTRY_ROLE_CUSTODIAN
      - REGISTRY_ROLE_BORROWER
      - REGISTRY_ROLE_ORIGINATOR
    default: REGISTRY_ROLE_UNSPECIFIED
    description: >-
      RegistryRole defines the different types of roles that can be assigned to
      addresses.

      These roles determine the permissions and capabilities that an address has
      within the registry system.

       - REGISTRY_ROLE_UNSPECIFIED: REGISTRY_ROLE_UNSPECIFIED indicates no role is assigned.
      This is the default state for addresses that have not been granted any
      specific permissions.
       - REGISTRY_ROLE_SERVICER: REGISTRY_ROLE_SERVICER indicates the address has servicer privileges.
      Servicers are responsible for maintaining and servicing the underlying
      assets.
       - REGISTRY_ROLE_SUBSERVICER: REGISTRY_ROLE_SUBSERVICER indicates the address has subservicer privileges.
      Subservicers assist servicers in their duties and may have limited
      administrative capabilities.
       - REGISTRY_ROLE_CONTROLLER: REGISTRY_ROLE_CONTROLLER indicates the address has controller privileges.
      Controllers have administrative control over the registry entries and can
      modify roles.
       - REGISTRY_ROLE_CUSTODIAN: REGISTRY_ROLE_CUSTODIAN indicates the address has custodian privileges.
      Custodians are responsible for holding and safeguarding the underlying
      assets.
       - REGISTRY_ROLE_BORROWER: REGISTRY_ROLE_BORROWER indicates the address has borrower privileges.
      Borrowers can borrow against the underlying assets within defined
      parameters.
       - REGISTRY_ROLE_ORIGINATOR: REGISTRY_ROLE_ORIGINATOR indicates the address has originator privileges.
      Originators are responsible for creating and originating the underlying
      assets.
  provenance.registry.v1.RolesEntry:
    type: object
    properties:
      role:
        description: role is the type of role being assigned.
        type: string
        enum:
          - REGISTRY_ROLE_UNSPECIFIED
          - REGISTRY_ROLE_SERVICER
          - REGISTRY_ROLE_SUBSERVICER
          - REGISTRY_ROLE_CONTROLLER
          - REGISTRY_ROLE_CUSTODIAN
          - REGISTRY_ROLE_BORROWER
          - REGISTRY_ROLE_ORIGINATOR
        default: REGISTRY_ROLE_UNSPECIFIED
      addresses:
        type: array
        items:
          type: string
        description: >-
          addresses is the list of blockchain addresses that can perform this
          role.

          These addresses have the permissions associated with the specified
          role.
    description: >-
      RolesEntry represents a mapping between a role and the addresses that can
      perform that role.

      This allows multiple addresses to be assigned the same role for a given
      registry entry.
  provenance.registry.v1.MsgGrantRoleResponse:
    type: object
    description: |-
      MsgGrantRoleResponse defines the response for GrantRole.
      This is an empty response indicating successful role grant.
  provenance.registry.v1.MsgRegisterNFTResponse:
    type: object
    description: |-
      MsgRegisterNFTResponse defines the response for RegisterNFT.
      This is an empty response indicating successful registration.
  provenance.registry.v1.MsgRegistryBulkUpdateResponse:
    type: object
    description: |-
      MsgRegistryBulkUpdateResponse defines the response for RegistryBulkUpdate.
      This is an empty response indicating successful bulk update.
  provenance.registry.v1.MsgRevokeRoleResponse:
    type: object
    description: |-
      MsgRevokeRoleResponse defines the response for RevokeRole.
      This is an empty response indicating successful role revocation.
  provenance.registry.v1.MsgUnregisterNFTResponse:
    type: object
    description: |-
      MsgUnregisterNFTResponse defines the response for UnregisterNFT.
      This is an empty response indicating successful unregistration.
  provenance.trigger.v1.QueryTriggerByIDResponse:
    type: object
    properties:
      trigger:
        description: The trigger object that was queried for.
        type: object
        properties:
          id:
            type: string
            format: uint64
            description: An integer to uniquely identify the trigger.
          owner:
            type: string
            description: The owner of the trigger.
          event:
            description: The event that must be detected for the trigger to fire.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          actions:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := ptypes.MarshalAny(foo)
                     ...
                     foo := &pb.Foo{}
                     if err := ptypes.UnmarshalAny(any, foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".



                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            description: The messages to run when the trigger fires.
        title: Trigger
    description: QueryTriggerByIDResponse contains the requested Trigger.
  provenance.trigger.v1.QueryTriggersResponse:
    type: object
    properties:
      triggers:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
              description: An integer to uniquely identify the trigger.
            owner:
              type: string
              description: The owner of the trigger.
            event:
              description: The event that must be detected for the trigger to fire.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            actions:
              type: array
              items:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := ptypes.MarshalAny(foo)
                       ...
                       foo := &pb.Foo{}
                       if err := ptypes.UnmarshalAny(any, foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".



                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
              description: The messages to run when the trigger fires.
          title: Trigger
        description: List of Trigger objects.
      pagination:
        description: pagination defines an optional pagination for the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryTriggersResponse contains the list of Triggers.
  provenance.trigger.v1.Trigger:
    type: object
    properties:
      id:
        type: string
        format: uint64
        description: An integer to uniquely identify the trigger.
      owner:
        type: string
        description: The owner of the trigger.
      event:
        description: The event that must be detected for the trigger to fire.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      actions:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := ptypes.MarshalAny(foo)
                 ...
                 foo := &pb.Foo{}
                 if err := ptypes.UnmarshalAny(any, foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".



            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        description: The messages to run when the trigger fires.
    title: Trigger
  provenance.trigger.v1.MsgCreateTriggerResponse:
    type: object
    properties:
      id:
        type: string
        format: uint64
        description: trigger id that is generated on creation.
    title: MsgCreateTriggerResponse is the response type for creating a trigger RPC
  provenance.trigger.v1.MsgDestroyTriggerResponse:
    type: object
    title: MsgDestroyTriggerResponse is the response type for creating a trigger RPC
  cosmos.auth.v1beta1.BaseAccount:
    type: object
    properties:
      address:
        type: string
      pub_key:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := ptypes.MarshalAny(foo)
               ...
               foo := &pb.Foo{}
               if err := ptypes.UnmarshalAny(any, foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".



          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
      account_number:
        type: string
        format: uint64
      sequence:
        type: string
        format: uint64
      pubKey:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
      accountNumber:
        type: string
        format: uint64
    description: >-
      BaseAccount defines a base account type. It contains all the necessary
      fields

      for basic account functionality. Any custom account type should extend
      this

      type for additional functionality (e.g. vesting).
  provlabs.vault.v1.AccountBalance:
    type: object
    properties:
      address:
        type: string
        description: address is the account address.
      coins:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: coins is the balance of the account.
    description: AccountBalance represents the coin balance of a single account.
  provlabs.vault.v1.Params:
    type: object
    properties:
      tech_fee_address:
        type: string
        description: >-
          tech_fee_address is the address where all AUM fees are collected and
          which holds

          the authority to update per-vault bips.
      default_aum_fee_bips:
        type: integer
        format: int64
        description: >-
          default_aum_fee_bips is the default fee rate (in basis points) applied
          to new vaults upon creation.
      gov_only_vault_creation:
        type: boolean
        description: >-
          gov_only_vault_creation restricts CreateVault to the governance module
          account.

          When true, a vault can only come into existence through a passed
          governance proposal.

          When false (the default), any account may sign CreateVault directly,
          which is how

          development, docker, and testnet chains are expected to run.
    description: Params defines the set of module parameters.
  provlabs.vault.v1.Payment:
    type: object
    properties:
      source:
        type: string
        description: >-
          source is the account that created the payment and owns the escrowed
          source_amount.
      source_amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          source_amount is the funds the source pays the target. A hold is
          placed on this

          amount in the source account until the payment is accepted, rejected,
          or cancelled.
      target:
        type: string
        description: >-
          target is the account that can accept the payment; for the vault's
          settlement

          workflow it is the vault.
      target_amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: >-
          target_amount is the funds the target pays the source in exchange for
          source_amount.
      external_id:
        type: string
        description: external_id, together with source, uniquely identifies the payment.
    description: >-
      Payment is the vault module's view of a Provenance exchange-module
      payment. It

      mirrors the exchange Payment, exposing only the fields relevant to the
      vault's

      asset settlement workflow.


      It serves two roles. As a query result it reports a pending payment
      targeting the

      vault. In MsgAcceptAssetRequest it carries the terms the asset manager
      reviewed, and

      settlement proceeds only if they still match the stored payment exactly.
  provlabs.vault.v1.PendingSwapOut:
    type: object
    properties:
      owner:
        type: string
        description: owner is the address initiating the swap out.
      vault_address:
        type: string
        description: vault_address is the address of the vault processing the withdrawal.
      shares:
        description: shares are the shares that were escrowed by the user.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      redeem_denom:
        type: string
        description: >-
          redeem_denom is the denomination of the asset to be redeemed. Always
          the

          vault's underlying_asset; retained for wire compatibility.
      failure_count:
        type: integer
        format: int64
        description: >-
          failure_count is the number of consecutive failed processing attempts
          that

          left this request queued. Each failure re-keys the request to a later
          retry

          time so it cannot hold the front of the queue. Reset when expedited.
    description: >-
      PendingSwapOut are swap outs that have not yet been processed and
      completed.
  provlabs.vault.v1.PendingSwapOutWithTimeout:
    type: object
    properties:
      request_id:
        type: string
        format: uint64
        description: request_id is the unique identifier for the pending swap out request.
      pending_swap_out:
        description: pending_swap_out contains the details of the swap out request.
        type: object
        properties:
          owner:
            type: string
            description: owner is the address initiating the swap out.
          vault_address:
            type: string
            description: >-
              vault_address is the address of the vault processing the
              withdrawal.
          shares:
            description: shares are the shares that were escrowed by the user.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          redeem_denom:
            type: string
            description: >-
              redeem_denom is the denomination of the asset to be redeemed.
              Always the

              vault's underlying_asset; retained for wire compatibility.
          failure_count:
            type: integer
            format: int64
            description: >-
              failure_count is the number of consecutive failed processing
              attempts that

              left this request queued. Each failure re-keys the request to a
              later retry

              time so it cannot hold the front of the queue. Reset when
              expedited.
      timeout:
        type: string
        format: date-time
        description: >-
          timeout is the time at which the pending swap out will expire if not
          processed.
    description: PendingSwapOutWithTimeout is a pending swap out with its timeout.
  provlabs.vault.v1.QueryEstimateSwapInResponse:
    type: object
    properties:
      assets:
        description: assets is the estimated amount of shares that would be received.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      height:
        type: string
        format: int64
        description: The block height when the estimate occurred.
      time:
        type: string
        format: date-time
        description: The UTC block time when the estimate occurred.
    description: >-
      QueryEstimateSwapInResponse is the response message for the
      Query/EstimateSwapIn endpoint.
  provlabs.vault.v1.QueryEstimateSwapOutResponse:
    type: object
    properties:
      assets:
        description: >-
          assets is the estimated amount of underlying assets that would be
          received.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      height:
        type: string
        format: int64
        description: The block height when the estimate occurred.
      time:
        type: string
        format: date-time
        description: The UTC block time when the estimate occurred.
    description: >-
      QueryEstimateSwapOutResponse is the response message for the
      Query/EstimateSwapOut endpoint.
  provlabs.vault.v1.QueryNavValueResponse:
    type: object
    properties:
      nav:
        description: nav is the internal NAV entry for the requested vault and denom.
        type: object
        properties:
          denom:
            type: string
            description: denom is the asset denomination this entry prices.
          price:
            description: >-
              price is the total value of `volume` units of the denom,
              denominated in the

              vault's underlying asset. The per-unit value is price divided by
              volume.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          volume:
            type: string
            description: >-
              volume is the number of units of the denom that price covers. It
              must be

              positive; the per-unit value of the denom is price divided by
              volume.
          source:
            type: string
            description: >-
              source identifies the origin of this NAV entry (for example an
              oracle name

              or the settlement path), mirroring the Marker module's NAV source
              attribution.
          updated_block_height:
            type: string
            format: int64
            description: >-
              updated_block_height is the block height at which this entry was
              last updated.
          updated_time:
            type: string
            format: date-time
            description: >-
              updated_time is the block time at which this entry was last
              updated.
    description: >-
      QueryNavValueResponse is the response message for the Query/NavValue
      endpoint.
  provlabs.vault.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the vault module parameters.
        type: object
        properties:
          tech_fee_address:
            type: string
            description: >-
              tech_fee_address is the address where all AUM fees are collected
              and which holds

              the authority to update per-vault bips.
          default_aum_fee_bips:
            type: integer
            format: int64
            description: >-
              default_aum_fee_bips is the default fee rate (in basis points)
              applied to new vaults upon creation.
          gov_only_vault_creation:
            type: boolean
            description: >-
              gov_only_vault_creation restricts CreateVault to the governance
              module account.

              When true, a vault can only come into existence through a passed
              governance proposal.

              When false (the default), any account may sign CreateVault
              directly, which is how

              development, docker, and testnet chains are expected to run.
    description: QueryParamsResponse is the response message for the Query/Params endpoint.
  provlabs.vault.v1.QueryPendingSwapOutsResponse:
    type: object
    properties:
      pending_swap_outs:
        type: array
        items:
          type: object
          properties:
            request_id:
              type: string
              format: uint64
              description: >-
                request_id is the unique identifier for the pending swap out
                request.
            pending_swap_out:
              description: pending_swap_out contains the details of the swap out request.
              type: object
              properties:
                owner:
                  type: string
                  description: owner is the address initiating the swap out.
                vault_address:
                  type: string
                  description: >-
                    vault_address is the address of the vault processing the
                    withdrawal.
                shares:
                  description: shares are the shares that were escrowed by the user.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                redeem_denom:
                  type: string
                  description: >-
                    redeem_denom is the denomination of the asset to be
                    redeemed. Always the

                    vault's underlying_asset; retained for wire compatibility.
                failure_count:
                  type: integer
                  format: int64
                  description: >-
                    failure_count is the number of consecutive failed processing
                    attempts that

                    left this request queued. Each failure re-keys the request
                    to a later retry

                    time so it cannot hold the front of the queue. Reset when
                    expedited.
            timeout:
              type: string
              format: date-time
              description: >-
                timeout is the time at which the pending swap out will expire if
                not processed.
          description: PendingSwapOutWithTimeout is a pending swap out with its timeout.
        description: pending_swap_outs is a list of all pending swap outs.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryPendingSwapOutsResponse is the response message for the
      Query/PendingSwapOuts endpoint.
  provlabs.vault.v1.QueryVaultNavsResponse:
    type: object
    properties:
      navs:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
              description: denom is the asset denomination this entry prices.
            price:
              description: >-
                price is the total value of `volume` units of the denom,
                denominated in the

                vault's underlying asset. The per-unit value is price divided by
                volume.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            volume:
              type: string
              description: >-
                volume is the number of units of the denom that price covers. It
                must be

                positive; the per-unit value of the denom is price divided by
                volume.
            source:
              type: string
              description: >-
                source identifies the origin of this NAV entry (for example an
                oracle name

                or the settlement path), mirroring the Marker module's NAV
                source attribution.
            updated_block_height:
              type: string
              format: int64
              description: >-
                updated_block_height is the block height at which this entry was
                last updated.
            updated_time:
              type: string
              format: date-time
              description: >-
                updated_time is the block time at which this entry was last
                updated.
          description: >-
            VaultNAV is a single internal net asset value entry recording the
            price of one

            asset denom held by a vault. The vault module is the sole source of
            truth for

            these values; the NAV authority maintains them via
            MsgUpdateVaultNAV.
        description: navs is the list of internal NAV entries held by the vault.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryVaultNavsResponse is the response message for the Query/VaultNavs
      endpoint.
  provlabs.vault.v1.QueryVaultPaymentResponse:
    type: object
    properties:
      payment:
        description: payment is the pending exchange-module payment targeting the vault.
        type: object
        properties:
          source:
            type: string
            description: >-
              source is the account that created the payment and owns the
              escrowed source_amount.
          source_amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              source_amount is the funds the source pays the target. A hold is
              placed on this

              amount in the source account until the payment is accepted,
              rejected, or cancelled.
          target:
            type: string
            description: >-
              target is the account that can accept the payment; for the vault's
              settlement

              workflow it is the vault.
          target_amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: >-
              target_amount is the funds the target pays the source in exchange
              for source_amount.
          external_id:
            type: string
            description: >-
              external_id, together with source, uniquely identifies the
              payment.
    description: >-
      QueryVaultPaymentResponse is the response message for the
      Query/VaultPayment endpoint.
  provlabs.vault.v1.QueryVaultPaymentsResponse:
    type: object
    properties:
      payments:
        type: array
        items:
          type: object
          properties:
            source:
              type: string
              description: >-
                source is the account that created the payment and owns the
                escrowed source_amount.
            source_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: >-
                source_amount is the funds the source pays the target. A hold is
                placed on this

                amount in the source account until the payment is accepted,
                rejected, or cancelled.
            target:
              type: string
              description: >-
                target is the account that can accept the payment; for the
                vault's settlement

                workflow it is the vault.
            target_amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: >-
                target_amount is the funds the target pays the source in
                exchange for source_amount.
            external_id:
              type: string
              description: >-
                external_id, together with source, uniquely identifies the
                payment.
          description: >-
            Payment is the vault module's view of a Provenance exchange-module
            payment. It

            mirrors the exchange Payment, exposing only the fields relevant to
            the vault's

            asset settlement workflow.


            It serves two roles. As a query result it reports a pending payment
            targeting the

            vault. In MsgAcceptAssetRequest it carries the terms the asset
            manager reviewed, and

            settlement proceeds only if they still match the stored payment
            exactly.
        description: >-
          payments is the list of pending exchange-module payments targeting the
          vault.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryVaultPaymentsResponse is the response message for the
      Query/VaultPayments endpoint.
  provlabs.vault.v1.QueryVaultPendingSwapOutsResponse:
    type: object
    properties:
      pending_swap_outs:
        type: array
        items:
          type: object
          properties:
            request_id:
              type: string
              format: uint64
              description: >-
                request_id is the unique identifier for the pending swap out
                request.
            pending_swap_out:
              description: pending_swap_out contains the details of the swap out request.
              type: object
              properties:
                owner:
                  type: string
                  description: owner is the address initiating the swap out.
                vault_address:
                  type: string
                  description: >-
                    vault_address is the address of the vault processing the
                    withdrawal.
                shares:
                  description: shares are the shares that were escrowed by the user.
                  type: object
                  properties:
                    denom:
                      type: string
                    amount:
                      type: string
                redeem_denom:
                  type: string
                  description: >-
                    redeem_denom is the denomination of the asset to be
                    redeemed. Always the

                    vault's underlying_asset; retained for wire compatibility.
                failure_count:
                  type: integer
                  format: int64
                  description: >-
                    failure_count is the number of consecutive failed processing
                    attempts that

                    left this request queued. Each failure re-keys the request
                    to a later retry

                    time so it cannot hold the front of the queue. Reset when
                    expedited.
            timeout:
              type: string
              format: date-time
              description: >-
                timeout is the time at which the pending swap out will expire if
                not processed.
          description: PendingSwapOutWithTimeout is a pending swap out with its timeout.
        description: pending_swap_outs is a list of all pending swap outs.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryVaultPendingSwapOutsResponse is the response message for the
      Query/VaultPendingSwapOuts endpoint.
  provlabs.vault.v1.QueryVaultResponse:
    type: object
    properties:
      vault:
        description: vault is the requested vault.
        type: object
        properties:
          base_account:
            description: >-
              base_account cosmos account information including address and coin
              holdings.
            type: object
            properties:
              address:
                type: string
              pub_key:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := ptypes.MarshalAny(foo)
                       ...
                       foo := &pb.Foo{}
                       if err := ptypes.UnmarshalAny(any, foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".



                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
              account_number:
                type: string
                format: uint64
              sequence:
                type: string
                format: uint64
          total_shares:
            description: >-
              total_shares is the total number of shares that have ever been
              issued by the vault.

              It serves as the canonical supply-of-record for all shares,
              regardless of whether

              they are held locally on Provenance or externally (e.g., bridged).
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          underlying_asset:
            type: string
            description: >-
              underlying_asset is the vault’s single principal collateral AND
              valuation/base unit.

              - Exactly one denom.

              - Total Vault Value (TVV) and NAV-per-share are computed and
              reported in this denom.

              - Interest accrual, fees, and internal accounting are measured in
              this denom.

              - Held assets acquired via settlement must have an internal NAV
              record priced INTO this denom.
          payment_denom:
            type: string
            description: >-
              payment_denom always equals underlying_asset. Vaults are
              single-denom; the

              field is inert and retained only for wire compatibility.


              Deprecated: The mixed-denom vault concept has been flattened into
              a single

              underlying denom. Deletion of this field is deferred to a future
              major release.
          admin:
            type: string
            description: >-
              admin is the address that has administrative privileges over the
              vault.
          current_interest_rate:
            type: string
            description: >-
              current_interest_rate is a decimal string (e.g., "0.9" for 90% and
              "0.9001353" for 90.01353%) representing the actual annual interest
              rate currently being applied.

              This may be adjusted programmatically (e.g., due to lack of
              funds).
          desired_interest_rate:
            type: string
            description: >-
              desired_interest_rate is a decimal string (e.g., "0.9" for 90% and
              "0.9001353" for 90.01353%) representing the target annual interest
              rate that the vault intends to apply.
          min_interest_rate:
            type: string
            description: >-
              min_interest_rate is a decimal string (e.g., "0.9" for 90% and
              "0.9001353" for 90.01353%) representing the lowest annual interest
              rate the admin is allowed to set.

              If unset (empty string), there is no lower limit.
          max_interest_rate:
            type: string
            description: >-
              max_interest_rate is a decimal string (e.g., "0.9" for 90% and
              "0.9001353" for 90.01353%) representing the highest annual
              interest rate the admin is allowed to set.

              If unset (empty string), there is no upper limit.
          period_start:
            type: string
            format: int64
            description: >-
              The start time (in Unix seconds) of the current interest accrual
              period.
          period_timeout:
            type: string
            format: int64
            description: >-
              The expire time (in Unix seconds) of the current interest accrual
              period.
          swap_in_enabled:
            type: boolean
            description: >-
              swap_in_enabled indicates whether users are allowed to deposit
              into the vault.
          swap_out_enabled:
            type: boolean
            description: >-
              swap_out_enabled indicates whether users are allowed to withdraw
              from the vault.
          withdrawal_delay_seconds:
            type: string
            format: uint64
            description: >-
              withdrawal_delay_seconds is the configured time period (in
              seconds) that a withdrawal

              request must wait in the pending queue before being processed.
          paused:
            type: boolean
            description: >-
              paused indicates that all user-facing swap-in and swap-out
              operations are disabled.
          paused_balance:
            description: >-
              paused_balance is the total vault value snapshot taken at the
              moment of pausing.

              This value is used for all NAV calculations while the vault is
              paused to prevent

              apparent devaluation during collateral rebalancing. It is cleared
              upon unpausing.

              Nothing done during the pause moves it, neither a principal
              deposit or withdrawal

              nor a NAV repricing; those all take effect together when the vault
              is unpaused.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          paused_reason:
            type: string
            description: >-
              paused_reason is a human-readable string explaining why the vault
              was paused, particularly for automatic pauses.
          paused_by:
            type: string
            description: >-
              paused_by is the address that initiated the current pause, empty
              for an automatic

              pause and cleared on unpause. MsgRepriceVault consults it so the
              NAV authority

              can only resume a pause it took itself.
          paused_forced:
            type: boolean
            description: >-
              paused_forced reports whether the current pause waived the strict
              reconcile and

              valuation gate, true for a forced MsgPauseVault and every
              automatic pause. A forced

              pause records a tolerated failure, so MsgRepriceVault refuses to
              resume one.
          bridge_address:
            type: string
            description: >-
              bridge_address is the single external address allowed to mint or
              burn shares on behalf

              of this vault (e.g., for bridging to another chain). All mint/burn
              must flow through the

              vault keeper, which enforces that marker supply never exceeds
              total_shares.
          bridge_enabled:
            type: boolean
            description: >-
              bridge_enabled indicates whether the bridge functionality is
              active. If false, the

              bridge_address has no effect and cannot mint or burn.
          asset_manager:
            type: string
            description: >-
              asset_manager is an optional address that, when set, is authorized
              to manage certain

              collateral operations alongside the admin (e.g.,
              pausing/unpausing, depositing/withdrawing

              principal or interest funds). If unset (empty string), only the
              admin may perform those actions.
          fee_period_start:
            type: string
            format: int64
            description: >-
              fee_period_start is the start time (in Unix seconds) of the
              current AUM fee collection period.

              This is a module-managed timestamp kept in sync with the fee
              timeout queue machinery.
          fee_period_timeout:
            type: string
            format: int64
            description: >-
              fee_period_timeout is the end time (in Unix seconds) of the
              current AUM fee collection period.

              This is a module-managed timestamp kept in sync with the fee
              timeout queue machinery.
          outstanding_aum_fee:
            description: >-
              outstanding_aum_fee is the amount of AUM fee that has been
              calculated but not yet collected

              due to insufficient liquidity in the principal marker. This amount
              is always denominated

              in the vault's underlying_asset, must be preserved, and is carried
              into valuation computations.
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
          aum_fee_bips:
            type: integer
            format: int64
            description: >-
              aum_fee_bips is the AUM fee rate (in basis points) for this
              specific vault.

              Units: Basis Points (1 bps = 0.01%).

              Valid Range: 0 to 10000 (0% to 100%).

              A value of 0 disables AUM fee collection for this vault.

              Negative values are not supported (uint32).
          min_swap_in_value:
            type: string
            description: >-
              min_swap_in_value is a string representing the minimum value
              required for a deposit.

              - The value is measured in the underlying_asset.

              - Values must be non-negative (>= 0).

              - If empty ("") or "0", there is no minimum limit.
          min_swap_out_value:
            type: string
            description: >-
              min_swap_out_value is a string representing the minimum value
              required for a withdrawal.

              - The value is measured in the underlying_asset.

              - Outgoing redemptions are converted to this unit before checking.

              - Values must be non-negative (>= 0).

              - If empty ("") or "0", there is no minimum limit.
          max_swap_in_value:
            type: string
            description: >-
              max_swap_in_value is a string representing the maximum value
              allowed for a deposit.

              - The value is measured in the underlying_asset.

              - Values must be positive (> 0).

              - An empty string "" indicates no maximum limit.
          max_swap_out_value:
            type: string
            description: >-
              max_swap_out_value is a string representing the maximum value
              allowed for a withdrawal.

              - The value is measured in the underlying_asset.

              - Outgoing redemptions are converted to this unit before checking.

              - Values must be positive (> 0).

              - An empty string "" indicates no maximum limit.
          nav_authority:
            type: string
            description: >-
              nav_authority is the address authorized to mutate this vault's
              internal NAV

              table via MsgUpdateVaultNAV. If empty, the vault admin is treated
              as the NAV

              authority. It is rotated only via MsgUpdateNAVAuthority.
      principal:
        description: principal is the total amount of principal held in the vault's marker.
        type: object
        properties:
          address:
            type: string
            description: address is the account address.
          coins:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: coins is the balance of the account.
      reserves:
        description: >-
          reserves is the total amount of reserves held in the vault account for
          interest payments.
        type: object
        properties:
          address:
            type: string
            description: address is the account address.
          coins:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: coins is the balance of the account.
      total_vault_value:
        description: >-
          total_vault_value is the estimated total value of the vault in its

          underlying asset. It includes current principal and estimated unpaid

          interest (at query block height), but excludes reserves. The value is
          approximate and may differ

          from the reconciled amount.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: QueryVaultResponse is the response message for the Query/Vault endpoint.
  provlabs.vault.v1.QueryVaultsResponse:
    type: object
    properties:
      vaults:
        type: array
        items:
          type: object
          properties:
            base_account:
              description: >-
                base_account cosmos account information including address and
                coin holdings.
              type: object
              properties:
                address:
                  type: string
                pub_key:
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                  description: >-
                    `Any` contains an arbitrary serialized protocol buffer
                    message along with a

                    URL that describes the type of the serialized message.


                    Protobuf library provides support to pack/unpack Any values
                    in the form

                    of utility functions or additional generated methods of the
                    Any type.


                    Example 1: Pack and unpack a message in C++.

                        Foo foo = ...;
                        Any any;
                        any.PackFrom(foo);
                        ...
                        if (any.UnpackTo(&foo)) {
                          ...
                        }

                    Example 2: Pack and unpack a message in Java.

                        Foo foo = ...;
                        Any any = Any.pack(foo);
                        ...
                        if (any.is(Foo.class)) {
                          foo = any.unpack(Foo.class);
                        }

                     Example 3: Pack and unpack a message in Python.

                        foo = Foo(...)
                        any = Any()
                        any.Pack(foo)
                        ...
                        if any.Is(Foo.DESCRIPTOR):
                          any.Unpack(foo)
                          ...

                     Example 4: Pack and unpack a message in Go

                         foo := &pb.Foo{...}
                         any, err := ptypes.MarshalAny(foo)
                         ...
                         foo := &pb.Foo{}
                         if err := ptypes.UnmarshalAny(any, foo); err != nil {
                           ...
                         }

                    The pack methods provided by protobuf library will by
                    default use

                    'type.googleapis.com/full.type.name' as the type URL and the
                    unpack

                    methods only use the fully qualified type name after the
                    last '/'

                    in the type URL, for example "foo.bar.com/x/y.z" will yield
                    type

                    name "y.z".



                    JSON

                    ====

                    The JSON representation of an `Any` value uses the regular

                    representation of the deserialized, embedded message, with
                    an

                    additional field `@type` which contains the type URL.
                    Example:

                        package google.profile;
                        message Person {
                          string first_name = 1;
                          string last_name = 2;
                        }

                        {
                          "@type": "type.googleapis.com/google.profile.Person",
                          "firstName": <string>,
                          "lastName": <string>
                        }

                    If the embedded message type is well-known and has a custom
                    JSON

                    representation, that representation will be embedded adding
                    a field

                    `value` which holds the custom JSON in addition to the
                    `@type`

                    field. Example (for message [google.protobuf.Duration][]):

                        {
                          "@type": "type.googleapis.com/google.protobuf.Duration",
                          "value": "1.212s"
                        }
                account_number:
                  type: string
                  format: uint64
                sequence:
                  type: string
                  format: uint64
            total_shares:
              description: >-
                total_shares is the total number of shares that have ever been
                issued by the vault.

                It serves as the canonical supply-of-record for all shares,
                regardless of whether

                they are held locally on Provenance or externally (e.g.,
                bridged).
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            underlying_asset:
              type: string
              description: >-
                underlying_asset is the vault’s single principal collateral AND
                valuation/base unit.

                - Exactly one denom.

                - Total Vault Value (TVV) and NAV-per-share are computed and
                reported in this denom.

                - Interest accrual, fees, and internal accounting are measured
                in this denom.

                - Held assets acquired via settlement must have an internal NAV
                record priced INTO this denom.
            payment_denom:
              type: string
              description: >-
                payment_denom always equals underlying_asset. Vaults are
                single-denom; the

                field is inert and retained only for wire compatibility.


                Deprecated: The mixed-denom vault concept has been flattened
                into a single

                underlying denom. Deletion of this field is deferred to a future
                major release.
            admin:
              type: string
              description: >-
                admin is the address that has administrative privileges over the
                vault.
            current_interest_rate:
              type: string
              description: >-
                current_interest_rate is a decimal string (e.g., "0.9" for 90%
                and "0.9001353" for 90.01353%) representing the actual annual
                interest rate currently being applied.

                This may be adjusted programmatically (e.g., due to lack of
                funds).
            desired_interest_rate:
              type: string
              description: >-
                desired_interest_rate is a decimal string (e.g., "0.9" for 90%
                and "0.9001353" for 90.01353%) representing the target annual
                interest rate that the vault intends to apply.
            min_interest_rate:
              type: string
              description: >-
                min_interest_rate is a decimal string (e.g., "0.9" for 90% and
                "0.9001353" for 90.01353%) representing the lowest annual
                interest rate the admin is allowed to set.

                If unset (empty string), there is no lower limit.
            max_interest_rate:
              type: string
              description: >-
                max_interest_rate is a decimal string (e.g., "0.9" for 90% and
                "0.9001353" for 90.01353%) representing the highest annual
                interest rate the admin is allowed to set.

                If unset (empty string), there is no upper limit.
            period_start:
              type: string
              format: int64
              description: >-
                The start time (in Unix seconds) of the current interest accrual
                period.
            period_timeout:
              type: string
              format: int64
              description: >-
                The expire time (in Unix seconds) of the current interest
                accrual period.
            swap_in_enabled:
              type: boolean
              description: >-
                swap_in_enabled indicates whether users are allowed to deposit
                into the vault.
            swap_out_enabled:
              type: boolean
              description: >-
                swap_out_enabled indicates whether users are allowed to withdraw
                from the vault.
            withdrawal_delay_seconds:
              type: string
              format: uint64
              description: >-
                withdrawal_delay_seconds is the configured time period (in
                seconds) that a withdrawal

                request must wait in the pending queue before being processed.
            paused:
              type: boolean
              description: >-
                paused indicates that all user-facing swap-in and swap-out
                operations are disabled.
            paused_balance:
              description: >-
                paused_balance is the total vault value snapshot taken at the
                moment of pausing.

                This value is used for all NAV calculations while the vault is
                paused to prevent

                apparent devaluation during collateral rebalancing. It is
                cleared upon unpausing.

                Nothing done during the pause moves it, neither a principal
                deposit or withdrawal

                nor a NAV repricing; those all take effect together when the
                vault is unpaused.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            paused_reason:
              type: string
              description: >-
                paused_reason is a human-readable string explaining why the
                vault was paused, particularly for automatic pauses.
            paused_by:
              type: string
              description: >-
                paused_by is the address that initiated the current pause, empty
                for an automatic

                pause and cleared on unpause. MsgRepriceVault consults it so the
                NAV authority

                can only resume a pause it took itself.
            paused_forced:
              type: boolean
              description: >-
                paused_forced reports whether the current pause waived the
                strict reconcile and

                valuation gate, true for a forced MsgPauseVault and every
                automatic pause. A forced

                pause records a tolerated failure, so MsgRepriceVault refuses to
                resume one.
            bridge_address:
              type: string
              description: >-
                bridge_address is the single external address allowed to mint or
                burn shares on behalf

                of this vault (e.g., for bridging to another chain). All
                mint/burn must flow through the

                vault keeper, which enforces that marker supply never exceeds
                total_shares.
            bridge_enabled:
              type: boolean
              description: >-
                bridge_enabled indicates whether the bridge functionality is
                active. If false, the

                bridge_address has no effect and cannot mint or burn.
            asset_manager:
              type: string
              description: >-
                asset_manager is an optional address that, when set, is
                authorized to manage certain

                collateral operations alongside the admin (e.g.,
                pausing/unpausing, depositing/withdrawing

                principal or interest funds). If unset (empty string), only the
                admin may perform those actions.
            fee_period_start:
              type: string
              format: int64
              description: >-
                fee_period_start is the start time (in Unix seconds) of the
                current AUM fee collection period.

                This is a module-managed timestamp kept in sync with the fee
                timeout queue machinery.
            fee_period_timeout:
              type: string
              format: int64
              description: >-
                fee_period_timeout is the end time (in Unix seconds) of the
                current AUM fee collection period.

                This is a module-managed timestamp kept in sync with the fee
                timeout queue machinery.
            outstanding_aum_fee:
              description: >-
                outstanding_aum_fee is the amount of AUM fee that has been
                calculated but not yet collected

                due to insufficient liquidity in the principal marker. This
                amount is always denominated

                in the vault's underlying_asset, must be preserved, and is
                carried into valuation computations.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
            aum_fee_bips:
              type: integer
              format: int64
              description: >-
                aum_fee_bips is the AUM fee rate (in basis points) for this
                specific vault.

                Units: Basis Points (1 bps = 0.01%).

                Valid Range: 0 to 10000 (0% to 100%).

                A value of 0 disables AUM fee collection for this vault.

                Negative values are not supported (uint32).
            min_swap_in_value:
              type: string
              description: >-
                min_swap_in_value is a string representing the minimum value
                required for a deposit.

                - The value is measured in the underlying_asset.

                - Values must be non-negative (>= 0).

                - If empty ("") or "0", there is no minimum limit.
            min_swap_out_value:
              type: string
              description: >-
                min_swap_out_value is a string representing the minimum value
                required for a withdrawal.

                - The value is measured in the underlying_asset.

                - Outgoing redemptions are converted to this unit before
                checking.

                - Values must be non-negative (>= 0).

                - If empty ("") or "0", there is no minimum limit.
            max_swap_in_value:
              type: string
              description: >-
                max_swap_in_value is a string representing the maximum value
                allowed for a deposit.

                - The value is measured in the underlying_asset.

                - Values must be positive (> 0).

                - An empty string "" indicates no maximum limit.
            max_swap_out_value:
              type: string
              description: >-
                max_swap_out_value is a string representing the maximum value
                allowed for a withdrawal.

                - The value is measured in the underlying_asset.

                - Outgoing redemptions are converted to this unit before
                checking.

                - Values must be positive (> 0).

                - An empty string "" indicates no maximum limit.
            nav_authority:
              type: string
              description: >-
                nav_authority is the address authorized to mutate this vault's
                internal NAV

                table via MsgUpdateVaultNAV. If empty, the vault admin is
                treated as the NAV

                authority. It is rotated only via MsgUpdateNAVAuthority.
          description: >-
            VaultAccount represents a central holding place for assets, governed
            by a set of rules.

            It is based on the ERC-4626 standard and builds upon the Provenance
            Marker module.
        description: vaults is a list of all vaults.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryVaultsResponse is the response message for the Query/Vaults endpoint.
  provlabs.vault.v1.VaultAccount:
    type: object
    properties:
      base_account:
        description: >-
          base_account cosmos account information including address and coin
          holdings.
        type: object
        properties:
          address:
            type: string
          pub_key:
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
            description: >-
              `Any` contains an arbitrary serialized protocol buffer message
              along with a

              URL that describes the type of the serialized message.


              Protobuf library provides support to pack/unpack Any values in the
              form

              of utility functions or additional generated methods of the Any
              type.


              Example 1: Pack and unpack a message in C++.

                  Foo foo = ...;
                  Any any;
                  any.PackFrom(foo);
                  ...
                  if (any.UnpackTo(&foo)) {
                    ...
                  }

              Example 2: Pack and unpack a message in Java.

                  Foo foo = ...;
                  Any any = Any.pack(foo);
                  ...
                  if (any.is(Foo.class)) {
                    foo = any.unpack(Foo.class);
                  }

               Example 3: Pack and unpack a message in Python.

                  foo = Foo(...)
                  any = Any()
                  any.Pack(foo)
                  ...
                  if any.Is(Foo.DESCRIPTOR):
                    any.Unpack(foo)
                    ...

               Example 4: Pack and unpack a message in Go

                   foo := &pb.Foo{...}
                   any, err := ptypes.MarshalAny(foo)
                   ...
                   foo := &pb.Foo{}
                   if err := ptypes.UnmarshalAny(any, foo); err != nil {
                     ...
                   }

              The pack methods provided by protobuf library will by default use

              'type.googleapis.com/full.type.name' as the type URL and the
              unpack

              methods only use the fully qualified type name after the last '/'

              in the type URL, for example "foo.bar.com/x/y.z" will yield type

              name "y.z".



              JSON

              ====

              The JSON representation of an `Any` value uses the regular

              representation of the deserialized, embedded message, with an

              additional field `@type` which contains the type URL. Example:

                  package google.profile;
                  message Person {
                    string first_name = 1;
                    string last_name = 2;
                  }

                  {
                    "@type": "type.googleapis.com/google.profile.Person",
                    "firstName": <string>,
                    "lastName": <string>
                  }

              If the embedded message type is well-known and has a custom JSON

              representation, that representation will be embedded adding a
              field

              `value` which holds the custom JSON in addition to the `@type`

              field. Example (for message [google.protobuf.Duration][]):

                  {
                    "@type": "type.googleapis.com/google.protobuf.Duration",
                    "value": "1.212s"
                  }
          account_number:
            type: string
            format: uint64
          sequence:
            type: string
            format: uint64
      total_shares:
        description: >-
          total_shares is the total number of shares that have ever been issued
          by the vault.

          It serves as the canonical supply-of-record for all shares, regardless
          of whether

          they are held locally on Provenance or externally (e.g., bridged).
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      underlying_asset:
        type: string
        description: >-
          underlying_asset is the vault’s single principal collateral AND
          valuation/base unit.

          - Exactly one denom.

          - Total Vault Value (TVV) and NAV-per-share are computed and reported
          in this denom.

          - Interest accrual, fees, and internal accounting are measured in this
          denom.

          - Held assets acquired via settlement must have an internal NAV record
          priced INTO this denom.
      payment_denom:
        type: string
        description: >-
          payment_denom always equals underlying_asset. Vaults are single-denom;
          the

          field is inert and retained only for wire compatibility.


          Deprecated: The mixed-denom vault concept has been flattened into a
          single

          underlying denom. Deletion of this field is deferred to a future major
          release.
      admin:
        type: string
        description: >-
          admin is the address that has administrative privileges over the
          vault.
      current_interest_rate:
        type: string
        description: >-
          current_interest_rate is a decimal string (e.g., "0.9" for 90% and
          "0.9001353" for 90.01353%) representing the actual annual interest
          rate currently being applied.

          This may be adjusted programmatically (e.g., due to lack of funds).
      desired_interest_rate:
        type: string
        description: >-
          desired_interest_rate is a decimal string (e.g., "0.9" for 90% and
          "0.9001353" for 90.01353%) representing the target annual interest
          rate that the vault intends to apply.
      min_interest_rate:
        type: string
        description: >-
          min_interest_rate is a decimal string (e.g., "0.9" for 90% and
          "0.9001353" for 90.01353%) representing the lowest annual interest
          rate the admin is allowed to set.

          If unset (empty string), there is no lower limit.
      max_interest_rate:
        type: string
        description: >-
          max_interest_rate is a decimal string (e.g., "0.9" for 90% and
          "0.9001353" for 90.01353%) representing the highest annual interest
          rate the admin is allowed to set.

          If unset (empty string), there is no upper limit.
      period_start:
        type: string
        format: int64
        description: >-
          The start time (in Unix seconds) of the current interest accrual
          period.
      period_timeout:
        type: string
        format: int64
        description: >-
          The expire time (in Unix seconds) of the current interest accrual
          period.
      swap_in_enabled:
        type: boolean
        description: >-
          swap_in_enabled indicates whether users are allowed to deposit into
          the vault.
      swap_out_enabled:
        type: boolean
        description: >-
          swap_out_enabled indicates whether users are allowed to withdraw from
          the vault.
      withdrawal_delay_seconds:
        type: string
        format: uint64
        description: >-
          withdrawal_delay_seconds is the configured time period (in seconds)
          that a withdrawal

          request must wait in the pending queue before being processed.
      paused:
        type: boolean
        description: >-
          paused indicates that all user-facing swap-in and swap-out operations
          are disabled.
      paused_balance:
        description: >-
          paused_balance is the total vault value snapshot taken at the moment
          of pausing.

          This value is used for all NAV calculations while the vault is paused
          to prevent

          apparent devaluation during collateral rebalancing. It is cleared upon
          unpausing.

          Nothing done during the pause moves it, neither a principal deposit or
          withdrawal

          nor a NAV repricing; those all take effect together when the vault is
          unpaused.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      paused_reason:
        type: string
        description: >-
          paused_reason is a human-readable string explaining why the vault was
          paused, particularly for automatic pauses.
      paused_by:
        type: string
        description: >-
          paused_by is the address that initiated the current pause, empty for
          an automatic

          pause and cleared on unpause. MsgRepriceVault consults it so the NAV
          authority

          can only resume a pause it took itself.
      paused_forced:
        type: boolean
        description: >-
          paused_forced reports whether the current pause waived the strict
          reconcile and

          valuation gate, true for a forced MsgPauseVault and every automatic
          pause. A forced

          pause records a tolerated failure, so MsgRepriceVault refuses to
          resume one.
      bridge_address:
        type: string
        description: >-
          bridge_address is the single external address allowed to mint or burn
          shares on behalf

          of this vault (e.g., for bridging to another chain). All mint/burn
          must flow through the

          vault keeper, which enforces that marker supply never exceeds
          total_shares.
      bridge_enabled:
        type: boolean
        description: >-
          bridge_enabled indicates whether the bridge functionality is active.
          If false, the

          bridge_address has no effect and cannot mint or burn.
      asset_manager:
        type: string
        description: >-
          asset_manager is an optional address that, when set, is authorized to
          manage certain

          collateral operations alongside the admin (e.g., pausing/unpausing,
          depositing/withdrawing

          principal or interest funds). If unset (empty string), only the admin
          may perform those actions.
      fee_period_start:
        type: string
        format: int64
        description: >-
          fee_period_start is the start time (in Unix seconds) of the current
          AUM fee collection period.

          This is a module-managed timestamp kept in sync with the fee timeout
          queue machinery.
      fee_period_timeout:
        type: string
        format: int64
        description: >-
          fee_period_timeout is the end time (in Unix seconds) of the current
          AUM fee collection period.

          This is a module-managed timestamp kept in sync with the fee timeout
          queue machinery.
      outstanding_aum_fee:
        description: >-
          outstanding_aum_fee is the amount of AUM fee that has been calculated
          but not yet collected

          due to insufficient liquidity in the principal marker. This amount is
          always denominated

          in the vault's underlying_asset, must be preserved, and is carried
          into valuation computations.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      aum_fee_bips:
        type: integer
        format: int64
        description: >-
          aum_fee_bips is the AUM fee rate (in basis points) for this specific
          vault.

          Units: Basis Points (1 bps = 0.01%).

          Valid Range: 0 to 10000 (0% to 100%).

          A value of 0 disables AUM fee collection for this vault.

          Negative values are not supported (uint32).
      min_swap_in_value:
        type: string
        description: >-
          min_swap_in_value is a string representing the minimum value required
          for a deposit.

          - The value is measured in the underlying_asset.

          - Values must be non-negative (>= 0).

          - If empty ("") or "0", there is no minimum limit.
      min_swap_out_value:
        type: string
        description: >-
          min_swap_out_value is a string representing the minimum value required
          for a withdrawal.

          - The value is measured in the underlying_asset.

          - Outgoing redemptions are converted to this unit before checking.

          - Values must be non-negative (>= 0).

          - If empty ("") or "0", there is no minimum limit.
      max_swap_in_value:
        type: string
        description: >-
          max_swap_in_value is a string representing the maximum value allowed
          for a deposit.

          - The value is measured in the underlying_asset.

          - Values must be positive (> 0).

          - An empty string "" indicates no maximum limit.
      max_swap_out_value:
        type: string
        description: >-
          max_swap_out_value is a string representing the maximum value allowed
          for a withdrawal.

          - The value is measured in the underlying_asset.

          - Outgoing redemptions are converted to this unit before checking.

          - Values must be positive (> 0).

          - An empty string "" indicates no maximum limit.
      nav_authority:
        type: string
        description: >-
          nav_authority is the address authorized to mutate this vault's
          internal NAV

          table via MsgUpdateVaultNAV. If empty, the vault admin is treated as
          the NAV

          authority. It is rotated only via MsgUpdateNAVAuthority.
    description: >-
      VaultAccount represents a central holding place for assets, governed by a
      set of rules.

      It is based on the ERC-4626 standard and builds upon the Provenance Marker
      module.
  provlabs.vault.v1.VaultNAV:
    type: object
    properties:
      denom:
        type: string
        description: denom is the asset denomination this entry prices.
      price:
        description: >-
          price is the total value of `volume` units of the denom, denominated
          in the

          vault's underlying asset. The per-unit value is price divided by
          volume.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      volume:
        type: string
        description: >-
          volume is the number of units of the denom that price covers. It must
          be

          positive; the per-unit value of the denom is price divided by volume.
      source:
        type: string
        description: >-
          source identifies the origin of this NAV entry (for example an oracle
          name

          or the settlement path), mirroring the Marker module's NAV source
          attribution.
      updated_block_height:
        type: string
        format: int64
        description: >-
          updated_block_height is the block height at which this entry was last
          updated.
      updated_time:
        type: string
        format: date-time
        description: updated_time is the block time at which this entry was last updated.
    description: >-
      VaultNAV is a single internal net asset value entry recording the price of
      one

      asset denom held by a vault. The vault module is the sole source of truth
      for

      these values; the NAV authority maintains them via MsgUpdateVaultNAV.
  provlabs.vault.v1.MsgAcceptAssetResponse:
    type: object
    description: >-
      MsgAcceptAssetResponse is the response message for the AcceptAsset
      endpoint.
  provlabs.vault.v1.MsgBridgeBurnSharesResponse:
    type: object
    description: >-
      MsgBridgeBurnSharesResponse is the response message for the
      BridgeBurnShares endpoint.
  provlabs.vault.v1.MsgBridgeMintSharesResponse:
    type: object
    description: >-
      MsgBridgeMintSharesResponse is the response message for the
      BridgeMintShares endpoint.
  provlabs.vault.v1.MsgCreateVaultResponse:
    type: object
    properties:
      vault_address:
        type: string
        description: vault_address is the bech32 address of the newly created vault.
    description: >-
      MsgCreateVaultResponse is the response message for the CreateVault
      endpoint.
  provlabs.vault.v1.MsgDepositInterestFundsResponse:
    type: object
    description: >-
      MsgDepositInterestFundsResponse is the response message for the
      DepositInterestFunds endpoint.
  provlabs.vault.v1.MsgDepositPrincipalFundsResponse:
    type: object
    description: >-
      MsgDepositPrincipalFundsResponse is the response message for the
      DepositPrincipalFunds endpoint.
  provlabs.vault.v1.MsgExpeditePendingSwapOutResponse:
    type: object
    description: >-
      MsgExpeditePendingSwapOutResponse is the response message for the
      ExpeditePendingSwapOut endpoint.
  provlabs.vault.v1.MsgPauseVaultResponse:
    type: object
    description: MsgPauseVaultResponse is the response message for the PauseVault endpoint.
  provlabs.vault.v1.MsgRejectAssetResponse:
    type: object
    description: >-
      MsgRejectAssetResponse is the response message for the RejectAsset
      endpoint.
  provlabs.vault.v1.MsgRemoveVaultNAVResponse:
    type: object
    description: >-
      MsgRemoveVaultNAVResponse is the response message for the RemoveVaultNAV
      endpoint.
  provlabs.vault.v1.MsgRepriceVaultResponse:
    type: object
    description: >-
      MsgRepriceVaultResponse is the response message for the RepriceVault
      endpoint.
  provlabs.vault.v1.MsgSetAssetManagerResponse:
    type: object
    description: >-
      MsgSetAssetManagerResponse is the response message for the SetAssetManager
      endpoint.
  provlabs.vault.v1.MsgSetBridgeAddressResponse:
    type: object
    description: >-
      MsgSetBridgeAddressResponse is the response message for the
      SetBridgeAddress endpoint.
  provlabs.vault.v1.MsgSetShareDenomMetadataResponse:
    type: object
    description: >-
      MsgSetShareDenomMetadataResponse defines the response for setting a
      vault's share denom metadata.
  provlabs.vault.v1.MsgSwapInResponse:
    type: object
    properties:
      shares_received:
        description: shares_received is the amount of vault shares minted to the depositor.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: MsgSwapInResponse is the response message for a successful SwapIn.
  provlabs.vault.v1.MsgSwapOutResponse:
    type: object
    properties:
      request_id:
        type: string
        format: uint64
        description: >-
          request_id is the unique identifier for the newly queued swap out
          request.
    description: MsgSwapOutResponse is the response message for the SwapOut endpoint.
  provlabs.vault.v1.MsgToggleBridgeResponse:
    type: object
    description: >-
      MsgToggleBridgeResponse is the response message for the ToggleBridge
      endpoint.
  provlabs.vault.v1.MsgToggleSwapInResponse:
    type: object
    description: >-
      MsgToggleSwapInResponse is the response message for the ToggleSwapIn
      endpoint.
  provlabs.vault.v1.MsgToggleSwapOutResponse:
    type: object
    description: >-
      MsgToggleSwapOutResponse is the response message for the ToggleSwapOut
      endpoint.
  provlabs.vault.v1.MsgUnpauseVaultResponse:
    type: object
    description: >-
      MsgUnpauseVaultResponse is the response message for the UnpauseVault
      endpoint.
  provlabs.vault.v1.MsgUpdateInterestRateResponse:
    type: object
    description: >-
      MsgUpdateInterestRateResponse is the response message for the
      UpdateInterestRate endpoint.
  provlabs.vault.v1.MsgUpdateMaxInterestRateResponse:
    type: object
    description: >-
      MsgUpdateMaxInterestRateResponse is the response message for the
      UpdateMaxInterestRate endpoint.
  provlabs.vault.v1.MsgUpdateMaxSwapInValueResponse:
    type: object
    description: >-
      MsgUpdateMaxSwapInValueResponse is the response message for the
      UpdateMaxSwapInValue endpoint.
  provlabs.vault.v1.MsgUpdateMaxSwapOutValueResponse:
    type: object
    description: >-
      MsgUpdateMaxSwapOutValueResponse is the response message for the
      UpdateMaxSwapOutValue endpoint.
  provlabs.vault.v1.MsgUpdateMinInterestRateResponse:
    type: object
    description: >-
      MsgUpdateMinInterestRateResponse is the response message for the
      UpdateMinInterestRate endpoint.
  provlabs.vault.v1.MsgUpdateMinSwapInValueResponse:
    type: object
    description: >-
      MsgUpdateMinSwapInValueResponse is the response message for the
      UpdateMinSwapInValue endpoint.
  provlabs.vault.v1.MsgUpdateMinSwapOutValueResponse:
    type: object
    description: >-
      MsgUpdateMinSwapOutValueResponse is the response message for the
      UpdateMinSwapOutValue endpoint.
  provlabs.vault.v1.MsgUpdateNAVAuthorityResponse:
    type: object
    description: >-
      MsgUpdateNAVAuthorityResponse is the response message for the
      UpdateNAVAuthority endpoint.
  provlabs.vault.v1.MsgUpdateParamsResponse:
    type: object
    description: >-
      MsgUpdateParamsResponse is the response message for the UpdateParams
      endpoint.
  provlabs.vault.v1.MsgUpdateVaultAUMFeeBipsResponse:
    type: object
    description: >-
      MsgUpdateVaultAUMFeeBipsResponse is the response message for the
      UpdateVaultAUMFeeBips endpoint.
  provlabs.vault.v1.MsgUpdateVaultNAVResponse:
    type: object
    description: >-
      MsgUpdateVaultNAVResponse is the response message for the UpdateVaultNAV
      endpoint.
  provlabs.vault.v1.MsgUpdateWithdrawalDelayResponse:
    type: object
    description: >-
      MsgUpdateWithdrawalDelayResponse is the response message for the
      UpdateWithdrawalDelay endpoint.
  provlabs.vault.v1.MsgWithdrawInterestFundsResponse:
    type: object
    description: >-
      MsgWithdrawInterestFundsResponse is the response message for the
      WithdrawInterestFunds endpoint.
  provlabs.vault.v1.MsgWithdrawPrincipalFundsResponse:
    type: object
    description: >-
      MsgWithdrawPrincipalFundsResponse is the response message for the
      WithdrawPrincipalFunds endpoint.
  provlabs.vault.v1.NAVUpdate:
    type: object
    properties:
      denom:
        type: string
        description: >-
          denom is the asset denomination being priced. It must not be the
          vault's share denom.
      price:
        description: >-
          price is the total value of `volume` units of the denom, denominated
          in

          the vault's underlying asset.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
      volume:
        type: string
        description: >-
          volume is the number of units of the denom that price covers. It must
          be positive.
      source:
        type: string
        description: >-
          source identifies the origin of this NAV update (for example an oracle
          name).

          It is optional.
    description: >-
      NAVUpdate is one denom's price restatement within a RepriceVault batch. It
      carries

      only the operator-supplied fields of a VaultNAV; the module stamps the
      update height

      and time itself.
  cosmos.app.v1alpha1.Config:
    type: object
    properties:
      modules:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              description: >-
                name is the unique name of the module within the app. It should
                be a name

                that persists between different versions of a module so that
                modules

                can be smoothly upgraded to new versions.


                For example, for the module cosmos.bank.module.v1.Module, we may
                chose

                to simply name the module "bank" in the app. When we upgrade to

                cosmos.bank.module.v2.Module, the app-specific name "bank" stays
                the same

                and the framework knows that the v2 module should receive all
                the same state

                that the v1 module had. Note: modules should provide info on
                which versions

                they can migrate from in the ModuleDescriptor.can_migration_from
                field.
            config:
              description: >-
                config is the config object for the module. Module config
                messages should

                define a ModuleDescriptor using the
                cosmos.app.v1alpha1.is_module extension.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            golangBindings:
              type: array
              items:
                type: object
                properties:
                  interfaceType:
                    type: string
                    title: >-
                      interface_type is the interface type which will be bound
                      to a specific implementation type
                  implementation:
                    type: string
                    title: >-
                      implementation is the implementing type which will be
                      supplied when an input of type interface is requested
                description: >-
                  GolangBinding is an explicit interface type to implementing
                  type binding for dependency injection.
              description: >-
                golang_bindings specifies explicit interface to implementation
                type bindings which

                depinject uses to resolve interface inputs to provider
                functions.  The scope of this

                field's configuration is module specific.
          description: ModuleConfig is a module configuration for an app.
        description: modules are the module configurations for the app.
      golangBindings:
        type: array
        items:
          type: object
          properties:
            interfaceType:
              type: string
              title: >-
                interface_type is the interface type which will be bound to a
                specific implementation type
            implementation:
              type: string
              title: >-
                implementation is the implementing type which will be supplied
                when an input of type interface is requested
          description: >-
            GolangBinding is an explicit interface type to implementing type
            binding for dependency injection.
        description: >-
          golang_bindings specifies explicit interface to implementation type
          bindings which

          depinject uses to resolve interface inputs to provider functions.  The
          scope of this

          field's configuration is global (not module specific).
    description: >-
      Config represents the configuration for a Cosmos SDK ABCI app.

      It is intended that all state machine logic including the version of

      baseapp and tx handlers (and possibly even Tendermint) that an app needs

      can be described in a config object. For compatibility, the framework
      should

      allow a mixture of declarative and imperative app wiring, however, apps

      that strive for the maximum ease of maintainability should be able to
      describe

      their state machine with a config object alone.
  cosmos.app.v1alpha1.GolangBinding:
    type: object
    properties:
      interfaceType:
        type: string
        title: >-
          interface_type is the interface type which will be bound to a specific
          implementation type
      implementation:
        type: string
        title: >-
          implementation is the implementing type which will be supplied when an
          input of type interface is requested
    description: >-
      GolangBinding is an explicit interface type to implementing type binding
      for dependency injection.
  cosmos.app.v1alpha1.ModuleConfig:
    type: object
    properties:
      name:
        type: string
        description: >-
          name is the unique name of the module within the app. It should be a
          name

          that persists between different versions of a module so that modules

          can be smoothly upgraded to new versions.


          For example, for the module cosmos.bank.module.v1.Module, we may chose

          to simply name the module "bank" in the app. When we upgrade to

          cosmos.bank.module.v2.Module, the app-specific name "bank" stays the
          same

          and the framework knows that the v2 module should receive all the same
          state

          that the v1 module had. Note: modules should provide info on which
          versions

          they can migrate from in the ModuleDescriptor.can_migration_from
          field.
      config:
        description: >-
          config is the config object for the module. Module config messages
          should

          define a ModuleDescriptor using the cosmos.app.v1alpha1.is_module
          extension.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      golangBindings:
        type: array
        items:
          type: object
          properties:
            interfaceType:
              type: string
              title: >-
                interface_type is the interface type which will be bound to a
                specific implementation type
            implementation:
              type: string
              title: >-
                implementation is the implementing type which will be supplied
                when an input of type interface is requested
          description: >-
            GolangBinding is an explicit interface type to implementing type
            binding for dependency injection.
        description: >-
          golang_bindings specifies explicit interface to implementation type
          bindings which

          depinject uses to resolve interface inputs to provider functions.  The
          scope of this

          field's configuration is module specific.
    description: ModuleConfig is a module configuration for an app.
  cosmos.app.v1alpha1.QueryConfigResponse:
    type: object
    properties:
      config:
        description: config is the current app config.
        type: object
        properties:
          modules:
            type: array
            items:
              type: object
              properties:
                name:
                  type: string
                  description: >-
                    name is the unique name of the module within the app. It
                    should be a name

                    that persists between different versions of a module so that
                    modules

                    can be smoothly upgraded to new versions.


                    For example, for the module cosmos.bank.module.v1.Module, we
                    may chose

                    to simply name the module "bank" in the app. When we upgrade
                    to

                    cosmos.bank.module.v2.Module, the app-specific name "bank"
                    stays the same

                    and the framework knows that the v2 module should receive
                    all the same state

                    that the v1 module had. Note: modules should provide info on
                    which versions

                    they can migrate from in the
                    ModuleDescriptor.can_migration_from field.
                config:
                  description: >-
                    config is the config object for the module. Module config
                    messages should

                    define a ModuleDescriptor using the
                    cosmos.app.v1alpha1.is_module extension.
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                golangBindings:
                  type: array
                  items:
                    type: object
                    properties:
                      interfaceType:
                        type: string
                        title: >-
                          interface_type is the interface type which will be
                          bound to a specific implementation type
                      implementation:
                        type: string
                        title: >-
                          implementation is the implementing type which will be
                          supplied when an input of type interface is requested
                    description: >-
                      GolangBinding is an explicit interface type to
                      implementing type binding for dependency injection.
                  description: >-
                    golang_bindings specifies explicit interface to
                    implementation type bindings which

                    depinject uses to resolve interface inputs to provider
                    functions.  The scope of this

                    field's configuration is module specific.
              description: ModuleConfig is a module configuration for an app.
            description: modules are the module configurations for the app.
          golangBindings:
            type: array
            items:
              type: object
              properties:
                interfaceType:
                  type: string
                  title: >-
                    interface_type is the interface type which will be bound to
                    a specific implementation type
                implementation:
                  type: string
                  title: >-
                    implementation is the implementing type which will be
                    supplied when an input of type interface is requested
              description: >-
                GolangBinding is an explicit interface type to implementing type
                binding for dependency injection.
            description: >-
              golang_bindings specifies explicit interface to implementation
              type bindings which

              depinject uses to resolve interface inputs to provider functions. 
              The scope of this

              field's configuration is global (not module specific).
    description: QueryConfigResponse is the Query/Config response type.
  cosmos.auth.v1beta1.AddressBytesToStringResponse:
    type: object
    properties:
      addressString:
        type: string
    description: >-
      AddressBytesToStringResponse is the response type for AddressString rpc
      method.
  cosmos.auth.v1beta1.AddressStringToBytesResponse:
    type: object
    properties:
      addressBytes:
        type: string
        format: byte
    description: >-
      AddressStringToBytesResponse is the response type for AddressBytes rpc
      method.
  cosmos.auth.v1beta1.Bech32PrefixResponse:
    type: object
    properties:
      bech32Prefix:
        type: string
    description: Bech32PrefixResponse is the response type for Bech32Prefix rpc method.
  cosmos.auth.v1beta1.Params:
    type: object
    properties:
      maxMemoCharacters:
        type: string
        format: uint64
      txSigLimit:
        type: string
        format: uint64
      txSizeCostPerByte:
        type: string
        format: uint64
      sigVerifyCostEd25519:
        type: string
        format: uint64
      sigVerifyCostSecp256k1:
        type: string
        format: uint64
    description: Params defines the parameters for the auth module.
  cosmos.auth.v1beta1.QueryAccountAddressByIDResponse:
    type: object
    properties:
      accountAddress:
        type: string
    title: >-
      QueryAccountAddressByIDResponse is the response type for
      AccountAddressByID rpc method
  cosmos.auth.v1beta1.QueryAccountInfoResponse:
    type: object
    properties:
      info:
        description: info is the account info which is represented by BaseAccount.
        type: object
        properties:
          address:
            type: string
          pubKey:
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
            description: >-
              `Any` contains an arbitrary serialized protocol buffer message
              along with a

              URL that describes the type of the serialized message.


              Protobuf library provides support to pack/unpack Any values in the
              form

              of utility functions or additional generated methods of the Any
              type.


              Example 1: Pack and unpack a message in C++.

                  Foo foo = ...;
                  Any any;
                  any.PackFrom(foo);
                  ...
                  if (any.UnpackTo(&foo)) {
                    ...
                  }

              Example 2: Pack and unpack a message in Java.

                  Foo foo = ...;
                  Any any = Any.pack(foo);
                  ...
                  if (any.is(Foo.class)) {
                    foo = any.unpack(Foo.class);
                  }
                  // or ...
                  if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                    foo = any.unpack(Foo.getDefaultInstance());
                  }

               Example 3: Pack and unpack a message in Python.

                  foo = Foo(...)
                  any = Any()
                  any.Pack(foo)
                  ...
                  if any.Is(Foo.DESCRIPTOR):
                    any.Unpack(foo)
                    ...

               Example 4: Pack and unpack a message in Go

                   foo := &pb.Foo{...}
                   any, err := anypb.New(foo)
                   if err != nil {
                     ...
                   }
                   ...
                   foo := &pb.Foo{}
                   if err := any.UnmarshalTo(foo); err != nil {
                     ...
                   }

              The pack methods provided by protobuf library will by default use

              'type.googleapis.com/full.type.name' as the type URL and the
              unpack

              methods only use the fully qualified type name after the last '/'

              in the type URL, for example "foo.bar.com/x/y.z" will yield type

              name "y.z".


              JSON

              ====

              The JSON representation of an `Any` value uses the regular

              representation of the deserialized, embedded message, with an

              additional field `@type` which contains the type URL. Example:

                  package google.profile;
                  message Person {
                    string first_name = 1;
                    string last_name = 2;
                  }

                  {
                    "@type": "type.googleapis.com/google.profile.Person",
                    "firstName": <string>,
                    "lastName": <string>
                  }

              If the embedded message type is well-known and has a custom JSON

              representation, that representation will be embedded adding a
              field

              `value` which holds the custom JSON in addition to the `@type`

              field. Example (for message [google.protobuf.Duration][]):

                  {
                    "@type": "type.googleapis.com/google.protobuf.Duration",
                    "value": "1.212s"
                  }
          accountNumber:
            type: string
            format: uint64
          sequence:
            type: string
            format: uint64
    description: QueryAccountInfoResponse is the Query/AccountInfo response type.
  cosmos.auth.v1beta1.QueryAccountResponse:
    type: object
    properties:
      account:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
    description: >-
      QueryAccountResponse is the response type for the Query/Account RPC
      method.
  cosmos.auth.v1beta1.QueryAccountsResponse:
    type: object
    properties:
      accounts:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        title: accounts are the existing accounts
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAccountsResponse is the response type for the Query/Accounts RPC
      method.
  cosmos.auth.v1beta1.QueryModuleAccountByNameResponse:
    type: object
    properties:
      account:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
    description: >-
      QueryModuleAccountByNameResponse is the response type for the
      Query/ModuleAccountByName RPC method.
  cosmos.auth.v1beta1.QueryModuleAccountsResponse:
    type: object
    properties:
      accounts:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
    description: >-
      QueryModuleAccountsResponse is the response type for the
      Query/ModuleAccounts RPC method.
  cosmos.auth.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          maxMemoCharacters:
            type: string
            format: uint64
          txSigLimit:
            type: string
            format: uint64
          txSizeCostPerByte:
            type: string
            format: uint64
          sigVerifyCostEd25519:
            type: string
            format: uint64
          sigVerifyCostSecp256k1:
            type: string
            format: uint64
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  cosmos.authz.v1beta1.Grant:
    type: object
    properties:
      authorization:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
      expiration:
        type: string
        format: date-time
        title: >-
          time when the grant will expire and will be pruned. If null, then the
          grant

          doesn't have a time expiration (other conditions  in `authorization`

          may apply to invalidate the grant)
    description: |-
      Grant gives permissions to execute
      the provide method with expiration time.
  cosmos.authz.v1beta1.GrantAuthorization:
    type: object
    properties:
      granter:
        type: string
      grantee:
        type: string
      authorization:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
      expiration:
        type: string
        format: date-time
    title: >-
      GrantAuthorization extends a grant with both the addresses of the grantee
      and granter.

      It is used in genesis.proto and query.proto
  cosmos.authz.v1beta1.QueryGranteeGrantsResponse:
    type: object
    properties:
      grants:
        type: array
        items:
          type: object
          properties:
            granter:
              type: string
            grantee:
              type: string
            authorization:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            expiration:
              type: string
              format: date-time
          title: >-
            GrantAuthorization extends a grant with both the addresses of the
            grantee and granter.

            It is used in genesis.proto and query.proto
        description: grants is a list of grants granted to the grantee.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGranteeGrantsResponse is the response type for the
      Query/GranteeGrants RPC method.
  cosmos.authz.v1beta1.QueryGranterGrantsResponse:
    type: object
    properties:
      grants:
        type: array
        items:
          type: object
          properties:
            granter:
              type: string
            grantee:
              type: string
            authorization:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            expiration:
              type: string
              format: date-time
          title: >-
            GrantAuthorization extends a grant with both the addresses of the
            grantee and granter.

            It is used in genesis.proto and query.proto
        description: grants is a list of grants granted by the granter.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGranterGrantsResponse is the response type for the
      Query/GranterGrants RPC method.
  cosmos.authz.v1beta1.QueryGrantsResponse:
    type: object
    properties:
      grants:
        type: array
        items:
          type: object
          properties:
            authorization:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            expiration:
              type: string
              format: date-time
              title: >-
                time when the grant will expire and will be pruned. If null,
                then the grant

                doesn't have a time expiration (other conditions  in
                `authorization`

                may apply to invalidate the grant)
          description: |-
            Grant gives permissions to execute
            the provide method with expiration time.
        description: authorizations is a list of grants granted for grantee by granter.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGrantsResponse is the response type for the Query/Authorizations RPC
      method.
  cosmos.bank.v1beta1.DenomOwner:
    type: object
    properties:
      address:
        type: string
        description: address defines the address that owns a particular denomination.
      balance:
        description: balance is the balance of the denominated coin for an account.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: |-
      DenomOwner defines structure representing an account that owns or holds a
      particular denominated token. It contains the account address and account
      balance of the denominated token.
  cosmos.bank.v1beta1.Params:
    type: object
    properties:
      sendEnabled:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            enabled:
              type: boolean
          description: >-
            SendEnabled maps coin denom to a send_enabled status (whether a
            denom is

            sendable).
        description: >-
          Deprecated: Use of SendEnabled in params is deprecated.

          For genesis, use the newly added send_enabled field in the genesis
          object.

          Storage, lookup, and manipulation of this information is now in the
          keeper.


          As of cosmos-sdk 0.47, this only exists for backwards compatibility of
          genesis files.
      defaultSendEnabled:
        type: boolean
    description: Params defines the parameters for the bank module.
  cosmos.bank.v1beta1.QueryAllBalancesResponse:
    type: object
    properties:
      balances:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: balances is the balances of all the coins.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAllBalancesResponse is the response type for the Query/AllBalances
      RPC

      method.
  cosmos.bank.v1beta1.QueryBalanceResponse:
    type: object
    properties:
      balance:
        description: balance is the balance of the coin.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      QueryBalanceResponse is the response type for the Query/Balance RPC
      method.
  cosmos.bank.v1beta1.QueryDenomMetadataByQueryStringResponse:
    type: object
    properties:
      metadata:
        description: >-
          metadata describes and provides all the client information for the
          requested token.
        type: object
        properties:
          description:
            type: string
          denomUnits:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                  description: >-
                    denom represents the string name of the given denom unit
                    (e.g uatom).
                exponent:
                  type: integer
                  format: int64
                  description: >-
                    exponent represents power of 10 exponent that one must

                    raise the base_denom to in order to equal the given
                    DenomUnit's denom

                    1 denom = 10^exponent base_denom

                    (e.g. with a base_denom of uatom, one can create a DenomUnit
                    of 'atom' with

                    exponent = 6, thus: 1 atom = 10^6 uatom).
                aliases:
                  type: array
                  items:
                    type: string
                  title: aliases is a list of string aliases for the given denom
              description: |-
                DenomUnit represents a struct that describes a given
                denomination unit of the basic token.
            title: denom_units represents the list of DenomUnit's for a given coin
          base:
            type: string
            description: >-
              base represents the base denom (should be the DenomUnit with
              exponent = 0).
          display:
            type: string
            description: |-
              display indicates the suggested denom that should be
              displayed in clients.
          name:
            type: string
            title: 'name defines the name of the token (eg: Cosmos Atom)'
          symbol:
            type: string
            description: >-
              symbol is the token symbol usually shown on exchanges (eg: ATOM).
              This can

              be the same as the display.
          uri:
            type: string
            description: >-
              URI to a document (on or off-chain) that contains additional
              information. Optional.
          uriHash:
            type: string
            description: >-
              URIHash is a sha256 hash of a document pointed by URI. It's used
              to verify that

              the document didn't change. Optional.
    description: >-
      QueryDenomMetadataByQueryStringResponse is the response type for the
      Query/DenomMetadata RPC

      method. Identical with QueryDenomMetadataResponse but receives denom as
      query string in request.
  cosmos.bank.v1beta1.QueryDenomMetadataResponse:
    type: object
    properties:
      metadata:
        description: >-
          metadata describes and provides all the client information for the
          requested token.
        type: object
        properties:
          description:
            type: string
          denomUnits:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                  description: >-
                    denom represents the string name of the given denom unit
                    (e.g uatom).
                exponent:
                  type: integer
                  format: int64
                  description: >-
                    exponent represents power of 10 exponent that one must

                    raise the base_denom to in order to equal the given
                    DenomUnit's denom

                    1 denom = 10^exponent base_denom

                    (e.g. with a base_denom of uatom, one can create a DenomUnit
                    of 'atom' with

                    exponent = 6, thus: 1 atom = 10^6 uatom).
                aliases:
                  type: array
                  items:
                    type: string
                  title: aliases is a list of string aliases for the given denom
              description: |-
                DenomUnit represents a struct that describes a given
                denomination unit of the basic token.
            title: denom_units represents the list of DenomUnit's for a given coin
          base:
            type: string
            description: >-
              base represents the base denom (should be the DenomUnit with
              exponent = 0).
          display:
            type: string
            description: |-
              display indicates the suggested denom that should be
              displayed in clients.
          name:
            type: string
            title: 'name defines the name of the token (eg: Cosmos Atom)'
          symbol:
            type: string
            description: >-
              symbol is the token symbol usually shown on exchanges (eg: ATOM).
              This can

              be the same as the display.
          uri:
            type: string
            description: >-
              URI to a document (on or off-chain) that contains additional
              information. Optional.
          uriHash:
            type: string
            description: >-
              URIHash is a sha256 hash of a document pointed by URI. It's used
              to verify that

              the document didn't change. Optional.
    description: >-
      QueryDenomMetadataResponse is the response type for the
      Query/DenomMetadata RPC

      method.
  cosmos.bank.v1beta1.QueryDenomOwnersByQueryResponse:
    type: object
    properties:
      denomOwners:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address defines the address that owns a particular denomination.
            balance:
              description: balance is the balance of the denominated coin for an account.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            DenomOwner defines structure representing an account that owns or
            holds a

            particular denominated token. It contains the account address and
            account

            balance of the denominated token.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryDenomOwnersByQueryResponse defines the RPC response of a
      DenomOwnersByQuery RPC query.
  cosmos.bank.v1beta1.QueryDenomOwnersResponse:
    type: object
    properties:
      denomOwners:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address defines the address that owns a particular denomination.
            balance:
              description: balance is the balance of the denominated coin for an account.
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
          description: >-
            DenomOwner defines structure representing an account that owns or
            holds a

            particular denominated token. It contains the account address and
            account

            balance of the denominated token.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryDenomOwnersResponse defines the RPC response of a DenomOwners RPC
      query.
  cosmos.bank.v1beta1.QueryDenomsMetadataResponse:
    type: object
    properties:
      metadatas:
        type: array
        items:
          type: object
          properties:
            description:
              type: string
            denomUnits:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                    description: >-
                      denom represents the string name of the given denom unit
                      (e.g uatom).
                  exponent:
                    type: integer
                    format: int64
                    description: >-
                      exponent represents power of 10 exponent that one must

                      raise the base_denom to in order to equal the given
                      DenomUnit's denom

                      1 denom = 10^exponent base_denom

                      (e.g. with a base_denom of uatom, one can create a
                      DenomUnit of 'atom' with

                      exponent = 6, thus: 1 atom = 10^6 uatom).
                  aliases:
                    type: array
                    items:
                      type: string
                    title: aliases is a list of string aliases for the given denom
                description: |-
                  DenomUnit represents a struct that describes a given
                  denomination unit of the basic token.
              title: denom_units represents the list of DenomUnit's for a given coin
            base:
              type: string
              description: >-
                base represents the base denom (should be the DenomUnit with
                exponent = 0).
            display:
              type: string
              description: |-
                display indicates the suggested denom that should be
                displayed in clients.
            name:
              type: string
              title: 'name defines the name of the token (eg: Cosmos Atom)'
            symbol:
              type: string
              description: >-
                symbol is the token symbol usually shown on exchanges (eg:
                ATOM). This can

                be the same as the display.
            uri:
              type: string
              description: >-
                URI to a document (on or off-chain) that contains additional
                information. Optional.
            uriHash:
              type: string
              description: >-
                URIHash is a sha256 hash of a document pointed by URI. It's used
                to verify that

                the document didn't change. Optional.
          description: |-
            Metadata represents a struct that describes
            a basic token.
        description: >-
          metadata provides the client information for all the registered
          tokens.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryDenomsMetadataResponse is the response type for the
      Query/DenomsMetadata RPC

      method.
  cosmos.bank.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params provides the parameters of the bank module.
        type: object
        properties:
          sendEnabled:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                enabled:
                  type: boolean
              description: >-
                SendEnabled maps coin denom to a send_enabled status (whether a
                denom is

                sendable).
            description: >-
              Deprecated: Use of SendEnabled in params is deprecated.

              For genesis, use the newly added send_enabled field in the genesis
              object.

              Storage, lookup, and manipulation of this information is now in
              the keeper.


              As of cosmos-sdk 0.47, this only exists for backwards
              compatibility of genesis files.
          defaultSendEnabled:
            type: boolean
    description: >-
      QueryParamsResponse defines the response type for querying x/bank
      parameters.
  cosmos.bank.v1beta1.QuerySendEnabledResponse:
    type: object
    properties:
      sendEnabled:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            enabled:
              type: boolean
          description: >-
            SendEnabled maps coin denom to a send_enabled status (whether a
            denom is

            sendable).
      pagination:
        description: |-
          pagination defines the pagination in the response. This field is only
          populated if the denoms field in the request is empty.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QuerySendEnabledResponse defines the RPC response of a SendEnable query.
  cosmos.bank.v1beta1.QuerySpendableBalanceByDenomResponse:
    type: object
    properties:
      balance:
        description: balance is the balance of the coin.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      QuerySpendableBalanceByDenomResponse defines the gRPC response structure
      for

      querying an account's spendable balance for a specific denom.
  cosmos.bank.v1beta1.QuerySpendableBalancesResponse:
    type: object
    properties:
      balances:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: balances is the spendable balances of all the coins.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QuerySpendableBalancesResponse defines the gRPC response structure for
      querying

      an account's spendable balances.
  cosmos.bank.v1beta1.QuerySupplyOfResponse:
    type: object
    properties:
      amount:
        description: amount is the supply of the coin.
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
    description: >-
      QuerySupplyOfResponse is the response type for the Query/SupplyOf RPC
      method.
  cosmos.bank.v1beta1.QueryTotalSupplyResponse:
    type: object
    properties:
      supply:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: supply is the supply of the coins
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: >-
      QueryTotalSupplyResponse is the response type for the Query/TotalSupply
      RPC

      method
  cosmos.bank.v1beta1.SendEnabled:
    type: object
    properties:
      denom:
        type: string
      enabled:
        type: boolean
    description: |-
      SendEnabled maps coin denom to a send_enabled status (whether a denom is
      sendable).
  cosmos.base.node.v1beta1.ConfigResponse:
    type: object
    properties:
      minimumGasPrice:
        type: string
      pruningKeepRecent:
        type: string
      pruningInterval:
        type: string
      haltHeight:
        type: string
        format: uint64
    description: ConfigResponse defines the response structure for the Config gRPC query.
  cosmos.base.node.v1beta1.StatusResponse:
    type: object
    properties:
      earliestStoreHeight:
        type: string
        format: uint64
        title: earliest block height available in the store
      height:
        type: string
        format: uint64
        title: current block height
      timestamp:
        type: string
        format: date-time
        title: block height timestamp
      appHash:
        type: string
        format: byte
        title: app hash of the current block
      validatorHash:
        type: string
        format: byte
        title: validator hash provided by the consensus header
    description: StateResponse defines the response structure for the status of a node.
  cosmos.base.tendermint.v1beta1.ABCIQueryResponse:
    type: object
    properties:
      code:
        type: integer
        format: int64
      log:
        type: string
        title: nondeterministic
      info:
        type: string
        title: nondeterministic
      index:
        type: string
        format: int64
      key:
        type: string
        format: byte
      value:
        type: string
        format: byte
      proofOps:
        type: object
        properties:
          ops:
            type: array
            items:
              type: object
              properties:
                type:
                  type: string
                key:
                  type: string
                  format: byte
                data:
                  type: string
                  format: byte
              description: >-
                ProofOp defines an operation used for calculating Merkle root.
                The data could

                be arbitrary format, providing necessary data for example
                neighbouring node

                hash.


                Note: This type is a duplicate of the ProofOp proto type defined
                in Tendermint.
        description: >-
          ProofOps is Merkle proof defined by the list of ProofOps.


          Note: This type is a duplicate of the ProofOps proto type defined in
          Tendermint.
      height:
        type: string
        format: int64
      codespace:
        type: string
    description: >-
      ABCIQueryResponse defines the response structure for the ABCIQuery gRPC
      query.


      Note: This type is a duplicate of the ResponseQuery proto type defined in

      Tendermint.
  cosmos.base.tendermint.v1beta1.Block:
    type: object
    properties:
      header:
        type: object
        properties:
          version:
            title: basic block info
            type: object
            properties:
              block:
                type: string
                format: uint64
              app:
                type: string
                format: uint64
            description: >-
              Consensus captures the consensus rules for processing a block in
              the blockchain,

              including all blockchain data structures and the rules of the
              application's

              state transition machine.
          chainId:
            type: string
          height:
            type: string
            format: int64
          time:
            type: string
            format: date-time
          lastBlockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
          lastCommitHash:
            type: string
            format: byte
            description: commit from validators from the last block
            title: hashes of block data
          dataHash:
            type: string
            format: byte
            title: transactions
          validatorsHash:
            type: string
            format: byte
            description: validators for the current block
            title: hashes from the app output from the prev block
          nextValidatorsHash:
            type: string
            format: byte
            title: validators for the next block
          consensusHash:
            type: string
            format: byte
            title: consensus params for current block
          appHash:
            type: string
            format: byte
            title: state after txs from the previous block
          lastResultsHash:
            type: string
            format: byte
            title: root hash of all results from the txs from the previous block
          evidenceHash:
            type: string
            format: byte
            description: evidence included in the block
            title: consensus info
          proposerAddress:
            type: string
            description: >-
              proposer_address is the original block proposer address, formatted
              as a Bech32 string.

              In Tendermint, this type is `bytes`, but in the SDK, we convert it
              to a Bech32 string

              for better UX.


              original proposer of the block
        description: Header defines the structure of a Tendermint block header.
      data:
        type: object
        properties:
          txs:
            type: array
            items:
              type: string
              format: byte
            description: >-
              Txs that will be applied by state @ block.Height+1.

              NOTE: not all txs here are valid.  We're just agreeing on the
              order first.

              This means that block.AppHash does not include these txs.
        title: Data contains the set of transactions included in the block
      evidence:
        type: object
        properties:
          evidence:
            type: array
            items:
              type: object
              properties:
                duplicateVoteEvidence:
                  type: object
                  properties:
                    voteA:
                      type: object
                      properties:
                        type:
                          type: string
                          enum:
                            - SIGNED_MSG_TYPE_UNKNOWN
                            - SIGNED_MSG_TYPE_PREVOTE
                            - SIGNED_MSG_TYPE_PRECOMMIT
                            - SIGNED_MSG_TYPE_PROPOSAL
                          default: SIGNED_MSG_TYPE_UNKNOWN
                          description: >-
                            SignedMsgType is a type of signed message in the
                            consensus.

                             - SIGNED_MSG_TYPE_PREVOTE: Votes
                             - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                        height:
                          type: string
                          format: int64
                        round:
                          type: integer
                          format: int32
                        blockId:
                          type: object
                          properties:
                            hash:
                              type: string
                              format: byte
                            partSetHeader:
                              type: object
                              properties:
                                total:
                                  type: integer
                                  format: int64
                                hash:
                                  type: string
                                  format: byte
                              title: PartsetHeader
                          title: BlockID
                          description: zero if vote is nil.
                        timestamp:
                          type: string
                          format: date-time
                        validatorAddress:
                          type: string
                          format: byte
                        validatorIndex:
                          type: integer
                          format: int32
                        signature:
                          type: string
                          format: byte
                          description: >-
                            Vote signature by the validator if they participated
                            in consensus for the

                            associated block.
                        extension:
                          type: string
                          format: byte
                          description: >-
                            Vote extension provided by the application. Only
                            valid for precommit

                            messages.
                        extensionSignature:
                          type: string
                          format: byte
                          description: >-
                            Vote extension signature by the validator if they
                            participated in

                            consensus for the associated block.

                            Only valid for precommit messages.
                      description: >-
                        Vote represents a prevote or precommit vote from
                        validators for

                        consensus.
                    voteB:
                      type: object
                      properties:
                        type:
                          type: string
                          enum:
                            - SIGNED_MSG_TYPE_UNKNOWN
                            - SIGNED_MSG_TYPE_PREVOTE
                            - SIGNED_MSG_TYPE_PRECOMMIT
                            - SIGNED_MSG_TYPE_PROPOSAL
                          default: SIGNED_MSG_TYPE_UNKNOWN
                          description: >-
                            SignedMsgType is a type of signed message in the
                            consensus.

                             - SIGNED_MSG_TYPE_PREVOTE: Votes
                             - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                        height:
                          type: string
                          format: int64
                        round:
                          type: integer
                          format: int32
                        blockId:
                          type: object
                          properties:
                            hash:
                              type: string
                              format: byte
                            partSetHeader:
                              type: object
                              properties:
                                total:
                                  type: integer
                                  format: int64
                                hash:
                                  type: string
                                  format: byte
                              title: PartsetHeader
                          title: BlockID
                          description: zero if vote is nil.
                        timestamp:
                          type: string
                          format: date-time
                        validatorAddress:
                          type: string
                          format: byte
                        validatorIndex:
                          type: integer
                          format: int32
                        signature:
                          type: string
                          format: byte
                          description: >-
                            Vote signature by the validator if they participated
                            in consensus for the

                            associated block.
                        extension:
                          type: string
                          format: byte
                          description: >-
                            Vote extension provided by the application. Only
                            valid for precommit

                            messages.
                        extensionSignature:
                          type: string
                          format: byte
                          description: >-
                            Vote extension signature by the validator if they
                            participated in

                            consensus for the associated block.

                            Only valid for precommit messages.
                      description: >-
                        Vote represents a prevote or precommit vote from
                        validators for

                        consensus.
                    totalVotingPower:
                      type: string
                      format: int64
                    validatorPower:
                      type: string
                      format: int64
                    timestamp:
                      type: string
                      format: date-time
                  description: >-
                    DuplicateVoteEvidence contains evidence of a validator
                    signed two conflicting votes.
                lightClientAttackEvidence:
                  type: object
                  properties:
                    conflictingBlock:
                      type: object
                      properties:
                        signedHeader:
                          type: object
                          properties:
                            header:
                              type: object
                              properties:
                                version:
                                  title: basic block info
                                  type: object
                                  properties:
                                    block:
                                      type: string
                                      format: uint64
                                    app:
                                      type: string
                                      format: uint64
                                  description: >-
                                    Consensus captures the consensus rules for
                                    processing a block in the blockchain,

                                    including all blockchain data structures and
                                    the rules of the application's

                                    state transition machine.
                                chainId:
                                  type: string
                                height:
                                  type: string
                                  format: int64
                                time:
                                  type: string
                                  format: date-time
                                lastBlockId:
                                  type: object
                                  properties:
                                    hash:
                                      type: string
                                      format: byte
                                    partSetHeader:
                                      type: object
                                      properties:
                                        total:
                                          type: integer
                                          format: int64
                                        hash:
                                          type: string
                                          format: byte
                                      title: PartsetHeader
                                  title: BlockID
                                lastCommitHash:
                                  type: string
                                  format: byte
                                  description: commit from validators from the last block
                                  title: hashes of block data
                                dataHash:
                                  type: string
                                  format: byte
                                  title: transactions
                                validatorsHash:
                                  type: string
                                  format: byte
                                  description: validators for the current block
                                  title: >-
                                    hashes from the app output from the prev
                                    block
                                nextValidatorsHash:
                                  type: string
                                  format: byte
                                  title: validators for the next block
                                consensusHash:
                                  type: string
                                  format: byte
                                  title: consensus params for current block
                                appHash:
                                  type: string
                                  format: byte
                                  title: state after txs from the previous block
                                lastResultsHash:
                                  type: string
                                  format: byte
                                  title: >-
                                    root hash of all results from the txs from
                                    the previous block
                                evidenceHash:
                                  type: string
                                  format: byte
                                  description: evidence included in the block
                                  title: consensus info
                                proposerAddress:
                                  type: string
                                  format: byte
                                  title: original proposer of the block
                              description: Header defines the structure of a block header.
                            commit:
                              type: object
                              properties:
                                height:
                                  type: string
                                  format: int64
                                round:
                                  type: integer
                                  format: int32
                                blockId:
                                  type: object
                                  properties:
                                    hash:
                                      type: string
                                      format: byte
                                    partSetHeader:
                                      type: object
                                      properties:
                                        total:
                                          type: integer
                                          format: int64
                                        hash:
                                          type: string
                                          format: byte
                                      title: PartsetHeader
                                  title: BlockID
                                signatures:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      blockIdFlag:
                                        type: string
                                        enum:
                                          - BLOCK_ID_FLAG_UNKNOWN
                                          - BLOCK_ID_FLAG_ABSENT
                                          - BLOCK_ID_FLAG_COMMIT
                                          - BLOCK_ID_FLAG_NIL
                                        default: BLOCK_ID_FLAG_UNKNOWN
                                        description: >-
                                          - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                          error condition
                                           - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                           - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                           - BLOCK_ID_FLAG_NIL: voted for nil
                                        title: >-
                                          BlockIdFlag indicates which BlockID the
                                          signature is for
                                      validatorAddress:
                                        type: string
                                        format: byte
                                      timestamp:
                                        type: string
                                        format: date-time
                                      signature:
                                        type: string
                                        format: byte
                                    description: >-
                                      CommitSig is a part of the Vote included
                                      in a Commit.
                              description: >-
                                Commit contains the evidence that a block was
                                committed by a set of validators.
                        validatorSet:
                          type: object
                          properties:
                            validators:
                              type: array
                              items:
                                type: object
                                properties:
                                  address:
                                    type: string
                                    format: byte
                                  pubKey:
                                    type: object
                                    properties:
                                      ed25519:
                                        type: string
                                        format: byte
                                      secp256k1:
                                        type: string
                                        format: byte
                                    title: >-
                                      PublicKey defines the keys available for
                                      use with Validators
                                  votingPower:
                                    type: string
                                    format: int64
                                  proposerPriority:
                                    type: string
                                    format: int64
                            proposer:
                              type: object
                              properties:
                                address:
                                  type: string
                                  format: byte
                                pubKey:
                                  type: object
                                  properties:
                                    ed25519:
                                      type: string
                                      format: byte
                                    secp256k1:
                                      type: string
                                      format: byte
                                  title: >-
                                    PublicKey defines the keys available for use
                                    with Validators
                                votingPower:
                                  type: string
                                  format: int64
                                proposerPriority:
                                  type: string
                                  format: int64
                            totalVotingPower:
                              type: string
                              format: int64
                    commonHeight:
                      type: string
                      format: int64
                    byzantineValidators:
                      type: array
                      items:
                        type: object
                        properties:
                          address:
                            type: string
                            format: byte
                          pubKey:
                            type: object
                            properties:
                              ed25519:
                                type: string
                                format: byte
                              secp256k1:
                                type: string
                                format: byte
                            title: >-
                              PublicKey defines the keys available for use with
                              Validators
                          votingPower:
                            type: string
                            format: int64
                          proposerPriority:
                            type: string
                            format: int64
                    totalVotingPower:
                      type: string
                      format: int64
                    timestamp:
                      type: string
                      format: date-time
                  description: >-
                    LightClientAttackEvidence contains evidence of a set of
                    validators attempting to mislead a light client.
      lastCommit:
        type: object
        properties:
          height:
            type: string
            format: int64
          round:
            type: integer
            format: int32
          blockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
          signatures:
            type: array
            items:
              type: object
              properties:
                blockIdFlag:
                  type: string
                  enum:
                    - BLOCK_ID_FLAG_UNKNOWN
                    - BLOCK_ID_FLAG_ABSENT
                    - BLOCK_ID_FLAG_COMMIT
                    - BLOCK_ID_FLAG_NIL
                  default: BLOCK_ID_FLAG_UNKNOWN
                  description: |-
                    - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                     - BLOCK_ID_FLAG_ABSENT: the vote was not received
                     - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                     - BLOCK_ID_FLAG_NIL: voted for nil
                  title: BlockIdFlag indicates which BlockID the signature is for
                validatorAddress:
                  type: string
                  format: byte
                timestamp:
                  type: string
                  format: date-time
                signature:
                  type: string
                  format: byte
              description: CommitSig is a part of the Vote included in a Commit.
        description: >-
          Commit contains the evidence that a block was committed by a set of
          validators.
    description: |-
      Block is tendermint type Block, with the Header proposer address
      field converted to bech32 string.
  cosmos.base.tendermint.v1beta1.GetBlockByHeightResponse:
    type: object
    properties:
      blockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
      block:
        title: 'Deprecated: please use `sdk_block` instead'
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                format: byte
                title: original proposer of the block
            description: Header defines the structure of a block header.
          data:
            type: object
            properties:
              txs:
                type: array
                items:
                  type: string
                  format: byte
                description: >-
                  Txs that will be applied by state @ block.Height+1.

                  NOTE: not all txs here are valid.  We're just agreeing on the
                  order first.

                  This means that block.AppHash does not include these txs.
            title: Data contains the set of transactions included in the block
          evidence:
            type: object
            properties:
              evidence:
                type: array
                items:
                  type: object
                  properties:
                    duplicateVoteEvidence:
                      type: object
                      properties:
                        voteA:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        voteB:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        totalVotingPower:
                          type: string
                          format: int64
                        validatorPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        DuplicateVoteEvidence contains evidence of a validator
                        signed two conflicting votes.
                    lightClientAttackEvidence:
                      type: object
                      properties:
                        conflictingBlock:
                          type: object
                          properties:
                            signedHeader:
                              type: object
                              properties:
                                header:
                                  type: object
                                  properties:
                                    version:
                                      title: basic block info
                                      type: object
                                      properties:
                                        block:
                                          type: string
                                          format: uint64
                                        app:
                                          type: string
                                          format: uint64
                                      description: >-
                                        Consensus captures the consensus rules
                                        for processing a block in the
                                        blockchain,

                                        including all blockchain data structures
                                        and the rules of the application's

                                        state transition machine.
                                    chainId:
                                      type: string
                                    height:
                                      type: string
                                      format: int64
                                    time:
                                      type: string
                                      format: date-time
                                    lastBlockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    lastCommitHash:
                                      type: string
                                      format: byte
                                      description: >-
                                        commit from validators from the last
                                        block
                                      title: hashes of block data
                                    dataHash:
                                      type: string
                                      format: byte
                                      title: transactions
                                    validatorsHash:
                                      type: string
                                      format: byte
                                      description: validators for the current block
                                      title: >-
                                        hashes from the app output from the prev
                                        block
                                    nextValidatorsHash:
                                      type: string
                                      format: byte
                                      title: validators for the next block
                                    consensusHash:
                                      type: string
                                      format: byte
                                      title: consensus params for current block
                                    appHash:
                                      type: string
                                      format: byte
                                      title: state after txs from the previous block
                                    lastResultsHash:
                                      type: string
                                      format: byte
                                      title: >-
                                        root hash of all results from the txs
                                        from the previous block
                                    evidenceHash:
                                      type: string
                                      format: byte
                                      description: evidence included in the block
                                      title: consensus info
                                    proposerAddress:
                                      type: string
                                      format: byte
                                      title: original proposer of the block
                                  description: >-
                                    Header defines the structure of a block
                                    header.
                                commit:
                                  type: object
                                  properties:
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    signatures:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          blockIdFlag:
                                            type: string
                                            enum:
                                              - BLOCK_ID_FLAG_UNKNOWN
                                              - BLOCK_ID_FLAG_ABSENT
                                              - BLOCK_ID_FLAG_COMMIT
                                              - BLOCK_ID_FLAG_NIL
                                            default: BLOCK_ID_FLAG_UNKNOWN
                                            description: >-
                                              - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                              error condition
                                               - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                               - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                               - BLOCK_ID_FLAG_NIL: voted for nil
                                            title: >-
                                              BlockIdFlag indicates which BlockID the
                                              signature is for
                                          validatorAddress:
                                            type: string
                                            format: byte
                                          timestamp:
                                            type: string
                                            format: date-time
                                          signature:
                                            type: string
                                            format: byte
                                        description: >-
                                          CommitSig is a part of the Vote included
                                          in a Commit.
                                  description: >-
                                    Commit contains the evidence that a block
                                    was committed by a set of validators.
                            validatorSet:
                              type: object
                              properties:
                                validators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                proposer:
                                  type: object
                                  properties:
                                    address:
                                      type: string
                                      format: byte
                                    pubKey:
                                      type: object
                                      properties:
                                        ed25519:
                                          type: string
                                          format: byte
                                        secp256k1:
                                          type: string
                                          format: byte
                                      title: >-
                                        PublicKey defines the keys available for
                                        use with Validators
                                    votingPower:
                                      type: string
                                      format: int64
                                    proposerPriority:
                                      type: string
                                      format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                        commonHeight:
                          type: string
                          format: int64
                        byzantineValidators:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                format: byte
                              pubKey:
                                type: object
                                properties:
                                  ed25519:
                                    type: string
                                    format: byte
                                  secp256k1:
                                    type: string
                                    format: byte
                                title: >-
                                  PublicKey defines the keys available for use
                                  with Validators
                              votingPower:
                                type: string
                                format: int64
                              proposerPriority:
                                type: string
                                format: int64
                        totalVotingPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        LightClientAttackEvidence contains evidence of a set of
                        validators attempting to mislead a light client.
          lastCommit:
            type: object
            properties:
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              signatures:
                type: array
                items:
                  type: object
                  properties:
                    blockIdFlag:
                      type: string
                      enum:
                        - BLOCK_ID_FLAG_UNKNOWN
                        - BLOCK_ID_FLAG_ABSENT
                        - BLOCK_ID_FLAG_COMMIT
                        - BLOCK_ID_FLAG_NIL
                      default: BLOCK_ID_FLAG_UNKNOWN
                      description: |-
                        - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                         - BLOCK_ID_FLAG_ABSENT: the vote was not received
                         - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                         - BLOCK_ID_FLAG_NIL: voted for nil
                      title: BlockIdFlag indicates which BlockID the signature is for
                    validatorAddress:
                      type: string
                      format: byte
                    timestamp:
                      type: string
                      format: date-time
                    signature:
                      type: string
                      format: byte
                  description: CommitSig is a part of the Vote included in a Commit.
            description: >-
              Commit contains the evidence that a block was committed by a set
              of validators.
      sdkBlock:
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                description: >-
                  proposer_address is the original block proposer address,
                  formatted as a Bech32 string.

                  In Tendermint, this type is `bytes`, but in the SDK, we
                  convert it to a Bech32 string

                  for better UX.


                  original proposer of the block
            description: Header defines the structure of a Tendermint block header.
          data:
            type: object
            properties:
              txs:
                type: array
                items:
                  type: string
                  format: byte
                description: >-
                  Txs that will be applied by state @ block.Height+1.

                  NOTE: not all txs here are valid.  We're just agreeing on the
                  order first.

                  This means that block.AppHash does not include these txs.
            title: Data contains the set of transactions included in the block
          evidence:
            type: object
            properties:
              evidence:
                type: array
                items:
                  type: object
                  properties:
                    duplicateVoteEvidence:
                      type: object
                      properties:
                        voteA:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        voteB:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        totalVotingPower:
                          type: string
                          format: int64
                        validatorPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        DuplicateVoteEvidence contains evidence of a validator
                        signed two conflicting votes.
                    lightClientAttackEvidence:
                      type: object
                      properties:
                        conflictingBlock:
                          type: object
                          properties:
                            signedHeader:
                              type: object
                              properties:
                                header:
                                  type: object
                                  properties:
                                    version:
                                      title: basic block info
                                      type: object
                                      properties:
                                        block:
                                          type: string
                                          format: uint64
                                        app:
                                          type: string
                                          format: uint64
                                      description: >-
                                        Consensus captures the consensus rules
                                        for processing a block in the
                                        blockchain,

                                        including all blockchain data structures
                                        and the rules of the application's

                                        state transition machine.
                                    chainId:
                                      type: string
                                    height:
                                      type: string
                                      format: int64
                                    time:
                                      type: string
                                      format: date-time
                                    lastBlockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    lastCommitHash:
                                      type: string
                                      format: byte
                                      description: >-
                                        commit from validators from the last
                                        block
                                      title: hashes of block data
                                    dataHash:
                                      type: string
                                      format: byte
                                      title: transactions
                                    validatorsHash:
                                      type: string
                                      format: byte
                                      description: validators for the current block
                                      title: >-
                                        hashes from the app output from the prev
                                        block
                                    nextValidatorsHash:
                                      type: string
                                      format: byte
                                      title: validators for the next block
                                    consensusHash:
                                      type: string
                                      format: byte
                                      title: consensus params for current block
                                    appHash:
                                      type: string
                                      format: byte
                                      title: state after txs from the previous block
                                    lastResultsHash:
                                      type: string
                                      format: byte
                                      title: >-
                                        root hash of all results from the txs
                                        from the previous block
                                    evidenceHash:
                                      type: string
                                      format: byte
                                      description: evidence included in the block
                                      title: consensus info
                                    proposerAddress:
                                      type: string
                                      format: byte
                                      title: original proposer of the block
                                  description: >-
                                    Header defines the structure of a block
                                    header.
                                commit:
                                  type: object
                                  properties:
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    signatures:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          blockIdFlag:
                                            type: string
                                            enum:
                                              - BLOCK_ID_FLAG_UNKNOWN
                                              - BLOCK_ID_FLAG_ABSENT
                                              - BLOCK_ID_FLAG_COMMIT
                                              - BLOCK_ID_FLAG_NIL
                                            default: BLOCK_ID_FLAG_UNKNOWN
                                            description: >-
                                              - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                              error condition
                                               - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                               - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                               - BLOCK_ID_FLAG_NIL: voted for nil
                                            title: >-
                                              BlockIdFlag indicates which BlockID the
                                              signature is for
                                          validatorAddress:
                                            type: string
                                            format: byte
                                          timestamp:
                                            type: string
                                            format: date-time
                                          signature:
                                            type: string
                                            format: byte
                                        description: >-
                                          CommitSig is a part of the Vote included
                                          in a Commit.
                                  description: >-
                                    Commit contains the evidence that a block
                                    was committed by a set of validators.
                            validatorSet:
                              type: object
                              properties:
                                validators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                proposer:
                                  type: object
                                  properties:
                                    address:
                                      type: string
                                      format: byte
                                    pubKey:
                                      type: object
                                      properties:
                                        ed25519:
                                          type: string
                                          format: byte
                                        secp256k1:
                                          type: string
                                          format: byte
                                      title: >-
                                        PublicKey defines the keys available for
                                        use with Validators
                                    votingPower:
                                      type: string
                                      format: int64
                                    proposerPriority:
                                      type: string
                                      format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                        commonHeight:
                          type: string
                          format: int64
                        byzantineValidators:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                format: byte
                              pubKey:
                                type: object
                                properties:
                                  ed25519:
                                    type: string
                                    format: byte
                                  secp256k1:
                                    type: string
                                    format: byte
                                title: >-
                                  PublicKey defines the keys available for use
                                  with Validators
                              votingPower:
                                type: string
                                format: int64
                              proposerPriority:
                                type: string
                                format: int64
                        totalVotingPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        LightClientAttackEvidence contains evidence of a set of
                        validators attempting to mislead a light client.
          lastCommit:
            type: object
            properties:
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              signatures:
                type: array
                items:
                  type: object
                  properties:
                    blockIdFlag:
                      type: string
                      enum:
                        - BLOCK_ID_FLAG_UNKNOWN
                        - BLOCK_ID_FLAG_ABSENT
                        - BLOCK_ID_FLAG_COMMIT
                        - BLOCK_ID_FLAG_NIL
                      default: BLOCK_ID_FLAG_UNKNOWN
                      description: |-
                        - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                         - BLOCK_ID_FLAG_ABSENT: the vote was not received
                         - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                         - BLOCK_ID_FLAG_NIL: voted for nil
                      title: BlockIdFlag indicates which BlockID the signature is for
                    validatorAddress:
                      type: string
                      format: byte
                    timestamp:
                      type: string
                      format: date-time
                    signature:
                      type: string
                      format: byte
                  description: CommitSig is a part of the Vote included in a Commit.
            description: >-
              Commit contains the evidence that a block was committed by a set
              of validators.
        description: |-
          Block is tendermint type Block, with the Header proposer address
          field converted to bech32 string.
    description: >-
      GetBlockByHeightResponse is the response type for the
      Query/GetBlockByHeight RPC method.
  cosmos.base.tendermint.v1beta1.GetLatestBlockResponse:
    type: object
    properties:
      blockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
      block:
        title: 'Deprecated: please use `sdk_block` instead'
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                format: byte
                title: original proposer of the block
            description: Header defines the structure of a block header.
          data:
            type: object
            properties:
              txs:
                type: array
                items:
                  type: string
                  format: byte
                description: >-
                  Txs that will be applied by state @ block.Height+1.

                  NOTE: not all txs here are valid.  We're just agreeing on the
                  order first.

                  This means that block.AppHash does not include these txs.
            title: Data contains the set of transactions included in the block
          evidence:
            type: object
            properties:
              evidence:
                type: array
                items:
                  type: object
                  properties:
                    duplicateVoteEvidence:
                      type: object
                      properties:
                        voteA:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        voteB:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        totalVotingPower:
                          type: string
                          format: int64
                        validatorPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        DuplicateVoteEvidence contains evidence of a validator
                        signed two conflicting votes.
                    lightClientAttackEvidence:
                      type: object
                      properties:
                        conflictingBlock:
                          type: object
                          properties:
                            signedHeader:
                              type: object
                              properties:
                                header:
                                  type: object
                                  properties:
                                    version:
                                      title: basic block info
                                      type: object
                                      properties:
                                        block:
                                          type: string
                                          format: uint64
                                        app:
                                          type: string
                                          format: uint64
                                      description: >-
                                        Consensus captures the consensus rules
                                        for processing a block in the
                                        blockchain,

                                        including all blockchain data structures
                                        and the rules of the application's

                                        state transition machine.
                                    chainId:
                                      type: string
                                    height:
                                      type: string
                                      format: int64
                                    time:
                                      type: string
                                      format: date-time
                                    lastBlockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    lastCommitHash:
                                      type: string
                                      format: byte
                                      description: >-
                                        commit from validators from the last
                                        block
                                      title: hashes of block data
                                    dataHash:
                                      type: string
                                      format: byte
                                      title: transactions
                                    validatorsHash:
                                      type: string
                                      format: byte
                                      description: validators for the current block
                                      title: >-
                                        hashes from the app output from the prev
                                        block
                                    nextValidatorsHash:
                                      type: string
                                      format: byte
                                      title: validators for the next block
                                    consensusHash:
                                      type: string
                                      format: byte
                                      title: consensus params for current block
                                    appHash:
                                      type: string
                                      format: byte
                                      title: state after txs from the previous block
                                    lastResultsHash:
                                      type: string
                                      format: byte
                                      title: >-
                                        root hash of all results from the txs
                                        from the previous block
                                    evidenceHash:
                                      type: string
                                      format: byte
                                      description: evidence included in the block
                                      title: consensus info
                                    proposerAddress:
                                      type: string
                                      format: byte
                                      title: original proposer of the block
                                  description: >-
                                    Header defines the structure of a block
                                    header.
                                commit:
                                  type: object
                                  properties:
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    signatures:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          blockIdFlag:
                                            type: string
                                            enum:
                                              - BLOCK_ID_FLAG_UNKNOWN
                                              - BLOCK_ID_FLAG_ABSENT
                                              - BLOCK_ID_FLAG_COMMIT
                                              - BLOCK_ID_FLAG_NIL
                                            default: BLOCK_ID_FLAG_UNKNOWN
                                            description: >-
                                              - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                              error condition
                                               - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                               - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                               - BLOCK_ID_FLAG_NIL: voted for nil
                                            title: >-
                                              BlockIdFlag indicates which BlockID the
                                              signature is for
                                          validatorAddress:
                                            type: string
                                            format: byte
                                          timestamp:
                                            type: string
                                            format: date-time
                                          signature:
                                            type: string
                                            format: byte
                                        description: >-
                                          CommitSig is a part of the Vote included
                                          in a Commit.
                                  description: >-
                                    Commit contains the evidence that a block
                                    was committed by a set of validators.
                            validatorSet:
                              type: object
                              properties:
                                validators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                proposer:
                                  type: object
                                  properties:
                                    address:
                                      type: string
                                      format: byte
                                    pubKey:
                                      type: object
                                      properties:
                                        ed25519:
                                          type: string
                                          format: byte
                                        secp256k1:
                                          type: string
                                          format: byte
                                      title: >-
                                        PublicKey defines the keys available for
                                        use with Validators
                                    votingPower:
                                      type: string
                                      format: int64
                                    proposerPriority:
                                      type: string
                                      format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                        commonHeight:
                          type: string
                          format: int64
                        byzantineValidators:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                format: byte
                              pubKey:
                                type: object
                                properties:
                                  ed25519:
                                    type: string
                                    format: byte
                                  secp256k1:
                                    type: string
                                    format: byte
                                title: >-
                                  PublicKey defines the keys available for use
                                  with Validators
                              votingPower:
                                type: string
                                format: int64
                              proposerPriority:
                                type: string
                                format: int64
                        totalVotingPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        LightClientAttackEvidence contains evidence of a set of
                        validators attempting to mislead a light client.
          lastCommit:
            type: object
            properties:
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              signatures:
                type: array
                items:
                  type: object
                  properties:
                    blockIdFlag:
                      type: string
                      enum:
                        - BLOCK_ID_FLAG_UNKNOWN
                        - BLOCK_ID_FLAG_ABSENT
                        - BLOCK_ID_FLAG_COMMIT
                        - BLOCK_ID_FLAG_NIL
                      default: BLOCK_ID_FLAG_UNKNOWN
                      description: |-
                        - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                         - BLOCK_ID_FLAG_ABSENT: the vote was not received
                         - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                         - BLOCK_ID_FLAG_NIL: voted for nil
                      title: BlockIdFlag indicates which BlockID the signature is for
                    validatorAddress:
                      type: string
                      format: byte
                    timestamp:
                      type: string
                      format: date-time
                    signature:
                      type: string
                      format: byte
                  description: CommitSig is a part of the Vote included in a Commit.
            description: >-
              Commit contains the evidence that a block was committed by a set
              of validators.
      sdkBlock:
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                description: >-
                  proposer_address is the original block proposer address,
                  formatted as a Bech32 string.

                  In Tendermint, this type is `bytes`, but in the SDK, we
                  convert it to a Bech32 string

                  for better UX.


                  original proposer of the block
            description: Header defines the structure of a Tendermint block header.
          data:
            type: object
            properties:
              txs:
                type: array
                items:
                  type: string
                  format: byte
                description: >-
                  Txs that will be applied by state @ block.Height+1.

                  NOTE: not all txs here are valid.  We're just agreeing on the
                  order first.

                  This means that block.AppHash does not include these txs.
            title: Data contains the set of transactions included in the block
          evidence:
            type: object
            properties:
              evidence:
                type: array
                items:
                  type: object
                  properties:
                    duplicateVoteEvidence:
                      type: object
                      properties:
                        voteA:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        voteB:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        totalVotingPower:
                          type: string
                          format: int64
                        validatorPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        DuplicateVoteEvidence contains evidence of a validator
                        signed two conflicting votes.
                    lightClientAttackEvidence:
                      type: object
                      properties:
                        conflictingBlock:
                          type: object
                          properties:
                            signedHeader:
                              type: object
                              properties:
                                header:
                                  type: object
                                  properties:
                                    version:
                                      title: basic block info
                                      type: object
                                      properties:
                                        block:
                                          type: string
                                          format: uint64
                                        app:
                                          type: string
                                          format: uint64
                                      description: >-
                                        Consensus captures the consensus rules
                                        for processing a block in the
                                        blockchain,

                                        including all blockchain data structures
                                        and the rules of the application's

                                        state transition machine.
                                    chainId:
                                      type: string
                                    height:
                                      type: string
                                      format: int64
                                    time:
                                      type: string
                                      format: date-time
                                    lastBlockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    lastCommitHash:
                                      type: string
                                      format: byte
                                      description: >-
                                        commit from validators from the last
                                        block
                                      title: hashes of block data
                                    dataHash:
                                      type: string
                                      format: byte
                                      title: transactions
                                    validatorsHash:
                                      type: string
                                      format: byte
                                      description: validators for the current block
                                      title: >-
                                        hashes from the app output from the prev
                                        block
                                    nextValidatorsHash:
                                      type: string
                                      format: byte
                                      title: validators for the next block
                                    consensusHash:
                                      type: string
                                      format: byte
                                      title: consensus params for current block
                                    appHash:
                                      type: string
                                      format: byte
                                      title: state after txs from the previous block
                                    lastResultsHash:
                                      type: string
                                      format: byte
                                      title: >-
                                        root hash of all results from the txs
                                        from the previous block
                                    evidenceHash:
                                      type: string
                                      format: byte
                                      description: evidence included in the block
                                      title: consensus info
                                    proposerAddress:
                                      type: string
                                      format: byte
                                      title: original proposer of the block
                                  description: >-
                                    Header defines the structure of a block
                                    header.
                                commit:
                                  type: object
                                  properties:
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    signatures:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          blockIdFlag:
                                            type: string
                                            enum:
                                              - BLOCK_ID_FLAG_UNKNOWN
                                              - BLOCK_ID_FLAG_ABSENT
                                              - BLOCK_ID_FLAG_COMMIT
                                              - BLOCK_ID_FLAG_NIL
                                            default: BLOCK_ID_FLAG_UNKNOWN
                                            description: >-
                                              - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                              error condition
                                               - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                               - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                               - BLOCK_ID_FLAG_NIL: voted for nil
                                            title: >-
                                              BlockIdFlag indicates which BlockID the
                                              signature is for
                                          validatorAddress:
                                            type: string
                                            format: byte
                                          timestamp:
                                            type: string
                                            format: date-time
                                          signature:
                                            type: string
                                            format: byte
                                        description: >-
                                          CommitSig is a part of the Vote included
                                          in a Commit.
                                  description: >-
                                    Commit contains the evidence that a block
                                    was committed by a set of validators.
                            validatorSet:
                              type: object
                              properties:
                                validators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                proposer:
                                  type: object
                                  properties:
                                    address:
                                      type: string
                                      format: byte
                                    pubKey:
                                      type: object
                                      properties:
                                        ed25519:
                                          type: string
                                          format: byte
                                        secp256k1:
                                          type: string
                                          format: byte
                                      title: >-
                                        PublicKey defines the keys available for
                                        use with Validators
                                    votingPower:
                                      type: string
                                      format: int64
                                    proposerPriority:
                                      type: string
                                      format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                        commonHeight:
                          type: string
                          format: int64
                        byzantineValidators:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                format: byte
                              pubKey:
                                type: object
                                properties:
                                  ed25519:
                                    type: string
                                    format: byte
                                  secp256k1:
                                    type: string
                                    format: byte
                                title: >-
                                  PublicKey defines the keys available for use
                                  with Validators
                              votingPower:
                                type: string
                                format: int64
                              proposerPriority:
                                type: string
                                format: int64
                        totalVotingPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        LightClientAttackEvidence contains evidence of a set of
                        validators attempting to mislead a light client.
          lastCommit:
            type: object
            properties:
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              signatures:
                type: array
                items:
                  type: object
                  properties:
                    blockIdFlag:
                      type: string
                      enum:
                        - BLOCK_ID_FLAG_UNKNOWN
                        - BLOCK_ID_FLAG_ABSENT
                        - BLOCK_ID_FLAG_COMMIT
                        - BLOCK_ID_FLAG_NIL
                      default: BLOCK_ID_FLAG_UNKNOWN
                      description: |-
                        - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                         - BLOCK_ID_FLAG_ABSENT: the vote was not received
                         - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                         - BLOCK_ID_FLAG_NIL: voted for nil
                      title: BlockIdFlag indicates which BlockID the signature is for
                    validatorAddress:
                      type: string
                      format: byte
                    timestamp:
                      type: string
                      format: date-time
                    signature:
                      type: string
                      format: byte
                  description: CommitSig is a part of the Vote included in a Commit.
            description: >-
              Commit contains the evidence that a block was committed by a set
              of validators.
        description: |-
          Block is tendermint type Block, with the Header proposer address
          field converted to bech32 string.
    description: >-
      GetLatestBlockResponse is the response type for the Query/GetLatestBlock
      RPC method.
  cosmos.base.tendermint.v1beta1.GetLatestValidatorSetResponse:
    type: object
    properties:
      blockHeight:
        type: string
        format: int64
      validators:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            pubKey:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            votingPower:
              type: string
              format: int64
            proposerPriority:
              type: string
              format: int64
          description: Validator is the type for the validator-set.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      GetLatestValidatorSetResponse is the response type for the
      Query/GetValidatorSetByHeight RPC method.
  cosmos.base.tendermint.v1beta1.GetNodeInfoResponse:
    type: object
    properties:
      defaultNodeInfo:
        type: object
        properties:
          protocolVersion:
            type: object
            properties:
              p2p:
                type: string
                format: uint64
              block:
                type: string
                format: uint64
              app:
                type: string
                format: uint64
          defaultNodeId:
            type: string
          listenAddr:
            type: string
          network:
            type: string
          version:
            type: string
          channels:
            type: string
            format: byte
          moniker:
            type: string
          other:
            type: object
            properties:
              txIndex:
                type: string
              rpcAddress:
                type: string
      applicationVersion:
        type: object
        properties:
          name:
            type: string
          appName:
            type: string
          version:
            type: string
          gitCommit:
            type: string
          buildTags:
            type: string
          goVersion:
            type: string
          buildDeps:
            type: array
            items:
              type: object
              properties:
                path:
                  type: string
                  title: module path
                version:
                  type: string
                  title: module version
                sum:
                  type: string
                  title: checksum
              title: Module is the type for VersionInfo
          cosmosSdkVersion:
            type: string
        description: VersionInfo is the type for the GetNodeInfoResponse message.
    description: >-
      GetNodeInfoResponse is the response type for the Query/GetNodeInfo RPC
      method.
  cosmos.base.tendermint.v1beta1.GetSyncingResponse:
    type: object
    properties:
      syncing:
        type: boolean
    description: >-
      GetSyncingResponse is the response type for the Query/GetSyncing RPC
      method.
  cosmos.base.tendermint.v1beta1.GetValidatorSetByHeightResponse:
    type: object
    properties:
      blockHeight:
        type: string
        format: int64
      validators:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            pubKey:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            votingPower:
              type: string
              format: int64
            proposerPriority:
              type: string
              format: int64
          description: Validator is the type for the validator-set.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      GetValidatorSetByHeightResponse is the response type for the
      Query/GetValidatorSetByHeight RPC method.
  cosmos.base.tendermint.v1beta1.Header:
    type: object
    properties:
      version:
        title: basic block info
        type: object
        properties:
          block:
            type: string
            format: uint64
          app:
            type: string
            format: uint64
        description: >-
          Consensus captures the consensus rules for processing a block in the
          blockchain,

          including all blockchain data structures and the rules of the
          application's

          state transition machine.
      chainId:
        type: string
      height:
        type: string
        format: int64
      time:
        type: string
        format: date-time
      lastBlockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
      lastCommitHash:
        type: string
        format: byte
        description: commit from validators from the last block
        title: hashes of block data
      dataHash:
        type: string
        format: byte
        title: transactions
      validatorsHash:
        type: string
        format: byte
        description: validators for the current block
        title: hashes from the app output from the prev block
      nextValidatorsHash:
        type: string
        format: byte
        title: validators for the next block
      consensusHash:
        type: string
        format: byte
        title: consensus params for current block
      appHash:
        type: string
        format: byte
        title: state after txs from the previous block
      lastResultsHash:
        type: string
        format: byte
        title: root hash of all results from the txs from the previous block
      evidenceHash:
        type: string
        format: byte
        description: evidence included in the block
        title: consensus info
      proposerAddress:
        type: string
        description: >-
          proposer_address is the original block proposer address, formatted as
          a Bech32 string.

          In Tendermint, this type is `bytes`, but in the SDK, we convert it to
          a Bech32 string

          for better UX.


          original proposer of the block
    description: Header defines the structure of a Tendermint block header.
  cosmos.base.tendermint.v1beta1.Module:
    type: object
    properties:
      path:
        type: string
        title: module path
      version:
        type: string
        title: module version
      sum:
        type: string
        title: checksum
    title: Module is the type for VersionInfo
  cosmos.base.tendermint.v1beta1.ProofOp:
    type: object
    properties:
      type:
        type: string
      key:
        type: string
        format: byte
      data:
        type: string
        format: byte
    description: >-
      ProofOp defines an operation used for calculating Merkle root. The data
      could

      be arbitrary format, providing necessary data for example neighbouring
      node

      hash.


      Note: This type is a duplicate of the ProofOp proto type defined in
      Tendermint.
  cosmos.base.tendermint.v1beta1.ProofOps:
    type: object
    properties:
      ops:
        type: array
        items:
          type: object
          properties:
            type:
              type: string
            key:
              type: string
              format: byte
            data:
              type: string
              format: byte
          description: >-
            ProofOp defines an operation used for calculating Merkle root. The
            data could

            be arbitrary format, providing necessary data for example
            neighbouring node

            hash.


            Note: This type is a duplicate of the ProofOp proto type defined in
            Tendermint.
    description: >-
      ProofOps is Merkle proof defined by the list of ProofOps.


      Note: This type is a duplicate of the ProofOps proto type defined in
      Tendermint.
  cosmos.base.tendermint.v1beta1.Validator:
    type: object
    properties:
      address:
        type: string
      pubKey:
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
      votingPower:
        type: string
        format: int64
      proposerPriority:
        type: string
        format: int64
    description: Validator is the type for the validator-set.
  cosmos.base.tendermint.v1beta1.VersionInfo:
    type: object
    properties:
      name:
        type: string
      appName:
        type: string
      version:
        type: string
      gitCommit:
        type: string
      buildTags:
        type: string
      goVersion:
        type: string
      buildDeps:
        type: array
        items:
          type: object
          properties:
            path:
              type: string
              title: module path
            version:
              type: string
              title: module version
            sum:
              type: string
              title: checksum
          title: Module is the type for VersionInfo
      cosmosSdkVersion:
        type: string
    description: VersionInfo is the type for the GetNodeInfoResponse message.
  tendermint.crypto.PublicKey:
    type: object
    properties:
      ed25519:
        type: string
        format: byte
      secp256k1:
        type: string
        format: byte
    title: PublicKey defines the keys available for use with Validators
  tendermint.p2p.DefaultNodeInfo:
    type: object
    properties:
      protocolVersion:
        type: object
        properties:
          p2p:
            type: string
            format: uint64
          block:
            type: string
            format: uint64
          app:
            type: string
            format: uint64
      defaultNodeId:
        type: string
      listenAddr:
        type: string
      network:
        type: string
      version:
        type: string
      channels:
        type: string
        format: byte
      moniker:
        type: string
      other:
        type: object
        properties:
          txIndex:
            type: string
          rpcAddress:
            type: string
  tendermint.p2p.DefaultNodeInfoOther:
    type: object
    properties:
      txIndex:
        type: string
      rpcAddress:
        type: string
  tendermint.p2p.ProtocolVersion:
    type: object
    properties:
      p2p:
        type: string
        format: uint64
      block:
        type: string
        format: uint64
      app:
        type: string
        format: uint64
  tendermint.types.Block:
    type: object
    properties:
      header:
        type: object
        properties:
          version:
            title: basic block info
            type: object
            properties:
              block:
                type: string
                format: uint64
              app:
                type: string
                format: uint64
            description: >-
              Consensus captures the consensus rules for processing a block in
              the blockchain,

              including all blockchain data structures and the rules of the
              application's

              state transition machine.
          chainId:
            type: string
          height:
            type: string
            format: int64
          time:
            type: string
            format: date-time
          lastBlockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
          lastCommitHash:
            type: string
            format: byte
            description: commit from validators from the last block
            title: hashes of block data
          dataHash:
            type: string
            format: byte
            title: transactions
          validatorsHash:
            type: string
            format: byte
            description: validators for the current block
            title: hashes from the app output from the prev block
          nextValidatorsHash:
            type: string
            format: byte
            title: validators for the next block
          consensusHash:
            type: string
            format: byte
            title: consensus params for current block
          appHash:
            type: string
            format: byte
            title: state after txs from the previous block
          lastResultsHash:
            type: string
            format: byte
            title: root hash of all results from the txs from the previous block
          evidenceHash:
            type: string
            format: byte
            description: evidence included in the block
            title: consensus info
          proposerAddress:
            type: string
            format: byte
            title: original proposer of the block
        description: Header defines the structure of a block header.
      data:
        type: object
        properties:
          txs:
            type: array
            items:
              type: string
              format: byte
            description: >-
              Txs that will be applied by state @ block.Height+1.

              NOTE: not all txs here are valid.  We're just agreeing on the
              order first.

              This means that block.AppHash does not include these txs.
        title: Data contains the set of transactions included in the block
      evidence:
        type: object
        properties:
          evidence:
            type: array
            items:
              type: object
              properties:
                duplicateVoteEvidence:
                  type: object
                  properties:
                    voteA:
                      type: object
                      properties:
                        type:
                          type: string
                          enum:
                            - SIGNED_MSG_TYPE_UNKNOWN
                            - SIGNED_MSG_TYPE_PREVOTE
                            - SIGNED_MSG_TYPE_PRECOMMIT
                            - SIGNED_MSG_TYPE_PROPOSAL
                          default: SIGNED_MSG_TYPE_UNKNOWN
                          description: >-
                            SignedMsgType is a type of signed message in the
                            consensus.

                             - SIGNED_MSG_TYPE_PREVOTE: Votes
                             - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                        height:
                          type: string
                          format: int64
                        round:
                          type: integer
                          format: int32
                        blockId:
                          type: object
                          properties:
                            hash:
                              type: string
                              format: byte
                            partSetHeader:
                              type: object
                              properties:
                                total:
                                  type: integer
                                  format: int64
                                hash:
                                  type: string
                                  format: byte
                              title: PartsetHeader
                          title: BlockID
                          description: zero if vote is nil.
                        timestamp:
                          type: string
                          format: date-time
                        validatorAddress:
                          type: string
                          format: byte
                        validatorIndex:
                          type: integer
                          format: int32
                        signature:
                          type: string
                          format: byte
                          description: >-
                            Vote signature by the validator if they participated
                            in consensus for the

                            associated block.
                        extension:
                          type: string
                          format: byte
                          description: >-
                            Vote extension provided by the application. Only
                            valid for precommit

                            messages.
                        extensionSignature:
                          type: string
                          format: byte
                          description: >-
                            Vote extension signature by the validator if they
                            participated in

                            consensus for the associated block.

                            Only valid for precommit messages.
                      description: >-
                        Vote represents a prevote or precommit vote from
                        validators for

                        consensus.
                    voteB:
                      type: object
                      properties:
                        type:
                          type: string
                          enum:
                            - SIGNED_MSG_TYPE_UNKNOWN
                            - SIGNED_MSG_TYPE_PREVOTE
                            - SIGNED_MSG_TYPE_PRECOMMIT
                            - SIGNED_MSG_TYPE_PROPOSAL
                          default: SIGNED_MSG_TYPE_UNKNOWN
                          description: >-
                            SignedMsgType is a type of signed message in the
                            consensus.

                             - SIGNED_MSG_TYPE_PREVOTE: Votes
                             - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                        height:
                          type: string
                          format: int64
                        round:
                          type: integer
                          format: int32
                        blockId:
                          type: object
                          properties:
                            hash:
                              type: string
                              format: byte
                            partSetHeader:
                              type: object
                              properties:
                                total:
                                  type: integer
                                  format: int64
                                hash:
                                  type: string
                                  format: byte
                              title: PartsetHeader
                          title: BlockID
                          description: zero if vote is nil.
                        timestamp:
                          type: string
                          format: date-time
                        validatorAddress:
                          type: string
                          format: byte
                        validatorIndex:
                          type: integer
                          format: int32
                        signature:
                          type: string
                          format: byte
                          description: >-
                            Vote signature by the validator if they participated
                            in consensus for the

                            associated block.
                        extension:
                          type: string
                          format: byte
                          description: >-
                            Vote extension provided by the application. Only
                            valid for precommit

                            messages.
                        extensionSignature:
                          type: string
                          format: byte
                          description: >-
                            Vote extension signature by the validator if they
                            participated in

                            consensus for the associated block.

                            Only valid for precommit messages.
                      description: >-
                        Vote represents a prevote or precommit vote from
                        validators for

                        consensus.
                    totalVotingPower:
                      type: string
                      format: int64
                    validatorPower:
                      type: string
                      format: int64
                    timestamp:
                      type: string
                      format: date-time
                  description: >-
                    DuplicateVoteEvidence contains evidence of a validator
                    signed two conflicting votes.
                lightClientAttackEvidence:
                  type: object
                  properties:
                    conflictingBlock:
                      type: object
                      properties:
                        signedHeader:
                          type: object
                          properties:
                            header:
                              type: object
                              properties:
                                version:
                                  title: basic block info
                                  type: object
                                  properties:
                                    block:
                                      type: string
                                      format: uint64
                                    app:
                                      type: string
                                      format: uint64
                                  description: >-
                                    Consensus captures the consensus rules for
                                    processing a block in the blockchain,

                                    including all blockchain data structures and
                                    the rules of the application's

                                    state transition machine.
                                chainId:
                                  type: string
                                height:
                                  type: string
                                  format: int64
                                time:
                                  type: string
                                  format: date-time
                                lastBlockId:
                                  type: object
                                  properties:
                                    hash:
                                      type: string
                                      format: byte
                                    partSetHeader:
                                      type: object
                                      properties:
                                        total:
                                          type: integer
                                          format: int64
                                        hash:
                                          type: string
                                          format: byte
                                      title: PartsetHeader
                                  title: BlockID
                                lastCommitHash:
                                  type: string
                                  format: byte
                                  description: commit from validators from the last block
                                  title: hashes of block data
                                dataHash:
                                  type: string
                                  format: byte
                                  title: transactions
                                validatorsHash:
                                  type: string
                                  format: byte
                                  description: validators for the current block
                                  title: >-
                                    hashes from the app output from the prev
                                    block
                                nextValidatorsHash:
                                  type: string
                                  format: byte
                                  title: validators for the next block
                                consensusHash:
                                  type: string
                                  format: byte
                                  title: consensus params for current block
                                appHash:
                                  type: string
                                  format: byte
                                  title: state after txs from the previous block
                                lastResultsHash:
                                  type: string
                                  format: byte
                                  title: >-
                                    root hash of all results from the txs from
                                    the previous block
                                evidenceHash:
                                  type: string
                                  format: byte
                                  description: evidence included in the block
                                  title: consensus info
                                proposerAddress:
                                  type: string
                                  format: byte
                                  title: original proposer of the block
                              description: Header defines the structure of a block header.
                            commit:
                              type: object
                              properties:
                                height:
                                  type: string
                                  format: int64
                                round:
                                  type: integer
                                  format: int32
                                blockId:
                                  type: object
                                  properties:
                                    hash:
                                      type: string
                                      format: byte
                                    partSetHeader:
                                      type: object
                                      properties:
                                        total:
                                          type: integer
                                          format: int64
                                        hash:
                                          type: string
                                          format: byte
                                      title: PartsetHeader
                                  title: BlockID
                                signatures:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      blockIdFlag:
                                        type: string
                                        enum:
                                          - BLOCK_ID_FLAG_UNKNOWN
                                          - BLOCK_ID_FLAG_ABSENT
                                          - BLOCK_ID_FLAG_COMMIT
                                          - BLOCK_ID_FLAG_NIL
                                        default: BLOCK_ID_FLAG_UNKNOWN
                                        description: >-
                                          - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                          error condition
                                           - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                           - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                           - BLOCK_ID_FLAG_NIL: voted for nil
                                        title: >-
                                          BlockIdFlag indicates which BlockID the
                                          signature is for
                                      validatorAddress:
                                        type: string
                                        format: byte
                                      timestamp:
                                        type: string
                                        format: date-time
                                      signature:
                                        type: string
                                        format: byte
                                    description: >-
                                      CommitSig is a part of the Vote included
                                      in a Commit.
                              description: >-
                                Commit contains the evidence that a block was
                                committed by a set of validators.
                        validatorSet:
                          type: object
                          properties:
                            validators:
                              type: array
                              items:
                                type: object
                                properties:
                                  address:
                                    type: string
                                    format: byte
                                  pubKey:
                                    type: object
                                    properties:
                                      ed25519:
                                        type: string
                                        format: byte
                                      secp256k1:
                                        type: string
                                        format: byte
                                    title: >-
                                      PublicKey defines the keys available for
                                      use with Validators
                                  votingPower:
                                    type: string
                                    format: int64
                                  proposerPriority:
                                    type: string
                                    format: int64
                            proposer:
                              type: object
                              properties:
                                address:
                                  type: string
                                  format: byte
                                pubKey:
                                  type: object
                                  properties:
                                    ed25519:
                                      type: string
                                      format: byte
                                    secp256k1:
                                      type: string
                                      format: byte
                                  title: >-
                                    PublicKey defines the keys available for use
                                    with Validators
                                votingPower:
                                  type: string
                                  format: int64
                                proposerPriority:
                                  type: string
                                  format: int64
                            totalVotingPower:
                              type: string
                              format: int64
                    commonHeight:
                      type: string
                      format: int64
                    byzantineValidators:
                      type: array
                      items:
                        type: object
                        properties:
                          address:
                            type: string
                            format: byte
                          pubKey:
                            type: object
                            properties:
                              ed25519:
                                type: string
                                format: byte
                              secp256k1:
                                type: string
                                format: byte
                            title: >-
                              PublicKey defines the keys available for use with
                              Validators
                          votingPower:
                            type: string
                            format: int64
                          proposerPriority:
                            type: string
                            format: int64
                    totalVotingPower:
                      type: string
                      format: int64
                    timestamp:
                      type: string
                      format: date-time
                  description: >-
                    LightClientAttackEvidence contains evidence of a set of
                    validators attempting to mislead a light client.
      lastCommit:
        type: object
        properties:
          height:
            type: string
            format: int64
          round:
            type: integer
            format: int32
          blockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
          signatures:
            type: array
            items:
              type: object
              properties:
                blockIdFlag:
                  type: string
                  enum:
                    - BLOCK_ID_FLAG_UNKNOWN
                    - BLOCK_ID_FLAG_ABSENT
                    - BLOCK_ID_FLAG_COMMIT
                    - BLOCK_ID_FLAG_NIL
                  default: BLOCK_ID_FLAG_UNKNOWN
                  description: |-
                    - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                     - BLOCK_ID_FLAG_ABSENT: the vote was not received
                     - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                     - BLOCK_ID_FLAG_NIL: voted for nil
                  title: BlockIdFlag indicates which BlockID the signature is for
                validatorAddress:
                  type: string
                  format: byte
                timestamp:
                  type: string
                  format: date-time
                signature:
                  type: string
                  format: byte
              description: CommitSig is a part of the Vote included in a Commit.
        description: >-
          Commit contains the evidence that a block was committed by a set of
          validators.
  tendermint.types.BlockID:
    type: object
    properties:
      hash:
        type: string
        format: byte
      partSetHeader:
        type: object
        properties:
          total:
            type: integer
            format: int64
          hash:
            type: string
            format: byte
        title: PartsetHeader
    title: BlockID
  tendermint.types.BlockIDFlag:
    type: string
    enum:
      - BLOCK_ID_FLAG_UNKNOWN
      - BLOCK_ID_FLAG_ABSENT
      - BLOCK_ID_FLAG_COMMIT
      - BLOCK_ID_FLAG_NIL
    default: BLOCK_ID_FLAG_UNKNOWN
    description: |-
      - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
       - BLOCK_ID_FLAG_ABSENT: the vote was not received
       - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
       - BLOCK_ID_FLAG_NIL: voted for nil
    title: BlockIdFlag indicates which BlockID the signature is for
  tendermint.types.Commit:
    type: object
    properties:
      height:
        type: string
        format: int64
      round:
        type: integer
        format: int32
      blockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
      signatures:
        type: array
        items:
          type: object
          properties:
            blockIdFlag:
              type: string
              enum:
                - BLOCK_ID_FLAG_UNKNOWN
                - BLOCK_ID_FLAG_ABSENT
                - BLOCK_ID_FLAG_COMMIT
                - BLOCK_ID_FLAG_NIL
              default: BLOCK_ID_FLAG_UNKNOWN
              description: |-
                - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                 - BLOCK_ID_FLAG_ABSENT: the vote was not received
                 - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                 - BLOCK_ID_FLAG_NIL: voted for nil
              title: BlockIdFlag indicates which BlockID the signature is for
            validatorAddress:
              type: string
              format: byte
            timestamp:
              type: string
              format: date-time
            signature:
              type: string
              format: byte
          description: CommitSig is a part of the Vote included in a Commit.
    description: >-
      Commit contains the evidence that a block was committed by a set of
      validators.
  tendermint.types.CommitSig:
    type: object
    properties:
      blockIdFlag:
        type: string
        enum:
          - BLOCK_ID_FLAG_UNKNOWN
          - BLOCK_ID_FLAG_ABSENT
          - BLOCK_ID_FLAG_COMMIT
          - BLOCK_ID_FLAG_NIL
        default: BLOCK_ID_FLAG_UNKNOWN
        description: |-
          - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
           - BLOCK_ID_FLAG_ABSENT: the vote was not received
           - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
           - BLOCK_ID_FLAG_NIL: voted for nil
        title: BlockIdFlag indicates which BlockID the signature is for
      validatorAddress:
        type: string
        format: byte
      timestamp:
        type: string
        format: date-time
      signature:
        type: string
        format: byte
    description: CommitSig is a part of the Vote included in a Commit.
  tendermint.types.Data:
    type: object
    properties:
      txs:
        type: array
        items:
          type: string
          format: byte
        description: >-
          Txs that will be applied by state @ block.Height+1.

          NOTE: not all txs here are valid.  We're just agreeing on the order
          first.

          This means that block.AppHash does not include these txs.
    title: Data contains the set of transactions included in the block
  tendermint.types.DuplicateVoteEvidence:
    type: object
    properties:
      voteA:
        type: object
        properties:
          type:
            type: string
            enum:
              - SIGNED_MSG_TYPE_UNKNOWN
              - SIGNED_MSG_TYPE_PREVOTE
              - SIGNED_MSG_TYPE_PRECOMMIT
              - SIGNED_MSG_TYPE_PROPOSAL
            default: SIGNED_MSG_TYPE_UNKNOWN
            description: |-
              SignedMsgType is a type of signed message in the consensus.

               - SIGNED_MSG_TYPE_PREVOTE: Votes
               - SIGNED_MSG_TYPE_PROPOSAL: Proposals
          height:
            type: string
            format: int64
          round:
            type: integer
            format: int32
          blockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
            description: zero if vote is nil.
          timestamp:
            type: string
            format: date-time
          validatorAddress:
            type: string
            format: byte
          validatorIndex:
            type: integer
            format: int32
          signature:
            type: string
            format: byte
            description: >-
              Vote signature by the validator if they participated in consensus
              for the

              associated block.
          extension:
            type: string
            format: byte
            description: >-
              Vote extension provided by the application. Only valid for
              precommit

              messages.
          extensionSignature:
            type: string
            format: byte
            description: |-
              Vote extension signature by the validator if they participated in
              consensus for the associated block.
              Only valid for precommit messages.
        description: |-
          Vote represents a prevote or precommit vote from validators for
          consensus.
      voteB:
        type: object
        properties:
          type:
            type: string
            enum:
              - SIGNED_MSG_TYPE_UNKNOWN
              - SIGNED_MSG_TYPE_PREVOTE
              - SIGNED_MSG_TYPE_PRECOMMIT
              - SIGNED_MSG_TYPE_PROPOSAL
            default: SIGNED_MSG_TYPE_UNKNOWN
            description: |-
              SignedMsgType is a type of signed message in the consensus.

               - SIGNED_MSG_TYPE_PREVOTE: Votes
               - SIGNED_MSG_TYPE_PROPOSAL: Proposals
          height:
            type: string
            format: int64
          round:
            type: integer
            format: int32
          blockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
            description: zero if vote is nil.
          timestamp:
            type: string
            format: date-time
          validatorAddress:
            type: string
            format: byte
          validatorIndex:
            type: integer
            format: int32
          signature:
            type: string
            format: byte
            description: >-
              Vote signature by the validator if they participated in consensus
              for the

              associated block.
          extension:
            type: string
            format: byte
            description: >-
              Vote extension provided by the application. Only valid for
              precommit

              messages.
          extensionSignature:
            type: string
            format: byte
            description: |-
              Vote extension signature by the validator if they participated in
              consensus for the associated block.
              Only valid for precommit messages.
        description: |-
          Vote represents a prevote or precommit vote from validators for
          consensus.
      totalVotingPower:
        type: string
        format: int64
      validatorPower:
        type: string
        format: int64
      timestamp:
        type: string
        format: date-time
    description: >-
      DuplicateVoteEvidence contains evidence of a validator signed two
      conflicting votes.
  tendermint.types.Evidence:
    type: object
    properties:
      duplicateVoteEvidence:
        type: object
        properties:
          voteA:
            type: object
            properties:
              type:
                type: string
                enum:
                  - SIGNED_MSG_TYPE_UNKNOWN
                  - SIGNED_MSG_TYPE_PREVOTE
                  - SIGNED_MSG_TYPE_PRECOMMIT
                  - SIGNED_MSG_TYPE_PROPOSAL
                default: SIGNED_MSG_TYPE_UNKNOWN
                description: |-
                  SignedMsgType is a type of signed message in the consensus.

                   - SIGNED_MSG_TYPE_PREVOTE: Votes
                   - SIGNED_MSG_TYPE_PROPOSAL: Proposals
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
                description: zero if vote is nil.
              timestamp:
                type: string
                format: date-time
              validatorAddress:
                type: string
                format: byte
              validatorIndex:
                type: integer
                format: int32
              signature:
                type: string
                format: byte
                description: >-
                  Vote signature by the validator if they participated in
                  consensus for the

                  associated block.
              extension:
                type: string
                format: byte
                description: >-
                  Vote extension provided by the application. Only valid for
                  precommit

                  messages.
              extensionSignature:
                type: string
                format: byte
                description: >-
                  Vote extension signature by the validator if they participated
                  in

                  consensus for the associated block.

                  Only valid for precommit messages.
            description: |-
              Vote represents a prevote or precommit vote from validators for
              consensus.
          voteB:
            type: object
            properties:
              type:
                type: string
                enum:
                  - SIGNED_MSG_TYPE_UNKNOWN
                  - SIGNED_MSG_TYPE_PREVOTE
                  - SIGNED_MSG_TYPE_PRECOMMIT
                  - SIGNED_MSG_TYPE_PROPOSAL
                default: SIGNED_MSG_TYPE_UNKNOWN
                description: |-
                  SignedMsgType is a type of signed message in the consensus.

                   - SIGNED_MSG_TYPE_PREVOTE: Votes
                   - SIGNED_MSG_TYPE_PROPOSAL: Proposals
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
                description: zero if vote is nil.
              timestamp:
                type: string
                format: date-time
              validatorAddress:
                type: string
                format: byte
              validatorIndex:
                type: integer
                format: int32
              signature:
                type: string
                format: byte
                description: >-
                  Vote signature by the validator if they participated in
                  consensus for the

                  associated block.
              extension:
                type: string
                format: byte
                description: >-
                  Vote extension provided by the application. Only valid for
                  precommit

                  messages.
              extensionSignature:
                type: string
                format: byte
                description: >-
                  Vote extension signature by the validator if they participated
                  in

                  consensus for the associated block.

                  Only valid for precommit messages.
            description: |-
              Vote represents a prevote or precommit vote from validators for
              consensus.
          totalVotingPower:
            type: string
            format: int64
          validatorPower:
            type: string
            format: int64
          timestamp:
            type: string
            format: date-time
        description: >-
          DuplicateVoteEvidence contains evidence of a validator signed two
          conflicting votes.
      lightClientAttackEvidence:
        type: object
        properties:
          conflictingBlock:
            type: object
            properties:
              signedHeader:
                type: object
                properties:
                  header:
                    type: object
                    properties:
                      version:
                        title: basic block info
                        type: object
                        properties:
                          block:
                            type: string
                            format: uint64
                          app:
                            type: string
                            format: uint64
                        description: >-
                          Consensus captures the consensus rules for processing
                          a block in the blockchain,

                          including all blockchain data structures and the rules
                          of the application's

                          state transition machine.
                      chainId:
                        type: string
                      height:
                        type: string
                        format: int64
                      time:
                        type: string
                        format: date-time
                      lastBlockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      lastCommitHash:
                        type: string
                        format: byte
                        description: commit from validators from the last block
                        title: hashes of block data
                      dataHash:
                        type: string
                        format: byte
                        title: transactions
                      validatorsHash:
                        type: string
                        format: byte
                        description: validators for the current block
                        title: hashes from the app output from the prev block
                      nextValidatorsHash:
                        type: string
                        format: byte
                        title: validators for the next block
                      consensusHash:
                        type: string
                        format: byte
                        title: consensus params for current block
                      appHash:
                        type: string
                        format: byte
                        title: state after txs from the previous block
                      lastResultsHash:
                        type: string
                        format: byte
                        title: >-
                          root hash of all results from the txs from the
                          previous block
                      evidenceHash:
                        type: string
                        format: byte
                        description: evidence included in the block
                        title: consensus info
                      proposerAddress:
                        type: string
                        format: byte
                        title: original proposer of the block
                    description: Header defines the structure of a block header.
                  commit:
                    type: object
                    properties:
                      height:
                        type: string
                        format: int64
                      round:
                        type: integer
                        format: int32
                      blockId:
                        type: object
                        properties:
                          hash:
                            type: string
                            format: byte
                          partSetHeader:
                            type: object
                            properties:
                              total:
                                type: integer
                                format: int64
                              hash:
                                type: string
                                format: byte
                            title: PartsetHeader
                        title: BlockID
                      signatures:
                        type: array
                        items:
                          type: object
                          properties:
                            blockIdFlag:
                              type: string
                              enum:
                                - BLOCK_ID_FLAG_UNKNOWN
                                - BLOCK_ID_FLAG_ABSENT
                                - BLOCK_ID_FLAG_COMMIT
                                - BLOCK_ID_FLAG_NIL
                              default: BLOCK_ID_FLAG_UNKNOWN
                              description: >-
                                - BLOCK_ID_FLAG_UNKNOWN: indicates an error
                                condition
                                 - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                 - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                 - BLOCK_ID_FLAG_NIL: voted for nil
                              title: >-
                                BlockIdFlag indicates which BlockID the
                                signature is for
                            validatorAddress:
                              type: string
                              format: byte
                            timestamp:
                              type: string
                              format: date-time
                            signature:
                              type: string
                              format: byte
                          description: >-
                            CommitSig is a part of the Vote included in a
                            Commit.
                    description: >-
                      Commit contains the evidence that a block was committed by
                      a set of validators.
              validatorSet:
                type: object
                properties:
                  validators:
                    type: array
                    items:
                      type: object
                      properties:
                        address:
                          type: string
                          format: byte
                        pubKey:
                          type: object
                          properties:
                            ed25519:
                              type: string
                              format: byte
                            secp256k1:
                              type: string
                              format: byte
                          title: >-
                            PublicKey defines the keys available for use with
                            Validators
                        votingPower:
                          type: string
                          format: int64
                        proposerPriority:
                          type: string
                          format: int64
                  proposer:
                    type: object
                    properties:
                      address:
                        type: string
                        format: byte
                      pubKey:
                        type: object
                        properties:
                          ed25519:
                            type: string
                            format: byte
                          secp256k1:
                            type: string
                            format: byte
                        title: >-
                          PublicKey defines the keys available for use with
                          Validators
                      votingPower:
                        type: string
                        format: int64
                      proposerPriority:
                        type: string
                        format: int64
                  totalVotingPower:
                    type: string
                    format: int64
          commonHeight:
            type: string
            format: int64
          byzantineValidators:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                  format: byte
                pubKey:
                  type: object
                  properties:
                    ed25519:
                      type: string
                      format: byte
                    secp256k1:
                      type: string
                      format: byte
                  title: PublicKey defines the keys available for use with Validators
                votingPower:
                  type: string
                  format: int64
                proposerPriority:
                  type: string
                  format: int64
          totalVotingPower:
            type: string
            format: int64
          timestamp:
            type: string
            format: date-time
        description: >-
          LightClientAttackEvidence contains evidence of a set of validators
          attempting to mislead a light client.
  tendermint.types.EvidenceList:
    type: object
    properties:
      evidence:
        type: array
        items:
          type: object
          properties:
            duplicateVoteEvidence:
              type: object
              properties:
                voteA:
                  type: object
                  properties:
                    type:
                      type: string
                      enum:
                        - SIGNED_MSG_TYPE_UNKNOWN
                        - SIGNED_MSG_TYPE_PREVOTE
                        - SIGNED_MSG_TYPE_PRECOMMIT
                        - SIGNED_MSG_TYPE_PROPOSAL
                      default: SIGNED_MSG_TYPE_UNKNOWN
                      description: >-
                        SignedMsgType is a type of signed message in the
                        consensus.

                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                    height:
                      type: string
                      format: int64
                    round:
                      type: integer
                      format: int32
                    blockId:
                      type: object
                      properties:
                        hash:
                          type: string
                          format: byte
                        partSetHeader:
                          type: object
                          properties:
                            total:
                              type: integer
                              format: int64
                            hash:
                              type: string
                              format: byte
                          title: PartsetHeader
                      title: BlockID
                      description: zero if vote is nil.
                    timestamp:
                      type: string
                      format: date-time
                    validatorAddress:
                      type: string
                      format: byte
                    validatorIndex:
                      type: integer
                      format: int32
                    signature:
                      type: string
                      format: byte
                      description: >-
                        Vote signature by the validator if they participated in
                        consensus for the

                        associated block.
                    extension:
                      type: string
                      format: byte
                      description: >-
                        Vote extension provided by the application. Only valid
                        for precommit

                        messages.
                    extensionSignature:
                      type: string
                      format: byte
                      description: >-
                        Vote extension signature by the validator if they
                        participated in

                        consensus for the associated block.

                        Only valid for precommit messages.
                  description: >-
                    Vote represents a prevote or precommit vote from validators
                    for

                    consensus.
                voteB:
                  type: object
                  properties:
                    type:
                      type: string
                      enum:
                        - SIGNED_MSG_TYPE_UNKNOWN
                        - SIGNED_MSG_TYPE_PREVOTE
                        - SIGNED_MSG_TYPE_PRECOMMIT
                        - SIGNED_MSG_TYPE_PROPOSAL
                      default: SIGNED_MSG_TYPE_UNKNOWN
                      description: >-
                        SignedMsgType is a type of signed message in the
                        consensus.

                         - SIGNED_MSG_TYPE_PREVOTE: Votes
                         - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                    height:
                      type: string
                      format: int64
                    round:
                      type: integer
                      format: int32
                    blockId:
                      type: object
                      properties:
                        hash:
                          type: string
                          format: byte
                        partSetHeader:
                          type: object
                          properties:
                            total:
                              type: integer
                              format: int64
                            hash:
                              type: string
                              format: byte
                          title: PartsetHeader
                      title: BlockID
                      description: zero if vote is nil.
                    timestamp:
                      type: string
                      format: date-time
                    validatorAddress:
                      type: string
                      format: byte
                    validatorIndex:
                      type: integer
                      format: int32
                    signature:
                      type: string
                      format: byte
                      description: >-
                        Vote signature by the validator if they participated in
                        consensus for the

                        associated block.
                    extension:
                      type: string
                      format: byte
                      description: >-
                        Vote extension provided by the application. Only valid
                        for precommit

                        messages.
                    extensionSignature:
                      type: string
                      format: byte
                      description: >-
                        Vote extension signature by the validator if they
                        participated in

                        consensus for the associated block.

                        Only valid for precommit messages.
                  description: >-
                    Vote represents a prevote or precommit vote from validators
                    for

                    consensus.
                totalVotingPower:
                  type: string
                  format: int64
                validatorPower:
                  type: string
                  format: int64
                timestamp:
                  type: string
                  format: date-time
              description: >-
                DuplicateVoteEvidence contains evidence of a validator signed
                two conflicting votes.
            lightClientAttackEvidence:
              type: object
              properties:
                conflictingBlock:
                  type: object
                  properties:
                    signedHeader:
                      type: object
                      properties:
                        header:
                          type: object
                          properties:
                            version:
                              title: basic block info
                              type: object
                              properties:
                                block:
                                  type: string
                                  format: uint64
                                app:
                                  type: string
                                  format: uint64
                              description: >-
                                Consensus captures the consensus rules for
                                processing a block in the blockchain,

                                including all blockchain data structures and the
                                rules of the application's

                                state transition machine.
                            chainId:
                              type: string
                            height:
                              type: string
                              format: int64
                            time:
                              type: string
                              format: date-time
                            lastBlockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                            lastCommitHash:
                              type: string
                              format: byte
                              description: commit from validators from the last block
                              title: hashes of block data
                            dataHash:
                              type: string
                              format: byte
                              title: transactions
                            validatorsHash:
                              type: string
                              format: byte
                              description: validators for the current block
                              title: hashes from the app output from the prev block
                            nextValidatorsHash:
                              type: string
                              format: byte
                              title: validators for the next block
                            consensusHash:
                              type: string
                              format: byte
                              title: consensus params for current block
                            appHash:
                              type: string
                              format: byte
                              title: state after txs from the previous block
                            lastResultsHash:
                              type: string
                              format: byte
                              title: >-
                                root hash of all results from the txs from the
                                previous block
                            evidenceHash:
                              type: string
                              format: byte
                              description: evidence included in the block
                              title: consensus info
                            proposerAddress:
                              type: string
                              format: byte
                              title: original proposer of the block
                          description: Header defines the structure of a block header.
                        commit:
                          type: object
                          properties:
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                            signatures:
                              type: array
                              items:
                                type: object
                                properties:
                                  blockIdFlag:
                                    type: string
                                    enum:
                                      - BLOCK_ID_FLAG_UNKNOWN
                                      - BLOCK_ID_FLAG_ABSENT
                                      - BLOCK_ID_FLAG_COMMIT
                                      - BLOCK_ID_FLAG_NIL
                                    default: BLOCK_ID_FLAG_UNKNOWN
                                    description: >-
                                      - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                      error condition
                                       - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                       - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                       - BLOCK_ID_FLAG_NIL: voted for nil
                                    title: >-
                                      BlockIdFlag indicates which BlockID the
                                      signature is for
                                  validatorAddress:
                                    type: string
                                    format: byte
                                  timestamp:
                                    type: string
                                    format: date-time
                                  signature:
                                    type: string
                                    format: byte
                                description: >-
                                  CommitSig is a part of the Vote included in a
                                  Commit.
                          description: >-
                            Commit contains the evidence that a block was
                            committed by a set of validators.
                    validatorSet:
                      type: object
                      properties:
                        validators:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                format: byte
                              pubKey:
                                type: object
                                properties:
                                  ed25519:
                                    type: string
                                    format: byte
                                  secp256k1:
                                    type: string
                                    format: byte
                                title: >-
                                  PublicKey defines the keys available for use
                                  with Validators
                              votingPower:
                                type: string
                                format: int64
                              proposerPriority:
                                type: string
                                format: int64
                        proposer:
                          type: object
                          properties:
                            address:
                              type: string
                              format: byte
                            pubKey:
                              type: object
                              properties:
                                ed25519:
                                  type: string
                                  format: byte
                                secp256k1:
                                  type: string
                                  format: byte
                              title: >-
                                PublicKey defines the keys available for use
                                with Validators
                            votingPower:
                              type: string
                              format: int64
                            proposerPriority:
                              type: string
                              format: int64
                        totalVotingPower:
                          type: string
                          format: int64
                commonHeight:
                  type: string
                  format: int64
                byzantineValidators:
                  type: array
                  items:
                    type: object
                    properties:
                      address:
                        type: string
                        format: byte
                      pubKey:
                        type: object
                        properties:
                          ed25519:
                            type: string
                            format: byte
                          secp256k1:
                            type: string
                            format: byte
                        title: >-
                          PublicKey defines the keys available for use with
                          Validators
                      votingPower:
                        type: string
                        format: int64
                      proposerPriority:
                        type: string
                        format: int64
                totalVotingPower:
                  type: string
                  format: int64
                timestamp:
                  type: string
                  format: date-time
              description: >-
                LightClientAttackEvidence contains evidence of a set of
                validators attempting to mislead a light client.
  tendermint.types.Header:
    type: object
    properties:
      version:
        title: basic block info
        type: object
        properties:
          block:
            type: string
            format: uint64
          app:
            type: string
            format: uint64
        description: >-
          Consensus captures the consensus rules for processing a block in the
          blockchain,

          including all blockchain data structures and the rules of the
          application's

          state transition machine.
      chainId:
        type: string
      height:
        type: string
        format: int64
      time:
        type: string
        format: date-time
      lastBlockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
      lastCommitHash:
        type: string
        format: byte
        description: commit from validators from the last block
        title: hashes of block data
      dataHash:
        type: string
        format: byte
        title: transactions
      validatorsHash:
        type: string
        format: byte
        description: validators for the current block
        title: hashes from the app output from the prev block
      nextValidatorsHash:
        type: string
        format: byte
        title: validators for the next block
      consensusHash:
        type: string
        format: byte
        title: consensus params for current block
      appHash:
        type: string
        format: byte
        title: state after txs from the previous block
      lastResultsHash:
        type: string
        format: byte
        title: root hash of all results from the txs from the previous block
      evidenceHash:
        type: string
        format: byte
        description: evidence included in the block
        title: consensus info
      proposerAddress:
        type: string
        format: byte
        title: original proposer of the block
    description: Header defines the structure of a block header.
  tendermint.types.LightBlock:
    type: object
    properties:
      signedHeader:
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                format: byte
                title: original proposer of the block
            description: Header defines the structure of a block header.
          commit:
            type: object
            properties:
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              signatures:
                type: array
                items:
                  type: object
                  properties:
                    blockIdFlag:
                      type: string
                      enum:
                        - BLOCK_ID_FLAG_UNKNOWN
                        - BLOCK_ID_FLAG_ABSENT
                        - BLOCK_ID_FLAG_COMMIT
                        - BLOCK_ID_FLAG_NIL
                      default: BLOCK_ID_FLAG_UNKNOWN
                      description: |-
                        - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                         - BLOCK_ID_FLAG_ABSENT: the vote was not received
                         - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                         - BLOCK_ID_FLAG_NIL: voted for nil
                      title: BlockIdFlag indicates which BlockID the signature is for
                    validatorAddress:
                      type: string
                      format: byte
                    timestamp:
                      type: string
                      format: date-time
                    signature:
                      type: string
                      format: byte
                  description: CommitSig is a part of the Vote included in a Commit.
            description: >-
              Commit contains the evidence that a block was committed by a set
              of validators.
      validatorSet:
        type: object
        properties:
          validators:
            type: array
            items:
              type: object
              properties:
                address:
                  type: string
                  format: byte
                pubKey:
                  type: object
                  properties:
                    ed25519:
                      type: string
                      format: byte
                    secp256k1:
                      type: string
                      format: byte
                  title: PublicKey defines the keys available for use with Validators
                votingPower:
                  type: string
                  format: int64
                proposerPriority:
                  type: string
                  format: int64
          proposer:
            type: object
            properties:
              address:
                type: string
                format: byte
              pubKey:
                type: object
                properties:
                  ed25519:
                    type: string
                    format: byte
                  secp256k1:
                    type: string
                    format: byte
                title: PublicKey defines the keys available for use with Validators
              votingPower:
                type: string
                format: int64
              proposerPriority:
                type: string
                format: int64
          totalVotingPower:
            type: string
            format: int64
  tendermint.types.LightClientAttackEvidence:
    type: object
    properties:
      conflictingBlock:
        type: object
        properties:
          signedHeader:
            type: object
            properties:
              header:
                type: object
                properties:
                  version:
                    title: basic block info
                    type: object
                    properties:
                      block:
                        type: string
                        format: uint64
                      app:
                        type: string
                        format: uint64
                    description: >-
                      Consensus captures the consensus rules for processing a
                      block in the blockchain,

                      including all blockchain data structures and the rules of
                      the application's

                      state transition machine.
                  chainId:
                    type: string
                  height:
                    type: string
                    format: int64
                  time:
                    type: string
                    format: date-time
                  lastBlockId:
                    type: object
                    properties:
                      hash:
                        type: string
                        format: byte
                      partSetHeader:
                        type: object
                        properties:
                          total:
                            type: integer
                            format: int64
                          hash:
                            type: string
                            format: byte
                        title: PartsetHeader
                    title: BlockID
                  lastCommitHash:
                    type: string
                    format: byte
                    description: commit from validators from the last block
                    title: hashes of block data
                  dataHash:
                    type: string
                    format: byte
                    title: transactions
                  validatorsHash:
                    type: string
                    format: byte
                    description: validators for the current block
                    title: hashes from the app output from the prev block
                  nextValidatorsHash:
                    type: string
                    format: byte
                    title: validators for the next block
                  consensusHash:
                    type: string
                    format: byte
                    title: consensus params for current block
                  appHash:
                    type: string
                    format: byte
                    title: state after txs from the previous block
                  lastResultsHash:
                    type: string
                    format: byte
                    title: >-
                      root hash of all results from the txs from the previous
                      block
                  evidenceHash:
                    type: string
                    format: byte
                    description: evidence included in the block
                    title: consensus info
                  proposerAddress:
                    type: string
                    format: byte
                    title: original proposer of the block
                description: Header defines the structure of a block header.
              commit:
                type: object
                properties:
                  height:
                    type: string
                    format: int64
                  round:
                    type: integer
                    format: int32
                  blockId:
                    type: object
                    properties:
                      hash:
                        type: string
                        format: byte
                      partSetHeader:
                        type: object
                        properties:
                          total:
                            type: integer
                            format: int64
                          hash:
                            type: string
                            format: byte
                        title: PartsetHeader
                    title: BlockID
                  signatures:
                    type: array
                    items:
                      type: object
                      properties:
                        blockIdFlag:
                          type: string
                          enum:
                            - BLOCK_ID_FLAG_UNKNOWN
                            - BLOCK_ID_FLAG_ABSENT
                            - BLOCK_ID_FLAG_COMMIT
                            - BLOCK_ID_FLAG_NIL
                          default: BLOCK_ID_FLAG_UNKNOWN
                          description: >-
                            - BLOCK_ID_FLAG_UNKNOWN: indicates an error
                            condition
                             - BLOCK_ID_FLAG_ABSENT: the vote was not received
                             - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                             - BLOCK_ID_FLAG_NIL: voted for nil
                          title: >-
                            BlockIdFlag indicates which BlockID the signature is
                            for
                        validatorAddress:
                          type: string
                          format: byte
                        timestamp:
                          type: string
                          format: date-time
                        signature:
                          type: string
                          format: byte
                      description: CommitSig is a part of the Vote included in a Commit.
                description: >-
                  Commit contains the evidence that a block was committed by a
                  set of validators.
          validatorSet:
            type: object
            properties:
              validators:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      format: byte
                    pubKey:
                      type: object
                      properties:
                        ed25519:
                          type: string
                          format: byte
                        secp256k1:
                          type: string
                          format: byte
                      title: >-
                        PublicKey defines the keys available for use with
                        Validators
                    votingPower:
                      type: string
                      format: int64
                    proposerPriority:
                      type: string
                      format: int64
              proposer:
                type: object
                properties:
                  address:
                    type: string
                    format: byte
                  pubKey:
                    type: object
                    properties:
                      ed25519:
                        type: string
                        format: byte
                      secp256k1:
                        type: string
                        format: byte
                    title: >-
                      PublicKey defines the keys available for use with
                      Validators
                  votingPower:
                    type: string
                    format: int64
                  proposerPriority:
                    type: string
                    format: int64
              totalVotingPower:
                type: string
                format: int64
      commonHeight:
        type: string
        format: int64
      byzantineValidators:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              format: byte
            pubKey:
              type: object
              properties:
                ed25519:
                  type: string
                  format: byte
                secp256k1:
                  type: string
                  format: byte
              title: PublicKey defines the keys available for use with Validators
            votingPower:
              type: string
              format: int64
            proposerPriority:
              type: string
              format: int64
      totalVotingPower:
        type: string
        format: int64
      timestamp:
        type: string
        format: date-time
    description: >-
      LightClientAttackEvidence contains evidence of a set of validators
      attempting to mislead a light client.
  tendermint.types.PartSetHeader:
    type: object
    properties:
      total:
        type: integer
        format: int64
      hash:
        type: string
        format: byte
    title: PartsetHeader
  tendermint.types.SignedHeader:
    type: object
    properties:
      header:
        type: object
        properties:
          version:
            title: basic block info
            type: object
            properties:
              block:
                type: string
                format: uint64
              app:
                type: string
                format: uint64
            description: >-
              Consensus captures the consensus rules for processing a block in
              the blockchain,

              including all blockchain data structures and the rules of the
              application's

              state transition machine.
          chainId:
            type: string
          height:
            type: string
            format: int64
          time:
            type: string
            format: date-time
          lastBlockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
          lastCommitHash:
            type: string
            format: byte
            description: commit from validators from the last block
            title: hashes of block data
          dataHash:
            type: string
            format: byte
            title: transactions
          validatorsHash:
            type: string
            format: byte
            description: validators for the current block
            title: hashes from the app output from the prev block
          nextValidatorsHash:
            type: string
            format: byte
            title: validators for the next block
          consensusHash:
            type: string
            format: byte
            title: consensus params for current block
          appHash:
            type: string
            format: byte
            title: state after txs from the previous block
          lastResultsHash:
            type: string
            format: byte
            title: root hash of all results from the txs from the previous block
          evidenceHash:
            type: string
            format: byte
            description: evidence included in the block
            title: consensus info
          proposerAddress:
            type: string
            format: byte
            title: original proposer of the block
        description: Header defines the structure of a block header.
      commit:
        type: object
        properties:
          height:
            type: string
            format: int64
          round:
            type: integer
            format: int32
          blockId:
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
            title: BlockID
          signatures:
            type: array
            items:
              type: object
              properties:
                blockIdFlag:
                  type: string
                  enum:
                    - BLOCK_ID_FLAG_UNKNOWN
                    - BLOCK_ID_FLAG_ABSENT
                    - BLOCK_ID_FLAG_COMMIT
                    - BLOCK_ID_FLAG_NIL
                  default: BLOCK_ID_FLAG_UNKNOWN
                  description: |-
                    - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                     - BLOCK_ID_FLAG_ABSENT: the vote was not received
                     - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                     - BLOCK_ID_FLAG_NIL: voted for nil
                  title: BlockIdFlag indicates which BlockID the signature is for
                validatorAddress:
                  type: string
                  format: byte
                timestamp:
                  type: string
                  format: date-time
                signature:
                  type: string
                  format: byte
              description: CommitSig is a part of the Vote included in a Commit.
        description: >-
          Commit contains the evidence that a block was committed by a set of
          validators.
  tendermint.types.SignedMsgType:
    type: string
    enum:
      - SIGNED_MSG_TYPE_UNKNOWN
      - SIGNED_MSG_TYPE_PREVOTE
      - SIGNED_MSG_TYPE_PRECOMMIT
      - SIGNED_MSG_TYPE_PROPOSAL
    default: SIGNED_MSG_TYPE_UNKNOWN
    description: |-
      SignedMsgType is a type of signed message in the consensus.

       - SIGNED_MSG_TYPE_PREVOTE: Votes
       - SIGNED_MSG_TYPE_PROPOSAL: Proposals
  tendermint.types.Validator:
    type: object
    properties:
      address:
        type: string
        format: byte
      pubKey:
        type: object
        properties:
          ed25519:
            type: string
            format: byte
          secp256k1:
            type: string
            format: byte
        title: PublicKey defines the keys available for use with Validators
      votingPower:
        type: string
        format: int64
      proposerPriority:
        type: string
        format: int64
  tendermint.types.ValidatorSet:
    type: object
    properties:
      validators:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              format: byte
            pubKey:
              type: object
              properties:
                ed25519:
                  type: string
                  format: byte
                secp256k1:
                  type: string
                  format: byte
              title: PublicKey defines the keys available for use with Validators
            votingPower:
              type: string
              format: int64
            proposerPriority:
              type: string
              format: int64
      proposer:
        type: object
        properties:
          address:
            type: string
            format: byte
          pubKey:
            type: object
            properties:
              ed25519:
                type: string
                format: byte
              secp256k1:
                type: string
                format: byte
            title: PublicKey defines the keys available for use with Validators
          votingPower:
            type: string
            format: int64
          proposerPriority:
            type: string
            format: int64
      totalVotingPower:
        type: string
        format: int64
  tendermint.types.Vote:
    type: object
    properties:
      type:
        type: string
        enum:
          - SIGNED_MSG_TYPE_UNKNOWN
          - SIGNED_MSG_TYPE_PREVOTE
          - SIGNED_MSG_TYPE_PRECOMMIT
          - SIGNED_MSG_TYPE_PROPOSAL
        default: SIGNED_MSG_TYPE_UNKNOWN
        description: |-
          SignedMsgType is a type of signed message in the consensus.

           - SIGNED_MSG_TYPE_PREVOTE: Votes
           - SIGNED_MSG_TYPE_PROPOSAL: Proposals
      height:
        type: string
        format: int64
      round:
        type: integer
        format: int32
      blockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
        description: zero if vote is nil.
      timestamp:
        type: string
        format: date-time
      validatorAddress:
        type: string
        format: byte
      validatorIndex:
        type: integer
        format: int32
      signature:
        type: string
        format: byte
        description: >-
          Vote signature by the validator if they participated in consensus for
          the

          associated block.
      extension:
        type: string
        format: byte
        description: |-
          Vote extension provided by the application. Only valid for precommit
          messages.
      extensionSignature:
        type: string
        format: byte
        description: |-
          Vote extension signature by the validator if they participated in
          consensus for the associated block.
          Only valid for precommit messages.
    description: |-
      Vote represents a prevote or precommit vote from validators for
      consensus.
  tendermint.version.Consensus:
    type: object
    properties:
      block:
        type: string
        format: uint64
      app:
        type: string
        format: uint64
    description: >-
      Consensus captures the consensus rules for processing a block in the
      blockchain,

      including all blockchain data structures and the rules of the
      application's

      state transition machine.
  cosmos.circuit.v1.AccountResponse:
    type: object
    properties:
      permission:
        type: object
        properties:
          level:
            description: level is the level of permissions granted to this account.
            type: string
            enum:
              - LEVEL_NONE_UNSPECIFIED
              - LEVEL_SOME_MSGS
              - LEVEL_ALL_MSGS
              - LEVEL_SUPER_ADMIN
            default: LEVEL_NONE_UNSPECIFIED
          limitTypeUrls:
            type: array
            items:
              type: string
            description: >-
              limit_type_urls is used with LEVEL_SOME_MSGS to limit the lists of
              Msg type

              URLs that the account can trip. It is an error to use
              limit_type_urls with

              a level other than LEVEL_SOME_MSGS.
        description: |-
          Permissions are the permissions that an account has to trip
          or reset the circuit breaker.
    description: AccountResponse is the response type for the Query/Account RPC method.
  cosmos.circuit.v1.AccountsResponse:
    type: object
    properties:
      accounts:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            permissions:
              type: object
              properties:
                level:
                  description: level is the level of permissions granted to this account.
                  type: string
                  enum:
                    - LEVEL_NONE_UNSPECIFIED
                    - LEVEL_SOME_MSGS
                    - LEVEL_ALL_MSGS
                    - LEVEL_SUPER_ADMIN
                  default: LEVEL_NONE_UNSPECIFIED
                limitTypeUrls:
                  type: array
                  items:
                    type: string
                  description: >-
                    limit_type_urls is used with LEVEL_SOME_MSGS to limit the
                    lists of Msg type

                    URLs that the account can trip. It is an error to use
                    limit_type_urls with

                    a level other than LEVEL_SOME_MSGS.
              description: |-
                Permissions are the permissions that an account has to trip
                or reset the circuit breaker.
          title: >-
            GenesisAccountPermissions is the account permissions for the circuit
            breaker in genesis
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: AccountsResponse is the response type for the Query/Accounts RPC method.
  cosmos.circuit.v1.DisabledListResponse:
    type: object
    properties:
      disabledList:
        type: array
        items:
          type: string
    description: >-
      DisabledListResponse is the response type for the Query/DisabledList RPC
      method.
  cosmos.circuit.v1.GenesisAccountPermissions:
    type: object
    properties:
      address:
        type: string
      permissions:
        type: object
        properties:
          level:
            description: level is the level of permissions granted to this account.
            type: string
            enum:
              - LEVEL_NONE_UNSPECIFIED
              - LEVEL_SOME_MSGS
              - LEVEL_ALL_MSGS
              - LEVEL_SUPER_ADMIN
            default: LEVEL_NONE_UNSPECIFIED
          limitTypeUrls:
            type: array
            items:
              type: string
            description: >-
              limit_type_urls is used with LEVEL_SOME_MSGS to limit the lists of
              Msg type

              URLs that the account can trip. It is an error to use
              limit_type_urls with

              a level other than LEVEL_SOME_MSGS.
        description: |-
          Permissions are the permissions that an account has to trip
          or reset the circuit breaker.
    title: >-
      GenesisAccountPermissions is the account permissions for the circuit
      breaker in genesis
  cosmos.circuit.v1.Permissions:
    type: object
    properties:
      level:
        description: level is the level of permissions granted to this account.
        type: string
        enum:
          - LEVEL_NONE_UNSPECIFIED
          - LEVEL_SOME_MSGS
          - LEVEL_ALL_MSGS
          - LEVEL_SUPER_ADMIN
        default: LEVEL_NONE_UNSPECIFIED
      limitTypeUrls:
        type: array
        items:
          type: string
        description: >-
          limit_type_urls is used with LEVEL_SOME_MSGS to limit the lists of Msg
          type

          URLs that the account can trip. It is an error to use limit_type_urls
          with

          a level other than LEVEL_SOME_MSGS.
    description: |-
      Permissions are the permissions that an account has to trip
      or reset the circuit breaker.
  cosmos.circuit.v1.Permissions.Level:
    type: string
    enum:
      - LEVEL_NONE_UNSPECIFIED
      - LEVEL_SOME_MSGS
      - LEVEL_ALL_MSGS
      - LEVEL_SUPER_ADMIN
    default: LEVEL_NONE_UNSPECIFIED
    description: |-
      Level is the permission level.

       - LEVEL_NONE_UNSPECIFIED: LEVEL_NONE_UNSPECIFIED indicates that the account will have no circuit
      breaker permissions.
       - LEVEL_SOME_MSGS: LEVEL_SOME_MSGS indicates that the account will have permission to
      trip or reset the circuit breaker for some Msg type URLs. If this level
      is chosen, a non-empty list of Msg type URLs must be provided in
      limit_type_urls.
       - LEVEL_ALL_MSGS: LEVEL_ALL_MSGS indicates that the account can trip or reset the circuit
      breaker for Msg's of all type URLs.
       - LEVEL_SUPER_ADMIN: LEVEL_SUPER_ADMIN indicates that the account can take all circuit breaker
      actions and can grant permissions to other accounts.
  cosmos.consensus.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: >-
          params are the tendermint consensus params stored in the consensus
          module.

          Please note that `params.version` is not populated in this response,
          it is

          tracked separately in the x/upgrade module.
        type: object
        properties:
          block:
            type: object
            properties:
              maxBytes:
                type: string
                format: int64
                title: |-
                  Max block size, in bytes.
                  Note: must be greater than 0
              maxGas:
                type: string
                format: int64
                title: |-
                  Max gas per block.
                  Note: must be greater or equal to -1
            description: BlockParams contains limits on the block size.
          evidence:
            type: object
            properties:
              maxAgeNumBlocks:
                type: string
                format: int64
                description: >-
                  Max age of evidence, in blocks.


                  The basic formula for calculating this is: MaxAgeDuration /
                  {average block

                  time}.
              maxAgeDuration:
                type: string
                description: >-
                  Max age of evidence, in time.


                  It should correspond with an app's "unbonding period" or other
                  similar

                  mechanism for handling [Nothing-At-Stake

                  attacks](https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed).
              maxBytes:
                type: string
                format: int64
                title: >-
                  This sets the maximum size of total evidence in bytes that can
                  be committed in a single block.

                  and should fall comfortably under the max block bytes.

                  Default is 1048576 or 1MB
            description: EvidenceParams determine how we handle evidence of malfeasance.
          validator:
            type: object
            properties:
              pubKeyTypes:
                type: array
                items:
                  type: string
            description: |-
              ValidatorParams restrict the public key types validators can use.
              NOTE: uses ABCI pubkey naming, not Amino names.
          version:
            type: object
            properties:
              app:
                type: string
                format: uint64
            description: VersionParams contains the ABCI application version.
          abci:
            type: object
            properties:
              voteExtensionsEnableHeight:
                type: string
                format: int64
                description: >-
                  vote_extensions_enable_height configures the first height
                  during which

                  vote extensions will be enabled. During this specified height,
                  and for all

                  subsequent heights, precommit messages that do not contain
                  valid extension data

                  will be considered invalid. Prior to this height, vote
                  extensions will not

                  be used or accepted by validators on the network.


                  Once enabled, vote extensions will be created by the
                  application in ExtendVote,

                  passed to the application for validation in
                  VerifyVoteExtension and given

                  to the application to use when proposing a block during
                  PrepareProposal.
            description: >-
              ABCIParams configure functionality specific to the Application
              Blockchain Interface.
    description: >-
      QueryParamsResponse defines the response type for querying x/consensus
      parameters.
  tendermint.types.ABCIParams:
    type: object
    properties:
      voteExtensionsEnableHeight:
        type: string
        format: int64
        description: >-
          vote_extensions_enable_height configures the first height during which

          vote extensions will be enabled. During this specified height, and for
          all

          subsequent heights, precommit messages that do not contain valid
          extension data

          will be considered invalid. Prior to this height, vote extensions will
          not

          be used or accepted by validators on the network.


          Once enabled, vote extensions will be created by the application in
          ExtendVote,

          passed to the application for validation in VerifyVoteExtension and
          given

          to the application to use when proposing a block during
          PrepareProposal.
    description: >-
      ABCIParams configure functionality specific to the Application Blockchain
      Interface.
  tendermint.types.BlockParams:
    type: object
    properties:
      maxBytes:
        type: string
        format: int64
        title: |-
          Max block size, in bytes.
          Note: must be greater than 0
      maxGas:
        type: string
        format: int64
        title: |-
          Max gas per block.
          Note: must be greater or equal to -1
    description: BlockParams contains limits on the block size.
  tendermint.types.ConsensusParams:
    type: object
    properties:
      block:
        type: object
        properties:
          maxBytes:
            type: string
            format: int64
            title: |-
              Max block size, in bytes.
              Note: must be greater than 0
          maxGas:
            type: string
            format: int64
            title: |-
              Max gas per block.
              Note: must be greater or equal to -1
        description: BlockParams contains limits on the block size.
      evidence:
        type: object
        properties:
          maxAgeNumBlocks:
            type: string
            format: int64
            description: >-
              Max age of evidence, in blocks.


              The basic formula for calculating this is: MaxAgeDuration /
              {average block

              time}.
          maxAgeDuration:
            type: string
            description: >-
              Max age of evidence, in time.


              It should correspond with an app's "unbonding period" or other
              similar

              mechanism for handling [Nothing-At-Stake

              attacks](https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed).
          maxBytes:
            type: string
            format: int64
            title: >-
              This sets the maximum size of total evidence in bytes that can be
              committed in a single block.

              and should fall comfortably under the max block bytes.

              Default is 1048576 or 1MB
        description: EvidenceParams determine how we handle evidence of malfeasance.
      validator:
        type: object
        properties:
          pubKeyTypes:
            type: array
            items:
              type: string
        description: |-
          ValidatorParams restrict the public key types validators can use.
          NOTE: uses ABCI pubkey naming, not Amino names.
      version:
        type: object
        properties:
          app:
            type: string
            format: uint64
        description: VersionParams contains the ABCI application version.
      abci:
        type: object
        properties:
          voteExtensionsEnableHeight:
            type: string
            format: int64
            description: >-
              vote_extensions_enable_height configures the first height during
              which

              vote extensions will be enabled. During this specified height, and
              for all

              subsequent heights, precommit messages that do not contain valid
              extension data

              will be considered invalid. Prior to this height, vote extensions
              will not

              be used or accepted by validators on the network.


              Once enabled, vote extensions will be created by the application
              in ExtendVote,

              passed to the application for validation in VerifyVoteExtension
              and given

              to the application to use when proposing a block during
              PrepareProposal.
        description: >-
          ABCIParams configure functionality specific to the Application
          Blockchain Interface.
    description: |-
      ConsensusParams contains consensus critical parameters that determine the
      validity of blocks.
  tendermint.types.EvidenceParams:
    type: object
    properties:
      maxAgeNumBlocks:
        type: string
        format: int64
        description: >-
          Max age of evidence, in blocks.


          The basic formula for calculating this is: MaxAgeDuration / {average
          block

          time}.
      maxAgeDuration:
        type: string
        description: >-
          Max age of evidence, in time.


          It should correspond with an app's "unbonding period" or other similar

          mechanism for handling [Nothing-At-Stake

          attacks](https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed).
      maxBytes:
        type: string
        format: int64
        title: >-
          This sets the maximum size of total evidence in bytes that can be
          committed in a single block.

          and should fall comfortably under the max block bytes.

          Default is 1048576 or 1MB
    description: EvidenceParams determine how we handle evidence of malfeasance.
  tendermint.types.ValidatorParams:
    type: object
    properties:
      pubKeyTypes:
        type: array
        items:
          type: string
    description: |-
      ValidatorParams restrict the public key types validators can use.
      NOTE: uses ABCI pubkey naming, not Amino names.
  tendermint.types.VersionParams:
    type: object
    properties:
      app:
        type: string
        format: uint64
    description: VersionParams contains the ABCI application version.
  cosmos.base.v1beta1.DecCoin:
    type: object
    properties:
      denom:
        type: string
      amount:
        type: string
    description: |-
      DecCoin defines a token with a denomination and a decimal amount.

      NOTE: The amount field is an Dec which implements the custom method
      signatures required by gogoproto.
  cosmos.distribution.v1beta1.DelegationDelegatorReward:
    type: object
    properties:
      validatorAddress:
        type: string
      reward:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
    description: |-
      DelegationDelegatorReward represents the properties
      of a delegator's delegation reward.
  cosmos.distribution.v1beta1.Params:
    type: object
    properties:
      communityTax:
        type: string
      baseProposerReward:
        type: string
        description: >-
          Deprecated: The base_proposer_reward field is deprecated and is no
          longer used

          in the x/distribution module's reward mechanism.
      bonusProposerReward:
        type: string
        description: >-
          Deprecated: The bonus_proposer_reward field is deprecated and is no
          longer used

          in the x/distribution module's reward mechanism.
      withdrawAddrEnabled:
        type: boolean
    description: Params defines the set of params for the distribution module.
  cosmos.distribution.v1beta1.QueryCommunityPoolResponse:
    type: object
    properties:
      pool:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
        description: pool defines community pool's coins.
    description: >-
      QueryCommunityPoolResponse is the response type for the
      Query/CommunityPool

      RPC method.
  cosmos.distribution.v1beta1.QueryDelegationRewardsResponse:
    type: object
    properties:
      rewards:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
        description: rewards defines the rewards accrued by a delegation.
    description: |-
      QueryDelegationRewardsResponse is the response type for the
      Query/DelegationRewards RPC method.
  cosmos.distribution.v1beta1.QueryDelegationTotalRewardsResponse:
    type: object
    properties:
      rewards:
        type: array
        items:
          type: object
          properties:
            validatorAddress:
              type: string
            reward:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  DecCoin defines a token with a denomination and a decimal
                  amount.


                  NOTE: The amount field is an Dec which implements the custom
                  method

                  signatures required by gogoproto.
          description: |-
            DelegationDelegatorReward represents the properties
            of a delegator's delegation reward.
        description: rewards defines all the rewards accrued by a delegator.
      total:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
        description: total defines the sum of all the rewards.
    description: |-
      QueryDelegationTotalRewardsResponse is the response type for the
      Query/DelegationTotalRewards RPC method.
  cosmos.distribution.v1beta1.QueryDelegatorValidatorsResponse:
    type: object
    properties:
      validators:
        type: array
        items:
          type: string
        description: validators defines the validators a delegator is delegating for.
    description: |-
      QueryDelegatorValidatorsResponse is the response type for the
      Query/DelegatorValidators RPC method.
  cosmos.distribution.v1beta1.QueryDelegatorWithdrawAddressResponse:
    type: object
    properties:
      withdrawAddress:
        type: string
        description: withdraw_address defines the delegator address to query for.
    description: |-
      QueryDelegatorWithdrawAddressResponse is the response type for the
      Query/DelegatorWithdrawAddress RPC method.
  cosmos.distribution.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          communityTax:
            type: string
          baseProposerReward:
            type: string
            description: >-
              Deprecated: The base_proposer_reward field is deprecated and is no
              longer used

              in the x/distribution module's reward mechanism.
          bonusProposerReward:
            type: string
            description: >-
              Deprecated: The bonus_proposer_reward field is deprecated and is
              no longer used

              in the x/distribution module's reward mechanism.
          withdrawAddrEnabled:
            type: boolean
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  cosmos.distribution.v1beta1.QueryValidatorCommissionResponse:
    type: object
    properties:
      commission:
        description: commission defines the commission the validator received.
        type: object
        properties:
          commission:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                DecCoin defines a token with a denomination and a decimal
                amount.


                NOTE: The amount field is an Dec which implements the custom
                method

                signatures required by gogoproto.
    title: |-
      QueryValidatorCommissionResponse is the response type for the
      Query/ValidatorCommission RPC method
  cosmos.distribution.v1beta1.QueryValidatorDistributionInfoResponse:
    type: object
    properties:
      operatorAddress:
        type: string
        description: operator_address defines the validator operator address.
      selfBondRewards:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
        description: self_bond_rewards defines the self delegations rewards.
      commission:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
        description: commission defines the commission the validator received.
    description: >-
      QueryValidatorDistributionInfoResponse is the response type for the
      Query/ValidatorDistributionInfo RPC method.
  cosmos.distribution.v1beta1.QueryValidatorOutstandingRewardsResponse:
    type: object
    properties:
      rewards:
        type: object
        properties:
          rewards:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                DecCoin defines a token with a denomination and a decimal
                amount.


                NOTE: The amount field is an Dec which implements the custom
                method

                signatures required by gogoproto.
        description: >-
          ValidatorOutstandingRewards represents outstanding (un-withdrawn)
          rewards

          for a validator inexpensive to track, allows simple sanity checks.
    description: |-
      QueryValidatorOutstandingRewardsResponse is the response type for the
      Query/ValidatorOutstandingRewards RPC method.
  cosmos.distribution.v1beta1.QueryValidatorSlashesResponse:
    type: object
    properties:
      slashes:
        type: array
        items:
          type: object
          properties:
            validatorPeriod:
              type: string
              format: uint64
            fraction:
              type: string
          description: |-
            ValidatorSlashEvent represents a validator slash event.
            Height is implicit within the store key.
            This is needed to calculate appropriate amount of staking tokens
            for delegations which are withdrawn after a slash has occurred.
        description: slashes defines the slashes the validator received.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryValidatorSlashesResponse is the response type for the
      Query/ValidatorSlashes RPC method.
  cosmos.distribution.v1beta1.ValidatorAccumulatedCommission:
    type: object
    properties:
      commission:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
    description: |-
      ValidatorAccumulatedCommission represents accumulated commission
      for a validator kept as a running counter, can be withdrawn at any time.
  cosmos.distribution.v1beta1.ValidatorOutstandingRewards:
    type: object
    properties:
      rewards:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            DecCoin defines a token with a denomination and a decimal amount.

            NOTE: The amount field is an Dec which implements the custom method
            signatures required by gogoproto.
    description: |-
      ValidatorOutstandingRewards represents outstanding (un-withdrawn) rewards
      for a validator inexpensive to track, allows simple sanity checks.
  cosmos.distribution.v1beta1.ValidatorSlashEvent:
    type: object
    properties:
      validatorPeriod:
        type: string
        format: uint64
      fraction:
        type: string
    description: |-
      ValidatorSlashEvent represents a validator slash event.
      Height is implicit within the store key.
      This is needed to calculate appropriate amount of staking tokens
      for delegations which are withdrawn after a slash has occurred.
  cosmos.epochs.v1beta1.EpochInfo:
    type: object
    properties:
      identifier:
        type: string
        description: identifier is a unique reference to this particular timer.
      startTime:
        type: string
        format: date-time
        description: >-
          start_time is the time at which the timer first ever ticks.

          If start_time is in the future, the epoch will not begin until the
          start

          time.
      duration:
        type: string
        description: |-
          duration is the time in between epoch ticks.
          In order for intended behavior to be met, duration should
          be greater than the chains expected block time.
          Duration must be non-zero.
      currentEpoch:
        type: string
        format: int64
        description: >-
          current_epoch is the current epoch number, or in other words,

          how many times has the timer 'ticked'.

          The first tick (current_epoch=1) is defined as

          the first block whose blocktime is greater than the EpochInfo
          start_time.
      currentEpochStartTime:
        type: string
        format: date-time
        description: >-
          current_epoch_start_time describes the start time of the current timer

          interval. The interval is (current_epoch_start_time,

          current_epoch_start_time + duration] When the timer ticks, this is set
          to

          current_epoch_start_time = last_epoch_start_time + duration only one
          timer

          tick for a given identifier can occur per block.


          NOTE! The current_epoch_start_time may diverge significantly from the

          wall-clock time the epoch began at. Wall-clock time of epoch start may
          be

          >> current_epoch_start_time. Suppose current_epoch_start_time = 10,

          duration = 5. Suppose the chain goes offline at t=14, and comes back
          online

          at t=30, and produces blocks at every successive time. (t=31, 32,
          etc.)

          * The t=30 block will start the epoch for (10, 15]

          * The t=31 block will start the epoch for (15, 20]

          * The t=32 block will start the epoch for (20, 25]

          * The t=33 block will start the epoch for (25, 30]

          * The t=34 block will start the epoch for (30, 35]

          * The **t=36** block will start the epoch for (35, 40]
      epochCountingStarted:
        type: boolean
        description: |-
          epoch_counting_started is a boolean, that indicates whether this
          epoch timer has began yet.
      currentEpochStartHeight:
        type: string
        format: int64
        title: >-
          current_epoch_start_height is the block height at which the current
          epoch

          started. (The block height at which the timer last ticked)
    description: |-
      EpochInfo is a struct that describes the data going into
      a timer defined by the x/epochs module.
  cosmos.epochs.v1beta1.QueryCurrentEpochResponse:
    type: object
    properties:
      currentEpoch:
        type: string
        format: int64
    description: |-
      QueryCurrentEpochResponse defines the gRPC response structure for
      querying an epoch by its identifier.
  cosmos.epochs.v1beta1.QueryEpochInfosResponse:
    type: object
    properties:
      epochs:
        type: array
        items:
          type: object
          properties:
            identifier:
              type: string
              description: identifier is a unique reference to this particular timer.
            startTime:
              type: string
              format: date-time
              description: >-
                start_time is the time at which the timer first ever ticks.

                If start_time is in the future, the epoch will not begin until
                the start

                time.
            duration:
              type: string
              description: |-
                duration is the time in between epoch ticks.
                In order for intended behavior to be met, duration should
                be greater than the chains expected block time.
                Duration must be non-zero.
            currentEpoch:
              type: string
              format: int64
              description: >-
                current_epoch is the current epoch number, or in other words,

                how many times has the timer 'ticked'.

                The first tick (current_epoch=1) is defined as

                the first block whose blocktime is greater than the EpochInfo
                start_time.
            currentEpochStartTime:
              type: string
              format: date-time
              description: >-
                current_epoch_start_time describes the start time of the current
                timer

                interval. The interval is (current_epoch_start_time,

                current_epoch_start_time + duration] When the timer ticks, this
                is set to

                current_epoch_start_time = last_epoch_start_time + duration only
                one timer

                tick for a given identifier can occur per block.


                NOTE! The current_epoch_start_time may diverge significantly
                from the

                wall-clock time the epoch began at. Wall-clock time of epoch
                start may be

                >> current_epoch_start_time. Suppose current_epoch_start_time =
                10,

                duration = 5. Suppose the chain goes offline at t=14, and comes
                back online

                at t=30, and produces blocks at every successive time. (t=31,
                32, etc.)

                * The t=30 block will start the epoch for (10, 15]

                * The t=31 block will start the epoch for (15, 20]

                * The t=32 block will start the epoch for (20, 25]

                * The t=33 block will start the epoch for (25, 30]

                * The t=34 block will start the epoch for (30, 35]

                * The **t=36** block will start the epoch for (35, 40]
            epochCountingStarted:
              type: boolean
              description: |-
                epoch_counting_started is a boolean, that indicates whether this
                epoch timer has began yet.
            currentEpochStartHeight:
              type: string
              format: int64
              title: >-
                current_epoch_start_height is the block height at which the
                current epoch

                started. (The block height at which the timer last ticked)
          description: |-
            EpochInfo is a struct that describes the data going into
            a timer defined by the x/epochs module.
    description: |-
      QueryEpochInfosRequest defines the gRPC response structure for
      querying all epoch info.
  cosmos.evidence.v1beta1.QueryAllEvidenceResponse:
    type: object
    properties:
      evidence:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        description: evidence returns all evidences.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAllEvidenceResponse is the response type for the Query/AllEvidence
      RPC

      method.
  cosmos.evidence.v1beta1.QueryEvidenceResponse:
    type: object
    properties:
      evidence:
        description: evidence returns the requested evidence.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
    description: >-
      QueryEvidenceResponse is the response type for the Query/Evidence RPC
      method.
  cosmos.feegrant.v1beta1.Grant:
    type: object
    properties:
      granter:
        type: string
        description: >-
          granter is the address of the user granting an allowance of their
          funds.
      grantee:
        type: string
        description: >-
          grantee is the address of the user being granted an allowance of
          another user's funds.
      allowance:
        description: allowance can be any of basic, periodic, allowed fee allowance.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
    title: Grant is stored in the KVStore to record a grant with full context
  cosmos.feegrant.v1beta1.QueryAllowanceResponse:
    type: object
    properties:
      allowance:
        description: allowance is a allowance granted for grantee by granter.
        type: object
        properties:
          granter:
            type: string
            description: >-
              granter is the address of the user granting an allowance of their
              funds.
          grantee:
            type: string
            description: >-
              grantee is the address of the user being granted an allowance of
              another user's funds.
          allowance:
            description: allowance can be any of basic, periodic, allowed fee allowance.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
        title: Grant is stored in the KVStore to record a grant with full context
    description: >-
      QueryAllowanceResponse is the response type for the Query/Allowance RPC
      method.
  cosmos.feegrant.v1beta1.QueryAllowancesByGranterResponse:
    type: object
    properties:
      allowances:
        type: array
        items:
          type: object
          properties:
            granter:
              type: string
              description: >-
                granter is the address of the user granting an allowance of
                their funds.
            grantee:
              type: string
              description: >-
                grantee is the address of the user being granted an allowance of
                another user's funds.
            allowance:
              description: allowance can be any of basic, periodic, allowed fee allowance.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
          title: Grant is stored in the KVStore to record a grant with full context
        description: allowances that have been issued by the granter.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAllowancesByGranterResponse is the response type for the
      Query/AllowancesByGranter RPC method.
  cosmos.feegrant.v1beta1.QueryAllowancesResponse:
    type: object
    properties:
      allowances:
        type: array
        items:
          type: object
          properties:
            granter:
              type: string
              description: >-
                granter is the address of the user granting an allowance of
                their funds.
            grantee:
              type: string
              description: >-
                grantee is the address of the user being granted an allowance of
                another user's funds.
            allowance:
              description: allowance can be any of basic, periodic, allowed fee allowance.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
          title: Grant is stored in the KVStore to record a grant with full context
        description: allowances are allowance's granted for grantee by granter.
      pagination:
        description: pagination defines an pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryAllowancesResponse is the response type for the Query/Allowances RPC
      method.
  cosmos.gov.v1.Deposit:
    type: object
    properties:
      proposalId:
        type: string
        format: uint64
        description: proposal_id defines the unique id of the proposal.
      depositor:
        type: string
        description: depositor defines the deposit addresses from the proposals.
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount to be deposited by depositor.
    description: |-
      Deposit defines an amount deposited by an account address to an active
      proposal.
  cosmos.gov.v1.DepositParams:
    type: object
    properties:
      minDeposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: Minimum deposit for a proposal to enter voting period.
      maxDepositPeriod:
        type: string
        description: >-
          Maximum period for Atom holders to deposit on a proposal. Initial
          value: 2

          months.
    description: DepositParams defines the params for deposits on governance proposals.
  cosmos.gov.v1.Params:
    type: object
    properties:
      minDeposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: Minimum deposit for a proposal to enter voting period.
      maxDepositPeriod:
        type: string
        description: >-
          Maximum period for Atom holders to deposit on a proposal. Initial
          value: 2

          months.
      votingPeriod:
        type: string
        description: Duration of the voting period.
      quorum:
        type: string
        description: |-
          Minimum percentage of total stake needed to vote for a result to be
           considered valid.
      threshold:
        type: string
        description: >-
          Minimum proportion of Yes votes for proposal to pass. Default value:
          0.5.
      vetoThreshold:
        type: string
        description: |-
          Minimum value of Veto votes to Total votes ratio for proposal to be
           vetoed. Default value: 1/3.
      minInitialDepositRatio:
        type: string
        description: >-
          The ratio representing the proportion of the deposit value that must
          be paid at proposal submission.
      proposalCancelRatio:
        type: string
        description: >-
          The cancel ratio which will not be returned back to the depositors
          when a proposal is cancelled.
      proposalCancelDest:
        type: string
        description: >-
          The address which will receive (proposal_cancel_ratio * deposit)
          proposal deposits.

          If empty, the (proposal_cancel_ratio * deposit) proposal deposits will
          be burned.
      expeditedVotingPeriod:
        type: string
        description: Duration of the voting period of an expedited proposal.
      expeditedThreshold:
        type: string
        description: >-
          Minimum proportion of Yes votes for proposal to pass. Default value:
          0.67.
      expeditedMinDeposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: Minimum expedited deposit for a proposal to enter voting period.
      burnVoteQuorum:
        type: boolean
        title: burn deposits if a proposal does not meet quorum
      burnProposalDepositPrevote:
        type: boolean
        title: burn deposits if the proposal does not enter voting period
      burnVoteVeto:
        type: boolean
        title: burn deposits if quorum with vote type no_veto is met
      minDepositRatio:
        type: string
        description: >-
          The ratio representing the proportion of the deposit value minimum
          that must be met when making a deposit.

          Default value: 0.01. Meaning that for a chain with a min_deposit of
          100stake, a deposit of 1stake would be

          required.
    description: Params defines the parameters for the x/gov module.
  cosmos.gov.v1.Proposal:
    type: object
    properties:
      id:
        type: string
        format: uint64
        description: id defines the unique id of the proposal.
      messages:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        description: >-
          messages are the arbitrary messages to be executed if the proposal
          passes.
      status:
        description: status defines the proposal status.
        type: string
        enum:
          - PROPOSAL_STATUS_UNSPECIFIED
          - PROPOSAL_STATUS_DEPOSIT_PERIOD
          - PROPOSAL_STATUS_VOTING_PERIOD
          - PROPOSAL_STATUS_PASSED
          - PROPOSAL_STATUS_REJECTED
          - PROPOSAL_STATUS_FAILED
        default: PROPOSAL_STATUS_UNSPECIFIED
      finalTallyResult:
        description: |-
          final_tally_result is the final tally result of the proposal. When
          querying a proposal via gRPC, this field is not populated until the
          proposal's voting period has ended.
        type: object
        properties:
          yesCount:
            type: string
            description: yes_count is the number of yes votes on a proposal.
          abstainCount:
            type: string
            description: abstain_count is the number of abstain votes on a proposal.
          noCount:
            type: string
            description: no_count is the number of no votes on a proposal.
          noWithVetoCount:
            type: string
            description: >-
              no_with_veto_count is the number of no with veto votes on a
              proposal.
      submitTime:
        type: string
        format: date-time
        description: submit_time is the time of proposal submission.
      depositEndTime:
        type: string
        format: date-time
        description: deposit_end_time is the end time for deposition.
      totalDeposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: total_deposit is the total deposit on the proposal.
      votingStartTime:
        type: string
        format: date-time
        description: voting_start_time is the starting time to vote on a proposal.
      votingEndTime:
        type: string
        format: date-time
        description: voting_end_time is the end time of voting on a proposal.
      metadata:
        type: string
        title: |-
          metadata is any arbitrary metadata attached to the proposal.
          the recommended format of the metadata is to be found here:
          https://docs.cosmos.network/v0.47/modules/gov#proposal-3
      title:
        type: string
        title: title is the title of the proposal
      summary:
        type: string
        title: summary is a short summary of the proposal
      proposer:
        type: string
        title: proposer is the address of the proposal sumbitter
      expedited:
        type: boolean
        title: expedited defines if the proposal is expedited
      failedReason:
        type: string
        title: failed_reason defines the reason why the proposal failed
    description: Proposal defines the core field members of a governance proposal.
  cosmos.gov.v1.ProposalStatus:
    type: string
    enum:
      - PROPOSAL_STATUS_UNSPECIFIED
      - PROPOSAL_STATUS_DEPOSIT_PERIOD
      - PROPOSAL_STATUS_VOTING_PERIOD
      - PROPOSAL_STATUS_PASSED
      - PROPOSAL_STATUS_REJECTED
      - PROPOSAL_STATUS_FAILED
    default: PROPOSAL_STATUS_UNSPECIFIED
    description: |-
      ProposalStatus enumerates the valid statuses of a proposal.

       - PROPOSAL_STATUS_UNSPECIFIED: PROPOSAL_STATUS_UNSPECIFIED defines the default proposal status.
       - PROPOSAL_STATUS_DEPOSIT_PERIOD: PROPOSAL_STATUS_DEPOSIT_PERIOD defines a proposal status during the deposit
      period.
       - PROPOSAL_STATUS_VOTING_PERIOD: PROPOSAL_STATUS_VOTING_PERIOD defines a proposal status during the voting
      period.
       - PROPOSAL_STATUS_PASSED: PROPOSAL_STATUS_PASSED defines a proposal status of a proposal that has
      passed.
       - PROPOSAL_STATUS_REJECTED: PROPOSAL_STATUS_REJECTED defines a proposal status of a proposal that has
      been rejected.
       - PROPOSAL_STATUS_FAILED: PROPOSAL_STATUS_FAILED defines a proposal status of a proposal that has
      failed.
  cosmos.gov.v1.QueryConstitutionResponse:
    type: object
    properties:
      constitution:
        type: string
    title: >-
      QueryConstitutionResponse is the response type for the Query/Constitution
      RPC method
  cosmos.gov.v1.QueryDepositResponse:
    type: object
    properties:
      deposit:
        description: deposit defines the requested deposit.
        type: object
        properties:
          proposalId:
            type: string
            format: uint64
            description: proposal_id defines the unique id of the proposal.
          depositor:
            type: string
            description: depositor defines the deposit addresses from the proposals.
          amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: amount to be deposited by depositor.
    description: >-
      QueryDepositResponse is the response type for the Query/Deposit RPC
      method.
  cosmos.gov.v1.QueryDepositsResponse:
    type: object
    properties:
      deposits:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal_id defines the unique id of the proposal.
            depositor:
              type: string
              description: depositor defines the deposit addresses from the proposals.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount to be deposited by depositor.
          description: >-
            Deposit defines an amount deposited by an account address to an
            active

            proposal.
        description: deposits defines the requested deposits.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryDepositsResponse is the response type for the Query/Deposits RPC
      method.
  cosmos.gov.v1.QueryParamsResponse:
    type: object
    properties:
      votingParams:
        description: |-
          Deprecated: Prefer to use `params` instead.
          voting_params defines the parameters related to voting.
        type: object
        properties:
          votingPeriod:
            type: string
            description: Duration of the voting period.
      depositParams:
        description: |-
          Deprecated: Prefer to use `params` instead.
          deposit_params defines the parameters related to deposit.
        type: object
        properties:
          minDeposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: Minimum deposit for a proposal to enter voting period.
          maxDepositPeriod:
            type: string
            description: >-
              Maximum period for Atom holders to deposit on a proposal. Initial
              value: 2

              months.
      tallyParams:
        description: |-
          Deprecated: Prefer to use `params` instead.
          tally_params defines the parameters related to tally.
        type: object
        properties:
          quorum:
            type: string
            description: >-
              Minimum percentage of total stake needed to vote for a result to
              be

              considered valid.
          threshold:
            type: string
            description: >-
              Minimum proportion of Yes votes for proposal to pass. Default
              value: 0.5.
          vetoThreshold:
            type: string
            description: >-
              Minimum value of Veto votes to Total votes ratio for proposal to
              be

              vetoed. Default value: 1/3.
      params:
        description: params defines all the paramaters of x/gov module.
        type: object
        properties:
          minDeposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: Minimum deposit for a proposal to enter voting period.
          maxDepositPeriod:
            type: string
            description: >-
              Maximum period for Atom holders to deposit on a proposal. Initial
              value: 2

              months.
          votingPeriod:
            type: string
            description: Duration of the voting period.
          quorum:
            type: string
            description: >-
              Minimum percentage of total stake needed to vote for a result to
              be
               considered valid.
          threshold:
            type: string
            description: >-
              Minimum proportion of Yes votes for proposal to pass. Default
              value: 0.5.
          vetoThreshold:
            type: string
            description: >-
              Minimum value of Veto votes to Total votes ratio for proposal to
              be
               vetoed. Default value: 1/3.
          minInitialDepositRatio:
            type: string
            description: >-
              The ratio representing the proportion of the deposit value that
              must be paid at proposal submission.
          proposalCancelRatio:
            type: string
            description: >-
              The cancel ratio which will not be returned back to the depositors
              when a proposal is cancelled.
          proposalCancelDest:
            type: string
            description: >-
              The address which will receive (proposal_cancel_ratio * deposit)
              proposal deposits.

              If empty, the (proposal_cancel_ratio * deposit) proposal deposits
              will be burned.
          expeditedVotingPeriod:
            type: string
            description: Duration of the voting period of an expedited proposal.
          expeditedThreshold:
            type: string
            description: >-
              Minimum proportion of Yes votes for proposal to pass. Default
              value: 0.67.
          expeditedMinDeposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: Minimum expedited deposit for a proposal to enter voting period.
          burnVoteQuorum:
            type: boolean
            title: burn deposits if a proposal does not meet quorum
          burnProposalDepositPrevote:
            type: boolean
            title: burn deposits if the proposal does not enter voting period
          burnVoteVeto:
            type: boolean
            title: burn deposits if quorum with vote type no_veto is met
          minDepositRatio:
            type: string
            description: >-
              The ratio representing the proportion of the deposit value minimum
              that must be met when making a deposit.

              Default value: 0.01. Meaning that for a chain with a min_deposit
              of 100stake, a deposit of 1stake would be

              required.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  cosmos.gov.v1.QueryProposalResponse:
    type: object
    properties:
      proposal:
        description: proposal is the requested governance proposal.
        type: object
        properties:
          id:
            type: string
            format: uint64
            description: id defines the unique id of the proposal.
          messages:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            description: >-
              messages are the arbitrary messages to be executed if the proposal
              passes.
          status:
            description: status defines the proposal status.
            type: string
            enum:
              - PROPOSAL_STATUS_UNSPECIFIED
              - PROPOSAL_STATUS_DEPOSIT_PERIOD
              - PROPOSAL_STATUS_VOTING_PERIOD
              - PROPOSAL_STATUS_PASSED
              - PROPOSAL_STATUS_REJECTED
              - PROPOSAL_STATUS_FAILED
            default: PROPOSAL_STATUS_UNSPECIFIED
          finalTallyResult:
            description: >-
              final_tally_result is the final tally result of the proposal. When

              querying a proposal via gRPC, this field is not populated until
              the

              proposal's voting period has ended.
            type: object
            properties:
              yesCount:
                type: string
                description: yes_count is the number of yes votes on a proposal.
              abstainCount:
                type: string
                description: abstain_count is the number of abstain votes on a proposal.
              noCount:
                type: string
                description: no_count is the number of no votes on a proposal.
              noWithVetoCount:
                type: string
                description: >-
                  no_with_veto_count is the number of no with veto votes on a
                  proposal.
          submitTime:
            type: string
            format: date-time
            description: submit_time is the time of proposal submission.
          depositEndTime:
            type: string
            format: date-time
            description: deposit_end_time is the end time for deposition.
          totalDeposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: total_deposit is the total deposit on the proposal.
          votingStartTime:
            type: string
            format: date-time
            description: voting_start_time is the starting time to vote on a proposal.
          votingEndTime:
            type: string
            format: date-time
            description: voting_end_time is the end time of voting on a proposal.
          metadata:
            type: string
            title: |-
              metadata is any arbitrary metadata attached to the proposal.
              the recommended format of the metadata is to be found here:
              https://docs.cosmos.network/v0.47/modules/gov#proposal-3
          title:
            type: string
            title: title is the title of the proposal
          summary:
            type: string
            title: summary is a short summary of the proposal
          proposer:
            type: string
            title: proposer is the address of the proposal sumbitter
          expedited:
            type: boolean
            title: expedited defines if the proposal is expedited
          failedReason:
            type: string
            title: failed_reason defines the reason why the proposal failed
    description: >-
      QueryProposalResponse is the response type for the Query/Proposal RPC
      method.
  cosmos.gov.v1.QueryProposalsResponse:
    type: object
    properties:
      proposals:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
              description: id defines the unique id of the proposal.
            messages:
              type: array
              items:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
              description: >-
                messages are the arbitrary messages to be executed if the
                proposal passes.
            status:
              description: status defines the proposal status.
              type: string
              enum:
                - PROPOSAL_STATUS_UNSPECIFIED
                - PROPOSAL_STATUS_DEPOSIT_PERIOD
                - PROPOSAL_STATUS_VOTING_PERIOD
                - PROPOSAL_STATUS_PASSED
                - PROPOSAL_STATUS_REJECTED
                - PROPOSAL_STATUS_FAILED
              default: PROPOSAL_STATUS_UNSPECIFIED
            finalTallyResult:
              description: >-
                final_tally_result is the final tally result of the proposal.
                When

                querying a proposal via gRPC, this field is not populated until
                the

                proposal's voting period has ended.
              type: object
              properties:
                yesCount:
                  type: string
                  description: yes_count is the number of yes votes on a proposal.
                abstainCount:
                  type: string
                  description: abstain_count is the number of abstain votes on a proposal.
                noCount:
                  type: string
                  description: no_count is the number of no votes on a proposal.
                noWithVetoCount:
                  type: string
                  description: >-
                    no_with_veto_count is the number of no with veto votes on a
                    proposal.
            submitTime:
              type: string
              format: date-time
              description: submit_time is the time of proposal submission.
            depositEndTime:
              type: string
              format: date-time
              description: deposit_end_time is the end time for deposition.
            totalDeposit:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: total_deposit is the total deposit on the proposal.
            votingStartTime:
              type: string
              format: date-time
              description: voting_start_time is the starting time to vote on a proposal.
            votingEndTime:
              type: string
              format: date-time
              description: voting_end_time is the end time of voting on a proposal.
            metadata:
              type: string
              title: |-
                metadata is any arbitrary metadata attached to the proposal.
                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/gov#proposal-3
            title:
              type: string
              title: title is the title of the proposal
            summary:
              type: string
              title: summary is a short summary of the proposal
            proposer:
              type: string
              title: proposer is the address of the proposal sumbitter
            expedited:
              type: boolean
              title: expedited defines if the proposal is expedited
            failedReason:
              type: string
              title: failed_reason defines the reason why the proposal failed
          description: Proposal defines the core field members of a governance proposal.
        description: proposals defines all the requested governance proposals.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryProposalsResponse is the response type for the Query/Proposals RPC
      method.
  cosmos.gov.v1.QueryTallyResultResponse:
    type: object
    properties:
      tally:
        description: tally defines the requested tally.
        type: object
        properties:
          yesCount:
            type: string
            description: yes_count is the number of yes votes on a proposal.
          abstainCount:
            type: string
            description: abstain_count is the number of abstain votes on a proposal.
          noCount:
            type: string
            description: no_count is the number of no votes on a proposal.
          noWithVetoCount:
            type: string
            description: >-
              no_with_veto_count is the number of no with veto votes on a
              proposal.
    description: >-
      QueryTallyResultResponse is the response type for the Query/Tally RPC
      method.
  cosmos.gov.v1.QueryVoteResponse:
    type: object
    properties:
      vote:
        description: vote defines the queried vote.
        type: object
        properties:
          proposalId:
            type: string
            format: uint64
            description: proposal_id defines the unique id of the proposal.
          voter:
            type: string
            description: voter is the voter address of the proposal.
          options:
            type: array
            items:
              type: object
              properties:
                option:
                  description: >-
                    option defines the valid vote options, it must not contain
                    duplicate vote options.
                  type: string
                  enum:
                    - VOTE_OPTION_UNSPECIFIED
                    - VOTE_OPTION_YES
                    - VOTE_OPTION_ABSTAIN
                    - VOTE_OPTION_NO
                    - VOTE_OPTION_NO_WITH_VETO
                  default: VOTE_OPTION_UNSPECIFIED
                weight:
                  type: string
                  description: weight is the vote weight associated with the vote option.
              description: WeightedVoteOption defines a unit of vote for vote split.
            description: options is the weighted vote options.
          metadata:
            type: string
            title: >-
              metadata is any arbitrary metadata attached to the vote.

              the recommended format of the metadata is to be found here:
              https://docs.cosmos.network/v0.47/modules/gov#vote-5
    description: QueryVoteResponse is the response type for the Query/Vote RPC method.
  cosmos.gov.v1.QueryVotesResponse:
    type: object
    properties:
      votes:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal_id defines the unique id of the proposal.
            voter:
              type: string
              description: voter is the voter address of the proposal.
            options:
              type: array
              items:
                type: object
                properties:
                  option:
                    description: >-
                      option defines the valid vote options, it must not contain
                      duplicate vote options.
                    type: string
                    enum:
                      - VOTE_OPTION_UNSPECIFIED
                      - VOTE_OPTION_YES
                      - VOTE_OPTION_ABSTAIN
                      - VOTE_OPTION_NO
                      - VOTE_OPTION_NO_WITH_VETO
                    default: VOTE_OPTION_UNSPECIFIED
                  weight:
                    type: string
                    description: weight is the vote weight associated with the vote option.
                description: WeightedVoteOption defines a unit of vote for vote split.
              description: options is the weighted vote options.
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata attached to the vote.

                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/gov#vote-5
          description: |-
            Vote defines a vote on a governance proposal.
            A Vote consists of a proposal ID, the voter, and the vote option.
        description: votes defines the queried votes.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryVotesResponse is the response type for the Query/Votes RPC method.
  cosmos.gov.v1.TallyParams:
    type: object
    properties:
      quorum:
        type: string
        description: |-
          Minimum percentage of total stake needed to vote for a result to be
          considered valid.
      threshold:
        type: string
        description: >-
          Minimum proportion of Yes votes for proposal to pass. Default value:
          0.5.
      vetoThreshold:
        type: string
        description: |-
          Minimum value of Veto votes to Total votes ratio for proposal to be
          vetoed. Default value: 1/3.
    description: TallyParams defines the params for tallying votes on governance proposals.
  cosmos.gov.v1.TallyResult:
    type: object
    properties:
      yesCount:
        type: string
        description: yes_count is the number of yes votes on a proposal.
      abstainCount:
        type: string
        description: abstain_count is the number of abstain votes on a proposal.
      noCount:
        type: string
        description: no_count is the number of no votes on a proposal.
      noWithVetoCount:
        type: string
        description: no_with_veto_count is the number of no with veto votes on a proposal.
    description: TallyResult defines a standard tally for a governance proposal.
  cosmos.gov.v1.Vote:
    type: object
    properties:
      proposalId:
        type: string
        format: uint64
        description: proposal_id defines the unique id of the proposal.
      voter:
        type: string
        description: voter is the voter address of the proposal.
      options:
        type: array
        items:
          type: object
          properties:
            option:
              description: >-
                option defines the valid vote options, it must not contain
                duplicate vote options.
              type: string
              enum:
                - VOTE_OPTION_UNSPECIFIED
                - VOTE_OPTION_YES
                - VOTE_OPTION_ABSTAIN
                - VOTE_OPTION_NO
                - VOTE_OPTION_NO_WITH_VETO
              default: VOTE_OPTION_UNSPECIFIED
            weight:
              type: string
              description: weight is the vote weight associated with the vote option.
          description: WeightedVoteOption defines a unit of vote for vote split.
        description: options is the weighted vote options.
      metadata:
        type: string
        title: >-
          metadata is any arbitrary metadata attached to the vote.

          the recommended format of the metadata is to be found here:
          https://docs.cosmos.network/v0.47/modules/gov#vote-5
    description: |-
      Vote defines a vote on a governance proposal.
      A Vote consists of a proposal ID, the voter, and the vote option.
  cosmos.gov.v1.VoteOption:
    type: string
    enum:
      - VOTE_OPTION_UNSPECIFIED
      - VOTE_OPTION_YES
      - VOTE_OPTION_ABSTAIN
      - VOTE_OPTION_NO
      - VOTE_OPTION_NO_WITH_VETO
    default: VOTE_OPTION_UNSPECIFIED
    description: >-
      VoteOption enumerates the valid vote options for a given governance
      proposal.

       - VOTE_OPTION_UNSPECIFIED: VOTE_OPTION_UNSPECIFIED defines a no-op vote option.
       - VOTE_OPTION_YES: VOTE_OPTION_YES defines a yes vote option.
       - VOTE_OPTION_ABSTAIN: VOTE_OPTION_ABSTAIN defines an abstain vote option.
       - VOTE_OPTION_NO: VOTE_OPTION_NO defines a no vote option.
       - VOTE_OPTION_NO_WITH_VETO: VOTE_OPTION_NO_WITH_VETO defines a no with veto vote option.
  cosmos.gov.v1.VotingParams:
    type: object
    properties:
      votingPeriod:
        type: string
        description: Duration of the voting period.
    description: VotingParams defines the params for voting on governance proposals.
  cosmos.gov.v1.WeightedVoteOption:
    type: object
    properties:
      option:
        description: >-
          option defines the valid vote options, it must not contain duplicate
          vote options.
        type: string
        enum:
          - VOTE_OPTION_UNSPECIFIED
          - VOTE_OPTION_YES
          - VOTE_OPTION_ABSTAIN
          - VOTE_OPTION_NO
          - VOTE_OPTION_NO_WITH_VETO
        default: VOTE_OPTION_UNSPECIFIED
      weight:
        type: string
        description: weight is the vote weight associated with the vote option.
    description: WeightedVoteOption defines a unit of vote for vote split.
  cosmos.gov.v1beta1.Deposit:
    type: object
    properties:
      proposalId:
        type: string
        format: uint64
        description: proposal_id defines the unique id of the proposal.
      depositor:
        type: string
        description: depositor defines the deposit addresses from the proposals.
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: amount to be deposited by depositor.
    description: |-
      Deposit defines an amount deposited by an account address to an active
      proposal.
  cosmos.gov.v1beta1.DepositParams:
    type: object
    properties:
      minDeposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: Minimum deposit for a proposal to enter voting period.
      maxDepositPeriod:
        type: string
        description: >-
          Maximum period for Atom holders to deposit on a proposal. Initial
          value: 2

          months.
    description: DepositParams defines the params for deposits on governance proposals.
  cosmos.gov.v1beta1.Proposal:
    type: object
    properties:
      proposalId:
        type: string
        format: uint64
        description: proposal_id defines the unique id of the proposal.
      content:
        description: content is the proposal's content.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      status:
        description: status defines the proposal status.
        type: string
        enum:
          - PROPOSAL_STATUS_UNSPECIFIED
          - PROPOSAL_STATUS_DEPOSIT_PERIOD
          - PROPOSAL_STATUS_VOTING_PERIOD
          - PROPOSAL_STATUS_PASSED
          - PROPOSAL_STATUS_REJECTED
          - PROPOSAL_STATUS_FAILED
        default: PROPOSAL_STATUS_UNSPECIFIED
      finalTallyResult:
        description: |-
          final_tally_result is the final tally result of the proposal. When
          querying a proposal via gRPC, this field is not populated until the
          proposal's voting period has ended.
        type: object
        properties:
          'yes':
            type: string
            description: yes is the number of yes votes on a proposal.
          abstain:
            type: string
            description: abstain is the number of abstain votes on a proposal.
          'no':
            type: string
            description: no is the number of no votes on a proposal.
          noWithVeto:
            type: string
            description: no_with_veto is the number of no with veto votes on a proposal.
      submitTime:
        type: string
        format: date-time
        description: submit_time is the time of proposal submission.
      depositEndTime:
        type: string
        format: date-time
        description: deposit_end_time is the end time for deposition.
      totalDeposit:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: total_deposit is the total deposit on the proposal.
      votingStartTime:
        type: string
        format: date-time
        description: voting_start_time is the starting time to vote on a proposal.
      votingEndTime:
        type: string
        format: date-time
        description: voting_end_time is the end time of voting on a proposal.
    description: Proposal defines the core field members of a governance proposal.
  cosmos.gov.v1beta1.ProposalStatus:
    type: string
    enum:
      - PROPOSAL_STATUS_UNSPECIFIED
      - PROPOSAL_STATUS_DEPOSIT_PERIOD
      - PROPOSAL_STATUS_VOTING_PERIOD
      - PROPOSAL_STATUS_PASSED
      - PROPOSAL_STATUS_REJECTED
      - PROPOSAL_STATUS_FAILED
    default: PROPOSAL_STATUS_UNSPECIFIED
    description: |-
      ProposalStatus enumerates the valid statuses of a proposal.

       - PROPOSAL_STATUS_UNSPECIFIED: PROPOSAL_STATUS_UNSPECIFIED defines the default proposal status.
       - PROPOSAL_STATUS_DEPOSIT_PERIOD: PROPOSAL_STATUS_DEPOSIT_PERIOD defines a proposal status during the deposit
      period.
       - PROPOSAL_STATUS_VOTING_PERIOD: PROPOSAL_STATUS_VOTING_PERIOD defines a proposal status during the voting
      period.
       - PROPOSAL_STATUS_PASSED: PROPOSAL_STATUS_PASSED defines a proposal status of a proposal that has
      passed.
       - PROPOSAL_STATUS_REJECTED: PROPOSAL_STATUS_REJECTED defines a proposal status of a proposal that has
      been rejected.
       - PROPOSAL_STATUS_FAILED: PROPOSAL_STATUS_FAILED defines a proposal status of a proposal that has
      failed.
  cosmos.gov.v1beta1.QueryDepositResponse:
    type: object
    properties:
      deposit:
        description: deposit defines the requested deposit.
        type: object
        properties:
          proposalId:
            type: string
            format: uint64
            description: proposal_id defines the unique id of the proposal.
          depositor:
            type: string
            description: depositor defines the deposit addresses from the proposals.
          amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: amount to be deposited by depositor.
    description: >-
      QueryDepositResponse is the response type for the Query/Deposit RPC
      method.
  cosmos.gov.v1beta1.QueryDepositsResponse:
    type: object
    properties:
      deposits:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal_id defines the unique id of the proposal.
            depositor:
              type: string
              description: depositor defines the deposit addresses from the proposals.
            amount:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: amount to be deposited by depositor.
          description: >-
            Deposit defines an amount deposited by an account address to an
            active

            proposal.
        description: deposits defines the requested deposits.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryDepositsResponse is the response type for the Query/Deposits RPC
      method.
  cosmos.gov.v1beta1.QueryParamsResponse:
    type: object
    properties:
      votingParams:
        description: voting_params defines the parameters related to voting.
        type: object
        properties:
          votingPeriod:
            type: string
            description: Duration of the voting period.
      depositParams:
        description: deposit_params defines the parameters related to deposit.
        type: object
        properties:
          minDeposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: Minimum deposit for a proposal to enter voting period.
          maxDepositPeriod:
            type: string
            description: >-
              Maximum period for Atom holders to deposit on a proposal. Initial
              value: 2

              months.
      tallyParams:
        description: tally_params defines the parameters related to tally.
        type: object
        properties:
          quorum:
            type: string
            format: byte
            description: >-
              Minimum percentage of total stake needed to vote for a result to
              be

              considered valid.
          threshold:
            type: string
            format: byte
            description: >-
              Minimum proportion of Yes votes for proposal to pass. Default
              value: 0.5.
          vetoThreshold:
            type: string
            format: byte
            description: >-
              Minimum value of Veto votes to Total votes ratio for proposal to
              be

              vetoed. Default value: 1/3.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  cosmos.gov.v1beta1.QueryProposalResponse:
    type: object
    properties:
      proposal:
        type: object
        properties:
          proposalId:
            type: string
            format: uint64
            description: proposal_id defines the unique id of the proposal.
          content:
            description: content is the proposal's content.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          status:
            description: status defines the proposal status.
            type: string
            enum:
              - PROPOSAL_STATUS_UNSPECIFIED
              - PROPOSAL_STATUS_DEPOSIT_PERIOD
              - PROPOSAL_STATUS_VOTING_PERIOD
              - PROPOSAL_STATUS_PASSED
              - PROPOSAL_STATUS_REJECTED
              - PROPOSAL_STATUS_FAILED
            default: PROPOSAL_STATUS_UNSPECIFIED
          finalTallyResult:
            description: >-
              final_tally_result is the final tally result of the proposal. When

              querying a proposal via gRPC, this field is not populated until
              the

              proposal's voting period has ended.
            type: object
            properties:
              'yes':
                type: string
                description: yes is the number of yes votes on a proposal.
              abstain:
                type: string
                description: abstain is the number of abstain votes on a proposal.
              'no':
                type: string
                description: no is the number of no votes on a proposal.
              noWithVeto:
                type: string
                description: >-
                  no_with_veto is the number of no with veto votes on a
                  proposal.
          submitTime:
            type: string
            format: date-time
            description: submit_time is the time of proposal submission.
          depositEndTime:
            type: string
            format: date-time
            description: deposit_end_time is the end time for deposition.
          totalDeposit:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            description: total_deposit is the total deposit on the proposal.
          votingStartTime:
            type: string
            format: date-time
            description: voting_start_time is the starting time to vote on a proposal.
          votingEndTime:
            type: string
            format: date-time
            description: voting_end_time is the end time of voting on a proposal.
        description: Proposal defines the core field members of a governance proposal.
    description: >-
      QueryProposalResponse is the response type for the Query/Proposal RPC
      method.
  cosmos.gov.v1beta1.QueryProposalsResponse:
    type: object
    properties:
      proposals:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal_id defines the unique id of the proposal.
            content:
              description: content is the proposal's content.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            status:
              description: status defines the proposal status.
              type: string
              enum:
                - PROPOSAL_STATUS_UNSPECIFIED
                - PROPOSAL_STATUS_DEPOSIT_PERIOD
                - PROPOSAL_STATUS_VOTING_PERIOD
                - PROPOSAL_STATUS_PASSED
                - PROPOSAL_STATUS_REJECTED
                - PROPOSAL_STATUS_FAILED
              default: PROPOSAL_STATUS_UNSPECIFIED
            finalTallyResult:
              description: >-
                final_tally_result is the final tally result of the proposal.
                When

                querying a proposal via gRPC, this field is not populated until
                the

                proposal's voting period has ended.
              type: object
              properties:
                'yes':
                  type: string
                  description: yes is the number of yes votes on a proposal.
                abstain:
                  type: string
                  description: abstain is the number of abstain votes on a proposal.
                'no':
                  type: string
                  description: no is the number of no votes on a proposal.
                noWithVeto:
                  type: string
                  description: >-
                    no_with_veto is the number of no with veto votes on a
                    proposal.
            submitTime:
              type: string
              format: date-time
              description: submit_time is the time of proposal submission.
            depositEndTime:
              type: string
              format: date-time
              description: deposit_end_time is the end time for deposition.
            totalDeposit:
              type: array
              items:
                type: object
                properties:
                  denom:
                    type: string
                  amount:
                    type: string
                description: >-
                  Coin defines a token with a denomination and an amount.


                  NOTE: The amount field is an Int which implements the custom
                  method

                  signatures required by gogoproto.
              description: total_deposit is the total deposit on the proposal.
            votingStartTime:
              type: string
              format: date-time
              description: voting_start_time is the starting time to vote on a proposal.
            votingEndTime:
              type: string
              format: date-time
              description: voting_end_time is the end time of voting on a proposal.
          description: Proposal defines the core field members of a governance proposal.
        description: proposals defines all the requested governance proposals.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryProposalsResponse is the response type for the Query/Proposals RPC
      method.
  cosmos.gov.v1beta1.QueryTallyResultResponse:
    type: object
    properties:
      tally:
        description: tally defines the requested tally.
        type: object
        properties:
          'yes':
            type: string
            description: yes is the number of yes votes on a proposal.
          abstain:
            type: string
            description: abstain is the number of abstain votes on a proposal.
          'no':
            type: string
            description: no is the number of no votes on a proposal.
          noWithVeto:
            type: string
            description: no_with_veto is the number of no with veto votes on a proposal.
    description: >-
      QueryTallyResultResponse is the response type for the Query/Tally RPC
      method.
  cosmos.gov.v1beta1.QueryVoteResponse:
    type: object
    properties:
      vote:
        description: vote defines the queried vote.
        type: object
        properties:
          proposalId:
            type: string
            format: uint64
            description: proposal_id defines the unique id of the proposal.
          voter:
            type: string
            description: voter is the voter address of the proposal.
          option:
            description: >-
              Deprecated: Prefer to use `options` instead. This field is set in
              queries

              if and only if `len(options) == 1` and that option has weight 1.
              In all

              other cases, this field will default to VOTE_OPTION_UNSPECIFIED.
            type: string
            enum:
              - VOTE_OPTION_UNSPECIFIED
              - VOTE_OPTION_YES
              - VOTE_OPTION_ABSTAIN
              - VOTE_OPTION_NO
              - VOTE_OPTION_NO_WITH_VETO
            default: VOTE_OPTION_UNSPECIFIED
          options:
            type: array
            items:
              type: object
              properties:
                option:
                  description: >-
                    option defines the valid vote options, it must not contain
                    duplicate vote options.
                  type: string
                  enum:
                    - VOTE_OPTION_UNSPECIFIED
                    - VOTE_OPTION_YES
                    - VOTE_OPTION_ABSTAIN
                    - VOTE_OPTION_NO
                    - VOTE_OPTION_NO_WITH_VETO
                  default: VOTE_OPTION_UNSPECIFIED
                weight:
                  type: string
                  description: weight is the vote weight associated with the vote option.
              description: WeightedVoteOption defines a unit of vote for vote split.
            description: options is the weighted vote options.
    description: QueryVoteResponse is the response type for the Query/Vote RPC method.
  cosmos.gov.v1beta1.QueryVotesResponse:
    type: object
    properties:
      votes:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal_id defines the unique id of the proposal.
            voter:
              type: string
              description: voter is the voter address of the proposal.
            option:
              description: >-
                Deprecated: Prefer to use `options` instead. This field is set
                in queries

                if and only if `len(options) == 1` and that option has weight 1.
                In all

                other cases, this field will default to VOTE_OPTION_UNSPECIFIED.
              type: string
              enum:
                - VOTE_OPTION_UNSPECIFIED
                - VOTE_OPTION_YES
                - VOTE_OPTION_ABSTAIN
                - VOTE_OPTION_NO
                - VOTE_OPTION_NO_WITH_VETO
              default: VOTE_OPTION_UNSPECIFIED
            options:
              type: array
              items:
                type: object
                properties:
                  option:
                    description: >-
                      option defines the valid vote options, it must not contain
                      duplicate vote options.
                    type: string
                    enum:
                      - VOTE_OPTION_UNSPECIFIED
                      - VOTE_OPTION_YES
                      - VOTE_OPTION_ABSTAIN
                      - VOTE_OPTION_NO
                      - VOTE_OPTION_NO_WITH_VETO
                    default: VOTE_OPTION_UNSPECIFIED
                  weight:
                    type: string
                    description: weight is the vote weight associated with the vote option.
                description: WeightedVoteOption defines a unit of vote for vote split.
              description: options is the weighted vote options.
          description: |-
            Vote defines a vote on a governance proposal.
            A Vote consists of a proposal ID, the voter, and the vote option.
        description: votes defines the queried votes.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryVotesResponse is the response type for the Query/Votes RPC method.
  cosmos.gov.v1beta1.TallyParams:
    type: object
    properties:
      quorum:
        type: string
        format: byte
        description: |-
          Minimum percentage of total stake needed to vote for a result to be
          considered valid.
      threshold:
        type: string
        format: byte
        description: >-
          Minimum proportion of Yes votes for proposal to pass. Default value:
          0.5.
      vetoThreshold:
        type: string
        format: byte
        description: |-
          Minimum value of Veto votes to Total votes ratio for proposal to be
          vetoed. Default value: 1/3.
    description: TallyParams defines the params for tallying votes on governance proposals.
  cosmos.gov.v1beta1.TallyResult:
    type: object
    properties:
      'yes':
        type: string
        description: yes is the number of yes votes on a proposal.
      abstain:
        type: string
        description: abstain is the number of abstain votes on a proposal.
      'no':
        type: string
        description: no is the number of no votes on a proposal.
      noWithVeto:
        type: string
        description: no_with_veto is the number of no with veto votes on a proposal.
    description: TallyResult defines a standard tally for a governance proposal.
  cosmos.gov.v1beta1.Vote:
    type: object
    properties:
      proposalId:
        type: string
        format: uint64
        description: proposal_id defines the unique id of the proposal.
      voter:
        type: string
        description: voter is the voter address of the proposal.
      option:
        description: >-
          Deprecated: Prefer to use `options` instead. This field is set in
          queries

          if and only if `len(options) == 1` and that option has weight 1. In
          all

          other cases, this field will default to VOTE_OPTION_UNSPECIFIED.
        type: string
        enum:
          - VOTE_OPTION_UNSPECIFIED
          - VOTE_OPTION_YES
          - VOTE_OPTION_ABSTAIN
          - VOTE_OPTION_NO
          - VOTE_OPTION_NO_WITH_VETO
        default: VOTE_OPTION_UNSPECIFIED
      options:
        type: array
        items:
          type: object
          properties:
            option:
              description: >-
                option defines the valid vote options, it must not contain
                duplicate vote options.
              type: string
              enum:
                - VOTE_OPTION_UNSPECIFIED
                - VOTE_OPTION_YES
                - VOTE_OPTION_ABSTAIN
                - VOTE_OPTION_NO
                - VOTE_OPTION_NO_WITH_VETO
              default: VOTE_OPTION_UNSPECIFIED
            weight:
              type: string
              description: weight is the vote weight associated with the vote option.
          description: WeightedVoteOption defines a unit of vote for vote split.
        description: options is the weighted vote options.
    description: |-
      Vote defines a vote on a governance proposal.
      A Vote consists of a proposal ID, the voter, and the vote option.
  cosmos.gov.v1beta1.VoteOption:
    type: string
    enum:
      - VOTE_OPTION_UNSPECIFIED
      - VOTE_OPTION_YES
      - VOTE_OPTION_ABSTAIN
      - VOTE_OPTION_NO
      - VOTE_OPTION_NO_WITH_VETO
    default: VOTE_OPTION_UNSPECIFIED
    description: >-
      VoteOption enumerates the valid vote options for a given governance
      proposal.

       - VOTE_OPTION_UNSPECIFIED: VOTE_OPTION_UNSPECIFIED defines a no-op vote option.
       - VOTE_OPTION_YES: VOTE_OPTION_YES defines a yes vote option.
       - VOTE_OPTION_ABSTAIN: VOTE_OPTION_ABSTAIN defines an abstain vote option.
       - VOTE_OPTION_NO: VOTE_OPTION_NO defines a no vote option.
       - VOTE_OPTION_NO_WITH_VETO: VOTE_OPTION_NO_WITH_VETO defines a no with veto vote option.
  cosmos.gov.v1beta1.VotingParams:
    type: object
    properties:
      votingPeriod:
        type: string
        description: Duration of the voting period.
    description: VotingParams defines the params for voting on governance proposals.
  cosmos.gov.v1beta1.WeightedVoteOption:
    type: object
    properties:
      option:
        description: >-
          option defines the valid vote options, it must not contain duplicate
          vote options.
        type: string
        enum:
          - VOTE_OPTION_UNSPECIFIED
          - VOTE_OPTION_YES
          - VOTE_OPTION_ABSTAIN
          - VOTE_OPTION_NO
          - VOTE_OPTION_NO_WITH_VETO
        default: VOTE_OPTION_UNSPECIFIED
      weight:
        type: string
        description: weight is the vote weight associated with the vote option.
    description: WeightedVoteOption defines a unit of vote for vote split.
  cosmos.group.v1.GroupInfo:
    type: object
    properties:
      id:
        type: string
        format: uint64
        description: id is the unique ID of the group.
      admin:
        type: string
        description: admin is the account address of the group's admin.
      metadata:
        type: string
        title: >-
          metadata is any arbitrary metadata to attached to the group.

          the recommended format of the metadata is to be found here:
          https://docs.cosmos.network/v0.47/modules/group#group-1
      version:
        type: string
        format: uint64
        title: >-
          version is used to track changes to a group's membership structure
          that

          would break existing proposals. Whenever any members weight is
          changed,

          or any member is added or removed this version is incremented and will

          cause proposals based on older versions of this group to fail
      totalWeight:
        type: string
        description: total_weight is the sum of the group members' weights.
      createdAt:
        type: string
        format: date-time
        description: created_at is a timestamp specifying when a group was created.
    description: GroupInfo represents the high-level on-chain information for a group.
  cosmos.group.v1.GroupMember:
    type: object
    properties:
      groupId:
        type: string
        format: uint64
        description: group_id is the unique ID of the group.
      member:
        description: member is the member data.
        type: object
        properties:
          address:
            type: string
            description: address is the member's account address.
          weight:
            type: string
            description: >-
              weight is the member's voting weight that should be greater than
              0.
          metadata:
            type: string
            description: metadata is any arbitrary metadata attached to the member.
          addedAt:
            type: string
            format: date-time
            description: added_at is a timestamp specifying when a member was added.
    description: GroupMember represents the relationship between a group and a member.
  cosmos.group.v1.GroupPolicyInfo:
    type: object
    properties:
      address:
        type: string
        description: address is the account address of group policy.
      groupId:
        type: string
        format: uint64
        description: group_id is the unique ID of the group.
      admin:
        type: string
        description: admin is the account address of the group admin.
      metadata:
        type: string
        title: |-
          metadata is any arbitrary metadata attached to the group policy.
          the recommended format of the metadata is to be found here:
          https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
      version:
        type: string
        format: uint64
        description: >-
          version is used to track changes to a group's GroupPolicyInfo
          structure that

          would create a different result on a running proposal.
      decisionPolicy:
        description: decision_policy specifies the group policy's decision policy.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      createdAt:
        type: string
        format: date-time
        description: created_at is a timestamp specifying when a group policy was created.
    description: >-
      GroupPolicyInfo represents the high-level on-chain information for a group
      policy.
  cosmos.group.v1.Member:
    type: object
    properties:
      address:
        type: string
        description: address is the member's account address.
      weight:
        type: string
        description: weight is the member's voting weight that should be greater than 0.
      metadata:
        type: string
        description: metadata is any arbitrary metadata attached to the member.
      addedAt:
        type: string
        format: date-time
        description: added_at is a timestamp specifying when a member was added.
    description: |-
      Member represents a group member with an account address,
      non-zero weight, metadata and added_at timestamp.
  cosmos.group.v1.Proposal:
    type: object
    properties:
      id:
        type: string
        format: uint64
        description: id is the unique id of the proposal.
      groupPolicyAddress:
        type: string
        description: group_policy_address is the account address of group policy.
      metadata:
        type: string
        title: |-
          metadata is any arbitrary metadata attached to the proposal.
          the recommended format of the metadata is to be found here:
          https://docs.cosmos.network/v0.47/modules/group#proposal-4
      proposers:
        type: array
        items:
          type: string
        description: proposers are the account addresses of the proposers.
      submitTime:
        type: string
        format: date-time
        description: submit_time is a timestamp specifying when a proposal was submitted.
      groupVersion:
        type: string
        format: uint64
        description: |-
          group_version tracks the version of the group at proposal submission.
          This field is here for informational purposes only.
      groupPolicyVersion:
        type: string
        format: uint64
        description: >-
          group_policy_version tracks the version of the group policy at
          proposal submission.

          When a decision policy is changed, existing proposals from previous
          policy

          versions will become invalid with the `ABORTED` status.

          This field is here for informational purposes only.
      status:
        description: >-
          status represents the high level position in the life cycle of the
          proposal. Initial value is Submitted.
        type: string
        enum:
          - PROPOSAL_STATUS_UNSPECIFIED
          - PROPOSAL_STATUS_SUBMITTED
          - PROPOSAL_STATUS_ACCEPTED
          - PROPOSAL_STATUS_REJECTED
          - PROPOSAL_STATUS_ABORTED
          - PROPOSAL_STATUS_WITHDRAWN
        default: PROPOSAL_STATUS_UNSPECIFIED
      finalTallyResult:
        description: >-
          final_tally_result contains the sums of all weighted votes for this

          proposal for each vote option. It is empty at submission, and only

          populated after tallying, at voting period end or at proposal
          execution,

          whichever happens first.
        type: object
        properties:
          yesCount:
            type: string
            description: yes_count is the weighted sum of yes votes.
          abstainCount:
            type: string
            description: abstain_count is the weighted sum of abstainers.
          noCount:
            type: string
            description: no_count is the weighted sum of no votes.
          noWithVetoCount:
            type: string
            description: no_with_veto_count is the weighted sum of veto.
      votingPeriodEnd:
        type: string
        format: date-time
        description: >-
          voting_period_end is the timestamp before which voting must be done.

          Unless a successful MsgExec is called before (to execute a proposal
          whose

          tally is successful before the voting period ends), tallying will be
          done

          at this point, and the `final_tally_result`and `status` fields will be

          accordingly updated.
      executorResult:
        description: >-
          executor_result is the final result of the proposal execution. Initial
          value is NotRun.
        type: string
        enum:
          - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
          - PROPOSAL_EXECUTOR_RESULT_NOT_RUN
          - PROPOSAL_EXECUTOR_RESULT_SUCCESS
          - PROPOSAL_EXECUTOR_RESULT_FAILURE
        default: PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
      messages:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        description: >-
          messages is a list of `sdk.Msg`s that will be executed if the proposal
          passes.
      title:
        type: string
        title: title is the title of the proposal
      summary:
        type: string
        title: summary is a short summary of the proposal
    description: >-
      Proposal defines a group proposal. Any member of a group can submit a
      proposal

      for a group policy to decide upon.

      A proposal consists of a set of `sdk.Msg`s that will be executed if the
      proposal

      passes as well as some optional metadata associated with the proposal.
  cosmos.group.v1.ProposalExecutorResult:
    type: string
    enum:
      - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
      - PROPOSAL_EXECUTOR_RESULT_NOT_RUN
      - PROPOSAL_EXECUTOR_RESULT_SUCCESS
      - PROPOSAL_EXECUTOR_RESULT_FAILURE
    default: PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
    description: |-
      ProposalExecutorResult defines types of proposal executor results.

       - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED: An empty value is not allowed.
       - PROPOSAL_EXECUTOR_RESULT_NOT_RUN: We have not yet run the executor.
       - PROPOSAL_EXECUTOR_RESULT_SUCCESS: The executor was successful and proposed action updated state.
       - PROPOSAL_EXECUTOR_RESULT_FAILURE: The executor returned an error and proposed action didn't update state.
  cosmos.group.v1.ProposalStatus:
    type: string
    enum:
      - PROPOSAL_STATUS_UNSPECIFIED
      - PROPOSAL_STATUS_SUBMITTED
      - PROPOSAL_STATUS_ACCEPTED
      - PROPOSAL_STATUS_REJECTED
      - PROPOSAL_STATUS_ABORTED
      - PROPOSAL_STATUS_WITHDRAWN
    default: PROPOSAL_STATUS_UNSPECIFIED
    description: |-
      ProposalStatus defines proposal statuses.

       - PROPOSAL_STATUS_UNSPECIFIED: An empty value is invalid and not allowed.
       - PROPOSAL_STATUS_SUBMITTED: Initial status of a proposal when submitted.
       - PROPOSAL_STATUS_ACCEPTED: Final status of a proposal when the final tally is done and the outcome
      passes the group policy's decision policy.
       - PROPOSAL_STATUS_REJECTED: Final status of a proposal when the final tally is done and the outcome
      is rejected by the group policy's decision policy.
       - PROPOSAL_STATUS_ABORTED: Final status of a proposal when the group policy is modified before the
      final tally.
       - PROPOSAL_STATUS_WITHDRAWN: A proposal can be withdrawn before the voting start time by the owner.
      When this happens the final status is Withdrawn.
  cosmos.group.v1.QueryGroupInfoResponse:
    type: object
    properties:
      info:
        description: info is the GroupInfo of the group.
        type: object
        properties:
          id:
            type: string
            format: uint64
            description: id is the unique ID of the group.
          admin:
            type: string
            description: admin is the account address of the group's admin.
          metadata:
            type: string
            title: >-
              metadata is any arbitrary metadata to attached to the group.

              the recommended format of the metadata is to be found here:
              https://docs.cosmos.network/v0.47/modules/group#group-1
          version:
            type: string
            format: uint64
            title: >-
              version is used to track changes to a group's membership structure
              that

              would break existing proposals. Whenever any members weight is
              changed,

              or any member is added or removed this version is incremented and
              will

              cause proposals based on older versions of this group to fail
          totalWeight:
            type: string
            description: total_weight is the sum of the group members' weights.
          createdAt:
            type: string
            format: date-time
            description: created_at is a timestamp specifying when a group was created.
    description: QueryGroupInfoResponse is the Query/GroupInfo response type.
  cosmos.group.v1.QueryGroupMembersResponse:
    type: object
    properties:
      members:
        type: array
        items:
          type: object
          properties:
            groupId:
              type: string
              format: uint64
              description: group_id is the unique ID of the group.
            member:
              description: member is the member data.
              type: object
              properties:
                address:
                  type: string
                  description: address is the member's account address.
                weight:
                  type: string
                  description: >-
                    weight is the member's voting weight that should be greater
                    than 0.
                metadata:
                  type: string
                  description: metadata is any arbitrary metadata attached to the member.
                addedAt:
                  type: string
                  format: date-time
                  description: added_at is a timestamp specifying when a member was added.
          description: >-
            GroupMember represents the relationship between a group and a
            member.
        description: members are the members of the group with given group_id.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryGroupMembersResponse is the Query/GroupMembersResponse response type.
  cosmos.group.v1.QueryGroupPoliciesByAdminResponse:
    type: object
    properties:
      groupPolicies:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address is the account address of group policy.
            groupId:
              type: string
              format: uint64
              description: group_id is the unique ID of the group.
            admin:
              type: string
              description: admin is the account address of the group admin.
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata attached to the group policy.

                the recommended format of the metadata is to be found here:

                https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
            version:
              type: string
              format: uint64
              description: >-
                version is used to track changes to a group's GroupPolicyInfo
                structure that

                would create a different result on a running proposal.
            decisionPolicy:
              description: decision_policy specifies the group policy's decision policy.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            createdAt:
              type: string
              format: date-time
              description: >-
                created_at is a timestamp specifying when a group policy was
                created.
          description: >-
            GroupPolicyInfo represents the high-level on-chain information for a
            group policy.
        description: group_policies are the group policies info with provided admin.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGroupPoliciesByAdminResponse is the Query/GroupPoliciesByAdmin
      response type.
  cosmos.group.v1.QueryGroupPoliciesByGroupResponse:
    type: object
    properties:
      groupPolicies:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              description: address is the account address of group policy.
            groupId:
              type: string
              format: uint64
              description: group_id is the unique ID of the group.
            admin:
              type: string
              description: admin is the account address of the group admin.
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata attached to the group policy.

                the recommended format of the metadata is to be found here:

                https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
            version:
              type: string
              format: uint64
              description: >-
                version is used to track changes to a group's GroupPolicyInfo
                structure that

                would create a different result on a running proposal.
            decisionPolicy:
              description: decision_policy specifies the group policy's decision policy.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            createdAt:
              type: string
              format: date-time
              description: >-
                created_at is a timestamp specifying when a group policy was
                created.
          description: >-
            GroupPolicyInfo represents the high-level on-chain information for a
            group policy.
        description: >-
          group_policies are the group policies info associated with the
          provided group.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGroupPoliciesByGroupResponse is the Query/GroupPoliciesByGroup
      response type.
  cosmos.group.v1.QueryGroupPolicyInfoResponse:
    type: object
    properties:
      info:
        description: info is the GroupPolicyInfo of the group policy.
        type: object
        properties:
          address:
            type: string
            description: address is the account address of group policy.
          groupId:
            type: string
            format: uint64
            description: group_id is the unique ID of the group.
          admin:
            type: string
            description: admin is the account address of the group admin.
          metadata:
            type: string
            title: |-
              metadata is any arbitrary metadata attached to the group policy.
              the recommended format of the metadata is to be found here:
              https://docs.cosmos.network/v0.47/modules/group#decision-policy-1
          version:
            type: string
            format: uint64
            description: >-
              version is used to track changes to a group's GroupPolicyInfo
              structure that

              would create a different result on a running proposal.
          decisionPolicy:
            description: decision_policy specifies the group policy's decision policy.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          createdAt:
            type: string
            format: date-time
            description: >-
              created_at is a timestamp specifying when a group policy was
              created.
    description: QueryGroupPolicyInfoResponse is the Query/GroupPolicyInfo response type.
  cosmos.group.v1.QueryGroupsByAdminResponse:
    type: object
    properties:
      groups:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
              description: id is the unique ID of the group.
            admin:
              type: string
              description: admin is the account address of the group's admin.
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata to attached to the group.

                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/group#group-1
            version:
              type: string
              format: uint64
              title: >-
                version is used to track changes to a group's membership
                structure that

                would break existing proposals. Whenever any members weight is
                changed,

                or any member is added or removed this version is incremented
                and will

                cause proposals based on older versions of this group to fail
            totalWeight:
              type: string
              description: total_weight is the sum of the group members' weights.
            createdAt:
              type: string
              format: date-time
              description: created_at is a timestamp specifying when a group was created.
          description: >-
            GroupInfo represents the high-level on-chain information for a
            group.
        description: groups are the groups info with the provided admin.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryGroupsByAdminResponse is the Query/GroupsByAdminResponse response
      type.
  cosmos.group.v1.QueryGroupsByMemberResponse:
    type: object
    properties:
      groups:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
              description: id is the unique ID of the group.
            admin:
              type: string
              description: admin is the account address of the group's admin.
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata to attached to the group.

                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/group#group-1
            version:
              type: string
              format: uint64
              title: >-
                version is used to track changes to a group's membership
                structure that

                would break existing proposals. Whenever any members weight is
                changed,

                or any member is added or removed this version is incremented
                and will

                cause proposals based on older versions of this group to fail
            totalWeight:
              type: string
              description: total_weight is the sum of the group members' weights.
            createdAt:
              type: string
              format: date-time
              description: created_at is a timestamp specifying when a group was created.
          description: >-
            GroupInfo represents the high-level on-chain information for a
            group.
        description: groups are the groups info with the provided group member.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryGroupsByMemberResponse is the Query/GroupsByMember response type.
  cosmos.group.v1.QueryGroupsResponse:
    type: object
    properties:
      groups:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
              description: id is the unique ID of the group.
            admin:
              type: string
              description: admin is the account address of the group's admin.
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata to attached to the group.

                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/group#group-1
            version:
              type: string
              format: uint64
              title: >-
                version is used to track changes to a group's membership
                structure that

                would break existing proposals. Whenever any members weight is
                changed,

                or any member is added or removed this version is incremented
                and will

                cause proposals based on older versions of this group to fail
            totalWeight:
              type: string
              description: total_weight is the sum of the group members' weights.
            createdAt:
              type: string
              format: date-time
              description: created_at is a timestamp specifying when a group was created.
          description: >-
            GroupInfo represents the high-level on-chain information for a
            group.
        description: '`groups` is all the groups present in state.'
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryGroupsResponse is the Query/Groups response type.
  cosmos.group.v1.QueryProposalResponse:
    type: object
    properties:
      proposal:
        description: proposal is the proposal info.
        type: object
        properties:
          id:
            type: string
            format: uint64
            description: id is the unique id of the proposal.
          groupPolicyAddress:
            type: string
            description: group_policy_address is the account address of group policy.
          metadata:
            type: string
            title: |-
              metadata is any arbitrary metadata attached to the proposal.
              the recommended format of the metadata is to be found here:
              https://docs.cosmos.network/v0.47/modules/group#proposal-4
          proposers:
            type: array
            items:
              type: string
            description: proposers are the account addresses of the proposers.
          submitTime:
            type: string
            format: date-time
            description: >-
              submit_time is a timestamp specifying when a proposal was
              submitted.
          groupVersion:
            type: string
            format: uint64
            description: >-
              group_version tracks the version of the group at proposal
              submission.

              This field is here for informational purposes only.
          groupPolicyVersion:
            type: string
            format: uint64
            description: >-
              group_policy_version tracks the version of the group policy at
              proposal submission.

              When a decision policy is changed, existing proposals from
              previous policy

              versions will become invalid with the `ABORTED` status.

              This field is here for informational purposes only.
          status:
            description: >-
              status represents the high level position in the life cycle of the
              proposal. Initial value is Submitted.
            type: string
            enum:
              - PROPOSAL_STATUS_UNSPECIFIED
              - PROPOSAL_STATUS_SUBMITTED
              - PROPOSAL_STATUS_ACCEPTED
              - PROPOSAL_STATUS_REJECTED
              - PROPOSAL_STATUS_ABORTED
              - PROPOSAL_STATUS_WITHDRAWN
            default: PROPOSAL_STATUS_UNSPECIFIED
          finalTallyResult:
            description: >-
              final_tally_result contains the sums of all weighted votes for
              this

              proposal for each vote option. It is empty at submission, and only

              populated after tallying, at voting period end or at proposal
              execution,

              whichever happens first.
            type: object
            properties:
              yesCount:
                type: string
                description: yes_count is the weighted sum of yes votes.
              abstainCount:
                type: string
                description: abstain_count is the weighted sum of abstainers.
              noCount:
                type: string
                description: no_count is the weighted sum of no votes.
              noWithVetoCount:
                type: string
                description: no_with_veto_count is the weighted sum of veto.
          votingPeriodEnd:
            type: string
            format: date-time
            description: >-
              voting_period_end is the timestamp before which voting must be
              done.

              Unless a successful MsgExec is called before (to execute a
              proposal whose

              tally is successful before the voting period ends), tallying will
              be done

              at this point, and the `final_tally_result`and `status` fields
              will be

              accordingly updated.
          executorResult:
            description: >-
              executor_result is the final result of the proposal execution.
              Initial value is NotRun.
            type: string
            enum:
              - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
              - PROPOSAL_EXECUTOR_RESULT_NOT_RUN
              - PROPOSAL_EXECUTOR_RESULT_SUCCESS
              - PROPOSAL_EXECUTOR_RESULT_FAILURE
            default: PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
          messages:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            description: >-
              messages is a list of `sdk.Msg`s that will be executed if the
              proposal passes.
          title:
            type: string
            title: title is the title of the proposal
          summary:
            type: string
            title: summary is a short summary of the proposal
    description: QueryProposalResponse is the Query/Proposal response type.
  cosmos.group.v1.QueryProposalsByGroupPolicyResponse:
    type: object
    properties:
      proposals:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
              description: id is the unique id of the proposal.
            groupPolicyAddress:
              type: string
              description: group_policy_address is the account address of group policy.
            metadata:
              type: string
              title: |-
                metadata is any arbitrary metadata attached to the proposal.
                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/group#proposal-4
            proposers:
              type: array
              items:
                type: string
              description: proposers are the account addresses of the proposers.
            submitTime:
              type: string
              format: date-time
              description: >-
                submit_time is a timestamp specifying when a proposal was
                submitted.
            groupVersion:
              type: string
              format: uint64
              description: >-
                group_version tracks the version of the group at proposal
                submission.

                This field is here for informational purposes only.
            groupPolicyVersion:
              type: string
              format: uint64
              description: >-
                group_policy_version tracks the version of the group policy at
                proposal submission.

                When a decision policy is changed, existing proposals from
                previous policy

                versions will become invalid with the `ABORTED` status.

                This field is here for informational purposes only.
            status:
              description: >-
                status represents the high level position in the life cycle of
                the proposal. Initial value is Submitted.
              type: string
              enum:
                - PROPOSAL_STATUS_UNSPECIFIED
                - PROPOSAL_STATUS_SUBMITTED
                - PROPOSAL_STATUS_ACCEPTED
                - PROPOSAL_STATUS_REJECTED
                - PROPOSAL_STATUS_ABORTED
                - PROPOSAL_STATUS_WITHDRAWN
              default: PROPOSAL_STATUS_UNSPECIFIED
            finalTallyResult:
              description: >-
                final_tally_result contains the sums of all weighted votes for
                this

                proposal for each vote option. It is empty at submission, and
                only

                populated after tallying, at voting period end or at proposal
                execution,

                whichever happens first.
              type: object
              properties:
                yesCount:
                  type: string
                  description: yes_count is the weighted sum of yes votes.
                abstainCount:
                  type: string
                  description: abstain_count is the weighted sum of abstainers.
                noCount:
                  type: string
                  description: no_count is the weighted sum of no votes.
                noWithVetoCount:
                  type: string
                  description: no_with_veto_count is the weighted sum of veto.
            votingPeriodEnd:
              type: string
              format: date-time
              description: >-
                voting_period_end is the timestamp before which voting must be
                done.

                Unless a successful MsgExec is called before (to execute a
                proposal whose

                tally is successful before the voting period ends), tallying
                will be done

                at this point, and the `final_tally_result`and `status` fields
                will be

                accordingly updated.
            executorResult:
              description: >-
                executor_result is the final result of the proposal execution.
                Initial value is NotRun.
              type: string
              enum:
                - PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
                - PROPOSAL_EXECUTOR_RESULT_NOT_RUN
                - PROPOSAL_EXECUTOR_RESULT_SUCCESS
                - PROPOSAL_EXECUTOR_RESULT_FAILURE
              default: PROPOSAL_EXECUTOR_RESULT_UNSPECIFIED
            messages:
              type: array
              items:
                type: object
                properties:
                  '@type':
                    type: string
                    description: >-
                      A URL/resource name that uniquely identifies the type of
                      the serialized

                      protocol buffer message. This string must contain at least

                      one "/" character. The last segment of the URL's path must
                      represent

                      the fully qualified name of the type (as in

                      `path/google.protobuf.Duration`). The name should be in a
                      canonical form

                      (e.g., leading "." is not accepted).


                      In practice, teams usually precompile into the binary all
                      types that they

                      expect it to use in the context of Any. However, for URLs
                      which use the

                      scheme `http`, `https`, or no scheme, one can optionally
                      set up a type

                      server that maps type URLs to message definitions as
                      follows:


                      * If no scheme is provided, `https` is assumed.

                      * An HTTP GET on the URL must yield a
                      [google.protobuf.Type][]
                        value in binary format, or produce an error.
                      * Applications are allowed to cache lookup results based
                      on the
                        URL, or have them precompiled into a binary to avoid any
                        lookup. Therefore, binary compatibility needs to be preserved
                        on changes to types. (Use versioned type names to manage
                        breaking changes.)

                      Note: this functionality is not currently available in the
                      official

                      protobuf release, and it is not used for type URLs
                      beginning with

                      type.googleapis.com. As of May 2023, there are no widely
                      used type server

                      implementations and no plans to implement one.


                      Schemes other than `http`, `https` (or the empty scheme)
                      might be

                      used with implementation specific semantics.
                additionalProperties: {}
                description: >-
                  `Any` contains an arbitrary serialized protocol buffer message
                  along with a

                  URL that describes the type of the serialized message.


                  Protobuf library provides support to pack/unpack Any values in
                  the form

                  of utility functions or additional generated methods of the
                  Any type.


                  Example 1: Pack and unpack a message in C++.

                      Foo foo = ...;
                      Any any;
                      any.PackFrom(foo);
                      ...
                      if (any.UnpackTo(&foo)) {
                        ...
                      }

                  Example 2: Pack and unpack a message in Java.

                      Foo foo = ...;
                      Any any = Any.pack(foo);
                      ...
                      if (any.is(Foo.class)) {
                        foo = any.unpack(Foo.class);
                      }
                      // or ...
                      if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                        foo = any.unpack(Foo.getDefaultInstance());
                      }

                   Example 3: Pack and unpack a message in Python.

                      foo = Foo(...)
                      any = Any()
                      any.Pack(foo)
                      ...
                      if any.Is(Foo.DESCRIPTOR):
                        any.Unpack(foo)
                        ...

                   Example 4: Pack and unpack a message in Go

                       foo := &pb.Foo{...}
                       any, err := anypb.New(foo)
                       if err != nil {
                         ...
                       }
                       ...
                       foo := &pb.Foo{}
                       if err := any.UnmarshalTo(foo); err != nil {
                         ...
                       }

                  The pack methods provided by protobuf library will by default
                  use

                  'type.googleapis.com/full.type.name' as the type URL and the
                  unpack

                  methods only use the fully qualified type name after the last
                  '/'

                  in the type URL, for example "foo.bar.com/x/y.z" will yield
                  type

                  name "y.z".


                  JSON

                  ====

                  The JSON representation of an `Any` value uses the regular

                  representation of the deserialized, embedded message, with an

                  additional field `@type` which contains the type URL. Example:

                      package google.profile;
                      message Person {
                        string first_name = 1;
                        string last_name = 2;
                      }

                      {
                        "@type": "type.googleapis.com/google.profile.Person",
                        "firstName": <string>,
                        "lastName": <string>
                      }

                  If the embedded message type is well-known and has a custom
                  JSON

                  representation, that representation will be embedded adding a
                  field

                  `value` which holds the custom JSON in addition to the `@type`

                  field. Example (for message [google.protobuf.Duration][]):

                      {
                        "@type": "type.googleapis.com/google.protobuf.Duration",
                        "value": "1.212s"
                      }
              description: >-
                messages is a list of `sdk.Msg`s that will be executed if the
                proposal passes.
            title:
              type: string
              title: title is the title of the proposal
            summary:
              type: string
              title: summary is a short summary of the proposal
          description: >-
            Proposal defines a group proposal. Any member of a group can submit
            a proposal

            for a group policy to decide upon.

            A proposal consists of a set of `sdk.Msg`s that will be executed if
            the proposal

            passes as well as some optional metadata associated with the
            proposal.
        description: proposals are the proposals with given group policy.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryProposalsByGroupPolicyResponse is the Query/ProposalByGroupPolicy
      response type.
  cosmos.group.v1.QueryTallyResultResponse:
    type: object
    properties:
      tally:
        description: tally defines the requested tally.
        type: object
        properties:
          yesCount:
            type: string
            description: yes_count is the weighted sum of yes votes.
          abstainCount:
            type: string
            description: abstain_count is the weighted sum of abstainers.
          noCount:
            type: string
            description: no_count is the weighted sum of no votes.
          noWithVetoCount:
            type: string
            description: no_with_veto_count is the weighted sum of veto.
    description: QueryTallyResultResponse is the Query/TallyResult response type.
  cosmos.group.v1.QueryVoteByProposalVoterResponse:
    type: object
    properties:
      vote:
        description: vote is the vote with given proposal_id and voter.
        type: object
        properties:
          proposalId:
            type: string
            format: uint64
            description: proposal is the unique ID of the proposal.
          voter:
            type: string
            description: voter is the account address of the voter.
          option:
            description: option is the voter's choice on the proposal.
            type: string
            enum:
              - VOTE_OPTION_UNSPECIFIED
              - VOTE_OPTION_YES
              - VOTE_OPTION_ABSTAIN
              - VOTE_OPTION_NO
              - VOTE_OPTION_NO_WITH_VETO
            default: VOTE_OPTION_UNSPECIFIED
          metadata:
            type: string
            title: >-
              metadata is any arbitrary metadata attached to the vote.

              the recommended format of the metadata is to be found here:
              https://docs.cosmos.network/v0.47/modules/group#vote-2
          submitTime:
            type: string
            format: date-time
            description: submit_time is the timestamp when the vote was submitted.
        title: Vote represents a vote for a proposal.string metadata
    description: >-
      QueryVoteByProposalVoterResponse is the Query/VoteByProposalVoter response
      type.
  cosmos.group.v1.QueryVotesByProposalResponse:
    type: object
    properties:
      votes:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal is the unique ID of the proposal.
            voter:
              type: string
              description: voter is the account address of the voter.
            option:
              description: option is the voter's choice on the proposal.
              type: string
              enum:
                - VOTE_OPTION_UNSPECIFIED
                - VOTE_OPTION_YES
                - VOTE_OPTION_ABSTAIN
                - VOTE_OPTION_NO
                - VOTE_OPTION_NO_WITH_VETO
              default: VOTE_OPTION_UNSPECIFIED
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata attached to the vote.

                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/group#vote-2
            submitTime:
              type: string
              format: date-time
              description: submit_time is the timestamp when the vote was submitted.
          title: Vote represents a vote for a proposal.string metadata
        description: votes are the list of votes for given proposal_id.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryVotesByProposalResponse is the Query/VotesByProposal response type.
  cosmos.group.v1.QueryVotesByVoterResponse:
    type: object
    properties:
      votes:
        type: array
        items:
          type: object
          properties:
            proposalId:
              type: string
              format: uint64
              description: proposal is the unique ID of the proposal.
            voter:
              type: string
              description: voter is the account address of the voter.
            option:
              description: option is the voter's choice on the proposal.
              type: string
              enum:
                - VOTE_OPTION_UNSPECIFIED
                - VOTE_OPTION_YES
                - VOTE_OPTION_ABSTAIN
                - VOTE_OPTION_NO
                - VOTE_OPTION_NO_WITH_VETO
              default: VOTE_OPTION_UNSPECIFIED
            metadata:
              type: string
              title: >-
                metadata is any arbitrary metadata attached to the vote.

                the recommended format of the metadata is to be found here:
                https://docs.cosmos.network/v0.47/modules/group#vote-2
            submitTime:
              type: string
              format: date-time
              description: submit_time is the timestamp when the vote was submitted.
          title: Vote represents a vote for a proposal.string metadata
        description: votes are the list of votes by given voter.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: QueryVotesByVoterResponse is the Query/VotesByVoter response type.
  cosmos.group.v1.TallyResult:
    type: object
    properties:
      yesCount:
        type: string
        description: yes_count is the weighted sum of yes votes.
      abstainCount:
        type: string
        description: abstain_count is the weighted sum of abstainers.
      noCount:
        type: string
        description: no_count is the weighted sum of no votes.
      noWithVetoCount:
        type: string
        description: no_with_veto_count is the weighted sum of veto.
    description: TallyResult represents the sum of weighted votes for each vote option.
  cosmos.group.v1.Vote:
    type: object
    properties:
      proposalId:
        type: string
        format: uint64
        description: proposal is the unique ID of the proposal.
      voter:
        type: string
        description: voter is the account address of the voter.
      option:
        description: option is the voter's choice on the proposal.
        type: string
        enum:
          - VOTE_OPTION_UNSPECIFIED
          - VOTE_OPTION_YES
          - VOTE_OPTION_ABSTAIN
          - VOTE_OPTION_NO
          - VOTE_OPTION_NO_WITH_VETO
        default: VOTE_OPTION_UNSPECIFIED
      metadata:
        type: string
        title: >-
          metadata is any arbitrary metadata attached to the vote.

          the recommended format of the metadata is to be found here:
          https://docs.cosmos.network/v0.47/modules/group#vote-2
      submitTime:
        type: string
        format: date-time
        description: submit_time is the timestamp when the vote was submitted.
    title: Vote represents a vote for a proposal.string metadata
  cosmos.group.v1.VoteOption:
    type: string
    enum:
      - VOTE_OPTION_UNSPECIFIED
      - VOTE_OPTION_YES
      - VOTE_OPTION_ABSTAIN
      - VOTE_OPTION_NO
      - VOTE_OPTION_NO_WITH_VETO
    default: VOTE_OPTION_UNSPECIFIED
    description: |-
      VoteOption enumerates the valid vote options for a given proposal.

       - VOTE_OPTION_UNSPECIFIED: VOTE_OPTION_UNSPECIFIED defines an unspecified vote option which will
      return an error.
       - VOTE_OPTION_YES: VOTE_OPTION_YES defines a yes vote option.
       - VOTE_OPTION_ABSTAIN: VOTE_OPTION_ABSTAIN defines an abstain vote option.
       - VOTE_OPTION_NO: VOTE_OPTION_NO defines a no vote option.
       - VOTE_OPTION_NO_WITH_VETO: VOTE_OPTION_NO_WITH_VETO defines a no with veto vote option.
  cosmos.mint.v1beta1.Params:
    type: object
    properties:
      mintDenom:
        type: string
        title: type of coin to mint
      inflationRateChange:
        type: string
        title: maximum annual change in inflation rate
      inflationMax:
        type: string
        title: maximum inflation rate
      inflationMin:
        type: string
        title: minimum inflation rate
      goalBonded:
        type: string
        title: goal of percent bonded atoms
      blocksPerYear:
        type: string
        format: uint64
        title: expected blocks per year
    description: Params defines the parameters for the x/mint module.
  cosmos.mint.v1beta1.QueryAnnualProvisionsResponse:
    type: object
    properties:
      annualProvisions:
        type: string
        format: byte
        description: annual_provisions is the current minting annual provisions value.
    description: |-
      QueryAnnualProvisionsResponse is the response type for the
      Query/AnnualProvisions RPC method.
  cosmos.mint.v1beta1.QueryInflationResponse:
    type: object
    properties:
      inflation:
        type: string
        format: byte
        description: inflation is the current minting inflation value.
    description: |-
      QueryInflationResponse is the response type for the Query/Inflation RPC
      method.
  cosmos.mint.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params defines the parameters of the module.
        type: object
        properties:
          mintDenom:
            type: string
            title: type of coin to mint
          inflationRateChange:
            type: string
            title: maximum annual change in inflation rate
          inflationMax:
            type: string
            title: maximum inflation rate
          inflationMin:
            type: string
            title: minimum inflation rate
          goalBonded:
            type: string
            title: goal of percent bonded atoms
          blocksPerYear:
            type: string
            format: uint64
            title: expected blocks per year
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  cosmos.nft.v1beta1.Class:
    type: object
    properties:
      id:
        type: string
        title: >-
          id defines the unique identifier of the NFT classification, similar to
          the contract address of ERC721
      name:
        type: string
        title: >-
          name defines the human-readable name of the NFT classification.
          Optional
      symbol:
        type: string
        title: symbol is an abbreviated name for nft classification. Optional
      description:
        type: string
        title: description is a brief description of nft classification. Optional
      uri:
        type: string
        title: >-
          uri for the class metadata stored off chain. It can define schema for
          Class and NFT `Data` attributes. Optional
      uriHash:
        type: string
        title: uri_hash is a hash of the document pointed by uri. Optional
      data:
        title: data is the app specific metadata of the NFT class. Optional
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
    description: Class defines the class of the nft type.
  cosmos.nft.v1beta1.NFT:
    type: object
    properties:
      classId:
        type: string
        title: >-
          class_id associated with the NFT, similar to the contract address of
          ERC721
      id:
        type: string
        title: id is a unique identifier of the NFT
      uri:
        type: string
        title: uri for the NFT metadata stored off chain
      uriHash:
        type: string
        title: uri_hash is a hash of the document pointed by uri
      data:
        title: data is an app specific data of the NFT. Optional
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
        description: >-
          `Any` contains an arbitrary serialized protocol buffer message along
          with a

          URL that describes the type of the serialized message.


          Protobuf library provides support to pack/unpack Any values in the
          form

          of utility functions or additional generated methods of the Any type.


          Example 1: Pack and unpack a message in C++.

              Foo foo = ...;
              Any any;
              any.PackFrom(foo);
              ...
              if (any.UnpackTo(&foo)) {
                ...
              }

          Example 2: Pack and unpack a message in Java.

              Foo foo = ...;
              Any any = Any.pack(foo);
              ...
              if (any.is(Foo.class)) {
                foo = any.unpack(Foo.class);
              }
              // or ...
              if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                foo = any.unpack(Foo.getDefaultInstance());
              }

           Example 3: Pack and unpack a message in Python.

              foo = Foo(...)
              any = Any()
              any.Pack(foo)
              ...
              if any.Is(Foo.DESCRIPTOR):
                any.Unpack(foo)
                ...

           Example 4: Pack and unpack a message in Go

               foo := &pb.Foo{...}
               any, err := anypb.New(foo)
               if err != nil {
                 ...
               }
               ...
               foo := &pb.Foo{}
               if err := any.UnmarshalTo(foo); err != nil {
                 ...
               }

          The pack methods provided by protobuf library will by default use

          'type.googleapis.com/full.type.name' as the type URL and the unpack

          methods only use the fully qualified type name after the last '/'

          in the type URL, for example "foo.bar.com/x/y.z" will yield type

          name "y.z".


          JSON

          ====

          The JSON representation of an `Any` value uses the regular

          representation of the deserialized, embedded message, with an

          additional field `@type` which contains the type URL. Example:

              package google.profile;
              message Person {
                string first_name = 1;
                string last_name = 2;
              }

              {
                "@type": "type.googleapis.com/google.profile.Person",
                "firstName": <string>,
                "lastName": <string>
              }

          If the embedded message type is well-known and has a custom JSON

          representation, that representation will be embedded adding a field

          `value` which holds the custom JSON in addition to the `@type`

          field. Example (for message [google.protobuf.Duration][]):

              {
                "@type": "type.googleapis.com/google.protobuf.Duration",
                "value": "1.212s"
              }
    description: NFT defines the NFT.
  cosmos.nft.v1beta1.QueryBalanceResponse:
    type: object
    properties:
      amount:
        type: string
        format: uint64
        title: amount is the number of all NFTs of a given class owned by the owner
    title: QueryBalanceResponse is the response type for the Query/Balance RPC method
  cosmos.nft.v1beta1.QueryClassResponse:
    type: object
    properties:
      class:
        description: class defines the class of the nft type.
        type: object
        properties:
          id:
            type: string
            title: >-
              id defines the unique identifier of the NFT classification,
              similar to the contract address of ERC721
          name:
            type: string
            title: >-
              name defines the human-readable name of the NFT classification.
              Optional
          symbol:
            type: string
            title: symbol is an abbreviated name for nft classification. Optional
          description:
            type: string
            title: description is a brief description of nft classification. Optional
          uri:
            type: string
            title: >-
              uri for the class metadata stored off chain. It can define schema
              for Class and NFT `Data` attributes. Optional
          uriHash:
            type: string
            title: uri_hash is a hash of the document pointed by uri. Optional
          data:
            title: data is the app specific metadata of the NFT class. Optional
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
            description: >-
              `Any` contains an arbitrary serialized protocol buffer message
              along with a

              URL that describes the type of the serialized message.


              Protobuf library provides support to pack/unpack Any values in the
              form

              of utility functions or additional generated methods of the Any
              type.


              Example 1: Pack and unpack a message in C++.

                  Foo foo = ...;
                  Any any;
                  any.PackFrom(foo);
                  ...
                  if (any.UnpackTo(&foo)) {
                    ...
                  }

              Example 2: Pack and unpack a message in Java.

                  Foo foo = ...;
                  Any any = Any.pack(foo);
                  ...
                  if (any.is(Foo.class)) {
                    foo = any.unpack(Foo.class);
                  }
                  // or ...
                  if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                    foo = any.unpack(Foo.getDefaultInstance());
                  }

               Example 3: Pack and unpack a message in Python.

                  foo = Foo(...)
                  any = Any()
                  any.Pack(foo)
                  ...
                  if any.Is(Foo.DESCRIPTOR):
                    any.Unpack(foo)
                    ...

               Example 4: Pack and unpack a message in Go

                   foo := &pb.Foo{...}
                   any, err := anypb.New(foo)
                   if err != nil {
                     ...
                   }
                   ...
                   foo := &pb.Foo{}
                   if err := any.UnmarshalTo(foo); err != nil {
                     ...
                   }

              The pack methods provided by protobuf library will by default use

              'type.googleapis.com/full.type.name' as the type URL and the
              unpack

              methods only use the fully qualified type name after the last '/'

              in the type URL, for example "foo.bar.com/x/y.z" will yield type

              name "y.z".


              JSON

              ====

              The JSON representation of an `Any` value uses the regular

              representation of the deserialized, embedded message, with an

              additional field `@type` which contains the type URL. Example:

                  package google.profile;
                  message Person {
                    string first_name = 1;
                    string last_name = 2;
                  }

                  {
                    "@type": "type.googleapis.com/google.profile.Person",
                    "firstName": <string>,
                    "lastName": <string>
                  }

              If the embedded message type is well-known and has a custom JSON

              representation, that representation will be embedded adding a
              field

              `value` which holds the custom JSON in addition to the `@type`

              field. Example (for message [google.protobuf.Duration][]):

                  {
                    "@type": "type.googleapis.com/google.protobuf.Duration",
                    "value": "1.212s"
                  }
    title: QueryClassResponse is the response type for the Query/Class RPC method
  cosmos.nft.v1beta1.QueryClassesResponse:
    type: object
    properties:
      classes:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              title: >-
                id defines the unique identifier of the NFT classification,
                similar to the contract address of ERC721
            name:
              type: string
              title: >-
                name defines the human-readable name of the NFT classification.
                Optional
            symbol:
              type: string
              title: symbol is an abbreviated name for nft classification. Optional
            description:
              type: string
              title: >-
                description is a brief description of nft classification.
                Optional
            uri:
              type: string
              title: >-
                uri for the class metadata stored off chain. It can define
                schema for Class and NFT `Data` attributes. Optional
            uriHash:
              type: string
              title: uri_hash is a hash of the document pointed by uri. Optional
            data:
              title: data is the app specific metadata of the NFT class. Optional
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
          description: Class defines the class of the nft type.
        description: class defines the class of the nft type.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: QueryClassesResponse is the response type for the Query/Classes RPC method
  cosmos.nft.v1beta1.QueryNFTResponse:
    type: object
    properties:
      nft:
        title: owner is the owner address of the nft
        type: object
        properties:
          classId:
            type: string
            title: >-
              class_id associated with the NFT, similar to the contract address
              of ERC721
          id:
            type: string
            title: id is a unique identifier of the NFT
          uri:
            type: string
            title: uri for the NFT metadata stored off chain
          uriHash:
            type: string
            title: uri_hash is a hash of the document pointed by uri
          data:
            title: data is an app specific data of the NFT. Optional
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
            description: >-
              `Any` contains an arbitrary serialized protocol buffer message
              along with a

              URL that describes the type of the serialized message.


              Protobuf library provides support to pack/unpack Any values in the
              form

              of utility functions or additional generated methods of the Any
              type.


              Example 1: Pack and unpack a message in C++.

                  Foo foo = ...;
                  Any any;
                  any.PackFrom(foo);
                  ...
                  if (any.UnpackTo(&foo)) {
                    ...
                  }

              Example 2: Pack and unpack a message in Java.

                  Foo foo = ...;
                  Any any = Any.pack(foo);
                  ...
                  if (any.is(Foo.class)) {
                    foo = any.unpack(Foo.class);
                  }
                  // or ...
                  if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                    foo = any.unpack(Foo.getDefaultInstance());
                  }

               Example 3: Pack and unpack a message in Python.

                  foo = Foo(...)
                  any = Any()
                  any.Pack(foo)
                  ...
                  if any.Is(Foo.DESCRIPTOR):
                    any.Unpack(foo)
                    ...

               Example 4: Pack and unpack a message in Go

                   foo := &pb.Foo{...}
                   any, err := anypb.New(foo)
                   if err != nil {
                     ...
                   }
                   ...
                   foo := &pb.Foo{}
                   if err := any.UnmarshalTo(foo); err != nil {
                     ...
                   }

              The pack methods provided by protobuf library will by default use

              'type.googleapis.com/full.type.name' as the type URL and the
              unpack

              methods only use the fully qualified type name after the last '/'

              in the type URL, for example "foo.bar.com/x/y.z" will yield type

              name "y.z".


              JSON

              ====

              The JSON representation of an `Any` value uses the regular

              representation of the deserialized, embedded message, with an

              additional field `@type` which contains the type URL. Example:

                  package google.profile;
                  message Person {
                    string first_name = 1;
                    string last_name = 2;
                  }

                  {
                    "@type": "type.googleapis.com/google.profile.Person",
                    "firstName": <string>,
                    "lastName": <string>
                  }

              If the embedded message type is well-known and has a custom JSON

              representation, that representation will be embedded adding a
              field

              `value` which holds the custom JSON in addition to the `@type`

              field. Example (for message [google.protobuf.Duration][]):

                  {
                    "@type": "type.googleapis.com/google.protobuf.Duration",
                    "value": "1.212s"
                  }
        description: NFT defines the NFT.
    title: QueryNFTResponse is the response type for the Query/NFT RPC method
  cosmos.nft.v1beta1.QueryNFTsResponse:
    type: object
    properties:
      nfts:
        type: array
        items:
          type: object
          properties:
            classId:
              type: string
              title: >-
                class_id associated with the NFT, similar to the contract
                address of ERC721
            id:
              type: string
              title: id is a unique identifier of the NFT
            uri:
              type: string
              title: uri for the NFT metadata stored off chain
            uriHash:
              type: string
              title: uri_hash is a hash of the document pointed by uri
            data:
              title: data is an app specific data of the NFT. Optional
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
          description: NFT defines the NFT.
        title: NFT defines the NFT
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: QueryNFTsResponse is the response type for the Query/NFTs RPC methods
  cosmos.nft.v1beta1.QueryOwnerResponse:
    type: object
    properties:
      owner:
        type: string
        title: owner is the owner address of the nft
    title: QueryOwnerResponse is the response type for the Query/Owner RPC method
  cosmos.nft.v1beta1.QuerySupplyResponse:
    type: object
    properties:
      amount:
        type: string
        format: uint64
        title: amount is the number of all NFTs from the given class
    title: QuerySupplyResponse is the response type for the Query/Supply RPC method
  cosmos.params.v1beta1.ParamChange:
    type: object
    properties:
      subspace:
        type: string
      key:
        type: string
      value:
        type: string
    description: |-
      ParamChange defines an individual parameter change, for use in
      ParameterChangeProposal.
  cosmos.params.v1beta1.QueryParamsResponse:
    type: object
    properties:
      param:
        description: param defines the queried parameter.
        type: object
        properties:
          subspace:
            type: string
          key:
            type: string
          value:
            type: string
    description: QueryParamsResponse is response type for the Query/Params RPC method.
  cosmos.params.v1beta1.QuerySubspacesResponse:
    type: object
    properties:
      subspaces:
        type: array
        items:
          type: object
          properties:
            subspace:
              type: string
            keys:
              type: array
              items:
                type: string
          description: >-
            Subspace defines a parameter subspace name and all the keys that
            exist for

            the subspace.
    description: |-
      QuerySubspacesResponse defines the response types for querying for all
      registered subspaces and all keys for a subspace.
  cosmos.params.v1beta1.Subspace:
    type: object
    properties:
      subspace:
        type: string
      keys:
        type: array
        items:
          type: string
    description: |-
      Subspace defines a parameter subspace name and all the keys that exist for
      the subspace.
  cosmos.protocolpool.v1.ContinuousFund:
    type: object
    properties:
      recipient:
        type: string
        description: Recipient is the address string of the account receiving funds.
      percentage:
        type: string
        description: >-
          Percentage is the percentage of funds to be allocated from Community
          pool.
      expiry:
        type: string
        format: date-time
        description: Optional, if expiry is set, removes the state object when expired.
    description: ContinuousFund defines the fields of continuous fund proposal.
  cosmos.protocolpool.v1.Params:
    type: object
    properties:
      enabledDistributionDenoms:
        type: array
        items:
          type: string
        description: >-
          EnabledDistributionDenoms lists the denoms that are allowed to be
          distributed.

          This is to avoid spending time distributing undesired tokens to
          continuous funds and budgets.
      distributionFrequency:
        type: string
        format: uint64
        description: >-
          DistributionFrequency is the frequency (in terms of blocks) that funds
          are distributed out from the

          x/protocolpool module.
    description: Params defines the parameters for the protocolpool module.
  cosmos.protocolpool.v1.QueryCommunityPoolResponse:
    type: object
    properties:
      pool:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        description: pool defines community pool's coins.
    description: >-
      QueryCommunityPoolResponse is the response type for the
      Query/CommunityPool

      RPC method.
  cosmos.protocolpool.v1.QueryContinuousFundResponse:
    type: object
    properties:
      continuousFund:
        description: ContinuousFunds is the given continuous fund returned in the query.
        type: object
        properties:
          recipient:
            type: string
            description: Recipient is the address string of the account receiving funds.
          percentage:
            type: string
            description: >-
              Percentage is the percentage of funds to be allocated from
              Community pool.
          expiry:
            type: string
            format: date-time
            description: Optional, if expiry is set, removes the state object when expired.
    description: >-
      QueryUnclaimedBudgetResponse is the response type for the
      Query/ContinuousFund

      RPC method.
  cosmos.protocolpool.v1.QueryContinuousFundsResponse:
    type: object
    properties:
      continuousFunds:
        type: array
        items:
          type: object
          properties:
            recipient:
              type: string
              description: Recipient is the address string of the account receiving funds.
            percentage:
              type: string
              description: >-
                Percentage is the percentage of funds to be allocated from
                Community pool.
            expiry:
              type: string
              format: date-time
              description: >-
                Optional, if expiry is set, removes the state object when
                expired.
          description: ContinuousFund defines the fields of continuous fund proposal.
        description: ContinuousFunds defines all continuous funds in state.
    description: >-
      QueryUnclaimedBudgetResponse is the response type for the
      Query/ContinuousFunds

      RPC method.
  cosmos.protocolpool.v1.QueryParamsResponse:
    type: object
    properties:
      params:
        type: object
        properties:
          enabledDistributionDenoms:
            type: array
            items:
              type: string
            description: >-
              EnabledDistributionDenoms lists the denoms that are allowed to be
              distributed.

              This is to avoid spending time distributing undesired tokens to
              continuous funds and budgets.
          distributionFrequency:
            type: string
            format: uint64
            description: >-
              DistributionFrequency is the frequency (in terms of blocks) that
              funds are distributed out from the

              x/protocolpool module.
        description: Params defines the parameters for the protocolpool module.
    description: QueryParamsResponse is the response type for the Query/Params RPC method.
  cosmos.slashing.v1beta1.Params:
    type: object
    properties:
      signedBlocksWindow:
        type: string
        format: int64
      minSignedPerWindow:
        type: string
        format: byte
      downtimeJailDuration:
        type: string
      slashFractionDoubleSign:
        type: string
        format: byte
      slashFractionDowntime:
        type: string
        format: byte
    description: Params represents the parameters used for by the slashing module.
  cosmos.slashing.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        type: object
        properties:
          signedBlocksWindow:
            type: string
            format: int64
          minSignedPerWindow:
            type: string
            format: byte
          downtimeJailDuration:
            type: string
          slashFractionDoubleSign:
            type: string
            format: byte
          slashFractionDowntime:
            type: string
            format: byte
        description: Params represents the parameters used for by the slashing module.
    title: QueryParamsResponse is the response type for the Query/Params RPC method
  cosmos.slashing.v1beta1.QuerySigningInfoResponse:
    type: object
    properties:
      valSigningInfo:
        title: val_signing_info is the signing info of requested val cons address
        type: object
        properties:
          address:
            type: string
          startHeight:
            type: string
            format: int64
            title: Height at which validator was first a candidate OR was un-jailed
          indexOffset:
            type: string
            format: int64
            description: >-
              Index which is incremented every time a validator is bonded in a
              block and

              _may_ have signed a pre-commit or not. This in conjunction with
              the

              signed_blocks_window param determines the index in the missed
              block bitmap.
          jailedUntil:
            type: string
            format: date-time
            description: >-
              Timestamp until which the validator is jailed due to liveness
              downtime.
          tombstoned:
            type: boolean
            description: >-
              Whether or not a validator has been tombstoned (killed out of
              validator

              set). It is set once the validator commits an equivocation or for
              any other

              configured misbehavior.
          missedBlocksCounter:
            type: string
            format: int64
            description: >-
              A counter of missed (unsigned) blocks. It is used to avoid
              unnecessary

              reads in the missed block bitmap.
        description: >-
          ValidatorSigningInfo defines a validator's signing info for monitoring
          their

          liveness activity.
    title: >-
      QuerySigningInfoResponse is the response type for the Query/SigningInfo
      RPC

      method
  cosmos.slashing.v1beta1.QuerySigningInfosResponse:
    type: object
    properties:
      info:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
            startHeight:
              type: string
              format: int64
              title: Height at which validator was first a candidate OR was un-jailed
            indexOffset:
              type: string
              format: int64
              description: >-
                Index which is incremented every time a validator is bonded in a
                block and

                _may_ have signed a pre-commit or not. This in conjunction with
                the

                signed_blocks_window param determines the index in the missed
                block bitmap.
            jailedUntil:
              type: string
              format: date-time
              description: >-
                Timestamp until which the validator is jailed due to liveness
                downtime.
            tombstoned:
              type: boolean
              description: >-
                Whether or not a validator has been tombstoned (killed out of
                validator

                set). It is set once the validator commits an equivocation or
                for any other

                configured misbehavior.
            missedBlocksCounter:
              type: string
              format: int64
              description: >-
                A counter of missed (unsigned) blocks. It is used to avoid
                unnecessary

                reads in the missed block bitmap.
          description: >-
            ValidatorSigningInfo defines a validator's signing info for
            monitoring their

            liveness activity.
        title: info is the signing info of all validators
      pagination:
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
    title: >-
      QuerySigningInfosResponse is the response type for the Query/SigningInfos
      RPC

      method
  cosmos.slashing.v1beta1.ValidatorSigningInfo:
    type: object
    properties:
      address:
        type: string
      startHeight:
        type: string
        format: int64
        title: Height at which validator was first a candidate OR was un-jailed
      indexOffset:
        type: string
        format: int64
        description: >-
          Index which is incremented every time a validator is bonded in a block
          and

          _may_ have signed a pre-commit or not. This in conjunction with the

          signed_blocks_window param determines the index in the missed block
          bitmap.
      jailedUntil:
        type: string
        format: date-time
        description: >-
          Timestamp until which the validator is jailed due to liveness
          downtime.
      tombstoned:
        type: boolean
        description: >-
          Whether or not a validator has been tombstoned (killed out of
          validator

          set). It is set once the validator commits an equivocation or for any
          other

          configured misbehavior.
      missedBlocksCounter:
        type: string
        format: int64
        description: |-
          A counter of missed (unsigned) blocks. It is used to avoid unnecessary
          reads in the missed block bitmap.
    description: >-
      ValidatorSigningInfo defines a validator's signing info for monitoring
      their

      liveness activity.
  cosmos.staking.v1beta1.BondStatus:
    type: string
    enum:
      - BOND_STATUS_UNSPECIFIED
      - BOND_STATUS_UNBONDED
      - BOND_STATUS_UNBONDING
      - BOND_STATUS_BONDED
    default: BOND_STATUS_UNSPECIFIED
    description: |-
      BondStatus is the status of a validator.

       - BOND_STATUS_UNSPECIFIED: UNSPECIFIED defines an invalid validator status.
       - BOND_STATUS_UNBONDED: UNBONDED defines a validator that is not bonded.
       - BOND_STATUS_UNBONDING: UNBONDING defines a validator that is unbonding.
       - BOND_STATUS_BONDED: BONDED defines a validator that is bonded.
  cosmos.staking.v1beta1.Commission:
    type: object
    properties:
      commissionRates:
        description: >-
          commission_rates defines the initial commission rates to be used for
          creating a validator.
        type: object
        properties:
          rate:
            type: string
            description: rate is the commission rate charged to delegators, as a fraction.
          maxRate:
            type: string
            description: >-
              max_rate defines the maximum commission rate which validator can
              ever charge, as a fraction.
          maxChangeRate:
            type: string
            description: >-
              max_change_rate defines the maximum daily increase of the
              validator commission, as a fraction.
      updateTime:
        type: string
        format: date-time
        description: update_time is the last time the commission rate was changed.
    description: Commission defines commission parameters for a given validator.
  cosmos.staking.v1beta1.CommissionRates:
    type: object
    properties:
      rate:
        type: string
        description: rate is the commission rate charged to delegators, as a fraction.
      maxRate:
        type: string
        description: >-
          max_rate defines the maximum commission rate which validator can ever
          charge, as a fraction.
      maxChangeRate:
        type: string
        description: >-
          max_change_rate defines the maximum daily increase of the validator
          commission, as a fraction.
    description: >-
      CommissionRates defines the initial commission rates to be used for
      creating

      a validator.
  cosmos.staking.v1beta1.Delegation:
    type: object
    properties:
      delegatorAddress:
        type: string
        description: delegator_address is the encoded address of the delegator.
      validatorAddress:
        type: string
        description: validator_address is the encoded address of the validator.
      shares:
        type: string
        description: shares define the delegation shares received.
    description: |-
      Delegation represents the bond with tokens held by an account. It is
      owned by one delegator, and is associated with the voting power of one
      validator.
  cosmos.staking.v1beta1.DelegationResponse:
    type: object
    properties:
      delegation:
        type: object
        properties:
          delegatorAddress:
            type: string
            description: delegator_address is the encoded address of the delegator.
          validatorAddress:
            type: string
            description: validator_address is the encoded address of the validator.
          shares:
            type: string
            description: shares define the delegation shares received.
        description: |-
          Delegation represents the bond with tokens held by an account. It is
          owned by one delegator, and is associated with the voting power of one
          validator.
      balance:
        type: object
        properties:
          denom:
            type: string
          amount:
            type: string
        description: |-
          Coin defines a token with a denomination and an amount.

          NOTE: The amount field is an Int which implements the custom method
          signatures required by gogoproto.
    description: |-
      DelegationResponse is equivalent to Delegation except that it contains a
      balance in addition to shares which is more suitable for client responses.
  cosmos.staking.v1beta1.Description:
    type: object
    properties:
      moniker:
        type: string
        description: moniker defines a human-readable name for the validator.
      identity:
        type: string
        description: >-
          identity defines an optional identity signature (ex. UPort or
          Keybase).
      website:
        type: string
        description: website defines an optional website link.
      securityContact:
        type: string
        description: security_contact defines an optional email for security contact.
      details:
        type: string
        description: details define other optional details.
    description: Description defines a validator description.
  cosmos.staking.v1beta1.HistoricalInfo:
    type: object
    properties:
      header:
        type: object
        properties:
          version:
            title: basic block info
            type: object
            properties:
              block:
                type: string
                format: uint64
              app:
                type: string
                format: uint64
            description: >-
              Consensus captures the consensus rules for processing a block in
              the blockchain,

              including all blockchain data structures and the rules of the
              application's

              state transition machine.
          chainId:
            type: string
          height:
            type: string
            format: int64
          time:
            type: string
            format: date-time
          lastBlockId:
            title: prev block info
            type: object
            properties:
              hash:
                type: string
                format: byte
              partSetHeader:
                type: object
                properties:
                  total:
                    type: integer
                    format: int64
                  hash:
                    type: string
                    format: byte
                title: PartsetHeader
          lastCommitHash:
            type: string
            format: byte
            description: commit from validators from the last block
            title: hashes of block data
          dataHash:
            type: string
            format: byte
            title: transactions
          validatorsHash:
            type: string
            format: byte
            description: validators for the current block
            title: hashes from the app output from the prev block
          nextValidatorsHash:
            type: string
            format: byte
            title: validators for the next block
          consensusHash:
            type: string
            format: byte
            title: consensus params for current block
          appHash:
            type: string
            format: byte
            title: state after txs from the previous block
          lastResultsHash:
            type: string
            format: byte
            title: root hash of all results from the txs from the previous block
          evidenceHash:
            type: string
            format: byte
            description: evidence included in the block
            title: consensus info
          proposerAddress:
            type: string
            format: byte
            title: original proposer of the block
        description: Header defines the structure of a block header.
      valset:
        type: array
        items:
          type: object
          properties:
            operatorAddress:
              type: string
              description: >-
                operator_address defines the address of the validator's
                operator; bech encoded in JSON.
            consensusPubkey:
              description: >-
                consensus_pubkey is the consensus public key of the validator,
                as a Protobuf Any.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            jailed:
              type: boolean
              description: >-
                jailed defined whether the validator has been jailed from bonded
                status or not.
            status:
              description: status is the validator status (bonded/unbonding/unbonded).
              type: string
              enum:
                - BOND_STATUS_UNSPECIFIED
                - BOND_STATUS_UNBONDED
                - BOND_STATUS_UNBONDING
                - BOND_STATUS_BONDED
              default: BOND_STATUS_UNSPECIFIED
            tokens:
              type: string
              description: tokens define the delegated tokens (incl. self-delegation).
            delegatorShares:
              type: string
              description: >-
                delegator_shares defines total shares issued to a validator's
                delegators.
            description:
              description: description defines the description terms for the validator.
              type: object
              properties:
                moniker:
                  type: string
                  description: moniker defines a human-readable name for the validator.
                identity:
                  type: string
                  description: >-
                    identity defines an optional identity signature (ex. UPort
                    or Keybase).
                website:
                  type: string
                  description: website defines an optional website link.
                securityContact:
                  type: string
                  description: >-
                    security_contact defines an optional email for security
                    contact.
                details:
                  type: string
                  description: details define other optional details.
            unbondingHeight:
              type: string
              format: int64
              description: >-
                unbonding_height defines, if unbonding, the height at which this
                validator has begun unbonding.
            unbondingTime:
              type: string
              format: date-time
              description: >-
                unbonding_time defines, if unbonding, the min time for the
                validator to complete unbonding.
            commission:
              description: commission defines the commission parameters.
              type: object
              properties:
                commissionRates:
                  description: >-
                    commission_rates defines the initial commission rates to be
                    used for creating a validator.
                  type: object
                  properties:
                    rate:
                      type: string
                      description: >-
                        rate is the commission rate charged to delegators, as a
                        fraction.
                    maxRate:
                      type: string
                      description: >-
                        max_rate defines the maximum commission rate which
                        validator can ever charge, as a fraction.
                    maxChangeRate:
                      type: string
                      description: >-
                        max_change_rate defines the maximum daily increase of
                        the validator commission, as a fraction.
                updateTime:
                  type: string
                  format: date-time
                  description: >-
                    update_time is the last time the commission rate was
                    changed.
            minSelfDelegation:
              type: string
              description: >-
                min_self_delegation is the validator's self declared minimum
                self delegation.
            unbondingOnHoldRefCount:
              type: string
              format: int64
              title: >-
                strictly positive if this validator's unbonding has been stopped
                by external modules
            unbondingIds:
              type: array
              items:
                type: string
                format: uint64
              title: >-
                list of unbonding ids, each uniquely identifing an unbonding of
                this validator
          description: >-
            Validator defines a validator, together with the total amount of the

            Validator's bond shares and their exchange rate to coins. Slashing
            results in

            a decrease in the exchange rate, allowing correct calculation of
            future

            undelegations without iterating over delegators. When coins are
            delegated to

            this validator, the validator is credited with a delegation whose
            number of

            bond shares is based on the amount of coins delegated divided by the
            current

            exchange rate. Voting power can be calculated as total bonded shares

            multiplied by exchange rate.
    description: >-
      HistoricalInfo contains header and validator information for a given
      block.

      It is stored as part of staking module's state, which persists the `n`
      most

      recent HistoricalInfo

      (`n` is set by the staking module's `historical_entries` parameter).
  cosmos.staking.v1beta1.Params:
    type: object
    properties:
      unbondingTime:
        type: string
        description: unbonding_time is the time duration of unbonding.
      maxValidators:
        type: integer
        format: int64
        description: max_validators is the maximum number of validators.
      maxEntries:
        type: integer
        format: int64
        description: >-
          max_entries is the max entries for either unbonding delegation or
          redelegation (per pair/trio).
      historicalEntries:
        type: integer
        format: int64
        description: historical_entries is the number of historical entries to persist.
      bondDenom:
        type: string
        description: bond_denom defines the bondable coin denomination.
      minCommissionRate:
        type: string
        title: >-
          min_commission_rate is the chain-wide minimum commission rate that a
          validator can charge their delegators
    description: Params defines the parameters for the x/staking module.
  cosmos.staking.v1beta1.Pool:
    type: object
    properties:
      notBondedTokens:
        type: string
      bondedTokens:
        type: string
    description: |-
      Pool is used for tracking bonded and not-bonded token supply of the bond
      denomination.
  cosmos.staking.v1beta1.QueryDelegationResponse:
    type: object
    properties:
      delegationResponse:
        description: delegation_responses defines the delegation info of a delegation.
        type: object
        properties:
          delegation:
            type: object
            properties:
              delegatorAddress:
                type: string
                description: delegator_address is the encoded address of the delegator.
              validatorAddress:
                type: string
                description: validator_address is the encoded address of the validator.
              shares:
                type: string
                description: shares define the delegation shares received.
            description: >-
              Delegation represents the bond with tokens held by an account. It
              is

              owned by one delegator, and is associated with the voting power of
              one

              validator.
          balance:
            type: object
            properties:
              denom:
                type: string
              amount:
                type: string
            description: >-
              Coin defines a token with a denomination and an amount.


              NOTE: The amount field is an Int which implements the custom
              method

              signatures required by gogoproto.
    description: >-
      QueryDelegationResponse is response type for the Query/Delegation RPC
      method.
  cosmos.staking.v1beta1.QueryDelegatorDelegationsResponse:
    type: object
    properties:
      delegationResponses:
        type: array
        items:
          type: object
          properties:
            delegation:
              type: object
              properties:
                delegatorAddress:
                  type: string
                  description: delegator_address is the encoded address of the delegator.
                validatorAddress:
                  type: string
                  description: validator_address is the encoded address of the validator.
                shares:
                  type: string
                  description: shares define the delegation shares received.
              description: >-
                Delegation represents the bond with tokens held by an account.
                It is

                owned by one delegator, and is associated with the voting power
                of one

                validator.
            balance:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
          description: >-
            DelegationResponse is equivalent to Delegation except that it
            contains a

            balance in addition to shares which is more suitable for client
            responses.
        description: delegation_responses defines all the delegations' info of a delegator.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryDelegatorDelegationsResponse is response type for the
      Query/DelegatorDelegations RPC method.
  cosmos.staking.v1beta1.QueryDelegatorUnbondingDelegationsResponse:
    type: object
    properties:
      unbondingResponses:
        type: array
        items:
          type: object
          properties:
            delegatorAddress:
              type: string
              description: delegator_address is the encoded address of the delegator.
            validatorAddress:
              type: string
              description: validator_address is the encoded address of the validator.
            entries:
              type: array
              items:
                type: object
                properties:
                  creationHeight:
                    type: string
                    format: int64
                    description: >-
                      creation_height is the height which the unbonding took
                      place.
                  completionTime:
                    type: string
                    format: date-time
                    description: completion_time is the unix time for unbonding completion.
                  initialBalance:
                    type: string
                    description: >-
                      initial_balance defines the tokens initially scheduled to
                      receive at completion.
                  balance:
                    type: string
                    description: balance defines the tokens to receive at completion.
                  unbondingId:
                    type: string
                    format: uint64
                    title: Incrementing id that uniquely identifies this entry
                  unbondingOnHoldRefCount:
                    type: string
                    format: int64
                    title: >-
                      Strictly positive if this entry's unbonding has been
                      stopped by external modules
                description: >-
                  UnbondingDelegationEntry defines an unbonding object with
                  relevant metadata.
              description: |-
                entries are the unbonding delegation entries.

                unbonding delegation entries
          description: >-
            UnbondingDelegation stores all of a single delegator's unbonding
            bonds

            for a single validator in an time-ordered list.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryUnbondingDelegatorDelegationsResponse is response type for the
      Query/UnbondingDelegatorDelegations RPC method.
  cosmos.staking.v1beta1.QueryDelegatorValidatorResponse:
    type: object
    properties:
      validator:
        description: validator defines the validator info.
        type: object
        properties:
          operatorAddress:
            type: string
            description: >-
              operator_address defines the address of the validator's operator;
              bech encoded in JSON.
          consensusPubkey:
            description: >-
              consensus_pubkey is the consensus public key of the validator, as
              a Protobuf Any.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          jailed:
            type: boolean
            description: >-
              jailed defined whether the validator has been jailed from bonded
              status or not.
          status:
            description: status is the validator status (bonded/unbonding/unbonded).
            type: string
            enum:
              - BOND_STATUS_UNSPECIFIED
              - BOND_STATUS_UNBONDED
              - BOND_STATUS_UNBONDING
              - BOND_STATUS_BONDED
            default: BOND_STATUS_UNSPECIFIED
          tokens:
            type: string
            description: tokens define the delegated tokens (incl. self-delegation).
          delegatorShares:
            type: string
            description: >-
              delegator_shares defines total shares issued to a validator's
              delegators.
          description:
            description: description defines the description terms for the validator.
            type: object
            properties:
              moniker:
                type: string
                description: moniker defines a human-readable name for the validator.
              identity:
                type: string
                description: >-
                  identity defines an optional identity signature (ex. UPort or
                  Keybase).
              website:
                type: string
                description: website defines an optional website link.
              securityContact:
                type: string
                description: >-
                  security_contact defines an optional email for security
                  contact.
              details:
                type: string
                description: details define other optional details.
          unbondingHeight:
            type: string
            format: int64
            description: >-
              unbonding_height defines, if unbonding, the height at which this
              validator has begun unbonding.
          unbondingTime:
            type: string
            format: date-time
            description: >-
              unbonding_time defines, if unbonding, the min time for the
              validator to complete unbonding.
          commission:
            description: commission defines the commission parameters.
            type: object
            properties:
              commissionRates:
                description: >-
                  commission_rates defines the initial commission rates to be
                  used for creating a validator.
                type: object
                properties:
                  rate:
                    type: string
                    description: >-
                      rate is the commission rate charged to delegators, as a
                      fraction.
                  maxRate:
                    type: string
                    description: >-
                      max_rate defines the maximum commission rate which
                      validator can ever charge, as a fraction.
                  maxChangeRate:
                    type: string
                    description: >-
                      max_change_rate defines the maximum daily increase of the
                      validator commission, as a fraction.
              updateTime:
                type: string
                format: date-time
                description: update_time is the last time the commission rate was changed.
          minSelfDelegation:
            type: string
            description: >-
              min_self_delegation is the validator's self declared minimum self
              delegation.
          unbondingOnHoldRefCount:
            type: string
            format: int64
            title: >-
              strictly positive if this validator's unbonding has been stopped
              by external modules
          unbondingIds:
            type: array
            items:
              type: string
              format: uint64
            title: >-
              list of unbonding ids, each uniquely identifing an unbonding of
              this validator
    description: |-
      QueryDelegatorValidatorResponse response type for the
      Query/DelegatorValidator RPC method.
  cosmos.staking.v1beta1.QueryDelegatorValidatorsResponse:
    type: object
    properties:
      validators:
        type: array
        items:
          type: object
          properties:
            operatorAddress:
              type: string
              description: >-
                operator_address defines the address of the validator's
                operator; bech encoded in JSON.
            consensusPubkey:
              description: >-
                consensus_pubkey is the consensus public key of the validator,
                as a Protobuf Any.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            jailed:
              type: boolean
              description: >-
                jailed defined whether the validator has been jailed from bonded
                status or not.
            status:
              description: status is the validator status (bonded/unbonding/unbonded).
              type: string
              enum:
                - BOND_STATUS_UNSPECIFIED
                - BOND_STATUS_UNBONDED
                - BOND_STATUS_UNBONDING
                - BOND_STATUS_BONDED
              default: BOND_STATUS_UNSPECIFIED
            tokens:
              type: string
              description: tokens define the delegated tokens (incl. self-delegation).
            delegatorShares:
              type: string
              description: >-
                delegator_shares defines total shares issued to a validator's
                delegators.
            description:
              description: description defines the description terms for the validator.
              type: object
              properties:
                moniker:
                  type: string
                  description: moniker defines a human-readable name for the validator.
                identity:
                  type: string
                  description: >-
                    identity defines an optional identity signature (ex. UPort
                    or Keybase).
                website:
                  type: string
                  description: website defines an optional website link.
                securityContact:
                  type: string
                  description: >-
                    security_contact defines an optional email for security
                    contact.
                details:
                  type: string
                  description: details define other optional details.
            unbondingHeight:
              type: string
              format: int64
              description: >-
                unbonding_height defines, if unbonding, the height at which this
                validator has begun unbonding.
            unbondingTime:
              type: string
              format: date-time
              description: >-
                unbonding_time defines, if unbonding, the min time for the
                validator to complete unbonding.
            commission:
              description: commission defines the commission parameters.
              type: object
              properties:
                commissionRates:
                  description: >-
                    commission_rates defines the initial commission rates to be
                    used for creating a validator.
                  type: object
                  properties:
                    rate:
                      type: string
                      description: >-
                        rate is the commission rate charged to delegators, as a
                        fraction.
                    maxRate:
                      type: string
                      description: >-
                        max_rate defines the maximum commission rate which
                        validator can ever charge, as a fraction.
                    maxChangeRate:
                      type: string
                      description: >-
                        max_change_rate defines the maximum daily increase of
                        the validator commission, as a fraction.
                updateTime:
                  type: string
                  format: date-time
                  description: >-
                    update_time is the last time the commission rate was
                    changed.
            minSelfDelegation:
              type: string
              description: >-
                min_self_delegation is the validator's self declared minimum
                self delegation.
            unbondingOnHoldRefCount:
              type: string
              format: int64
              title: >-
                strictly positive if this validator's unbonding has been stopped
                by external modules
            unbondingIds:
              type: array
              items:
                type: string
                format: uint64
              title: >-
                list of unbonding ids, each uniquely identifing an unbonding of
                this validator
          description: >-
            Validator defines a validator, together with the total amount of the

            Validator's bond shares and their exchange rate to coins. Slashing
            results in

            a decrease in the exchange rate, allowing correct calculation of
            future

            undelegations without iterating over delegators. When coins are
            delegated to

            this validator, the validator is credited with a delegation whose
            number of

            bond shares is based on the amount of coins delegated divided by the
            current

            exchange rate. Voting power can be calculated as total bonded shares

            multiplied by exchange rate.
        description: validators defines the validators' info of a delegator.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryDelegatorValidatorsResponse is response type for the
      Query/DelegatorValidators RPC method.
  cosmos.staking.v1beta1.QueryHistoricalInfoResponse:
    type: object
    properties:
      hist:
        description: hist defines the historical info at the given height.
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                title: prev block info
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                format: byte
                title: original proposer of the block
            description: Header defines the structure of a block header.
          valset:
            type: array
            items:
              type: object
              properties:
                operatorAddress:
                  type: string
                  description: >-
                    operator_address defines the address of the validator's
                    operator; bech encoded in JSON.
                consensusPubkey:
                  description: >-
                    consensus_pubkey is the consensus public key of the
                    validator, as a Protobuf Any.
                  type: object
                  properties:
                    '@type':
                      type: string
                      description: >-
                        A URL/resource name that uniquely identifies the type of
                        the serialized

                        protocol buffer message. This string must contain at
                        least

                        one "/" character. The last segment of the URL's path
                        must represent

                        the fully qualified name of the type (as in

                        `path/google.protobuf.Duration`). The name should be in
                        a canonical form

                        (e.g., leading "." is not accepted).


                        In practice, teams usually precompile into the binary
                        all types that they

                        expect it to use in the context of Any. However, for
                        URLs which use the

                        scheme `http`, `https`, or no scheme, one can optionally
                        set up a type

                        server that maps type URLs to message definitions as
                        follows:


                        * If no scheme is provided, `https` is assumed.

                        * An HTTP GET on the URL must yield a
                        [google.protobuf.Type][]
                          value in binary format, or produce an error.
                        * Applications are allowed to cache lookup results based
                        on the
                          URL, or have them precompiled into a binary to avoid any
                          lookup. Therefore, binary compatibility needs to be preserved
                          on changes to types. (Use versioned type names to manage
                          breaking changes.)

                        Note: this functionality is not currently available in
                        the official

                        protobuf release, and it is not used for type URLs
                        beginning with

                        type.googleapis.com. As of May 2023, there are no widely
                        used type server

                        implementations and no plans to implement one.


                        Schemes other than `http`, `https` (or the empty scheme)
                        might be

                        used with implementation specific semantics.
                  additionalProperties: {}
                jailed:
                  type: boolean
                  description: >-
                    jailed defined whether the validator has been jailed from
                    bonded status or not.
                status:
                  description: status is the validator status (bonded/unbonding/unbonded).
                  type: string
                  enum:
                    - BOND_STATUS_UNSPECIFIED
                    - BOND_STATUS_UNBONDED
                    - BOND_STATUS_UNBONDING
                    - BOND_STATUS_BONDED
                  default: BOND_STATUS_UNSPECIFIED
                tokens:
                  type: string
                  description: tokens define the delegated tokens (incl. self-delegation).
                delegatorShares:
                  type: string
                  description: >-
                    delegator_shares defines total shares issued to a
                    validator's delegators.
                description:
                  description: description defines the description terms for the validator.
                  type: object
                  properties:
                    moniker:
                      type: string
                      description: moniker defines a human-readable name for the validator.
                    identity:
                      type: string
                      description: >-
                        identity defines an optional identity signature (ex.
                        UPort or Keybase).
                    website:
                      type: string
                      description: website defines an optional website link.
                    securityContact:
                      type: string
                      description: >-
                        security_contact defines an optional email for security
                        contact.
                    details:
                      type: string
                      description: details define other optional details.
                unbondingHeight:
                  type: string
                  format: int64
                  description: >-
                    unbonding_height defines, if unbonding, the height at which
                    this validator has begun unbonding.
                unbondingTime:
                  type: string
                  format: date-time
                  description: >-
                    unbonding_time defines, if unbonding, the min time for the
                    validator to complete unbonding.
                commission:
                  description: commission defines the commission parameters.
                  type: object
                  properties:
                    commissionRates:
                      description: >-
                        commission_rates defines the initial commission rates to
                        be used for creating a validator.
                      type: object
                      properties:
                        rate:
                          type: string
                          description: >-
                            rate is the commission rate charged to delegators,
                            as a fraction.
                        maxRate:
                          type: string
                          description: >-
                            max_rate defines the maximum commission rate which
                            validator can ever charge, as a fraction.
                        maxChangeRate:
                          type: string
                          description: >-
                            max_change_rate defines the maximum daily increase
                            of the validator commission, as a fraction.
                    updateTime:
                      type: string
                      format: date-time
                      description: >-
                        update_time is the last time the commission rate was
                        changed.
                minSelfDelegation:
                  type: string
                  description: >-
                    min_self_delegation is the validator's self declared minimum
                    self delegation.
                unbondingOnHoldRefCount:
                  type: string
                  format: int64
                  title: >-
                    strictly positive if this validator's unbonding has been
                    stopped by external modules
                unbondingIds:
                  type: array
                  items:
                    type: string
                    format: uint64
                  title: >-
                    list of unbonding ids, each uniquely identifing an unbonding
                    of this validator
              description: >-
                Validator defines a validator, together with the total amount of
                the

                Validator's bond shares and their exchange rate to coins.
                Slashing results in

                a decrease in the exchange rate, allowing correct calculation of
                future

                undelegations without iterating over delegators. When coins are
                delegated to

                this validator, the validator is credited with a delegation
                whose number of

                bond shares is based on the amount of coins delegated divided by
                the current

                exchange rate. Voting power can be calculated as total bonded
                shares

                multiplied by exchange rate.
    description: >-
      QueryHistoricalInfoResponse is response type for the Query/HistoricalInfo
      RPC

      method.
  cosmos.staking.v1beta1.QueryParamsResponse:
    type: object
    properties:
      params:
        description: params holds all the parameters of this module.
        type: object
        properties:
          unbondingTime:
            type: string
            description: unbonding_time is the time duration of unbonding.
          maxValidators:
            type: integer
            format: int64
            description: max_validators is the maximum number of validators.
          maxEntries:
            type: integer
            format: int64
            description: >-
              max_entries is the max entries for either unbonding delegation or
              redelegation (per pair/trio).
          historicalEntries:
            type: integer
            format: int64
            description: historical_entries is the number of historical entries to persist.
          bondDenom:
            type: string
            description: bond_denom defines the bondable coin denomination.
          minCommissionRate:
            type: string
            title: >-
              min_commission_rate is the chain-wide minimum commission rate that
              a validator can charge their delegators
    description: QueryParamsResponse is response type for the Query/Params RPC method.
  cosmos.staking.v1beta1.QueryPoolResponse:
    type: object
    properties:
      pool:
        description: pool defines the pool info.
        type: object
        properties:
          notBondedTokens:
            type: string
          bondedTokens:
            type: string
    description: QueryPoolResponse is response type for the Query/Pool RPC method.
  cosmos.staking.v1beta1.QueryRedelegationsResponse:
    type: object
    properties:
      redelegationResponses:
        type: array
        items:
          type: object
          properties:
            redelegation:
              type: object
              properties:
                delegatorAddress:
                  type: string
                  description: >-
                    delegator_address is the bech32-encoded address of the
                    delegator.
                validatorSrcAddress:
                  type: string
                  description: >-
                    validator_src_address is the validator redelegation source
                    operator address.
                validatorDstAddress:
                  type: string
                  description: >-
                    validator_dst_address is the validator redelegation
                    destination operator address.
                entries:
                  type: array
                  items:
                    type: object
                    properties:
                      creationHeight:
                        type: string
                        format: int64
                        description: >-
                          creation_height  defines the height which the
                          redelegation took place.
                      completionTime:
                        type: string
                        format: date-time
                        description: >-
                          completion_time defines the unix time for redelegation
                          completion.
                      initialBalance:
                        type: string
                        description: >-
                          initial_balance defines the initial balance when
                          redelegation started.
                      sharesDst:
                        type: string
                        description: >-
                          shares_dst is the amount of destination-validator
                          shares created by redelegation.
                      unbondingId:
                        type: string
                        format: uint64
                        title: Incrementing id that uniquely identifies this entry
                      unbondingOnHoldRefCount:
                        type: string
                        format: int64
                        title: >-
                          Strictly positive if this entry's unbonding has been
                          stopped by external modules
                    description: >-
                      RedelegationEntry defines a redelegation object with
                      relevant metadata.
                  description: |-
                    entries are the redelegation entries.

                    redelegation entries
              description: >-
                Redelegation contains the list of a particular delegator's
                redelegating bonds

                from a particular source validator to a particular destination
                validator.
            entries:
              type: array
              items:
                type: object
                properties:
                  redelegationEntry:
                    type: object
                    properties:
                      creationHeight:
                        type: string
                        format: int64
                        description: >-
                          creation_height  defines the height which the
                          redelegation took place.
                      completionTime:
                        type: string
                        format: date-time
                        description: >-
                          completion_time defines the unix time for redelegation
                          completion.
                      initialBalance:
                        type: string
                        description: >-
                          initial_balance defines the initial balance when
                          redelegation started.
                      sharesDst:
                        type: string
                        description: >-
                          shares_dst is the amount of destination-validator
                          shares created by redelegation.
                      unbondingId:
                        type: string
                        format: uint64
                        title: Incrementing id that uniquely identifies this entry
                      unbondingOnHoldRefCount:
                        type: string
                        format: int64
                        title: >-
                          Strictly positive if this entry's unbonding has been
                          stopped by external modules
                    description: >-
                      RedelegationEntry defines a redelegation object with
                      relevant metadata.
                  balance:
                    type: string
                description: >-
                  RedelegationEntryResponse is equivalent to a RedelegationEntry
                  except that it

                  contains a balance in addition to shares which is more
                  suitable for client

                  responses.
          description: >-
            RedelegationResponse is equivalent to a Redelegation except that its
            entries

            contain a balance in addition to shares which is more suitable for
            client

            responses.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      QueryRedelegationsResponse is response type for the Query/Redelegations
      RPC

      method.
  cosmos.staking.v1beta1.QueryUnbondingDelegationResponse:
    type: object
    properties:
      unbond:
        description: unbond defines the unbonding information of a delegation.
        type: object
        properties:
          delegatorAddress:
            type: string
            description: delegator_address is the encoded address of the delegator.
          validatorAddress:
            type: string
            description: validator_address is the encoded address of the validator.
          entries:
            type: array
            items:
              type: object
              properties:
                creationHeight:
                  type: string
                  format: int64
                  description: >-
                    creation_height is the height which the unbonding took
                    place.
                completionTime:
                  type: string
                  format: date-time
                  description: completion_time is the unix time for unbonding completion.
                initialBalance:
                  type: string
                  description: >-
                    initial_balance defines the tokens initially scheduled to
                    receive at completion.
                balance:
                  type: string
                  description: balance defines the tokens to receive at completion.
                unbondingId:
                  type: string
                  format: uint64
                  title: Incrementing id that uniquely identifies this entry
                unbondingOnHoldRefCount:
                  type: string
                  format: int64
                  title: >-
                    Strictly positive if this entry's unbonding has been stopped
                    by external modules
              description: >-
                UnbondingDelegationEntry defines an unbonding object with
                relevant metadata.
            description: |-
              entries are the unbonding delegation entries.

              unbonding delegation entries
    description: |-
      QueryDelegationResponse is response type for the Query/UnbondingDelegation
      RPC method.
  cosmos.staking.v1beta1.QueryValidatorDelegationsResponse:
    type: object
    properties:
      delegationResponses:
        type: array
        items:
          type: object
          properties:
            delegation:
              type: object
              properties:
                delegatorAddress:
                  type: string
                  description: delegator_address is the encoded address of the delegator.
                validatorAddress:
                  type: string
                  description: validator_address is the encoded address of the validator.
                shares:
                  type: string
                  description: shares define the delegation shares received.
              description: >-
                Delegation represents the bond with tokens held by an account.
                It is

                owned by one delegator, and is associated with the voting power
                of one

                validator.
            balance:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
          description: >-
            DelegationResponse is equivalent to Delegation except that it
            contains a

            balance in addition to shares which is more suitable for client
            responses.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: |-
      QueryValidatorDelegationsResponse is response type for the
      Query/ValidatorDelegations RPC method
  cosmos.staking.v1beta1.QueryValidatorResponse:
    type: object
    properties:
      validator:
        description: validator defines the validator info.
        type: object
        properties:
          operatorAddress:
            type: string
            description: >-
              operator_address defines the address of the validator's operator;
              bech encoded in JSON.
          consensusPubkey:
            description: >-
              consensus_pubkey is the consensus public key of the validator, as
              a Protobuf Any.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          jailed:
            type: boolean
            description: >-
              jailed defined whether the validator has been jailed from bonded
              status or not.
          status:
            description: status is the validator status (bonded/unbonding/unbonded).
            type: string
            enum:
              - BOND_STATUS_UNSPECIFIED
              - BOND_STATUS_UNBONDED
              - BOND_STATUS_UNBONDING
              - BOND_STATUS_BONDED
            default: BOND_STATUS_UNSPECIFIED
          tokens:
            type: string
            description: tokens define the delegated tokens (incl. self-delegation).
          delegatorShares:
            type: string
            description: >-
              delegator_shares defines total shares issued to a validator's
              delegators.
          description:
            description: description defines the description terms for the validator.
            type: object
            properties:
              moniker:
                type: string
                description: moniker defines a human-readable name for the validator.
              identity:
                type: string
                description: >-
                  identity defines an optional identity signature (ex. UPort or
                  Keybase).
              website:
                type: string
                description: website defines an optional website link.
              securityContact:
                type: string
                description: >-
                  security_contact defines an optional email for security
                  contact.
              details:
                type: string
                description: details define other optional details.
          unbondingHeight:
            type: string
            format: int64
            description: >-
              unbonding_height defines, if unbonding, the height at which this
              validator has begun unbonding.
          unbondingTime:
            type: string
            format: date-time
            description: >-
              unbonding_time defines, if unbonding, the min time for the
              validator to complete unbonding.
          commission:
            description: commission defines the commission parameters.
            type: object
            properties:
              commissionRates:
                description: >-
                  commission_rates defines the initial commission rates to be
                  used for creating a validator.
                type: object
                properties:
                  rate:
                    type: string
                    description: >-
                      rate is the commission rate charged to delegators, as a
                      fraction.
                  maxRate:
                    type: string
                    description: >-
                      max_rate defines the maximum commission rate which
                      validator can ever charge, as a fraction.
                  maxChangeRate:
                    type: string
                    description: >-
                      max_change_rate defines the maximum daily increase of the
                      validator commission, as a fraction.
              updateTime:
                type: string
                format: date-time
                description: update_time is the last time the commission rate was changed.
          minSelfDelegation:
            type: string
            description: >-
              min_self_delegation is the validator's self declared minimum self
              delegation.
          unbondingOnHoldRefCount:
            type: string
            format: int64
            title: >-
              strictly positive if this validator's unbonding has been stopped
              by external modules
          unbondingIds:
            type: array
            items:
              type: string
              format: uint64
            title: >-
              list of unbonding ids, each uniquely identifing an unbonding of
              this validator
    title: QueryValidatorResponse is response type for the Query/Validator RPC method
  cosmos.staking.v1beta1.QueryValidatorUnbondingDelegationsResponse:
    type: object
    properties:
      unbondingResponses:
        type: array
        items:
          type: object
          properties:
            delegatorAddress:
              type: string
              description: delegator_address is the encoded address of the delegator.
            validatorAddress:
              type: string
              description: validator_address is the encoded address of the validator.
            entries:
              type: array
              items:
                type: object
                properties:
                  creationHeight:
                    type: string
                    format: int64
                    description: >-
                      creation_height is the height which the unbonding took
                      place.
                  completionTime:
                    type: string
                    format: date-time
                    description: completion_time is the unix time for unbonding completion.
                  initialBalance:
                    type: string
                    description: >-
                      initial_balance defines the tokens initially scheduled to
                      receive at completion.
                  balance:
                    type: string
                    description: balance defines the tokens to receive at completion.
                  unbondingId:
                    type: string
                    format: uint64
                    title: Incrementing id that uniquely identifies this entry
                  unbondingOnHoldRefCount:
                    type: string
                    format: int64
                    title: >-
                      Strictly positive if this entry's unbonding has been
                      stopped by external modules
                description: >-
                  UnbondingDelegationEntry defines an unbonding object with
                  relevant metadata.
              description: |-
                entries are the unbonding delegation entries.

                unbonding delegation entries
          description: >-
            UnbondingDelegation stores all of a single delegator's unbonding
            bonds

            for a single validator in an time-ordered list.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: |-
      QueryValidatorUnbondingDelegationsResponse is response type for the
      Query/ValidatorUnbondingDelegations RPC method.
  cosmos.staking.v1beta1.QueryValidatorsResponse:
    type: object
    properties:
      validators:
        type: array
        items:
          type: object
          properties:
            operatorAddress:
              type: string
              description: >-
                operator_address defines the address of the validator's
                operator; bech encoded in JSON.
            consensusPubkey:
              description: >-
                consensus_pubkey is the consensus public key of the validator,
                as a Protobuf Any.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            jailed:
              type: boolean
              description: >-
                jailed defined whether the validator has been jailed from bonded
                status or not.
            status:
              description: status is the validator status (bonded/unbonding/unbonded).
              type: string
              enum:
                - BOND_STATUS_UNSPECIFIED
                - BOND_STATUS_UNBONDED
                - BOND_STATUS_UNBONDING
                - BOND_STATUS_BONDED
              default: BOND_STATUS_UNSPECIFIED
            tokens:
              type: string
              description: tokens define the delegated tokens (incl. self-delegation).
            delegatorShares:
              type: string
              description: >-
                delegator_shares defines total shares issued to a validator's
                delegators.
            description:
              description: description defines the description terms for the validator.
              type: object
              properties:
                moniker:
                  type: string
                  description: moniker defines a human-readable name for the validator.
                identity:
                  type: string
                  description: >-
                    identity defines an optional identity signature (ex. UPort
                    or Keybase).
                website:
                  type: string
                  description: website defines an optional website link.
                securityContact:
                  type: string
                  description: >-
                    security_contact defines an optional email for security
                    contact.
                details:
                  type: string
                  description: details define other optional details.
            unbondingHeight:
              type: string
              format: int64
              description: >-
                unbonding_height defines, if unbonding, the height at which this
                validator has begun unbonding.
            unbondingTime:
              type: string
              format: date-time
              description: >-
                unbonding_time defines, if unbonding, the min time for the
                validator to complete unbonding.
            commission:
              description: commission defines the commission parameters.
              type: object
              properties:
                commissionRates:
                  description: >-
                    commission_rates defines the initial commission rates to be
                    used for creating a validator.
                  type: object
                  properties:
                    rate:
                      type: string
                      description: >-
                        rate is the commission rate charged to delegators, as a
                        fraction.
                    maxRate:
                      type: string
                      description: >-
                        max_rate defines the maximum commission rate which
                        validator can ever charge, as a fraction.
                    maxChangeRate:
                      type: string
                      description: >-
                        max_change_rate defines the maximum daily increase of
                        the validator commission, as a fraction.
                updateTime:
                  type: string
                  format: date-time
                  description: >-
                    update_time is the last time the commission rate was
                    changed.
            minSelfDelegation:
              type: string
              description: >-
                min_self_delegation is the validator's self declared minimum
                self delegation.
            unbondingOnHoldRefCount:
              type: string
              format: int64
              title: >-
                strictly positive if this validator's unbonding has been stopped
                by external modules
            unbondingIds:
              type: array
              items:
                type: string
                format: uint64
              title: >-
                list of unbonding ids, each uniquely identifing an unbonding of
                this validator
          description: >-
            Validator defines a validator, together with the total amount of the

            Validator's bond shares and their exchange rate to coins. Slashing
            results in

            a decrease in the exchange rate, allowing correct calculation of
            future

            undelegations without iterating over delegators. When coins are
            delegated to

            this validator, the validator is credited with a delegation whose
            number of

            bond shares is based on the amount of coins delegated divided by the
            current

            exchange rate. Voting power can be calculated as total bonded shares

            multiplied by exchange rate.
        description: validators contains all the queried validators.
      pagination:
        description: pagination defines the pagination in the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    title: >-
      QueryValidatorsResponse is response type for the Query/Validators RPC
      method
  cosmos.staking.v1beta1.Redelegation:
    type: object
    properties:
      delegatorAddress:
        type: string
        description: delegator_address is the bech32-encoded address of the delegator.
      validatorSrcAddress:
        type: string
        description: >-
          validator_src_address is the validator redelegation source operator
          address.
      validatorDstAddress:
        type: string
        description: >-
          validator_dst_address is the validator redelegation destination
          operator address.
      entries:
        type: array
        items:
          type: object
          properties:
            creationHeight:
              type: string
              format: int64
              description: >-
                creation_height  defines the height which the redelegation took
                place.
            completionTime:
              type: string
              format: date-time
              description: >-
                completion_time defines the unix time for redelegation
                completion.
            initialBalance:
              type: string
              description: >-
                initial_balance defines the initial balance when redelegation
                started.
            sharesDst:
              type: string
              description: >-
                shares_dst is the amount of destination-validator shares created
                by redelegation.
            unbondingId:
              type: string
              format: uint64
              title: Incrementing id that uniquely identifies this entry
            unbondingOnHoldRefCount:
              type: string
              format: int64
              title: >-
                Strictly positive if this entry's unbonding has been stopped by
                external modules
          description: >-
            RedelegationEntry defines a redelegation object with relevant
            metadata.
        description: |-
          entries are the redelegation entries.

          redelegation entries
    description: >-
      Redelegation contains the list of a particular delegator's redelegating
      bonds

      from a particular source validator to a particular destination validator.
  cosmos.staking.v1beta1.RedelegationEntry:
    type: object
    properties:
      creationHeight:
        type: string
        format: int64
        description: creation_height  defines the height which the redelegation took place.
      completionTime:
        type: string
        format: date-time
        description: completion_time defines the unix time for redelegation completion.
      initialBalance:
        type: string
        description: initial_balance defines the initial balance when redelegation started.
      sharesDst:
        type: string
        description: >-
          shares_dst is the amount of destination-validator shares created by
          redelegation.
      unbondingId:
        type: string
        format: uint64
        title: Incrementing id that uniquely identifies this entry
      unbondingOnHoldRefCount:
        type: string
        format: int64
        title: >-
          Strictly positive if this entry's unbonding has been stopped by
          external modules
    description: RedelegationEntry defines a redelegation object with relevant metadata.
  cosmos.staking.v1beta1.RedelegationEntryResponse:
    type: object
    properties:
      redelegationEntry:
        type: object
        properties:
          creationHeight:
            type: string
            format: int64
            description: >-
              creation_height  defines the height which the redelegation took
              place.
          completionTime:
            type: string
            format: date-time
            description: completion_time defines the unix time for redelegation completion.
          initialBalance:
            type: string
            description: >-
              initial_balance defines the initial balance when redelegation
              started.
          sharesDst:
            type: string
            description: >-
              shares_dst is the amount of destination-validator shares created
              by redelegation.
          unbondingId:
            type: string
            format: uint64
            title: Incrementing id that uniquely identifies this entry
          unbondingOnHoldRefCount:
            type: string
            format: int64
            title: >-
              Strictly positive if this entry's unbonding has been stopped by
              external modules
        description: >-
          RedelegationEntry defines a redelegation object with relevant
          metadata.
      balance:
        type: string
    description: >-
      RedelegationEntryResponse is equivalent to a RedelegationEntry except that
      it

      contains a balance in addition to shares which is more suitable for client

      responses.
  cosmos.staking.v1beta1.RedelegationResponse:
    type: object
    properties:
      redelegation:
        type: object
        properties:
          delegatorAddress:
            type: string
            description: delegator_address is the bech32-encoded address of the delegator.
          validatorSrcAddress:
            type: string
            description: >-
              validator_src_address is the validator redelegation source
              operator address.
          validatorDstAddress:
            type: string
            description: >-
              validator_dst_address is the validator redelegation destination
              operator address.
          entries:
            type: array
            items:
              type: object
              properties:
                creationHeight:
                  type: string
                  format: int64
                  description: >-
                    creation_height  defines the height which the redelegation
                    took place.
                completionTime:
                  type: string
                  format: date-time
                  description: >-
                    completion_time defines the unix time for redelegation
                    completion.
                initialBalance:
                  type: string
                  description: >-
                    initial_balance defines the initial balance when
                    redelegation started.
                sharesDst:
                  type: string
                  description: >-
                    shares_dst is the amount of destination-validator shares
                    created by redelegation.
                unbondingId:
                  type: string
                  format: uint64
                  title: Incrementing id that uniquely identifies this entry
                unbondingOnHoldRefCount:
                  type: string
                  format: int64
                  title: >-
                    Strictly positive if this entry's unbonding has been stopped
                    by external modules
              description: >-
                RedelegationEntry defines a redelegation object with relevant
                metadata.
            description: |-
              entries are the redelegation entries.

              redelegation entries
        description: >-
          Redelegation contains the list of a particular delegator's
          redelegating bonds

          from a particular source validator to a particular destination
          validator.
      entries:
        type: array
        items:
          type: object
          properties:
            redelegationEntry:
              type: object
              properties:
                creationHeight:
                  type: string
                  format: int64
                  description: >-
                    creation_height  defines the height which the redelegation
                    took place.
                completionTime:
                  type: string
                  format: date-time
                  description: >-
                    completion_time defines the unix time for redelegation
                    completion.
                initialBalance:
                  type: string
                  description: >-
                    initial_balance defines the initial balance when
                    redelegation started.
                sharesDst:
                  type: string
                  description: >-
                    shares_dst is the amount of destination-validator shares
                    created by redelegation.
                unbondingId:
                  type: string
                  format: uint64
                  title: Incrementing id that uniquely identifies this entry
                unbondingOnHoldRefCount:
                  type: string
                  format: int64
                  title: >-
                    Strictly positive if this entry's unbonding has been stopped
                    by external modules
              description: >-
                RedelegationEntry defines a redelegation object with relevant
                metadata.
            balance:
              type: string
          description: >-
            RedelegationEntryResponse is equivalent to a RedelegationEntry
            except that it

            contains a balance in addition to shares which is more suitable for
            client

            responses.
    description: >-
      RedelegationResponse is equivalent to a Redelegation except that its
      entries

      contain a balance in addition to shares which is more suitable for client

      responses.
  cosmos.staking.v1beta1.UnbondingDelegation:
    type: object
    properties:
      delegatorAddress:
        type: string
        description: delegator_address is the encoded address of the delegator.
      validatorAddress:
        type: string
        description: validator_address is the encoded address of the validator.
      entries:
        type: array
        items:
          type: object
          properties:
            creationHeight:
              type: string
              format: int64
              description: creation_height is the height which the unbonding took place.
            completionTime:
              type: string
              format: date-time
              description: completion_time is the unix time for unbonding completion.
            initialBalance:
              type: string
              description: >-
                initial_balance defines the tokens initially scheduled to
                receive at completion.
            balance:
              type: string
              description: balance defines the tokens to receive at completion.
            unbondingId:
              type: string
              format: uint64
              title: Incrementing id that uniquely identifies this entry
            unbondingOnHoldRefCount:
              type: string
              format: int64
              title: >-
                Strictly positive if this entry's unbonding has been stopped by
                external modules
          description: >-
            UnbondingDelegationEntry defines an unbonding object with relevant
            metadata.
        description: |-
          entries are the unbonding delegation entries.

          unbonding delegation entries
    description: |-
      UnbondingDelegation stores all of a single delegator's unbonding bonds
      for a single validator in an time-ordered list.
  cosmos.staking.v1beta1.UnbondingDelegationEntry:
    type: object
    properties:
      creationHeight:
        type: string
        format: int64
        description: creation_height is the height which the unbonding took place.
      completionTime:
        type: string
        format: date-time
        description: completion_time is the unix time for unbonding completion.
      initialBalance:
        type: string
        description: >-
          initial_balance defines the tokens initially scheduled to receive at
          completion.
      balance:
        type: string
        description: balance defines the tokens to receive at completion.
      unbondingId:
        type: string
        format: uint64
        title: Incrementing id that uniquely identifies this entry
      unbondingOnHoldRefCount:
        type: string
        format: int64
        title: >-
          Strictly positive if this entry's unbonding has been stopped by
          external modules
    description: >-
      UnbondingDelegationEntry defines an unbonding object with relevant
      metadata.
  cosmos.staking.v1beta1.Validator:
    type: object
    properties:
      operatorAddress:
        type: string
        description: >-
          operator_address defines the address of the validator's operator; bech
          encoded in JSON.
      consensusPubkey:
        description: >-
          consensus_pubkey is the consensus public key of the validator, as a
          Protobuf Any.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      jailed:
        type: boolean
        description: >-
          jailed defined whether the validator has been jailed from bonded
          status or not.
      status:
        description: status is the validator status (bonded/unbonding/unbonded).
        type: string
        enum:
          - BOND_STATUS_UNSPECIFIED
          - BOND_STATUS_UNBONDED
          - BOND_STATUS_UNBONDING
          - BOND_STATUS_BONDED
        default: BOND_STATUS_UNSPECIFIED
      tokens:
        type: string
        description: tokens define the delegated tokens (incl. self-delegation).
      delegatorShares:
        type: string
        description: >-
          delegator_shares defines total shares issued to a validator's
          delegators.
      description:
        description: description defines the description terms for the validator.
        type: object
        properties:
          moniker:
            type: string
            description: moniker defines a human-readable name for the validator.
          identity:
            type: string
            description: >-
              identity defines an optional identity signature (ex. UPort or
              Keybase).
          website:
            type: string
            description: website defines an optional website link.
          securityContact:
            type: string
            description: security_contact defines an optional email for security contact.
          details:
            type: string
            description: details define other optional details.
      unbondingHeight:
        type: string
        format: int64
        description: >-
          unbonding_height defines, if unbonding, the height at which this
          validator has begun unbonding.
      unbondingTime:
        type: string
        format: date-time
        description: >-
          unbonding_time defines, if unbonding, the min time for the validator
          to complete unbonding.
      commission:
        description: commission defines the commission parameters.
        type: object
        properties:
          commissionRates:
            description: >-
              commission_rates defines the initial commission rates to be used
              for creating a validator.
            type: object
            properties:
              rate:
                type: string
                description: >-
                  rate is the commission rate charged to delegators, as a
                  fraction.
              maxRate:
                type: string
                description: >-
                  max_rate defines the maximum commission rate which validator
                  can ever charge, as a fraction.
              maxChangeRate:
                type: string
                description: >-
                  max_change_rate defines the maximum daily increase of the
                  validator commission, as a fraction.
          updateTime:
            type: string
            format: date-time
            description: update_time is the last time the commission rate was changed.
      minSelfDelegation:
        type: string
        description: >-
          min_self_delegation is the validator's self declared minimum self
          delegation.
      unbondingOnHoldRefCount:
        type: string
        format: int64
        title: >-
          strictly positive if this validator's unbonding has been stopped by
          external modules
      unbondingIds:
        type: array
        items:
          type: string
          format: uint64
        title: >-
          list of unbonding ids, each uniquely identifing an unbonding of this
          validator
    description: >-
      Validator defines a validator, together with the total amount of the

      Validator's bond shares and their exchange rate to coins. Slashing results
      in

      a decrease in the exchange rate, allowing correct calculation of future

      undelegations without iterating over delegators. When coins are delegated
      to

      this validator, the validator is credited with a delegation whose number
      of

      bond shares is based on the amount of coins delegated divided by the
      current

      exchange rate. Voting power can be calculated as total bonded shares

      multiplied by exchange rate.
  cosmos.base.abci.v1beta1.ABCIMessageLog:
    type: object
    properties:
      msgIndex:
        type: integer
        format: int64
      log:
        type: string
      events:
        type: array
        items:
          type: object
          properties:
            type:
              type: string
            attributes:
              type: array
              items:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                description: >-
                  Attribute defines an attribute wrapper where the key and value
                  are

                  strings instead of raw bytes.
          description: |-
            StringEvent defines en Event object wrapper where all the attributes
            contain key/value pairs that are strings instead of raw bytes.
        description: |-
          Events contains a slice of Event objects that were emitted during some
          execution.
    description: >-
      ABCIMessageLog defines a structure containing an indexed tx ABCI message
      log.
  cosmos.base.abci.v1beta1.Attribute:
    type: object
    properties:
      key:
        type: string
      value:
        type: string
    description: |-
      Attribute defines an attribute wrapper where the key and value are
      strings instead of raw bytes.
  cosmos.base.abci.v1beta1.GasInfo:
    type: object
    properties:
      gasWanted:
        type: string
        format: uint64
        description: GasWanted is the maximum units of work we allow this tx to perform.
      gasUsed:
        type: string
        format: uint64
        description: GasUsed is the amount of gas actually consumed.
    description: GasInfo defines tx execution gas context.
  cosmos.base.abci.v1beta1.Result:
    type: object
    properties:
      data:
        type: string
        format: byte
        description: >-
          Data is any data returned from message or handler execution. It MUST
          be

          length prefixed in order to separate data from multiple message
          executions.

          Deprecated. This field is still populated, but prefer msg_response
          instead

          because it also contains the Msg response typeURL.
      log:
        type: string
        description: Log contains the log information from message or handler execution.
      events:
        type: array
        items:
          type: object
          properties:
            type:
              type: string
            attributes:
              type: array
              items:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                  index:
                    type: boolean
                    title: nondeterministic
                description: >-
                  EventAttribute is a single key-value pair, associated with an
                  event.
          description: >-
            Event allows application developers to attach additional information
            to

            ResponseFinalizeBlock and ResponseCheckTx.

            Later, transactions may be queried using these events.
        description: >-
          Events contains a slice of Event objects that were emitted during
          message

          or handler execution.
      msgResponses:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        description: msg_responses contains the Msg handler responses type packed in Anys.
    description: Result is the union of ResponseFormat and ResponseCheckTx.
  cosmos.base.abci.v1beta1.StringEvent:
    type: object
    properties:
      type:
        type: string
      attributes:
        type: array
        items:
          type: object
          properties:
            key:
              type: string
            value:
              type: string
          description: |-
            Attribute defines an attribute wrapper where the key and value are
            strings instead of raw bytes.
    description: |-
      StringEvent defines en Event object wrapper where all the attributes
      contain key/value pairs that are strings instead of raw bytes.
  cosmos.base.abci.v1beta1.TxResponse:
    type: object
    properties:
      height:
        type: string
        format: int64
        title: The block height
      txhash:
        type: string
        description: The transaction hash.
      codespace:
        type: string
        title: Namespace for the Code
      code:
        type: integer
        format: int64
        description: Response code.
      data:
        type: string
        description: Result bytes, if any.
      rawLog:
        type: string
        description: |-
          The output of the application's logger (raw string). May be
          non-deterministic.
      logs:
        type: array
        items:
          type: object
          properties:
            msgIndex:
              type: integer
              format: int64
            log:
              type: string
            events:
              type: array
              items:
                type: object
                properties:
                  type:
                    type: string
                  attributes:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                      description: >-
                        Attribute defines an attribute wrapper where the key and
                        value are

                        strings instead of raw bytes.
                description: >-
                  StringEvent defines en Event object wrapper where all the
                  attributes

                  contain key/value pairs that are strings instead of raw bytes.
              description: >-
                Events contains a slice of Event objects that were emitted
                during some

                execution.
          description: >-
            ABCIMessageLog defines a structure containing an indexed tx ABCI
            message log.
        description: >-
          The output of the application's logger (typed). May be
          non-deterministic.
      info:
        type: string
        description: Additional information. May be non-deterministic.
      gasWanted:
        type: string
        format: int64
        description: Amount of gas requested for transaction.
      gasUsed:
        type: string
        format: int64
        description: Amount of gas consumed by transaction.
      tx:
        description: The request transaction bytes.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      timestamp:
        type: string
        description: >-
          Time of the previous block. For heights > 1, it's the weighted median
          of

          the timestamps of the valid votes in the block.LastCommit. For height
          == 1,

          it's genesis time.
      events:
        type: array
        items:
          type: object
          properties:
            type:
              type: string
            attributes:
              type: array
              items:
                type: object
                properties:
                  key:
                    type: string
                  value:
                    type: string
                  index:
                    type: boolean
                    title: nondeterministic
                description: >-
                  EventAttribute is a single key-value pair, associated with an
                  event.
          description: >-
            Event allows application developers to attach additional information
            to

            ResponseFinalizeBlock and ResponseCheckTx.

            Later, transactions may be queried using these events.
        description: >-
          Events defines all the events emitted by processing a transaction.
          Note,

          these events include those emitted by processing all the messages and
          those

          emitted from the ante. Whereas Logs contains the events, with

          additional metadata, emitted only by processing the messages.
    description: >-
      TxResponse defines a structure containing relevant tx data and metadata.
      The

      tags are stringified and the log is JSON decoded.
  cosmos.crypto.multisig.v1beta1.CompactBitArray:
    type: object
    properties:
      extraBitsStored:
        type: integer
        format: int64
      elems:
        type: string
        format: byte
    description: |-
      CompactBitArray is an implementation of a space efficient bit array.
      This is used to ensure that the encoded data takes up a minimal amount of
      space after proto encoding.
      This is not thread safe, and is not intended for concurrent usage.
  cosmos.tx.signing.v1beta1.SignMode:
    type: string
    enum:
      - SIGN_MODE_UNSPECIFIED
      - SIGN_MODE_DIRECT
      - SIGN_MODE_TEXTUAL
      - SIGN_MODE_DIRECT_AUX
      - SIGN_MODE_LEGACY_AMINO_JSON
      - SIGN_MODE_EIP_191
    default: SIGN_MODE_UNSPECIFIED
    description: |-
      SignMode represents a signing mode with its own security guarantees.

      This enum should be considered a registry of all known sign modes
      in the Cosmos ecosystem. Apps are not expected to support all known
      sign modes. Apps that would like to support custom  sign modes are
      encouraged to open a small PR against this file to add a new case
      to this SignMode enum describing their sign mode so that different
      apps have a consistent version of this enum.

       - SIGN_MODE_UNSPECIFIED: SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
      rejected.
       - SIGN_MODE_DIRECT: SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
      verified with raw bytes from Tx.
       - SIGN_MODE_TEXTUAL: SIGN_MODE_TEXTUAL is a future signing mode that will verify some
      human-readable textual representation on top of the binary representation
      from SIGN_MODE_DIRECT.

      Since: cosmos-sdk 0.50
       - SIGN_MODE_DIRECT_AUX: SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
      SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
      require signers signing over other signers' `signer_info`.

      Since: cosmos-sdk 0.46
       - SIGN_MODE_LEGACY_AMINO_JSON: SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
      Amino JSON and will be removed in the future.
       - SIGN_MODE_EIP_191: SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
      SDK. Ref: https://eips.ethereum.org/EIPS/eip-191

      Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
      but is not implemented on the SDK by default. To enable EIP-191, you need
      to pass a custom `TxConfig` that has an implementation of
      `SignModeHandler` for EIP-191. The SDK may decide to fully support
      EIP-191 in the future.

      Since: cosmos-sdk 0.45.2
  cosmos.tx.v1beta1.AuthInfo:
    type: object
    properties:
      signerInfos:
        type: array
        items:
          type: object
          $ref: '#/definitions/cosmos.tx.v1beta1.SignerInfo'
        description: >-
          signer_infos defines the signing modes for the required signers. The
          number

          and order of elements must match the required signers from TxBody's

          messages. The first element is the primary signer and the one which
          pays

          the fee.
      fee:
        description: >-
          Fee is the fee and gas limit for the transaction. The first signer is
          the

          primary signer and the one which pays the fee. The fee can be
          calculated

          based on the cost of evaluating the body and doing signature
          verification

          of the signers. This can be estimated via simulation.
        type: object
        properties:
          amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: amount is the amount of coins to be paid as a fee
          gasLimit:
            type: string
            format: uint64
            title: >-
              gas_limit is the maximum gas that can be used in transaction
              processing

              before an out of gas error occurs
          payer:
            type: string
            description: >-
              if unset, the first signer is responsible for paying the fees. If
              set, the

              specified account must pay the fees. the payer must be a tx signer
              (and

              thus have signed this field in AuthInfo). setting this field does
              *not*

              change the ordering of required signers for the transaction.
          granter:
            type: string
            title: >-
              if set, the fee payer (either the first signer or the value of the
              payer

              field) requests that a fee grant be used to pay fees instead of
              the fee

              payer's own balance. If an appropriate fee grant does not exist or
              the

              chain does not support fee grants, this will fail
      tip:
        description: >-
          Tip is the optional tip used for transactions fees paid in another
          denom.


          This field is ignored if the chain didn't enable tips, i.e. didn't add
          the

          `TipDecorator` in its posthandler.
        type: object
        properties:
          amount:
            type: array
            items:
              type: object
              properties:
                denom:
                  type: string
                amount:
                  type: string
              description: >-
                Coin defines a token with a denomination and an amount.


                NOTE: The amount field is an Int which implements the custom
                method

                signatures required by gogoproto.
            title: amount is the amount of the tip
          tipper:
            type: string
            title: tipper is the address of the account paying for the tip
    description: |-
      AuthInfo describes the fee and signer modes that are used to sign a
      transaction.
  cosmos.tx.v1beta1.BroadcastMode:
    type: string
    enum:
      - BROADCAST_MODE_UNSPECIFIED
      - BROADCAST_MODE_BLOCK
      - BROADCAST_MODE_SYNC
      - BROADCAST_MODE_ASYNC
    default: BROADCAST_MODE_UNSPECIFIED
    description: |-
      BroadcastMode specifies the broadcast mode for the TxService.Broadcast RPC
      method.

       - BROADCAST_MODE_UNSPECIFIED: zero-value for mode ordering
       - BROADCAST_MODE_BLOCK: DEPRECATED: use BROADCAST_MODE_SYNC instead,
      BROADCAST_MODE_BLOCK is not supported by the SDK from v0.47.x onwards.
       - BROADCAST_MODE_SYNC: BROADCAST_MODE_SYNC defines a tx broadcasting mode where the client waits
      for a CheckTx execution response only.
       - BROADCAST_MODE_ASYNC: BROADCAST_MODE_ASYNC defines a tx broadcasting mode where the client
      returns immediately.
  cosmos.tx.v1beta1.BroadcastTxRequest:
    type: object
    properties:
      txBytes:
        type: string
        format: byte
        description: tx_bytes is the raw transaction.
      mode:
        type: string
        enum:
          - BROADCAST_MODE_UNSPECIFIED
          - BROADCAST_MODE_BLOCK
          - BROADCAST_MODE_SYNC
          - BROADCAST_MODE_ASYNC
        default: BROADCAST_MODE_UNSPECIFIED
        description: >-
          BroadcastMode specifies the broadcast mode for the TxService.Broadcast
          RPC

          method.

           - BROADCAST_MODE_UNSPECIFIED: zero-value for mode ordering
           - BROADCAST_MODE_BLOCK: DEPRECATED: use BROADCAST_MODE_SYNC instead,
          BROADCAST_MODE_BLOCK is not supported by the SDK from v0.47.x onwards.
           - BROADCAST_MODE_SYNC: BROADCAST_MODE_SYNC defines a tx broadcasting mode where the client waits
          for a CheckTx execution response only.
           - BROADCAST_MODE_ASYNC: BROADCAST_MODE_ASYNC defines a tx broadcasting mode where the client
          returns immediately.
    description: |-
      BroadcastTxRequest is the request type for the Service.BroadcastTxRequest
      RPC method.
  cosmos.tx.v1beta1.BroadcastTxResponse:
    type: object
    properties:
      txResponse:
        description: tx_response is the queried TxResponses.
        type: object
        properties:
          height:
            type: string
            format: int64
            title: The block height
          txhash:
            type: string
            description: The transaction hash.
          codespace:
            type: string
            title: Namespace for the Code
          code:
            type: integer
            format: int64
            description: Response code.
          data:
            type: string
            description: Result bytes, if any.
          rawLog:
            type: string
            description: |-
              The output of the application's logger (raw string). May be
              non-deterministic.
          logs:
            type: array
            items:
              type: object
              properties:
                msgIndex:
                  type: integer
                  format: int64
                log:
                  type: string
                events:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                      attributes:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                            value:
                              type: string
                          description: >-
                            Attribute defines an attribute wrapper where the key
                            and value are

                            strings instead of raw bytes.
                    description: >-
                      StringEvent defines en Event object wrapper where all the
                      attributes

                      contain key/value pairs that are strings instead of raw
                      bytes.
                  description: >-
                    Events contains a slice of Event objects that were emitted
                    during some

                    execution.
              description: >-
                ABCIMessageLog defines a structure containing an indexed tx ABCI
                message log.
            description: >-
              The output of the application's logger (typed). May be
              non-deterministic.
          info:
            type: string
            description: Additional information. May be non-deterministic.
          gasWanted:
            type: string
            format: int64
            description: Amount of gas requested for transaction.
          gasUsed:
            type: string
            format: int64
            description: Amount of gas consumed by transaction.
          tx:
            description: The request transaction bytes.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          timestamp:
            type: string
            description: >-
              Time of the previous block. For heights > 1, it's the weighted
              median of

              the timestamps of the valid votes in the block.LastCommit. For
              height == 1,

              it's genesis time.
          events:
            type: array
            items:
              type: object
              properties:
                type:
                  type: string
                attributes:
                  type: array
                  items:
                    type: object
                    properties:
                      key:
                        type: string
                      value:
                        type: string
                      index:
                        type: boolean
                        title: nondeterministic
                    description: >-
                      EventAttribute is a single key-value pair, associated with
                      an event.
              description: >-
                Event allows application developers to attach additional
                information to

                ResponseFinalizeBlock and ResponseCheckTx.

                Later, transactions may be queried using these events.
            description: >-
              Events defines all the events emitted by processing a transaction.
              Note,

              these events include those emitted by processing all the messages
              and those

              emitted from the ante. Whereas Logs contains the events, with

              additional metadata, emitted only by processing the messages.
    description: |-
      BroadcastTxResponse is the response type for the
      Service.BroadcastTx method.
  cosmos.tx.v1beta1.Fee:
    type: object
    properties:
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: amount is the amount of coins to be paid as a fee
      gasLimit:
        type: string
        format: uint64
        title: >-
          gas_limit is the maximum gas that can be used in transaction
          processing

          before an out of gas error occurs
      payer:
        type: string
        description: >-
          if unset, the first signer is responsible for paying the fees. If set,
          the

          specified account must pay the fees. the payer must be a tx signer
          (and

          thus have signed this field in AuthInfo). setting this field does
          *not*

          change the ordering of required signers for the transaction.
      granter:
        type: string
        title: >-
          if set, the fee payer (either the first signer or the value of the
          payer

          field) requests that a fee grant be used to pay fees instead of the
          fee

          payer's own balance. If an appropriate fee grant does not exist or the

          chain does not support fee grants, this will fail
    description: >-
      Fee includes the amount of coins paid in fees and the maximum

      gas to be used by the transaction. The ratio yields an effective
      "gasprice",

      which must be above some miminum to be accepted into the mempool.
  cosmos.tx.v1beta1.GetBlockWithTxsResponse:
    type: object
    properties:
      txs:
        type: array
        items:
          type: object
          $ref: '#/definitions/cosmos.tx.v1beta1.Tx'
        description: txs are the transactions in the block.
      blockId:
        type: object
        properties:
          hash:
            type: string
            format: byte
          partSetHeader:
            type: object
            properties:
              total:
                type: integer
                format: int64
              hash:
                type: string
                format: byte
            title: PartsetHeader
        title: BlockID
      block:
        type: object
        properties:
          header:
            type: object
            properties:
              version:
                title: basic block info
                type: object
                properties:
                  block:
                    type: string
                    format: uint64
                  app:
                    type: string
                    format: uint64
                description: >-
                  Consensus captures the consensus rules for processing a block
                  in the blockchain,

                  including all blockchain data structures and the rules of the
                  application's

                  state transition machine.
              chainId:
                type: string
              height:
                type: string
                format: int64
              time:
                type: string
                format: date-time
              lastBlockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              lastCommitHash:
                type: string
                format: byte
                description: commit from validators from the last block
                title: hashes of block data
              dataHash:
                type: string
                format: byte
                title: transactions
              validatorsHash:
                type: string
                format: byte
                description: validators for the current block
                title: hashes from the app output from the prev block
              nextValidatorsHash:
                type: string
                format: byte
                title: validators for the next block
              consensusHash:
                type: string
                format: byte
                title: consensus params for current block
              appHash:
                type: string
                format: byte
                title: state after txs from the previous block
              lastResultsHash:
                type: string
                format: byte
                title: root hash of all results from the txs from the previous block
              evidenceHash:
                type: string
                format: byte
                description: evidence included in the block
                title: consensus info
              proposerAddress:
                type: string
                format: byte
                title: original proposer of the block
            description: Header defines the structure of a block header.
          data:
            type: object
            properties:
              txs:
                type: array
                items:
                  type: string
                  format: byte
                description: >-
                  Txs that will be applied by state @ block.Height+1.

                  NOTE: not all txs here are valid.  We're just agreeing on the
                  order first.

                  This means that block.AppHash does not include these txs.
            title: Data contains the set of transactions included in the block
          evidence:
            type: object
            properties:
              evidence:
                type: array
                items:
                  type: object
                  properties:
                    duplicateVoteEvidence:
                      type: object
                      properties:
                        voteA:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        voteB:
                          type: object
                          properties:
                            type:
                              type: string
                              enum:
                                - SIGNED_MSG_TYPE_UNKNOWN
                                - SIGNED_MSG_TYPE_PREVOTE
                                - SIGNED_MSG_TYPE_PRECOMMIT
                                - SIGNED_MSG_TYPE_PROPOSAL
                              default: SIGNED_MSG_TYPE_UNKNOWN
                              description: >-
                                SignedMsgType is a type of signed message in the
                                consensus.

                                 - SIGNED_MSG_TYPE_PREVOTE: Votes
                                 - SIGNED_MSG_TYPE_PROPOSAL: Proposals
                            height:
                              type: string
                              format: int64
                            round:
                              type: integer
                              format: int32
                            blockId:
                              type: object
                              properties:
                                hash:
                                  type: string
                                  format: byte
                                partSetHeader:
                                  type: object
                                  properties:
                                    total:
                                      type: integer
                                      format: int64
                                    hash:
                                      type: string
                                      format: byte
                                  title: PartsetHeader
                              title: BlockID
                              description: zero if vote is nil.
                            timestamp:
                              type: string
                              format: date-time
                            validatorAddress:
                              type: string
                              format: byte
                            validatorIndex:
                              type: integer
                              format: int32
                            signature:
                              type: string
                              format: byte
                              description: >-
                                Vote signature by the validator if they
                                participated in consensus for the

                                associated block.
                            extension:
                              type: string
                              format: byte
                              description: >-
                                Vote extension provided by the application. Only
                                valid for precommit

                                messages.
                            extensionSignature:
                              type: string
                              format: byte
                              description: >-
                                Vote extension signature by the validator if
                                they participated in

                                consensus for the associated block.

                                Only valid for precommit messages.
                          description: >-
                            Vote represents a prevote or precommit vote from
                            validators for

                            consensus.
                        totalVotingPower:
                          type: string
                          format: int64
                        validatorPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        DuplicateVoteEvidence contains evidence of a validator
                        signed two conflicting votes.
                    lightClientAttackEvidence:
                      type: object
                      properties:
                        conflictingBlock:
                          type: object
                          properties:
                            signedHeader:
                              type: object
                              properties:
                                header:
                                  type: object
                                  properties:
                                    version:
                                      title: basic block info
                                      type: object
                                      properties:
                                        block:
                                          type: string
                                          format: uint64
                                        app:
                                          type: string
                                          format: uint64
                                      description: >-
                                        Consensus captures the consensus rules
                                        for processing a block in the
                                        blockchain,

                                        including all blockchain data structures
                                        and the rules of the application's

                                        state transition machine.
                                    chainId:
                                      type: string
                                    height:
                                      type: string
                                      format: int64
                                    time:
                                      type: string
                                      format: date-time
                                    lastBlockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    lastCommitHash:
                                      type: string
                                      format: byte
                                      description: >-
                                        commit from validators from the last
                                        block
                                      title: hashes of block data
                                    dataHash:
                                      type: string
                                      format: byte
                                      title: transactions
                                    validatorsHash:
                                      type: string
                                      format: byte
                                      description: validators for the current block
                                      title: >-
                                        hashes from the app output from the prev
                                        block
                                    nextValidatorsHash:
                                      type: string
                                      format: byte
                                      title: validators for the next block
                                    consensusHash:
                                      type: string
                                      format: byte
                                      title: consensus params for current block
                                    appHash:
                                      type: string
                                      format: byte
                                      title: state after txs from the previous block
                                    lastResultsHash:
                                      type: string
                                      format: byte
                                      title: >-
                                        root hash of all results from the txs
                                        from the previous block
                                    evidenceHash:
                                      type: string
                                      format: byte
                                      description: evidence included in the block
                                      title: consensus info
                                    proposerAddress:
                                      type: string
                                      format: byte
                                      title: original proposer of the block
                                  description: >-
                                    Header defines the structure of a block
                                    header.
                                commit:
                                  type: object
                                  properties:
                                    height:
                                      type: string
                                      format: int64
                                    round:
                                      type: integer
                                      format: int32
                                    blockId:
                                      type: object
                                      properties:
                                        hash:
                                          type: string
                                          format: byte
                                        partSetHeader:
                                          type: object
                                          properties:
                                            total:
                                              type: integer
                                              format: int64
                                            hash:
                                              type: string
                                              format: byte
                                          title: PartsetHeader
                                      title: BlockID
                                    signatures:
                                      type: array
                                      items:
                                        type: object
                                        properties:
                                          blockIdFlag:
                                            type: string
                                            enum:
                                              - BLOCK_ID_FLAG_UNKNOWN
                                              - BLOCK_ID_FLAG_ABSENT
                                              - BLOCK_ID_FLAG_COMMIT
                                              - BLOCK_ID_FLAG_NIL
                                            default: BLOCK_ID_FLAG_UNKNOWN
                                            description: >-
                                              - BLOCK_ID_FLAG_UNKNOWN: indicates an
                                              error condition
                                               - BLOCK_ID_FLAG_ABSENT: the vote was not received
                                               - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                                               - BLOCK_ID_FLAG_NIL: voted for nil
                                            title: >-
                                              BlockIdFlag indicates which BlockID the
                                              signature is for
                                          validatorAddress:
                                            type: string
                                            format: byte
                                          timestamp:
                                            type: string
                                            format: date-time
                                          signature:
                                            type: string
                                            format: byte
                                        description: >-
                                          CommitSig is a part of the Vote included
                                          in a Commit.
                                  description: >-
                                    Commit contains the evidence that a block
                                    was committed by a set of validators.
                            validatorSet:
                              type: object
                              properties:
                                validators:
                                  type: array
                                  items:
                                    type: object
                                    properties:
                                      address:
                                        type: string
                                        format: byte
                                      pubKey:
                                        type: object
                                        properties:
                                          ed25519:
                                            type: string
                                            format: byte
                                          secp256k1:
                                            type: string
                                            format: byte
                                        title: >-
                                          PublicKey defines the keys available for
                                          use with Validators
                                      votingPower:
                                        type: string
                                        format: int64
                                      proposerPriority:
                                        type: string
                                        format: int64
                                proposer:
                                  type: object
                                  properties:
                                    address:
                                      type: string
                                      format: byte
                                    pubKey:
                                      type: object
                                      properties:
                                        ed25519:
                                          type: string
                                          format: byte
                                        secp256k1:
                                          type: string
                                          format: byte
                                      title: >-
                                        PublicKey defines the keys available for
                                        use with Validators
                                    votingPower:
                                      type: string
                                      format: int64
                                    proposerPriority:
                                      type: string
                                      format: int64
                                totalVotingPower:
                                  type: string
                                  format: int64
                        commonHeight:
                          type: string
                          format: int64
                        byzantineValidators:
                          type: array
                          items:
                            type: object
                            properties:
                              address:
                                type: string
                                format: byte
                              pubKey:
                                type: object
                                properties:
                                  ed25519:
                                    type: string
                                    format: byte
                                  secp256k1:
                                    type: string
                                    format: byte
                                title: >-
                                  PublicKey defines the keys available for use
                                  with Validators
                              votingPower:
                                type: string
                                format: int64
                              proposerPriority:
                                type: string
                                format: int64
                        totalVotingPower:
                          type: string
                          format: int64
                        timestamp:
                          type: string
                          format: date-time
                      description: >-
                        LightClientAttackEvidence contains evidence of a set of
                        validators attempting to mislead a light client.
          lastCommit:
            type: object
            properties:
              height:
                type: string
                format: int64
              round:
                type: integer
                format: int32
              blockId:
                type: object
                properties:
                  hash:
                    type: string
                    format: byte
                  partSetHeader:
                    type: object
                    properties:
                      total:
                        type: integer
                        format: int64
                      hash:
                        type: string
                        format: byte
                    title: PartsetHeader
                title: BlockID
              signatures:
                type: array
                items:
                  type: object
                  properties:
                    blockIdFlag:
                      type: string
                      enum:
                        - BLOCK_ID_FLAG_UNKNOWN
                        - BLOCK_ID_FLAG_ABSENT
                        - BLOCK_ID_FLAG_COMMIT
                        - BLOCK_ID_FLAG_NIL
                      default: BLOCK_ID_FLAG_UNKNOWN
                      description: |-
                        - BLOCK_ID_FLAG_UNKNOWN: indicates an error condition
                         - BLOCK_ID_FLAG_ABSENT: the vote was not received
                         - BLOCK_ID_FLAG_COMMIT: voted for the block that received the majority
                         - BLOCK_ID_FLAG_NIL: voted for nil
                      title: BlockIdFlag indicates which BlockID the signature is for
                    validatorAddress:
                      type: string
                      format: byte
                    timestamp:
                      type: string
                      format: date-time
                    signature:
                      type: string
                      format: byte
                  description: CommitSig is a part of the Vote included in a Commit.
            description: >-
              Commit contains the evidence that a block was committed by a set
              of validators.
      pagination:
        description: pagination defines a pagination for the response.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
    description: >-
      GetBlockWithTxsResponse is the response type for the
      Service.GetBlockWithTxs

      method.
  cosmos.tx.v1beta1.GetTxResponse:
    type: object
    properties:
      tx:
        $ref: '#/definitions/cosmos.tx.v1beta1.Tx'
        description: tx is the queried transaction.
      txResponse:
        description: tx_response is the queried TxResponses.
        type: object
        properties:
          height:
            type: string
            format: int64
            title: The block height
          txhash:
            type: string
            description: The transaction hash.
          codespace:
            type: string
            title: Namespace for the Code
          code:
            type: integer
            format: int64
            description: Response code.
          data:
            type: string
            description: Result bytes, if any.
          rawLog:
            type: string
            description: |-
              The output of the application's logger (raw string). May be
              non-deterministic.
          logs:
            type: array
            items:
              type: object
              properties:
                msgIndex:
                  type: integer
                  format: int64
                log:
                  type: string
                events:
                  type: array
                  items:
                    type: object
                    properties:
                      type:
                        type: string
                      attributes:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                            value:
                              type: string
                          description: >-
                            Attribute defines an attribute wrapper where the key
                            and value are

                            strings instead of raw bytes.
                    description: >-
                      StringEvent defines en Event object wrapper where all the
                      attributes

                      contain key/value pairs that are strings instead of raw
                      bytes.
                  description: >-
                    Events contains a slice of Event objects that were emitted
                    during some

                    execution.
              description: >-
                ABCIMessageLog defines a structure containing an indexed tx ABCI
                message log.
            description: >-
              The output of the application's logger (typed). May be
              non-deterministic.
          info:
            type: string
            description: Additional information. May be non-deterministic.
          gasWanted:
            type: string
            format: int64
            description: Amount of gas requested for transaction.
          gasUsed:
            type: string
            format: int64
            description: Amount of gas consumed by transaction.
          tx:
            description: The request transaction bytes.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
          timestamp:
            type: string
            description: >-
              Time of the previous block. For heights > 1, it's the weighted
              median of

              the timestamps of the valid votes in the block.LastCommit. For
              height == 1,

              it's genesis time.
          events:
            type: array
            items:
              type: object
              properties:
                type:
                  type: string
                attributes:
                  type: array
                  items:
                    type: object
                    properties:
                      key:
                        type: string
                      value:
                        type: string
                      index:
                        type: boolean
                        title: nondeterministic
                    description: >-
                      EventAttribute is a single key-value pair, associated with
                      an event.
              description: >-
                Event allows application developers to attach additional
                information to

                ResponseFinalizeBlock and ResponseCheckTx.

                Later, transactions may be queried using these events.
            description: >-
              Events defines all the events emitted by processing a transaction.
              Note,

              these events include those emitted by processing all the messages
              and those

              emitted from the ante. Whereas Logs contains the events, with

              additional metadata, emitted only by processing the messages.
    description: GetTxResponse is the response type for the Service.GetTx method.
  cosmos.tx.v1beta1.GetTxsEventResponse:
    type: object
    properties:
      txs:
        type: array
        items:
          type: object
          $ref: '#/definitions/cosmos.tx.v1beta1.Tx'
        description: txs is the list of queried transactions.
      txResponses:
        type: array
        items:
          type: object
          properties:
            height:
              type: string
              format: int64
              title: The block height
            txhash:
              type: string
              description: The transaction hash.
            codespace:
              type: string
              title: Namespace for the Code
            code:
              type: integer
              format: int64
              description: Response code.
            data:
              type: string
              description: Result bytes, if any.
            rawLog:
              type: string
              description: |-
                The output of the application's logger (raw string). May be
                non-deterministic.
            logs:
              type: array
              items:
                type: object
                properties:
                  msgIndex:
                    type: integer
                    format: int64
                  log:
                    type: string
                  events:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                        attributes:
                          type: array
                          items:
                            type: object
                            properties:
                              key:
                                type: string
                              value:
                                type: string
                            description: >-
                              Attribute defines an attribute wrapper where the
                              key and value are

                              strings instead of raw bytes.
                      description: >-
                        StringEvent defines en Event object wrapper where all
                        the attributes

                        contain key/value pairs that are strings instead of raw
                        bytes.
                    description: >-
                      Events contains a slice of Event objects that were emitted
                      during some

                      execution.
                description: >-
                  ABCIMessageLog defines a structure containing an indexed tx
                  ABCI message log.
              description: >-
                The output of the application's logger (typed). May be
                non-deterministic.
            info:
              type: string
              description: Additional information. May be non-deterministic.
            gasWanted:
              type: string
              format: int64
              description: Amount of gas requested for transaction.
            gasUsed:
              type: string
              format: int64
              description: Amount of gas consumed by transaction.
            tx:
              description: The request transaction bytes.
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
            timestamp:
              type: string
              description: >-
                Time of the previous block. For heights > 1, it's the weighted
                median of

                the timestamps of the valid votes in the block.LastCommit. For
                height == 1,

                it's genesis time.
            events:
              type: array
              items:
                type: object
                properties:
                  type:
                    type: string
                  attributes:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                        value:
                          type: string
                        index:
                          type: boolean
                          title: nondeterministic
                      description: >-
                        EventAttribute is a single key-value pair, associated
                        with an event.
                description: >-
                  Event allows application developers to attach additional
                  information to

                  ResponseFinalizeBlock and ResponseCheckTx.

                  Later, transactions may be queried using these events.
              description: >-
                Events defines all the events emitted by processing a
                transaction. Note,

                these events include those emitted by processing all the
                messages and those

                emitted from the ante. Whereas Logs contains the events, with

                additional metadata, emitted only by processing the messages.
          description: >-
            TxResponse defines a structure containing relevant tx data and
            metadata. The

            tags are stringified and the log is JSON decoded.
        description: tx_responses is the list of queried TxResponses.
      pagination:
        description: |-
          pagination defines a pagination for the response.
          Deprecated post v0.46.x: use total instead.
        type: object
        properties:
          nextKey:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
      total:
        type: string
        format: uint64
        title: total is total number of results available
    description: |-
      GetTxsEventResponse is the response type for the Service.TxsByEvents
      RPC method.
  cosmos.tx.v1beta1.ModeInfo:
    type: object
    properties:
      single:
        title: single represents a single signer
        type: object
        properties:
          mode:
            title: mode is the signing mode of the single signer
            type: string
            enum:
              - SIGN_MODE_UNSPECIFIED
              - SIGN_MODE_DIRECT
              - SIGN_MODE_TEXTUAL
              - SIGN_MODE_DIRECT_AUX
              - SIGN_MODE_LEGACY_AMINO_JSON
              - SIGN_MODE_EIP_191
            default: SIGN_MODE_UNSPECIFIED
            description: >-
              SignMode represents a signing mode with its own security
              guarantees.


              This enum should be considered a registry of all known sign modes

              in the Cosmos ecosystem. Apps are not expected to support all
              known

              sign modes. Apps that would like to support custom  sign modes are

              encouraged to open a small PR against this file to add a new case

              to this SignMode enum describing their sign mode so that different

              apps have a consistent version of this enum.

               - SIGN_MODE_UNSPECIFIED: SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
              rejected.
               - SIGN_MODE_DIRECT: SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
              verified with raw bytes from Tx.
               - SIGN_MODE_TEXTUAL: SIGN_MODE_TEXTUAL is a future signing mode that will verify some
              human-readable textual representation on top of the binary
              representation

              from SIGN_MODE_DIRECT.


              Since: cosmos-sdk 0.50
               - SIGN_MODE_DIRECT_AUX: SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
              SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode
              does not

              require signers signing over other signers' `signer_info`.


              Since: cosmos-sdk 0.46
               - SIGN_MODE_LEGACY_AMINO_JSON: SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
              Amino JSON and will be removed in the future.
               - SIGN_MODE_EIP_191: SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
              SDK. Ref: https://eips.ethereum.org/EIPS/eip-191


              Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum
              variant,

              but is not implemented on the SDK by default. To enable EIP-191,
              you need

              to pass a custom `TxConfig` that has an implementation of

              `SignModeHandler` for EIP-191. The SDK may decide to fully support

              EIP-191 in the future.


              Since: cosmos-sdk 0.45.2
      multi:
        $ref: '#/definitions/cosmos.tx.v1beta1.ModeInfo.Multi'
        title: multi represents a nested multisig signer
    description: ModeInfo describes the signing mode of a single or nested multisig signer.
  cosmos.tx.v1beta1.ModeInfo.Multi:
    type: object
    properties:
      bitarray:
        title: bitarray specifies which keys within the multisig are signing
        type: object
        properties:
          extraBitsStored:
            type: integer
            format: int64
          elems:
            type: string
            format: byte
        description: >-
          CompactBitArray is an implementation of a space efficient bit array.

          This is used to ensure that the encoded data takes up a minimal amount
          of

          space after proto encoding.

          This is not thread safe, and is not intended for concurrent usage.
      modeInfos:
        type: array
        items:
          type: object
          $ref: '#/definitions/cosmos.tx.v1beta1.ModeInfo'
        title: |-
          mode_infos is the corresponding modes of the signers of the multisig
          which could include nested multisig public keys
    title: Multi is the mode info for a multisig public key
  cosmos.tx.v1beta1.ModeInfo.Single:
    type: object
    properties:
      mode:
        title: mode is the signing mode of the single signer
        type: string
        enum:
          - SIGN_MODE_UNSPECIFIED
          - SIGN_MODE_DIRECT
          - SIGN_MODE_TEXTUAL
          - SIGN_MODE_DIRECT_AUX
          - SIGN_MODE_LEGACY_AMINO_JSON
          - SIGN_MODE_EIP_191
        default: SIGN_MODE_UNSPECIFIED
        description: >-
          SignMode represents a signing mode with its own security guarantees.


          This enum should be considered a registry of all known sign modes

          in the Cosmos ecosystem. Apps are not expected to support all known

          sign modes. Apps that would like to support custom  sign modes are

          encouraged to open a small PR against this file to add a new case

          to this SignMode enum describing their sign mode so that different

          apps have a consistent version of this enum.

           - SIGN_MODE_UNSPECIFIED: SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
          rejected.
           - SIGN_MODE_DIRECT: SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
          verified with raw bytes from Tx.
           - SIGN_MODE_TEXTUAL: SIGN_MODE_TEXTUAL is a future signing mode that will verify some
          human-readable textual representation on top of the binary
          representation

          from SIGN_MODE_DIRECT.


          Since: cosmos-sdk 0.50
           - SIGN_MODE_DIRECT_AUX: SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
          SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does
          not

          require signers signing over other signers' `signer_info`.


          Since: cosmos-sdk 0.46
           - SIGN_MODE_LEGACY_AMINO_JSON: SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
          Amino JSON and will be removed in the future.
           - SIGN_MODE_EIP_191: SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
          SDK. Ref: https://eips.ethereum.org/EIPS/eip-191


          Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,

          but is not implemented on the SDK by default. To enable EIP-191, you
          need

          to pass a custom `TxConfig` that has an implementation of

          `SignModeHandler` for EIP-191. The SDK may decide to fully support

          EIP-191 in the future.


          Since: cosmos-sdk 0.45.2
    title: |-
      Single is the mode info for a single signer. It is structured as a message
      to allow for additional fields such as locale for SIGN_MODE_TEXTUAL in the
      future
  cosmos.tx.v1beta1.OrderBy:
    type: string
    enum:
      - ORDER_BY_UNSPECIFIED
      - ORDER_BY_ASC
      - ORDER_BY_DESC
    default: ORDER_BY_UNSPECIFIED
    description: >-
      - ORDER_BY_UNSPECIFIED: ORDER_BY_UNSPECIFIED specifies an unknown sorting
      order. OrderBy defaults

      to ASC in this case.
       - ORDER_BY_ASC: ORDER_BY_ASC defines ascending order
       - ORDER_BY_DESC: ORDER_BY_DESC defines descending order
    title: OrderBy defines the sorting order
  cosmos.tx.v1beta1.SignerInfo:
    type: object
    properties:
      publicKey:
        description: >-
          public_key is the public key of the signer. It is optional for
          accounts

          that already exist in state. If unset, the verifier can use the
          required \

          signer address for this position and lookup the public key.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
      modeInfo:
        $ref: '#/definitions/cosmos.tx.v1beta1.ModeInfo'
        title: |-
          mode_info describes the signing mode of the signer and is a nested
          structure to support nested multisig pubkey's
      sequence:
        type: string
        format: uint64
        description: >-
          sequence is the sequence of the account, which describes the

          number of committed transactions signed by a given address. It is used
          to

          prevent replay attacks.
    description: |-
      SignerInfo describes the public key and signing mode of a single top-level
      signer.
  cosmos.tx.v1beta1.SimulateRequest:
    type: object
    properties:
      tx:
        $ref: '#/definitions/cosmos.tx.v1beta1.Tx'
        description: |-
          tx is the transaction to simulate.
          Deprecated. Send raw tx bytes instead.
      txBytes:
        type: string
        format: byte
        description: tx_bytes is the raw transaction.
    description: |-
      SimulateRequest is the request type for the Service.Simulate
      RPC method.
  cosmos.tx.v1beta1.SimulateResponse:
    type: object
    properties:
      gasInfo:
        description: gas_info is the information about gas used in the simulation.
        type: object
        properties:
          gasWanted:
            type: string
            format: uint64
            description: >-
              GasWanted is the maximum units of work we allow this tx to
              perform.
          gasUsed:
            type: string
            format: uint64
            description: GasUsed is the amount of gas actually consumed.
      result:
        description: result is the result of the simulation.
        type: object
        properties:
          data:
            type: string
            format: byte
            description: >-
              Data is any data returned from message or handler execution. It
              MUST be

              length prefixed in order to separate data from multiple message
              executions.

              Deprecated. This field is still populated, but prefer msg_response
              instead

              because it also contains the Msg response typeURL.
          log:
            type: string
            description: >-
              Log contains the log information from message or handler
              execution.
          events:
            type: array
            items:
              type: object
              properties:
                type:
                  type: string
                attributes:
                  type: array
                  items:
                    type: object
                    properties:
                      key:
                        type: string
                      value:
                        type: string
                      index:
                        type: boolean
                        title: nondeterministic
                    description: >-
                      EventAttribute is a single key-value pair, associated with
                      an event.
              description: >-
                Event allows application developers to attach additional
                information to

                ResponseFinalizeBlock and ResponseCheckTx.

                Later, transactions may be queried using these events.
            description: >-
              Events contains a slice of Event objects that were emitted during
              message

              or handler execution.
          msgResponses:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            description: >-
              msg_responses contains the Msg handler responses type packed in
              Anys.
    description: |-
      SimulateResponse is the response type for the
      Service.SimulateRPC method.
  cosmos.tx.v1beta1.Tip:
    type: object
    properties:
      amount:
        type: array
        items:
          type: object
          properties:
            denom:
              type: string
            amount:
              type: string
          description: |-
            Coin defines a token with a denomination and an amount.

            NOTE: The amount field is an Int which implements the custom method
            signatures required by gogoproto.
        title: amount is the amount of the tip
      tipper:
        type: string
        title: tipper is the address of the account paying for the tip
    description: Tip is the tip used for meta-transactions.
  cosmos.tx.v1beta1.Tx:
    type: object
    properties:
      body:
        title: body is the processable content of the transaction
        type: object
        properties:
          messages:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            description: >-
              messages is a list of messages to be executed. The required
              signers of

              those messages define the number and order of elements in
              AuthInfo's

              signer_infos and Tx's signatures. Each required signer address is
              added to

              the list only the first time it occurs.

              By convention, the first required signer (usually from the first
              message)

              is referred to as the primary signer and pays the fee for the
              whole

              transaction.
          memo:
            type: string
            description: >-
              memo is any arbitrary note/comment to be added to the transaction.

              WARNING: in clients, any publicly exposed text should not be
              called memo,

              but should be called `note` instead (see

              https://github.com/cosmos/cosmos-sdk/issues/9122).
          timeoutHeight:
            type: string
            format: uint64
            description: >-
              timeout_height is the block height after which this transaction
              will not

              be processed by the chain.
          unordered:
            type: boolean
            description: >-
              unordered, when set to true, indicates that the transaction
              signer(s)

              intend for the transaction to be evaluated and executed in an
              un-ordered

              fashion. Specifically, the account's nonce will NOT be checked or

              incremented, which allows for fire-and-forget as well as
              concurrent

              transaction execution.


              Note, when set to true, the existing 'timeout_timestamp' value
              must

              be set and will be used to correspond to a timestamp in which the
              transaction is deemed

              valid.


              When true, the sequence value MUST be 0, and any transaction with
              unordered=true and a non-zero sequence value will

              be rejected.

              External services that make assumptions about sequence values may
              need to be updated because of this.
          timeoutTimestamp:
            type: string
            format: date-time
            description: >-
              timeout_timestamp is the block time after which this transaction
              will not

              be processed by the chain.


              Note, if unordered=true this value MUST be set

              and will act as a short-lived TTL in which the transaction is
              deemed valid

              and kept in memory to prevent duplicates.
          extensionOptions:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            title: >-
              extension_options are arbitrary options that can be added by
              chains

              when the default options are not sufficient. If any of these are
              present

              and can't be handled, the transaction will be rejected
          nonCriticalExtensionOptions:
            type: array
            items:
              type: object
              properties:
                '@type':
                  type: string
                  description: >-
                    A URL/resource name that uniquely identifies the type of the
                    serialized

                    protocol buffer message. This string must contain at least

                    one "/" character. The last segment of the URL's path must
                    represent

                    the fully qualified name of the type (as in

                    `path/google.protobuf.Duration`). The name should be in a
                    canonical form

                    (e.g., leading "." is not accepted).


                    In practice, teams usually precompile into the binary all
                    types that they

                    expect it to use in the context of Any. However, for URLs
                    which use the

                    scheme `http`, `https`, or no scheme, one can optionally set
                    up a type

                    server that maps type URLs to message definitions as
                    follows:


                    * If no scheme is provided, `https` is assumed.

                    * An HTTP GET on the URL must yield a
                    [google.protobuf.Type][]
                      value in binary format, or produce an error.
                    * Applications are allowed to cache lookup results based on
                    the
                      URL, or have them precompiled into a binary to avoid any
                      lookup. Therefore, binary compatibility needs to be preserved
                      on changes to types. (Use versioned type names to manage
                      breaking changes.)

                    Note: this functionality is not currently available in the
                    official

                    protobuf release, and it is not used for type URLs beginning
                    with

                    type.googleapis.com. As of May 2023, there are no widely
                    used type server

                    implementations and no plans to implement one.


                    Schemes other than `http`, `https` (or the empty scheme)
                    might be

                    used with implementation specific semantics.
              additionalProperties: {}
              description: >-
                `Any` contains an arbitrary serialized protocol buffer message
                along with a

                URL that describes the type of the serialized message.


                Protobuf library provides support to pack/unpack Any values in
                the form

                of utility functions or additional generated methods of the Any
                type.


                Example 1: Pack and unpack a message in C++.

                    Foo foo = ...;
                    Any any;
                    any.PackFrom(foo);
                    ...
                    if (any.UnpackTo(&foo)) {
                      ...
                    }

                Example 2: Pack and unpack a message in Java.

                    Foo foo = ...;
                    Any any = Any.pack(foo);
                    ...
                    if (any.is(Foo.class)) {
                      foo = any.unpack(Foo.class);
                    }
                    // or ...
                    if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                      foo = any.unpack(Foo.getDefaultInstance());
                    }

                 Example 3: Pack and unpack a message in Python.

                    foo = Foo(...)
                    any = Any()
                    any.Pack(foo)
                    ...
                    if any.Is(Foo.DESCRIPTOR):
                      any.Unpack(foo)
                      ...

                 Example 4: Pack and unpack a message in Go

                     foo := &pb.Foo{...}
                     any, err := anypb.New(foo)
                     if err != nil {
                       ...
                     }
                     ...
                     foo := &pb.Foo{}
                     if err := any.UnmarshalTo(foo); err != nil {
                       ...
                     }

                The pack methods provided by protobuf library will by default
                use

                'type.googleapis.com/full.type.name' as the type URL and the
                unpack

                methods only use the fully qualified type name after the last
                '/'

                in the type URL, for example "foo.bar.com/x/y.z" will yield type

                name "y.z".


                JSON

                ====

                The JSON representation of an `Any` value uses the regular

                representation of the deserialized, embedded message, with an

                additional field `@type` which contains the type URL. Example:

                    package google.profile;
                    message Person {
                      string first_name = 1;
                      string last_name = 2;
                    }

                    {
                      "@type": "type.googleapis.com/google.profile.Person",
                      "firstName": <string>,
                      "lastName": <string>
                    }

                If the embedded message type is well-known and has a custom JSON

                representation, that representation will be embedded adding a
                field

                `value` which holds the custom JSON in addition to the `@type`

                field. Example (for message [google.protobuf.Duration][]):

                    {
                      "@type": "type.googleapis.com/google.protobuf.Duration",
                      "value": "1.212s"
                    }
            title: >-
              extension_options are arbitrary options that can be added by
              chains

              when the default options are not sufficient. If any of these are
              present

              and can't be handled, they will be ignored
        description: TxBody is the body of a transaction that all signers sign over.
      authInfo:
        $ref: '#/definitions/cosmos.tx.v1beta1.AuthInfo'
        title: |-
          auth_info is the authorization related content of the transaction,
          specifically signers, signer modes and fee
      signatures:
        type: array
        items:
          type: string
          format: byte
        description: >-
          signatures is a list of signatures that matches the length and order
          of

          AuthInfo's signer_infos to allow connecting signature meta information
          like

          public key and signing mode by position.
    description: Tx is the standard type used for broadcasting transactions.
  cosmos.tx.v1beta1.TxBody:
    type: object
    properties:
      messages:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        description: >-
          messages is a list of messages to be executed. The required signers of

          those messages define the number and order of elements in AuthInfo's

          signer_infos and Tx's signatures. Each required signer address is
          added to

          the list only the first time it occurs.

          By convention, the first required signer (usually from the first
          message)

          is referred to as the primary signer and pays the fee for the whole

          transaction.
      memo:
        type: string
        description: >-
          memo is any arbitrary note/comment to be added to the transaction.

          WARNING: in clients, any publicly exposed text should not be called
          memo,

          but should be called `note` instead (see

          https://github.com/cosmos/cosmos-sdk/issues/9122).
      timeoutHeight:
        type: string
        format: uint64
        description: >-
          timeout_height is the block height after which this transaction will
          not

          be processed by the chain.
      unordered:
        type: boolean
        description: >-
          unordered, when set to true, indicates that the transaction signer(s)

          intend for the transaction to be evaluated and executed in an
          un-ordered

          fashion. Specifically, the account's nonce will NOT be checked or

          incremented, which allows for fire-and-forget as well as concurrent

          transaction execution.


          Note, when set to true, the existing 'timeout_timestamp' value must

          be set and will be used to correspond to a timestamp in which the
          transaction is deemed

          valid.


          When true, the sequence value MUST be 0, and any transaction with
          unordered=true and a non-zero sequence value will

          be rejected.

          External services that make assumptions about sequence values may need
          to be updated because of this.
      timeoutTimestamp:
        type: string
        format: date-time
        description: >-
          timeout_timestamp is the block time after which this transaction will
          not

          be processed by the chain.


          Note, if unordered=true this value MUST be set

          and will act as a short-lived TTL in which the transaction is deemed
          valid

          and kept in memory to prevent duplicates.
      extensionOptions:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        title: >-
          extension_options are arbitrary options that can be added by chains

          when the default options are not sufficient. If any of these are
          present

          and can't be handled, the transaction will be rejected
      nonCriticalExtensionOptions:
        type: array
        items:
          type: object
          properties:
            '@type':
              type: string
              description: >-
                A URL/resource name that uniquely identifies the type of the
                serialized

                protocol buffer message. This string must contain at least

                one "/" character. The last segment of the URL's path must
                represent

                the fully qualified name of the type (as in

                `path/google.protobuf.Duration`). The name should be in a
                canonical form

                (e.g., leading "." is not accepted).


                In practice, teams usually precompile into the binary all types
                that they

                expect it to use in the context of Any. However, for URLs which
                use the

                scheme `http`, `https`, or no scheme, one can optionally set up
                a type

                server that maps type URLs to message definitions as follows:


                * If no scheme is provided, `https` is assumed.

                * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                  value in binary format, or produce an error.
                * Applications are allowed to cache lookup results based on the
                  URL, or have them precompiled into a binary to avoid any
                  lookup. Therefore, binary compatibility needs to be preserved
                  on changes to types. (Use versioned type names to manage
                  breaking changes.)

                Note: this functionality is not currently available in the
                official

                protobuf release, and it is not used for type URLs beginning
                with

                type.googleapis.com. As of May 2023, there are no widely used
                type server

                implementations and no plans to implement one.


                Schemes other than `http`, `https` (or the empty scheme) might
                be

                used with implementation specific semantics.
          additionalProperties: {}
          description: >-
            `Any` contains an arbitrary serialized protocol buffer message along
            with a

            URL that describes the type of the serialized message.


            Protobuf library provides support to pack/unpack Any values in the
            form

            of utility functions or additional generated methods of the Any
            type.


            Example 1: Pack and unpack a message in C++.

                Foo foo = ...;
                Any any;
                any.PackFrom(foo);
                ...
                if (any.UnpackTo(&foo)) {
                  ...
                }

            Example 2: Pack and unpack a message in Java.

                Foo foo = ...;
                Any any = Any.pack(foo);
                ...
                if (any.is(Foo.class)) {
                  foo = any.unpack(Foo.class);
                }
                // or ...
                if (any.isSameTypeAs(Foo.getDefaultInstance())) {
                  foo = any.unpack(Foo.getDefaultInstance());
                }

             Example 3: Pack and unpack a message in Python.

                foo = Foo(...)
                any = Any()
                any.Pack(foo)
                ...
                if any.Is(Foo.DESCRIPTOR):
                  any.Unpack(foo)
                  ...

             Example 4: Pack and unpack a message in Go

                 foo := &pb.Foo{...}
                 any, err := anypb.New(foo)
                 if err != nil {
                   ...
                 }
                 ...
                 foo := &pb.Foo{}
                 if err := any.UnmarshalTo(foo); err != nil {
                   ...
                 }

            The pack methods provided by protobuf library will by default use

            'type.googleapis.com/full.type.name' as the type URL and the unpack

            methods only use the fully qualified type name after the last '/'

            in the type URL, for example "foo.bar.com/x/y.z" will yield type

            name "y.z".


            JSON

            ====

            The JSON representation of an `Any` value uses the regular

            representation of the deserialized, embedded message, with an

            additional field `@type` which contains the type URL. Example:

                package google.profile;
                message Person {
                  string first_name = 1;
                  string last_name = 2;
                }

                {
                  "@type": "type.googleapis.com/google.profile.Person",
                  "firstName": <string>,
                  "lastName": <string>
                }

            If the embedded message type is well-known and has a custom JSON

            representation, that representation will be embedded adding a field

            `value` which holds the custom JSON in addition to the `@type`

            field. Example (for message [google.protobuf.Duration][]):

                {
                  "@type": "type.googleapis.com/google.protobuf.Duration",
                  "value": "1.212s"
                }
        title: >-
          extension_options are arbitrary options that can be added by chains

          when the default options are not sufficient. If any of these are
          present

          and can't be handled, they will be ignored
    description: TxBody is the body of a transaction that all signers sign over.
  cosmos.tx.v1beta1.TxDecodeAminoRequest:
    type: object
    properties:
      aminoBinary:
        type: string
        format: byte
    description: |-
      TxDecodeAminoRequest is the request type for the Service.TxDecodeAmino
      RPC method.
  cosmos.tx.v1beta1.TxDecodeAminoResponse:
    type: object
    properties:
      aminoJson:
        type: string
    description: |-
      TxDecodeAminoResponse is the response type for the Service.TxDecodeAmino
      RPC method.
  cosmos.tx.v1beta1.TxDecodeRequest:
    type: object
    properties:
      txBytes:
        type: string
        format: byte
        description: tx_bytes is the raw transaction.
    description: |-
      TxDecodeRequest is the request type for the Service.TxDecode
      RPC method.
  cosmos.tx.v1beta1.TxDecodeResponse:
    type: object
    properties:
      tx:
        $ref: '#/definitions/cosmos.tx.v1beta1.Tx'
        description: tx is the decoded transaction.
    description: |-
      TxDecodeResponse is the response type for the
      Service.TxDecode method.
  cosmos.tx.v1beta1.TxEncodeAminoRequest:
    type: object
    properties:
      aminoJson:
        type: string
    description: |-
      TxEncodeAminoRequest is the request type for the Service.TxEncodeAmino
      RPC method.
  cosmos.tx.v1beta1.TxEncodeAminoResponse:
    type: object
    properties:
      aminoBinary:
        type: string
        format: byte
    description: |-
      TxEncodeAminoResponse is the response type for the Service.TxEncodeAmino
      RPC method.
  cosmos.tx.v1beta1.TxEncodeRequest:
    type: object
    properties:
      tx:
        $ref: '#/definitions/cosmos.tx.v1beta1.Tx'
        description: tx is the transaction to encode.
    description: |-
      TxEncodeRequest is the request type for the Service.TxEncode
      RPC method.
  cosmos.tx.v1beta1.TxEncodeResponse:
    type: object
    properties:
      txBytes:
        type: string
        format: byte
        description: tx_bytes is the encoded transaction bytes.
    description: |-
      TxEncodeResponse is the response type for the
      Service.TxEncode method.
  tendermint.abci.Event:
    type: object
    properties:
      type:
        type: string
      attributes:
        type: array
        items:
          type: object
          properties:
            key:
              type: string
            value:
              type: string
            index:
              type: boolean
              title: nondeterministic
          description: EventAttribute is a single key-value pair, associated with an event.
    description: |-
      Event allows application developers to attach additional information to
      ResponseFinalizeBlock and ResponseCheckTx.
      Later, transactions may be queried using these events.
  tendermint.abci.EventAttribute:
    type: object
    properties:
      key:
        type: string
      value:
        type: string
      index:
        type: boolean
        title: nondeterministic
    description: EventAttribute is a single key-value pair, associated with an event.
  cosmos.upgrade.v1beta1.ModuleVersion:
    type: object
    properties:
      name:
        type: string
        title: name of the app module
      version:
        type: string
        format: uint64
        title: consensus version of the app module
    description: ModuleVersion specifies a module and its consensus version.
  cosmos.upgrade.v1beta1.Plan:
    type: object
    properties:
      name:
        type: string
        description: >-
          Sets the name for the upgrade. This name will be used by the upgraded

          version of the software to apply any special "on-upgrade" commands
          during

          the first BeginBlock method after the upgrade is applied. It is also
          used

          to detect whether a software version can handle a given upgrade. If no

          upgrade handler with this name has been set in the software, it will
          be

          assumed that the software is out-of-date when the upgrade Time or
          Height is

          reached and the software will exit.
      time:
        type: string
        format: date-time
        description: >-
          Deprecated: Time based upgrades have been deprecated. Time based
          upgrade logic

          has been removed from the SDK.

          If this field is not empty, an error will be thrown.
      height:
        type: string
        format: int64
        description: The height at which the upgrade must be performed.
      info:
        type: string
        title: |-
          Any application specific upgrade info to be included on-chain
          such as a git commit that validators could automatically upgrade to
      upgradedClientState:
        description: >-
          Deprecated: UpgradedClientState field has been deprecated. IBC upgrade
          logic has been

          moved to the IBC module in the sub module 02-client.

          If this field is not empty, an error will be thrown.
        type: object
        properties:
          '@type':
            type: string
            description: >-
              A URL/resource name that uniquely identifies the type of the
              serialized

              protocol buffer message. This string must contain at least

              one "/" character. The last segment of the URL's path must
              represent

              the fully qualified name of the type (as in

              `path/google.protobuf.Duration`). The name should be in a
              canonical form

              (e.g., leading "." is not accepted).


              In practice, teams usually precompile into the binary all types
              that they

              expect it to use in the context of Any. However, for URLs which
              use the

              scheme `http`, `https`, or no scheme, one can optionally set up a
              type

              server that maps type URLs to message definitions as follows:


              * If no scheme is provided, `https` is assumed.

              * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                value in binary format, or produce an error.
              * Applications are allowed to cache lookup results based on the
                URL, or have them precompiled into a binary to avoid any
                lookup. Therefore, binary compatibility needs to be preserved
                on changes to types. (Use versioned type names to manage
                breaking changes.)

              Note: this functionality is not currently available in the
              official

              protobuf release, and it is not used for type URLs beginning with

              type.googleapis.com. As of May 2023, there are no widely used type
              server

              implementations and no plans to implement one.


              Schemes other than `http`, `https` (or the empty scheme) might be

              used with implementation specific semantics.
        additionalProperties: {}
    description: >-
      Plan specifies information about a planned upgrade and when it should
      occur.
  cosmos.upgrade.v1beta1.QueryAppliedPlanResponse:
    type: object
    properties:
      height:
        type: string
        format: int64
        description: height is the block height at which the plan was applied.
    description: >-
      QueryAppliedPlanResponse is the response type for the Query/AppliedPlan
      RPC

      method.
  cosmos.upgrade.v1beta1.QueryAuthorityResponse:
    type: object
    properties:
      address:
        type: string
    title: QueryAuthorityResponse is the response type for Query/Authority
  cosmos.upgrade.v1beta1.QueryCurrentPlanResponse:
    type: object
    properties:
      plan:
        description: plan is the current upgrade plan.
        type: object
        properties:
          name:
            type: string
            description: >-
              Sets the name for the upgrade. This name will be used by the
              upgraded

              version of the software to apply any special "on-upgrade" commands
              during

              the first BeginBlock method after the upgrade is applied. It is
              also used

              to detect whether a software version can handle a given upgrade.
              If no

              upgrade handler with this name has been set in the software, it
              will be

              assumed that the software is out-of-date when the upgrade Time or
              Height is

              reached and the software will exit.
          time:
            type: string
            format: date-time
            description: >-
              Deprecated: Time based upgrades have been deprecated. Time based
              upgrade logic

              has been removed from the SDK.

              If this field is not empty, an error will be thrown.
          height:
            type: string
            format: int64
            description: The height at which the upgrade must be performed.
          info:
            type: string
            title: >-
              Any application specific upgrade info to be included on-chain

              such as a git commit that validators could automatically upgrade
              to
          upgradedClientState:
            description: >-
              Deprecated: UpgradedClientState field has been deprecated. IBC
              upgrade logic has been

              moved to the IBC module in the sub module 02-client.

              If this field is not empty, an error will be thrown.
            type: object
            properties:
              '@type':
                type: string
                description: >-
                  A URL/resource name that uniquely identifies the type of the
                  serialized

                  protocol buffer message. This string must contain at least

                  one "/" character. The last segment of the URL's path must
                  represent

                  the fully qualified name of the type (as in

                  `path/google.protobuf.Duration`). The name should be in a
                  canonical form

                  (e.g., leading "." is not accepted).


                  In practice, teams usually precompile into the binary all
                  types that they

                  expect it to use in the context of Any. However, for URLs
                  which use the

                  scheme `http`, `https`, or no scheme, one can optionally set
                  up a type

                  server that maps type URLs to message definitions as follows:


                  * If no scheme is provided, `https` is assumed.

                  * An HTTP GET on the URL must yield a [google.protobuf.Type][]
                    value in binary format, or produce an error.
                  * Applications are allowed to cache lookup results based on
                  the
                    URL, or have them precompiled into a binary to avoid any
                    lookup. Therefore, binary compatibility needs to be preserved
                    on changes to types. (Use versioned type names to manage
                    breaking changes.)

                  Note: this functionality is not currently available in the
                  official

                  protobuf release, and it is not used for type URLs beginning
                  with

                  type.googleapis.com. As of May 2023, there are no widely used
                  type server

                  implementations and no plans to implement one.


                  Schemes other than `http`, `https` (or the empty scheme) might
                  be

                  used with implementation specific semantics.
            additionalProperties: {}
    description: >-
      QueryCurrentPlanResponse is the response type for the Query/CurrentPlan
      RPC

      method.
  cosmos.upgrade.v1beta1.QueryModuleVersionsResponse:
    type: object
    properties:
      moduleVersions:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              title: name of the app module
            version:
              type: string
              format: uint64
              title: consensus version of the app module
          description: ModuleVersion specifies a module and its consensus version.
        description: >-
          module_versions is a list of module names with their consensus
          versions.
    description: >-
      QueryModuleVersionsResponse is the response type for the
      Query/ModuleVersions

      RPC method.
  cosmos.upgrade.v1beta1.QueryUpgradedConsensusStateResponse:
    type: object
    properties:
      upgradedConsensusState:
        type: string
        format: byte
    description: >-
      QueryUpgradedConsensusStateResponse is the response type for the
      Query/UpgradedConsensusState

      RPC method.
